# Codex 接手说明

这个文件是后续 Codex 智能体进入本项目时必须先读的固定入口。用户已经明确要求：除本次单独授权的根目录 `README.en.md` 外，所有文档使用中文描写；代码只添加必要注释；所有计划和开发历史要及时写入文档，方便新智能体复用上下文并追溯决策。

## Goal mode execution policy

- 已批准的编号计划视为封闭范围。
- 每项达到其最小验收条件后，必须立即转向下一项。
- 不影响当前验收的重构、精修和新发现，只记录，不实施。
- 不得自行扩展完成标准。
- 全部必需任务完成后必须停止，不得创造后续工作。
- 遇到范围歧义、需要产品决策或重复失败时，停止并询问用户。

## 精简分阶段交付

- 跨多个子系统或已经出现上下文膨胀的长任务使用 `$run-lean-staged-delivery`；明确要求四级流程时再叠加 `$four-officials-flow`。四级职责按**整个权威编号**履行一次，不得下沉到每个内部阶段、首败、夹具修正或机械改字。
- 主线程默认是唯一执行者，直接负责已冻结设计下的调查、编码、测试夹具、首败诊断和窄修。只有任务起点的重大架构调查、集成冻结后的独立代码复核、最终真实入口验收需要委派；不得为正则、断言、字段名、测试时序或三文件以内的局部修正切换角色。
- 完整四级默认由“独立中书舍人一次 + 主线程尚书郎 + 独立给事中一次 + 独立观察使一次”组成。每个权威编号最多启动三个委派角色，最多再使用两个 blocker-only 复查回合；超过该预算必须停止并取得用户授权。子智能体不得继续派生智能体，同一时刻只允许一个委派角色运行。
- 一个权威编号可在同一任务分支拆成内部阶段；每阶段只冻结目标、最小验收、非目标、唯一写者和文件边界，由主线程完成廉价门与 checkpoint。内部阶段不重复完整调查、复核和观察使，也不得提前宣称权威任务完成。
- 委派默认使用 `fork_turns: "none"`，只传权威条目、冻结 diff、验收和 artifact 路径；长日志、trace、截图和矩阵留在本地产物。每个委派角色必须交付可核验的调查结论、`ACCEPT / BLOCK` 或真实验收结果，不能只交付过程报告。
- 新建专用浏览器夹具原则上不得超过 `500` 行；预计超过时必须先复用或抽取共享 harness。单阶段测试 / 工具新增行数不得超过产品新增行数，确有必要时须在动手前写明理由。不得继续扩写已有超大单体工具来绕过产品问题。
- 测试按静态门、专项 Node、小数据真实入口、集成冻结后的代表性大数据、最终全量终验逐级升级。每个阶段默认各跑一次静态门和专项门；浏览器门首次失败后只允许一次最窄诊断和一次目标复验。夹具连续两次失败或同一阻断再次出现，必须冻结并重新审视方案，不得继续“补夹具—跑全门”循环。
- 每 `30` 分钟或每个阶段边界必须汇报产品文件 / 行数、测试工具文件 / 行数、完成阶段比例、真实通过的命令和委派等待时间；若没有形成产品、测试或验收证据，必须明确报告本周期产出为 `0`。阶段接受后立即建立 checkpoint。
- 不影响当前阶段最小验收的新发现只记录，不顺手扩修；不得以节流为由取消必需的真实浏览器、旧数据、回滚或视觉验收。当前权威项的内部阶段与恢复顺序以 `docs/current-plan.md` 为准。

## 项目目标

当前项目在 `source/Fantasy-Map-Generator` 中放置原 Fantasy Map Generator 源码。我们的目标不是修改原项目，而是基于原项目的功能、数据结构和视觉表现，复刻一个功能相似但由 WebGL 实现的独立地图生成器。

核心原则：

- `source/` 是参考实现、行为对照和性能基线来源，不是改造目标。
- 禁止修改 `source/` 原项目代码；只有为了安装依赖和运行参考项目而产生的锁文件例外，例如 `pnpm-lock.yaml`。
- 新增项目代码、工具、原型和文档默认放在当前项目根目录下，不要放进 `source/`。
- 可以阅读、运行、profile 和浏览器观察 `source/`，用来确认原项目行为、数据模型和视觉表现。

## 文档约定

- 所有手写文档必须使用中文；用户本次明确要求的根目录 `README.en.md` 是唯一英文例外。
- 计划、阶段进度、重要决策、风险和执行结果都要及时写入 `docs/`。
- 如果新增脚本会生成 Markdown 报告，生成内容也应为中文。
- 关键文档：
  - `docs/README.md`：docs 目录结构和文档放置规则。
  - `graphics-reimplementation-plan.md`：早期图形化重实现分析，已被新复刻计划取代，仅作参考。
  - `docs/plans/gl-reimplementation-acceptance-plan.md`：独立 WebGL 地图生成器复刻可验收计划。
  - `docs/current-plan.md`：唯一权威任务清单，只保留未完成、进行中或暂缓任务及最小验收口径；其它文档不得另建当前待办。
  - `docs/task-archives/README.md`：已完成权威任务的时间分卷与编号索引；接手执行默认不读，仅在追溯历史决策时按需检索。
  - `docs/development-log.md`：开发历史与决策记录。
  - `docs/performance/performance-baseline.md`：第 0 里程碑 profiling 工具说明。
  - `docs/milestones/milestone-1-webgl-prototype.md`：第 1 里程碑 WebGL cells 原型说明。
  - `docs/performance/webgl-svg-performance-comparison.md`：WebGL 原型与 SVG 基线性能对照。
  - `docs/audits/source-generation-audit-and-rectification-plan.md`：source 生成算法重新审查和正式应用生成质量整改方案。
  - `docs/task-notes/README.md`：专题计划、评估记录和执行细则索引。
  - `docs/task-notes/editor-and-stat-panel-inventory.md`：正式版编辑器与统计面板清单，记录各领域面板职责、优先级和暂缓范围。
  - `docs/generated/` 下的报告、截图和 baseline 是本地可复现产物，默认不作为接手必读文件。

## 提交约定

- 后续所有 Git 提交记录必须使用中文，包括 commit 标题和正文；除非用户明确要求，否则不要再使用英文提交信息。
- 当前权威队列中的每个编号必须使用独立任务分支。单项达到最终最小验收后，先显式暂存该项授权文件、中文提交并推送任务分支，再把该分支合入并推送 `main`；只有确认远端 `main` 已包含该项提交，才允许从最新 `main` 创建下一项分支。内部阶段 checkpoint 不得提前合入 `main`，未合入 `main` 的任务也不得标为完成或开始下一项。
- 当前队列固定执行 `322 → 327 → 323 → 324 → 326 → 328`：保留现有第 322 项分支 `codex-task-322-workerization`，第 322 项完整验收并合入远端 `main` 后，再从该 `main` 新建第 327 项分支；第 327 项同样完成、合入并推送 `main` 后，才能新建第 323 项分支，后续依次类推。不得从旧分支、未合并 checkpoint 或落后于远端的本地 `main` 派生下一项。

## 代码约定

- 写代码时只添加必要注释，注释应解释意图、约束或非显然逻辑。
- 不要为了注释而注释。
- 手动编辑文件使用 `apply_patch`。
- 不要向 `source/` 写入新项目代码，也不要修改原项目源码。
- 运行工具或测试前，先确认是否需要依赖、网络或权限。

## 当前技术路线

总方案改为“参考原项目，独立复刻 WebGL 地图生成器”的路线：

- 独立实现新的生成器应用，主视图使用 `canvas` + WebGL2，优先考虑轻量 WebGL2 封装或后续成熟渲染库。
- 原项目的生成流程、数据字段、图层顺序、编辑交互和导出行为作为参考对象，不直接接入或替换原项目代码。
- 文本、纹章、编辑手柄等复杂能力可以先用新项目自己的 HTML/SVG overlay 过渡，但 overlay 代码仍放在新项目目录。
- PNG/JPEG 导出最终从新应用 canvas 读取；SVG/数据导出在新项目中按能力重新实现，必要时参考原项目格式。

## 当前状态

2026-08-13 权威任务第 322 项已完成并归档。十一类重生成、复合与整图生成、网格 / 社会 / 经济 / 人口 / 路线城市 / 军事入口、大型存档和动态显示准备已接入同一 Worker / fallback、持久镜像、prepared renderer、原子提交与历史恢复体系；普通用户文案不泄漏 Worker、浏览器存储或协议概念。Stage E 的完整 10k / 100k、旧档、取消、迟到、故障、回滚、真实 API、存档与文案门通过；100k hard-cell 单格操作从约 `206ms` 降至 `17.4 / 9.2ms`，正式 base CPU / 分段 GPU 和 patch 字节同源。100k routes 仅按用户统一处置门保留 reused 与取消后 fresh 各最多一条 `≤80ms` 的已归因末端样式 / 布局登记，其它终验操作 LongTask 为 `0`；最终 health、console、page、WebGL 与 Loading 错误为 `0`。独立给事中与观察使均 `ACCEPT`。下一项必须从已包含第 322 项提交的最新远端 `main` 新建 `codex-task-327-*` 分支，执行第 327 项；不得在第 322 项分支继续施工后续编号。

2026-08-12 权威任务第 328 项已登记待执行，并排在现有队列末尾，固定顺序更新为 `322 → 327 → 323 → 324 → 326 → 328`。新图政治生成和用户明确触发的国家 / 省份重生成将把基于 canonical 流量、宽度、等级和连续长度的大河作为可审计软阻隔：强河可以成为省界，在两岸都具备合法中心与连通腹地时也可成为国界；小河不得普遍制造碎片，中心、人口、文化、地形、聚落、港口、道路与合法渡河走廊仍共同参与划界。旧图载入、普通渲染和手工边界不自动重划，锁定行政对象优先，河网不被行政结果反写。本轮只登记文档，不修改产品、构建、浏览器、用户地图、`source/` 或 Wiki；当前第 322 项施工树保持不动。

2026-08-10 权威任务第 327 项已登记待执行，优先级仅次于当前暂停的第 322 项。固定执行顺序改为 `322 → 327 → 323 → 324 → 326`：下次恢复目标时先在现有 `codex-task-322-workerization` 分支按精简阶段门完成第 322 项，验收后推送任务分支、合入并推送 `main`，再从最新远端 `main` 新建第 327 项分支；不得直接跳到第 323 项。第 327 项治理 AI 上下文、根 / 嵌套 `AGENTS.md` 自动注入预算、权威文档路由、开发日志分卷、定向检索、阶段 handoff、本地产物以及 `$run-lean-staged-delivery` / token-efficient `$four-officials-flow`。根说明预算为 `16 KiB`，普通路径累计项目说明为 `24 KiB`；完整历史、trace、截图和大矩阵按需读取，四级流程默认最小角色、无完整历史继承、顺序接力、单写者、冻结复核和首败停止，但不得削弱真实独立验收。本轮只登记权威范围、顺序与逐项合并门，不实施结构迁移、Skill 再改造、代码、构建、CDP、Git 清理或会话清理；第 322 项仍暂停，产品代码、`source/`、Wiki、用户地图和浏览器未改。

2026-08-10 权威任务第 326 项已登记待执行。现有只读 `analysis` 命名空间将新增地点解析、距离和方位三项浏览器 / 无头同源 API；首期支持国家、省份、城镇，名称冲突按“国家 > 省份 > 城镇”、同类按稳定升序 ID 首选，并返回全部候选与规则。精确输入复用统一 `{kind, id}` 对象引用，AI 应先按名称解析 ID 再测量。正式距离统一从世界坐标与当前单位数据计算，屏幕像素距离仅作浏览器可选诊断；国家优先首都、省份优先省会，回退时回显代表点来源。方位返回正北 `0°` 顺时针角度、八方位中文结果和同源距离，两点重合单独标注。三项均无副作用并进入 API schema / 发现和 AI 文档。本轮仅登记文档，未实现代码或修改 `source/`；第 325 项已完成并归档，第 322～324、326 项待执行。

2026-08-10 权威任务第 325 项已完成并归档。河流成功、全锁、取消、失败回滚与撤销 / 重做均不触碰道路三镜像、全部 route / packRoute 对象、道路 salt、锁、GPU buffer、route mesh 或道路 picking；旧档 city / burg / grid / pack 身份和 TypedArray 镜像按两阶段原子 reconcile，路线端点与显示点不再跨 cell 借用旧 burg。加载只诊断港口拓扑，显式细分 / 道路重算才在同一 refinement mother、行政与陆地身份、互为主导水体 overlap 和正式 haven / riverEdges / 温度 / 水路门内修复普通港口，锁对象写前拒绝。冻结真实 `100000` cell 存档中河流 `1531 → 1528` 后道路仍为 `607` 且道路刷新为 `0`；显式道路重算得到 `288` 条路线，其中海路 `45`，`188` 个港口全有效，可达水体缺海路为 `0`，逐段邻接、陆海、正式水路、三镜像、端点、display teleport、港口与 picking 错误均为 `0`，真实 route picking 命中。独立系统 Chrome CDP 验证内部引用与 GPU / picking 门且不连接用户 Chrome；观察使另在用户原 `5410` 同一标签页经页面授权执行正式河流、道路重算，路线面板实显 `288 / 45`，选择、高亮并定位的道路沿陆地连续，全图无蜘蛛网。两路非性能 health、console / page 与 WebGL error 为 `0`，只保留第 322 项已登记的性能信号；浏览器恢复点和私人存档未被覆盖，`source/` 与 Wiki 未改。第 322～324、326 项继续待执行。

2026-08-10 权威任务第 324 项已登记待执行。生成 Tab 将新增独立于程序化高度模板的真实地理 / 历史场景模板入口，用户选择模板与第 323 项约束下的 cell 数后一键创建；canonical 海陆、高程、水文、区域掩膜和保护锚点需确定性映射到目标网格。首批固定世界、中国、欧洲、北美、南美、非洲、亚洲、东亚、澳洲组合、南极洲、中原、本州岛、朝鲜半岛、中东、神圣罗马帝国和罗马帝国地中海 16 项。中国须完整包含藏南、阿克赛钦、台湾、南海诸岛与钓鱼岛；东亚 / 中原按用户四至，澳洲组合不得漏岛，两类历史模板须固定年代与来源并内置对应政治 / 人文场景。模板版本 / 许可 / checksum 可审计、离线按需加载，创建结果为普通可编辑地图且旧图兼容。本轮仅登记文档，未改代码、资源或 `source/`；当前第 322～324 项待执行。

2026-08-10 权威任务第 323 项已登记待执行。地图规模的统一目标边界将收敛为整数 `1～100000`：更大有限数值自动使用 `100000`，小于 `1` 自动使用 `1`；生成 Tab、生成选项、公开 API / schema / 配方、URL / 预设恢复与 `grid.refine` 共用同一规则，并同步 UI、options / metadata、预检与日志。最小一格地图必须可生成、可见、可导出和存档；达到 100k 后的更大细分请求 no-op。超限旧存档不得静默删 cell，须在替换前拒绝并保留当前地图；100k 及以下旧档兼容。当前第 322～323 项待执行，本轮仅登记文档，未改代码或 `source/`。

2026-08-10 权威任务第 321 项已完成并归档，第 322 项已登记待后续实施。路线按陆地 / 水体分组建立确定性稀疏骨架，保留首都、省会和行政组必要覆盖，并拒绝合并成重复 cell 闭环；10k / 50k / 100k 的初始、城镇、河流、道路四阶段逐段邻接与陆海错误均为 `0`。河网候选只在 canonical 共享汇流 cell 的父河局部窗建立锚点，以交点事件识别错接、自交、回头与反向重叠，冻结 / 旧图 / 手工河保持；显示曲线保留逐点 flux，碎片策略只随本次非冻结候选固化。重生成先绘制唯一 generation Loading，地图读取、解析、装载和回滚使用统一 owner；成功、失败、取消、锁冲突、空存档和已亮 operation 的并发反例峰值均为 `1`。第 322 项梳理公开十一类重生成及人口 / 经济下游、现有整图 Worker 的结果复制 / 正式装载 / 派生渲染准备、高度 / 气候 / 洋流、网格 / 拓扑 / 水文 / 寻路 / 社会派生、大型存档与 CPU mesh / 标签 / picking；现有整图 `main-thread-long-task / operation-stall` 保留为该项性能基线。本轮未修改用户现有地图或 `source/`，未提交或推送。

2026-08-10 权威任务第 320 项已完成并归档。“保存到浏览器”不再向控制面板写入进行中、成功体积摘要或失败文本；仅精确清除该入口遗留的旧成功摘要，其它文件操作诊断不动。成功继续使用下方“保存成功” toast；失败改为下方红色“保存到浏览器失败：具体原因”，时长 `6s`。隔离 Chrome 真实下拉成功与故障注入均确认面板为空、错误 toast `5.5s` 仍显示、约 `6s` 后关闭，console / page error 为 `0`；数据兼容、海路回归和生产构建通过。IndexedDB / LocalStorage 降级、保存数据、压缩、存档格式、自动恢复、云端保存、地图、API 与其它操作提示未改。全局交互流程审计的 HF-03 ControlPanel `detail: {target}` 证据及快捷键模式 `30 / 29` 为既有基线漂移，未在本项扩修。当前没有活动权威任务。

2026-08-10 权威任务第 319 项已完成并归档。用户报告的“重生成城镇和河流后道路变蜘蛛网”实际为跨世界海路：现场路线面板有 `168` 条海路，最长约 `1.1万 km`。根因是水域 Urquhart 候选边保留 `65%` 时误按距离倒序，保留了最远边；现改为正序保留近边，陆路逻辑不变。纯候选固定选中最近 `1～13` 边；隔离 Chrome 经正式 API 依次新图、重生成城镇、重生成河流，最长海路端点跨度占对角线 `6.56% / 9.62% / 6.21%`、路线渲染及错误面为 `0`。锁定重生成、10k / 50k / 100k 陆路连通和生产构建通过。没有改用户当前线上地图、`source/`、存档、API、河流 / 城镇 / 政治数据或手工锁路；既有蜘蛛网仅在用户后续明确重算道路或重生成时按修复逻辑替换。当前没有活动权威任务。

2026-08-10 权威任务第 318 项已完成并归档。边界与河流实验室线上无用例的根因是页面请求未部署的正式源码模块，被 SPA fallback 以 HTML 返回后触发模块 MIME 错误，初始化在夹具绘制前中止。两页现各自 Vite 构建为自包含 `assets/`，边界页保留既有只读几何 vendor；生产浏览器门禁确认边界 `20 / 7 / 20`、河网 `8 / 3 / 8` 个固定夹具 / 附加项 / 矩阵，API 初始化、console / page error 和正式源码模块请求均为 `0`。正式应用 `app/`、运行时、地图、存档、公开 API、生成流程、实验室算法、`source/` 与 Wiki 未改；面向读者的实验室文档只保留在线预览链接。构建、部署、控制面板导引和专项回归通过，既有 `regress:shoreline` 的 `lineTriangleCount=11964` 低于旧 `25000 / 30000` 阈值未在本项扩修，AI 机器目录四项陈旧状态保持未动，当前没有活动权威任务。

2026-08-09 权威任务第 317 项已完成并归档。`https://fmg.mosuzi.top/prototype/web-cells/` 现以透明兼容改写复用既有 `webgl-cells` 静态目录及相对 `src/`、`data/` 资源，旧 `/prototype/webgl-cells/` 保留；控制面板“简介”新增一个“实验室”下拉，四个中文条目均在新标签页打开正式路径。生产 Chrome `1280 / 390 / 320px`、线上四页标题、相对模块与固定样本均通过，四类错误为 `0`。构建、部署静态路由、交互面、文档链接与差异检查通过；AI 机器目录既有四项陈旧状态保持未动，`source/`、Wiki、地图、存档、API 与生成流程未改，当前没有活动权威任务。

2026-08-09 权威任务第 316 项已完成并归档。`prototype/` 当前固定为 WebGL cells 历史原型、共享边界拓扑实验室、画卷加载概念稿和河流网络实验室四项；统一职责、入口、验证、部署与正式隔离边界见 `docs/architecture/laboratory-prototypes.md`，AI 选择与写入禁止规则见 `docs/ai/laboratory-prototypes.md`。根 README 中英文版、docs / AI 索引和 Vercel 说明均已引用，`docs/wiki/` 未动。拓扑实验室的少量纯函数属于正式岸线受控复用，河网实验室页面只读；专项和部署回归通过。既有 `docs/generated/ai` 机器目录与当前源码不同步，未在本项文档范围再生成；当前没有活动权威任务。

2026-08-09 权威任务第 314～315 项已完成并归档。正式河网候选对具备共享终端水文 cell 的声明父子关系允许使用既有 `24 world` 最大恢复上限，任何非零短 gap 都以通过全部安全门的共享三次曲线精确落到父河；不补造独立 parent，不修改 canonical 河网，500+ 长桥和六类安全反例继续拒绝。正式 Chrome 的 10k / 50k / 100k 全部 `26 / 70 / 156` 条父子汇流最大缝隙为 `0 / 1.14e-13 / 1.14e-13`。共享 runtime operation 新增 `420ms` 延迟气泡，快速操作不闪，慢操作显示“山河正徐徐推演，请稍候片刻。”或阶段文案，成功 / 错误 / 取消 / 重叠 / generation 共存可靠清理；同步长任务仍按性能问题治理。控制点、锁、存档、API、导出、实验室、生产构建和浏览器错误面通过，`source/` 未改，当前没有活动权威任务。

2026-08-09 权威任务第 313 项已完成并归档。河流实验室候选已抽为正式共享模块，实验室只保留薄转发；正式 `buildRivers` 在 canonical `normalizeRiverNetwork` 后应用通过安全门的显示汇流曲线与对应流量 / 宽度派生，并把结构化结果写入河流 metadata。固定 10k / 50k / 100k 的 canonical cells、父子、流域、源头、河口不变；100k 已知支流断接实际贴合，500+ 长桥与六类安全反例仍拒绝，冻结河流和旧存档加载不变。正式 10k / 100k Chrome 的渲染、picking、PNG、GeoJSON 与四类错误门通过；发布前城镇 10k / 100k 严格交互门连续两轮长任务为 `0`。完成改动按功能分批中文提交后统一推送 `main`，`source/` 与用户 5410 地图未改；当前没有活动权威任务。

2026-08-09 权威任务第 312 项已完成并归档。用户在当前 Chrome 的 5403 河流实验室确认“支流未接入干流”仍显示 candidate rejected；根因是主夹具自身使用了 `gap 52/12` 的不可安全修复长桥，浏览器回归也固化 rejected，而独立 `shared-cell-display-gap` 已另行覆盖 500+ 长桥反例。主夹具现为 `gap 16 > 12`、局部容差 `20.595445` 的可修复正例，生成曲率 `1.787951` 的三次汇流段并通过五类安全门，C / D 与页面 candidate 均 accepted；超长桥仍拒绝。用户当前 5403 页已刷新并现场复验，正式生成器、正式河流、API、存档、用户 5410 和 `source/` 未改，当前没有活动权威任务。

2026-08-09 权威任务第 311 项已完成并归档。河流控制点 session 现以 working 相对 begin baseline 的完整状态重算累计 `changed`，不会再因 pointerup 与最后 RAF 同坐标、单次 move inspection 返回 no-op 而丢失 dirty；拖动结束后橙色变化曲线持续显示，“应用控制点”立即可用，净变化回到 baseline 时才恢复未改状态。Node 与隔离 Chrome 10k / 100k 均覆盖拖动后持久预览、按钮状态、直接应用、预览 / 提交同源和交互零长任务 / 零错误；控制点几何、共享三次曲线、正式河流、API、存档、生成算法、用户 5410 和 `source/` 未改。独立河流实验室已在用户 Chrome 的 `http://127.0.0.1:5403/` 新标签页打开，当前没有活动权威任务。

2026-08-09 权威任务第 310 项已完成并归档。城镇成功提交后不再退出 `city:move`，而是清理本次预览并把固定 `36×36 CSS px` 起拖环迁到新位置；同一城镇可连续拖动，每次提交独立写入历史。点击当前城镇以外的位置会退出并继续普通选择链，活动按钮显示“退出移动城市”，手动退出和 Escape 保持。隔离 Chrome 10k / 100k 完成连续两次提交、面板 / 起拖环更新、两种退出和撤销 / 重做；100k handler P95 / max `0 / 0.1ms`、preview `0.3 / 0.5ms`、交互长任务和交互期错误为 `0`。第 306～309 项迁移事务、行政规则、热区、细分拾取、API、存档、生成算法、用户 5410 与 `source/` 未改，当前没有活动权威任务。

2026-08-09 权威任务第 309 项已完成并归档。此前隔离回归只覆盖直接生成 100k，漏掉用户 5410 的 10k→100k 细分网格仍被 `pickGridCell` 按规则行列推导 ID，导致城镇对象可拾取但移动目标 cell 为空。正式拾取现从附近母 cell 展开 `grid.refinement.children`，以子多边形包含和有界最近生成点确定 cell；城镇水陆预检使用正式画面的 grid 高度。移动继续使用半径 `18 CSS px`、不随缩放变化的热区与 `36×36 CSS px` 起拖环，密集冲突取屏幕最近城镇；ghost 只用 DOM 合成层，不创建第二 WebGL context。用户原 5410 细分 100k 地图已用真实鼠标把城市 `#0` 从 `grid 3521 / pack 1660` 移到 `grid 41503 / pack 19870`，城市管理即时同步且页面响应，随后同一历史事务撤销恢复原位置和选择。细分、城镇迁移、拾取、面板、构建与隔离 10k / 100k 交互门通过；`source/` 未改，当前没有活动权威任务。

2026-08-09 权威任务第 308 项已完成并归档。城市对象详情新增“打开城市管理”，与“定位 / 名称库改名 / 编辑名称”固定组成两列两行；入口只对城市显示，点击后复用既有城市管理面板并保持当前城市选中。隔离真实 Chrome `1280 / 390 / 320px` 的四按钮均为 `2×2`，横向溢出和文字裁切为 `0`，health / console / page / WebGL error 为 `0`。城市数据、事务、API、存档、生成算法、其它对象详情、用户 5410 和 `source/` 未改，当前没有活动权威任务。

2026-08-09 权威任务第 307 项已完成并归档。城镇移动热区固定为半径 `18 CSS px`，不随相机 / 画布缩放改变用户视角尺寸；世界索引只召回候选，最终按 CSS 屏幕距离选择最近城镇，等距按稳定 ID。同一落点仅容忍既有 `0.1 world` 释放量化误差并保留循环，普通同 cell / 远景近邻仍取最近对象，移动模式继续优先已选重合对象。Node 与隔离 Chrome 10k / 100k 覆盖 `0.35～9x`、`17.5 / 18.5px` 边界、24px 密集冲突、真实边缘起手与 60 帧取消；两档交互长任务和四类错误为 `0`。用户现有 5410 只读检查，未刷新或写图；`source/` 未改，当前没有活动权威任务。

2026-08-09 权威任务第 304、305、306 项已完成并归档。第 304-F～G 修正实验室的候选接受、遥测 checksum、同快照 A/B、真实显示桥接距离和六类曲线安全门；100k 旧 baseline 保持 BLOCK，新候选 accepted，但候选仍只留在独立实验室，未接入正式 `rivers.js`。第 305 项移除河流控制点 legacy packCell 输入链，以世界坐标和稳定 ID 统一新增 / 拖动 / 双击删除，并让预览、正式渲染、picking、长度和 GeoJSON 共用向心 Catmull–Rom 转分段三次 Bézier。第 306 项把城镇拖动改为 RAF ghost、可取消异步 preflight 和局部提交，允许同 cell 多城及跨境首府重算；目标缺首府时确定性接纳迁入城，原实体无候选时允许无首府但保留合法所属陆地 center，路线 / 港口 / 市场按已验收降级处理。独立 10k / 100k 严格 Chrome 连续两轮交互长任务为 `0`；用户现有 5410 的 100k 地图完成城镇跨境同 cell 移动后撤销恢复，以及河流新增 / 拖动 / 预览曲线继续新增 / 双击删除 / 不应用退出，健康基线未增加、WebGL error 为 `0`。`source/` 未改，当前没有活动权威任务。

2026-08-08 权威任务第 301、302、303 项已完成并按完成日期归档。第 301-B / C 完成岸线同侧颜色局部刷新与跨水陆拓扑局部刷新，第 301-D 预热高度空间索引和 Grid → Pack 映射，第 301-E 补齐真实高度选区高亮恢复、动作 / 作用范围按钮选中语义、选中且禁用视觉和全局画笔大小偏好；第302完成城镇拖动预览、取消、单事务提交、行政 / 港口 / 路线镜像同步、selection / picking 和 10k / 100k 隔离 Chrome 验收；第303完成可选河流控制点集合、同 cell 多点、预览拖动 / 新增 / 双击删除、集合 picking、单事务历史和双档真实指针验收。第303控制点交互期间 application console / page / health / WebGL error 为 `0`，生成阶段约 `1.05s / 2.20s / 4.55s` 长任务作为后续性能证据保留。`source/`、用户当前 Chrome 标签页和用户地图未改；该次归档当时没有活动权威任务。

2026-08-07 权威任务第 298 项已完成。新增受控 `grid` API 六个方法，可对现有网格读取摘要 / 快照、预检并事务式提交同规模结构写入或拓扑保持式细分；固定旧地图由 `10004` 扩展到 `100000` cells，旧点与旧高度精确保留，海陆连通分量、母域邻接与河流 / 路线连续邻接通过，国家、城市、Marker、行政中心、河流、路线和地区引用完成迁移。陈旧绑定、非对称邻接和非法跨母域关系会拒绝，失败可完整回滚，撤销 / 重做及旧 / 新存档往返通过。用户现有 `5410` 页面和独立生产 Chrome 均完成真实细分、稳定渲染及 PNG；生产链耗时约 `7.3s`，回收后保留 JS 堆约 `696 MiB`，四类功能错误为 `0`。当前 API 为 `18` 个命名空间、`322` 个方法，能力矩阵 `1213` 行且缺口为 `0`；`source/` 零改动。第 298 项已按完成日期归档；第 300 项随后完成并归档，当前没有活动权威任务。

2026-08-07 权威任务第 284 项已完成。284-A 为 100k 浏览器存档增加 IndexedDB quota 降级并保留旧 LocalStorage / envelope / 旧存档兼容；284-B 收敛高度编辑预览、脏区刷新、取消和历史；284-C 收敛 viewport / overlay / picking；284-D 以原生 Blob + FileReader 优先替代大段 JS base64 中间字符串，100k 存档实际 `99846` cells 保存 / 恢复通过，raw / gzip / envelope 约 `64.5MB / 14.5MB / 19.4MB`。284-E 的隔离系统 Chrome 对照覆盖 10k / 100k 导出、存档、历史、面板、平移缩放、PNG、拓扑细分和旧数据迁移；checksum、schema、地图数据、存档兼容、真实视觉、console、page、API health error 与 WebGL error 通过，性能长任务按风险单列。所有实现均未修改 `source/`，第 284 项已按完成日期归档，当前没有活动权威任务。

2026-08-07 权威任务第 299 项已完成。`docs/current-plan.md` 已从约 `949 KB` 的历史混合清单收敛为只含未完成任务的执行入口；`233` 个既有标准完成 / 取代条目按 `2026-07-08～07-19`、`07-20～07-25`、`07-26～07-31`、`08-01～08-07` 四个时间卷归档，第 299 项完成记录也进入最后一卷。归档索引支持按日期、编号和关键词检索，生成 / 检查脚本会拒绝重复编号和把未完成任务写入归档。第 299 项完成当时只剩第 284、298 项暂缓；第 298 项后来已完成并归档。

2026-08-05 权威任务第 279 项已完成。第 278 项极简几何继续采用小圆环、三角、菱形、双环、五角星、六边形、断口马蹄、方框和帐篷，但正式地图不再生成城镇 HTML/SVG 节点；真实 `10k` 地图的 `839` 个城镇由独立 WebGL2 实例层单次绘制，缩放和平移帧只改 uniform，连续尺寸、阈值平滑显隐、纯白内线与深色硬描边均在 shader 中完成。标签增加视口外预热，道路 / 河流 overscan 按视口和 DPR 自适应；生产 Chrome 平移帧 P95 为 `1.7ms`，新进入标签首帧约 `0.90` opacity 且无滤镜。PNG 城镇开关、选中、图层、拾取、三档视口、构建与四类错误门禁通过；人口、角色、文化、手工视觉、存档、schema、API、地图数据和 `source/` 未改，当前没有活动权威任务。

2026-08-05 权威任务第 276 项已完成。九类城镇基础剪影已从轻俯视建筑立面改为方案 C 的严格俯视聚落平面：人口四级用开放院篱、断续木栅、半围城垣和完整四门城垣递进，首都 / 省会通过内部院落分化，港口、寨堡与营帐保留临水开口、棱堡和营栅语义。九个基础指纹 `9 / 9`、四级指纹 `4 / 4`；真实 `10k` 地图的 `839 / 839` 城镇、角色徽记、拾取、选中高亮、PNG、三档 Chrome 与生产构建通过。人口、手工视觉、碰撞、标签净空、存档、schema、API、地图数据和 `source/` 均未改；本项未提交或推送，当前没有活动权威任务。

2026-08-05 权威任务第 275 项已完成。远端 GitHub Wiki 的 `14` 处图片引用不再落到返回 `406` 的 `wiki/assets/...` 页面路由；仓库内源稿继续使用可直接预览的相对路径，发布副本自动改写为 `raw.githubusercontent.com/wiki/...`。重新发布后的 `12` 个含图页面、`14` 处实际 `<img src>` 和 `10` 张唯一 PNG 全部公开返回 `200 image/png`，远端 Markdown 除图片目标转换外与源稿语义一致；真实 Chrome 确认首页图片完整加载并正常显示。Wiki 提交为 `f16e2ce25db62469bc11473b9cdecf2e80b8124e`。产品功能、图片像素、截图清单、`source/` 均未改；当前没有活动权威任务。

2026-08-04 权威任务第 226 项的远端阻塞已解除。用户将主仓库公开并创建初始 Wiki 首页后，规范 `.wiki.git` 远端已经接入可复现发布脚本；`docs/wiki/` 的 `20` 个中文页面、`10` 张长期 PNG 与 `1` 份截图清单已同步到 GitHub Wiki，首页、侧栏、正文、图片和回链完成远端验收。仓库内源稿仍是唯一权威来源；当前没有活动权威任务。

2026-08-04 权威任务第 274 项已完成。中文 Wiki 已扩展为 20 个页面、1544 行、14 处图片引用和 10 张长期 PNG，按普通用户任务覆盖生成显示、自然世界、人文世界、表达对象、安全交换与自动化分析；递归审计从当前源码冻结七个页签、十二种专题、五组二十二个图层开关、五组二十四个管理入口和二十九个注册面板。隔离系统 Chrome 两次 clean capture 的总摘要均为 `d449abc847dca78b0fd619c9eb41fdf9f15ad1e09c6365b3ea72195a7dbd2771`，两轮独立复核均为 `ACCEPT`。产品功能、地图数据、schema、API、默认偏好、`source/` 和远端 Wiki 未改，本项未提交或推送；当前没有活动权威任务。

2026-08-04 权威任务第 268 项已完成并推送。生成 Tab 新增地图名称；名称输入右侧的无边框设置图标按需展开共享存档格式、token 帮助和实时预览。本地保存、高级压缩导出和云端新建共用同一持久化模板，原生 gzip JSON 扩展名为 `.webfmg`，旧 `.webgl-map.json.gz / .json.gz / .gz / .json` 继续兼容。CDP 精确测量并修正齿轮 SVG 原向上 `1.5px` 的光学偏差后，初次加载与刷新均为 `0px`；实际双下载、三档视口、生产构建和四类错误面通过，`source/` 零改动。

2026-08-03 权威任务第 267 项已完成。README 两张示例图已重新分工：relief 固定为 `height` 自然地貌，国家 / 省份 / 城市标签、城市图标、道路和政治边界全部隐藏；atlas 固定为 `states` 国家视角，显示国家着色、国界、国家名称、首都 / 城市、河流和道路。截图工具改为逐场景应用完整 `viewMode + layers`，等待门禁只等待当前可见的动态图层；两次 clean run 的 relief / atlas 摘要分别稳定为 `7b96922b…05257 / b19943c5…68967`，可见标签为 `0 / 0 / 0` 与 `12 / 0 / 10`。生产构建、静态门禁、系统 Chrome、人工视觉、故障注入和清理通过，四类错误为 `0`；产品 fresh 默认、算法、schema、旧偏好和 `source/` 未改。本项未提交或推送，当前没有活动权威任务。

2026-08-03 权威任务第 266 项已完成。fresh session 默认只保留地貌、河流、道路、城镇、国家名称、人口、比例尺、岸线与国界等核心画面，城市标签预算固定为 `128`；国家 / 省份 / 首都 / 城市默认字体、字号、字重、字距、颜色和透明度已收敛，并补入本机实际可用的 Noto 简体中文字体名。新增 `pnpm run capture:readme-showcase`，可在隔离系统 Chrome 的专属 CDP session 中固定生成 `mountains-and-seas` relief / atlas 两张 `1440×960` 正式 PNG，并由中英文 README 直接引用。连续 clean run 的两张摘要稳定，正常与故障注入均清理服务、浏览器和 profile；既有用户偏好、旧图显式样式、地图算法、存档 schema、`source/` 和用户 Chrome 会话均未改，独立复核与最终观察使均为 `ACCEPT`。本项未提交或推送，当前没有活动权威任务。

2026-08-03 权威任务第 265 项已完成。大型城市已改为 A 方案圆盘内“完整环城城墙 + 正门 + 中央尖顶塔楼 + 墙内屋舍”的一级轮廓，其余八个城镇基础剪影以同一暖墙体、深屋顶、厚墨线和轻俯视语言重画，九个几何指纹保持 `9 / 9`；人口阈值、P90、角色徽记、文化调色、手工视觉与旧图格式均未改。应用图标保留原始 `app-icon-master.jpg` 作为不可覆盖来源，正式链改用完整无缝的透明圆形高保真派生母版，外部墨绿方框和朱红方印均已删除；favicon 与 manifest 使用 `?v=265` 规避旧资源缓存。首轮独立复核阻断的圆盘接缝、缓存覆盖和元数据伪指纹均已限修，复核与终验均为 `ACCEPT`；生产构建、系统 Chrome 三档视口、真实 `10k` 地图、DOM / PNG 同源、picking 与 PNG crop 通过，console、page、health 与 WebGL error 为 `0`。`source/` 零改动，本项未提交或推送，当前没有活动权威任务。

2026-08-03 权威任务第 263～264 项已完成。用户提供的 `1280×1280` A 方案原图已逐字节保存为正式母版，SHA-256 固定为 `50c20b1e8d908b0a45e91988e5421a0792d9f4570db7a504de91205851ab771d`；SVG 内嵌母版并逐字节核对，六档 PNG 只做等比缩放，manifest 使用 `any` 避免蒙版裁切。画布图标注册表固定城镇 `9 + 3`、Marker / 资源点 `58`、军事 `10`，共 `80` 个稳定图形键；自动 Marker 按 type 显示而不写 `type:*`，旧手工 symbol、城市剪影和军事别名兼容。首轮独立复核 `BLOCK` 的面板旧 symbol、角色组合、六文化与军事正式样式已限修，终验 `ACCEPT`；真实 `10004` grid cells 地图、DOM / PNG 同源、拾取、三档 Chrome 与生产构建通过。`source/` 零改动，未提交或推送，当前没有活动权威任务。

2026-08-03 权威任务第 262 项已完成。Dropbox 与 Google Drive 默认把短期 access token、到期时间和 provider / origin / 配置 / scope 指纹保存在当前标签页 `sessionStorage`，刷新时只恢复未过期且指纹一致的连接；过期、401、配置变化、损坏记录和主动断开会清理。Google 不再强制重复 `prompt: "consent"`。两 provider 协议、旧浏览器存储、生产构建与系统 Chrome 刷新 / 断开反例通过；首轮独立复核阻断的 Google 旧目录请求竞态已用连接代次与请求身份门禁修复，最终审计再阻断的延迟 `401` 误清新会话已用请求 token + 连接代次快照修复，复验为 `ACCEPT`。本项没有增加开关、refresh token、LocalStorage / IndexedDB、跨标签页共享、后台同步或新的云盘权限，未访问真实云盘、提交或推送；当前没有活动权威任务。

2026-08-02 权威任务第 255 项已完成。`origin/codex-smooth-boundaries` 通过双父提交 `a5832dc` 合入当前 `main`；共享拓扑海岸 / 湖岸平滑、局部回退和硬边恢复通过 20 夹具、10k / 50k pure / formal、生产构建及 Chrome 六态验收。canonical 地图数据、checksum、存档、API、编辑和 picking 不变，国家 / 省份边界继续使用硬边；本项未推送，当前没有活动权威任务。

2026-08-02 权威任务第 254 项已完成。悬停信息卡右上角新增信息圆按钮，悬停或键盘聚焦后显示从权威注册表派生的 `22` 项全局动作、`23` 组全局按键和 `2` 项场景动作；稳定 DOM、弹层滚动、高度画笔 pick 保留与 `1440 / 390 / 320px` 均通过独立 Chrome 验收。本项不改变快捷键执行、地图数据、历史或存档，未提交或推送，当前没有活动权威任务。

2026-08-02 权威任务第 253 项已完成。“全部地区”不再伪装成独立 `zones` 图层，而是五个既有地区状态的三态批量控制；半选可一键全开，全开可一键全关，四个细分开关和底层兼容字段保留。系统 Chrome `1440 / 390 / 320px` 与同源临时 `5411` 页通过，原用户页为保护未保存地图未强制刷新；服务保持运行，本项未提交或推送，当前没有活动权威任务。

2026-08-02 权威任务第 252 项已完成。云端存储说明、配置、连接和边界卡片统一为现有暗色主题，代码标签、链接、文件项和状态文字保持清晰层级；系统 Chrome `1440 / 390 / 320px` 无横向溢出或应用错误。provider、OAuth、配置与上传下载语义未改，`5411` 服务保持运行，本项未提交或推送，当前没有活动权威任务。

2026-08-02 权威任务第 248～251 项已完成并通过系统 Chrome `1440 / 390 / 320px` 验收。地区管理批量按钮相邻间距统一为 `8px`；地区重生成同步刷新覆盖与标签，旧自动编号名在显式重生成时迁移，列表名称置于编号之前；地区图层恢复“全部地区、事件地区、自然地区、自动无人区、地区名称”五个入口。`5411` 服务保持运行，本批未提交或推送，当前没有活动权威任务。

2026-08-03 权威任务第 261 项已完成。Google Drive 新存档默认保存到可配置的 `/webFMG`；Cloud Provider Config 新增 `providers.googleDrive.folderPath`、`FMG_GOOGLE_FOLDER_PATH` 与旧 `VITE_FMG_GOOGLE_FOLDER_PATH` 兼容入口，支持多级路径与显式 `/` 根目录。目录只在新建存档时按需创建，列表保留本应用旧根目录地图，覆盖不静默迁移；配置、mock 协议、生产构建与系统 Chrome 回归通过，未扩大 `drive.file`、读取任意用户文件、启动真实 OAuth、提交或推送。当前没有活动权威任务。

2026-08-03 权威任务第 260 项已完成。中文正式项目名统一为“WebGL 幻想地图生成器”，加载画卷等紧凑界面使用“幻想地图生成器”，manifest 短名为“幻想地图”；英文名继续使用“WebGL Fantasy Map Generator”。README、页面标题、加载画卷、manifest、SVG 图标标题、概念稿、部署说明和品牌门禁均已同步，旧中文品牌词搜索为 `0`；Chrome 三档视口和生产构建通过。保留“架空历史创作”等普通语义，未改包名、域名、代码命名空间或原项目名称。

2026-08-03 权威任务第 259 项已完成。Dropbox 官方与本地 Redirect URI 统一为 `/oauth/dropbox/callback`；独立轻量页不加载地图应用，只把授权码、state 或错误送回 opener 并自动关闭。opener 统一执行 origin / popup source / state / 十分钟期限 / 并发门禁和 PKCE 换令牌；旧整页 popup 也不再跨窗口传 token。开发与生产 Chrome 专项、旧回调、过期和并发反例、构建及两轮独立复核通过。Google Drive 已由用户确认正常且语义未改；本项未访问云盘、提交或推送。

2026-08-03 权威任务第 258 项已完成。应用图标已补齐 `64 / 256px`；用户明确提供的三项浏览器公开 OAuth identifier 已写入正式 Cloud Provider Config，无环境覆盖时官方构建自动保留。私人部署可用统一 JSON、新逐项变量、旧 `VITE_FMG_*` 或构建后替换覆盖，不需改源码；`.env.example` 保持空模板。仅 `localhost:5410` 使用本地 Dropbox 回调，其他位置保持正式回调；构建和运行时均拒绝 `GOCSPX-` Client secret，provider state 不回显 identifier。静态、构建与系统 Chrome 官方 / 空 / 私人覆盖三态均通过；本项未推送或触发线上 OAuth。

2026-08-02 权威任务第 257 项已完成。该项最初建立原创无文字“地图圆盘 + 朱红方印”图标链；第 265 项已按用户要求删除外框与朱印，当前正式资源只保留透明圆形地图。SVG 主稿及第 258 项扩充后的 `32 / 64 / 180 / 192 / 256 / 512px` PNG 位于 `app/webgl-generator/public/`；favicon、Apple Touch Icon、Web App Manifest 和主题色均已接入，生成与静态 / Chrome 验收脚本已固定资源和入口契约。不得回退空 `data:,` favicon、复制原作图标或把 FMG 字母缩写写入图标。

2026-08-02 权威任务第 245～247 项已完成并通过两轮阻断限修、第三轮独立复核与系统 Chrome 验收。地图语义标签固定为国家、省份、首都、城市、手工、地区六类，地区样式可独立编辑；画布文字机器目录为语义标签 `6`、成品注记 `3`、HUD `8`、诊断 `1`、未渲染名称 `3`。源码门禁会识别未标记固定文字、绕过 helper 的 renderer 动态文字和对应未登记 CSS；军事数字、图例和比例尺继续使用独立样式来源，HUD / 诊断与河流 / 道路 / marker 名称不得冒充标签覆盖。

2026-08-02 权威任务第 256 项已完成，后由第 260 项更新中文名称。根 README 的中文项目名现统一为“WebGL 幻想地图生成器”，英文为“WebGL Fantasy Map Generator”；不得再以“FMG WebGL 地图生成器 / FMG WebGL Map Generator”作为项目名称，原 Fantasy Map Generator 的正式名称仅在参考与致谢语境中保留。

2026-08-02 权威任务第 242～244 项已完成并通过二轮独立复核与系统 Chrome 验收。地区管理和云端存储组件随入口构建加载、面板继续延迟挂载；共享懒加载恢复不得自动刷新未保存地图。地区重新生成已在控制面板和地区面板公开，继续复用现有 `zones` API、地图快照事务、锁定保护与生成器。

2026-08-02 权威任务第 236～241 项已完成并通过三轮独立复核与系统浏览器验收。图层只合并首层 UI 入口，不合并底层状态键；网格单元无偏好时默认关闭；自动无人区改用周边地理语义名称；纹章入口已隐藏但兼容链保留；共享下拉箭头间距已收紧。Dropbox / Google Drive 云存储通过统一 provider registry 接入，公开 client identifier 由 `VITE_FMG_*` 环境变量配置，不复用原作硬编码 app key；第 241 项当时令牌仅存内存，后由第 262 项调整为当前标签页 `sessionStorage` 中的短期会话。fixture / mock 与未配置状态已验收，没有项目方 OAuth 配置时不得声称真实账号联调完成。

2026-08-02 第 229～235 项已完成。第 235 项明确当前应用代码 100% 由 Codex + GPT 实现，文档同样由 Codex + GPT 编写；未来继续保持完全由 Codex + GPT 完成是内部开发约束，README 只陈述当前事实，不重复解释 `100%` 的含义，也不强调未来承诺。第 234 项已清理中英文 README 的翻译腔与模板化表达，两版继续只简述 API 与自动化入口，把精确能力、安全边界和使用步骤交给仓库内 Wiki 源稿与 AI 手册。

2026-08-01 第 229～232 项已完成，当前没有活动权威任务。第 232 项只公开匿名化问题模式和通用行动闭环；真实存档、阶段指标、对象 / cell id 和截图只写 Git 忽略的 `private-cases/`，不得提交仓库。

2026-07-31 用户新增权威任务第 227～228 项。第 227 项懒加载、默认只读、可刷新重连的当前标签页 AI 受控桥与第 228 项带事务回滚、幂等和安全输出的无头写入 API 均已完成并通过真实浏览器验收；两项保持并行适配器关系，不得开放远程监听、任意 JavaScript、裸 map 写入或绕过现有领域规则。

2026-07-31 新增权威任务第 223～226 项。第 223 项共享 API 核心与无浏览器只读运行时、第 224 项区域聚合分析、第 225 项 AI 文档系统和第 226 项 GitHub Wiki 人类文档与可复现发布流程均已完成；第 226 项原私有仓库套餐阻塞已于 2026-08-04 在用户公开仓库并初始化 Wiki 后解除。当前不得越界实现受控标签页桥、无头写入、远程 HTTP 或三个标准问题的自动写算法。

正式应用已经跨过早期占位原型阶段。权威任务第 28～52、54～279、298～299 项已完成，第 53 项按用户决定移除；第 226 项远端 GitHub Wiki 也已发布。第 227 项新增当前标签页受控桥并把区域分析同步到浏览器 API；第 228 项新增无 DOM 的安全写会话和 `6` 个无头写方法；第 232 项新增三类匿名化复杂区域干预模式与通用行动闭环。当前公开 API 为 `18` 个命名空间、`322` 个方法和 `179` 个编辑方法，稳定等级为 `314 / 7 / 1`，逐方法描述为 `322 / 322`，对象查询覆盖 `20` 类。完整能力矩阵为 `1213` 行、`covered 1139 / excluded 74 / deferred 0 / gap 0`；复合语义矩阵为 `80` 个动作、`70` 个完整事务与 `10` 个玩法配方，`322` 个公开方法全部完成分类，结构缺口为 `0`。第 205 项锁仓分母继续保持 `14 / 15 / 22`、双向差集 `0`；第 218 项另覆盖城市 / burg / 省份范围锁、路线锁、手工行政标识与共享 grid 冲突的原子保护。正式加载页继续使用旧纸自卷画轴和指定 PNG 印面；独立 prototype 继续作为视觉基线。批准范围统一查看 `docs/current-plan.md` 的“权威任务清单”；README、专题文档中的“下一步”只作候选或历史语义，不能覆盖权威清单。

第 216 项代码、机器门禁、独立复核与第 211～216 项统一 Chrome 验收均已完成；复核提出的 `safeTop` 生命周期、异步内容重排、低位入口空白误算、共存布局、精确锚点与离屏锚点均已限修。普通 / 150% 字体六档矩阵 document 溢出为 `0`，管理主面板和二级弹框均在安全区内且正文可滚到底，焦点视觉一致，application console、page 与 WebGL error 为 `0`。

第 217～218 项代码、专项、兼容矩阵、生产构建、两组独立复核与系统 Chrome 验收均已完成。省会重评在新图道路 / 标签 / 经济派生前自动运行，旧图只允许“预览 → 指纹确认”的显式单事务；`#150 / #284` 固定反例选择 `#284`，`10k / 50k / 100k` 与共享 grid 冲突样本通过，真实 Chrome 全量预览为变更 `0`、不变 `229`，主面板完整位于视口安全区且 application console error 为 `0`。

第 219 项代码、专项、API / 矩阵、生产构建与系统 Chrome 验收均已完成。高度灰度图固定读取基础 `grid.cells.h` 与 Grid Voronoi 几何，按完整世界尺寸把高度 `0～100` 线性映射为灰度 `0～255`，复用普通 PNG 的 `1x～4x` 倍率，不读取相机、主题、图层或 overlay。UI `2x` 实际下载与 API `1x` data URL 均通过，全部 `3015` 个测试 Grid Cells 被绘制，地图 checksum、历史和相机不变，application console、page、health 与 WebGL error 为 `0`。

第 220～222 项代码、专项、旧数据兼容、API / 矩阵、生产构建、独立复核与系统 Chrome 验收均已完成。事件地区统一保存并显示结构化参与方；地区现有九类内置自然类型、完全自定义类型、底区 / 覆盖区和四项中性基础影响；自动无人区按中立低人口陆地的共享边连通分量生成独立对象、名称与标签。地区标签改名、撤销 / 重做、选择、定位和独立显隐闭环，自动无人区与地区名称分开关均不修改地图数据；清空 health 后 application console、page、active health 与 WebGL error 为 `0`。

2026-07-30 发布前按用户要求由全新智能体再次验收。首轮动态复现发现区域人口增减 / 转移有 `6` 个城市规模差异、国家合并有 `28` 个差异，并补出新建国家改变 P90 后既有城市未刷新的写入面；现已集中到共享规模 / 自动视觉刷新及字段存在性快照，角色字段不再覆盖人口规模。原样复跑差异均为 `0`，新建国家、手工视觉、撤销 / 重做、故障回滚、旧图缺字段、生产构建、完整能力矩阵和复合语义均通过，两路终轮复核为 `ACCEPT`。系统 Chrome 5410 的既有旧图预览为变更 `214`、拒绝 `21`，有拒绝项时确认禁用，取消后清空，application console、page 与 WebGL error 为 `0`。

2026-07-30 用户截图纠正了 debug 工具栏验收口径，并进一步明确四个文字入口都不得内部折行：整条 `.map-toolbar-actions` 必须单排，“控制面板 / 适配视图 / 测量 / 开发模式”也必须各自完整单行。当前四项均按内容宽度分配，`294px` 以下仅按视口比例收紧字号、间距与内边距，不使用负字距、隐藏、裁剪或横向滚动；系统 Chrome 在 `480 / 390 / 320px` 普通与 150% 页面缩放六档中测得五个控件 top 差均为 `0px`、工具栏与 document 横向溢出均为 `0`。控制面板顶部七个 Tab 继续固定在正文滚动区之外，管理正文可滚到底且 Tab 位移为 `0px`，工具栏收展、开发入口和错误面均正常；最终独立验收为 `ACCEPT`。

以下内容是仍有参考价值的早期里程碑记录，不代表当前待办：

- 阅读 `source/Fantasy-Map-Generator` 核心结构。
- 编写图形化重实现总方案：`graphics-reimplementation-plan.md`。
- 完成第 0 里程碑外部性能基线工具：`tools/fmg-profile.mjs`。
- 编写第 0 里程碑说明：`docs/performance/performance-baseline.md`。
- 跑出可信 `10000/50000/100000` 三档性能基线：
  - `docs/generated/reports/performance-baseline-results.json`
  - `docs/generated/reports/performance-baseline-results.md`
- 开始并跑通第 1 里程碑最小 WebGL cells 原型：
  - `tools/fmg-export-snapshot.mjs`
  - `tools/serve-prototype.mjs`
  - `prototype/webgl-cells/`
  - `docs/milestones/milestone-1-webgl-prototype.md`
- 整理第 1 里程碑 WebGL 原型与第 0 里程碑 SVG 基线对照：
  - `docs/performance/webgl-svg-performance-comparison.md`
- 将 WebGL 原型主接口收敛为 `GraphicsMapRenderer`：
  - `prototype/webgl-cells/src/renderer.js`
  - 保留 `CellWebGLRenderer` 兼容别名。
- 新增正式 WebGL 原型性能采集脚本：
  - `tools/webgl-prototype-profile.mjs`
  - `docs/generated/reports/webgl-prototype-profile-results.json`
  - `docs/generated/reports/webgl-prototype-profile-results.md`
- 修正底层 mesh 数据源：
  - 基础 cell mesh 使用 `grid.points`、`grid.cells.v`、`grid.vertices.p`。
  - `pack.cells` 只用于国家、边界、河流、picking 等业务语义。
  - 不要再把 `pack.cells` 当作均匀底层网格，否则水域/边界 pack cell 会出现巨型多边形。
- 修正原型级河流折线河口处理：
  - 当前没有复刻原版 `Rivers.getRiverPath()` 的变宽河道。
  - fallback 河流折线遇到第一个水域 cell 会插入近似河口点并停止，避免河流画到海里。
- 正式应用已完成第一轮 source 生成算法整改：
  - `grid.cells.c` 改为共享边邻接，并用于高度、feature、河流、路线和语义扩张。
  - 高度末端不再使用全局百分位重排，改为海平面校准、连续 relief 拉伸和坡脚平滑。
  - 路线改为 A* 成本寻路，失败时不再画直连。
  - 河流改为动态河源上限和更低 flux 阈值。
  - 文化、宗教、国家、省份和区域改为邻接成本扩张。
- 更新当前计划和开发历史：
  - `docs/current-plan.md`
  - `docs/development-log.md`

第 0 里程碑可信基线摘要：

| 目标 cells | 实际 grid cells | 实际 pack cells | 生成耗时 ms | 完整绘制 ms | SVG 节点 |
|---:|---:|---:|---:|---:|---:|
| 10000 | 10004 | 5890 | 431.1 | 203.6 | 11462 |
| 50000 | 50142 | 20870 | 2471 | 229.5 | 41573 |
| 100000 | 99846 | 44682 | 4420.9 | 314 | 77894 |

注意事项：

- Windows 端口 `5109-5208` 在当前机器上被 TCP 排除，Vite profiling 使用 `5300`。
- Playwright 自带 Chromium 下载曾超时，当前可用 `--browser-channel chrome` 复用系统 Chrome。
- 原项目 `generate()` 会在 points 未锁定时把点数重置为默认 10k；profiling 工具已经在生成前设置点数并锁定 `lock_points`。

第 1 里程碑历史原型摘要：

| 指标 | 数值 |
|---|---:|
| 目标 cells | 100000 |
| 实际 grid cells | 99846 |
| 实际 pack cells | 58251 |
| 渲染来源 | grid |
| grid Voronoi 顶点 | 200338 |
| pack Voronoi 顶点 | 117148 |
| 三角形 | 598519 |
| GPU 顶点 | 1795557 |
| 国家边界线段 | 1404 |
| 河流数量 | 1021 |
| 河流线段 | 5641 |
| picking 索引桶 | 39204 |
| 平均候选 cells | 5.9 |
| 最大候选 cells | 17 |

第 1 里程碑历史 WebGL 性能采集摘要：

| 指标 | 数值 |
|---|---:|
| buffer 构建 | 336.5ms |
| buffer 上传 | 36.6ms |
| draw 平均值 | 0.27ms |
| draw 最大值 | 1.3ms |
| picking 平均值 | 0.01ms |
| picking 最大值 | 0.1ms |

运行原型：

```powershell
node .\tools\serve-prototype.mjs --port 5400
```

然后访问 `http://127.0.0.1:5400`。

## 接手建议

新智能体接手时，按顺序阅读：

1. `AGENTS.md`
2. `docs/README.md`
3. `docs/current-plan.md`
4. `docs/development-log.md`
5. `graphics-reimplementation-plan.md`
6. `docs/performance/performance-baseline.md`
7. `docs/milestones/milestone-1-webgl-prototype.md`
8. `docs/performance/webgl-svg-performance-comparison.md`
9. `docs/audits/source-generation-audit-and-rectification-plan.md`
10. `docs/task-notes/README.md`
11. `docs/task-notes/editor-and-stat-panel-inventory.md`

然后只按 `docs/current-plan.md` 的未完成权威任务继续；已完成任务只在需要追溯时查询 `docs/task-archives/README.md`，不要把归档当作默认接手必读。当前没有活动权威任务；不得从历史归档、里程碑、README、FOLLOWUPS、矩阵缺口或专题文档的“下一步”自行创造实施任务。规则事务继续遵守既有分层：`cells.inspectAction` 只负责空间 / 原子输入预检，领域 `inspect + execute` 才负责单事务游戏规则，AI planner 只编排多个已授权规则事务。第 195 项继续复用第 200 项现有 `info.describe`、schema registry 与 `objects.*`；独立 prototype 继续作为视觉基线。
