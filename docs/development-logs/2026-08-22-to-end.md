# 开发日志：2026-08-22 至 2026-08 月末

> 本卷保存当前日期片的新增开发记录；旧日志分卷与迁移摘要保持不变。

## 2026-08-28：第 369 项恢复旧浏览器存档包装兼容

- 用户原 Chrome 标签页启动恢复报“文件不是当前地图保存格式”，现场显示浏览器来源被误标成“API 文档”，但原缓存未删除。根因是并行 canonical 主线程解析未复用 Worker 已有的 `webgl-generator-local-map-storage` v1 解包，导致合法 plain / gzip-base64 包装在主线程先失败。
- 浏览器包装现由共享入口识别和解包；非二进制旧档重新使用 Worker canonical handoff 并分片物化，避免主线程同步解析大 JSON，直接二进制 v3 保持现有并行 canonical 快路。地图文档 / v3 schema、持久身份、公开 API 和失败时保留原缓存的契约未改变。
- 真实浏览器旧档门恢复 seed `browser-storage-legacy-v2`、checksum `3429cb16`、`10004` grid cells 与国家“邯郸”，原始缓存逐字节不变；损坏包装仍非破坏失败。首次功能修复后的 `930ms` LongTask 经分片 handoff 收敛为最大 `73ms`，`>200ms = 0`；直接二进制 / fallback 最大 `71ms`。专项、typecheck 与 `1407 modules` build 通过，版本 `0.5.78`。

## 2026-08-28：第 368 项增加显式辖下省份后缀批量事务

- 国家面板新增独立“调整辖下省份后缀”，按当前国家文化策略提供候选；政体修改保持原语义，不自动改写省份。默认保护不等于旧 `name + formName` 的手工定制全名，只有显式开启覆盖才统一改写，并显示改写、无需变化和跳过定制的数量。
- 新命令只更新 `formName / fullName`，同步 politics / pack，单历史提交并按 before-image 撤销 / 重做；无变化成功 no-op，非法文化后缀拒绝。省份公开对象读取补齐两个名称字段，省界、归属、省会、颜色、基础名及其它地理身份不变。
- Node 专项默认为 `1 / 1 / 1`、强制覆盖为 `3 / 0 / 0`，政体不联动、公开读取和相邻政体面板门通过。production UI 首轮暴露后缀状态 watcher 未首次初始化、提交后反馈被清空；补 `immediate: true` 后，国家 `#1` 的 `15` 个省份中 `14` 个标准全名改为“郡”、`1` 个定制全名保持，历史 `0 → 1`，撤销精确恢复。Loading 与 health / console / page / WebGL error 均为 `0`，LongTask 最大 `93ms`、`>200ms = 0`；`1407 modules` build 通过，版本 `0.5.77`。

## 2026-08-28：第 367 项把导入 coverage 缺失改为兼容补建

- 用户纠正第 363 项“对覆盖缺失严格拒绝”的边界：缺失的 cell identity、surface ranges 或基础 geometry 属于可从 canonical grid 恢复的派生数据，应先尝试生成，不应让可读取旧存档直接失败。现把严格校验移到补建之后；只有规范网格不可读取、cell 无法形成安全表面或补建结果仍非法时才结构化拒绝。
- 紧凑构建改按 `grid.cells.i` 逐项产生完整 ranges，缺少原 polygon 顶点的 cell 继续走既有安全三角化回退。render preparation 对 ranges 缺失、中间缺口、尾部截断、geometry 缺失和 identity 缺失五类损坏均从 canonical grid 补建；五类输出和正常路径 geometry checksum、ranges 完全一致，`grid / grid.cells / pack` owner 不变。规范源损坏固定为 `render-surface-compact-rebuild-failed ← grid-cell-surface-source-invalid`。
- 唯一指定 `krichars (3).webfmg`（SHA-256 `CF7402BC2BEA22AD1FCDE441444479F880DC0DB15D55520EF5A1A399D335DA61`）在最终 `v0.5.76` production UI 冷导入 `4185ms`，其中 prepare `3160.5ms`、prepared install `430.5ms`；恢复 `100000 grid / 43419 pack / 1251 cities / 442 routes / 7976 segments`，完整彩色全图和西陆、道路、河流、城镇、标签、国界均正常，Loading `0`、WebGL error `0`，健康窗仅有 `map-ready`。专项、prepared installer、GPU display mutation、typecheck 与 `1407 modules` build 通过。

## 2026-08-28：第 366 项在用户原标签页闭合加载、道路结果与两层柔化

- 唯一使用 `krichars (3).webfmg`（SHA-256 `CF7402BC2BEA22AD1FCDE441444479F880DC0DB15D55520EF5A1A399D335DA61`）和用户原 Chrome 标签页 `294257337`。旧 `5413` 页失败由构建后旧 HTML 内存与新散列资源混装造成，路线面板旧 chunk 已不存在；原截图外框虚化则是上一轮验收遗留的 edge fade 开启状态。
- 海岸最近线段查询改用数值分桶、代际访问标记和平方距离，指定档 topology build 由约 `1.08～1.14s` 降到约 `0.74～0.82s`；首屏安装使用 `16ms / 1MiB` 分片。正式五轮冷导入为 `3949 / 3802 / 3985 / 4068 / 3991ms`，中位 `3985ms`、最大 `4068ms`，五轮完整身份、彩色画面、标签、路线、健康和 WebGL 门同时通过。
- UI 现明确提供“疆界柔化（海岸/行政）”与“整图外框柔化”。外框开关只改变边缘裁剪 `7309 / 55600` 像素、中心 `0`，关→开→关逐像素恢复；疆界开关改变 `6559 / 612521` 像素，开→关→开无阈值以上差异。终态恢复疆界开、外框关。
- 当前路线面板正式重算为 `442 → 438 / 7976 → 7821`；西陆 `108` 条道路、`1440` 道路格、触达 `203 / 350` 城市、`6 / 6` 首都接入、陆路水格 `0`，全图与西陆画面均可见道路网。撤销恢复 `442 / 7976`；本地保存显示完成反馈。专项、typecheck 与 `1407 modules` build 通过，版本 `0.5.75`。
- 三名独立验收者最终全部 `ACCEPT`：加载者五轮中位 `4042ms`、最大 `4198ms` 并逐轮查看完整画面；道路者独立执行 UI、定量、西陆画面和撤销；柔化者首次因首帧取证受面板残影 / 工具超时影响返回 `BLOCK`，blocker-only 复测在导入前关闭面板后确认首个完整帧与 `≥1200ms` 帧原始字节和解码像素差均为 `0`，两类柔化往返也均恢复为 `0` 差异。

## 2026-08-28：第 365 项重构生成校验关系并完成指定存档全入口终验

- 按用户原则建立长期关系文档：旧目标错误不得阻塞主动重生成；显式锁只捕获本体，生成时不可见，结果生成后按稳定 ID 原样覆盖；跨域镜像、父级、首都 / 省会、统计和支撑闭包均为软关系，只有水陆、邻接、有限几何、身份和事务安全是提交硬门。
- 正常锁与内部损坏锁的 `12 × 15` 矩阵均为 `180 / 180`，旧目标损坏容错 `12 / 12`，没有全锁 no-op 或失败。普通生成 10k / 50k / 100k 主要域全部非空，100k 为 `1207` 城镇、`505` 路线、`743` 河流、`43` 市场、`22328` 交易、`12` 洋流。
- 唯一指定存档 `krichars (3).webfmg`（SHA-256 `CF7402BC2BEA22AD1FCDE441444479F880DC0DB15D55520EF5A1A399D335DA61`）底层十二类 `12 / 12`。道路 `442 → 438`，西陆 `108` 条陆路、`1440` 道路格、触达 `203 / 350` 城市，`6 / 6` 首都接入、陆路水格 `0`。
- production UI 将正式十一类、复合六类和直接五类共 `22` 个入口逐项从原档重新导入后验收；实际内容、基础物理、锁定 city `#8` before-image、单历史、撤销 / 重做指纹、Loading、health、console / page / WebGL 全部通过。人工查看路线和最终截图未见黑图、边界第三态或大陆漏路；typecheck、相关协议门、路线质量、经济 Worker 与 `1407 modules` build 通过，版本 `0.5.74`。

## 2026-08-27：第 364 项修复锁定首都导致整片西陆道路归零

- 用户指出上一轮道路验收只证明命令执行、锁、历史和索引，没有检查结果是否覆盖各大陆。唯一存档只读复现确认：西陆 `feature 3` 有 `9732` 个陆地 pack cell、`350` 城市、`6` 首都，且全部城市位于同一可通行连通分量；重生成却从原 `114` 条陆路、`1439` 道路格、触达 `205` 城市变成 `0 / 0 / 0`。
- 根因是显式锁定城市 `8` 的锁优先级闭包合法加入其大陆 feature，但 `buildPackRoutes` 把只读支撑 feature 错误当成道路禁区，同时排除 road / trail。道路不改写 feature，因此现只保留锁路线的边与 owner，不再用 `lockedFeatures` 排除路线分组；全局锁闭包、城市、地形、视觉和其它重生成领域均未改动。
- 确定性专项锁住多首都大陆的一个首都后，`14 / 14` 目标首都接入、`13` 条干道；锁路共享边、10k / 50k / 100k 连通、网络质量、海路同水体、路线 identity、拾取刷新和 render preparation 均 PASS。指定存档 Node 复验西陆为 `110` 条陆路、`1448` 道路格、触达 `209` 城市、`6 / 6` 首都接入，锁定首都原像不变，陆路进入水格 `0`。
- `0.5.73` production 页面两次从正式 UI 导入唯一存档：中央与 RoutePanel 专用入口均为 `442 / 7976 → 432 / 7615`，撤销 / 重做路线数 `432 → 442 → 432`；锁定路线 `#0` 后再重算为 `436 / 7822`，其 `道路 / 庐陵 / 霜寒 / 61 / 1112.6 千米` before-image 与锁状态不变。两次无面板全图均直接看到西陆道路网，console error / warn、WebGL error、Loading 与 busy 均为 `0`。
- 扩大到全领域 `regress:worker-task` 时，既有 `features` handler 出现与道路无关的补丁声明差异；按首败即停规则没有重复全跑或放宽断言。本任务道路专项、指定存档和正式浏览器门全部通过。

## 2026-08-27：第 363 项把真实存档加载速度与完整画面合并验收

- 用户截图证明第 362 项只看加载耗时、身份和错误面仍会接受黑图，因此原加载 `ACCEPT` 作废。唯一输入继续为 `krichars (3).webfmg`（`9,642,587 bytes`，SHA-256 `CF7402BC2BEA22AD1FCDE441444479F880DC0DB15D55520EF5A1A399D335DA61`），完成门改为同一轮同时检查速度、完整 canvas、identity / revision、Loading、WebGL 与健康事件。
- 黑图根因是 compact GPU surface 把没有 cell identity 的 legacy political band 与基础 grid geometry 混装，cell ranges 失去完整覆盖后仍被压成三字顶点，shader 对无效 identity 退回黑色。现禁止 GPU resident surface 混入旧 band，直接生成 position + identity compact geometry，并对 `100000 / 100000` ranges、末尾覆盖和无效顶点 fail closed；指定档 states / provinces 均为 `966,474` 源顶点、无效 identity `0`。
- 加载性能同时移除 100k boundary cell correction 对象图、复用岸线保护关系的已知相交 / near 状态，并让 v3 `≤46 bits` 整数解包走安全 Number 位包路径。指定档 Node 解析约由 `2456ms` 降至 `2003ms`，岸线构建由约 `3.1s` 降至 warm `1.5～1.6s`；岸线仍为 `79` arcs、`5830 / 5665` source / render points，保护和回退统计不变。
- 最终 `0.5.72` production build 五轮正式导入为 `5947 / 5433 / 5496 / 5340 / 5197ms`，中位 `5433ms`、最大 `5947ms`。五轮均为 `imported:n / r0`、10 万 grid、`smooth / canonical`、Loading `0`、WebGL error `0`，健康窗无 LongTask / operation / input / frame 事件。最终画面目标区域 black / near-black ratio 均为 `0`、mean RGB `162.45`、saturated ratio `0.9537`。
- `控制面板 → 视图 → 地图边缘渐隐` 现在只切 draw range；真实档开→关改变 `11.1817%` 目标区域像素，关→开逐像素恢复、差异 `0`，耗时 `draw 0.7ms`。map-file Worker、v3 container、render preparation、prepared installer、GPU display mutation、boundary presentation、typecheck 与 `1406 modules` build 通过。既有 shoreline triangle 阈值在未修改 `8b29e02` 基线同样失败，本项未放宽。

## 2026-08-27：第 362 项闭合三项独立验收阻断并全部通过终验

- 第二轮原验收者复测揭示两个真实阻断：production 五轮导入中位 `6707ms > 6000ms`；道路 redo 后无锁残留 `169 ids / 2055 bucket segments`、加锁残留 `359 ids / 4039 bucket segments`。边界第二轮已在统一实际坐标取得 F0=F1、H0=H1 零像素差并先行 `ACCEPT`，但最终代码变化后仍重新完整验收。
- 导入安装保留分段让出主线程，将后台标签页 rAF 的导入专用 fallback 从 `120ms` 收敛为 `24ms`；硬边界常驻构建复用已封存 `sourceEdges`，缺失或非法时才回退整图邻接扫描。主线程真实档三轮预检为 `4759 / 4910 / 5086ms`；最终独立五轮为 `5392 / 5654 / 7056 / 5789 / 5343ms`，中位 `5654ms`、最大 `7056ms`，目标健康、identity、revision、Loading 与 WebGL 门全部通过。
- 路线历史刷新改为先清空全部 bucket route segments，再从当前路线全集重建；浏览器专项新增 after / undo / redo 三态逐桶审计。最终独立中央 / 专用 × 无锁 / 加锁共十二态的 current routes 外 ID、stale、missing、wrong reference 全为 `0`，段数与 index 精确一致；锁 before-image、端点、pack mirror、共享边 owner、单历史、错误码和健康门通过。
- 最终边界独立验收在 `2215×1073` 页面、DPR `1.125` 下确认 F0=F1、H0=H1 均为 `0` 像素差，smooth / hard 差异 `263430` 像素（`11.083879%`）；sourceEdges 海岸 / 湖岸段 `6328 / 190` 无丢失或重复，首次关闭平滑 `0 / 0.6 / 4.6ms`。原第 359、360、361 项三名验收者对同一冻结 manifest 全部 `ACCEPT`，第 362 项版本为 `0.5.71`。

## 2026-08-27：第 361 项消除首次边界第三态并完成三分支顺序交付

- 指定真实存档逐帧复测确认：第 359 项加载改造后，当前首个正式平滑帧已经能由 UI 关→开恢复，但两路 Worker 的 shore / surface 合并仍没有显示意图契约。现为 prepared render 加入覆盖 map / revision / topology / generation、视图、平滑、edge fade 与主题的指纹；并行合并和首次安装均在提交前 fail closed，surface 可见状态只取自已验证指纹，开发摘要固定显示 canonical。
- 平滑初始 `F0 / F1200 / 两次关开` 固定裁剪像素全为 `e3c30136...5e19c`；硬边界初始 `F0 / F1200 / 开关` 全为 `da59408a...9753`。两个指纹正好对应 UI 的两个合法状态，地图边缘渐隐始终关闭，不存在第三组画面或资源摘要。
- 五轮独立冷导入为 `4733 / 5112 / 5063 / 4963 / 5447ms`，中位 `5063ms`、最大 `5447ms`；每轮精确恢复 `100000 / 43419 / 1251 / 442 / 7976`，canonical true、WebGL error `0`，目标窗口无新增 console error / warn 或产品 LongTask。专项、typecheck 与 `1406 modules` 构建通过；既有 shoreline 三角数量阈值首败未在本项放宽。
- 第 361 项版本为 `0.5.70`，分支 `codex/task-361-initial-boundary-third-state` 从第 360 项完成提交顺序创建，只推送自身，不合入 `main`。至此第 359、360、361 项分别位于三个独立远端分支，`origin/main` 保持不变。

## 2026-08-27：第 360 项路线专用入口闭合等待、结果与错误码事务

- 指定存档真实首败为 RoutePanel 丢弃 Promise：`442 / 7976` 运行约 `2.4s` 时按钮始终可重复点击且无结果，道路核心本身成功。现用本地 request identity + 共享 runtime operation 同步禁用按钮，成功显示前后路线 / 段数及撤销提示，失败复用普通 / 调试统一错误码文案。
- `v0.5.69` 最终构建专用入口双击只产生一次 `442 / 7976 → 273 / 3645` 和一条 history；中央入口结果相同。正式 UI 锁定路线 `#0` 与端点城市 `#7 丰柏 / #18 霜寒` 后结果为 `83 / 1230`，三者 before-image 不变，撤销 / 重做恢复精确，WebGL error `0`。
- 错误归一专项固定 `operation_failed ← worker_regeneration_refresh_fault`：普通界面不泄漏内部码，调试界面显示公开码与内部码。路线专项、锁专项、typecheck、`1405 modules` build 和浏览器 error / warn 门通过；分支只推送不合 `main`，随后从该提交建立第 361 项。

## 2026-08-27：第 359 项真实存档加载进入绝对 6 秒门并归档

- 唯一使用用户指定 `krichars (3).webfmg`。正式 v3 导入不再为 adoption owner 完整再编码；主线程 canonical 解析与双 ComputeWorker 渲染准备并行，首屏 surface 改用 compact geometry + GPU attribute texture，picking 从 committed map 直接建立，后台预热绑定 map / revision 且可由下一次导入取消。
- `v0.5.68` production preview 预热一次后五次独立冷导入墙钟为 `4324 / 4274 / 4725 / 4970 / 5150ms`，中位 `4725ms`、最大 `5150ms`；五轮均精确恢复 `100000 / 43419 / 1251 / 442 / 7976`，目标导入窗口 LongTask / health error / console error-warn / WebGL error 为 `0`。地图点击拾取、路线面板和完整数据导出成功；无遮挡 canvas 在 map-ready 与 `5s` 后像素哈希同为 `cada9b22`。
- 重复导入曾暴露 compact surface 删除旧颜色 buffer 后恢复失效 attribute pointer 的 `WebGL 1282`，已让各绘制路径只绑定自身有效颜色来源。全套专项、typecheck 与 `1404 modules` 构建通过；任务分支只推送，不合入 `main`，随后从该完成提交建立第 360 项独立分支。

## 2026-08-26：登记第 359～361 项——撤销第 358 项三项完成结论并独立复修

- 用户确认真实存档仍然加载缓慢、路线专用入口仍表现为无法重生成，并给出新的决定性反例：首次加载的疆界柔化在关闭“平滑边界”后再次开启也无法恢复。因此该首帧不是受 `smoothCellBorders` 控制的正常柔化，而是导入路径产生的不可逆第三态；第 358 项用首帧 / `1,200ms` 稳定和 edge fade 开关完成验收属于验证对象错误。
- 三个问题拆为独立权威任务并固定顺序：第 359 项重构真实存档加载关键路径，以同存档五轮中位 `≤6.0s`、最大 `≤7.5s` 和产品 `>200ms LongTask = 0` 为绝对门；第 360 项只闭合“路线管理 → 重算道路”的 Promise / busy / feedback / error-code 事务并按真实首败决定是否窄修核心；第 361 项用首次帧、开关往返帧和 surface / shore / line 资源指纹消除不可逆第三态。
- 三项唯一完成输入均为 `C:\Users\mosuzi\Downloads\krichars (3).webfmg`（SHA-256 `CF7402BC2BEA22AD1FCDE441444479F880DC0DB15D55520EF5A1A399D335DA61`）；随机图和小夹具只能作廉价门。每项使用独立分支、独立提交和独立真实浏览器终验，一项证据不得替另一项归档。
- 本次只登记任务和冻结方案，没有修改产品代码、启动实现分支或宣称任何问题已修复。

## 2026-08-26：第 358 项只用用户指定真实存档完成复修与终验

- 唯一验收输入为 `C:\Users\mosuzi\Downloads\krichars (3).webfmg`，文件 `9,642,587 bytes`、SHA-256 `CF7402BC2BEA22AD1FCDE441444479F880DC0DB15D55520EF5A1A399D335DA61`；正式回读为 `100000 grid / 43419 pack / 1251 cities / 442 routes / 7976 segments`。运行时摘要 `2d56dc51` 与旧文档记录 `28eede3c` 口径不同，最终身份以文件哈希和对象计数冻结；随机图不再用于完成证据。
- 导入 Worker 移除解析文档的重复 encode→decode，只编码一次 adoption handoff；首次渲染只交接 boundary-only cell visual、当前岸线 resident 顶点，完整编辑期 cell mesh 和另一岸线模式改为按需惰性建立。真实存档传输 `123,573,771 → 71,422,775 bytes`（`-42.2%`），正式导入 `21,757 → 16,805ms`（`-22.76%`），目标导入窗口无新增 `>200ms` 产品 LongTask。
- runtime busy 统一同步到控制面板；中央十一类和各专用重生成入口加载期间均不再启动并发事务，结构化 `operation_busy` 保留给调试诊断。加载 `1,527ms` 时路线按钮为 disabled；正式路线 `442 → 273`、`3,039ms`，撤销恢复 `442 / 7,976`，历史 `undo 0 / redo 1`，WebGL error `0`。
- 新增独立“地图边缘渐隐”开关并把首次默认改为关闭，不再把 edge fade 硬编码到所有首帧。全新 origin 与真实存档均为“平滑边界开启 / 地图边缘渐隐关闭”；首帧与 `1,200ms` 后画面同为 `74,054 bytes` 且逐字节一致。右缘关闭 / 开启 / 恢复为 `2,468 / 2,635 / 2,468 bytes`，恢复画面逐字节相同。
- map file Worker、render preparation、prepared installer、重生成调试文案专项、typecheck、`1404 modules` build 与差异门通过；完成版本 `0.5.67`，第 358 项归档。

## 2026-08-26：登记第 358 项——撤销随机图验收，回到用户指定真实存档

- 复盘确认第 353、356 项已明确指定 `C:\Users\mosuzi\Downloads\krichars (3).webfmg`（`100000 / 43419 / checksum 28eede3c / routes 442`），且第 356 项明令不得用新图或替代页冒充；第 357 项最终浏览器却使用随机 10k 地图 `296 → 300`，因此道路与首帧完成声明不能成立。
- 生产站已核对为 `v0.5.66`，当前地图现场仍记录 `operation-failed / operation-stall / input-handler-stall / main-thread-long-task`，不能归因于未部署。第 358 项从 `main == origin/main == 659ee73` 建立 `codex/task-358-real-archive-regression`，只认用户指定存档的加载、道路错误码和首帧逐帧证据。
- 冻结 `358-A 真实复现 → 358-B 道路窄修 → 358-C 加载与首帧窄修 → 358-D 本地正式页面同存档终验`。主线程唯一写入，随机图和专项夹具只可作为廉价门，不得再作为完成证据。

## 2026-08-26：第 357 项全部重生成统一契约与本地浏览器终验通过

- 事务 working copy 现统一旧政治 / pack、标记、社会、地区与经济镜像，修复旧 `state.provinces / military` shape、外交 salt、市场中心和普通数组回滚；正式地图只接纳目标领域补丁，失败、撤销和重做保持完整 before-image。
- 十一类生成器统一把锁对象视为只占 identity / cell / edge / endpoint / owner 的保护快照：水陆、中心重叠、扩张、数量、评分、平衡、外交对称 / 单宗主、军团驻地、市场连通和洋流落点等限制只用于未锁对象，合并后不再重复执行；损坏 ID、越界、缺失引用和断裂路径仍精确拒绝。
- 代表性 `krichars (3).webfmg`（`100k grid / 43,419 pack`）正式入口 `11 / 11`、同类锁 / 无锁补测 `22 / 22`、复合入口 `6 / 6`、直接入口 `5 / 5` 通过；干净图 `11 × 15 = 165` 为 `164` 执行、`1` no-op、失败 `0`。最终复合 Worker 含 `100k`、错误码文案、typecheck、`1404 modules` build 与 `git diff --check` 通过。
- 本地 `debug=1` 页面路线正式重生成为 `296 → 300`，目标操作窗口 health、console error / warn 和 WebGL error 均为 `0`；首个正式帧与 `1,200ms` 后稳定帧均为 `94,950 bytes` 且 SHA-256 相同，没有边界先虚化再替换。开发服务器整页刷新曾单列记录一次启动 `generate.newMap 5169.7ms`，发生在目标重生成窗口之前，路线操作没有新增健康事件。完成版本为 `0.5.66`。

## 2026-08-26：登记第 357 项——统一全部重生成入口三阶段契约

- 用户要求把全部重生成统一为“未锁元素按既有限制重算 → 锁定元素跳过限制计算后加入 → 合并不重复验证”，调整完成后自行启动本地服务器、真实浏览器验收并提交推送。第 356 项已以 `2c93e33` 合入并推送远端 `main`，第 357 项从该精确主线建立 `codex/task-357-unified-regeneration-contract`。
- 代表性 `100k grid / 43,419 pack` 旧存档调查覆盖 `22` 个入口：正式十一类 `5 / 11`、复合六类 `1 / 6`、直接五类 `3 / 5` 通过。失败集中于旧镜像、旧闭包 / 首都锚点、外交 salt、军事 shape、标记锁合并改写、社会中心重复限制和经济旧数组回滚二次异常。
- 冻结四阶段：先完成事务 working copy 兼容与原子回滚，再统一正式十一类，随后闭合复合 / 直接入口，最后执行旧存档 `22 / 22`、撤销 / 重做、构建和本地真实浏览器终验。结构 / 引用 / 锁快照最终校验与 MapCoreEngine 安全门保留，地理 / 评分 / 数量等生成限制不对锁对象重复执行。

## 2026-08-26：第 356 项本地修复与真实浏览器终验通过

- 道路正式入口的真实首败不是生成器无路可建，而是旧存档 `politics.states` 与当前 `pack.states` 已分叉；调试模式正确显示公开码 `operation_failed` 和内部码 `settlement-politics-mirror-invalid`。路线 Worker 现于捕获锁后以当前 pack 政治数据收敛旧镜像，再执行既有严格身份、锁 envelope、owner、revision 与提交前校验；合法锁不会静默丢失，若收敛会触及受保护 envelope 仍精确拒绝。
- 代表性 `100k grid / 43,419 pack` 存档正式路线重生成由失败收敛为 `442 → 83`；锁定路线 `#0` 的类型、端点、`65` 段路径和 `1,388.1 km` 长度逐项不变，单历史提交撤销恢复 `442 / 7,976`，失败反馈消失，WebGL error `0`。新增旧政治镜像夹具同时固定共享 state / province 镜像与锁路身份。
- 加载主瓶颈之一是 Worker 把可由已提交 mesh 恢复的逐 cell 边界点、edge curve、shore edge set 和稠密 fallback 冗余克隆回主线程。生成加载改用 `runtime-compact` 传输，只保留正式三角形、ID / 顺序、中心、数量与稀疏 fallback；首次局部刷新仍可从正式 mesh 重建所需边界。另为边界自交检查增加 AABB 早拒绝，结果语义不变。
- 同一存档本地真实浏览器三次导入墙钟为 `16,026.5 / 15,878 / 15,975ms`，中位数由 `20,300ms` 降至 `15,975ms`（`-21.3%`）；传输约 `155.3MB → 108,365,159 bytes`（`-30.2%`），解包 `425ms → 129.7ms`（`-69.48%`）。首次正式可见帧和 `1,200ms` 后稳定帧 PNG 都是 `116,088 bytes` 且逐字节相同，没有边界虚化后再替换；目标窗口产品 `>200ms` LongTask、页面错误和 WebGL error 均为 `0`。
- 路线身份 / 路线锁 / 城镇路线锁 / 网络协议 / 渲染准备 / prepared installer / 调试文案 / API operation 专项、`typecheck:core`、`git diff --check` 与 `1403 modules` 生产构建通过。岸线全量浏览器门因既有 10k smooth 长度覆盖 `0.4474 < 0.5` 首败，按连续夹具失败规则冻结，未在本项顺改算法或阈值。用户授权交付后归档并将版本升至 `0.5.65`。

## 2026-08-25：登记第 356 项——加载、首帧边界与道路重生成修复

- Git 现场确认 `main == origin/main == dd6bb03`，`codex/map-core-engine-architecture-plan` 已成为 `main` 祖先；此前“架构分支不得合入 main”的接手文字已按用户新裁定修正。第 355 项以 `4fd4cf1` 建立本地 checkpoint 并快进合入本地 `main`，第 356 项从该主线建立独立分支。
- 第 356 项冻结四阶段：先在用户既有 Chrome 地图与同存档本地正式入口取得道路错误码、加载分段和首帧 / 稳定帧证据；再依次做道路窄修、加载 / 首帧窄修和集成终验。加载目标为同环境端到端中位数改善至少 `20%`，道路必须保锁成功并可撤销；不得用新地图或替代标签页冒充用户原图。
- 本项不修改 `source/`、存档 schema、公开 API 方法集合或视觉主题，不放宽 MapCoreEngine owner / revision / pre-commit / rollback 门。

## 2026-08-25：第 355 项调试模式显示重新生成错误码

- 控制面板重新生成失败在调试模式中固定显示公开错误码；通用 `operation_failed` 包装同时把原结构化 `code` 保留为 `details.internalCode`，文案可显示去重后的内部码链。普通模式继续只显示既有友好错误文案，不泄漏技术标识。
- 文案专项、运行时操作专项、UI 文案审计和 `1403 modules` 生产构建通过。真实本地页面用并发“重新生成城镇”形成稳定 `operation_busy`：调试模式显示错误码与底层消息，普通模式只显示“当前还有操作正在进行，请稍后再试。”；两边先发操作随后正常完成，新增应用错误为 `0`。
- 任务分支为 `codex/task-355-regeneration-debug-error-code`，完成版本为 `0.5.64`；建立本地 checkpoint 并合入最新 `main` 后再创建下一任务分支。路线重生成底层失败、地图加载性能和边界视觉不在本项范围。

## 2026-08-25：第 354 项并入引擎重构分支

- 冻结 `14` 个面板、`15` 类锁、`22` 个重新生成入口的静态分母，差异为 `0`。新增十一类正式入口 × 十五类锁的 `165` 组交叉矩阵：`164` 组正常执行并保持锁快照，唯一标记全锁场景成功 no-op，失败 `0`。
- 统一建立显式锁和支撑对象闭包，移除国家重生成对合法锁定外交的主动拒绝；政治扩张、城镇落位、省会重选、Feature 拓扑和军事生成均在锁外空间执行。显式社会对象按完整快照恢复，依赖锁仅保结构；Worker / 主线程复合入口在最终提交前统一断言。
- 原修复分支与重构分支均完成国家 / 外交 / 军事直接入口、高度派生、气候下游、洋流世界、海底重建、全锁 no-op 与 `after:rivers` 故障回滚；重构分支复验保留 MapCoreEngine 的 owner、revision、提交前校验和 GPU 快切约束，复合门最大 LongTask `169ms`，应用、页面与 WebGL 错误均为 `0`。本机 Chrome headless 在页面创建前退出，因此夹具保留 Chrome 默认值并增加可控浏览器频道，本次用系统 Edge Chromium 隔离复验。详细证据见 `docs/task-notes/task-354-regeneration-lock-priority.md`，重构分支版本升至 `0.5.63`，未合入 `main`。

## 2026-08-25：第 353 项并入引擎重构分支

- 用户原档路线拒绝的真实原因不是锁冲突：`finalizeSettlements` 漏传锁定城市，使城市 `#8` 的省份 `108 → 96`；更早的港口预处理还会在捕获锁之前改写其港口、类型、资源与视觉。现将路线 / 城市锁快照前移，港口预处理对可读取锁对象只登记跳过，收尾同时传入两类锁；用户原档正式浏览器路线 `442 → 432`，城市 `#8` 与 burg 镜像逐字段不变，单历史提交，LongTask / WebGL / 页面错误为 `0`。
- 多条锁定路线共享同一 pack 边时不再 `duplicate-edge` 拒绝；所有锁路原样保留，单值 route link 优先沿用既有锁路 owner，否则稳定选最小 ID，新路线绕开已占边。无效 ID、镜像缺失、越界和不相邻路径仍精确拒绝。
- 保存主瓶颈为 grid / pack 的 v3 紧凑编码。复用解码所得派生顶点拓扑并以双指纹防止原地变更后误用，合并首次拓扑扫描，同时优化整数位打包、ragged 预分配、小整数域稀疏统计和 checksum 循环；固定用户原档 raw v3 SHA-256 与优化前完全一致。
- 原修复分支正式浏览器三次保存中位数由 `2889.8ms` 降至 `1841.3ms`（`-36.28%`），保存窗口 LongTask `0`；Node v3 编码中位数由 `2052ms` 降至 `1142.2ms`。raw / gzip 往返、压缩 Blob 正式 UI 回读、旧版迁移、完整导出套件、路线锁、港口拓扑、正式构建和真实浏览器均已通过。详细证据见 `docs/task-notes/task-353-map-save-route-lock-priority.md`；移植到引擎重构分支后的版本为 `0.5.62`，未合入 `main`。

## 2026-08-23：第 351 项用户精确标签页终验通过

- 精确 Chrome 标签页 `294256474 / https://preview-fmg.mosuzi.top/` 经用户授权刷新后恢复原地图，没有生成替代地图；控制面板十二类视图全部可进入，人口与高度画面可见，最终恢复 `height / startup ready / Loading 0`。
- 目标复验窗口执行高度→温度→高度，两次切换后的新增 error / warn 均为 `0`。刷新恢复阶段的一条 `operation-stall` 与一条 `main-thread-long-task` 早于复验窗口且未在切换中重现，单列为既有 100k 存档恢复性能边界。
- 第 351 项本地 `10004 / 99846` cells 正式门、latest-revision 调度门与用户原图终验全部闭合；任务归档，分支只推送 `codex/task-351-view-switch-repair`，不直接合入 `main`。

## 2026-08-23：第 351 项本地实现与 10k / 100k 全视图验收通过

- deferred display 的 `surfacePatchScope` 现于 binding 发放前确定，原位颜色补丁保持 render generation；十二类视图全部接入 GPU 常驻 attribute / palette，外交主体只更新小 palette。
- 地图 ready 或正式事务完成后由独立可丢弃 ComputeWorker 预热高度 / 国家 / 省份共用的岸线 correction。队列只接纳最新 revision，旧 running 协作取消或 stale-drop；前台操作清除 queued，长期 MapWorker 不被占用或终止。
- 固定 `10004` cells 的十二类 cold / warm 为 `13.0～26.2ms / 12.7～16.3ms`；固定 `99846` cells 为 `15.5～29.1ms / 15.4～19.2ms`。每项 Worker 数据包、surface / line / picking / labels rebuild、LongTask 与最终错误面均为 `0`。
- 调度器同键 schedule 与无任务 cancel 均幂等，真实 10k sequence 从噪声性的 `4163` 收敛为 `3`。专项、typecheck 与 `1402 modules` production build 通过；版本升至 `0.5.60`，等待推送后在用户精确预览标签页完成最终复核。

## 2026-08-23：登记第 351 项——修复 100k 全视图切换并建立后台预热

- 用户精确 `https://preview-fmg.mosuzi.top/` Chrome 标签页的 100k 地图实测：五类既有 GPU 模式首次仍有约 `2～3s` Loading，其余七类不能切换。现场保持 `height`，health 两次记录 `surface 颜色补丁与正式资源 owner 不属于同一受控 revision`，并有 `2099.9 / 2264.7 / 2488.5ms` frame gap。
- 根因是 deferred display 在外层确定 `surfacePatchScope` 之前，通用 request 已把 surface 误判为 replacement 并推进 render generation；Worker 随后返回原位 `cell-colors`，installer 的严格 owner 门正确拒绝。第 351 项先修请求 binding，不放宽 owner；再完成十二类 GPU 常驻颜色模式，最后建立 latest-revision、前台优先、不会终止共享 MapWorker 的后台共享几何预热。
- 唯一分支为 `codex/task-351-view-switch-repair`，从已推送但未合入 `main` 的 `0.5.58` 架构分支建立。主线程唯一写入，不启用四级流程或子智能体；最终只推送任务分支，不直接合入 `main`。

## 2026-08-23：第 350 项完成 R6b / R7 全量终验并归档

- `R6b` 固定浏览器入口最终 `20 / 20` PASS，覆盖核心事务、10k/100k Worker session、拓扑、锁、拾取、overlay/viewport、导出、旧档、binary fallback、保存与反馈。最终冻结契约为 `16 invariants / 17 scenarios / 20 entries / 19 fixtures`，artifact contract `222 hard assertions / 381 mutations / 5 partitions`。
- `R7` 在同一最终树串联固定 10k、代表性 100k、v2 旧档、稳定故障恢复与 S-17 context restore `5 / 5`。10k 隔离复验未再现单次 `204ms` 抖动；100k 最大 LongTask `70ms`；旧档、故障恢复和 context restore 的目标 LongTask 均为 `0`。
- S-17 首次仅因夹具漏传 `worldToScreen` 的 canvas rect 失败；纯夹具 `R7-f3` 复用同一 rect 完成 world→screen 与 client picking，并以 29 项 R4b mutation 固定数据流。正式复验为 `10004` cells、same map、generation `+1`、唯一 draw、owners/state/pick exact，错误与 Loading 全 `0`。
- 最终 `regress:task-350-r6a`、typecheck 与 `1401 modules` production build PASS；逐一读取 R6b 20 组加 R7 5 组正式 artifact，25/25 的 full/summary 全存在且 summary `ok=true`。任务级最终复核关闭 AGENTS 状态滞后后为 `ACCEPT / P0 0 / P1 0 / P2 0`。
- 归档器首次运行暴露标题兼容缺口：只识别“权威任务第 N 项”，未识别 current-plan 的“第 N 项”，却仍重写为空计划。已恢复第 350 项标准归档摘要，解析器现兼容两种标题、统一归档标题，并在任务样式标题未完全解析时 fail-closed；复跑后新建 `2026-08-22-to-2026-08-31.md`，归档任务 `287`、duplicates `0`、activeInArchives `0`。
- 第 350 项完成版本为 `0.5.58`。分支 `codex/map-core-engine-architecture-plan` 仍以 `origin/main` / 本地 `main` 为祖先且未合入 `main`；混合工作树没有被暂存、提交、推送或合并。
