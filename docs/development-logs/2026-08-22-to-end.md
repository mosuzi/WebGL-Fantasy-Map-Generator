# 开发日志：2026-08-22 至 2026-08 月末

> 本卷保存 2026-08-22 至月末的开发历史；只在追溯具体任务时定向读取。

## 2026-08-24 第 353 项：正式保存性能与路线锁定优先重生成

- 用户原档路线拒绝的真实原因不是锁冲突：`finalizeSettlements` 漏传锁定城市，使城市 `#8` 的省份 `108 → 96`；更早的港口预处理还会在捕获锁之前改写其港口、类型、资源与视觉。现将路线 / 城市锁快照前移，港口预处理对可读取锁对象只登记跳过，收尾同时传入两类锁；用户原档正式浏览器路线 `442 → 432`，城市 `#8` 与 burg 镜像逐字段不变，单历史提交，LongTask / WebGL / 页面错误为 `0`。
- 多条锁定路线共享同一 pack 边时不再 `duplicate-edge` 拒绝；所有锁路原样保留，单值 route link 优先沿用既有锁路 owner，否则稳定选最小 ID，新路线绕开已占边。无效 ID、镜像缺失、越界和不相邻路径仍精确拒绝。
- 保存主瓶颈为 grid / pack 的 v3 紧凑编码。复用解码所得派生顶点拓扑并以双指纹防止原地变更后误用，合并首次拓扑扫描，同时优化整数位打包、ragged 预分配、小整数域稀疏统计和 checksum 循环；固定用户原档 raw v3 SHA-256 与优化前完全一致。
- 正式浏览器三次保存中位数由 `2889.8ms` 降至 `1841.3ms`（`-36.28%`），保存窗口 LongTask `0`；Node v3 编码中位数由 `2052ms` 降至 `1142.2ms`。raw / gzip 往返、最终 `0.5.6` 压缩 Blob 正式 UI 回读、旧版迁移、完整导出套件、路线锁、港口拓扑、正式构建和真实浏览器均通过。详细证据见 `docs/task-notes/task-353-map-save-route-lock-priority.md`，版本升至 `0.5.6`。

## 2026-08-24 第 352 项：启封舆图性能与边界柔化样式

- 从当前 `0.5.4` 分支切出 `codex/map-unseal-performance`，登记第 352 项。冻结用户提供的 `krichars (3).webfmg` 为只读代表样本，并以基线 commit 的隔离 worktree 与当前构建各跑三轮正式 UI 导入。
- 查明初载画面不是快照：prepared installer 原子换入实际地图、政治路径和线 mesh；首个正式帧与稳定帧的政治线签名、中心像素哈希相同。国界 / 省界的蒙眬来自亚像素宽度、低 alpha 与标准透明混合，不是全屏滤镜，也不是 surface smoothing。
- 样式页新增 `0～100` 的“疆界柔化”。`0` 清晰、`50` 精确保留旧视觉、`100` 更轻柔；只刷新政治边界线层。旧图默认 `50`，正式保存 / 导入持久化到既有 metadata section，不增加 v3 section，不改变 checksum、拓扑、对象语义或历史。
- 消除 Worker 完成 v3 解码迁移后再次编码完整接纳 handoff 的重复工作：原始 v3 / gzip v3 复用一次解压后的 canonical section 字节，主线程仍执行相同迁移和投影，JSON / 旧格式保持 fallback。正式 UI 三轮总耗时中位数由 `19,795.5ms` 降至 `17,509.1ms`（`-11.55%`），handoff 编码由 `2,064.6ms` 降至 `0.2ms`，Worker 总耗时由 `12,124.7ms` 降至 `9,879.3ms`；最大 LongTask `143ms`。
- 边界公式、地图迁移、24 段 v3 容器、文件 IO Worker、正式构建、真实浏览器首稳帧 / 三档视觉 / 压缩回读均通过。用户样本 checksum 保持 `28eede3c`，对象摘要、history `undo = 0` 与 WebGL error `0` 保持；详细证据见 `docs/task-notes/task-352-map-unseal-performance.md`。版本升至 `0.5.5`。
