# 开发日志：2026-08-22 至 2026-08 月末

> 本卷保存当前日期片的新增开发记录；旧日志分卷与迁移摘要保持不变。

## 2026-08-26：第 357 项全部重生成统一契约与本地浏览器终验通过

- 事务 working copy 现统一旧政治 / pack、标记、社会、地区与经济镜像，修复旧 `state.provinces / military` shape、外交 salt、市场中心和普通数组回滚；正式地图只接纳目标领域补丁，失败、撤销和重做保持完整 before-image。
- 十一类生成器统一把锁对象视为只占 identity / cell / edge / endpoint / owner 的保护快照：水陆、中心重叠、扩张、数量、评分、平衡、外交对称 / 单宗主、军团驻地、市场连通和洋流落点等限制只用于未锁对象，合并后不再重复执行；损坏 ID、越界、缺失引用和断裂路径仍精确拒绝。
- 代表性 `krichars (3).webfmg`（`100k grid / 43,419 pack`）正式入口 `11 / 11`、同类锁 / 无锁补测 `22 / 22`、复合入口 `6 / 6`、直接入口 `5 / 5` 通过；干净图 `11 × 15 = 165` 为 `164` 执行、`1` no-op、失败 `0`。最终复合 Worker 含 `100k`、错误码文案、typecheck、`1404 modules` build 与 `git diff --check` 通过。
- 本地 `debug=1` 页面路线正式重生成为 `296 → 300`，目标操作窗口 health、console error / warn 和 WebGL error 均为 `0`；首个正式帧与 `1,200ms` 后稳定帧均为 `94,950 bytes` 且 SHA-256 相同，没有边界先虚化再替换。开发服务器整页刷新曾单列记录一次启动 `generate.newMap 5169.7ms`，发生在目标重生成窗口之前，路线操作没有新增健康事件。完成版本为 `0.5.66`。

## 2026-08-26：登记第 357 项——统一全部重生成入口三阶段契约

- 用户要求把全部重生成统一为“未锁元素按既有限制重算 → 锁定元素跳过限制计算后加入 → 合并不重复验证”，调整完成后自行启动本地服务器、真实浏览器验收并提交推送。第 356 项已以 `2c93e33` 合入并推送远端 `main`，第 357 项从该精确主线建立 `codex/task-357-unified-regeneration-contract`。
- 代表性 `100k grid / 43,419 pack` 旧存档调查覆盖 `22` 个入口：正式十一类 `5 / 11`、复合六类 `1 / 6`、直接五类 `3 / 5` 通过。失败集中于旧镜像、旧闭包 / 首都锚点、外交 salt、军事 shape、标记锁合并改写、社会中心重复限制和经济旧数组回滚二次异常。
- 冻结四阶段：先完成事务 working copy 兼容与原子回滚，再统一正式十一类，随后闭合复合 / 直接入口，最后执行旧存档 `22 / 22`、撤销 / 重做、构建和本地真实浏览器终验。结构 / 引用 / 锁快照最终校验与 MapCoreEngine 安全门保留，地理 / 评分 / 数量等生成限制不对锁对象重复执行。

## 2026-08-26：第 356 项本地修复与真实浏览器终验通过

- 道路正式入口的真实首败不是生成器无路可建，而是旧存档 `politics.states` 与当前 `pack.states` 已分叉；调试模式正确显示公开码 `operation_failed` 和内部码 `settlement-politics-mirror-invalid`。路线 Worker 现于捕获锁后以当前 pack 政治数据收敛旧镜像，再执行既有严格身份、锁 envelope、owner、revision 与提交前校验；合法锁不会静默丢失，若收敛会触及受保护 envelope 仍精确拒绝。
- 代表性 `100k grid / 43,419 pack` 存档正式路线重生成由失败收敛为 `442 → 83`；锁定路线 `#0` 的类型、端点、`65` 段路径和 `1,388.1 km` 长度逐项不变，单历史提交撤销恢复 `442 / 7,976`，失败反馈消失，WebGL error `0`。新增旧政治镜像夹具同时固定共享 state / province 镜像与锁路身份。
- 加载主瓶颈之一是 Worker 把可由已提交 mesh 恢复的逐 cell 边界点、edge curve、shore edge set 和稠密 fallback 冗余克隆回主线程。生成加载改用 `runtime-compact` 传输，只保留正式三角形、ID / 顺序、中心、数量与稀疏 fallback；首次局部刷新仍可从正式 mesh 重建所需边界。另为边界自交检查增加 AABB 早拒绝，结果语义不变。
- 同一存档本地真实浏览器三次导入墙钟为 `16,026.5 / 15,878 / 15,975ms`，中位数由 `20,300ms` 降至 `15,975ms`（`-21.3%`）；传输约 `155.3MB → 108,365,159 bytes`（`-30.2%`），解包 `425ms → 129.7ms`（`-69.48%`）。首次正式可见帧和 `1,200ms` 后稳定帧 PNG 都是 `116,088 bytes` 且逐字节相同，没有边界虚化后再替换；目标窗口产品 `>200ms` LongTask、页面错误和 WebGL error 均为 `0`。
- 路线身份 / 路线锁 / 城镇路线锁 / 网络协议 / 渲染准备 / prepared installer / 调试文案 / API operation 专项、`typecheck:core`、`git diff --check` 与 `1403 modules` 生产构建通过。岸线全量浏览器门因既有 10k smooth 长度覆盖 `0.4474 < 0.5` 首败，按连续夹具失败规则冻结，未在本项顺改算法或阈值。用户授权交付后归档并将版本升至 `0.5.65`。

## 2026-08-25：登记第 356 项——加载、首帧边界与道路重生成修复

- Git 现场确认 `main == origin/main == dd6bb03`，`codex/map-core-engine-architecture-plan` 已成为 `main` 祖先；此前“架构分支不得合入 main”的接手文字已按用户新裁定修正。第 355 项以 `4fd4cf1` 建立本地 checkpoint 并快进合入本地 `main`，第 356 项从该主线建立独立分支。
- 第 356 项冻结四阶段：先在用户既有 Chrome 地图与同存档本地正式入口取得道路错误码、加载分段和首帧 / 稳定帧证据；再依次做道路窄修、加载 / 首帧窄修和集成终验。加载目标为同环境端到端中位数改善至少 `20%`，道路必须保锁成功并可撤销；不得用新地图或替代标签页冒充用户原图。
- 本项不修改 `source/`、存档 schema、公开 API 方法集合或视觉主题，不放宽 MapCoreEngine owner / revision / pre-commit / rollback 门。

## 2026-08-25：第 355 项调试模式显示重新生成错误码

- 控制面板重新生成失败在调试模式中固定显示公开错误码；通用 `operation_failed` 包装同时把原结构化 `code` 保留为 `details.internalCode`，文案可显示去重后的内部码链。普通模式继续只显示既有友好错误文案，不泄漏技术标识。
- 文案专项、运行时操作专项、UI 文案审计和 `1403 modules` 生产构建通过。真实本地页面用并发“重新生成城镇”形成稳定 `operation_busy`：调试模式显示错误码与底层消息，普通模式只显示“当前还有操作正在进行，请稍后再试。”；两边先发操作随后正常完成，新增应用错误为 `0`。
- 任务分支为 `codex/task-355-regeneration-debug-error-code`，完成版本为 `0.5.64`；建立本地 checkpoint 并合入最新 `main` 后再创建下一任务分支。路线重生成底层失败、地图加载性能和边界视觉不在本项范围。

## 2026-08-25：第 354 项并入引擎重构分支

- 冻结 `14` 个面板、`15` 类锁、`22` 个重新生成入口的静态分母，差异为 `0`。新增十一类正式入口 × 十五类锁的 `165` 组交叉矩阵：`164` 组正常执行并保持锁快照，唯一标记全锁场景成功 no-op，失败 `0`。
- 统一建立显式锁和支撑对象闭包，移除国家重生成对合法锁定外交的主动拒绝；政治扩张、城镇落位、省会重选、Feature 拓扑和军事生成均在锁外空间执行。显式社会对象按完整快照恢复，依赖锁仅保结构；Worker / 主线程复合入口在最终提交前统一断言。
- 原修复分支与重构分支均完成国家 / 外交 / 军事直接入口、高度派生、气候下游、洋流世界、海底重建、全锁 no-op 与 `after:rivers` 故障回滚；重构分支复验保留 MapCoreEngine 的 owner、revision、提交前校验和 GPU 快切约束，复合门最大 LongTask `169ms`，应用、页面与 WebGL 错误均为 `0`。本机 Chrome headless 在页面创建前退出，因此夹具保留 Chrome 默认值并增加可控浏览器频道，本次用系统 Edge Chromium 隔离复验。详细证据见 `docs/task-notes/task-354-regeneration-lock-priority.md`，重构分支版本升至 `0.5.63`，未合入 `main`。

## 2026-08-25：第 353 项并入引擎重构分支

- 用户原档路线拒绝的真实原因不是锁冲突：`finalizeSettlements` 漏传锁定城市，使城市 `#8` 的省份 `108 → 96`；更早的港口预处理还会在捕获锁之前改写其港口、类型、资源与视觉。现将路线 / 城市锁快照前移，港口预处理对可读取锁对象只登记跳过，收尾同时传入两类锁；用户原档正式浏览器路线 `442 → 432`，城市 `#8` 与 burg 镜像逐字段不变，单历史提交，LongTask / WebGL / 页面错误为 `0`。
- 多条锁定路线共享同一 pack 边时不再 `duplicate-edge` 拒绝；所有锁路原样保留，单值 route link 优先沿用既有锁路 owner，否则稳定选最小 ID，新路线绕开已占边。无效 ID、镜像缺失、越界和不相邻路径仍精确拒绝。
- 保存主瓶颈为 grid / pack 的 v3 紧凑编码。复用解码所得派生顶点拓扑并以双指纹防止原地变更后误用，合并首次拓扑扫描，同时优化整数位打包、ragged 预分配、小整数域稀疏统计和 checksum 循环；固定用户原档 raw v3 SHA-256 与优化前完全一致。
- 原修复分支正式浏览器三次保存中位数由 `2889.8ms` 降至 `1841.3ms`（`-36.28%`），保存窗口 LongTask `0`；Node v3 编码中位数由 `2052ms` 降至 `1142.2ms`。raw / gzip 往返、压缩 Blob 正式 UI 回读、旧版迁移、完整导出套件、路线锁、港口拓扑、正式构建和真实浏览器均已通过。详细证据见 `docs/task-notes/task-353-map-save-route-lock-priority.md`；移植到引擎重构分支后的版本为 `0.5.62`，未合入 `main`。

## 2026-08-23：第 351 项用户精确标签页终验通过

- 精确 Chrome 标签页 `294256474 / https://preview-fmg.mosuzi.top/` 经用户授权刷新后恢复原地图，没有生成替代地图；控制面板十二类视图全部可进入，人口与高度画面可见，最终恢复 `height / startup ready / Loading 0`。
- 目标复验窗口执行高度→温度→高度，两次切换后的新增 error / warn 均为 `0`。刷新恢复阶段的一条 `operation-stall` 与一条 `main-thread-long-task` 早于复验窗口且未在切换中重现，单列为既有 100k 存档恢复性能边界。
- 第 351 项本地 `10004 / 99846` cells 正式门、latest-revision 调度门与用户原图终验全部闭合；任务归档，分支只推送 `codex/task-351-view-switch-repair`，不直接合入 `main`。

## 2026-08-23：第 351 项本地实现与 10k / 100k 全视图验收通过

- deferred display 的 `surfacePatchScope` 现于 binding 发放前确定，原位颜色补丁保持 render generation；十二类视图全部接入 GPU 常驻 attribute / palette，外交主体只更新小 palette。
- 地图 ready 或正式事务完成后由独立可丢弃 ComputeWorker 预热高度 / 国家 / 省份共用的岸线 correction。队列只接纳最新 revision，旧 running 协作取消或 stale-drop；前台操作清除 queued，长期 MapWorker 不被占用或终止。
- 固定 `10004` cells 的十二类 cold / warm 为 `13.0～26.2ms / 12.7～16.3ms`；固定 `99846` cells 为 `15.5～29.1ms / 15.4～19.2ms`。每项 Worker 数据包、surface / line / picking / labels rebuild、LongTask 与最终错误面均为 `0`。
- 调度器同键 schedule 与无任务 cancel 均幂等，真实 10k sequence 从噪声性的 `4163` 收敛为 `3`。专项、typecheck 与 `1402 modules` production build 通过；版本升至 `0.5.60`，等待推送后在用户精确预览标签页完成最终复核。

## 2026-08-23：登记第 351 项——修复 100k 全视图切换并建立后台预热

- 用户精确 `https://preview-fmg.mosuzi.top/` Chrome 标签页的 100k 地图实测：五类既有 GPU 模式首次仍有约 `2～3s` Loading，其余七类不能切换。现场保持 `height`，health 两次记录 `surface 颜色补丁与正式资源 owner 不属于同一受控 revision`，并有 `2099.9 / 2264.7 / 2488.5ms` frame gap。
- 根因是 deferred display 在外层确定 `surfacePatchScope` 之前，通用 request 已把 surface 误判为 replacement 并推进 render generation；Worker 随后返回原位 `cell-colors`，installer 的严格 owner 门正确拒绝。第 351 项先修请求 binding，不放宽 owner；再完成十二类 GPU 常驻颜色模式，最后建立 latest-revision、前台优先、不会终止共享 MapWorker 的后台共享几何预热。
- 唯一分支为 `codex/task-351-view-switch-repair`，从已推送但未合入 `main` 的 `0.5.58` 架构分支建立。主线程唯一写入，不启用四级流程或子智能体；最终只推送任务分支，不直接合入 `main`。

## 2026-08-23：第 350 项完成 R6b / R7 全量终验并归档

- `R6b` 固定浏览器入口最终 `20 / 20` PASS，覆盖核心事务、10k/100k Worker session、拓扑、锁、拾取、overlay/viewport、导出、旧档、binary fallback、保存与反馈。最终冻结契约为 `16 invariants / 17 scenarios / 20 entries / 19 fixtures`，artifact contract `222 hard assertions / 381 mutations / 5 partitions`。
- `R7` 在同一最终树串联固定 10k、代表性 100k、v2 旧档、稳定故障恢复与 S-17 context restore `5 / 5`。10k 隔离复验未再现单次 `204ms` 抖动；100k 最大 LongTask `70ms`；旧档、故障恢复和 context restore 的目标 LongTask 均为 `0`。
- S-17 首次仅因夹具漏传 `worldToScreen` 的 canvas rect 失败；纯夹具 `R7-f3` 复用同一 rect 完成 world→screen 与 client picking，并以 29 项 R4b mutation 固定数据流。正式复验为 `10004` cells、same map、generation `+1`、唯一 draw、owners/state/pick exact，错误与 Loading 全 `0`。
- 最终 `regress:task-350-r6a`、typecheck 与 `1401 modules` production build PASS；逐一读取 R6b 20 组加 R7 5 组正式 artifact，25/25 的 full/summary 全存在且 summary `ok=true`。任务级最终复核关闭 AGENTS 状态滞后后为 `ACCEPT / P0 0 / P1 0 / P2 0`。
- 归档器首次运行暴露标题兼容缺口：只识别“权威任务第 N 项”，未识别 current-plan 的“第 N 项”，却仍重写为空计划。已恢复第 350 项标准归档摘要，解析器现兼容两种标题、统一归档标题，并在任务样式标题未完全解析时 fail-closed；复跑后新建 `2026-08-22-to-2026-08-31.md`，归档任务 `287`、duplicates `0`、activeInArchives `0`。
- 第 350 项完成版本为 `0.5.58`。分支 `codex/map-core-engine-architecture-plan` 仍以 `origin/main` / 本地 `main` 为祖先且未合入 `main`；混合工作树没有被暂存、提交、推送或合并。
