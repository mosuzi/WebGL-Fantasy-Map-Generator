# 开发日志：2026-08-15 至 2026-08-21

> 本卷保存当前日期片的新增开发记录；旧日志分卷与迁移摘要保持不变。

## 2026-08-20：登记第 350 项——MapCoreEngine / TypeScript CDP 全量浏览器验收

- 用户单独批准执行第 349 项此前只评估、未运行的浏览器验收方案。第 350 项不重开架构范围，不合入 `main`，不接管用户标签页；按环境冻结、10k 核心事务、100k 与 session、渲染 / 拾取 / 导出、兼容 / 反馈 / 综合终验五阶段串行。
- 固定执行 20 个既有 browser regression，并补一轮直接 CDP 串联；每组先跑 smoke，首败即停，只允许一次窄诊断和一次目标复验。详细日志、trace、截图和 JSON 统一进入 `Z:\tmp\codex\2026-08-20\task-350-map-core-cdp`，没有命中的断言不得记为通过。
- `350-0` 确认 20 个入口全部存在，`typecheck:core`、27 门防误触审计和 production build 通过；直接页面与干净 `regress:map-transaction-browser` 均在首次 `generate.newMap` 因 foundation binding 缺少 identity 被拒绝。该失败已确认是冷启动产品阻断，动态插入 `350-0a`，仅允许让 generation / import adoption 在尚无 canonical map 时使用其临时 owner identity，不放宽 validator。
- `350-0a` 首次修正后目标门仍失败；只读复核确认请求 binding 已合法，但 Worker 返回后的 current-binding 检查再次从空 revision owner 建立 binding。补齐范围限制为冷启动时只接受 `generated:<正整数>` / `imported:<正整数>` 候选 identity；一旦已有地图，fallback 被忽略，既有 stale 拒绝保持。
- 第二次补齐后 `regress:map-transaction-browser` 第三次仍以同一 foundation binding 错误失败，说明 adoption preload / refresh 中 state map 与 revision owner 之间还有未建模的 pending 窗口。按停止条件不再重跑，后续 19 门未执行；两次未通过的产品与测试改动全部撤回。第 350 项保持 `BLOCK`，后续需先建立显式 pending adoption binding owner，而不是继续追加局部 fallback。
- 用户批准后，`350-0a / 0.5.58` 候选建立 request、Worker pending、load、commit 与 invalidate 单一 owner；operation / token 连续性和双 revision 零起点均有负例，原严格 validator 未放宽。owner / foundation 专项、core typecheck 与 `1393 modules` build 通过，第一轮干净 Chromium 已越过原冷启动阻断。
- 同轮在 cities 暴露 history restore 后 `politics / pack` 只值相同、不再共享引用时的统计镜像缺口，因而插入 `350-0b`。城镇统计改为对两侧 states / provinces 容器分别重算，非同引用 Node 夹具及城镇地区协议门通过；限定第二轮浏览器已越过 cities，但在 provinces 再被 state 镜像拒绝。
- 省份首败精确到 `buildPackProvinces` 改写 `pack.states[*].provinces`，而 result 未携 states，导致 `applyPoliticsRegenerationResult` 只重绑 provinces。因同一浏览器门已连续两次失败，按硬止损冻结，不启动第三轮；当前 `350-0b BLOCK`，后续 19 个浏览器入口未执行。最窄恢复范围是显式提交省份任务真实改写的 states 镜像并补非同引用正例，不放宽 validator。
- 同一只读评审最终为 `BLOCK / P0 0 / P1 3`：除省份漏交 states 外，竞争 map-replace 的 `operation_busy` rejected Promise 会误清首个任务 owner；Worker / owner 已 commit 后 `preparedInstall.finalize()` 抛错仍会让外层回滚主线程，形成 Worker 新图 / 主线程旧图分叉。后续范围冻结为 operationId 条件失效、post-commit best-effort finalize、provinces result 显式 states 三项及对应 Node 反例；需用户重新明确授权后才允许一次目标浏览器复验。
- 用户授权后三项最窄闭合已完成：busy 请求只有成功取得自身 operationId 后才有权清理匹配 owner；post-commit finalize 降为 best-effort；省份 result 显式携带 states。owner、城镇、society-politics、typecheck、diff-check、`1393 modules` build 通过，同一只读评审给出 `ACCEPT / P0 0 / P1 0`，未运行浏览器。
- 用户为当前目标浏览器门新增最多 `10` 次复验额度。重试 `1 / 10` 已越过冷启动、cities、states、provinces，首败推进到 markers 的国家镜像门。动态插入 `350-0c`：资源点经济重算在 `politics / pack` 引用分离时显式同步两侧 states / provinces；分离引用 Node 正例及 `regress:features-networks-resources-core-protocol` 已通过，待独立评审后才启动重试 `2 / 10`。
- `350-0c` 首轮评审为 `BLOCK / P1 1`：通用 marker writer 使直接命令真实改写 politics，但 Manifest 与旧快照未覆盖，撤销不能恢复原分离对象图。因而插入 `350-0c-a`，将 politics 纳入直接命令 / resource-economy 写集，并用受影响子根的一次 structured clone 保存精确 before-image 和共享关系；不采用整图快照。
- `350-0c-a` 已由 Node 证明 Worker patch、直接 command、chunked success、chunked failure 四条分离镜像路径的提交 / 撤销 / 重做 / 回滚。地理网络资源协议、markers、零资源、锁、core Manifest / dependency、typecheck 和 diff-check 通过；一次会输出巨型差异的整图断言测试进程已明确终止并收窄为受影响域 SHA-256。当前待同一评审 blocker-only 复审，浏览器额度仍为 `1 / 10`。
- blocker-only 复审对 `350-0c-a` 给出 `ACCEPT / P0 0 / P1 0`，确认直接命令写集、依赖方向、精确 before-image 与四条事务路径均闭合；独立复跑五门通过且浏览器执行 `0`。现进入目标浏览器门重试 `2 / 10`。
- 重试 `2 / 10` 已越过 markers，首败推进到 diplomacy preparedRender binding。动态插入 `350-0d`：world-systems 不再把正式三字段 legacy renderer binding 误当成完整 compute binding，但仍严格校验 schema、identity、map revision 和 topology revision；首轮评审以 `P1` 发现 `Number(...)` 会错误接受 null / 数字字符串，严格 number 修正及对应反例经 blocker-only 复审获 `ACCEPT / P0 0 / P1 0`，现启动重试 `3 / 10`。
- 重试 `3 / 10` 已越过 diplomacy，首败推进到高度基础派生 render request，错误为没有 `height-derived` 的 picking 准备范围。动态插入 `350-0e`：height-derived、climate-downstream、ocean-current-world、grid-topology 四类完整地图复合任务显式使用全部五类 picking components，未知 kind 继续硬拒绝；Node 与评审通过前不启动重试 `4 / 10`。
- `350-0e` 的 10k render-preparation 门通过；扩展 composite 诊断先修正旧两字段 prepared binding fixture，随后通过 generation、height、climate、ocean 的 Worker / fallback / patch / undo-redo，在锁保护尾段发现 height features 会移除锁定河流 lake outlet 镜像。该 P1 归入既定锁验收，插入并重排 `350-2a` 到 350-1 后、350-2 前；当前 picking 阶段不冒充 composite 全绿。
- `350-0e` 经同一评审确认五族 picking 范围、完整安装、未知 kind 拒绝和 topology fixture 均正确，结论 `ACCEPT / P0 0 / P1 0`；独立 foundation core、typecheck、diff-check 通过且浏览器执行 `0`。现启动目标门重试 `4 / 10`。
- 重试 `4 / 10` 已完成全部 map transaction 与 UI 撤销重做，终验仅剩两次未处理的地图副本 checksum / revision 错误。动态插入 `350-0f`：修正 pendingSessionPatch 的失败链，前项失败后仍立即接管并消费后续 patch / binding Promise；会话已销毁时合法后项返回 false，自身拒绝归还当前调用，下一 Worker run 可重同步，不向普通页面泄漏内部副本错误。
- `350-0f` 经完整 worker-task、typecheck、build、diff-check 和同一独立评审接受，结论 `ACCEPT / P0 0 / P1 0`；当前 patch / binding 拒绝仍原样返回，只有前项屏障失败被吸收，浏览器执行 `0`。现启动目标门重试 `5 / 10`。
- 重试 `5 / 10` 的功能断言全部通过：11 类重生成、3 类高度派生、GEO、气候、UI 单事务及撤销重做成立，GL / 应用 console / page error 均为 `0`。但目标窗出现仅在 `>=250ms` 才上报的 `main-thread-long-task`，已超过第 350 项 `>200ms` 硬线；因此插入 `350-1a`，先补精确 PerformanceObserver 证据与硬断言，再做一次窄归因和实质优化，350-1 其余四门暂不提前运行。
- 重试 `6 / 10` 的精确 PerformanceObserver 硬门按预期失败：14 个主链 LongTask 为 `1.06–1.221s`，全派生为 `3.624s`，UI 三项为 `352–364ms`，health `operation = null`；它们全部落在夹具同步 `JSON.stringify(app.map)` before / after / undo / redo 指纹窗口。350-1a 已改用 canonical registry 的 `4ms` budget 分块 checksum，仍比较全 canonical map 且按单调 revision 失效缓存；专项 checksum、语法和 diff 门通过。首轮独立评审代码无 P0 / P1，仅以权威文档少记本轮证据给出 `BLOCK / P1 1`；当前待文档 blocker-only 复审，尚未宣称性能收敛，额度使用 `6 / 10`。
- 文档 blocker-only 复审接受 350-1a 后，重试 `7 / 10` 确认同步 stringify 的秒级阻塞消失，但仍有 `49` 个候选 LongTask。初版 `1014 grid / 906 pack` Node 对照低于公共入口实际 10k，已更正为 `10004 / 5720`：canonical checksum `240` 次 yield、最大同步 slice `4.9ms`。插入的 `350-1b` 子阶段夹具经独立评审接受后执行重试 `8 / 10`，把 `49` 项拆为 `45` 个测试专用 fingerprint 和 `4` 个产品 history 项；产品为 markers undo `343ms` 及 UI undo / redo / baseline-undo `423 / 350 / 360ms`。
- 产品根因是 history mutation 在主线程同步 `structuredClone(writes)` 后才排副本 patch。现改用既有 Worker graph 分包克隆；10k states Node 夹具 `38` 次 yield / `12` 包，保留共享引用并与活动地图脱离，捕获期间 revision / map 漂移则作废，空写集仍 invalidate。测试专用 fingerprint 超线继续记入 trace 但不冒充产品性能；map-replica、worker-task、typecheck、`1393 modules` build、语法和 diff 门通过，待同一评审后只复验目标门 `9 / 10`，额度已用 `8 / 10`。
- 同一独立评审对 350-1b 给出 `ACCEPT / P0 0 / P1 0`。重试 `9 / 10` 证明后台/API history 产品 LongTask 已清零，但国家面板打开后的 UI undo / redo / baseline-undo 仍为 `348 / 328 / 353ms`；功能、GL、应用 console 与 page error 继续成立，因而插入 `350-1c`。
- 350-1c 首轮按静态差异把根因归为国家面板 Vue 投影并实现线性索引；新增 UI-only CDP 后仍测得 `424 / 354 / 359ms`，该假设被实证否定，projection 产品、专项和 package 入口均完整回退。profile 进一步显示 history / panel 本身不足 `1ms`，阻塞实际来自 `selectionStore.batch 359–439ms` 内的 `refreshTerrainCaches 281–337ms`，且 terrain 与 scheduler 重复刷新 surface / lines。
- 最窄修正把 regeneration history 的 terrain、line、point、object-index、surface/draw、labels、selection 拆成可让出主线程的阶段，terrain 不再内嵌重复 surface / line；异步恢复期间 header 禁用 undo / redo。首轮独立评审仍以 `P1 2` 阻断：实现只核对 map 引用而未冻结 revision，且名称库 / 军事面板同步读取 Promise。现已增加 committed identity/revision guard，在 scheduler、panel、afterRefresh 与最终返回逐段复核；两类消费者用统一 settlement 保留同步形状并等待异步 resolve / reject。Node 反例确认首个 yield 推进 revision 会在 terrain 前终止，Promise 两种终态均收敛。
- 修正后最终产品树 UI-only CDP 的 history delta `1`、undo / redo / baseline-undo 恢复成立，产品 `>200ms` LongTask `0`、最大分段 `142ms`，GL / 应用 console / page error 均为 `0`。map-replica 为 `35 yields / 12 packets / max 7.024ms`，affected-summary、panel-refresh、完整 worker-task、typecheck、`1394 modules` build 与 diff-check 通过；同一 blocker-only 评审结论 `ACCEPT / P0 0 / P1 0`，聚焦诊断不消耗完整额度，现运行最后一次 `10 / 10`。
- 重试 `10 / 10` 在首个 `features` 撤销立即停止：regeneration history 现按契约返回 Promise，map transaction 夹具却在 settle 前同步 `unwrap`，只得到 `unknown`。该轮尚未进入 routes 或后续领域，没有产品事务 / LongTask 结论；动态插入的 `350-1d` 只调整六处 history receipt 为 await 后解包，Node 语法、diff-check 与同一独立评审结论 `ACCEPT / P0 0 / P1 0`，浏览器执行 `0`。既定 `10` 次额度如实记为已用尽。
- 用户随后为当前 map transaction 目标门追加授权 `5` 次完整复验；仍按首败即停、最窄归因、Node 反例与同一独立评审顺序执行，现启动追加额度 `1 / 5`。
- 追加复验 `1 / 5` 的 11 类重生成、3 类高度派生、GEO、气候与 UI 单 history、首次 undo / redo、climate `operation_busy`、产品 `>200ms` LongTask 与错误面均通过；16 个测试专用 fingerprint 超线项独立报告，最大 `952ms`，产品最大可见分段 `170ms`。独立评审仍以 `P1 1` 阻断：UI 最终 baseline 只等待 click，没有等待异步 history settle 或复算 canonical checksum；通用 API / climate 最终 baseline 也只凭 receipt。动态插入 `350-1e` 补齐三类最终基线证明，复审前不运行浏览器。
- `350-1e` 已补齐 UI final undo 的 pending settle、canonical fingerprint 与 undo history 基线，以及通用 API / climate 的最终 fingerprint / reference；Node 语法、diff-check 与同一独立评审为 `ACCEPT / P0 0 / P1 0`，浏览器执行 `0`。现使用追加额度 `2 / 5` 完整复验 map transaction。
- 追加复验 `2 / 5` 完整通过 map transaction：11 类重生成、3 类高度派生、GEO、climate、UI 的单 history、首次 undo / redo 与最终 baseline checksum/reference 均成立，UI 明确为 `undoRestored / redoRestored / baselineRestored = true / true / true`，climate 并发仍返回 `operation_busy`。产品 `>200ms` LongTask、GL、应用 console、page error 均为 `0`；测试专用 fingerprint 超线 `16` 项、最大 `980ms`，产品最大可见分段 `157ms`。同一独立评审结论 `ACCEPT / P0 0 / P1 0`，现进入普通 Worker regeneration 浏览器门。
- 普通 Worker regeneration 门首败于夹具同步读取异步 history receipt，报 `undo失败：unknown`；产品 Worker 结果未判定失败。动态插入 `350-1f`，修正该脚本两处 undo/redo await，并一次核对 population / social / economy 三门各自 `runApi / spanAsync` 已等待 task Promise，避免后续同类空转；复审前不再启动浏览器。
- `350-1f` 已修正 Worker regeneration 两处 history await，并确认 population `runApi`、social / economy `spanAsync` 均等待 Promise；四脚本语法、diff-check 与同一独立评审结论 `ACCEPT / P0 0 / P1 0`，浏览器执行 `0`。现只复验普通 Worker regeneration 首败门。
- 复验越过 history Promise 后首次在 diplomacy pre-commit 命中 `zone #0 身份槽无效`。根因是 war consistency 合法过滤失效 Warzone 后压紧数组并保留持久 id，而 settlement-zone validator 错把地区紧凑集合当作 id 槽数组。动态插入 `350-1g`；首轮独立评审拦截 `Number(...)` 强转误收 `null / false / 空串 / 数字字符串` 的 `P1`。最窄修正现分别严格校验原始 `i/id` 为 number 类型的非负安全整数，再检查一致性与唯一性，并补齐 missing/null/boolean/empty-string/numeric-string 负例；cells、镜像、战争引用和 metadata 不放宽。两组协议门、typecheck、diff-check 与 blocker-only 复核通过，结论 `ACCEPT / P0 0 / P1 0`，浏览器执行 `0`；现只复验普通 Worker regeneration 首败门。
- 普通 Worker 复验第一次因 `dist` 仍是 350-1g 前产物而重复旧 zone 文案；`1394 modules` 重建与 bundle 字符串核对后目标复验越过 diplomacy，首次在连续 `features → states` 命中孤立省会引用。动态插入 `350-1h`；聚焦 CDP 证明 features session 复用成功，states 在失效旧 session 后从 canonical 新建 session 仍同样失败，故不是副本残留。Node 固定 `states-chain-seed-5 / 2k` 定位到 city `317` / burg `318`：`province = 0` 但 `provincial = true`。非锁定城市归属中立省份时现同步清除 city / burg 两侧标记，正式连续链正例覆盖同一 before-image；society-politics 全套、settlements/zones、typecheck、浏览器脚本语法、diff-check 与同一独立评审通过，结论 `ACCEPT / P0 0 / P1 0`。修正后浏览器执行 `0`，现重建正式产物并复验普通 Worker 门。
- `350-1h` build 后的普通 Worker 复验越过 states / provinces / cities / routes / rivers / markers，首次在链式 diplomacy 被 `military.state.2.regiments 必须是数组` 拒绝。固定 `worker-regeneration-browser-chain / 10k` Node 链确认 17 个新国家均无军团数组，旧军事摘要仍为 `104` 个军团且 `stale=true`；这符合国家重生成使军事待派生、外交不重建军队的既有边界。动态插入 `350-1i`：外交只在 stale 为真时跳过军团集合与汇总的新鲜度检查，同时冻结来源国家军事字段、军事非战争根、除 campaign/front 外的 metadata 和 stale 状态。首轮独立评审以 `P1` 发现 Boolean 等价与 output-controlled skip 仍可让 stale 类型/值漂移；最窄返工改为原始 stale 严格一致、只从 source 授权 skip，fresh helper 不再把缺失军事 stale 写成 false，并补 undefined→0、false→null、带有效旧军团 true→1 三类反例。完整链、外交/战区/锁、typecheck、语法和 diff-check 通过，blocker-only 复审结论 `ACCEPT / P0 0 / P1 0`，浏览器执行 `0`；现重建并复验普通 Worker 门。
- 350-1i build 后的普通 Worker 门首败于 routes：通用 fresh helper 为保持缺失 military stale 而不再写 false，违反地理网络精确写集。该轮在 routes 即停，未形成后续领域结论。动态插入 `350-1i-a`，完整恢复通用 helper，只在 Worker / 主线程 diplomacy 的自身 fresh 调用前后保存并恢复 military stale 的 exists/value；features/networks/resources 与 economy/diplomacy/military 正式协议、两处语法、typecheck、diff-check 通过。同一独立评审确认 routes 以及外交 missing/false/true、no-op/throw、镜像/undo 均闭合，结论 `ACCEPT / P0 0 / P1 0`，浏览器执行 `0`；现再次 build / CDP。
- 普通 Worker regeneration 已在同一正式 build 完整返回 `ok=true`：独立 11 类与固定 10k 连续链 11 类的功能、session / packet、salt / 单 history、Loading、rivers 隔离、应用错误与 GL 断言均通过。独立评审仍给出 `BLOCK / P0 0 / P1 1`：旧脚本只在 undo / redo 前读取 execute LongTask，并过滤性能 health error，不能证明 execute、undo、redo 各自没有 `>200ms` 产品阻塞。动态插入 `350-1j`，为三阶段分别 settle / 采集 / 清空 PerformanceObserver，逐窗硬断言 `<=200ms`，并将 compact summary 与 full JSON 固定写入 Task 350 Z 盘 artifact；门禁实现经评审前不重复浏览器执行。
- `350-1j` 首轮评审继续拦截一项证据 P1：超线断言会早于 artifact 写入。现逐项累积 completed / active progress，在断言前记录当前 phase 完整任务；catch / finally 无论成功失败均写 full / summary，失败随后按原错误非零退出。blocker-only 复审 `ACCEPT / P0 0 / P1 0`，Node 语法、diff-check 通过，浏览器执行 `0`；只启动普通 Worker 固定门。
- `350-1j` 普通 Worker 固定门真实退出 `0`：独立 `11 / 11`、固定 10k 连续链 `11 / 11` accepted，连续链复用同一 session 且 input packets 均为 `3`；execute / undo / redo 全窗最大 LongTask `157ms`，`>200ms = 0`，旧功能 / history / Loading / rivers 隔离 / 错误 / GL 断言全部通过。full / summary artifact 为 `116382 / 8490 bytes`，SHA-256 分别 `AB1E8674...84C6F / 2D168007...2FE09`，位于既定 Task 350 Z 盘目录；待同一独立评审核验后进入 population。
- 同一独立评审已核对 350-1j 的两份 artifact、源码断言与文档，结论 `ACCEPT / P0 0 / P1 0`；确认独立三窗、连续链单窗、同 session / packets、accepted / committed / pending 与全部旧门真实通过。现进入 population Worker 固定浏览器入口。
- population Worker 固定入口在首个 adjustment 的正式 render request 以 `operation_invalid_input / 没有 population 的 picking 准备范围` 首败，未进入后续 transfer / history / 性能。动态插入 `350-1k`：population patch 更新 city 人口对象，picking bucket 持有 city 引用，故单一来源只补 `population → cities`，不退回五类全量或移除 picking；render-preparation 与真实 population task preparedRender Node 正例通过独立评审后才重建 / 复验。
- `350-1k` 已实现 cities-only picking 范围；render-preparation 单元门和真实 population task preparedRender components / binding 正例通过，既有 10k 完整事务边界与 100k `99846 / 57068 / 977ms` task 继续通过，diff-check PASS。修正后浏览器执行 `0`，现交同一独立评审。
- 同一独立评审确认 350-1k 范围精确、首次 / history renderLayers 一致、partial picking installer 保留与回滚闭合，独立复跑两项 Node 门与 diff-check PASS；结论 `ACCEPT / P0 0 / P1 0`。现重建正式 dist 并只复验 population。
- `1394 modules` build 后 population 复验越过 picking，随后在首个 adjustment 被 `population-worker-source-stale` 拒绝，未进入 transfer / history / 性能。插入 `350-1l`：结构化错误加入 expected / actual hash 与五组 mirror alias，浏览器夹具输出 details；只运行一次窄诊断，先区分值漂移与引用拓扑差异，禁止删除 stale 门或盲目重跑。
- 350-1l 诊断与纯 Node 复现确认 Worker 保留原图、主线程使用 v3 投影并额外执行 revision-0 normalizers，差异包含真实 population 输入，不能仅放宽 fingerprint。generation / import 现从同一 v3 bytes 建立 canonical projection，Worker 与主线程在 prepared render 前共用 social / suitability / label / war / river 规范化；值一致 detached mirror 不再制造假 stale，值漂移仍严格拒绝。v1 极简旧档补齐 suitability / population 列；whole-map、map-file、population 10k / 100k、v3、suitability、通用 Worker、war、typecheck、diff-check 通过。同一独立评审为 `ACCEPT / P0 0 / P1 0`，修正后浏览器执行 `0`，现只 build / 复验 population。
- 350-1l 后 `1395 modules` build，population 固定门完成 6 次 Worker / history 操作并到达最终性能断言，首败仅为两项 `132ms / 130ms` LongTask 与旧零容忍断言冲突。按项目规则插入 350-1m：一次窄诊断对齐 API / digest spans，一次实质优化，最终保存 artifact 并逐窗硬断言 `<=200ms`；不直接删性能门。
- 350-1m 窄诊断把 `131ms / 128ms` 定位到 adjustment / transfer 首个 await 前的同步 source fingerprint，digest 只占 `5.4～6.6ms`、Worker 单包解码最高 `0.5ms`。现改为同算法 `6ms` 分片异步 fingerprint，并在前后冻结 source map / revision / topology 防竞态；Worker strict stale 复算不动。async/sync parity、真实 yield、population 10k / 100k、typecheck、语法、diff-check 通过，浏览器修正后执行 `0`，待独立评审门禁与竞态边界。
- 350-1m 首轮评审 `BLOCK / P1 1`：页面 evaluate 内功能首败仍会跳过 artifact。现页面捕获 phase / active / completed / trace / failure，Node catch/finally 在关闭 browser 前无条件写 full / summary并保留非零退出，写盘失败也不跳过 browser/server 清理；语法、diff-check 与 blocker-only 复审通过，结论 `ACCEPT / P0 0 / P1 0`，现 build 后执行唯一目标复验。
- 350-1m 唯一 population 目标复验退出 `0`：6 次 Worker / 6 次 commit、同 session、精确 undo/redo、引用稳定，LongTask / application / page / health / GL error 均 `0`，Loading false。full / summary 为 `559785 / 498 bytes`，SHA `0E0D6E03...F7010 / BCC9F253...CC1B5`；同一独立评审复算并重放全部断言后 `ACCEPT / P0 0 / P1 0`，现进入 society。
- society 固定门首次 culture 在 Worker 前因缺 `culture-expansion` picking 范围首败，插入 350-1n。文化/宗教扩张均只登记 cities；双映射、未知 kind、10k 真实 picking DTO / binding 与原 100k 业务门通过。新增夹具曾因双全层 DTO 内存过量及 parity 混入 prepared DTO 两次失败，收窄为单个 picking-only 并分离 DTO 断言后通过；typecheck、diff-check 与独立评审通过，结论 `ACCEPT / P0 0 / P1 0`，现 build 后只复验 society。
- `1395 modules` build 后 society 固定门完整退出 `0`：culture / religion 与四次 undo-redo 共 `6` 次 Worker / commit，同一 session idle，精确历史、引用、Loading、错误、GL 和 LongTask 断言全部通过。因原脚本只在成功后向终端打印完整 report，本轮输出被截断且 evaluate 首败不会保存 Task 350 artifact，动态插入 350-1o；只补失败安全的 phase / active / completed / failure、full / summary JSON 和紧凑控制台输出，独立评审前不重复浏览器。
- 350-1o 首轮独立评审 `BLOCK / P0 0 / P1 1`：失败 artifact 与旧硬门无放宽，但 compact summary 丢失功能、history、引用、session reuse/status 和 commit delta/result，无法仅凭未截断摘要复核。现补齐这些既有断言的布尔/计数/状态摘要及 packet 数，语法和 diff-check 后只做 blocker-only 复审；浏览器执行仍为 `0`。
- 350-1o blocker-only 复审 `ACCEPT / P0 0 / P1 0`：compact summary 已覆盖全部既有功能/history/ref/session/reuse/packet/commit 与性能/错误关键标量，并同时进入控制台和 summary artifact；语法、diff-check PASS，浏览器执行 `0`。现只复跑 society 固定门一次形成可复核 artifact。
- 350-1o society 正式复验退出 `0`：6 次 Worker / commit、historyDelta 2、精确 undo/redo / refs、同 session 后续 5/5 reuse、packet 与 commit delta 全满足硬门；LongTask、application/page/health/GL error 为 `0`，Loading false。full / summary artifact `607495 / 1506 bytes`，SHA `25BB959B...EFCE1 / 68EBD009...5F63A`；现交同一独立评审做实证复核，未提前运行 economy。
- 350-1o artifact 独立复核 `ACCEPT / P0 0 / P1 0`；评审直接复算大小、SHA、6 次任务/commit、history/session/packet、错误与性能，并确认两个 operation-stall 早于目标窗。society 完成。为避免 economy 沿用的旧式超大 console / 首败无 artifact 再造成一次空转，动态插入 350-1p：浏览器前先补同型 page progress/failure、顶层 full/summary 与关键标量摘要，产品文件新增 `0`。
- 350-1p 首轮独立评审 `BLOCK / P0 0 / P1 1`：compact 缺逐 run 的 task/mode/sessionMode/payload map/render-prepared 层以及精确 LongTask / forbidden 数。现补 runProfiles、longTaskCount、forbiddenCount，旧断言与失败落盘不动；静态门后只做 blocker-only 复审，economy 浏览器执行仍为 `0`。
- 350-1p blocker-only 复审 `ACCEPT / P0 0 / P1 0`：compact 已与 full 共同覆盖全部旧硬门，语法、diff-check PASS，浏览器执行 `0`。现首次运行 economy 固定门，并保证成功/失败均在 Task 350 Z 盘留下 full/summary。
- economy 首次固定门在 api:rebuild 的 Worker 前命中缺 `economy` picking 范围；350-1p 失败 artifact `1850/724 bytes` 且 active=api:rebuild。动态插入 350-1q，只补 `economy → cities`：经济重建会同步 city 的 market/production/product/treasury，市场/交易不属于五类 object picking；render-preparation 与单个 10k 真实 economy task picking DTO/binding 正例后交独立评审，浏览器不重跑。
- 350-1q 已补 `economy → cities`；render-preparation、10k picking-only 与完整 economy task 通过，100k 为 `99846 / 63463 / 28256 deals / 2634.5ms`，原 parity/alias/identity/history/lock/fault/stale/buffer 门保持；typecheck、语法、diff-check PASS，现交独立评审，浏览器修正后执行 `0`。
- 350-1q 独立评审 `ACCEPT / P0 0 / P1 0`：economy 五类 picking 中仅 cities 受写集影响，首次/history targetKind 与三层 renderLayers 一致，未知 kind 未放宽；独立四门全通过，100k 本轮 `2602.9ms`。现重建正式 dist 并只复验 economy。
- `1395 modules` build 后 economy 复验越过 picking，主线程 pre-commit 首败 `binding is not defined`；失败 artifact `4691/676 bytes` 且 active=api:rebuild。插入 350-1r：economy assertOutput 显式接收 executeWorkerMapMutation 提供的 binding，并由市场归属正式入口回归锁定该签名；validator 不删除、不放宽，评审前不复跑浏览器。
- 350-1r 初选旧 market-assignment 聚合门命中既有文案漂移（夹具“应用并重算”、产品“应用并更新”），与 binding 一字修正无关；未重复运行也未扩修 UI/旧夹具。精确签名断言改由本阶段三域协议门承载，旧聚合门失败登记为非阻断残余；现只复验协议/typecheck/语法/diff-check。
- 精确签名反例首次又确认补丁误落相邻 economy history 回调，首次 mutation 仍缺 binding；未启动浏览器。撤回 history 参数并在 applyEconomyMutationViaWorker 精确补齐后，三域协议、typecheck、语法、diff-check PASS；产品净改动一个参数，现交独立评审。
- 350-1r 独立评审 `ACCEPT / P0 0 / P1 0`：执行器真实 binding 只进入首次 economy 回调和正式 validator，history 不漂移；限定函数切片反例、三域协议/typecheck/语法/diff-check 全通过，validator 未改。现重建 dist 并只复验 economy。
- 350-1r 后 economy 功能/history/session 全链通过，最终仅被 rebuild/assign `674ms/189ms` LongTask 阻断；首项超过 200ms，digest 约 47ms。artifact `355973/3267 bytes`、SHA `D9F9EC7B...41B7E/42C1E4E2...B66BE`。插入 350-1s，给真实 10k 协议正例记录 patch operation 数和 validator 同步时长，完成一次实质优化前不重跑浏览器。
- 350-1s 确认 `3241` 个 economy operation 的旧同步 validator 为 `1158.2ms`；线性 prefix / Set 先降至 `277.8ms`，再以只读 source projection、触达容器 copy-on-write、typed-array 单 view 与无排序深比较降至 `68.1～79.9ms`。新增 sourceMap 不可变断言后，三域 `44` 类协议拒绝、10k/100k economy task、typecheck、两处语法及 diff-check 均 PASS；100k 为 `99846 / 63463 / 28256 / 2655.2ms`。浏览器门保留 LongTask 全明细并按冻结规则硬断言 `>200ms = 0`，现交独立评审，修正后浏览器执行 `0`。
- 350-1s 独立评审 `ACCEPT / P0 0 / P1 0`：对 39 条路径的 `59319` 个有序序列穷举确认新旧 overlap 等价；独立 protocol `3241 ops / 81.1ms`、44 类拒绝、100k economy task `2639.5ms`、typecheck、语法与 diff-check 全 PASS，COW / 深比较 / LongTask 门均无偏差。现重建正式 dist 并只运行 economy 固定 CDP 门。
- 正式 build `1395 modules` 后 economy CDP 目标复验退出 `0`：4 run / commit、historyDelta `2`、精确 undo/redo、同 map、单 session 后续 `3/3` 复用与全部 prepared layers 均成立；packet `3→436 / 3→9 / 3→6 / 3→6`。仅有 1 个 `128ms` LongTask，`>200ms=0`；应用/page/health/GL 错误与 Loading 残留均为 0。full / summary artifact `356088 / 2535 bytes`，SHA-256 `3D0E03EC...93B35 / 0692A38E...B607`；现交同一独立评审核验后完成 350-1。
- economy artifact 终核 `ACCEPT / P0 0 / P1 0`：独立评审重算大小 / SHA 并确认 full 与 summary 对全部功能、session、packet、commit、性能与错误硬门逐字段一致。350-1 五个 10k 核心事务入口完成，现进入 350-2a 锁定河流的 Feature 镜像边界；浏览器执行仍等待本子阶段 Node 与评审接受。
- 350-2a 复现旧 composite 在锁定河流 `#59` 的湖泊出口镜像 `1→0` 后拒绝。修正把锁定河流依赖闭包的 lake guards 转成 Feature support snapshots，与显式 Feature 锁按 ID 合并后进入 topology rebuild；不改锁断言。安全正例 `40/40` 河流、`1/1` lake edge 保留且 rivers salt `0`，关联湖泊变陆的不安全负例以 `regeneration_lock_conflict / locked-feature-topology-changed` 在提交前拒绝。Composite 100k `99846 / 51950`、Feature 锁、50k/100k 河流锁、typecheck、语法、diff-check PASS；浏览器 `0`，现交独立评审。
- 350-2a 独立评审 `ACCEPT / P0 0 / P1 0`：support 来源、显式优先、pack/grid feature 约束、不安全拒绝及 Worker/main coexistence 均闭合，全部专项复跑 PASS。下一组四个旧浏览器入口的 artifact / compact / `>200ms` 规则不统一，按动态插入规则登记 350-2b；先统一失败留证和性能硬门、独立评审，再启动 350-2 的昂贵门。
- 350-2b 已以共享 helper 为 session 10k/100k、grid topology、direct-domain locks、compound locks 补齐成功/失败/teardown 的 full+summary Z 盘 artifact；session compact 保留 scenarios、Worker sessions/packets 与逐窗 LongTask，grid 采集 refine/undo/redo，两个锁门排除 newMap setup 后观察产品操作。四门均保留原始 LongTask 并硬拒绝 `>200ms`，同时记录错误、GL、Loading；helper 正负自测、四入口接线静态反例、五脚本语法与 diff-check PASS，browser `0`，现交独立评审。
- 350-2b 首轮独立评审 `BLOCK / P0 0 / P1 3`：直接领域门五个 Promise API 同步解包、teardown 覆盖首败 / close timeout 假成功、grid Loading 与 compact 功能证据不足。最窄返工已完成：所有 receipt 显式 await；首个功能 failure 保留且清理 failure 单列，context / browser / server 关闭超时结构化拒绝；grid 增加 Loading false 硬门与 fingerprint / input / topology / map / PNG / renderer 摘要。dual-failure、teardown-only、close-timeout 自测、四入口接线反例、六脚本 node check 及 diff-check PASS，browser 仍为 `0`，只交同一智能体做 blocker-only 复审。
- 350-2b blocker-only 独立复审 `ACCEPT / P0 0 / P1 0`：helper regression、六脚本语法与 diff-check 独立 PASS；首轮 async、失败证据与 Grid 三类阻断均闭合，browser / CDP `0`。预检阶段完成，下一步只生成正式 build 并按固定顺序执行 350-2 五门。
- 正式 build `1395 modules` 后，350-2 第一个 10k session 门首败于“全锁河流 no-op 未连续复用 session”。一次窄诊断显示 newMap 已采用 `generation.compute` 常驻 session，两次 no-op 均为同 id 的 `reused=true / committed=true`，并非产品拒绝或失效；旧断言仍假设锁命令后首个必须 fresh。动态插入 350-2c：显式等待 map replica patch，证明同 session 的 revision / lockFingerprint 更新后再接受两次 reuse，保留 committed、salt/history/renderer、错误与性能旧门；产品改动 `0`，静态/Node 与独立评审前不重跑目标门。
- 350-2c 独立评审 `ACCEPT / P0 0 / P1 0`：复核确认副本 patch 以 checksum / revision 连续性和失败 invalidate 保证原子性，后续 reuse 还需全 binding 匹配，新夹具没有掩盖 stale。artifact self-test、两脚本语法与 diff-check 独立 PASS，browser `0`；现使用唯一一次 10k session 目标复验。
- 350-2c 后唯一目标复验越过 session patch / reuse，推进到全锁 rivers no-op 的 renderer identity 总门失败；现有错误未暴露 surface / buffer / picking / overlay / camera 哪项漂移，无法静态决定产品修正或夹具 settle。该入口已用完标准的一次窄诊断和一次目标复验，后四门冻结。登记 350-2d，只先补各子身份及 surface refs/descriptors/bytes/valid 的失败证据；Node 语法 / diff-check 后停止，等待用户额外授权一次聚焦浏览器诊断。
- 用户为 350-2d 及其后续复验追加授权 `5` 次浏览器执行；现使用 `1 / 5` 仅跑 rivers no-op 聚焦诊断，仍按首败停止和独立评审顺序，不提前启动其余 350-2 门。
- 新增额度 `1 / 5` 未到产品差异即由诊断夹具 TypeError 停止：精简的 identity.surfaceBase 被误传给完整 probe exact helper。已改为逐字段结构化比较并补静态反例；这轮不作产品结论，非浏览器门通过后使用 `2 / 5` 继续同一 no-op 聚焦诊断。
- 新增额度 `2 / 5` 将 renderer 总门收敛到旧 probe 的 GPU aggregate bytes/checksum/综合 valid 失败；JS refs、descriptors、CPU bytes/checksum、其余 buffers/picking/overlay/camera 均不变，但该轮未单列 geometry/color `isBuffer`，尚不能判定资源删除。现补 afterMap / afterLockPatch / afterNoOps 三时点 liveness 和 contextLost，静态门后以 `3 / 5` 区分真实删除与公式漂移。
- 新增额度 `3 / 5` 确认三时点 geometry/color 全 live、context 未丢失，原失败是旧 probe 不认识 3-word geometry + 3-float color 双 buffer 与新分段公式。现分别读回两类 GPU buffer，验证各自/总 bytes 与 owner descriptors，并以完整 before GPU 快照做 after exact；CPU source 保持独立 exact。artifact self-test、脚本语法、prepared renderer 9-case owner regression、diff-check PASS，产品文件 `0`，独立评审后才可使用 `4 / 5` 完整目标复验。
- 350-2d 首轮评审 `BLOCK / P0 0 / P1 1`：新 probe 本体正确，但旧 caller 尚未全迁移，仍有 CPU/GPU 跨布局 checksum 直比和 geometry-only liveness/owner/upload 追踪。最窄修正穷尽全脚本调用方：GPU checksum 只对 GPU before-image，CPU/GPU 只比总字节；临时、活动、baseline 与 100k upload 同时纳入 geometry/color。artifact 自测新增旧断言与单 alias 反例，helper regression、两脚本语法、prepared installer 9-case、diff-check PASS；浏览器保持 `3 / 5`，只做 blocker-only 复审。
- 首次 blocker-only 复审仍 `BLOCK / P0 0 / P1 1`：late-context 的 CPU source 变量名为 `after.expected.surface`，漏过旧变量名静态规则。该第六处已改为只验 aggregate byteLength，自测升级为全局禁止 whole `.aggregate` 直接相等；browser 仍为 `3 / 5`，再次复审后再决定 `4 / 5`。
- 第二次 blocker-only 复审 `ACCEPT / P0 0 / P1 0`：whole aggregate 相等断言清零，九处 CPU/GPU 来源规模门只比较 byteLength，所有 owner/liveness/upload 调用方同时覆盖 geometry/color；独立 Node/语法/9-case/diff 门 PASS，browser `0`。350-2d 完成，现运行新增额度 `4 / 5` 的完整 10k session。
- 新增额度 `4 / 5` 越过 no-op renderer 后，invalidation 首个 routes 实际合法复用 generation adopted session，旧 fresh 断言首败。动态插入 350-2e：10k 路径改证 adoption、undo/lock replica patch 连续性和换图新 id；同步迁移 hard-cell、focused political、100k 首操作，fresh-routes 诊断仅在显式 invalidate 后验 fresh；fault/cancel 后 fresh 保持。最后 `5 / 5` 在 Node/静态与独立评审前冻结。
- 350-2e 首轮评审 `BLOCK / P0 0 / P1 1`：continuity 只验 id/status/revision 增大不足以证明精确 patch。现以统一 helper 固定 adopted/idle、id/mapIdentity/generationToken、revision `+1`、checksum changed/restored 与 lock fingerprint same/changed；换图要求新 binding 且 revision 0。静态反例同步锁定各阶段，最后一次浏览器仍冻结。
- 首次 blocker-only 复审继续 `BLOCK / P0 0 / P1 1`：checksum 是 replica lineage token，reused Worker operation 应保持 same，undo patch 也不应等于旧 generation token。现仅 undo/lock patch 要 changed，四次 reused operation 要 same；删除错误的可逆 checksum 断言并同步静态门，`5 / 5` 未运行。
- 第二次 blocker-only 复审 `ACCEPT / P0 0 / P1 0`：七段 transition、lineage checksum、replacement owner 与真正 fault/cancel fresh 门均精确，非浏览器门全 PASS。350-2e 完成，现使用最后 `5 / 5` 完整 10k session。
- 新增额度 `5 / 5` 已通过 continuity，随后 one-shot rivers refresh fault 的公开 code 为 `operation_failed`，旧夹具要求内部 code 而首败；额度用尽。350-2f 统一迁移 rivers/route pending/pending viewport：公开 outer 保持 operation_failed，临时 wrapper 捕获的 raw cause 必含 worker refresh code，且 finally 恢复；产品文件 `0`，不再运行浏览器。
- 350-2f 首轮评审 `BLOCK / P0 0 / P1 1`：静态门未保证共享与 inline wrapper 必在 finally 恢复；补两个函数切片的完整结构断言及删除-finally 反例后 blocker-only `ACCEPT / P0 0 / P1 0`。用户第二批再授权 `5` 次；`1 / 5` 完整 10k 复验确认 public `operation_failed`，但 manager wrapper 捕获不到 raw cause。动态插入 350-2g：console API 动态读取同一 `runtimeActions.generate.regenerate`，临时捕获边界移到该 action，catch/rethrow 后再由 apiCall 序列化，两处 finally 恢复；产品文件 `0`，修正后 browser `0`，剩余 `4 / 5` 未运行。
- 350-2g 文档 blocker-only 复审 `ACCEPT / P0 0 / P1 0`；第二批 `2 / 5` 完整 10k 复验仍停在 raw cause 内部码断言，公开 `operation_failed` 保持正确。动态插入 350-2h：不再试换边界，先记录 wrapperCalls 并把完整 raw error tree 放入持久化 failure message，精确区分 wrapper 未调用、错误被吞或 cause 结构不同；产品文件 `0`，修正后 browser `0`，剩余 `3 / 5` 未运行。
- 350-2h 经逐函数 slice、计数增量与逐路径 raw JSON 删除反例补强后独立 `ACCEPT / P0 0 / P1 0`。第二批 `3 / 5` 实证 wrapperCalls=1，raw cause 为 `river-water-tail-invalid`，并非注入故障；Node 固定 seed 重现合法 lake #7 natural outlet river #259 的 `2120 water → 2121 spill land`。动态插入 350-2i；首版原始 height/source closed 近似及 `Number()`/自报 status 被独立评审以两项 P1 阻断。现从正式生成器抽出共用 salt variation、有效高度、闭湖重判、洼地消解和湖泊水文评估流水线，validator 按源 salt 精确复算完整 overflow expectation，并严格要求 lake i/id、outlet、spillCell 原始 number 安全整数。开放湖、闭流可溢流、扰动改变最低岸格三正例及 incision-limited、伪造、字符串/null/布尔强转负例通过；固定地理协议、lake-overflow、river-network、锁定河流 50k/100k、worker-task、typecheck、build、diff-check PASS，Node validator 10k/100k `33.1/185.1ms`、独立 100k 冷/暖 `196.0/139.7–149.8ms`。产品/测试各 `2` 文件，blocker-only `ACCEPT / P0 0 / P1 0`、browser `0`；350-2i 接受并使用第二批 `4 / 5` 完整复验。
- 第二批 `4 / 5` 已越过合法湖泊出口并命中真实 after-render fault，公开 code 为精确 `worker_regeneration_refresh_fault`；旧 `operation_failed` 是前置 river protocolError 经 RuntimeOperation 包裹的结果。插入 350-2j：正式 RuntimeOperation 对 code/stage/suggestion 齐全的 Error 保留原 code，console API 也保留 Error code；据此同步 rivers、routes pending、pending viewport 三处 public 断言，raw tree、wrapper 次数、finally 恢复、回滚与 LongTask 门不变。artifact self-test 新增正式源码断言及退回 operation_failed 的反例，Node/语法/diff-check PASS；产品 `0`、测试 `2` 文件，独立复审 `ACCEPT / P0 0 / P1 0`、browser `0`；350-2j 接受并使用第二批最后 `5 / 5` 完整复验。
- 第二批最后 `5 / 5` 越过 refresh fault 全套、恢复 / 竞态、政治及 features / rivers，首败推进到 hard-cell 第一格缺少 patch range；artifact 已写 `Z:\tmp\codex\2026-08-20\task-350-map-core-cdp\worker-session-browser-full.json`，额度归零。正式 renderer 的高度局部更新仅在 `colorMode === "height"` 时进入 hard-cell patch；旧 10k / 100k 夹具未固定该模式，前序政治视图又按设计跨地图替换保留。插入 350-2k：两档门保存原 mode、操作前正式切换 height、两次编辑逐项回读 height，并在正常 / finally 恢复；静态自测读取 renderer 源码并含删除切换 / 恢复的反例。产品 `0`、测试 `2` 文件；两脚本语法、artifact regression、diff-check 独立 PASS，评审 `ACCEPT / P0 0 / P1 0`、browser `0`。350-2k 接受；无新授权不再运行浏览器。
- 用户根据前五个固定入口的返工审计，批准重新规划包括已接受入口在内的典型场景并整体修补已开发功能，不再让产品与夹具在完整浏览器门中互相逼近。`350-R0` 新方案冻结 `17` 类典型场景、`16` 项共同不变量、`20` 个固定入口重新覆盖和 `R0～R7` 顺序；保留第 349 项架构，不推倒重写。产品 blocker 与夹具 blocker 必须使用独立子阶段，`R0～R6a` 先完成静态 / Node / 构建 / 源码级集成审计，浏览器额度仍为 `0`；当前产品 / 工具改动 `0 / 0`，待同一只读智能体评审 R0 文档后进入机器可审计 catalog。
- `350-R0` 首轮独立评审为 `BLOCK / P0 0 / P1 5`：初版写错 canonical/history/publish/projection settle 顺序，漏掉 cache 的四字段 `RenderResourceBinding` 与 `renderGeneration`、archive export / headless / worker-only profile，并把正式十一领域链误写为含 economy、止于 diplomacy；`current-plan` 与根 `AGENTS` 仍保留旧五阶段 / 冷启动 BLOCK 权威路由。当前只按第 349 项冻结契约修正这五项，产品 / 工具仍为 `0 / 0`，browser `0`；blocker-only 接受前不进入 R1。
- `350-R0` blocker-only 复审 `ACCEPT / P0 0 / P1 0`：I-04 已对齐正式原子提交 / publish / projection settle，I-12 及 R3b/R4a 纳入四字段 cache binding 与 renderGeneration；generation/import/archive/headless 四 profile、真实十一领域链、R2b 独立三链和权威路由均闭合。机械计数 `16 invariants / 17 scenarios / 20 entries / 13 stages`，20 个 package 入口与本地链接齐全，diff-check PASS；browser `0`。R0 接受，现进入 R1 catalog。
- `350-R1` 建立唯一机器 catalog 与自测，机械固定 `16 invariants / 17 scenarios / 20 fixed entries / 1 supplemental / 4 profile owners`；前五项明确标为 `accepted-baseline-recheck-required`，其余 15 项为 pending，旧浏览器结果只作历史状态。catalog 自测通过后，第 349 项防误触审计因递归发现 foundation 新增的 adoption owner 子门而报 `28 !== 27`；这是夹具把顶层 gate 数与递归依赖闭包混为一谈，不是产品失败。动态插入 `350-R1-f1`，以 `27` 个顶层 gate + 唯一显式子门的精确集合收口，同时对二者所有工具导入链继续扫描浏览器启动原语；产品文件 `0`，browser `0`。
- R1 首轮只读评审为 `BLOCK / P0 0 / P1 3`：20 项 owner 被粗化成 R2/R3/R4/R5、没有 riskGroup 且 policy 可填任意字符串；Node 前置可替换为名称不可见但真实启动 Chromium 的 `regress:measurement`；终验 scanner 不识别 `import "./side-effect.mjs"`。动态插入 `350-R1-f2`，将 owner/risk/scale 固定为 20 项权威矩阵，六类 policy 改为封闭集合并补逐字段破坏反例；33 个声明前置、递归 package 子门和 44 个 tools 源码统一经 browser-launch scanner，真实 measurement 与 side-effect fixture 均必须拒绝。catalog、终验审计现重新 PASS，产品文件 `0`、browser `0`，待 blocker-only 复审。
- R1 第一次 blocker-only 复审确认 measurement 与 side-effect import 两项 P1 已闭合，但同一 owner-stage blocker 仍残留于 17 个 scenario 自身：`S-01.ownerStage` 删除或改为 R7 时 validator 未拒绝。当前补入 17 项 scenario owner 精确矩阵，并以缺失 / 跨阶段漂移两条负例固定；不改产品、不改场景分母、不运行浏览器，待第二次 blocker-only 复审。
- R1 第二次 blocker-only 复审 `ACCEPT / P0 0 / P1 0`：独立遍历 17 个 scenario 的删除 / 跨阶段漂移共 `34 / 34` 反例全部拒绝；20 fixed、1 supplemental、4 profile owner、封闭 policy、真实 measurement 和 side-effect import 防浏览器扫描继续成立。catalog、终验审计、typecheck、四工具语法与 diff-check 独立 PASS，产品 `0`、browser `0`；R1 完成，顺序进入 R2a 前五项共同事务边界复查。
- R2a 首轮串 / 并行 Node 盘点中，adoption owner、四类 whole-map profile、map-file、foundation、worker-task、map-replica、history async 现有两门及四组领域协议全部退出 0；一次并行 Vite 调试端口占用只产生提示，后续读门串行。盘点发现纯 Node prepared-installer 已有 9 类 commit / rollback / finalize / fault / nested / detached / ranges 语义覆盖并独立 PASS，但没有 package 路由、前五项 catalog 也未引用；history async 同样缺少职责命名组合门。动态插入纯夹具 `350-R2a-f1`，只补 package gate 和 catalog Node 前置，产品代码 / 浏览器执行均为 `0`。
- R2a 逐类核对 canonical source 后确认 society-politics / settlements 两组旧协议将同一对象既传入会原地变更的 Worker task、又作为 validator source；这与 features / economy 的分离模式不一致，会削弱 I-06 证据。动态插入纯夹具 `350-R2a-f2`：为每个 kind 保留 canonical source，Worker 只接 structured clone，validator 前后深比较来源；原 binding、write-set、镜像、引用和 history 断言不变。产品文件暂为 `0`，需串行复跑两门确定是否暴露真实产品 blocker。
- `R2a-f2` 串行复验通过，说明正式 society / settlements validator 能接 canonical before-image，未转成产品阶段。随后把 core contracts、core facade、core manifests 与 prepared-installer、history async 一起登记为前五项共同前置，并新增串行 `regress:task-350-r2a` 聚合门；当前产品改动仍为 `0`，只待聚合门、build、diff 与独立评审。
- `regress:task-350-r2a` 已串行完整退出 `0`：机器 catalog 为 `16 invariants / 17 scenarios / 20 fixed entries / 4 profile owners`，core manifests 为 `15 domains / 216 descriptors / 35 negatives`，prepared installer 为 `9` 类事务用例；adoption owner、whole-map profile、map-file、foundation、worker-task、map-replica、history async 及四组领域正式协议全部通过，society / settlements 的 canonical source 前后均保持不变。随后 `build:app`、第 349 项 `27` 个顶层门防浏览器审计与 diff-check PASS；R2a 产品文件 `0`、浏览器 / CDP `0`，阶段冻结并送同一只读智能体独立评审。
- R2a 独立复核 `ACCEPT / P0 0 / P1 0`：评审自行完整运行 `regress:task-350-r2a`，确认 `38` 个 Node 前置展开为 `41` 个 package scripts / `50` 个本地源码且没有浏览器启动；两组 canonical source / Worker clone、正式 validator、四 whole-map profile、三类 projection、9 类 prepared installer、fault / cancel / obsolete / session / rollback recovery 均成立，diff-check PASS。产品改动 `0`、browser `0`；阶段接受，按冻结顺序进入 R2b population / society / economy 领域链复查。
- R2b 首轮直接运行三条真实 10k / 100k Worker task 均 PASS：population `924.8ms`、society `549.8ms`、economy `2602.1ms`，alias、history、锁 / 取消 / fault / stale 与输入 buffer 均成立。完成度盘点确认 population / society 的 task 脚本未注册 package，catalog 也无法引用；三域 prepared-render 又只覆盖 adjustment / culture / rebuild 的 10k 首操作。插入纯夹具 `R2b-f1 / f2`，补显式 task / render / dependency / whole-map-chain 路由和删除反例，并补 transfer / religion / assignment 及三条 100k picking / binding 对称断言；增强后 task 为 `1153.7 / 492.1 / 2562.3ms` 且继续 PASS，产品与 validator 均未修改。
- R2b 随后发现真实产品登记缺口：正式 `social-expansion.compute`、文化 / 宗教扩张可撤销命令和公开 inspect / apply API 已长期存在，但 society-politics TypeScript Manifest 只登记 `regeneration.compute` 与省会命令；`core-manifests` 因此无法证明社会扩张写集和 owner，违反 I-05。动态插入独立产品阶段 `R2b-p1`，只把既有 command / query / worker / API 与精确 union 写集纳入 Manifest 并从 runtime 源核对，不改变运行时或浏览器夹具。
- `R2b-p1` 经 canonical root、权威 API metadata / business codes 和共享 transport clone 三个同 blocker 首败收口后，`core-manifests` PASS：society-politics descriptors `216 → 225`，新增 `social-expansion.compute`，真实 1k culture + religion 联动 patch 每条路径均受 Manifest union 覆盖，四个公开 API 已登记。随后 Manifest 新登记的既有 `regress:social-expansion-ui-api` 在动态门前因旧普通界面文案“只读预检 / 同事务联动宗教”失败；这是第 330 项后过期夹具，插入纯夹具 `R2b-f3`，仅对齐当前“查看影响范围 / 同时更新宗教分布”，产品 UI 与动态断言不变。
- R2b 最后以 population / social-expansion / economy 三个 Manifest 的完整 Worker write-set 直接规划依赖闭包，分别硬要求跨域 owner 与 worker / renderer / persistence / UI 四投影，不再保留人工代表路径假绿面。串行 `regress:task-350-r2b` 全绿：11 域链 100k `3429.68ms`，三条 task 100k `934.9 / 463.2 / 2663.9ms`，四类 whole-map profile、正式拒绝矩阵与 typecheck 均通过；随后 `1395 modules` build、27 门防浏览器审计、diff-check PASS。阶段产品改动为 society-politics Manifest `1` 文件（现有能力登记，不改 runtime），工具 / 路由为 `9` 个已跟踪文件加两份 catalog 新文件；browser / CDP `0`，现冻结送同一只读智能体评审。
- R2b 首轮评审未发现产品 P0/P1，但以两个夹具 P1 阻断：聚合与 society catalog 漏接 core Manifest / social UI API 两项关键门；1k 最大正例的共享 mirror 又让四个合法 owner 未被实际 patch 命中。插入纯夹具 `R2b-f4`，把两门接入聚合和精确 prerequisite 删除反例，分离 culture / religion / state / province 四组 mirror，并为 `21` 个 Manifest root 增加反向实际 patch 覆盖。修正后 core Manifest 输出 `21 roots / 515 paths / 4 detached mirrors`，catalog `44 / 47 / 54`，完整 R2b 聚合再次全绿；11 域链 100k `3502.11ms`，population / society / economy 100k `936.0 / 455.0 / 2575.7ms`，browser `0`。产品继续冻结，现只做 blocker-only 复审。
- R2b blocker-only 复审 `ACCEPT / P0 0 / P1 0`：独立确认唯一聚合入口显式运行两门，society fixed entry / prerequisite 矩阵 / 删除负例不可绕过；四组 detached mirror 与双向 `21 / 21` root 覆盖能拒绝删除任一 owner。catalog、core Manifest、social UI/API、diff-check 独立 PASS，产品无新增 P0/P1、browser `0`。R2b 完成并冻结，按顺序进入 R3a session / adoption / concurrency Node/static contract。
- R3a 基线中 worker-task、adoption owner、replica command patch 与独立 100k graph stream 全部 PASS；现有产品已动态覆盖 session commit/ACK、adopt-result-map、跨 task reuse、queued patch、checksum drift 后 fresh resync、cancel/obsolete 和 wrong-session guard。盘点只发现验收结构缺口：条目 6/7 没有职责命名 session contract，100k graph 无 package / catalog 路由。插入纯夹具 `R3a-f1`，新增 96 行 static contract，固定 6 段 continuity、replacement、两类 late race、pending viewport 两模式、100k session 与 coordinator fail-closed；另给 graph stream 加 `--100k`、三阶段 yield 与单包 `<50ms` 硬门。
- 修正后的 `regress:task-350-r3a` 全绿：catalog `48 / 50 / 56`，worker-task 11 kind 与 recovery 全过；10k graph `205 packets / 322 yields / 5.284ms`，100k `971 / 1807 / 6.354ms`，正式 buffer 不 detach，typecheck / diff-check PASS。R3a 产品文件 `0`，工具 / 路由为新 contract、graph、catalog、自测与 package；browser / CDP `0`，现冻结送同一只读智能体评审。

## 2026-08-19：登记第 349 项——地图核心引擎化与 TypeScript 渐进迁移

- 用户明确批准调整并实施地图核心引擎计划与 TypeScript 核心契约计划，要求拆成可独立验收的阶段；每阶段由主线程唯一写入，冻结 checkpoint 后复用同一个只读评审智能体确认与计划无偏差，才进入下一阶段。计划外但阻断验收或证明设计不安全的任务必须插入阶段列表，并重新评估未完成阶段顺序。
- `349-10e` 已冻结 economy / diplomacy / military 三份 shadow Manifest 和四类既有 Worker result pre-commit 契约；core registry 为 `15 domains / 216 descriptors`、dependency 为 `15 domains / 19 systems`。专项覆盖三域镜像 / 引用、精确与动态写集、10k / 100k Worker、history、锁和旧数据；军事事件归档缺失、外交锁误把 `Invasion` 当战争区域、战线固定样本漂移均作最窄修正。版本升至 `0.5.41`，浏览器执行 `0`，checkpoint 待同一评审智能体审查。
- `349-10e / 0.5.41` 首轮评审 `BLOCK` 的五项 P1 已在 `0.5.42` 闭合：精确写集逐路径限制删除，经济槽与跨域引用补齐，军事比例绑定请求国家并冻结非目标国家，外交复用正式 zone / Warzone 引用门，军事事件归档逐项绑定 before-image、顺序和 generation。协议负例由 `12` 增至 `23`；两档 Worker、规则 / 锁、typecheck、build 通过，浏览器执行仍为 `0`，当前待同一智能体复审。
- `349-10e / 0.5.42` 复审继续确认结果形状、经济整对象、军事策略全局根和第三国 Warzone cell 四类 P1；`0.5.43` 收紧 generationLog 来源前缀与计数 / 类型、economy 叶字段-only、军事事件 / 请求外根冻结与 campaign / front 引用、Warzone cell 所属国和 metadata 一致性。blocker-only 复审确认尚可改写合法的请求外战线及伪造汇总，`0.5.44` 因而冻结无关 campaign / front，并从军团重算军事汇总。协议负例增至 `38`，10k / 100k economy 与 military-policy、三域规则 / 锁、core registry / dependency、typecheck 均通过，浏览器执行仍为 `0`，当前待同一智能体复审。
- 同一只读评审智能体对 `349-10e / 9bfe682 / 0.5.44` 给出 `ACCEPT`，三轮评审无剩余 P0 / P1；状态 checkpoint 升至 `0.5.45` 并进入 349-10f，先盘点 generation / import / adoption / export / headless 的真实 owner、binding、事务终态和无 DOM Node 门。
- 第 349 项只在 `codex/map-core-engine-architecture-plan` 与 `main` 并行推进，不得合入 `main`。考虑到短期架构改造不直接反映为用户可见结果，全阶段不执行浏览器、视觉或 WebGL 验收；最终阶段只形成并评估浏览器验收方案。
- 初始阶段链为 `349-0` 计划冻结、`349-1` 现状盘点、`349-2` TS 工具链、`349-3` 核心契约、`349-4` Manifest、`349-5` 薄 facade、`349-6` notes 切片、`349-7` markers 切片、`349-8` Worker 切片、`349-9` dependency / projection、`349-10.x` 复杂域迁移和 legacy 收口、`349-11` 非浏览器终验与浏览器验收方案。当前只执行 `349-0`，产品代码改动为 `0`。
- `349-1` 盘点确认 interactive 唯一 canonical owner 是 `createGeneratorApp` 的 `state.map`，headless write 只拥有隔离文档；现有同步 command、全图 snapshot、Worker map mutation、map replacement 和 headless write 五类事务均已归类，`13 / 13` Worker task 无未知 owner。发现存档字段注册遗漏和四套 identity 语义冲突，故按用户规则插入独立 `349-3a`，并把复杂域按依赖拆为 `349-10a`～`349-10g`。本阶段产品 / 工具改动保持 `0`，浏览器未启动、未操作、未验收。
- `349-1` 只读评审首轮纠正 render / topology Worker session、headless session clone 和普通文档 identity 三项事实措辞；最窄修正后复审 `ACCEPT`。版本升至 `0.5.6`，下一阶段只接入受限 TypeScript 工具链。
- `349-2` 只新增 TypeScript `7.0.2`、受限 `tsconfig.core.json`、`typecheck:core` 和未进入 runtime import graph 的两行 sentinel；`allowJs: true / checkJs: false`，不扫描旧 JS。工具链接入前后以同一 `0.5.6` 构建均为 `1360` modules / `98` files，完整摘要同为 `F3C93DF43DDA6E48D54B722BA6D20BBF3D569D32DB1D38B6CB3B68A4BE598623`。产品运行代码 / 工具代码改动均为 `0`，浏览器未启动、未操作、未验收；阶段版本准备升至 `0.5.7`。
- `349-2` 同一只读评审智能体首轮 `ACCEPT`，版本升至 `0.5.7`；下一阶段只实现核心类型与 runtime validator，不接管旧 action、Manifest 或 facade。
- `349-3` 新增纯 TypeScript 核心契约：品牌身份、interactive / headless revision、pre-commit / committed-projection binding、presentation / render resource binding、五类 snapshot ownership、computed / committed patch、七步 commit lifecycle 与结构化 runtime error。类型负例固定 identity / revision 不可混用和 compute patch 无 `commitId / targetRevision`；专项 Node 直接执行同一 TypeScript validator 的编译结果。旧 action、registry、facade、Manifest 与产品 import graph 均未接线，浏览器未启动、未操作、未验收；阶段版本准备升至 `0.5.8`。
- `349-3` 只读评审首轮发现 computed binding 类型过宽、adoption before profile、presentation / render 混装、patch 错误路径和 headless 覆盖五项不一致；全部补齐编译 / runtime 负例后复审 `ACCEPT`。版本升至 `0.5.8`，下一阶段执行已插入的 `349-3a` registry 与 identity adapter 闭合。
- `349-3a` 把既有存档中的 `notes / measurements / labels / visualTheme / display` 补入唯一 canonical registry，顶层 section 为 `24 → 29`；新增 section 追加在旧 24 项之后，旧 `.webfmg v3` section id 不漂移。`layers / visualTheme / display` 标记为 persisted presentation，live viewport / display intent / pending render 不进入持久 revision。
- 普通地图文档新增独立 `PersistedDocumentId` v1：旧图按稳定元数据确定性派生，既有合法 id 保留，header / map 冲突或未知 identity version 拒绝；copy-on-export 不改写源 map。replica patch path 改由同一 registry 审计，五字段 target / applied checksum 与 v3 29-section 往返通过；TypeScript adapters 显式隔离 runtime、persisted、render preparation 和 headless identity。首轮 checkpoint 版本为 `0.5.9`，浏览器仍未启动、未操作、未验收，评审前不进入 Manifest。
- `349-3a` 首轮只读评审 `BLOCK`：v1 header-only identity 在迁移器内过早派生、拓扑 cell 数进入派生种子、persisted presentation 与 canonical-revision-only checksum cache 不一致、直接导出和 TS adapter 对非法 identity 不够严格。最窄修正把 header identity 在 v1 normalization 前传入 map，派生种子移除 checksum / topology 等可变状态，以 persisted-presentation checksum 扩充 cache key，并统一 JS / TS identity 语法与版本拒绝；对应反例加入复审门。
- 四项评审修正独立递增版本至 `0.5.10`，只用于 `349-3a` 复审，不提前开启 Manifest。
- `349-3a` 同一只读评审智能体复审 `ACCEPT`；当前进入 `349-4`，只建立 capability-aware Domain Manifest、注册器和不改路由的影子审计。
- `349-4` 建立 capability-aware `DomainModuleManifest` 与运行时注册审计，交叉核对 canonical field、command / undo、regeneration binding / lock / replacement、真实 Worker task / result / patch、panel 引用、persistence、API target / schema / capability / error / documentation，以及按实际能力要求的 regression coverage。notes、markers、population 三份样本均为 `shadow`，其中 `population.compute` 与真实 Worker registry 及源码 write set 对照；正式 runtime 对 Manifest 的 import 保持 `0`。
- `349-4` 首轮专项登记 `3` 个领域、`44` 个分类 descriptor 和 `19` 类负例；Manifest API binding 的真实 method 直接对照 `API_METHODS`，Marker 重生成按实际 `generate.regenerate(kind="markers")` 登记，不虚构独立公开方法。注册冲突也验证为原子失败，不会留下半注册状态。浏览器仍未启动、未操作、未验收，评审前不进入 facade。
- `349-4` 冻结门通过：core typecheck / contracts / manifests、canonical field audit、对象与辅助对象创建、人口增减 / 转移、API 数据兼容及 production build；构建为 `1361` modules。版本升至 `0.5.11`，checkpoint 待同一只读智能体评审。
- `349-4` 首轮只读评审 `BLOCK` 四项 P1：Worker / regression / API resolver 可缺省导致审计降级，API schema / errors / docs 仍是自填字符串，`markers.delete` 漏 `notes` 且多项 headless profile 与真实 registry 不符，regeneration 未进入全局 ID 账本。最窄修正将 resolver 全部改为必填，以既有 API description / metadata 逐项校验，收窄三域 profile 与 Marker 写集，并把 regeneration 纳入原子冲突预检；专项扩为 `45` 个 descriptor、`28` 类负例，修正 checkpoint 版本准备升至 `0.5.12`。
- 修正后 core Manifest、typecheck、`328 / 328` API description、headless write、辅助对象与 production build 均通过；构建仍为 `1361` modules。浏览器执行保持 `0`，修正 checkpoint 待同一智能体复审。
- `349-4` 同一只读评审智能体 blocker-only 复审 `ACCEPT`；版本 `0.5.12`，当前进入 `349-5`，只建立薄 facade 与 commit / projection 影子记录，不切换旧 action 或建立第二 map owner。
- `349-5` 新增 getter-only `MapCoreEngine` 与 `MapRuntimeCoordinator` 影子实现：facade 不持有 map，只观察 legacy owner 的 revision / history 事实；七步 lifecycle 在 canonical commit 前不分配 commitId，publish 时二次核对 owner。publish 前允许记录 legacy rollback，publish 后 projection 只进入 degraded / retry / resync，不反向改写 canonical history。
- facade 专项覆盖 owner 替换、borrow escape / async、interactive / headless / adoption revision、非法 lifecycle、projection 集合漂移、发布前回滚与发布后恢复；legacy revision / history 写入和 runtime import 均为 `0`。core 四门与 production build 通过，构建保持 `1361 modules`，`source/` 零改动；版本准备升至 `0.5.13`，浏览器仍未启动、未操作、未验收，checkpoint 待评审。
- `349-5` 首轮只读评审 `BLOCK` 四项 P1：嵌套 borrow 可改写/逃逸、一次 legacy owner/history 转换可被重复认领、rollback operation 可复活、公开 projection update 可绕过 coordinator；另指出根 `AGENTS.md` 状态滞后。最窄修正采用回调后撤销的深层只读 membrane、原子 transition claim（rollback 释放）、rollback 不可复活终态，以及 Core 统一状态迁移和 settled 判定；对应负例已加入，修正候选版本准备升至 `0.5.14`。
- `349-5` 首次 blocker-only 复审确认 transition claim、rollback 终态、projection 边界和 AGENTS 状态已闭合，但同一只读 membrane P1 仍有三条具体绕过：TypedArray 原生回调、accessor descriptor 与 ArrayBuffer transfer。用户依据仓库停止条件显式批准再做一次最窄修正；原生 callback 参数现重新包裹为借用引用，accessor 读取 / descriptor 不暴露真实 getter，ArrayBuffer / SharedArrayBuffer backing-store mutator 显式拒绝，并加入三条复现负例。
- `349-5` 最终修正版本 `0.5.15` 的 `typecheck:core`、Facade / core contract 专项和 production build 通过，构建保持 `1361 modules`，runtime import / source 改动 / 浏览器执行均为 `0`；同一只读评审智能体最终 blocker-only 复审 `ACCEPT`。当前进入 `349-6`，只做 notes command / history / query / persistence / API 垂直切片。
- `349-6` 以 TypeScript adapter 激活 notes Manifest 的五条正式 command，复用唯一 `state.map`、既有 `EditHistory / MapRevisionTracker` 与原 command 算法；对象面板备注、独立备注、导入 / 删除、undo / redo、面板 query 和备注摘要导出进入同一观察链。query / persistence 返回冻结 detached snapshot，invalid / no-op 不消耗 commit；全图压缩保存、Worker、regeneration、view 和 layer 均未扩入本阶段。
- notes core / typecheck / Manifest / facade / import / object / API / migration 门及 production build 通过，构建为 `1368 modules`；v1 旧备注、当前 save round-trip、九次单调 revision / commit 与 projection settled 均有专项证据。既有 note-import 门的标题断言滞后于 HEAD 文案，仅纠正测试期待。候选版本为 `0.5.16`，浏览器执行 `0`，checkpoint 待同一只读智能体评审。
- `349-6` 首轮评审 `BLOCK` 一项 P1：legacy history 已提交后若 UI 刷新失败，adapter 会按 `executed:false` 或 throw 误走提交前拒绝，造成 map / revision / history 已前进而 core commit 丢失。最窄修正以 revision + history 双事实识别已发生 canonical transition，先记录 / 发布 commit，再把 UI 标为 degraded；普通 command 不再冒充 no-op，undo / redo 的刷新异常仍可抛出但不会丢 commit。专项增加两类复现并达到 `11` 次单调 revision / commit，修正版本为 `0.5.17`，待 blocker-only 复审。
- `349-6` 首次 blocker-only 复审继续发现同类 P1：抛异常型 post-commit command 的 commit / degraded 已正确记录，但外层 catch 删除了 command 的 notes 归属，后续 undo 会绕过 adapter。最终窄修仅在确认没有 canonical transition 时删除 binding；新增“原异常保持、归属保留、undo 再提交”复现，专项达到 `13` 次单调 revision / commit。最终修正版本为 `0.5.18`，待最后一次 blocker-only 复审。
- `349-6` 最后一次 blocker-only 复审 `ACCEPT`。本阶段以 `0.5.18 / f71a0ff` 接受，`source/` 与浏览器执行均为 `0`；当前进入 `349-7`，只做 markers presentation / layer / picking 及 identity / draw / pick / export 的 Node / source 契约。
- `349-7` 新增共享 marker presentation source，统一 WebGL point / DOM icon、direct picking、picking DTO 回绑和 Feature GeoJSON 的 marker 顺序与 identity；TypeScript 只读 runtime 通过 getter-only core 向 marker panel 提供 detached snapshot，query 不产生 operation / commit。命令、history、资源重生成和 Worker 未迁移，Manifest 保持 shadow。
- 固定 1k 的 draw / picking / DTO / export 均为 `8`，markers core、typecheck、Manifest、面板图标、selection policy、10k render preparation、辅助对象与 production build 通过，构建为 `1370 modules`，候选版本 `0.5.19`。误执行一次实际带浏览器的 `regress:api-exports`，因既有 operation-stall 首败停止且不计验收，未诊断或复跑；后续继续禁用浏览器。
- `349-7` 同一只读评审智能体首轮 `ACCEPT`，最终文档修订后的 checkpoint 为 `0.5.19 / 57e05d5`。当前进入 `349-8`，仅围绕真实 `population.compute` Worker task 收敛 binding / result / patch 与 checksum / stale / cancel / gap / restart，不批量迁移其他 Worker。
- `349-8` 新增 TypeScript population Worker protocol adapter：主线程用既有 source fingerprint 锁定请求快照，正式 commit 前把 legacy binding 显式映射为核心 `compute / pre-commit` binding，并校验 output / plan、result kind、请求类型、来源指纹和 Manifest patch 写集。history patch 发往 Worker 前也过同一门；legacy v1 patch 因缺少 core checksum / revision 字段，不伪装成 `ComputedDomainPatch`。
- population core protocol、typecheck、Manifest、人口增减 / 转移、10k / 100k Worker、通用 Worker registry 与 production build `1373 modules` 通过；专项覆盖 stale / gap / checksum / unknown / duplicate / cancel rollback，100k 约 `627.7ms / 1560 patch paths`。既有 UI/API 门仍因 HEAD 文案“查看转移影响”与夹具期待“预检转移”不一致首败，本阶段未改该产品文案且不声称该门通过。候选版本 `0.5.20`，浏览器执行 `0`，checkpoint 待同一智能体评审。
- `349-8` 首轮评审 `BLOCK` 一项 P1：原 population source fingerprint 没覆盖 settlement routes、grid points / burg / feature、Feature 等真实读取依赖，同 revision 的漂移 Worker mirror 可绕过 stale 门并产出覆盖 `settlements.metadata` 的 patch。最窄修正让指纹覆盖全部可写根和额外真实读取集，专项逐项漂移 grid / Feature / routes / pack-grid / politics / society / economy / stale metadata，并以携旧指纹的真实 task 验证 apply 前拒绝。
- 修正后 population core protocol、typecheck、Manifest、10k / 100k Worker、通用 Worker registry 与 production build `1373 modules` 通过；100k 为约 `966.5ms / 1560 patch paths`。修正版本 `0.5.21`，浏览器执行保持 `0`，待同一智能体 blocker-only 复审。
- `349-8` 首次 blocker-only 复审继续发现同类 P1：`pack.goods` 缺失的旧数据会读取 `economy.goods`，首轮修正只覆盖前者，fallback goods 漂移仍会改变 demand patch。最终窄修指纹化两份存在性和 effective goods，新增旧数据真实 task 的 stale 拒绝；core protocol、typecheck、Manifest、10k / 100k Worker 与 build `1373 modules` 通过，100k 约 `983.7ms / 1560 patch paths`。最终修正版本 `0.5.22`，待最后一次 blocker-only 复审。
- `349-8` 最后一次 blocker-only 复审 `ACCEPT`，接受点为 `0.5.22 / 4411e86`；当前进入 `349-9`，只接 dependency registry、projection 状态与局部失效，不迁移未登记领域。
- `349-9` 为三份已登记 Manifest 的 derived system 补齐 invalidatedBy / scope / rebuild / presentation reuse / verifier，并新增 dependency planner：notes 带 affected object 为 local，已知无派生消费者为 exact，presentation 不推进 canonical，未知写 / 缺 scope / 宽依赖 / 缺 projection target 显式 full rebuild。active notes 的 projection / invalidated IDs 改由计划提供，markers / population 不扩大 active 路由。
- coordinator 新增受控 retry / resync：只从 degraded 开始，失败回到 degraded，成功 ready，不回滚已发布 revision / history。dependencies、Manifest 30 类负例、facade recovery、notes 13 commit / revision、markers / population core、typecheck 与 production build `1375 modules` 通过；初始 checkpoint 版本 `0.5.23`，浏览器执行 `0`。
- `349-9` 首轮评审给出两项 P1：derived verifier 未绑定真实门禁；非法 recovery mode 与空错误可能泄漏锁或滞留中间态。修正后 verifier 必须属于领域 regression gates 且继续解析到 package script；恢复模式与首转换纳入 finally 清理边界，空错误生成非空原因。Manifest 负例增至 `31`，全部既有目标门与 `1375 modules` 构建复验通过，版本升至 `0.5.24`，待同一智能体 blocker-only 复审。
- `349-9` 第二轮复审指出资源经济 verifier 虽解析到 auxiliary script，但该脚本未覆盖资源 marker、pack / economy 派生和失败恢复。新增 `regress:markers-resource-economy-core`：真实生成 `3` 个资源点、`4` 个 marker resource cell、`281` 个 marker resource deal，核对 demand 和 `economy-demand / object-index` 失效计划，并在 resource-economy 后注入失败验证四域原子恢复。Manifest、markers、typecheck 与 `1375 modules` 构建复验通过，版本升至 `0.5.25`，待同一智能体 blocker-only 复审。
- `349-9` 最后一次 blocker-only 复审 `ACCEPT`，verifier 解析、recovery 清理和资源经济 verifier 覆盖三项 P1 均闭合；当前进入 `349-10a`，只迁移 terrain / grid / height-derived / climate / ocean / topology 基础域。
- `349-10a` 新增 shadow foundation Manifest 与统一 Worker 结果 adapter，四个正式基础 task 在 commit 前校验 binding、result kind、patch / replacement 和 prepared renderer source；renderer preparation、cache、picking 与 surface owner 补齐 topology revision，旧 topology 结果不再复用。类型、Manifest / dependency、基础组合门、高度 history、10k→100k grid refinement 与 production build `1377 modules` 通过，候选版本 `0.5.26`，浏览器执行 `0`。
- 既有 ocean world 完整夹具稳定暴露省份 `110 / 118` 两项行政一致性拒绝，另一个约束 bundle 缺少锁国无省会样本；按首败停止规则未补夹具或放宽门。它们归入紧邻 `349-10b` 的首个最小复现 / 归因门，当前只冻结基础域 checkpoint 等待同一只读评审智能体。
- `349-10a@7669915 / 0.5.26` 首轮评审 `BLOCK` 两项 P1：正式 binding factory 未携带 topology revision，合成测试未覆盖真实 owner；replacement 只校验五个基础 section，残缺结果可能清空其余 canonical 域。最窄修复新增保持旧 API 形状的内部 core revision vector，topology 随 canonical commit 保守推进并可回滚，四入口与 renderer request 共用；replacement 从唯一 canonical registry 验证全部必需 section 和关键集合结构。真实 owner、ocean / grid 残缺结果原子拒绝、notes / markers / cells 兼容门已通过，修正候选版本 `0.5.27`。
- `349-10a@88ecc40 / 0.5.27` 第二轮复审仍 `BLOCK` 两项 P1：正式 commit 后手工只给 map revision 加一，实际 owner 同时推进 topology，renderer install 会立即 stale；顶层 section 齐全但域内容清空仍可通过整图 replacement。`0.5.28` 改为 history commit 后读取真实 core owner 并校验双 revision 单步推进，renderer install / grid fingerprint / UI settle 共用；replacement 逐域验证完整结构锚点，新增 commit→render→settle 及 ocean / grid / economy 空域原子拒绝回归，浏览器执行仍为 `0`。
- `349-10a@704a955 / 0.5.28` 经同一只读评审智能体 blocker-only 复审 `ACCEPT`，第二轮两项 P1 闭合且无新增 P0 / P1；阶段转入 `349-10b`，首门按冻结顺序先归因省份 110 / 118 与锁国无省会夹具，不放宽行政门。
- `349-10b` 首门确认省份 `110 / 118` 不是行政规则本身无解，而是 ocean world 的 cities-routes / states-provinces 调用没有启用既有 `repairInconsistentProvincialCapitals`；补齐后 10k / 50k / 100k 世界重建通过。锁国无省会改为从真实有效省份构造确定性反例，settlements 重建跳过锁定国家 / 省份的首府再生成，完整 state / province / city 支撑包络保持不变；锁冲突诊断对 `undefined` 快照不再自身崩溃。
- 新增 shadow `society-politics` Manifest 和 TypeScript Worker 输出 validator，真实 `regeneration.compute` 的 religions / states / provinces 在正式 commit 前统一校验完整 binding、精确 policy / patch 写集、society / politics 与 pack 镜像及行政首府引用；真实 `EditHistory + MapRevisionTracker` 的提交、撤销、重做与首轮六类拒绝通过。Manifest 注册表为 `5 domains / 78 descriptors`，dependency 注册表为 `9 systems`；旧 v1 迁移、行政 / 社会锁、世界重建和 production build `1379 modules` 均通过，版本准备升至 `0.5.29`。
- 完整 world constraint 继续前进后暴露独立问题：锁定 Feature 的港口引用在 `cities-routes` 后由 `382 → 366`，锁快照报告删除 16 个 `pack.burgs` 引用。该问题属于 routes / features owner，不扩入 10b；重新评估后维持 `10b → 10c → 10d` 顺序，并把它登记为原定 10d 首门。浏览器执行保持 `0`。
- `349-10b@2700d2b / 0.5.29` 首轮评审 `BLOCK` 两项 P1：operation 可用完整 writeSet 包装删除 / undefined 值并实际清空 canonical 路径；首府检查仅从行政对象正向查 city / burg，清零行政锚点后保留反向首府标记仍会放行。`0.5.30` 最窄修正给每个 executed 精确路径增加现存值和结构门，并把国家 / 省份 ↔ city ↔ burg 改为双向唯一引用；普通清零、孤立 / 重复标记拒绝，只有 before-image 已为零且受锁的行政对象允许零首府，锁国零省会正例继续通过。
- `349-10b@a71046d / 0.5.30` 复审确认双向首府与受锁 zero before-image 已闭合，但继续 `BLOCK` 一项 P1：DataView 可冒充 cell TypedArray，Uint8Array / Map 等非 plain object 可冒充 politics / settlements 记录。`0.5.31` 把 cell 容器限定为 Array 或非 DataView TypedArray，把记录限定为 plain / null-prototype object，并新增 DataView、TypedArray-as-record、Map-as-record 三类 pre-commit 拒绝。
- `349-10b / 0.5.31` 经同一只读评审智能体复审 `ACCEPT`，两轮 P1 全部闭合且无新增 P0 / P1；阶段转入 `349-10c`，首门盘点 settlements / zones / labels / measurements 的真实 owner、写集和依赖边界，不提前迁移 routes / economy。
- 10c 首门确认现有 `WorkerTaskDescriptor.id` 同时充当领域 descriptor identity 与真实 task registry key；`regeneration.compute` 已由 society-politics 登记后，cities / zones 无法在独立领域再次如实声明，即便 result kind 不重叠。按动态规则插入独立 `349-10c0`：新增显式 `task` transport binding，`id` 只作领域 descriptor；注册表允许同 task 的 disjoint result kind，仍按 `task + resultKind` 跨领域和单领域防重并保持失败原子。既有 foundation / population 显式声明同名 task，society-politics 改用领域专属 descriptor id，正式 Worker registry / DTO 与业务代码不变；版本准备升至 `0.5.32`。
- `349-10c0 / 0.5.32` 经同一只读评审智能体首轮 `ACCEPT`，共享 transport 正例、重叠 owner 拒绝、既有 Worker 兼容和计划同步均无 P0 / P1；当前返回 `349-10c`。
- `349-10c` 新增 settlements / zones / labels / measurements 四份 shadow Manifest，真实 cities / zones `regeneration.compute` 在 commit 前统一校验 binding、精确 patch、容器与 city↔burg / route / politics、zone↔pack 镜像；注册表为 `9 domains / 135 descriptors`，dependency 为 `13 systems`。2k Worker、history、标签备注、测量纯 Node 导入、v1 migration、四域代表性专项与 build `1382 modules` 通过，版本准备升至 `0.5.33`。
- `349-10c / 0.5.33` 首轮只读评审返回 `BLOCK`：cities validator 未闭合行政首府引用与 grid / pack burg cell 镜像，zones validator 未按 source pack 拒绝 cell 越界。`0.5.34` blocker-only 候选把 before-image sourceMap 接入正式 pre-commit，复用 society-politics 行政校验并兼容 from-empty 无 claim 的合法零首府，新增 dangling / 清零 / 重复 claim、双 cell 镜像和 zone 越界反例；core typecheck、新领域协议与 society-politics 全专项通过，浏览器运行 `0`，待同一评审复审。
- `349-10c / 0.5.34` 经同一只读评审智能体 blocker-only 复审 `ACCEPT`，三个 P1 全部闭合且无新增 P0 / P1；当前进入 349-10d，首门处理锁定 Feature 港口引用丢失。
- 本阶段校准了已被既有功能改变的测试夹具：harbor 现为 `0/1`、地区主动重生成从空结果建立、港口修复调用携 `repairProtectedDerived: true`、PNG 复用共享 selector、生成种子不再保证每图出现跨海国家；跨海语义继续由确定性夹具覆盖。误调用一次实际启动 Playwright 的 `regress:measurement` 后立即终止，不计验收且不使用结果；后续只用纯 Node measurement 门，最终验收方案须加入脚本防误触审计。候选 checkpoint 待同一智能体评审，未进入 349-10d。
- `349-10d` 首门确认 cities-routes 会用活动城市覆盖 16 个携锁定 Feature 港口引用的历史 tombstone 槽，导致直接引用 `382 → 366`；现保留引用墓碑与 counterpart ID。主动 Feature 重建另会给仅携 `port` 的锁定对象新增 `feature` 字段，现于拓扑重建最终断言前恢复锁定直接引用字段，并由 pre-commit 协议独立核验对象、cell 归属和引用集合。
- 新增 features / routes / rivers Manifest，markers 补齐真实 regeneration Worker；四类 `regeneration.compute` 与 route-path result ownership、精确写集、binding、容器及跨域镜像在正式 commit 前校验。注册表达到 `12 domains / 175 descriptors`，dependency 为 `16 systems`。阶段门暴露并闭合三个既有夹具漂移，以及自然湖泊出口从相邻岸上 spill cell 起步却被编辑预检拒绝的真实兼容缺口；完整 world constraint `11 stages / 15 locked kinds` 与 production build `1387 modules` 通过，浏览器执行 `0`，版本准备升至 `0.5.36` 并交同一评审智能体审查。
- `349-10d / 0.5.36` 首轮评审 `BLOCK` 五项 P1：route-path Manifest 宽写集、Feature 普通跨域镜像 / 引用、route cell links、river topology / cell mirror、湖泊出口方向均未闭合。`0.5.37` blocker-only 候选把 route-path 收为真实 `21` roots 并静态对照，新增 Feature city / route / marker 与直接引用、路线边索引、河流邻接 / 哨兵 / 双向归属门，岸上 spill 出口拒绝后续入湖；新协议 `21` 类负例及 route-path 10k / 100k、路线 / 河流 / Feature / 完整 world constraint 复验通过，待同一评审复审。
- `349-10d / 0.5.37` 第二轮复审继续 `BLOCK` 两项 P1：Feature city / marker cell 未绑定 source identity，river mirror 仍可部分清空 owner。`0.5.38` 候选对活动 city / burg / marker 身份位置做 source before-image 对照，并逐段核验河流 owner，只对水域尾段、父河与同父汇流保留显式例外；新增 2 类 Feature 负例、部分 river 清空负例与 3 个 10k 河网 owner 正例，新协议共 `23` 类拒绝，待同一评审复审。
- `349-10d / 0.5.38` 第三轮复审继续 `BLOCK` 一项 P1：水域零 owner 没有限定为路径尾缀，子河 owner 也没有限定为终点汇流。`0.5.39` 候选拒绝进入水域后再次回到陆地，并只允许子河 / 同父支流在最后一个真实 cell 提供合法 owner；新增 land-water-land 与 child-overlap 两类反例后，新协议共 `25` 类拒绝，3 个额外 10k 河网 owner 正例继续通过，待同一评审复审。
- 同一只读评审智能体对 `349-10d / 65ea8b2 / 0.5.39` 给出 `ACCEPT`，第三轮剩余 P1 已闭合且无新增 P0 / P1。状态 checkpoint 升至 `0.5.40` 并进入 349-10e，先盘点 economy / diplomacy / military 的真实 owner、精确写集、跨域引用与旧数据门，不提前收口全图 adoption。

## 2026-08-18：登记第 348 项——主动重生成后的紧凑保存与港口派生修复

- 用户当前 `5410/?debug=1` 地图在主动重生成后保存报“紧凑二进制拒绝 holey array”。正式生成函数最小复现确认，保留高编号锁定城镇 / burg / 路线时按旧 ID 直接赋值，分别可令 `settlements.cities`、`pack.burgs`、`pack.routes` 产生真实数组空洞，随后正式 v3 编码稳定拒绝；这是应用写出无效 canonical 形态，不归因于原存档天然损坏。
- 当前地图另保留 `protected-port-invalid` 主动重生成拒绝。第 348 项冻结锁定语义：核心身份、位置和人工内容继续保护；港口镜像、港口锚点及相关拓扑属于可重派生数据，用户主动重生成时允许在事务内修复并由撤销恢复，不再以旧派生不一致整项阻断。
- 最终覆盖高 ID 锁定重生成、既有 holey 内存地图抢救保存、紧凑往返、旧格式兼容、城镇面板结果反馈、Worker / 构建及用户原标签页“先保存、再重生成、再保存 / 载入、最后撤销”。用户明确长期开发服务器导致的页面卷次陈旧不在本项范围。

## 2026-08-18：第 348 项实施——稠密身份数组、派生港口与行政候选纠错

- 重生成保留高编号锁定对象时，不再以稀疏索引赋值留下数组 hole；cities / burgs / routes 的身份槽统一显式 `null`，摘要、GeoJSON、面板统计等读取入口跳过占位。正式保存增加不改写内存地图的 copy-on-export 稠密化，既有 holey map 也可抢救输出 v3。
- 锁继续保护城市 / 路线的身份、位置与人工内容；仅在用户主动城镇 / 路线重生成路径启用 `repairProtectedDerived`，让失效港口镜像、港口锚点和可重算海路在事务内修复，载入诊断与底层严格入口仍保持拒绝语义。城镇面板补齐成功、未执行和失败反馈。
- 首轮真实页面虽完成 `949 → 1194`，但专项复核发现仅剩 `1` 个首都。根因是行政候选来自 TypedArray，直接 `.map(cell => ({cell, score}))` 会把对象强制转换为数字 `0`，使全部候选的 `.cell` 成为 `undefined`。候选先展开为普通 Array 后再评分，并为无城镇国家清除悬空首都；原存档离线回归现为 `1194` 城镇、`20` 个有效首都、`1` 个空国家首都归零、`marineCities = 0`，21 个国家镜像无失配。
- 专项锁 / 路线、港口拓扑、v3 容器与生产构建通过。用户原标签页已先保存 949 城镇基线（`9,602,882B`），正式重生成至 `1194 / 20 / 294` 城镇 / 首都 / 港口，再保存为 `9,576,578B`；两份文件离线解码的 cities / burgs / routes hole 均为 `0`。
- Chrome remote debugging 恢复并释放旧控制会话后，重新接管用户精确 `5410` 标签页。正式 UI 先导入重生成文件，恢复 `1194 / 20 / 294 / 1176` 城镇 / 首都 / 港口 / 资源城镇；再导入基线文件，恢复 `949 / 21 / 218 / 852` 且撤销 / 重做为空。`holey`、保存失败、地图接纳失败、LongTask 与 WebGL 错误均为 `0`；仅保留原档装载约 `2.1s` 的 operation-stall 阶段告警。第 348 项以版本 `0.5.3` 完成。

## 2026-08-17：第 347 项完成——城镇重生成安装热点收敛

- 可信分段账本确认用户当前存档 `7754.3ms` 中，城镇领域计算仅 `534.2ms`，主线程 prepared install 为 `6337.4ms`，其中路线拾取引用对象化回绑至 `5661.0ms`。固定 10k / 100k 覆盖率分别为 `95.6% / 95.1%`，未再用 Loading 采样推断阶段占比。
- picking 分片回绑先以紧凑位图替换字符串去重，并用消息通道避免计时器让出开销；首轮用户复验仍为 `7108.3ms`。第二回合让城镇安装在校验 Worker DTO 版本、binding、组件和统计摘要后，直接从已提交新地图构建 `cities + routeSegments` 局部索引，其它领域继续使用原安全回绑。
- 固定 100k 从 `2307.3ms` 收敛至 `1276.4ms`，局部 picking `2.8ms`、LongTask `0`、撤销恢复一致。用户精确 `5410` 当前存档正式入口为 `1886.3ms`，相对第 346 项 `9969ms` 降低 `81.1%`；`949 → 1017` 城镇、名称实质变化、`from-empty / marineCities = 0`，撤销恢复 `949` 与原始名称序列。
- render preparation、prepared installer、完全重算、约束、完整十一类 Worker、固定 100k、生产构建与用户原图通过。组合整图夹具仍被既有省会不一致反例拒绝，未越界修改。版本升至 `0.5.2`，第 347 项归档。

## 2026-08-17：登记第 347 项——100k 城镇完全重生成端到端性能优化

- 第 346 项确认用户当前存档正式城镇重生成仍需 `9969ms`；此前只优化了 Loading 阶段表达，没有改变领域计算、传输、提交、渲染安装或 UI 刷新。第 347 项把耗时改善单独立项，不再混称文案优化。
- 347-A 先复用既有 Worker / commit telemetry 并补齐点击到完成未归属间隙，要求分段覆盖误差 `≤max(100ms, wall 的 5%)`；347-B 只优化实测最大热点；347-C 完成固定 10k / 100k 与用户原图正式入口、撤销和生产构建终验。
- 最终硬门为同一用户当前存档相对 `9969ms` 至少降低 `40%` 且 `≤6000ms`，主线程单任务 `≤200ms`。完全重算、`marineCities = 0`、身份变化、锁 / 局部 / 冲突、港口道路标签、事务、回滚和撤销均保持，不以降数量、删层、延迟尾部工作或改完成时点换取成绩。

## 2026-08-16：第 346 项完成——真实重生成 Profile 与 Loading 文案优化

- 用户精确 `5410/?debug=1` 当前标签页通过正式按钮重生成城镇，`1017 / 253 / 337 -> 1011 / 238 / 334`，端到端墙钟 `9969ms`。约 `1.14s` 后调试读取至完成期间不稳定，故只把总墙钟作为可信 Profile，不伪造领域计算、结果接收、提交、安装与 UI 刷新的独立数值；等待链包含结果流接收、领域 patch、渲染准备 / 提交、标签与对象索引刷新及 owner commit。
- 十一类 Loading 分别说明真实上游资料，阶段统一为“重开一卷 → 汇集依据 → 重新铺陈 → 校定关系 → 落定成图 → 描清细节 → 新卷落定”。阶段 rank 单调前进并去除相同文案刷新；后台 `render-prepare` 不再冒充上屏，迟到输出不能把文案倒回计算阶段，普通界面继续隔离内部术语。
- 专项文案回归、源码语法、差异检查及生产构建通过。新版正式 UI 从浏览器原存档 `949` 城镇重生成到 `1017 / 253 / 337`，完成文案为“城镇新卷已经落定”；撤销恢复 `949` 与原始名称序列，Loading 隐藏。旧全局 Loading 浏览器夹具在无关的导航上下文和 `File.text()` 观测断言连续停止，按用户裁定登记阶段外事项、不扩修。版本升至 `0.5.1`。

## 2026-08-16：第 345 项完成——十一类重新生成统一为从空结果重算

- “重新生成”不再把未锁旧对象当作修补输入。城镇全图与局部入口仅保留显式锁和范围外对象，从当前陆地适居度、文化、政区和人口重新选址，国家首都与省会在新城镇生成后重选；成功返回明确的 `from-empty / marineCities = 0` 契约，水域或无效落点在提交前拒绝。
- 十类伴随审计修复了 Feature 未锁身份稳定化、国家目标数读取旧首都数、资源点目标数读取旧资源数、地区默认保留 manual / 非事件旧对象和军事旧事件回灌。道路、河流、省份、外交、宗教保持既有从上游生成语义；所有十一类显式锁、局部范围、Worker owner、事务、写集、撤销和旧档兼容仍为硬门。
- Node 水域夹具高度 `14`，两次城镇重算均为 `marine = 0`、旧对象引用复用 `0` 且指纹变化；十一类矩阵全部 `reused = 0 / changed = true`。浏览器锁门确认全锁 no-op、锁城/锁路、局部范围和重复道路边冲突，冲突稳定拒绝且地图/history/salt 不变。
- 用户精确 `5410` 原标签页当前存档正式两次重设城镇：`949 -> 1017 -> 1011`、港口 `218 -> 253 -> 238`、道路 `607 -> 337 -> 334`，两轮 `1011` 个同槽中仅 `2` 个名称相同；两次撤销恢复 `949 / 218 / 607` 和原始名称序列。版本升至 `0.5.0`。

## 2026-08-16：第 344 项完成——城镇主动重生成无候选省会收敛

- 用户原 `5410` 100k 地图选择“城镇”后真实返回“请求包含 27 个数据不一致的省份，全部省会重评均已拒绝”。第 343 项只移除了外层 preflight；城镇重建内部 `no-valid-candidate` 仍在结果提交前整批回滚。
- 修复模式现保留无候选省份的领土中心与所属国家，清空 politics / pack 悬空省会引用和错误省会标记后继续提交；手工省会重评仍严格报告。城镇数量、选址、港口、道路和地理评分未改。
- 无候选专项、Worker 浏览器 session、局部重生成、城镇 / 道路锁、22 入口矩阵、生产构建与完整十一类 Worker 通过。用户原图城镇 `949 -> 1218`、港口 `218 -> 252`、道路 `607 -> 341`；提交 health 为空、WebGL error `0`，Ctrl+Z 恢复 `949 / 218 / 607`。版本升至 `0.4.9`。

## 2026-08-16：第 343 项完成——主动重生成优先级与旧锁兼容

- 删除省份 / 城镇 Worker 写入前的旧省会整批拒绝；旧 `province.burg`、候选失配和 politics / pack 省份镜像改在重生成事务内归一。手工省会重评仍保持严格预检；十一类入口审计未发现第二个基于建议性地理质量的写前硬门，显式锁、scope / kind、schema / 引用、owner、写集、commit / rollback / undo 均保留。
- 用户原图继续暴露锁定城市 #8 的旧数据组合：缺失 `burg.provincial`、politics / pack 省份锚点分歧。锁保护现把缺失布尔字段正规化为 `false`，只把双镜像一致的省会锚点纳入锁闭包；锁定 / 保留城市在 settlement finalize 的首轮归属同步中也会跳过。有效锁仍原样保护，不再由一个失配旧锚点整批阻断其它省份。
- 浏览器专项中，故意破坏全部活动省份省会后，省份 / 城镇均成功提交、镜像归一并可撤销；同时修复 surface 只重着色复用包漏传 correction geometry 导致撤销失败。完整 Worker 十一类、局部重设、锁定省份 / 国家 / 城市 / 路线、锁覆盖矩阵与生产构建通过。
- 用户精确 `5410` 原 100k 标签页正式点击“重设省份”成功：省份 `235 -> 268`、道路 `607 -> 372`；随后 Ctrl+Z 恢复 `21 / 235 / 607` 国家 / 省份 / 道路，history `undo 0 / redo 1`，Loading 隐藏、非性能 health error 与 WebGL error 为 `0`。版本升至 `0.4.8`。

## 2026-08-16：登记第 343 项——用户主动重生成优先级与地理门禁收敛

- 用户当前 `5410` 原 100k 地图点击重设省份后，真实返回“请求包含 235 个数据不一致的省份，全部省会重评均已拒绝”。源码确认第 334 项加入的省会镜像预检在领域重生成之前整批拒绝 `provinces / cities`，把本应由重生成替换或重评的旧省会状态当成前置硬门。
- 本项把可修复的旧省会 / 候选地理状态从“写入前整批拒绝”改为“事务内重建、修复后验证”；显式锁、输入 / schema / 引用完整性、Worker owner、原子回滚和撤销保持硬门。同步审计十一类重生成入口，区分建议性地理规则与结构安全规则。
- 真实验收必须操作用户精确 Chrome 标签页的当前原图并点击正式“重设省份”入口；替代新图或只调用内部函数不能代替。

## 2026-08-16：第 342 项完成——surface owner 原子绑定与水陆阈值量化纠错

- surface base、cell visual correction、CPU geometry、cell ranges 与 cell attribute store 现共享不可变 owner；owner 绑定地图 identity、revision、两类几何长度和 range 指纹。prepared commit / rollback 成组切换，绘制在清屏前拒绝跨 owner 混装，同长度不同地图不能再冒充可复用资源。
- 原存档海上深绿色多边形的直接根因不是数据、岸线或退化 cell：合法水域高度 `19.5～19.99` 在 GPU cell attribute 中被四舍五入为陆地色阶起点 `20`，相邻水 cell 因而连成假岛链。高度量化现保持原值所在水陆侧，`19.99 → 19`、`20.01 → 20`，不修改地图或存档。
- surface、prepared installer、render preparation、cell attribute 与完整 Worker 专项、生产构建通过；真实 prepared 浏览器反例返回 `sameLengthRejected=true / previousFramePreserved=true / glError=0`。用户精确 `5410` Chrome 标签页恢复 checksum `28eede3c` 原 100k 存档到 `complete`，越浦—月溪外海在高度 / 国家 × 平滑开 / 关四种组合均无陆色块，最终 `states / 硬边界 / GPU 96.6万 / WebGL error 0`，启动失败与 owner 接纳错误为 `0`。版本升至 `0.4.7`。

## 2026-08-16：登记第 342 项——GPU surface 资源 owner 原子绑定

- 用户原 `5410` Chrome 标签页恢复旧 100k 存档后，海岸线外仍有无岸线描边的大块深绿色三角 / 多边形；原存档数据水陆身份、15 个恢复 cell、完整 surface 三角和 shore correction 的只读审计均无法产生该尺度异常，根因范围收敛到应用侧 GPU surface 资源跨地图 / revision 混装。
- 当前 `surfaceBaseBufferSet` 与平滑 correction 的正式性只按 `floatLength / vertexCount / wordLength` 判断，同规模地图或 latest-wins 安装中的旧 geometry 与新 cell attribute texture 可以在长度相同的情况下被接受并绘制。
- 本项为 surface base、cell correction、cell attribute 使用状态、CPU geometry 与 ranges 建立同一 owner，绑定地图 identity、revision 与 range 指纹；Worker commit / rollback 成组安装，draw 在清屏前 fail-closed。存档、schema、地图数据、海岸算法、政治配色、公开 API 与 15-cell 安全恢复保持不变。

## 2026-08-16：第 341 项完成——adoption owner 改由装载事务显式收口

- `adopt-result-map` 不再启动普通 `map-mirror` 的 `120s` 主线程终态 watchdog；generation / import / browser restore 的 pending owner 只由既有装载事务显式 commit，或在失败、取消、obsolete、回滚时 invalidate。普通显示 pending transaction 的 `120s` 防死锁门保持不变。
- Worker 专项把 adoption watchdog 人为缩为 `1ms`，等待 `1.1s` 后仍保持 pending 并可提交；完整十一类 Worker 与生产构建通过。10k 生成 Chrome 门返回同一 session `committed=true / invalidated=false`，协调器最终 `idle + adopted`，Loading、应用 console、page 与 WebGL error 均为 `0`；首轮旧夹具把正确 owner 误判为遗留，已按第 334 项既定 adoption 合同修正后目标复验通过。
- 用户当前 `5410` 精确标签页重新直载原 100k 浏览器存档，加载追踪到 `complete / 地图进入可交互状态`，启动层 `ready`、Loading 隐藏、WebGL error `0`，不再出现 owner 接纳失败。该 100k 恢复仍记录约 `23.3s` operation stall 与 page-load-timeout 性能告警，本项未将其伪称为全绿，也未扩改地图、schema、渲染几何或公开 API。版本升至 `0.4.6`。

## 2026-08-16：登记第 341 项——新地图 adoption owner 生命周期纠错

- 用户当前 `5410` Chrome 标签页直载后真实报“启动失败：地图 Worker owner 接纳新地图失败”，控制台栈锁定 `generate.newMap → loadMapIntoRuntime → app.js:4991`；页面已有 10k 地图投影和 WebGL 画面，但最终 owner commit 被拒绝。
- 第 338 项新增的 pending transaction `120s` watchdog 无差别覆盖 `map-mirror` 与 `adopt-result-map`。本次主线程画面安装 / 面板恢复超过该墙钟时间后，协调器以 `session-main-commit-timeout` 销毁了已经生成成功的 pending adoption owner；后续显式 commit 因会话已不存在只返回 `false`。
- 本项只把 generation / import / browser restore adoption 生命周期恢复为装载事务显式 commit / invalidate；普通显示 `map-mirror` 的 `120s` 防死锁门保持不变。必须补超过短 watchdog 的时序反例，并在用户精确标签页重新直载到最终 `map-ready`，不得再以画面已出现或局部 UI 可操作冒充启动完成。

## 2026-08-16：第 340 项完成——旧 100k 退化 cell 由 Voronoi 半平面安全恢复

- 既有 resolved / stored / canonical 全部失败后新增最终只读恢复：用 cell 与合法邻居中心对地图矩形逐次做 Voronoi 半平面裁剪，并继续执行清理、中心包含、Earcut 面积与越界验证；平滑与 hard surface 共用，不恢复 center fan 或零 range。
- 冻结原文件精确回放由 `99985 / 100000` 收敛到 `100000 / 100000`，恢复 cell 身份恰为原 `15` 项；hard surface `100000` 个 range 全部非零，地图 checksum `28eede3c` 与文件 SHA-256 `80ED426F...5CC12AE` 不变。fresh 三档 `10004 / 50142 / 99846` 全覆盖且新恢复计数均为 `0`。
- surface、render preparation、prepared installer、三档直接 mesh 与生产构建通过。shoreline pure 的既有 GPU-resident 源码正则和 `0.4474 < 0.5` 历史覆盖率仍分别阻断，未在本项扩修或误报通过。
- 用户原 `5410` 标签页刷新后真实恢复同一 `10万 / pack 4.3万` 地图，国家视图平滑关闭 / 开启均无 surface、import 或 WebGL 错误，最终全图无缺面与底色色块。Chrome 扩展缺少 file URL 权限，自动重选 Downloads 原文件未执行；原文件已在本地正式解析 / renderer 入口只读终验。版本升至 `0.4.5`。

## 2026-08-16：登记第 340 项——旧 100k 精化存档退化 cell 的安全表面恢复

- 用户原 `5410` 标签页导入 `2026-08-14` 的 `100000` cell `.webfmg` 时 fail-closed：`平滑表面仍有 15 个 cell 未覆盖`。原文件可完整解压迁移，失败地图未安装，浏览器原地图与下载存档均保留。
- 只读回放确认 `99985 / 100000` cell 已安全覆盖，缺失 cell 为 `10255 / 15010 / 15307 / 42620 / 52137 / 73684 / 83228 / 95626 / 98951 / 99301 / 99305 / 99424 / 99425 / 99559 / 99981`；其整数化保存顶点全部共线，resolved / stored / canonical 均无法形成非零面积多边形。缺口集中在旧 topology-preserving 100k 精化图，fresh 第 339 项样本未覆盖该兼容入口。
- 冻结方案为只读渲染兜底：既有四级安全路径全部失败后，才用 cell / 邻居中心和地图矩形重建 Voronoi 半平面交集，并继续经过现有安全 Earcut 验证；平滑与硬表面共用，不修改地图数据、原文件、schema、身份或 checksum，不恢复 center fan 与零 range。

## 2026-08-16：第 339 项完成——显示提交与相机交互真正解耦

- 显示内容 current token 不再包含 camera / canvas；Worker 准备期间的平移缩放只记录 viewport 漂移，renderer 在安装后以最新相机重绘。地图 identity / revision、绑定、显示 presentation 与晚到 mutation 仍保持严格失效。
- 最终浏览器首败进一步锁定到 `after-resume`：成功 `resumePreparedWorkerRenderInstall` 已消费并清空 deferred 队列，旧代码却继续要求“队列仍存在”，所以每次完整 Worker 视图提交都会被误判 obsolete 并回滚。现分为提交前 sequence-current 与提交后 sequence-committed 两个门；成功消费通过，晚到 mutation 因序号增长仍拒绝旧结果。
- unsafe center fan 已从 cell visual / hard surface 最终兜底移除；resolved / stored 边界失败时改用同一 canonical 顶点的安全角序重排，仍不可安全三角化则 fail-closed，零长度 range 不再能作为正式全图成功结果。固定 `10k / 50k / 100k` 正式数据覆盖分别为 `10004 / 50142 / 99846` 个 cell，零 range 与 emergency fan 均为 `0`。
- 完整十一类 Worker、display replay、surface set、prepared installer 与生产构建通过。用户原 `5410` 当前实际为 10k，未换图：文化及政体切换期间缩放后均保持最终模式，快速“省份→宗教→国家”只提交国家，平滑边界快速奇数切换最终开启，Loading 清零，截图未见深色色块、逐 cell 接缝或底色丢失。版本升至 `0.4.4`；存档 schema、公开 API、政治配色、地图数据、`source/` 与 Wiki 未改。

## 2026-08-16：登记第 339 项——视图提交与相机交互解耦

- 第 338 项后用户确认两类问题仍在：国家视图、平滑关闭时海岸仍有深绿色多边形 / 三角；视图切换中平移缩放会令切换失败。原 `5410` 真实复现国家→政体后约 `25ms` 缩放，约 `7s` 后仍回到国家，Loading 一度持续约 `15s`。
- 源码确认 `createWorkerRegenerationRenderContextToken` 同时包含 map presentation 与 camera / canvas，Worker 准备期间相机改变便被 `isSourceCurrent / isTargetCurrent` 判为 obsolete；renderer 已有 `workerRenderInstallViewportChanged` 与最新 viewport reconcile，但尚未来得及使用就被前置 current gate 拒绝。
- 本项保留 Worker 化，不恢复主线程全图重计算；把地图 / 显示意图身份与 viewport revision 分开，只串行化 renderer / GPU pending install，并为 commit、superseded、aborted、failed 四类终态释放队列。第 338 项允许的零长度 range 只保留诊断语义，最终画面必须有不越界的完整 cell 覆盖。

## 2026-08-16：第 338 项完成——Worker 显示提交改为主线程终态 ACK

- grid-cells 的局部零长度 surface range 现作为“该 cell 无安全三角”的合法占位保留，后续 cell 身份与连续覆盖不再错位；cell-visual 与整图全空仍严格拒绝，避免把损坏 payload 当成正常结果。
- 显示事务改为 prepared install、恢复绘制和最终提交回调全部成功后，才向 MapWorker session 发 delta `0` commit ACK。持久 MapWorker transaction 以单槽排队等待前项 commit / invalidate 终态，`120s` 超时负责失效并放行，避免成功信号过早造成竞态，也避免只等成功而永久死锁。
- surface、installer、display intent、ACK 顺序与完整十一类 Worker Node 门及生产构建通过。用户原 `5410` 的 100k 地图刷新后，国家视图完成 `7` 次快速切换与 `8` 次并发触发，开关终态、政治陆海底色持续同源；新增 operation failure、range / recovery 错误、Loading 与 WebGL error 均为 `0`。版本升至 `0.4.3`，地图数据、存档 schema、公开 API、`source/` 与 Wiki 未改。

## 2026-08-16：登记第 338 项——平滑边界快速切换的原子恢复

- 用户在第 337 项发布后的原 5410 标签页再次复现：高山无人区显露大量 cell 接缝，快速连点平滑边界后整图政治底色丢失，只剩统一蓝色与细碎边缘。关闭平滑只能隐藏部分线条，不能恢复底色。
- 同页开发诊断给出权威首错：`Worker surface cell range 偏移无效`，随后升级为“地图显示更新失败且恢复未完整完成”。第 337 项允许危险 cell 不产出三角并记录零长度 range，而 installer 仍拒绝零长度；首次安装与恢复复用同一错误校验，形成半安装画面。
- 用户补充冻结事务语义：Worker 输出只进入 pending，主线程完整安装并绘制后才回传 commit ACK；同图后续事件默认排队，失败 / 取消必须回滚并以 reject / invalidate 终态释放队列，不允许无成功回执永久死锁。现代码虽然已有 session commit 与 latest-wins，但 commitSession 发生在 `resumePreparedWorkerRenderInstall` 之前，回执时点过早。
- 第 338 项只收敛 grid cell 零长度 range、缺面保守着色、prepared transaction / rollback / recovery 与快速 latest-wins 的画面原子性；不重生成用户地图，不改 canonical 地图、政治配色、存档 schema、Worker owner、公开 API、`source/` 或 Wiki。

## 2026-08-16：第 337 项完成——消除精细共享顶点自交后的残余越界面

- 第 336 项虽把普通非凸 cell 改为安全 Earcut，但精细共享顶点解析仍会令固定 100k 地图的 `60` 个 cell 边界自交，失败后又回退旧中心扇分；其中 `9` 个 cell 的 `12` 个三角会越出自身边界，形成海岸深色色块，并在局部显示更新时暴露相邻 cell 接缝。关闭平滑边界仍存在异常，证明并非 smooth correction 或 HMR 生命周期单独导致。
- 共享顶点解析现先检查全部关联 cell；若某组精细点导致任一边界自交，就把该共享组整体恢复为原始点，避免单 cell 回退造成邻接裂缝。hard surface 的精细边界失败时只允许安全的原始边界三角化；两套边界都不安全时不再恢复越界中心扇分。
- 固定 `10k / 50k / 100k` 的 resolved / stored 安全三角化跳过数均为 `0 / 0`；原 100k 的 `60` 个自交与 `12` 个越界三角归零。surface 专项 Node、生产构建通过；旧 shoreline pure 门因仍匹配已移除的旧 `shoreLayers` 源码字符串而失败，按非本项陈旧夹具记录，未放宽也未冒充通过。
- 用户原 5410 标签页未换图：刷新后打开平滑边界、放大到约 `50 千米` 比例，连续往返高度 / 生物群系 / 省份 / 人口 / 国家，海岸、高地未再出现深色三角或逐 cell 接缝；最终恢复国家视图、平滑边界与适配视图。版本升至 `0.4.2`，地图数据、存档 schema、Worker 协议、生成算法、`source/` 与 Wiki 未改。

## 2026-08-16：登记第 337 项——热更新 surface 一致性纠错

- 用户确认第 336 项后仍能在原 5410 标签看到海岸深绿色块，并在局部显示热更新时观察到海岸 / 高山 cell 边框隐约显露；上一项只覆盖了普通非凸边界，没有覆盖精细共享顶点自交后的失败回退。
- 现场关闭平滑边界后色块仍存在；同一页面完整刷新后暂时干净，说明 smooth correction 不是唯一根因，需直接审计 hard surface 的实际三角几何和共享顶点一致性。
- 第 337 项只收敛 hard surface 几何安全与相邻共享边界，不重生成地图、不恢复 Worker 全图显示链，不改地图数据、政治配色、生成算法或持久格式。

## 2026-08-16：第 336 项完成——修复细分 cell 越界三角色块

- 深绿色海洋三角和高原无人区异常色并非政治 palette、无人区数据或平滑 correction 漂移：现场命中的异常区域是 canonical 海洋 cell，真正根因是 100k 细分后存在非凸 cell，而 hard surface 仍按 seed center 扇分，三角会越过真实多边形边界；GPU 常驻视图在多次切换后复用这份 hard base，因此稳定复现。
- hard surface 现复用既有安全 Earcut 边界三角化；只有正式安全三角化拒绝时才保留原 center-fan 兜底。可变三角数量通过正式 `surfaceCellRanges` 进入 hard-cell 增量 patch 布局，未恢复整图 Worker 或破坏 GPU 常驻切换。
- 非凸外置中心 Node 反例、render preparation、生产构建与 100k 平滑专项通过；100k 开 / 关 / 再开为 `18.2 / 18.6 / 16.7ms`，Worker、surface / line 重建和 LongTask 均为 `0`。用户原 5410 标签页重复执行高度、生物群系、省份、人口、国家往返后，海岸外色块消失，国家控件与画面同源，console / page error 为 `0`。版本升至 `0.4.1`，地图数据、存档 schema、Worker 协议、`source/` 与 Wiki 未改。

## 2026-08-16：登记第 336 项——平滑边界跨视图陆海着色一致性

- 用户报告平滑边界开启后，多次切换颜色视图再返回国家视图会在海岸外侧出现深绿色三角 / 多边形色块，高原无人区颜色也发生异常；初次进入页面不出现，视图切换性能本身已基本解决。
- 第 336 项冻结调查 stable surface、内部 correction、岸线 correction / cover、GPU cell 属性、政治 palette 与当前视图状态的同源性；禁止通过关闭平滑边界、清空补片或恢复整图 Worker 重建绕过。
- 本项只修显示派生层，不改地图数据、生成 / 重生成、存档 schema、Worker 协议、`source/` 或 Wiki。最终提交按版本规则从 `0.4.0` 递增 patch 版本。

## 2026-08-15：第 335 项完成——GPU 常驻视图与零重编译切换

- 335-A～J 全部接受。surface 稳定几何、GPU 常驻 cell attribute / palette、政治调色板、主题 / 海底 / 普通图层、本地平滑 correction、稳定 point / overlay / city / picking、latest-wins、build handshake 和一次性会话自愈已形成完整显示链。
- 10k / 50k / 100k 普通视图、100k 主题 / 海底 / 标签 / 路线、100k 保存导入恢复及用户原 5410 标签页通过。100k 主题为 `105.5ms`，其余显示操作为 `12.2～38.5ms` 量级，产品 LongTask 为 `0`；保存压缩体积为 `7,288,011B`。真实用户页恢复到 `v0.4.0` 后，政体→国家及缩放后的省份→生物群系→国家均正确，不再首次失效或反转。
- 正式 WebGL context 丢失 / 恢复接线补齐：不重新计算 CPU 地图，只按 retained CPU 投影分片重建 GPU 资源。真实 10k 强制恢复后高度 / 生物群系 / 国家逐像素一致，GPU 引用已更新且 LongTask、health、Loading、WebGL error 为 `0`。独立集成复核与最终验收通过；版本升至 `0.4.0`。任务分支完成 checkpoint 后统一推送并合入 `main`，`source/`、持久 schema 和用户地图内容未改。

## 2026-08-15：第 335-I 阶段完成——显示 latest-wins 与版本自愈

- 正式显示入口使用独立递增序列：同步 A→B→C 只执行 C，运行中的旧操作不能覆盖最新控件；UI 只忽略明确被取代的预期错误，真实失败仍回写 renderer 正式状态。
- 页面与 Worker 的每条协议 envelope 核对编译时 build id；失配返回 `worker_build_mismatch` 并提示保存后刷新，accepted 前也不得主线程 fallback。仅完整回滚后的 stale session / commit rejected 自动清 session 并重试一次，第二次失败不吞诊断。
- 真实 10k 首次 states、height→provinces→biomes + wheel、两类单次自愈和 build mismatch 均通过；renderer / API / active Tab 同源，health error、Loading 与 WebGL error 为 `0`。语法、差异、latest-wins、Worker 全合同、display ledger、API 收敛、生产构建通过；artifact 为 `work/task335-i-display-consistency-10000/result.json`。版本升至 `0.3.25`，建立本地 checkpoint 但不推送；下一阶段只进入 335-J。

## 2026-08-15：第 335-H 阶段完成——点图层与 overlay identity 稳定

- 人口、城市、Marker、资源与军事改为稳定 point buffer + draw ranges，显示开关不再重建 / 上传 point、替换 overlay、重绑 picking 或重装城市实例；真实地图数据变化仍走既有完整刷新。城市总开关只切 WebGL draw gate，并用轻量 city / marker / military overlay 更新保持碰撞和选择语义。
- 固定 100k 十次反转 / 恢复为 `12.2～38.5ms`，固定 10k 为 `11.1～19.6ms`；两档 LongTask、Worker、point refresh、overlay replace、picking rebuild、city reinstall 均为 `0`，全部正式引用、隐藏 / 恢复 picking 与最终 framebuffer 精确同源。修复中出现过的 `67 / 58 / 57 / 52ms` 城市恢复尾窗最终已收敛为 LongTask `0`，未借登记阈值放宽。
- 点层、city WebGL、render preparation、prepared installer、Worker 全合同、API 收敛、主题、生产构建与 10k / 100k Chrome 通过；artifact 为 `work/task335-h-layer-identity-10000/result.json` 与 `work/task335-h-layer-identity-100000/result.json`。版本升至 `0.3.24`，建立本地 checkpoint 但不推送；下一阶段只进入 335-I。

## 2026-08-15：第 335-G 阶段完成——平滑边界 correction 与岸线双态缓存

- 平滑边界不再由 Worker 重建整张 cell surface：稳定 hard surface 上新增 packed cell correction，岸线继续复用正式拓扑 correction；Worker 全量准备阶段预备平滑岸线面和平滑 / 硬岸线线条缓存，本地开关只切换缓存与 draw count。
- correction 以 `8MiB` 完整三角分段并进入 prepared installer 原子所有权；10k / 100k 与旧 Worker framebuffer 像素误差均为 `0`，surface / correction / attributes / overlay / picking 引用稳定。100k 首次开启、关闭、重复开启为 `20.2 / 18.7 / 16.4ms`，Worker / 完整 surface / line 重建和 LongTask 均为 `0`。
- 语法、差异、专项 Node、Worker 全合同、render preparation、prepared installer、主题、生产构建及 10k / 100k Chrome 通过；artifact 为 `work/task335-g-smooth-borders-10000/result.json` 与 `work/task335-g-smooth-borders-100000/result.json`。版本升至 `0.3.23`，建立本地 checkpoint 但不推送；下一阶段只进入 335-H。

## 2026-08-15：第 335-F 阶段完成——主题、海底与普通图层本地提交

- 主题、海底、标签及缓存新鲜的路线等普通图层切换不再进入 Worker render preparation；不满足 GPU 常驻条件或会改变真实几何的图层继续 fail-safe 使用原路径。主题边界线改为复用 retained CPU 顶点并只上传颜色，道路和标签保持分片更新。
- 固定 10k Chrome 中四项响应为 `48.6 / 12.5 / 14.2 / 13.2ms`，Worker、surface refresh、overlay replace、picking rebuild 与 LongTask 全为 `0`；主题 / 海底 framebuffer 和标签样式与同图 Worker 基线精确一致，正式引用及错误面不漂移。
- 语法、差异、专项 Node、Worker 全合同、prepared installer、主题 / API 收敛 / display ledger、生产构建与浏览器目标门通过；artifact 为 `work/task335-f-display-mutations-10000/result.json`。版本升至 `0.3.22`，建立本地 checkpoint 但不推送；下一阶段只进入 335-G。

## 2026-08-15：第 335-E 阶段完成——政治 palette 与 topology cache 解耦

- 关闭平滑边界时，国家 / 省份直接从 GPU identity texture 读取行政 ID，并查询各自 palette；切换不再调用 Worker、重建 surface 或触碰政治 topology cache。平滑政治 surface 与 correction 保持原路径，留待 335-G。
- 政治颜色跨独立新图运行不作为稳定 checksum 分母；目标门改用同一张正式地图的旧 Worker / GPU framebuffer A/B。国家与省份逐像素差异均为 `0`，目标切换的 Worker、surface refresh、LongTask 均为 `0`，surface、attributes、political cache、picking、overlay 引用不变。
- 语法、差异、两项专项 Node、生产构建和固定 10k Chrome 通过；artifact 为 `work/task335-e-political-views-10000/result.json`。版本升至 `0.3.21`，建立本地 checkpoint 但不推送；下一阶段只进入 335-F。

## 2026-08-15：第 335-D 阶段完成——普通颜色视图进入 GPU 常驻路径

- 高度、生物群系和人口在非平滑完整 grid geometry 上直接以 packed cell ID 读取 GPU 常驻 terrain / numeric texture 与 biome palette；切换只更新 shader 状态和画面，不再调用 Worker、重建 surface、替换 overlay 或重绑 picking。平滑政治面与不完整 ranges 继续 fail-safe 走旧路径。
- installer 新增严格 `grid-cells` ranges 合同：cell ID、顺序、连续偏移与完整覆盖任一不符都会拒绝，失败不泄漏 buffer / texture。显示事务记录本地 GPU 证据，控件只在正式 draw 成功后提交，错误仍可回滚。
- 固定 `10004` cells 的真实 Chrome 验收中，高度 / 生物群系 / 人口 framebuffer checksum `2949860715 / 1641731067 / 2261249289` 与旧 Worker 基线精确一致；三次 Worker run、surface refresh、LongTask、health、Loading、应用错误和 WebGL error 均为 `0`，正式 GPU / picking / overlay 引用不变。artifact 为 `work/task335-d-gpu-resident-views-10000/result.json`。版本升至 `0.3.20`，建立本地 checkpoint 但不推送；下一阶段只进入 335-E。

## 2026-08-15：第 335-C 阶段完成——GPU cell attribute 与 palette store

- renderer 新增三张 GPU 常驻 cell attribute texture：identity 保存行政 / 文化 / 宗教 ID，terrain 保存高度 / biome / feature / land，numeric 保存人口 / 温度 / 降水 / region；国家、省份、生物群系、文化、宗教另用五张 palette texture。它们是当前 map 的派生 GPU 投影，不改变持久存档。
- changed cells 按 texture row 合并局部写入；失败先恢复 GPU 旧值，全部成功后才推进 typed snapshot。context restore 从同一 snapshot 重建；prepared installer 的新旧 store 采用引用所有权，未提交、已提交回滚、finalize、commit fault 和嵌套事务均不会泄漏或误删正式纹理。
- 纯 Node / FakeGL、生产构建与 10k 正式浏览器门通过。`10004` cells 的三张 cell texture 各 `160064B`、五张 palette 合计 `1068B`；局部 patch 后旧八张纹理全部删除，新八张有效，14 类既有显示操作的 LongTask、health、Loading、应用错误与 WebGL error 均为 `0`。artifact 为 `work/task335-c-cell-attributes-10000/result.json`。版本升至 `0.3.19`，建立本地 checkpoint 但不推送；下一阶段只进入 335-D。

## 2026-08-15：第 335-B 阶段完成——稳定 surface geometry

- 正式 surface base 从每顶点 `x/y/rgba/side` 改为 `x/y/packed(cellId, side)` 的 12-byte 稳定几何；独立 Float32 RGB 兼容流只负责 B→C 过渡，颜色补丁不再上传位置或 identity。geometry / color 同段共同参与 prepare、commit、rollback、finalize、嵌套事务和失败释放，首段 geometry 继续提供 legacy alias。
- 4-byte Uint8 兼容流的窄诊断显示 framebuffer 有 `64692` 个通道发生最大 `1/255` 差异，因此没有登记放宽视觉，而是恢复精确 Float32 RGB；335-C 接管后才移除逐顶点颜色。10k 正式 `452091` 顶点的 geometry / RGB 各 `5425092B`，packed 与 legacy base 全 framebuffer checksum `781237281`、逐通道差异 `0`。
- 纯 Node 重组、FakeGL transaction、生产构建与 10k 正式 14 类视图门通过；LongTask、health、Loading、应用错误与 WebGL error 为 `0`。artifact 为 `work/task335-b-surface-10000/result.json`。版本升至 `0.3.18`，建立本地 checkpoint 但不推送；下一阶段只进入 335-C。

## 2026-08-15：第 335-A 阶段完成——冷暖显示账本与 100k 补丁顺序

- 显示事务新增 cold / warm / stale 分类、输入输出分包与首帧 / 已呈现帧账本。固定 100k 暖普通颜色为 `101～186ms / 3` 包；revision `0→1` 后虽仍为 `3` 包，却因渲染 cache 全失效耗时 `5.02s`；session 丢失后 cold 为 `21.07s / 1032` 包，输入复制占 `12.21s`。这冻结了后续 GPU 常驻架构必须消除的两类分母。
- 100k 首轮同时发现 emergency hard fan 前置会打乱颜色补丁 cell ID，导致首次国家视图被 installer 安全拒绝。补丁生成现单独按 cell ID 排序，几何先绘顺序不变；10k / 100k 目标复验均无 LongTask、应用错误、Loading 残留或 WebGL error。
- 静态审计、账本纯 Node、Worker app replay、render preparation、生产构建及真实 10k / 100k 通过。artifact 为 `work/task335-a-view-ledger-10000/result.json` 与 `work/task335-a-view-ledger-100000/result.json`。版本升至 `0.3.17`，建立本地 checkpoint 但不推送；下一阶段只进入 335-B。

## 2026-08-15：登记第 335 项——GPU 常驻视图与零重编译切换

- 用户确认现有视图切换仍未根治：历史 100k fresh 端到端约 `10.029s`，完整地图输入约 `6.829s`，而领域算法仅约 `0.546s`；旧省份 `9.529s` 中 Worker 渲染资料构建约 `4.886s`、主线程安装约 `1.887s`，证明瓶颈是全图复制、CPU 渲染编译、结果回传和事务安装，不是真正 WebGL draw。
- 第 335 项已登记为当前唯一权威任务，并从最新远端 `main@fdf0c8c` 建立独立分支 `codex/task-335-gpu-resident-views`。方案将 surface 从烘焙顶点颜色迁为稳定几何 + packed cell identity / side，建立 GPU 常驻 cell attribute / palette，使普通颜色视图、主题、海底和非几何图层退出整图 Worker / prepared renderer 链；政治 topology、平滑 correction、overlay / picking identity、UI latest-wins 与 build handshake 分阶段收敛。
- 用户要求每个 335-A～J 接受阶段各做一次本地 checkpoint commit，但阶段提交不推送；全部阶段统一验收后才推送任务分支、合入并推送 `main`。当前只完成权威登记与分支建立，产品代码、构建、浏览器、用户地图和 `source/` 均未修改；下一阶段固定为 335-A 端到端账本。

## 2026-08-15：第 334 项完成——100k Worker 延迟、重生成可靠性与平滑边界

- 唯一长期 MapWorker 已覆盖生成、显示、重生成与存档 owner；暖操作复用 session，不再跨领域反复输入整图。v3 紧凑 handoff、流式 checksum、颜色 / 海底局部 surface、短提交挂起、视图控件与实际 GPU 画面同源等阶段证据统一收口。
- 省份与城镇在 Worker 写入前执行省会镜像预检；预期拒绝保持 session idle 且不写 history / revision。省份 prepared layers 按当前画面裁剪，颜色 patch 原位事务；picking canonical 回绑在单 bucket 内分片。100k 省份约 `9529→2799ms`、城镇约 `2558→1197ms`，输入均为 `3` 包；省份残余 `92ms` 按用户既定精确规则登记。
- 平滑边界只为最终拒绝的异常 cell 增加 canonical 硬边 fan 兜底，正常图 emergency 为 `0`；固定异常 cell 和隔离六模式通过。完整 shoreline 的既有 coverage / lineTriangleCount 历史阈值没有放宽，也没有冒充全门通过。
- 静态、专项 Node、生产构建、真实隔离浏览器拒绝 / session 原子性和现有 100k artifact 均完成复核；独立集成复核及最终观察使的 blocker-only 复查闭合。第 334 项归档，版本保持本次提交对应的 `0.3.16`，随后按用户要求推送任务分支并快进合入 `main`。
## 2026-08-20：第 349-10f 阶段整图 profile checkpoint

- generation / import adoption、persistence export 与隔离 headless write 统一使用整图 profile owner / receipt 协议，不改变既有 canonical owner、算法或地图格式。
- 生成 / 导入 handoff 前后双门、导出 session commit 前 identity / checksum / bytes 门、headless revision `+1` 与持久 identity 门已接正式入口；协议异常复用既有 rollback / invalidate。
- 专项 `4` owner、`12` 负例及旧档、100k map-file、Worker adoption / archive、headless、identity、typecheck、build 通过；浏览器执行 `0`。版本 `0.5.46`，待只读评审。
- 首轮评审发现 preload 失败 session cleanup、topology render binding 与真实文档 checksum / cell count 三个 P1；`0.5.47` 已逐项闭合，负例增至 `15`，并验证失败接纳后下一持久任务不排队。待 blocker-only 复审。
- blocker-only 复审对 `6bcd0cf / 0.5.47` 给出 ACCEPT；349-10f 无剩余 P0 / P1。`0.5.48` 记录接受状态并开启 349-10g 的已证冗余路径盘点。

## 2026-08-20：第 349-10g 阶段旧核心路径收口 checkpoint

- 定义—引用—owner—测试矩阵只确认 `revisionProfile` 为 `0 / 0` 引用且无独立协议职责，予以删除；headless、persisted、presentation、render-resource 四个零产品引用 adapter 仍承担跨 profile 契约负例，明确保留。
- 新增静态审计固定 `state.map`、单一 revision / history owner、getter-only facade、`1 active / 14 shadow` Manifest、八个正式 pre-commit validator 与 map-file receipt 无重复实现。core 四门、六组领域协议、notes、whole-map、headless、typecheck 和 `1392 modules` build 通过；浏览器执行 `0`。
- markers 专项连续两次暴露 349-7 夹具与 349-10d 正式能力登记漂移；不回退已经通过 Manifest / 地理网络资源协议门的产品契约，动态插入独立 `349-10g-a` 同步夹具。未完成顺序调整为 `349-10g -> 349-10g-a -> 349-11`，当前 10g 待只读评审。
- 首轮评审确认删除 / 保留结论，但阻断初版审计的 owner、canonical map、Manifest 精确分母和共享 metadata builder 断言，并指出正式 markers 契约实际来自 349-10d。`0.5.50` 已将审计改为全产品源码与实际计数，精确核对两个 canonical map 赋值点、`15 / 1 / 14` Manifest 和 map-file 三个共享 metadata 调用；阶段归因同步更正，待 blocker-only 复审。
- blocker-only 复审对 `e628f17 / 0.5.50` 给出 `ACCEPT`；349-10g 无剩余 P0 / P1。`0.5.51` 记录接受状态并开启动态插入的 349-10g-a，只同步 markers 专项旧 capability 断言。
- 349-10g-a 将 349-7 markers 专项的 Worker / regeneration capability 断言同步为 349-10d 正式 `required / required`，产品 Manifest / runtime 改动为 `0`。markers `8` 个对象的 presentation / point / picking / DTO / GeoJSON 同源，Manifest、地理网络资源、typecheck 与 `1392 modules` build 通过；`0.5.52` checkpoint 待只读评审，浏览器执行 `0`。
- 同一只读评审对 `a084d4a / 0.5.52` 给出 `ACCEPT`，349-10g-a 无 P0 / P1。`0.5.53` 记录接受状态并开启最终 349-11；本阶段只执行防误触审计通过的非浏览器终验，并形成、评估但不执行浏览器验收方案。
- 349-11 防误触审计固定 `27` 个 package gate / `36` 个 Node 入口，终验前后均通过。核心 / 六组领域 / 两个真实 100k Worker / 整图 / map-file 100k / 通用 Worker / headless / v1 migration / API compatibility / typecheck / `1392 modules` build 全部通过，完整工具墙钟约 4 分钟；浏览器执行与 `source/` 改动均为 `0`。
- 未执行浏览器方案已覆盖 10k / 100k / v1 / holey / fault 数据、候选自动化入口、identity / revision / history / Worker / projection / renderer / persistence 断言、`>200ms` 阻断与配对退化阈值、截图、恢复、停止条件。可执行性评估为具备单独获批条件，当前视觉 / UI 状态仍明确为“未验证”；`0.5.54` checkpoint 待最终只读评审。
- 首轮最终评审完整复跑 27 / 27 门并确认本轮无浏览器误触，但指出初版防误触审计不读取入口源码 / 本地 tools 导入链，无法拒绝仓库中命名不可见却启动 Chromium 的真实反例。`0.5.55` 已加入递归源码扫描、驱动 / launch / CDP / WebDriver / 进程启动原语拒绝，并固定 `regress:measurement` 为必须拒绝的反例，待 blocker-only 复审。
- blocker-only 复审对 `3ea6bba / 0.5.55` 给出 `ACCEPT`，防误触 P1 闭合，无新增 P0 / P1。第 349 项全部阶段完成并归档，浏览器状态保持“未验证”，并行分支不合入或推送 `main`；归档状态提交版本升至 `0.5.56`。

## 2026-08-21：第 350-R3a 阶段完成——session / adoption / concurrency 重新验收

- 正式 worker-task、adoption owner、replica patch 与 10k / 100k graph stream 证明 generation adopted session、commit ACK、连续 patch、checksum drift 后 fresh resync、accepted cancel / obsolete、map replacement、pending viewport 及三阶段分包让步成立；本阶段产品改动与产品 P0/P1 均为 `0`。
- 首轮 static contract 评审发现大函数切片加字符串包含可由跨 helper 注释或同 helper 字符串伪造，catalog 条目 6/7 也漏了 worker-task prerequisite 删除负例。动态插入纯夹具 `R3a-f2`，使用固定版 Babel parser 按精确函数 AST 边界读取真实语法 token，并以两类内存破坏反例证明夹具会拒绝伪证。
- blocker-only 复审独立通过 session contract（`6 continuity / 1 replacement / 2 negative cases`）、catalog（`48 prerequisites / 50 scripts / 56 sources`）、语法与 diff-check，结论 `ACCEPT / P0 0 / P1 0`。浏览器 / CDP 为 `0`；R3a 冻结并进入 R3b，分支继续与 `main` 并行。

## 2026-08-21：第 350-R3b-f1 阶段完成——高度 / states 锁契约冻结

- 旧专项仍按高度重建迁移前的主线程控制流切片，修复后改用 Babel AST 精确函数边界、`DOMAIN_BY_KIND` 八项结构属性和真实 executable token arrays，冻结主线程 / Worker 同一 world closure bundle、states core 透传、五类锁读取、外交写前冲突与 salt 回滚。
- 两轮评审依次拦截字符串伪证和“调用存在但未绑定 / 未透传同一 bundle”假绿面；最终三个破坏性反例分别删除真实声明、core bundle 参数和 `lockedStates` 读取，standalone executable call / string 均无法补证。
- 专项输出 `main / worker capture = 1 / 1`、`domains = 8`、`protectedSlices = 5`、`negativeCases = 3`；语法、diff-check 与同一只读评审全部通过，最终 `ACCEPT / P0 0 / P1 0`。产品改动与浏览器 / CDP 均为 `0`，现进入 `R3b-p1` 四字段资源 owner 收口。

## 2026-08-21：第 350-R3b-p1 阶段完成——资源 binding 与 GPU 恢复事务收口

- 产品运行时新增 strict source / complete resource binding：只有发行入口接受 map/source/topology，DTO、cache、picking、surface owner 与 prepared result 必须额外携带非空 `renderPreparationId` 和单调 `renderGeneration`，不再补零或自动合成 legacy id。retained render cache 只在同 map/source/topology/generation 下复用，并为当前 preparation 重绑 envelope。
- prepared installer 在 prepare、原位 prepareCommit 和 commit 三处核对 renderer 的 latest-issued binding；原位 surface patch 只允许同 identity、同 generation、0/1 对称 revision 推进，commit 在同物理 buffers 上原子换新 owner。提交事务登记 active transaction，新 commit 先 finalize 旧 commit，晚到旧 rollback 不再覆盖新 owner 或 buffers。
- 换图失败先恢复 canonical map、revision 与 history，再做可降级 renderer reload；WebGL context restore 先在 staged resource container 创建 program/buffers/textures，成功后换入并在 ready 状态唯一 draw，任一阶段失败清理新资源、清空 owner 并进入显式 invalid/retry，restoring/invalid 外部 draw 早退。
- 首轮评审拦截 4 个 P1，第一次 blocker-only 又拦截迟到 rollback 与 restoring draw 2 个 P1；第二次 blocker-only 最终 `ACCEPT / P0 0 / P1 0`。严格 binding、自身 latest-wins direct test、A/B transaction lifecycle、typecheck、build（1396 modules）、syntax/diff 均 PASS；旧 prepared/render/worker 夹具因仍传三字段 binding 按预期失败，已单列纯夹具 `R3b-f2`。browser / CDP `0`。

## 2026-08-21：第 350-R3b-p1a 阶段完成——原位提交故障与异步回滚竞态收口

- `R3b-f2` 迁移 prepared-installer 夹具时暴露产品残留：原位提交把 descriptor 当普通字段保存，回滚形状错误；commit 在后续 setter fault 后提前 finalized，CPU/GPU 颜色 before-image 无法恢复；A rollback 的分片让步中若 B 接管同图 owner，A 仍会继续覆盖共享颜色。
- 插入独立产品阶段 `R3b-p1a`：descriptor 使用专用 before-image；结构回滚与异步颜色恢复生命周期分离；颜色分片每次 checkpoint 同时核对外部 current 与 source owner，接管后以 obsolete 结束且不再上传。测试加入窄 `now` seam，以确定时钟保证 yield，并先断言 B 在 yield 内真实提交。
- 同一只读评审独立复跑 13-case installer 专项、两处语法与 diff-check，最终 `ACCEPT / P0 0 / P1 0`。browser / CDP `0`；现回到 `R3b-f2` 完成 worker-task binding 与 reload / context fault 夹具。

## 2026-08-21：第 350-R3b-f2 阶段冻结待评审——完整资源夹具与恢复故障自证

- render-preparation、prepared-installer 与 worker-task 的旧三字段资源载荷全部迁为 complete resource binding；session foundation binding 保持原协议，不再混作渲染 owner。prepared 13-case 覆盖字段拒绝、跨 identity、full/in-place latest-issued、A/B 迟到 rollback、commit fault 和确定性 yield 接管。
- worker-task 增加两类真实故障：提取并执行正式 map rollback helper，在 renderer reload 抛错时验证 canonical map/revision/history 已先恢复且 loading finally 关闭；FakeGL 在 staged surface buffer 初始化时抛错，验证已建 `3 program / 2 buffer / 1 VAO` 清理、owner 失效、resource state=`invalid`、draw=`0`。手工 deferred highlight 夹具同步声明当前正式 `ready` 前置。
- `regress:render-preparation`、`regress:prepared-render-installer`、`regress:worker-task`、`typecheck:core`、1396-module build 与 diff-check 全 PASS；browser / CDP `0`。阶段冻结，待同一只读评审，不提前进入 `R3b-p2`。
- 首轮评审 `BLOCK / P0 0 / P1 1`，产品无残留，唯一 blocker 是 reload fault 夹具把 revision 写成非正式 `{revision}` 且只检查方法名。夹具现使用正式 `MapRevisionTracker` / `EditHistory` before-image，restore 精确接收同一快照，严格 helper 发行完整 rollback binding，并在 `loadMapAsync` 内核对 map、map/topology revision、cursor、history 和 binding 后才抛原 fault；static/full worker-task、语法与 diff 再次 PASS，待 blocker-only。
- blocker-only 复审独立通过 static/full worker-task、语法与 diff，确认正式 revision/history before-image、严格 rollback binding、load fault 点与外层状态二次核对均闭合；最终 `ACCEPT / P0 0 / P1 0`，browser / CDP `0`。`R3b-f2` 冻结，下一阶段为 retained picking/overlay/label owner 的 `R3b-p2`。

## 2026-08-21：第 350-R3b-p2 阶段冻结待评审——retained picking / overlay / label owner

- 新增 retained resource binding helper，为 object picking、label layout、overlay DOM / city icon GPU instances 建立完整 owner 与真实引用 wrapper；full map load、prepared install 和 context restore 均从正式 complete binding 接纳，draw / pick 在使用前拒绝引用混装及跨 map/topology owner。
- prepared installer 将 retained data 与 owner 放在同一 before-image 栈；通用字段与嵌套字段改为 setter 执行前登记回滚项，确定性反例让 owner setter 先写脏再抛错，仍精确恢复 map、picking、owner 与 wrapper。
- 源码覆盖审计发现普通 edit/history/regeneration refresh 会重建 retained 数据却保留旧 revision owner；已让同步/异步 scheduler、河流局部 picking、城镇迁移及 rollback 路径按当前 revision 同批共享一个新 binding。合法未重建资源仍保留实际旧 owner，不用最新值冒充。
- installer `16 cases`、panel refresh、10004-cell render preparation、完整 11-kind worker-task、`typecheck:core`、1397-module production build、脚本语法与 diff-check 均 PASS。browser / CDP `0`；阶段冻结待同一只读智能体评审，未进入 R4a。
- 首轮评审 `BLOCK / P0 0 / P1 1`：正式 history 每次 advance 都保守推进 topology，而 retained-only refresh 使用新 topology、surface 仍保留旧 topology；标签编辑的下一 draw 与 city relocation 已提交后的即时 refresh 会被自身 mismatch 门拒绝。原 16-case fake renderer 只记录 binding 参数，未建立真实 surface owner，漏掉集成失败。现插入 `R3b-p2a`，不改变 tracker 策略，只分别处理 retained-only 与真实 surface refresh 的 binding。
- `R3b-p2a` 修正后，retained-only 组合 current source 与实际 surface topology；cell-colors / terrain refresh 把 current binding 传给 full/incremental surface，并在 draw 前同时重绑 surface 与全部 retained wrapper。river rollback 的 preserving-routes 中间态先保持 surface topology，再由后续真实 surface refresh 换代。
- 新 17-case 集成反例使用正式 EditHistory / MapRevisionTracker 和真实 surface owner：连续 history advance 后，sync/async retained-only、增量 surface draw、真实 city relocation 的 owner 数值分别为 `1/0`、`2/0`、`3/3`、`4/3`，全部 mismatch 为空。installer、panel、完整 worker-task、typecheck、1397-module build 与 diff PASS；browser/CDP `0`，现冻结待同一评审 blocker-only。
- 首次 blocker-only `BLOCK / P0 0 / P1 1`：async surface run 在 picking 换 current topology 后先 yield，surface 后换代；guard 在该 checkpoint 失败会永久留下混装。产品现为 surface async batch 增加 retained publish suspension，中间 draw/pick 隔离，成功终点 ready+pending draw，任一 fault 明确 invalid。
- 17-case 已扩展：async surface 每个受控 yield 均观察 suspended，成功 owner=`4/4`；city relocation=`5/4`；下一轮在第二个 assertCurrent 故障后 state=invalid、draw 计数不变、pick=null。installer、panel、完整 worker-task、typecheck、1397-module build 与 diff 再次 PASS，browser/CDP `0`，待第二次 blocker-only。
- 第二次 blocker-only 最终 `ACCEPT / P0 0 / P1 0`：同一评审确认逐 yield suspended、无 yield commit、fault invalid、pending draw、嵌套计数和 context 状态均闭合，独立 installer 17、panel、typecheck、diff 全过。R3b 全部冻结，进入 R4a presentation / picking / overlay 完成度复查。

## 2026-08-21：第 350-R4a-f1 阶段首败——live presentation 与 persisted presentation 混写

- R4a 盘点确认固定入口 11～13 的旧 Node 前置只覆盖 city/hover、overlay policy 和 viewport transform，未把 marker/route/river picking、prepared retained owner、deferred presentation replay 与 GPU display 0-delta 连成同一可审计门。新增 catalog 精确矩阵、逐项删除反例和串行 `regress:task-350-r4a`，产品文件改动为 `0`。
- 新 presentation contract 以 Babel AST 精确限定九个显示函数，并用真实 1k map 核对独立 document projection、source deep equal 与 replica checksum。首败定位到普通 theme intent 仍直接改写 canonical `visualTheme/options`，但 revision/history 不推进；同时 map-file normalization 让 source 旧主题覆盖 live export option。
- 该问题分类为产品 blocker，夹具预期冻结不再移动。插入 `R4a-p1`，只分离 live intent 与持久化投影；本阶段未运行 browser/CDP，额度仍为 `0`。

## 2026-08-21：第 350-R4a-p1 阶段——运行时主题与持久化投影解耦

- 普通 theme intent 已停止调用 canonical map 同步 helper，只更新 runtime / UI / renderer；正式用户主题命令仍由 history command 写入地图，因此没有把可持久化编辑误降为纯显示操作。
- `createMapDocument` 改为在 schema normalization 的独立副本上投影当前 `documentOptions.visualTheme`，同时复制 overrides、userThemes 与 colors。冻结 1k contract 证明 document options、document map options、map visualTheme preset 三处一致，源地图 deep equal 且 replica checksum 不变。
- presentation contract、GPU display、visual themes、API data compatibility、完整 11-kind worker-task、typecheck、1397-module build 与 diff-check 全部 PASS；browser/CDP `0`，阶段冻结待同一只读评审。
- 完整 map-file Worker 入口另在旧两字段 render binding 处首败，属于 R3b 后未迁移的既有夹具，不是本轮 export projection 产品失败；未在产品阶段放宽 binding 或改夹具，拟于产品接受后单列 `R4a-f2`。
- 同一评审首轮给出 `BLOCK / P0 0 / P1 2`：live/canonical 分离后用户主题命令的 before-image 仍混写，undo 会把 live 主题写回 canonical；active theme 若只在本地 registry，存档又只有 preset ID、没有颜色定义。两项均为产品缺口。
- 动态插入纯夹具 `R4a-f1a`：新增 visual-theme boundary 门，固定四类命令独立 presentation state、`default/ancient` history 往返及 registry-only user theme export/import；catalog 入口 11 加入不可删前置。首败精确落在 create 命令缺 `presentationPreset`，产品改动 `0`、browser/CDP `0`，现返回 p1 修正。
- `R4a-p1` blocker-only 产品修正完成：capture 将 canonical/live preset 分开，command 与 scheduler 按 apply/revert 当前方向恢复 presentation；四类用户主题操作显式计算 live after。正式 JSON、压缩与 archive Worker 导出把 active registry-only theme 文档作为独立 presentation 参数传给 map-file projection，校验 ID 后只在副本合并。
- visual-theme boundary 从首败转 PASS，另有 presentation、visual themes、API data、GPU display、完整 11-kind worker-task、typecheck、1397-module build、语法与 diff-check 全过；browser/CDP `0`，现交同一评审做 blocker-only。
- 第一次 blocker-only 为 `BLOCK / P0 0 / P1 1`：portable registry-only export 已接受，但 canonical `userThemes` 与 registry 全集仍共用 before-image，registry 超集时 undo 会污染地图。
- 插入纯夹具 `R4a-f1b`：registry 外置一个主题、canonical 空集，覆盖 create、import select=false、update registry-only、delete current/non-current 五类 apply→undo→redo；catalog 入口 11 增加不可删前置。首败落在 create 未分别 upsert 两份集合，产品 `0`、browser/CDP `0`，现返回 p1 最终修正。
- `R4a-p1` 最终修正将 canonical themes 与 registry themes 分别 capture/apply/revert，create/import/update 分别 upsert，delete 分别 remove。五类 registry 超集往返及相邻主题/显示/API/typecheck/1397-module build/diff 全过，browser/CDP `0`，现交同一评审做第二次 blocker-only。
- 第二次 blocker-only 最终 `ACCEPT / P0 0 / P1 0`：同一评审确认两份集合、旧 state fallback、四类操作、五类 history 往返与 portable export 全部闭合；R4a-p1 冻结。
- 进入纯夹具 `R4a-f2`：只迁移 map-file Worker 的旧两字段 render request / 三字段 expected 到完整 resource binding，产品 validator 保持冻结，browser/CDP `0`。
- `R4a-f2` 已以正式 helper 固定完整 request/expected binding；map-file Worker 的旧档、压缩、浏览器 envelope、损坏输入和 100k typed-array/transferable 全套 PASS，render-preparation 10k、typecheck、语法与 diff 亦过。产品 `0`、browser/CDP `0`，阶段冻结待同一评审。
- `R4a-f2` 独立评审最终 `ACCEPT / P0 0 / P1 0`：正式 Worker、完整五项 binding 深比较、render 进度、point typed DTO 与旧档/压缩/损坏/100k 门均成立，且产品 validator 未放宽。阶段冻结，返回 R4a 剩余 presentation cache owner 与 async publish 完成度盘点。
- R4a cache 盘点确认 line/point/route/river/trade-flow/selection 六类正式绘制 buffer 没有 binding owner/wrapper，draw 只审 surface 与 retained picking/overlay，context 换代及引用混装无法被 cache 自身证明。插入纯夹具 `R4a-f3`：冻结六族集合、map/topology/generation 兼容、引用一致、prepared before-image 与 async current-before-publish；新门按预期首败于 helper module 缺失。catalog `55 / 57 / 63`、语法/diff PASS，产品 `0`、browser/CDP `0`，现进入独立产品 `R4a-p2`。
- R4a-p2 新 cache 门、相邻专项与 1398-module build 通过后，聚合首次推进到 markers-core，旧夹具仍以 legacy canonicalRevision 对象调用严格 picking DTO。插入纯夹具 `R4a-f4`，只改为正式 complete binding 并由 build/rebuild 共用，产品 validator 与 markers 产品均不动；browser/CDP `0`。
- R4a-f4 独立评审 `ACCEPT / P0 0 / P1 0`：五项 binding、同源 DTO 往返和 8 个 marker 的 point/pick/DTO/export 全部闭合，产品改动 `0`、browser/CDP `0`；返回 p2 聚合。
- 补做 `R4a-f3` 独立评审首次得到 `BLOCK / P0 0 / P1 3`：正则/字符串 token 可假冒真实调用、只覆盖单个 route 引用、catalog 删除自检遗漏新前置，均归因为夹具问题。主线程未改 p2 产品，只将门收紧为 Babel AST executable call 与调用顺序、六族 17/17 引用逐项替换、入口 11～13 三个删除反例，并增加 3 个 comment/string mutation 自检。cache 门、catalog `55 / 57 / 63`、installer 17-case、diff-check PASS，browser/CDP `0`，现交同一评审 blocker-only。
- `R4a-f3` 第一次 blocker-only 再给 `BLOCK / P0 0 / P1 2`：`if(false)` 的 draw call 和结果被逗号表达式丢弃的 async current call 仍可假绿，夹具自有 17 字段也没锁产品 wrapper exact key set。继续只修夹具：draw 改为 try block 直接语句契约；route/river current 取反必须直接进入拒绝 OR、拒绝分支 return false、adopt 是其后直接发布；增加两个对应 mutation，并逐族比较 wrapper exact keys。cache 门为 `17 references / 5 AST mutations` PASS，产品/browser 均为 `0`。
- `R4a-f3` 第二次 blocker-only 只余 `BLOCK / P0 0 / P1 1`：同步 uploader 的递归 AST call 搜索会把未调用箭头函数内 adopt 误认为正式发布。继续不改产品，将八个上传方法逐一限定为正确 body/try block 的直接 adopt，并新增 selection 未调用箭头函数 mutation。目标输出为 `17 references / 6 AST mutations`，catalog/installer/diff 复跑 PASS，browser/CDP `0`。
- `R4a-f3` 最终复核 `ACCEPT / P0 0 / P1 0`：八条 uploader 直接发布、async current rejection、六族 exact wrapper keys、17 个引用变异和 6 个 AST mutation 全部闭合。纯夹具阶段冻结，现进入 `R4a-p2` 产品独立评审，browser/CDP 仍为 `0`。
- `R4a-p2` 产品独立评审给出 `BLOCK / P0 0 / P1 4`：partial prepared 错发 topology、surface-only 换代漏 retained owner、trade-flow pick 不验 cache、context restore 迟到覆盖/清理。动态插入 `R4a-p2a`，四项均按产品 blocker 处理。
- `R4a-p2a` 已完成：新增 render request source helper 分开 source/topology；prepared transaction 对同 generation surface-only 变化重绑 picking/label/overlay 并可精确 rollback；trade-flow pick 在消费前检查 owner/ref；context restore 全资源 staged，在每个异步边界校验 exact latest binding/map，最终同步发布，obsolete 只清本事务资源。新增 installer 19-case 与 Worker 受控 yield B 接管反例；相关 cache/显示/10k/typecheck/1398-module build/diff 全 PASS，browser/CDP `0`，待 blocker-only。
- `R4a-p2a` blocker-only 只剩 `BLOCK / P0 0 / P1 1`：obsolete A 没破坏 B，但事件 owner 未重启，状态永久 restoring。新增循环 owner `restoreWebGlContextUntilCurrent`，只为结构化 obsolete 重开 staged restore；attempt 退出先置 lost。受控门现要求两次 attempt、一次成功 draw、最终 ready/lost=false，且 A 仍只清自身 staged；worker-task PASS，browser/CDP `0`。
- `R4a-p2a` 第二次 blocker-only 指出重试未继承 B 且测试第二 attempt 为 stub，继续 `BLOCK / P0 0 / P1 1`。现冻结 B→C：owner 在 obsolete 后读取最新 B，下一次真实 restore 以 B source/topology 发行新 context generation C；fixture 第二 attempt 已在真实 1k map 与完整 FakeGL 上跑完全部 staged 资源，断言 `23 yields / 2 attempts / 1 draw`、六族 cache/surface/retained owner 对齐和最终 ready，browser/CDP `0`。
- `R4a-p2a` 最终 blocker-only `ACCEPT / P0 0 / P1 0`：评审确认 owner 捕获 latest B，第二次实际走完整产品 restore 并派生 C；实测 `source 3 / topology 1 / generation 3→4`，surface、六族 cache、picking、overlay/label owners 对齐 C，仅一次 draw 后 ready，普通 fault 不重试。独立 worker-task、installer 19-case、typecheck、语法、diff-check PASS，browser/CDP `0`；现运行 R4a 聚合与 build。
- R4a 最终 Node 聚合全部 PASS：catalog `16 / 17 / 20`、presentation/theme/registry、GPU/cache、完整 Worker、10k render、installer、三类 picking、line/viewport/hover/panel 与 typecheck 全绿；生产 build `1398 modules` PASS。browser/CDP `0`，交同一只读评审做阶段 inventory 后再进入 R4b。
- R4a inventory `BLOCK / P0 0 / P1 2`，没有新增产品缺陷：最终聚合未直接跑已接受 f2 的 map-file Worker，AGENTS/handoff 又保留旧等待/待评审状态且 p2 误混 f2 记录。插入纯夹具/文档 `R4a-f5`：aggregate 加入 map-file Worker，catalog audit 加阶段前置与删除反例；f1～f4、p1/p2/p2a 全部显式回写最终接受并删除误置段。产品 `0`、browser/CDP `0`，最窄门与 blocker-only 接受前不进入 R4b。
- `R4a-f5` 实施完成：catalog audit 直接核对实际 package aggregate，删除 map-file 前置反例必败；更新后的完整 R4a 聚合真实跑过 map-file plain/压缩/browser envelope/损坏/100k 后继续通过所有 presentation/cache/picking/overlay 与 typecheck。语法、diff-check PASS，产品 `0`、browser/CDP `0`，待 blocker-only。
- `R4a-f5` 首次 blocker-only 仍 `BLOCK / P0 0 / P1 1`：文档状态已闭合，仅 aggregate 正则可被 echo 字符串/注释 token 假绿。现改为只接受 `&&` 分隔的完整直接 `pnpm run <script>` 段，并加入 echo/comment 两个反例；产品和已通过的完整聚合冻结，browser/CDP `0`。
- `R4a-f5` 最终 blocker-only `ACCEPT / P0 0 / P1 0`：删除真实 map-file、echo token、shell comment 三类反例均被正式 validator 拒绝，真实 aggregate 保持直接执行；catalog/语法/diff 独立 PASS，产品 `0`、browser/CDP `0`。R4a 全阶段冻结，进入 R4b。
- R4b Node 基线 PASS：heightmap export、PNG options、9 项 export suite、context restore static controlled gate、typecheck/diff 全绿。盘点发现的是浏览器夹具完成度而非产品首败：两个 export 脚本仍为 3k、失败证据/LongTask/state exact 不完整；context restore 没有独立入口与稳定 debug hook。插入纯夹具 `R4b-f1` 冻结三入口的 10k、artifact、`>200ms`、state/owner/picking 契约；产品 `0`、browser/CDP `0`。
- `R4b-f1` 最终 `ACCEPT / P0 0 / P1 0`：先后修正 AST 不可达/未调用假绿、server timeout 证据丢失、context before owner 未冻结和错误状态字段。最终 validator 只接受直接 `page.evaluate`/`unwrap(await target)`，`27` 个变异全拒绝；artifact helper 的成功、功能失败、teardown 失败、永不退出 timeout 四态均 PASS。产品 `0`、browser/CDP `0`。
- 按冻结方案插入 `R4b-p1`：仅新增实验性 `debug.simulateContextLoss` 桥接既有 renderer restore，参数有界、调用前完整 owner 同源、成功 source/topology 不变且 generation `+1`、唯一 draw、最终 ready；先用确定性 Node 事件/扩展夹具验收，不运行浏览器。
- `R4b-p1` 实施完成待独立评审：API contract/schema/capability 已登记为实验性异步入口，renderer 经真实 `WEBGL_lose_context` lost/restored 事件复用现有恢复链，前后校验 surface、六族 cache 与 retained owner；确定性 Node 门覆盖成功、并发、五类参数、无扩展与混装 owner。API `329` 项、capability `1229 / 1155 / 74 / 0 / 0`、9 项 export、Worker context、cache `6 / 17 / 24 / 6`、installer `19 cases`、主题、城市 picking、typecheck、`1398 modules` build、R4b Node 聚合及 diff 全部 PASS；browser/CDP `0`。
- `R4b-p1` 首轮评审 `BLOCK / P0 0 / P1 3`：同步扩展 fault 泄漏事件 waiter；专项替代正式 restored handler；API schema 漏列 cache/retained owner mismatch。插入 `R4b-p1a` 后，waiter 可取消且 trigger fault 清 listener/timeout，正式 lifecycle handler 由构造器与专项共同安装，schema 补齐两类 code；新增 lose/restore fault 的 debug promise/timer/unhandled rejection 与 retained mismatch 负例。context 专项 `invalidCases 10`、API discovery/stability、Worker context、typecheck、语法、diff PASS，待 blocker-only；browser/CDP `0`。
- `R4b-p1a` blocker-only 最终 `ACCEPT / P0 0 / P1 0`：独立复跑 context 专项、三文件语法、typecheck 与 diff 全绿，确认 trigger fault 清理、正式生命周期 handler 和两类公开 owner code 均闭合。R4b 全部接受，进入 R5a 持久化兼容盘点；browser/CDP `0`。
- R5a 七组 Node 基线只有 `map-save-naming` 首败：旧夹具要求同步 map-load refresh，当前产品已在 `loadMapIntoRuntime` await async refresh 后更新 cloud preview 与 filename-template UI。插入纯夹具 `R5a-f1`，按精确函数切片固定该正式顺序；其余 whole-map 四 profile、map-file Worker 100k、compatibility/migration、cloud/storage 均 PASS。产品 `0`，专项/语法/diff PASS，browser/CDP `0`，待独立评审。
- `R5a-f1` 首评 `BLOCK / P0 0 / P1 1`，只涉及夹具：文本正则能被不可达代码骗过。现以 Babel AST 要求 async load 外层 try 中三个相邻直接语句，并加入 if-false、字符串和未调用函数三类 mutation；产品文件仍为 `0`，专项/语法/diff PASS，browser/CDP `0`，待 blocker-only。
- `R5a-f1` blocker-only 第二次仍 `BLOCK / P0 0 / P1 1`：可达性已闭合，但调用 shape 未限制 optional 类型、精确参数与 option 键。产品问题仍为 `0`；第三轮最窄方案及四类 mutation 已记录。因连续两次夹具失败触发仓库停止条件，现冻结请用户裁定；browser/CDP `0`。
- 用户授权第三轮后，`R5a-f1` 已将 refresh/preview/sync 的 AST 类型、arity、参数顺序、optional chain 与 options exact keys 全部锁定；新增五个错误调用形状 mutation，与原三类不可达 mutation共 `8` 类。产品 `0`，map-save naming、语法、diff PASS，browser/CDP `0`，待最终 blocker-only。
- `R5a-f1` 最终 blocker-only `ACCEPT / P0 0 / P1 0`：独立确认八类 mutation 全被正式 AST validator 拒绝；map-save naming、语法、diff PASS。产品 `0`、browser/CDP `0`，返回 R5a 本体盘点。
- R5a 完成度盘点发现 storage 后端仲裁与 holey/high-id identity export 尚无直接 Node 完成门，插入纯夹具 `R5a-f2`。新增确定性 LocalStorage/IndexedDB fake，真实调用 write/read/direct-binary 覆盖成功、quota/no-local fallback、错误保持、savedAt 仲裁与读失败；真实 1k map 扩展 city/burg `5000`、route `7000`，固定导出副本显式 null、roundtrip 保号且运行时源数组/对象不变。新增 `regress:persistence-boundary`、R5a 聚合、catalog 前置与删除反例；产品 `0`，完整 R5a Node 聚合、专项/catalog/语法/diff PASS，browser/CDP `0`，待独立评审。
- `R5a-f2` 首评 `BLOCK / P0 0 / P1 1`，仍为夹具问题：`Array.every` 跳过 hole，且只核对首洞，无法证明投影全稠密；产品问题 `0`。插入 `R5a-f2a`，改用显式索引检查每槽 own-property 与源 hole→null，并为三数组各加入“只补首洞”的破坏性反例；browser/CDP `0`，待最窄门与 blocker-only。
- `R5a-f2a` blocker-only `ACCEPT / P0 0 / P1 0`：独立确认逐槽 own-property/null、三类只补首洞反例与源 before-image 全部闭合；persistence、catalog、语法、diff PASS，browser/CDP `0`。返回 R5a 主阶段最终 inventory 评审。
- R5a inventory 首评 `BLOCK / P0 0 / P1 1`，唯一问题是 handoff 表头仍写 f2 待评审、产品文件只登记 f1；产品/夹具问题 `0`，四 profile、storage、archive/headless 与 source immutable 均无新 blocker。插入纯文档 `R5a-f3` 统一 f1/f2a 接受状态及 f1/f2/f2a 产品改动 `0`，只做 blocker-only 文档复审，browser/CDP `0`。
- `R5a-f3` blocker-only 与 R5a 主阶段最终均 `ACCEPT / P0 0 / P1 0`：权威状态一致，四 profile、旧/当前/high-id、storage/direct-binary、archive/headless 与 source immutable 无残余 blocker。固定顺序进入 R5b Loading/feedback/error surface 完成度盘点，browser/CDP `0`。
- R5b 廉价基线的 API operation、delayed feedback、UI copy、storage copy 全绿；global-shell 首败为旧“正在装配地图引擎”源码断言，插入纯夹具 `R5b-f1` 迁移到正式“正在展开地图画卷”，产品 `0`。同时正式 Node 复现普通启动页原样泄漏 `Worker session buffer checksum mismatch`，登记独立产品 `R5b-p1`，必须等 f1 接受后实施；browser/CDP `0`。
- `R5b-f1` 首评 `BLOCK / P0 0 / P1 1`，只因根 AGENTS 当前状态仍停在 R4b；global-shell 迁移忠实且产品未改。插入纯文档 `R5b-f1a` 同步为 R5a 已接受、f1 blocker-only、p1 已冻结，browser/CDP `0`。
- `R5b-f1a / f1` 最终 `ACCEPT / P0 0 / P1 0`。开始产品 `R5b-p1`：普通启动页/app-status 改用同一通用错误，health/console 保留原始异常，显式 debug formatter 可见详情；global-shell 新增 Worker/session/buffer/checksum 负例与 main 防 raw 拼接断言。产品仅 startup-loading/main，browser/CDP `0`。
- `R5b-p1` 首评 `BLOCK / P0 0 / P1 1`，只因根 AGENTS 当前状态仍写 f1 待复审；产品/夹具问题 `0`，全部最窄门通过。插入纯文档 `R5b-p1a` 同步 f1/f1a 已接受、p1 blocker-only，browser/CDP `0`。
- `R5b-p1a / p1` 最终 `ACCEPT / P0 0 / P1 0`。继续完成度盘点并插入纯夹具 `R5b-f2`：operation 扩展到 snapshot/rollback fault 与 late report 共 12 边界；delayed feedback 以确定性时钟锁 23/24ms、错 ID、unknown/destroy；UI copy 对 181 样本执行 21 禁词。固定入口 18～20、R5b aggregate/catalog 防删门同步，产品 `0`，完整 Node 聚合 PASS，browser/CDP `0`，待独立评审。
- `R5b-f2` 首评 `BLOCK / P0 0 / P1 1`，仅 catalog 只锁新增前置，既有 api-operation/delayed/map-save-naming 可删；产品/三专项问题 `0`。插入 `R5b-f2a`，把固定入口 18～20 完整前置集合逐项强制并加删除反例，browser/CDP `0`。
- `R5b-f2a / f2` 最终 `ACCEPT / P0 0 / P1 0`：入口 18～20 的 11 项前置均有正式 requirements 与逐项删除反例；catalog/语法/diff PASS，browser/CDP `0`。返回 R5b 主阶段最终 inventory。
- R5b inventory 首评 `BLOCK / P0 0 / P1 1`：高度模板缺存储时普通 notice 泄漏 `LocalStorage`，curated UI-copy 未执行正式面板路径。插入 `R5b-p2`：缺存储/访问拒绝统一为后端中立提示，显式 debug 与结构化 diagnostic 保留原异常；新增正式 `createHeightPanel` 动态 Node 门，R5b aggregate、入口 19/20 与 catalog 删除 mutation 同步为 13 项前置，browser/CDP `0`，待独立评审。
- `R5b-p2` 最终 `ACCEPT / P0 0 / P1 0`：正式高度面板 missing/getter-denied 动态路径、普通/debug/diagnostic 分层、183 samples UI-copy、catalog/aggregate、typecheck/语法/diff 全部闭合，browser/CDP `0`。返回 R5b 主阶段最终 inventory。
- R5b 最终 inventory 再次 `BLOCK / P0 0 / P1 1`：高度模板首次 quota 写失败会提前污染 `userTerrainPrograms`，同名重试产生 `-2` 并持久化 ghost，失败 receipt 也没有 structured cause。插入 `R5b-p3`，只修 `nextPrograms → persist → publish` 与写入异常包装；正式面板动态门补失败后同名重试原 id/单模板/diagnostic，delete/restore/import 不扩改，browser/CDP `0`。
- `R5b-p3` 最终 `ACCEPT / P0 0 / P1 0`：失败保存不发布内存，quota receipt 保留 backend/cause，同名重试保持原 id 且最终仅一个模板；missing/denied、183 samples、typecheck、语法、diff 全部独立通过。返回 R5b 最终 inventory，browser/CDP `0`。
- R5b 主阶段最终 `ACCEPT / P0 0 / P1 0`：12 类操作终态、delayed 精确边界、普通/debug/diagnostic、height 四类 persistence fault、入口 18～20 的 13 项前置均闭合；完整 R5b aggregate 与 diff 独立 PASS，browser/CDP `0`。固定顺序进入 R6a，产品树冻结。
- R6a 目标冻结 20 个 catalog 入口（19 个唯一脚本，worker-session 分 10k/100k 两模式）的 source/setup/window/assertions/cleanup/full+compact artifact/200ms policy；仅运行语法、artifact self-test、静态契约与 build，不启动 browser/CDP。
- R6a 基线首败为 artifact self-test 锁定旧 hard-cell renderer 顺序；正式实现已是高度/关闭平滑/零 range 时 patch→rebind→draw，失败 full fallback。插入纯夹具 `R6a-f1` 迁移方法级静态断言并加删除 patch/关闭平滑前置 mutation，产品 `0`，browser/CDP `0`。
- `R6a-f1` 首评 `BLOCK / P0 0 / P1 1`：正则未锁 `if(patch)` 与成功早返，两类破坏均假绿。插入 `R6a-f1a` 改用 Babel AST 固定 direct 控制流并补 `if(false)`/删 return mutation；artifact self-test、语法、diff PASS，产品 `0`、browser/CDP `0`，待 blocker-only。
- `R6a-f1a` blocker-only 仍 `BLOCK / P0 0 / P1 1`：原绕过已闭合，但关键调用实参与 `!map` guard 未锁。插入最后 `R6a-f1b`，精确 patch/rebind/draw/full fallback 实参和完整 guard，补错误 patch/full 参数 mutation；最窄门 PASS，产品 `0`、browser/CDP `0`，待复审。
- `R6a-f1b / f1a / f1` 最终 `ACCEPT / P0 0 / P1 0`：hard-cell AST 完整 guard/控制流/实参与六项 mutation 闭合，专项/语法/diff 独立 PASS，产品 `0`、browser/CDP `0`。
- R6a inventory 确认入口 1/11/12/13、16～20 共 9 个脚本没有 full+compact failure-safe artifact。拆为 `R6a-f2` transaction/presentation 四脚本、`R6a-f3` persistence/feedback 五脚本，最后统一冻结 catalog；产品 `0`、browser/CDP `0`。
- `R6a-f2` 已把 map transaction、city picking、overlay pan、viewport line preview 的 startup/功能失败/teardown/persist/重抛纳入共享 artifact owner；full 保持原 stdout report，compact 保留旧硬断言输入，UI-only 空 geo/climate 安全投影为 null。新增 AST 契约固定 78 个旧断言与 32 项破坏性反例；四脚本语法、artifact 两组 self-test、静态契约、diff PASS，产品 `0`、browser/CDP `0`，待同一评审。
- `R6a-f2` 首评 `BLOCK / P0 0 / P1 3`：两处 Playwright require 逃逸 owner，断言计数可被 noop 等量替换，compact 键存在不证明值来源且 map 缺逐操作证据。插入 `R6a-f2a`，将四脚本 startup 全纳 owner；以规范 AST digest 固定 78 个 assertion、显式 throw guard、full/compact 与直接依赖，补 map/overlay/viewport 逐操作/逐样本输入。130 项 mutation 全拒绝，专项/self-test/语法/diff PASS，产品 `0`、browser/CDP `0`，待 blocker-only。
- `R6a-f2a` blocker-only 仍有 `P1 1`：compact digest 未覆盖 map report producer，historyDelta 可伪写 999。最终改为四个夹具整棵规范 AST digest，并加入该精确反例；78 硬断言 / 131 mutation 契约与 diff PASS，产品 `0`、browser/CDP `0`，待最后 blocker-only。
- `R6a-f2a / f2` 最终 `ACCEPT / P0 0 / P1 0`：完整 producer AST 与 999 反例获独立确认；四脚本/78断言/131mutation、语法、diff PASS。现进入 `R6a-f3` 五个 persistence/feedback 入口，产品 `0`、browser/CDP `0`。
- `R6a-f3` 已把入口 16～20 的 storage compatibility/fallback、save feedback、loading single source、delayed feedback 接入统一 failure-safe artifact owner；动态依赖、服务、浏览器启动均在 owner 内，功能/teardown 首败无条件落 full+compact 后保持非零退出。共享 LongTask helper 按目标窗口收集原始条目，统一硬拒绝 `duration>200ms`；compatibility 三分窗、fallback/feedback 双页面，其余单窗。R6a AST contract 扩至 9 fixtures / 208 assertions / 340 mutations，并冻结 helper、调用次数、200ms 阈值和 overBudget/full 数据流；artifact self-test、七脚本语法、契约、diff PASS，产品 `0`、browser/CDP `0`，待同一评审。
- `R6a-f3` 独立评审最终 `ACCEPT / P0 0 / P1 0`：五入口 owner、失败持久化、LongTask 窗口与 full/compact 数据均闭合；artifact self-test、统一契约、七脚本语法与 diff 独立 PASS。按既定尾项进入 `R6a-f4` catalog 总冻结，产品 `0`、browser/CDP `0`。
- `R6a-f4` 已将 catalog schema 升至 3、20 个固定入口全部标为 `frozen`，并要求 20/20 artifact/finally 与非 presentation-zero LongTask 观测。新增 19 唯一脚本的规范 Program AST digest、19 次 node --check 与 19 个可执行 mutation；新增 R6a 聚合串联 catalog、两套 artifact self-test、R4b/R6a contract、fixture freeze、typecheck、build，并为八项前置逐项防删。完整聚合 PASS：R4b 27 mutations、R6a 9/208/340、freeze 19/19、`1398 modules` build；待 diff 和同一评审，产品 `0`、browser/CDP `0`。
- `R6a-f4` 首评 `BLOCK / P0 0 / P1 1`：当前聚合实际只有八个安全门且 browser0，但 validator 的子集判定允许尾部追加 browser entry。插入纯路由 `R6a-f4a`，R6a 分段必须与八项白名单保序精确相等，并补 map transaction / worker session / save feedback 三个追加反例；产品、fixture、browser `0`，只复跑最窄 catalog/aggregate/diff 与 blocker-only。
- `R6a-f4a / f4` 最终 `ACCEPT / P0 0 / P1 0`：八项聚合精确白名单与三项 browser 追加反例独立成立；catalog 20/19、freeze 19/19、aggregate/build/diff 全过。R6a 完成，产品0、browser0；按既有全量 CDP 授权进入 R6b 分组浏览器验收。
- R6b 固定入口 1 `regress:map-transaction-browser` 在首个 features 被 `renderer source mapRevision 与请求不一致` 拒绝，full/summary 已写入 `Z:\tmp\codex\2026-08-21\task-350-r6b`，后续 19 门停止。根因是 R3b 已把 render request 签给 commit 后 revision，而五组领域 validator 仍与提交前 compute binding 比较。动态插入产品阶段 `R6b-p1`：通用 pre-commit exact 核对完整 render resource binding，领域层分别接收 compute/render binding；五组协议新增 N/N+1 正例和缺失/旧代/伪造 id-generation 负例。五组直接协议、11-kind worker-task、typecheck、语法、diff PASS；同一只读评审最终 `ACCEPT / P0 0 / P1 0 / browser 0`，foundation 聚合中的 adoption-owner 旧静态顺序探针不属于本阶段且直接 foundation 协议已通过。现生产构建后只复验入口1。
- p1 后生产构建 `1399 modules` 通过；入口 1 目标复验越过原 revision 协议，下一首败为 features 提交后的 `route / river / tradeFlow:owner-topology-revision`，artifact 原位更新且入口 2～20 未启动。根因是 full surface 正确推进 topology/render generation 后，installer 仍只在 generation 不变时重绑同一 live map 中未准备但引用有效的 cache。插入 `R6b-p2`：以对象同一性和提交前 owner/wrapper/ref 完整性为前置，事务内重绑缺席 family，并冻结不同对象不得复用、混装提交前拒绝及 rollback exact；最窄 Node 门和同一评审前 browser0。
- R6b-p2 已实施待评审：六族未准备 cache 在旧 surface 上先逐族核 owner/wrapper/ref，再通过既有 transaction before-image 重绑目标 owner；不同 map 对象不复用。首轮 installer 专项抓到 cache registry 被误写为 picking/overlay 总开关，拆分布尔条件后新增 full-surface 正例、混装拒绝、普通 rollback 和提交 setter fault rollback均通过。installer `20 cases`、render preparation 10k、cache binding `17/24/6`、panel refresh、11-kind worker-task、typecheck、语法、diff PASS；产品约1文件30行、夹具约1文件125行，browser修正后0，现交同一评审。
- R6b-p2 首评 `BLOCK / P0 0 / P1 1 / browser 0`：same identity / different map object / same generation 会让未显式重绑的旧 wrapper 仍匹配新 surface。blocker-only p2a 在 full-surface 对象替换提交前强制 generation 更替，并补正式负例；既有 nested ownership 第二次对象替换也从复用 generation1 迁到 generation2。installer20、typecheck、语法、diff PASS，现只交同一评审 blocker-only，未运行浏览器。
- `R6b-p2a / p2` 最终 `ACCEPT / P0 0 / P1 0 / browser 0`：评审确认跨对象旧 generation 在任何正式写入前拒绝，同对象 full/in-place 与跨对象新 generation 保持；正式反例 prepare 后在 commit 命中且 map/六族 registry 不变。installer20、typecheck、syntax、diff 独立 PASS；现 build 后只复验入口1。
- p2a 后 build `1399 modules` 通过；入口1 features 正向链已通过，首败推进到 `history.undo.generate.regenerate.features` 的 `tradeFlow:owner-topology-revision`。history/edit scheduler 不经过 Worker installer，旧 `refreshCellSurface` 只发布 surface 与 picking/overlay，未标 dirty 的 tradeFlow 仍持有旧 topology owner。插入 `R6b-p3`：full-surface refresh 在首次 draw 前复用既有全 retained/cache helper，原子更新六族 owner 且不替换物理引用；browser 修正后0。
- `R6b-p3` 已实施待评审：cache binding 新增真实原型 topology `2 → 3` 场景与退回部分重绑的 AST mutation，输出 `17 reference / 24 negative / 7 AST mutation / historySurfaceCacheRebind=true`；installer20、history async、panel refresh、完整 11-kind worker-task、typecheck、语法、diff PASS。现交同一只读评审，接受前不复验入口1。
- R6b-p3 首评 `BLOCK / P0 0 / P1 2 / browser0`：产品 helper 可把漂移引用/跨 map owner 洗白；夹具递归 AST 可接受不可达调用。拆为纯夹具 p3a 与纯产品 p3b，先冻结期望。
- `R6b-p3a` 产品0：动态新增 routeBufferCamera reference drift 与 tradeFlow other-map owner 两个精确拒绝；AST 改为主 try block direct guarded call，新增 `if(false)` 与 unused closure mutation。fixture-only `17 reference / 26 negative / 9 AST mutation`、syntax、diff PASS；正式 cache 门按预期红于 route 漂移未拒绝，browser0，现交同一评审接受红门。
- p3a 首评 `BLOCK / P0 0 / P1 1`：调用前 direct return 可令原索引门假绿。blocker-only p3a1 增加 rebind 前 direct return/throw 拒绝和 early-return mutation；fixture-only `17/27/10`、syntax、diff PASS，正式红门不变，产品0/browser0，现复审。
- p3a1 blocker-only 再次 `BLOCK / P0 0 / P1 1 / browser0`：direct return 已闭合，`if(true) return` 仍可绕过可达性门。同一夹具阻断连续两次触发强制冻结，不再进行第三轮“补夹具—跑门”；p3b 产品与后续 browser 均未启动，等待用户裁定完整 AST 冻结或接受窄风险。
