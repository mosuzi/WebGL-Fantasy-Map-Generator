# 开发日志：2026-08-08 至 2026-08-14

> 本卷由第 327 项从旧开发总日志机械分卷；正文保持迁移前原文和原有相对顺序，只在需要追溯时定向读取。

## 2026-08-13：登记第 331 项——100k 重生成与存档性能、真实阶段 Loading

- 用户确认第 330 项之后重生成与保存 Loading 仍显普通，且 100k 重生成、保存总耗时过长。隔离 Chrome 的 `99846` cells 浏览器存档连续三次为 `16.316 / 17.219 / 18.804s`，IndexedDB 写入仅 `32.3～47.5ms`；最后一轮约到 `10.57s` 才进入 Worker compute，说明整图 graph 输入是最大单项。Worker 内另有 normalize stringify / parse、正式 stringify 和 gzip helper stringify 三次完整文档遍历。
- 正式 100k 重生成基线中，fresh rivers `13.343s` 由整图输入 `6.505s`、compute `4.438s` 和 render install `1.723s` 构成；同图 routes / markers reuse 后总耗时约 `3s`，输入只剩 `5ms`，但全量 picking / overlay 安装仍约 `1.9s`。当前普通 Loading 把这些真实阶段压成“准备 / 计算 / 应用 / 更新画面”，保存后半段 base64 / 信封 / 存储没有新阶段，文案与真实工作不完全同源。
- 第 331 项获批按 `A 精确阶段计时与文案 → B 单次序列化和大存档二进制 IndexedDB → C canonical map mirror 与安全增量渲染 → 集成复核 / 最终验收` 实施。前一轮只读调查计为任务级唯一独立调查，主线程为唯一写者；不修改领域结果、公开存档格式、旧数据兼容、用户 Chrome / 地图、`source/` 或 Wiki，不用假百分比、提高 LongTask 阈值或删正式图层换取通过。

## 2026-08-13：第 331 项阶段 A——真实阶段计时与 Loading 文案

- 重生成普通 Loading 现由真实 stage 驱动“梳理现有内容 → 汇拢山河脉络 → 推演新内容 → 收束结果 → 归入地图 → 重整细节”，保存按“收拢全图资料 → 压制存档体积 → 整理存档内容 → 妥存至浏览器”推进；普通文字不出现 Worker、线程、包、picking 或浏览器存储实现，调试数据保留标量精度。
- operation 阶段快照新增 progress / completed / total / layer 白名单；重生成 Worker 新增 setup、domain compute、patch capture、render prepare 计时，存档 Worker 新增 normalize、stringify、compress、package 计时，浏览器保存结果同步返回 transport 和 Worker 分段。
- 文案、operation、map-file Worker、十一类 session Node 与生产构建通过。10k 路线真实 Chrome 的六段文案均可见，fresh 输入 `214` 包 / `1108ms`、domain `50.5ms`、patch `0.3ms`、Worker render `34.1ms`、主线程 render install `888.3ms`，LongTask `0`；10k 存档四入口 LongTask、技术泄漏、health、console、page、WebGL 和 Loading 均为 `0`。旧大一统 Loading 夹具连续暴露 File.text / legacy load 历史探针，局部迁移已撤回并改用现有 Worker 工具窄入口，未伪报该旧门通过。

## 2026-08-13：第 331 项阶段 B——单次序列化与大型二进制浏览器存档

- 当前 live map 导出只生成一次正式 JSON 文本，gzip 直接消费同一文本，不再经历 normalize stringify / parse 与压缩 helper 的重复 stringify；旧 document 导出迁移语义不变。大型 gzip 达到 `4 MiB` 后直接保存为版本化 IndexedDB `Uint8Array` 记录，恢复继续走既有 map-file Worker bytes 协议；小存档、quota fallback、旧 gzip-base64 / plain envelope、LocalStorage 和旧 IndexedDB 字符串均保留。
- 10k quota fallback 三次真实保存为 `1.501～1.591s`。固定 `99846` cells 三次保存由优化前 `16.316～18.804s` 降到 `10.817～11.455s`，`14,209,461B` gzip 不再膨胀为 base64 envelope，base64 / encoding 为 `0ms`、IndexedDB 写入约 `17ms`；二进制记录恢复为同一 `99846` cells，console / page error 为 `0`。旧 v2 gzip-base64 LocalStorage 成功恢复且损坏原存档仍原样保留。
- 阶段 B 产品 `4` 个文件、测试 `2` 个文件；专项 Node、生产构建、10k / 100k 真实保存恢复与旧存档浏览器门通过，主线程委派与等待为 `0`。剩余 `6.23s` 整图 graph 输入及约 `5.15s` Worker 内工作进入阶段 C，不在本 checkpoint 伪报完成。

## 2026-08-13：登记第 332 项——100k 视图切换卡死

- 用户补充报告大型地图切换视图也会卡死。该问题独立登记在第 331 项之后，覆盖颜色 / 视图模式、主题和视图选项的正式入口；第 331 项阶段 C 可复用其 renderer / presentation 证据，但不得在保存与重生成分支顺手扩修并提前冒充完成。
- 后续必须用隔离 Chrome 分开计量 Worker prepare、surface / line、overlay DOM、draw、style / layout、LongTask 和最终稳定时间，锁地图 checksum / revision、相机、selection、hover、picking、图例及错误面；不删正式图层、不提高阈值、不连接用户 Chrome。

## 2026-08-13：第 331 项阶段 C——同图镜像复用与局部 picking

- 当前地图的浏览器存档导出已复用 `regeneration.compute` 的持久 map-mirror session；Worker 在 retained canonical map 上直接完成单次序列化与 gzip，delta `0` 提交保持 map revision 不变。固定 100k 首次保存仍需建立镜像，耗时 `10.516s`；随后两次输入由 `953` 包 / `6.839s` 降为 `3` 包 / `3.3～4.1ms`，总耗时降为 `3.606 / 3.705s`，gzip 为 `2.45～2.60s`，二进制 IndexedDB 写入、恢复、console 与 page error 继续通过。
- prepared picking 新增受控对象族写集：routes 只重绑 city / route，rivers 只重绑 river，markers / military 等只重绑各自对象族；zones 不再请求无数据写入的对象 picking。局部安装保留未变化 bucket 数组引用，空 bucket、统计和 Map 顺序可精确回滚；缺正式基线、全图 generation 或无法证明安全增量时继续走完整 picking。
- 10k / 100k routes 正式浏览器入口均 Worker accepted / committed、单历史、撤销 / 重做、分阶段 Loading、health、console、page、WebGL 与 Loading 清理通过。100k render install prepare 为 `1.526s`，其中 picking rebind `1.276s`，输入 / 输出单包最大 `2.1 / 4.2ms`，产品 LongTask 为 `0`；point、labels、city overlay 与路线 GPU 仍按正式写集更新。
- 阶段 C 产品 `6` 文件净增 `84` 行，测试 `3` 文件净增 `87` 行，未新建浏览器夹具；通用十一类 Worker/session/packet/ack/cancel、render preparation、prepared transaction、生产构建和两档真实 Chrome 目标门通过。阶段 A～C 现冻结在版本 `0.2.6`，下一步只做一次独立集成复核与一次独立最终真实入口验收；本阶段委派与等待为 `0`。
- 独立集成复核发现内部 `input-stream-complete / complete` 被宽正则提前映射为“地图存档已经妥善收好”，而压缩、结果回传和浏览器写入尚未完成。文案现精确保持为“正在收拢全图资料 / 正在整理存档内容”，只有现有成功 toast 在真实写入结束后宣告成功；专项反例锁住输入完成、Worker 完成和 storage-write 三个阶段，版本递增为 `0.2.7`。
- blocker-only 复查 `ACCEPT` 后，独立观察使在冻结树亲跑五项 Node、生产构建、10k routes、100k 三次保存 / 恢复及旧 v2 gzip-base64 兼容并给出 `ACCEPT`。100k 实值为 `10.542 / 3.610 / 3.666s`、输入 `953 / 3 / 3` 包；二进制存档约 `14.21MB`，写入约 `16～18ms`，恢复 `99846` cells 与 checksum 同源。第 331 项归档版本为 `0.2.8`；`source/`、Wiki、用户地图、console / page / WebGL 与残留进程未改或为 `0`，下一项固定为第 332 项。

## 2026-08-13：第 322 项 Stage D2 动态显示准备 Worker 闭环接受

- 主题、视图模式、单图层 / 图层组可见性、平滑边界、海洋高度和最大城市标签数统一进入独立的 `render.prepare` map-mirror session；领域计算 session 不被占用。首轮传递正式地图，后续显示操作只发送 `3` 个输入包；主线程只应用展示标量、校验 map / revision / token / sequence、提交 prepared renderer、确认 delta `0` session、恢复 UI 并完成 GPU / DOM 原子安装。Worker 预接受不可用时才在完整回滚后执行原同步兼容入口，普通用户只看到“正在改用兼容方式继续处理”。
- 只读盘点确认普通显示入口没有 picking CPU 重建；数据写事务的 picking 仍由既有 prepared renderer 路径负责。图层组操作合并为一次事务，显示失败按当前图 / 已脱离图区分 rollback 与 finalize；UI Promise 拒绝会从 renderer 已提交的 color mode、主题、view options、label options 和 layer visibility 恢复控件及持久偏好，不读取已经被用户改写的 DOM 值。
- 同一 100k 地图目标复验中，所有同步 `refreshCellSurface / refreshLineLayers / refreshLabels` 调用为 `0`，独立 session 首次输入 `989` 包、后续复用 `3` 包且最终 idle；视图模式、点图层开关和平滑边界 LongTask 为 `0`。主题从 `905ms` 降至 `55 / 60 / 68ms`，最大城市标签从 `255ms` 降至 `54 / 59ms`，剩余信号已归因于 prepared DOM 的末端样式 / 布局及最终 draw。按用户统一处置门登记 D2 有界例外：主题最多 `3` 条且单条不超过 `75ms`，最大标签最多 `2` 条且单条不超过 `60ms`；不扩张到其它入口，也不带入阶段 E。
- `node --check`、显示事务静态契约、`regress:api-action-convergence`、完整 Worker task / render preparation 专项和生产构建通过；目标浏览器非性能 health、WebGL、Loading 错误为 `0`。集成冻结复核另发现快速连续切换时 `operation_busy` 会把内部 action 名写入普通 toast，现改为不透传原异常的固定中文提示，并由静态反例锁定。证据摘要位于 `work/task322-d2-presentation-diagnostic-summary.json`。D2 产品 `1` 文件 `+282 / -13`，工具 `2` 文件 `+52 / -3`，专用临时诊断约 `110` 行且不提交；本阶段主线程委派等待为 `0`，集成复核为任务级唯一给事中。阶段 D 已闭合并建立 checkpoint，下一步只进入阶段 E 集成终验；用户地图、`source/` 与 Wiki 未改。

## 2026-08-13：第 322 项 Stage D1 大型存档读写与恢复 Worker 闭环接受

- 大型存档导出、浏览器保存、完整导入与浏览器恢复已改用既有 `map-file-io` Worker；导入 Worker 同一任务完成解析、迁移、校验和完整 renderer prepare，正式主线程只做原子安装、地图替换、GPU / DOM 提交与面板同步。`.webfmg`、JSON、gzip-base64、File / Blob、LocalStorage / IndexedDB、旧迁移和公开返回结构继续保留。
- 专项 Node、API data compatibility、action convergence、通用 Worker 协议及生产构建通过；10k Chrome 的四条正式入口均为 Worker accepted，两个导入均使用 prepared load、legacy load 为 `0`，地图 seed / checksum、历史、存储后端、Loading、非性能 health、应用 console、page、WebGL 与普通用户技术词门通过。
- 初次目标门在导入 / 恢复各记录 `729 / 684ms` LongTask。窄时间轴先排除 Worker 单包解码、renderer prepared install、overlay reveal和运行时面板刷新；一次“非持久 Worker 自行关闭”优化复验无收益后已完整撤回。继续沿调用顺序核对发现，导入在 prepared install 前以 `force:true` 把持久化主题应用到即将替换的旧图，触发同步 surface / line / labels 重建和 draw，时间窗与两条信号精确一致。
- renderer 新增 `setPreparedPresentation`，只接收 Worker 已准备画面对应的主题与规范化单位状态，不刷新旧图；新图背景、GPU、DOM和 overlay 仍由 prepared transaction 与正式 load 链完成。最终 10k Chrome 四条正式入口均 Worker accepted，prepared load `2`、legacy load `0`、prepared presentation `4`、旧图完整主题 / 单位更新均为 `0`；LongTask、非性能 health、应用 console、page、WebGL、Loading和普通用户技术词泄漏均为 `0`。存档专项 Node、API data compatibility / action convergence与生产构建通过。通过证据位于 `work/task322-map-file-io-presentation-fix/`；D1 已接受，阶段 D 下一步只进入 D2 动态 mesh / 标签 / picking 盘点。

## 2026-08-13：第 322 项 Stage C3d 军事策略 Worker 闭环接受

- `edit.military.setRatios` 与军事面板兵种比例入口已接入 `military-policy.compute` 持久 map-mirror session。Worker 在锁定军团保护下重建部署，返回受控 military / state patch 与 point / line / labels / picking prepared DTO；主线程只做 binding、写集、历史和 renderer 原子提交。军团状态、驻地、基地与战报等局部命令不属于本轮复杂策略计算，保持既有同步路径。
- 军事策略任务新增 Worker 历史模式，撤销 / 重做先在镜像应用目标 patch并准备四层画面，再由主线程移动正式历史栈。专项 Node 的 10k 正例锁住 `17` 条写集、锁定军团、legacy / fallback 同源、对象身份与镜像别名、prepared 四层和历史往返；100k 数据层约 `1.42s`。通用 Worker 协议、registry、API stability / suite / edit coverage 和生产构建通过。
- 首次 Chrome 门唯一失败是成功阶段把“军事策略 Worker 补丁已准备”写入普通 Loading；任务对用户可见的成功、取消与错误消息现统一使用自然中文，内部 task / code 保持不变。唯一目标复验随后通过正式应用、撤销、重做 `3` 次运行与 `3` 次提交，后两次复用同一 session；数据精确往返、国家 / 军事镜像别名、状态对象身份、LongTask、非性能 health、应用 console、page、WebGL、Loading 与技术词泄漏均为 `0`。
- C3d 产品 `3` 文件 `+208 / -33`，测试 `3` 文件 `+158 / -5`，专用浏览器夹具 `113` 行，委派等待为 `0`。阶段 C 已闭合并转入阶段 D；用户地图、`source/` 与 Wiki 未改，本 checkpoint 不合入 `main`。

## 2026-08-13：第 322 项 Stage C3c 路线 / 城市寻路 Worker 闭环接受

- 路线创建 / 改线、城市迁移关联路线重寻及两类历史恢复统一接入 `route-path.compute` 持久 map-mirror session。Worker 负责预检、寻路、正式镜像写入、历史前后 patch 和 route / point / labels / picking 准备；主线程只校验 binding、提交原命令与历史栈、安装 prepared renderer 并同步面板。路线面板与公开 inspect API 使用异步 latest-wins，普通用户文案不暴露 Worker、线程、会话、消息包或浏览器存储概念。
- 专项 Node 已覆盖 10k / 100k 的 create / edit / relocation / city-relocation、plan-only、正式镜像撤销 / 重做、取消、故障、陈旧、fallback 与写集；route edit、city relocation、Worker registry / session、API stability / suite / coverage 和生产构建通过。通用 `regress:api` 只命中既有 `generate.newMap` operation-stall health，未发现本切片契约错误。
- 正式 10k Chrome 已完成路线预检 / 更新 / 撤销 / 重做和城市预检 / 移动 / 撤销 / 重做共 `8` 次 Worker 运行与 `8` 次 session 提交；后 `7` 次复用同一 session，revision delta 为 `[0,1,1,1,0,1,1,1]`，两项操作各产生一条历史，主 map、路线、城市与 burg 权威集合身份稳定，四次撤销 / 重做值精确。旧夹具把会重建的 `pack.routes` 镜像数组引用误当权威身份，现只保留其内容与别名精确门。
- 初次浏览器门把八次大型 `structuredClone + JSON.stringify` 重指纹也计入 operation observer，得到 `11` 条 `52～194ms` 信号；按产品窗先结算、重指纹后置的方式完成一次夹具优化后，仍记录 `4` 条 `110 / 113 / 132 / 193ms` 级信号。用户按新统一规则登记放行：C3c 自动门最多 `4` 条且单条不超过 `200ms`，不再继续补夹具或重复性能诊断；任何超限、功能 / 原子性 / 回滚 / 错误面失败仍阻断，该例外不适用于军事、阶段 D 或阶段 E。
- C3c 累计产品 `6` 文件约 `+720 / -121`、测试 `3` 文件约 `+377 / -5`，专用浏览器夹具 `206` 行，委派等待为 `0`。阶段 C 下一切片只处理军事策略，用户地图、`source/` 与 Wiki 未改，本 checkpoint 不提前提交或合入 `main`。

## 2026-08-12：第 322 项 Stage C2 社会扩张与历史恢复 Worker 闭环接受

- 文化 / 宗教的正式扩张入口已接入 `social-expansion.compute`，并继续复用阶段 A～B 的持久 map-mirror session、prepared renderer、绑定校验、原子提交和失败恢复。历史命令新增只读 Worker patch 描述；C2 命令的撤销 / 重做会在同一 Worker 镜像应用 before / after patch、准备对应 renderer DTO，再由主线程移动历史栈并提交 GPU / DOM。其它领域历史命令不受影响，仍保持既有执行语义。
- 专项 Worker Node 门通过 10k 文化 / 宗教同源、锁、取消、故障、fallback 和精确 undo / redo 镜像往返；UI / API 契约保持 `322` 个公开方法、`179` 个编辑方法，历史入口允许异步结果；通用十一类 Worker session / packet / ack / cancel 回归和生产构建均通过。普通用户文案不出现 Worker、会话、消息包等实现词。
- 正式 Chrome 目标门已完成文化应用、撤销、重做、宗教应用、撤销、重做共 `6` 次 Worker 运行与 `6` 次 session 提交；六次操作复用同一 session，revision delta 均为 `1`，前后数据、对象引用、历史栈与最终 idle session 同源。旧历史恢复会同步执行 picking 与完整 surface 刷新，曾形成约 `251～329ms` 主线程阻塞；改造后撤销 / 重做本身不再出现该阻塞。
- 最终复验只剩首次文化操作启动窗口一条 `56ms` LongTask，比统一 `50ms` 门高 `6ms`；它不属于撤销 / 重做，现有证据只收敛到首次 fresh Worker 创建或首次地图输入流入口，没有把未证实原因写成产品结论。用户明确裁定登记放行：C2 仅允许首次 fresh 启动最多 `1` 条且不超过 `60ms`，不得扩张到后续复用、撤销 / 重做、其它切片或阶段 D / E；阶段 E 仍须按最终性能口径重新审计。
- C2 累计产品 `7` 文件 `+244 / -22`，既有工具 `2` 文件 `+33 / -7`，专用浏览器夹具 `72` 行，`package.json` 增加 `1` 行脚本；本阶段委派与等待均为 `0`。第 322 项内部 checkpoint 更新为 A、B1、B2、C1、C2，即 `5 / 6`；用户地图、`source/` 与 Wiki 未改，本轮不提交、不合并，也不提前启动第 327 项。

## 2026-08-12：第 322 项 Stage C1 网格拓扑 Worker 闭环接受

- `grid.applyWrite / grid.refine` 已移除独立的主线程整图 snapshot transaction 与 legacy renderer reload，改为复用阶段 A～B 的 `executeWorkerMapMutation`：正式 Worker、持久 map-mirror session、兼容 fallback、prepared renderer、绑定校验、原子地图替换、单历史与失败恢复共用一条提交协议。网格 binding 的 map / revision、generation token、锁和来源网格 fingerprint 同时进入普通请求、session 与 fallback；Worker 在替换地图上准备完整 renderer DTO，主线程只做当前性校验、正式 map identity 内的内容交换、GPU 提交和界面同步。
- 静态门与生产构建通过；`pnpm run regress:grid-topology-refinement` 真实完成 `10004 → 100000`，保持陆地 / 水体连通分量 `6 / 4`、正式 map identity、Worker prepared point transfer 与撤销来源 fingerprint。阶段增量为产品 `2` 文件 `+98 / -48`、工具 `2` 文件 `+92 / -3`，专用浏览器夹具 `202` 行，符合工具不高于产品及夹具不超过 `500` 行的门禁；本阶段委派和等待均为 `0`。
- 唯一一次正式浏览器目标复验未进入产品：系统 Chrome `151.0.7922.77` 由 Playwright `1.60.0` 启动后在创建 context / page 前以 `exitCode=0` 自行退出，端口和隔离进程随后为 `0`。该结果归类为环境启动失败，不能替代 CDP 验收；按首败止损未重跑或切换到更弱浏览器。C1 因缺正式浏览器证据保持未接受，未建立 checkpoint、未进入 C2，用户地图与 `source/` 未触碰。
- 后续同配置空白页诊断确认系统 Chrome context / page 已恢复。重复的 `Maximum call stack size exceeded` 经静态调用链定位到 `refineGridTopology` 在构造 100k metadata 时执行 `Math.max(...cells.c.map(...))`：旧主线程和新 Worker 路径都会运行该语句，而 Chrome / Worker 的函数参数上限低于 Node，因此两条架构同名失败。该处改为等价逐项归约并由源码反例门锁定；`pnpm run regress:grid-topology-refinement` 再次完成 `10004 → 100000`，拓扑、迁移、身份和撤销结果不漂移。
- 修正后正式 Chrome 已真实完成 Worker accepted / committed、输入 / 输出 `212 / 3954` 包、全部 `13` 个 prepared layer、100k 网格、单历史、撤销 / 重做、PNG、GL 与非性能错误面；旧堆栈阻断已闭合。新首败是目标操作耗时 `51.3s` 并记录 `60 / 165 / 114ms` 三条 LongTask。遥测显示主线程 render install prepare 为 `37.8s`，其中 cell visual shape / triangle / cells / edges 的逐项 `await checkpoint` 约执行一百万次并形成数千个极碎让步，而非 Worker compute、包解码或 GPU commit 阻断。
- cell visual 五个大循环现每 `32` 项或末项才进入异步 checkpoint，仍保留取消 / 过期检查、`6ms` 时间预算与进度报告；10k render preparation 专项通过并新增让步次数上限门。用户随后明确授权唯一一次 C1 性能复验：render install prepare 从 `37.8s` 降为 `2.9s`，操作从 `51.3s` 降为 `16.4s`，Worker accepted / committed、输入 / 输出 `212 / 3954` 包、全部 `13` 个 prepared layer、100k 网格、单历史、撤销 / 重做、PNG、GL 与非性能错误面通过；但正式窗口仍记录 `59 / 164 / 114ms` 三条 LongTask，且 `legacyLoadUnchanged=false`。阶段因此按首败冻结为 BLOCK，不追加诊断或第二轮。
- 后续裁定确认两处夹具口径：`legacyLoadUnchanged` 原先在 undo / redo 后才读取，而历史恢复按合同会调用 legacy load；改为 forward refine 后立即取样后真实为 `true`。LongTask observer 也把预检 / 基线与正式操作拆窗，原先 `59ms` 明确属于 setup；一次低侵入诊断中的正式操作窗为 `157 / 109 / 55ms`。按授权只做了“完成解码即释放内部节点索引 + 网格替换复用整图生成已验的隐藏 overlay 分批呈现”一组窄试修，静态、Worker graph、render preparation、100k topology Node 和生产构建均通过，但唯一目标复验仍为 `159 / 112ms`，未形成收益；该组试修已完整撤回，不把推测性架构留在产品。
- 最新正式复验继续通过 Worker accepted / committed、`212 / 3954` 输入 / 输出包、`13` 层 prepared install、100k 结果、单历史、撤销 / 重做、PNG、GL、Loading 与非性能错误面，旧 renderer 全量 load 也已确认为 `0` 次。用户随后明确批准把操作窗的 `159 / 112ms` 两条 LongTask 先登记放过；C1 自动门固定为最多 `2` 条且单条不超过 `160ms`，任何第三条、超限或其它功能 / 原子性 / 错误面失败仍为 BLOCK。该例外只接受 C1 checkpoint，不宣称性能问题已消除，也不向阶段 D 的 CPU renderer / 标签 / picking 或阶段 E 最终终验自动传递。
- C1 据此接受并准备建立内部 checkpoint。阶段累计产品 `4` 文件 `+109 / -54`、工具 `3` 文件 `+103 / -4`，专用浏览器夹具 `209` 行；第 322 项已接受 checkpoint 为 A、B1、B2、C1，即 `4 / 6`，本阶段委派等待 `0`。下一阶段按权威顺序进入 C2，不继续扩修 C1。

## 2026-08-12：登记第 328 项——大河参与国家与省份划界

- 用户要求省份自动划界和重新生成省份时适度考虑大河走势，并允许具备足够强度、长度和两岸行政条件的大河成为省界，乃至成为国界。新任务登记为第 328 项，排在现有队列末尾；当前固定执行顺序更新为 `322 → 327 → 323 → 324 → 326 → 328`，不得插入当前第 322 项施工分支提前实现。
- 第 328 项把大河定义为基于 canonical 流量、宽度、等级和连续长度的可审计软阻隔，而非所有河流都不可跨越的硬墙。国家 / 省份扩张仍综合中心、人口、文化、地形、距离、聚落、港口、道路与合法渡河走廊；只有两岸都具备可持续中心和连通腹地时才允许沿河分省或分国，禁止飞地、单 cell 长条、断裂区域和为贴河而反写河网。
- 范围只覆盖新图政治生成和用户明确触发的国家 / 省份重生成；旧图载入、普通渲染和手工边界不自动重划，锁定行政对象与用户边界优先。最小验收以强河 / 弱河对照、渡河走廊、锁定、无河及 10k / 50k / 100k 真实入口锁定沿河边界、行政连通、确定性、撤销 / 重做和原子回滚。本轮只登记权威范围、顺序和验收，不修改产品、构建、浏览器、用户地图、`source/` 或 Wiki。

## 2026-08-11：第 322 项 Stage B1 复合派生 Worker 闭环接受

- 高度基础 / 下游 / 全量、气候下游和洋流世界重算不再各自维护主线程提交链，统一复用阶段 A 的 `executeWorkerMapMutation`：正式 Worker 与兼容 fallback 共用不可变输入、map-mirror session、prepared renderer、binding / revision 校验、单条历史、deferred replay、原子 guard 及独立失败恢复。洋流全图替换命令保持 `state.map` 对象稳定并支持 exact undo / redo；公开高度 API 元数据改为异步。产品增量为 `7` 个文件、`+443 / -321`。
- 专项 Node 门真实通过：气候下游 `4.4s`，复合 Worker / fallback / 锁 / 取消 / 故障 / obsolete / 100k 高度输入 `27.9s`，普通用户文案门泄漏为 `0`；生产构建通过。既有地图事务浏览器门在把 setup `operation-stall` 与目标窗口分离后通过十一类重生成、三类高度、GEO、单历史、undo / redo、busy、UI 与 WebGL。新增独立洋流世界浏览器门为 `141` 行，正式 `2k` 入口通过十一阶段、`113 / 492` 个输入 / 输出包、session committed / idle、历史 `+1`、undo / redo、Loading / WebGL / console / page / 技术词泄漏全 `0`。
- 首轮洋流门发现普通 Loading 暴露输入分片、后台计算等底层实现，现统一改为“整理地图推演所需资料 / 准备地图推演 / 推演地图内容 / 整理地图推演结果”等自然中文，内部结构化 stage 与错误码不变。既有 compound-lock 浏览器夹具用任意首对象组成正例，四个固定 seed 均被正式锁安全门合法拒绝；海底重设夹具仍依赖已移除的旧按钮；两者未以改 seed、吞错或第三轮重跑伪绿。
- 旧 `regress:map-snapshot-transaction` 连续暴露阶段 A 前的主线程静态正则：更新同组入口后又停在下一条已移除的历史恢复接线。按新止损门撤回本轮局部夹具改动，不做第三次补丁 / 重跑；B1 以已通过的专项 Node、动态事务浏览器门和独立洋流正式入口为冻结证据。工具 / 脚本增量约 `+378 / -17`，未扩写约 `7018` 行的主 session 浏览器工具；阶段 B 完成 `1 / 2`，第 322 项约为 `2.5 / 6`，本阶段委派与等待均为 `0`。

## 2026-08-11：收紧智能体委派与长任务投入产出门禁

- 用户暂停第 322 项并要求核算投入产出。盘点确认内部进度只有阶段 0 与阶段 A，约为 `2 / 6`，但已形成 `28` 个第 322 项产物目录、`126` 个文件、约 `22.4 MB`，其中 `62` 份日志、`14` 份结果 JSON；阶段 A 的测试工具新增量约为产品新增量的 `1.29` 倍，主 session 浏览器工具已膨胀到约 `7018` 行。大量时间消耗在执行者与复核者往返、夹具假阳修复、重复全门和角色切换，未转化为相称的产品阶段完成。
- 根因不是“每个智能体都各自执行完整四级”，而是把任务级四级职责错误下沉到阶段、首败和机械修正，反复形成“执行 → 复核 → 限修 → 再复核”的微型审批流。即日起主线程默认直接承担冻结设计下的实现、夹具、诊断和窄修；完整四级只在权威编号级保留一次独立调查、主线程执行、一次集成复核和一次最终观察使验收，不再为正则、断言、字段名和测试等待切换角色。
- 新硬门包括：每个权威编号最多三个委派角色与两个 blocker-only 复查回合；子智能体不得派生智能体；专用浏览器夹具原则上不超过 `500` 行，单阶段工具新增不得高于产品新增；浏览器首败后只允许一次窄诊断与一次目标复验，夹具连续两次失败必须停下重审；每 `30` 分钟或阶段边界公开产品 / 工具增量、完成比例、真实门禁和委派等待时间。第 322 项产品施工继续暂停，本次只修改执行制度、当前计划与通用 Skill。

## 2026-08-11：第 322 项 Stage A 十一类重生成 Worker 闭环接受

- 连续全锁河流 no-op 的 `113ms` 首败经独立中书舍人归因，确认工具在 CPU surface 指纹后未等待 `PerformanceObserver` 异步投递。新窄诊断把锁定、CPU hash、引用快照、两次 no-op、输入 slice、Loading 与 LoAF 只记录为标量，并在 pre-probe 和 operation 后都执行 `setTimeout(0) + 2 rAF + takeRecords()`；前置唯一 LongTask 为 `120ms`，其中 CPU hash `106.1ms`，正式两次 no-op 窗口 LongTask 为 `0`。后置 surface GPU / CPU 字节、segment / buffer 引用、descriptor、TypedArray、renderer、session 和 Loading 精确门全部保留。
- 给事中冻结复核确认普通 full 与诊断路径使用同一 observer drain，诊断 flag 只控制 wrapper、artifact 与短路。观察使随后唯一一次运行当前树 `pnpm run regress:worker-session-browser`，exit `0`、最终 JSON `ok=true`；10k 全部正式 operation / 最终 LongTask 为 `0`，非性能 health、应用 console、page、WebGL 与 Loading 错误为 `0`，十一类 fresh / reuse、accepted 后 fallback `0`、取消 / 换图 / 迟到、原子回滚、十个 prepared fail-closed、deferred / recovery、九段 late context 与普通用户纯中文技术词门均通过。
- 此前冻结的 100k fresh routes 代表门继续有效：Worker 输入投递、输出解码、回传、安装提交和 UI 刷新为 `2.2 / 2.6 / 1.5 / 1.3 / 7.2ms`，两条已完整归因的末端展示任务均为 `57ms`，符合仅限 Stage A 的分阶段预算；Stage D 仍须继续收紧，Stage E 不自动继承。Stage A 据此接受并形成内部 checkpoint，但第 322 项尚未完成，不得据此合入 `main` 或启动第 327 项。证据位于 `work/task322-no-op-attribution/`、`work/task322-stagea-final-10k-rerun/` 和 `work/task322-100k-fresh-routes-diagnostic/`。

## 2026-08-11：第 322 项 Stage A 止损复核与 no-op 主线程阻断

- 用户指出第 322 项长时间运行却没有形成与耗时相称的权威完成结果。按权威阶段重新核算后，当前只有阶段 0 盘点完成；Stage A 尚未封板，Stage B～E 均未开始。执行改为精简阶段交付：冻结树后只做一次独立复核、一次代表性真实浏览器门；昂贵门首败后只允许一次归因、一次窄修和一次目标复验，不再循环重跑整套。
- 删除未能在正式 100k routes 命中的 overlay DOM 复用实验，保留正式原子整树安装；Stage A 100k 代表门只允许最多两条、单条不超过 `75ms` 且可归因于末端样式布局或 `resume / draw / updateLabels` 的展示任务，输入投递、输出解码、Worker 回传、安装提交和 UI 刷新仍各自低于 `50ms`。该额度仅用于 Stage A，Stage D 必须继续收紧，Stage E 不自动继承。当前 100k fresh routes 真实门通过，相关值为 `2.2 / 2.6 / 1.5 / 1.3 / 7.2ms`，两条末端任务均为 `57ms`。
- 独立给事中静态复核为 `ACCEPT`，但观察使唯一一次当前树 10k focused 在“两次连续全锁河流 no-op”首败，记录 `246ms / 225ms` LongTask。首轮归因确认夹具在操作窗前后执行整套 surface GPU `getBufferSubData` 指纹会制造大任务；隔离读回并保留 CPU / GPU 字节、buffer / segment 引用和 descriptor 精确门后，目标复验仍出现一条 `113ms` 正式窗口 LongTask。两次 no-op 的 Worker 遥测本身为 fresh / reuse 输入单包最大 `2.4 / 0.2ms`、计算 `29.1 / 28ms`、输出解码不超过 `0.8ms`，且安装与刷新均为 `0`，故尚有未归因的主线程连续任务。
- 按止损门不再继续第三轮诊断或完整回归。Stage A 当前结论为 `BLOCK`，不能提交 checkpoint，也不能进入 Stage B；后续若恢复，应先取得用户对“专门治理 no-op 主线程窗口”的授权，再用单一窄诊断定位，不得放宽 10k focused 的 LongTask `0` 标准。证据位于 `work/task322-stagea-final-10k/`、`work/task322-no-op-diagnostic/` 与 `work/task322-no-op-recheck/`。任务端口和隔离 Node / Chrome 已清理，用户 `5410` 页面未触碰。

## 2026-08-11：补充第 322 项普通用户无感知与纯中文文案门

- 用户明确要求第 322 项的 Worker 化不得向普通用户暴露技术实现：全局 Loading、控制面板、toast、对话框、状态、进度、取消、降级和错误提示不得出现 `Worker / worker`，也不得出现线程、任务会话、消息包、结构化克隆、buffer、`LocalStorage / sessionStorage / IndexedDB`、Blob、缓存后端等技术或浏览器存储概念。`routes / rivers` 等内部 kind 必须显示为“路线 / 河流”等自然中文。
- 所有第 322 项触达的普通用户界面文案优先使用纯中文，只有确实没有合适中文译法的专名才能保留外文；技术标识仅可留在开发模式、内部日志、测试产物或 API 结构化诊断字段。兼容降级只向用户说明正在以兼容方式继续处理，不解释底层线程或存储机制。
- 最终真实浏览器门新增可见文本审计，覆盖正常、长任务、取消、兼容降级、故障与回滚路径，并核对全局 Loading、控制面板和关联提示没有技术词、内部英文 kind 或可自然翻译却未翻译的英文文案。本轮只补权威范围与验收口径，不恢复第 322 项代码施工、构建或 CDP，不修改产品代码、`source/`、Wiki、用户地图或浏览器。

## 2026-08-10：固定当前权威队列的逐项分支与 main 合入门

- 用户要求当前目标中的每个权威任务都必须在独立分支完成，最终验收后先中文提交并推送该任务分支，再把它合入并推送 `main`；只有确认远端 `main` 已包含完成提交，才从最新 `main` 新建下一项分支。内部阶段 checkpoint 继续留在本项分支，不能以阶段通过代替权威任务完成或提前进入下一编号。
- 当前分支已是第 322 项专属 `codex-task-322-workerization`，本轮不切换、不提交、不合并，也不移动其未提交施工树。下次恢复时在该分支完成第 322 项全部阶段与终验；随后推送任务分支、合入并推送 `main`，再从最新远端 `main` 新建第 327 项分支。第 327 项完成并进入 `main` 后，才依次从最新 `main` 新建第 323、324、326 项分支。
- 权威顺序仍为 `322 → 327 → 323 → 324 → 326`。这一逐项合入门确保第 327 项的 AI token、项目文档和四级流程治理不会被遗留在未合并分支或被自然编号第 323 项越过。本轮同时保持 Git / QGIS 安装包、生成产物、Codex JSONL 和会话归档原样，未做任何清理。

## 2026-08-10：登记第 327 项——AI 上下文、项目文档与四级流程节流

- 用户要求把项目结构、文档体系、通用 Skill 以及此前尚未完全落地的 token 节省方案登记为新的权威任务，并把它插到第 322 项之后、第 323 项之前。权威执行顺序现固定为 `322 → 327 → 323 → 324 → 326`；下次恢复当前暂停目标时，必须先完成第 322 项，随后直接执行第 327 项，不得按自然编号跳到第 323 项。
- 第 327 项封闭治理根 / 嵌套 `AGENTS.md` 的自动注入预算、`docs/README.md` 与 `current-plan.md` 权威路由、开发日志分卷及归档隔离、定向检索、阶段 handoff、本地产物、`$run-lean-staged-delivery` 和 token-efficient `$four-officials-flow`。根项目说明硬门为 `16 KiB`，普通工作路径累计项目说明为 `24 KiB`；历史内容必须可追溯迁移，不能靠删除有效规则、完成证据或编号关系达标。
- 四级流程将以最小必要角色、无完整历史继承、顺序接力、单一写者、冻结复核、blocker-only 返工、昂贵门禁首败即停和 artifact 路径交接为默认；完整四角色只在用户明确要求或风险确需时启用。最终仍保留真实独立复核 / 验收，不允许用节省 token 降低旧档、回滚、真实浏览器或视觉标准。
- 本轮只登记第 327 项、顺序和最小验收，不实施文档迁移、Skill 再改造或上下文审计，不恢复第 322 项代码施工、构建或 CDP。第 327 项完成前不得提前开始第 323 项；产品代码、`source/`、Wiki、用户地图和浏览器均不改。

## 2026-08-10：引入通用精简分阶段交付 Skill，暂停并重整第 322 项执行

- 新增个人通用 Skill `$run-lean-staged-delivery`，把长任务固定为“冻结最终目标与当前阶段 → 只读调查 → 单一写者 → 冻结复核 → 分层真实验收 → checkpoint”的顺序。委派默认使用最小阶段上下文，详细日志 / trace / 截图 / 大矩阵写本地产物，主线程只保留结论、证据、门禁、阻断与 artifact 路径；该 Skill 可与真实 `$four-officials-flow` 叠加，但禁止多写者在共享移动树上边写边审。
- 项目入口 `AGENTS.md` 已把该 Skill 设为跨子系统、长上下文和多轮浏览器任务的执行规范；测试阶梯固定为静态、专项 Node、小数据真实入口、阶段冻结后的代表性大数据、最终全量终验。昂贵测试首败即停，未执行 / 超时 / 夹具失败不得谎报通过；节省的是重复上下文和重复全量测试，不降低旧数据、回滚、真实浏览器与视觉门禁。
- 第 322 项保持原 P0～P2 范围和最终验收不变，内部改为阶段 0 盘点、A 十一类重生成闭环、B P0 复合与整图链、C P1 核心入口、D P2 大数据与 CPU 渲染、E 集成终验。每阶段形成可恢复 checkpoint 后才进入下一阶段，内部通过不得冒充权威任务完成。
- 本次只建立 Skill 与执行门，没有恢复第 322 项产品代码施工、构建或 CDP。当前未提交 Worker 化工作树原样保留；下次恢复的第一步是只读分类“已独立验收 / 数据层已冻结 / 正式入口未接 / 动态门未过 / 非本项改动”，而不是继续在现有巨型上下文中追加修补。
- 用户进一步指出四级流程本身额度过高；`$four-officials-flow` 已改为 token-efficient 接力：显式完整四级仍使用四个真实角色，但默认 `fork_turns: "none"`、单角色运行、冻结后复核、blocker-only 返工、复用原角色、详细证据落本地产物，普通复杂任务按风险使用两级或三级最小角色集。Qianwen 特定路径与浏览器默认值移入按需读取的 reference，不再让所有仓库每次加载；FMG 同步把这些规则写入 `AGENTS.md`。本次仍未恢复第 322 项施工或启动子智能体。

## 2026-08-10：第 325 项完成——河流道路硬解耦、旧档港口拓扑与真实存档 CDP 终验

- 河流重算已从道路生成彻底解耦：成功、全锁 no-op、取消、冲突 / 渲染后失败回滚以及成功后的撤销 / 重做均不替换道路三镜像、route / packRoute 对象、道路 salt、锁仓及条目、GPU buffer、道路 picking bucket / segment，也不触发道路同步 / 异步 mesh 或全量对象索引刷新。旧档 city / burg / grid / pack 身份采用两阶段原子 reconcile；city 槽位、burg 一一对应与数值 ID、orphan、TypedArray 容量、legacy 镜像、端点四身份和 split 路线均有写前门禁。
- 港口拓扑修复只在显式细分或道路重算事务发生，加载仍只诊断并标道路待派生。候选限于同一 refinement mother、同陆地 feature / 国家 / 省份及通过互为主导 overlap 确认的同一水体，正式复用 haven、riverEdges、冻结温度和水路寻路；锁城、锁路端点、锁海路及无法确定的跨水体映射在写前拒绝。单侧 port 镜像缺失可在正式当前港位原地同步；同 cell 多港按锁定合法港、普通合法港、可同步港和稳定 ID 选唯一 owner，其余普通港迁移或清理，锁对象不动。
- 匿名旧档、保存往返、fresh `10k / 50k / 100k`、真实 `10k → 100k`、锁、no-op、取消、失败回滚、撤销 / 重做、港口与道路专项、生产构建和差异检查通过。冻结真实 `100000` cell 存档由页面正式导入时为 `607` 条道路、`1531` 条河流；河流重算到 `1528` 后道路仍为 `607`，道路 mesh / 全量 picking 刷新为 `0`、河流局部 picking 为 `1`。显式道路重算得到 `288` 条路线，其中道路 `16`、小径 `227`、海路 `45`；`188` 个港口全部正式有效，可达水体 `1`、缺海路 `0`。逐段邻接、陆海、正式水路、重复 cell、三镜像、端点、display teleport、港口、picking、行政覆盖、稀疏度和路径比例错误全部为 `0`，真实 route picking 命中；最大显示段 `71.491 world`、最大路径弦长比 `5.723`、最大端点度数 `4`。
- 独立 CDP 工具只读取环境变量指定的存档与外部冻结 SHA-256，在 no-store 本地服务和独立系统 Chrome 中走正式文件导入，不调用 `newMap`、不连接用户 Chrome、不改输入；三阶段截图只写临时目录，源码不硬编码私人 seed、checksum、大小、数量或 SHA-256。终验非性能 health error、application console、page 与 WebGL error 均为 `0`；观测到的长任务和 operation stall 已归入第 322 项性能基线。第 325 项据此完成归档，第 322～324、326 项继续待执行，`source/` 与 Wiki 未改。
- 观察使另认领用户原 `5410` Chrome 标签，不刷新、不换图、不调用 `newMap`；经页面视觉批准后依次调用正式河流与道路重算，地图 identity / checksum / `100000` grid 与 `43419` pack 保持。河流 `1531 → 1528` 时道路仍为 `607`，再显式重算为 `288` 条，其中海路 `45`；路线面板与桥接分页结果一致。实际选择、持久高亮并定位的一条 `60` cell 道路沿陆地连续，最终全图没有跨世界蜘蛛线；WebGL `glError=0`、`288` 条路线全部渲染、picking 含 `7325` 个 route segments，非性能错误为 `0`。浏览器恢复点未被本轮最终状态覆盖。

## 2026-08-10：登记第 326 项——地点解析、距离与方位查询 API

- 用户要求为 AI 和脚本增加通过地名或 ID 查询两地距离、方位的只读 API。首期地点固定为国家、省份和城镇；名称同名时先按“国家 > 省份 > 城镇”，同类型重名再按稳定升序 ID 选择第一个，同时返回完整候选和选择规则，不用自动首选掩盖歧义。精确调用复用项目统一的 `{kind, id}` 对象引用，避免三类对象编号碰撞；AI 的推荐流程固定为先按名称解析精确引用，再以两端 ID 查询。
- 正式距离不采用浏览器画面像素，而是由地图世界坐标和现有单位 / 比例尺数据计算；浏览器与无头运行时共用纯数据实现。浏览器可额外返回随缩放、DPR 和视口变化的屏幕像素距离作诊断，但不得将其当作地理距离。城镇使用正式坐标，国家优先首都、省份优先省会，缺失时才回退行政中心 / pole，并明确回显代表点来源。
- 方位以地图正北为 `0°`、顺时针递增，提供角度、八方位中文结果和同源距离摘要；Y 轴向下的世界坐标需正确换算，两点重合返回“重合”。浏览器与无头正式结果须在浮点容差内一致，三项 API 均为无副作用并进入 `info.describe`、schema、能力矩阵和 AI 文档。本轮只登记任务与验收边界，不实现方法，不修改地图、存档、运行时或 `source/`；第 325 项仍优先执行，第 322～324、326 项待执行。

## 2026-08-10：第 325 项现场补验——蜘蛛网消失后发现港口拓扑失效

- 河流重算不再调用城镇 / 道路终结器，也不捕获道路锁、推进道路 salt、替换道路三镜像或刷新 route mesh / 全量对象索引；新水文只把道路标记为待派生。全河锁定、取消、锁冲突、渲染后失败、成功后的撤销 / 重做均使用河流域级 no-op、回滚和历史，合法锁仓保持对象 identity，河流 GPU 可从失败态精确回到旧 checksum。
- 新增两阶段原子的城镇空间身份一致化：先完整验证 city 数组槽位、grid / pack 映射、burg 一一对应、ID 类型与 TypedArray 容量，再集中同步 burg cell / 坐标 / cityId 和稳定重建 legacy 镜像。道路显示点只有在 city、burg、端点 cell 与镜像四重身份一致时使用城市坐标，否则使用 pack 中心；split 路线端点不再沿用不属于首尾 cell 的旧 burg。
- 独立动态 A/B/A/B 门中，`173` 条 route、`173` 条 packRoute、`468` 个含路 picking 桶和 `1411` 个道路段的值与引用全部守恒，success / undo / redo 均为 `routeSync=0 / routeAsync=0 / fullObjectIndex=0 / riverPartial=1`。匿名旧档、map migration、真实 `10k → 100k`、fresh `10k / 50k / 100k`、锁、保存往返、生产构建和差异检查通过；真实 `56MB` 导出内存验算的 city / burg 漂移、幽灵镜像、端点身份、逐段邻接、陆海和 teleport 错误均为 `0`，最大显示段 `20.343 world`。
- 用户同一个 `http://127.0.0.1:5410/` 标签页完成第一轮现场验收：河流从 `1531` 重算为 `1528` 条后，道路仍为 `607` 条、总长 `16.5万 km`、资源路线 `361`、海路 `38`；界面明确只替换河流与水文并把下游标记待重算。随后由用户入口显式重算道路，路线收敛为 `243` 条；定位最长的“南衡 → 清海”道路可见其沿相邻陆地连续绕行，整图没有跨世界蜘蛛线，路线列表、定位、选择和高亮正常。现场 console 只记录第 322 项已登记的性能健康信号，没有其它功能错误。
- 该现场结果同时显示海路为 `0`，故没有按“蜘蛛网消失”提前归档。只读复演证明 `949` 个 city / burg 及 `218` 个 port 标记值一致，但 `10k → 100k` 后仅 `4 / 218` 个港口当前 pack cell 仍邻水；河流 salt 16 下大量端点无法进入水体，A* 可返回零海路。第 325 项重新标记执行中，新增当前 haven / 水体 feature / 可航入口的港口拓扑一致化和“同水体双可路由港至少一条海路”门禁。既有旧图安全边界继续有效：加载只诊断并标道路待派生，不静默搬城、清港或改写 x/y/port；真正迁移 / 清理只在显式细分或道路重算事务中发生，保护对象不动且可撤销。`source/`、Wiki 未改，第 322～324 项继续等待。

## 2026-08-10：登记第 325 项——河流重算解耦与 5410 细分存档道路身份修复

- 用户明确要求“重新生成河流”不得直接重新生成道路，并指出当前 5410 的已保存细分地图在显式重算道路后仍产生跨世界蜘蛛网。本轮已接管同一个用户 Chrome 标签页且未刷新、换图或新建地图，从该页导出当前完整数据作只读诊断。
- 现场数据证明 canonical 道路拓扑不是主因：`5799` 个连续 pack 步的断邻为 `0`，但 `949` 个活跃城镇对应 burg 全部仍指向细分前的旧 pack cell，加载后又形成 `792` 个幽灵 `pack.cells.burg`。道路显示点无条件消费这些旧 burg 坐标，造成 `627` 个大于 `25 world` 的瞬移段，P95 `619`、最大 `1223`；“灵云 → 丹庭”本应约 `3 world` 的相邻步被画成约 `944 world`。
- 第 325 项据此冻结两条硬门：河流重算不得触碰道路数据、salt、锁、mesh 或 picking，只把受新水文影响的道路标记为待派生；旧档加载与显式道路重算必须先按当前 city grid / pack 身份修复 burg 和 legacy cell 镜像，再原子生成三套路线镜像。验收必须覆盖匿名旧档、真实 `10k → 100k`、fresh 三档和用户同一个 5410 标签页的 CDP 实测，不能再用路线数量或 fresh 新图自证。第 322～324 项继续待执行，本项完成前不扩入 Worker 化、规模上限或模板系统。

## 2026-08-10：登记第 324 项——内置真实地理与历史场景地图模板系统

- 用户要求新增独立于现有程序化高度模板的地图模板系统：用户选择场景和 cell 数后即可一键创建，canonical 模板几何需确定性投影 / 重采样到目标网格，而不是固定复制模板原始 cell。目标规模统一复用第 323 项的 `1～100000` 规则，模板来源、版本、投影、范围、保护锚点、许可、checksum 与历史年代进入 manifest。
- 首批固定为 16 项：世界、中国、欧洲、北美、南美、非洲、亚洲、东亚、澳洲及巴布亚新几内亚岛与新西兰岛、南极洲、中原、日本本州岛、朝鲜半岛、中东、神圣罗马帝国、以罗马帝国为主要复刻目标的地中海。中国模板必须完整包括藏南、阿克赛钦、台湾、南海诸岛与钓鱼岛；东亚和中原模板按用户给定四至裁剪，关键小岛不得被简化或重采样静默删除。
- 普通地理模板默认提供真实地理底图并继续由生成器派生人文世界；两项历史模板必须内置有明确年代的政治 / 人文场景预设，并先固定单一史料口径。创建结果仍是普通可编辑地图，只保留来源 metadata，不持续覆盖用户编辑；旧存档和未选模板的生成链保持兼容。本轮只登记任务，不新增模板资源、UI、API 或生成逻辑，不修改 `source/`。

## 2026-08-10：登记第 323 项——地图规模统一限制为 1～100k

- 用户要求地图规模最大支持 `100000`，更大的目标数自动按 `100000` 执行；最小目标为 `1`，小于 `1` 的数字自动按 `1` 执行。现状存在两套边界：整图生成与生成 Tab 使用 `1000～100000`，`grid.refine` 及对应 API schema 仍允许到 `200000`，后续统一到同一归一化入口。
- 第 323 项将覆盖生成 Tab、整图生成选项、公开 API / schema / 配方、URL / 预设恢复和网格细分，并要求归一化结果同步回显到 UI、options / metadata、预检与日志。`cellsTarget=1` 必须生成至少一个有限可见单元；依赖多样本的阶段需安全降级，达到 100k 后的更大细分请求明确 no-op。
- 实际网格已超过 100k 的旧存档不能靠静默删 cell 降级，必须在地图替换前明确拒绝并保留当前地图；100k 及以下旧档继续兼容。本轮只登记任务与验收边界，不修改生成器、运行时、API、存档或 `source/`，执行顺序仍以 `docs/current-plan.md` 为准。

## 2026-08-10：完成第 321 项——重生成网络质量与单一 Loading 纠偏

- 路线生成不再把省域 / 海域中的全部端点铺成高密候选网：road 保留首都骨架，trail 强制覆盖合法首都、省会和每个可路由行政组，searoute 按水体成对选港；两类次级路线使用确定性 Kruskal 骨架并最多加入四条局部冗余。分段合并拒绝任何重复 cell 闭环。固定 `10k / 50k / 100k` 的初始、城镇、河流、道路四阶段中，逐段邻接、陆海、重复 cell、镜像、行政覆盖错误均为 `0`，最大端点度数 `3`、最大实际路径 / 弦长比 `5.645`；10k trail / searoute 四阶段最大值由失败基线 `564 / 136` 降为 `206 / 43`。
- 河网显示候选以 canonical 共享汇流 cell 和父河局部进度窗建立唯一锚点，禁止全河近点投影；交点按空间位置与双方进度一对一匹配，并单独识别同河非相邻回折、跨河顶点接触和共线重叠。共享 cell 的同父 sibling 亚像素双尾巴改为替换到单一 junction，near-anchor 提前穿越仍拒绝；候选改形按原路径进度补回 `[x,y,flux]` 第三维。冻结 child / parent、残余质量、无共享 cell、长桥和修复上限都具有明确 partial / rejected 证据。碎片策略只固化到本次非冻结候选；旧图、手工河和锁定河默认显示。三档、多 seed / salt、锁定与浏览器回归的断接、错锚、自交、回头、新增交叉、可见碎片和 flux 异常均为 `0`；浏览器最大汇流 gap `5.684e-14`，977 河候选 apply `156.587ms`。
- `generate.regenerate` 现由异步 operation 先绘制唯一 generation Loading 再执行同步计算；完整地图的新图、导入、浏览器恢复和高度图替换使用统一 owner，从文件读取 / 解析持续覆盖到成功或回滚，operation 继续持有 busy / health 而不显示第二个气泡。成功、未知类型、取消、锁冲突、文件读取、故障回滚、空存档和“普通 operation 气泡已显示超过 420ms 后再点生成”均确认同时可见数不超过 `1` 且结束归零。路线 / 河流连通与锁、API operation、浏览器保存、PNG / GeoJSON、picking、production build、归档和差异检查通过；application / page / active health / WebGL error 为 `0`。整图生成 / 装载仍有 `main-thread-long-task / operation-stall`，已作为第 322 项 Worker 化的真实性能基线，不在本项掩盖。第 321 项已归档，当前只保留第 322 项待执行；未修改用户现有地图或 `source/`，未提交或推送。

## 2026-08-10：登记第 321～322 项——网络质量纠偏与复杂计算 Worker 化

- 用户复查正式环境确认，第 319 项只改海路候选距离排序，仍未约束路线总量、完整路径绕行和逐段陆海合法性；现以第 321 项取代其完成口径，补齐城镇 / 河流 / 道路三条重生成链的稀疏网络与逐段门禁。
- 重生成当前由同步 `runtimeOperation.runSync` 执行，`420ms` 延迟提示的定时器与绘制机会会被主线程计算堵住；完整存档导入又同时驱动 generation 与 operation 两个 Loading。第 321 项统一为重生成先绘制唯一提示再计算，地图替换只显示 generation Loading，仍保留 operation 的 busy、阶段、回滚和健康语义。
- 河流纠偏将从 canonical 共享汇流 cell 构造局部唯一锚点，禁止支流投影到整条父河的任意近点；基础路径与补接段共同接受自交、回头和非父子交叉门，显示层消费确定性碎片隐藏策略，但不改河流 canonical 身份、锁或旧图加载。
- 第 322 项独立登记复杂计算 Worker 化：优先覆盖公开 `generate.regenerate` 的地理要素、路线、河流、城镇、国家、省份、标记、外交、宗教、军事与地区十一类重算及其人口 / 经济下游，并补齐现有整图生成 Worker 返回后的结果复制、正式装载和派生渲染准备；随后处理高度 / 气候 / 洋流、网格、拓扑、水文、寻路和社会派生算法，最后处理大型存档与 CPU mesh / 标签 / picking 构建。主线程保留事务、revision、锁、历史、DOM 和 GPU 提交；必须覆盖确定性、取消、迟到结果拒绝、回滚和安全降级。本项不混入第 321 项实施。

## 2026-08-10：完成第 320 项——浏览器地图保存反馈收敛

- “保存到浏览器”不再在控制面板写入进行中、成功体积摘要或失败文本；旧版遗留的浏览器保存摘要会在下一次保存时被精准清空，不影响其它导入、导出或诊断反馈。成功继续只在页面下方显示“保存成功”。
- 失败改为下方红色 toast 显示“保存到浏览器失败：具体原因”，可见时长由 `2.6s` 提升为 `6s`。真实下拉成功保存和故障注入均在隔离 Chrome 验证，控制面板为空、错误 toast 在 `5.5s` 仍可见并约 `6s` 后关闭，console / page error 为 `0`。
- `regress:api-data-compatibility`、海路候选 Node / Chrome 回归及生产构建通过；存储降级、数据编码、存档格式、自动恢复、云端保存、地图内容、API 和其它操作提示均未改变。全局交互流程审计继续被既有 HF-03 的 ControlPanel `detail: {target}` 证据和快捷键模式 `30 / 29` 基线漂移阻断，本项未扩修。

## 2026-08-10：完成第 319 项——重生成后的海路局部化

- 用户地图截图中的蜘蛛网是海路数据而非 WebGL 渲染故障：路线面板实测为 `168` 条海路，最长两条约 `1.1万 / 1万 km`。根因定位到水域 Urquhart 候选边筛选：代码本应保留较近的 `65%`，却按距离倒序取样，因而系统性丢弃近港口连接并保留跨世界长边。
- 现将水域候选改为距离正序；新增纯候选和真实重生成两条回归，固定纯候选只保留 `1～13` 号最近边。隔离 Chrome 由正式 API 依次执行新图、重生成城镇、重生成河流，海路最长端点跨度占对角线 `6.56% / 9.62% / 6.21%`，低于 `15%` 门禁，route mesh、console、page、WebGL error 均为 `0`。
- 锁定城镇 / 路线的 Node 与静态浏览器回归、10k / 50k / 100k 陆路连通和生产构建均通过。没有改动用户当前线上地图、`source/`、存档、API、河流 / 城镇 / 政治数据或手工锁定路线；当前已保存的蜘蛛网不会被自动写回，待用户明确执行一次“重算道路”或同类重生成时才会按修复后的逻辑替换。

## 2026-08-10：完成第 318 项——边界与河流实验室自包含静态部署

- `e1af257` 已推送 `main`，生产部署切换后直接打开两项实验室：边界页固定夹具 / 候选 / 矩阵为 `20 / 7 / 20`，河网页为 `8 / 3 / 8`；两页 API 已初始化，console / page error 与 `/app/webgl-generator/src/**` 模块请求均为 `0`。线上“没有用例”已消除。
- 两页现由构建阶段各自输出自包含静态 bundle，正式应用运行时代码、地图、存档、公开 API、生成流程与实验室算法没有改动。生产构建、部署回归、新增静态浏览器回归、控制面板实验室导引与归档检查通过；本次启动的临时服务均已关闭。
- 面向读者的实验室说明已收敛为单个“在线预览”链接，不再展开正式路径、历史入口或路由细节；部署实现仍由配置和回归门禁覆盖。`regress:shoreline` 的既有 `lineTriangleCount=11964` 阈值漂移，以及 AI 机器目录四项陈旧文件保持单列，未在本项扩修。`app/`、`source/`、Wiki、地图、存档、API 与生成流程未改。

## 2026-08-09：登记第 318 项——边界与河流实验室自包含静态部署（本地验收完成，待发布）

- 用户发现两项正式实验室在线上没有用例。生产页面诊断确认这不是夹具为空：边界页尝试加载 `/app/webgl-generator/src/renderer/coastline-topology.js`，河网页尝试加载 `/app/webgl-generator/src/generator/index.js`；两者均被 SPA fallback 以 HTML 返回，浏览器在模块 MIME 校验阶段中止，因此 `window.boundaryTopologyLab / window.riverNetworkLab` 没有初始化。
- 本项只调整 prototype 构建装配。`deploy-prototypes` 仍枚举四项实验室，但边界和河网改用独立 Vite 子构建输出自包含 `assets/`；边界页额外保留现有只读几何 vendor。正式应用 bundle、`app/webgl-generator/` 运行时代码、地图、存档、公开 API、生成流程和实验室算法均未修改，实验室页面也不再在生产环境请求 `/app/webgl-generator/src/**`。
- 本地生产构建、静态部署回归和新增浏览器门禁通过：边界页固定夹具 / 候选 / 矩阵为 `20 / 7 / 20`，河网页为 `8 / 3 / 8`；两页 API 均初始化，console / page error 和正式源码模块请求均为 `0`。控制面板实验室导引在 `1280 / 390 / 320px` 继续通过，构建后的正式主 bundle 指纹保持 `index-9hU9Jxqz.js`，本次临时服务已全部关闭。
- 拓扑与河网专项回归继续通过。受复用拓扑函数影响的既有 `regress:shoreline` 则报告 `lineTriangleCount=11964` 低于历史 `25000 / 30000` 阈值；本项没有改 `app/`、`shore-layer`、`surface-correction.js` 或任何正式岸线算法，故保留为独立基线问题，不在本任务扩修。`audit:ai-docs` 仍仅报告既有四份机器目录文件陈旧，未重新生成。当前尚未提交或推送，生产域名仍是旧部署，待发布后再运行同一浏览器门禁并归档第 318 项。

## 2026-08-09：完成第 317 项——正式实验室路径与简介导引

- 用户指定的 WebGL 单元格正式地址现为 `https://fmg.mosuzi.top/prototype/web-cells/`：Vercel 在通用 prototype / SPA fallback 之前，将页面透明改写到 `webgl-cells/index.html`，并把该路径下的相对 `src/`、`data/` 请求改写到同一静态目录；旧的 `/prototype/webgl-cells/` 继续可用。架构总览、逐页 README、AI 路由与部署说明已写入四个绝对生产 URL。
- 控制面板“简介”新增单个“实验室”下拉，菜单依次列出 WebGL 单元格、共享边界拓扑、画卷加载页概念和河流网络算法四项；点击使用 `noopener,noreferrer` 新标签页，不导航当前地图。生产 Chrome 覆盖 1280 / 390 / 320px，菜单完整可见且无横向溢出，四个 URL 和错误面均通过。
- `build:app`、部署回归、静态 alias 页面 / 相对模块 / 固定样本实测、交互面审计、Markdown 链接与差异检查通过。推送 `main` 后，线上四页标题分别命中实验室，`web-cells/src/main.js` 与 `data/sample-map.json` 均为 `200`、样本固定为 `99846` cells。`audit:ai-docs` 仍仅报告既有 `docs/generated/ai` 的四个机器文件陈旧，未扩大本项范围生成它们；全量 UI 系统审计在其无关的大型面板矩阵超时，已停止其临时 Chrome / 5458 服务。`source/`、Wiki、地图、存档、API 与生成流程未改。

## 2026-08-09：完成第 316 项——独立实验室文档、README 与 AI 路由

- 当前 `prototype/` 分母已明确收敛为四项：历史 WebGL cells、共享边界拓扑、画卷加载概念稿与河流网络。新增架构总览逐项说明用途、固定数据、启动 / 部署入口、专项回归、正式应用关系和维护规则；缺失的 WebGL cells README 已补齐，其余三份 README 均回链统一说明与 AI 操作边界。
- 新增 AI 专用路由，要求先判断正式应用还是固定夹具 / 概念稿，禁止用实验室替代用户地图、写入存档、浏览器存储或公开 API；同时冻结拓扑纯函数被正式岸线受控复用、河网页面只读且 `formalGeneratorWrite: false` 仅表示实验室页面不写正式地图。
- 根 README 中英文版、docs 索引、AI 接手入口和 Vercel 部署文档均已加入实验室入口；部署说明补齐河流网络路径和四项生产构建检查，不修改 Wiki 源稿或发布链。
- 11 份受影响 Markdown 相对链接检查、加载画卷 / 边界拓扑 / 河网专项与部署回归、归档检查及 `git diff --check` 通过。`audit:ai-docs` 发现当前源码与既有机器目录四个生成文件不同步；它不由本项文档改动引起，故未在本次限定范围再生成机器目录。未启动或修改用户 Chrome，`source/`、产品代码、构建 / 路由、存档和 API 均未改。

## 2026-08-09：完成第 314～315 项——残余汇流归零与延迟操作反馈

- 第 314 项复现了两类第 313 项未覆盖的声明父子关系：共享终端水文 cell 的真实显示 gap 略高于基础局部容差时仍会 partial，以及小于旧“已接近”阈值的非零 gap 不追加终点。正式候选现只在共享终端水文证据成立时使用既有 `24 world` 上限，并让每个非零 gap 走同源 cubic；若主曲线不安全，只尝试确定性局部弯曲方案且重新执行全部安全门，不靠直线、吸附或补造 parent 绕过约束。
- 固定 `314-residual-50000-0` 的 `#279 → #283` 与第二个 50k 种子均由 partial 修为 accepted。正式隔离 Chrome 的 10k / 50k / 100k 分别全量审计 `26 / 70 / 156` 条声明汇流，最大末端缝隙为 `0 / 1.14e-13 / 1.14e-13`；50k 已知残余关系精确为 `0`。100k picking 命中新曲线，PNG、Feature GeoJSON 和 WebGL error 通过；500+ 长桥、六类安全反例、canonical 河网、冻结河流与旧图保持。
- 第 315 项新增独立于 generation loading 的 token 化延迟操作气泡。共享 runtime operation 只有持续超过 `420ms` 才真正解除 hidden；快速操作不产生动画或闪烁，默认文案为“山河正徐徐推演，请稍候片刻。”，阶段文案可自动补全为“……，请稍候片刻。”。重叠操作、阶段更新、成功、取消、失败与销毁均按 token 清理，生成提示共存时自动错位，窄屏不溢出。
- 隔离 Chrome 验证 `0.8ms` 快操作 animation start 为 `0`，`680ms` 慢操作按阈值显示；取消 / 失败 / generation 共存后气泡均隐藏，`320px` overflow 为 `0`，active health、application console、page 和 WebGL error 为 `0`。河网正式 / 实验室 Node 与 Chrome、控制点 10k / 100k、锁定重生成、存档、API、GeoJSON、operation、文字注册表、生产构建、归档检查与差异检查通过；`source/` 与用户现有地图未改，未 stage、提交或推送。

## 2026-08-09：登记第 314～315 项——残留支流断接与延迟全局操作提示

- 用户提供正式地图截图，细支流仍停在干流旁。多种子正式生成扫描确认第 313 项只覆盖已声明 parent，且共享终端水文 cell 的父子关系仍可能因显示 gap 略高于局部容差而保留为 partial；另有小于“已接近”阈值的关系不追加终点，放大后仍可能露缝。第 314 项只修这两类已声明父子关系，不凭视觉接近补造独立河流 parent，也不放宽 500+ 长桥和六类安全门。
- 用户同时要求任何操作等待一段时间仍未完成时给出文雅 loading。第 315 项新增统一延迟反馈：快速操作不闪，可能跨过门槛的异步 operation 由共享生命周期持有；完成、错误、取消、重叠与迟到回调都必须清理。单段同步长任务继续按性能缺陷处理，不用 loading 掩盖。默认文案冻结为“山河正徐徐推演，请稍候片刻。”。
- 两项均不得修改 `source/`；按 314 → 315 顺序实现，并以固定反例、10k / 50k / 100k、真实 Chrome、错误面和生产构建收口。

## 2026-08-09：完成第 313 项——河流实验室候选接入正式河网生成器

- 河流实验室的 DAG / 汇流曲线 / 水文单调 / 六类安全门已抽到正式共享模块 `generator/river-network-candidate.js`；实验室 `algorithms.js` 只保留薄转发，正式与实验页面不再维护两套算法。
- 正式 `buildRivers` 在 canonical `normalizeRiverNetwork` 之后执行候选阶段。只把通过门禁的显示 `points / length` 与对应 `flux / discharge / width` 合并回原河流；`cells / gridCells / parent / basin / source / mouth`、对象顺序、锁定河流和拒绝关系保持原样。结果摘要写入 `rivers.metadata.networkCandidate`，旧存档加载不自动重写。
- 固定 10k / 50k / 100k 正式生成均为 candidate accepted，canonical 河网逐字段一致；100k 已知关系 `#760 → #5` 用共享三次曲线实际贴合且保持非零曲率，三条显示曲线应用、77 条水文派生更新，宽度 / 流量越级与拓扑问题均为零。500+ 显示长桥仍精确拒绝，冻结河流、旧 / 新存档、GeoJSON、PNG 与确定性门通过。
- 隔离正式 Chrome 的 10k / 100k 地图确认使用正式 metadata 与曲线结果；100k 河流 picking 命中修复对象，PNG / Feature GeoJSON 成功，application console、page、active health 与 WebGL error 为 `0`。生成期长任务约 `3.1s / 5.1s`，明确位于交互窗口之前。
- 发布前顺带收口混合工作树中的城镇严格门：人口点刷新只在交互静默后运行并可被新输入取消；编辑锁 DOM 只在锁签名变化时扫描；路线 typed-array 合并不再制造临时切片数组。连续两轮完整 10k + 100k 的交互长任务均为 `0`，第二轮 100k handler P95 / max `0.1 / 0.1ms`、preview `0.4 / 0.7ms`、提交 `4.7ms`，health / console / page error 为 `0`。
- `regress:river-network-candidate`、正式 candidate Chrome、河网 / 锁 / 控制点 / GeoJSON / API、实验室 Node / Chrome、城镇 Node / Chrome、生产构建、归档检查和差异检查通过；`source/` 与用户 5410 地图未改。按用户要求，完成改动随后按功能分批中文提交，所有批次统一推送 `main`。

## 2026-08-09：登记第 313 项——河流实验室候选接入正式应用并分批发布

- 用户确认第 312 项实验室正例后，明确批准把同一算法应用到正式河网生成器，并要求把当前完成改动按功能分批提交，最后统一推送。
- 第 313 项只在正式新图生成 / 显式河流重生成链接入候选：先完成既有 canonical 河网归一化，再对未冻结河流应用通过安全门的显示汇流曲线与派生流量 / 宽度；权威 cells、父子、流域、源头、河口和锁定快照不变，旧存档加载不自动改图。
- 为防实验室与正式实现再次漂移，候选算法将抽为正式共享模块，实验室只保留薄入口；固定 10k / 50k / 100k、长桥与六类安全反例、锁定河流、存档、导出、正式 Chrome 和错误面构成发布门。通过后按功能显式分批中文提交，全部完成后统一推送 `main`。

## 2026-08-09：完成第 312 项——河流实验室未接入支流可修复正例

- 用户在当前 Chrome 的 5403 河流实验室发现“支流未接入干流”仍显示 rejected。现场读取确认顶部为 `candidateDecision: rejected`，关系 `#2 → #1` 的显示 gap `52/12`，而“证据通过”只表示诊断命中。
- 源码确认主夹具把真正的未接入修复场景写成了超长桥拒绝反例，浏览器测试还固化其 confluence / hydrology 必须 rejected；与此同时 `shared-cell-display-gap` 已独立覆盖共享水文 cell 也不得生成 500+ 长桥。第 312 项只拆清两者职责：主夹具改为局部尺度内可修复正例，超长桥继续拒绝，不放宽算法门禁或接入正式生成器。
- 主夹具现保持 `tributary-unattached` 诊断，显示 gap 从 `52` 收敛为 `16`，仍超过旧固定诊断阈值 `12`，但位于 grid spacing、父段、河宽共同形成的局部容差 `20.595445` 内。候选生成曲率 `1.787951` 的 cubic Hermite / Bézier 汇流段，过冲、自交、折返、水域与新非汇流交叉五门全通过，confluence / hydrology 均 accepted。
- Node 与隔离 Chrome 回归改为要求主夹具 candidate accepted、几何实际变化、页面无拒绝连接段；独立 `shared-cell-display-gap` 继续以 `distance >= 500 / hydrologyDistance = 0` 精确拒绝 `confluence-display-gap`。10k / 50k / 100k 固定生成快照三阶段继续 accepted，100k 旧 baseline 继续 blocked、新 candidate accepted。
- 用户当前 `http://localhost:5403/` 标签页已刷新并重新选中“支流未接入干流”，现场为 `evidenceOk：true / candidateDecision：accepted`，关系显示 `accepted · display-endpoint · gap 16/20.595445`，C / D 均 accepted、拒绝连接段为 `0`。正式生成器、正式地图、API、存档、用户 5410 和 `source/` 未改。

## 2026-08-09：完成第 311 项——河流控制点拖动后持久预览与应用状态

- 用户反馈拖动控制点后橙色河道变化预览不再持续显示，“应用控制点”同时禁用；再新增并删除一个临时点后 Apply 又恢复，说明 working 修改未丢，只是交互状态错误。
- 调用链确认 pointermove 已把 working 更新为新位置，pointerup 会再 stage 一次相同末点；`inspectRiverControlPointAction` 合理地对这一次动作返回 `changed=false`，但 session 直接把该局部值写进 draft。RiverPanel 的 disabled 条件与 selection-layer 的曲线绘制都读取 draft.changed，因此同时失效。第 311 项只把 draft.changed 改为 working 相对 begin baseline 的累计 dirty，并补末帧同坐标反例；几何与正式写入不改。
- `river-waypoint-session.stageAction` 现用 working 相对 begin baseline 的完整指纹重算累计 `changed`，覆盖单次 inspection 的动作局部值；拖动停止后继续复用同一共享三次曲线预览，Apply 立即可用，只有 working 净回到 baseline 才恢复未改状态。
- Node 门覆盖“最后 RAF 已写入、pointerup 同坐标 no-op”反例，并确认变化曲线 mesh 仍多于仅控制点手柄。隔离 Chrome 10k / 100k 均完成 60 / 120 次真实 pointermove：拖动后 draft / renderer preview 保持 changed，橙色变化曲线持续，按钮启用且无需新增 / 删除绕行即可直接应用；预览与正式提交曲线摘要一致，交互长任务、application console、page 和 interaction health error 为 `0`。100k 生成阶段约 `2.8s / 5.1s` 的既有长任务位于交互窗之前并单列。
- `regress:river-control-points`、`regress:river-waypoint-interaction`、`regress:river-control-points-browser`、生产构建和差异检查通过；未改正式河网、API、存档、其它面板、`source/` 或用户 5410 地图，未 stage、提交或推送。
- 用户要求查看的独立河流实验室已在其 Chrome 新标签页打开 `http://127.0.0.1:5403/`；原 5410 地图标签未接管或刷新。

## 2026-08-09：完成第 310 项——城镇连续移动与显式退出

- 用户确认第 309 项城镇移动基本可用，并要求成功提交后继续保持移动模式，以便连续移动同一城镇；退出时机改为点击当前城镇以外的位置或手动点击“退出移动”。
- 提交链不再调用 `completeCanvasToolMode(CITY_MOVE)`；成功后只清理本次 ghost / pending / preview，保留 mode 与 cityId，并把 `36×36 CSS px` 起拖环更新到城镇新坐标。下一次仍可从新位置直接拖动；非当前城镇 pointerdown 会退出模式并继续普通点击选择链，活动按钮文案改为“退出移动城市”。提交失败仍留在模式内并恢复起拖环，允许用户重试。
- Node 专项确认同一绑定可连续写入两条移动历史，pointer capture 分别释放，点击别处进入退出回调；静态门禁止成功提交重新调用 CITY_MOVE complete。隔离 Chrome 10k / 100k 均完成“取消拖动 → 第一次提交后仍 active → 同一城镇第二次提交 → 两条独立历史 → 手动退出 → 重新进入后点击别处退出 → 撤销 / 重做”，两次提交后城市管理 grid / pack 与起拖环都指向最新位置。
- 100k pointer handler P95 / max 为 `0 / 0.1ms`，preview P95 / max 为 `0.3 / 0.5ms`，异步 preflight 最大 `45.9ms`、commit 最大 `16.1ms`；10k / 100k 交互时间窗 `>=50ms` 长任务、active health、console 和 page error 均为 `0`。城镇拾取、对象详情、编辑执行、面板刷新、Grid 细分、生产构建和差异检查通过。生成阶段 `2.724s / 4.760s` 既有长任务位于交互窗之前并单独保留；用户 5410 未接管或写图，`source/` 未改，未 stage、提交或推送。

## 2026-08-09：完成第 309 项——细分 100k 城镇拖动现场修复

- 用户现有 5410 地图是从约 10k cells 拓扑保持式细分到 100k 的地图；此前隔离回归只使用直接生成的 100k，因而漏掉正式拾取器仍以 `row * columns + column` 推导 cell ID 的错误。细分地图保留旧行列元数据并追加子 cell，结果是城市对象可以被拾取，但同一点的 `pickGridCell / pickCellClientPoint` 返回空，移动模式无法建立合法目标。这是此前“脚本通过而现场拖不动”的决定性验收缺口。
- `pickGridCell` 现从附近母 cell 展开 `grid.refinement.children`，先做细分多边形包含判断，边界数值误差时再在该有界候选集内按生成点距离确定性选取；不会扫描全部 100k cells。城镇水陆预检同时以正式画面使用的 `grid.cells.h` 为权威，不再让旧 pack 高度否决细分后的可见陆地。细分回归新增代表子 cell 与全部迁移城市可拾取门，城镇迁移回归新增 grid / pack 水陆不一致的正反例。
- 移动交互继续使用半径 `18 CSS px` 的固定屏幕热区和 `36×36 CSS px` 起拖环；密集冲突按屏幕距离选择最近城镇，同距按稳定 ID。ghost 已完全移除第二个全尺寸 WebGL2 context，只保留单 DOM 合成层；进入模式会确保所选城市可见，未命中时给出反馈。城市管理选中详情在提交后局部更新 grid / pack，不重建 100k 全列表。
- 在用户原 5410 标签页与原地图上，从可见起拖环中心以真实 Windows 鼠标拖到陆地：城市 `#0` 从 `grid 3521 / pack 1660` 提交到 `grid 41503 / pack 19870`，城市管理立即显示新位置且页面保持响应；随后通过应用同一历史事务的公开撤销入口恢复到 `grid 3521 / pack 1660`，并恢复原选择，未刷新或替换用户地图。隔离 10k / 100k 浏览器回归继续通过，100k handler P95 / max 为 `0 / 0.1ms`、preview 为 `0.3 / 0.7ms`、交互长任务为 `0`，交互期 console / health / page error 为空。城镇拾取、迁移、Grid 细分、面板刷新、对象详情、编辑执行、生产构建和差异检查通过；生成期既有长任务不冒充交互结果，`source/` 未改，未 stage、提交或推送。

## 2026-08-09：完成第 308 项——城市对象详情打开城市管理

- 用户要求在选中城市的对象详情中增加“打开城市管理”按钮，并把现有三项动作与新入口排成两行，避免单行拥挤。
- 城市对象详情现显示“定位 / 打开城市管理 / 名称库改名 / 编辑名称”四项；新入口只在 `OBJECT_KIND.CITY` 出现，复用既有选中感知打开链，把当前城市同步到城市管理。城市动作容器固定为两列，四项稳定形成 `2×2`；其它对象继续使用原自适应布局。
- 专项 Node 门确认组件、包装层、runtime 和 CSS 接线；隔离真实 Chrome 在 `1280 / 390 / 320px` 下均测得两行各两个按钮，动作区宽 `294 / 294 / 278px`，按钮宽 `143 / 143 / 135px`，横向溢出和文字裁切均为 `0`。点击后城市管理保持城市 `#0`“晴川镇”为选中项，非城市对象不显示入口，health / console / page / WebGL error 为 `0`。
- 对象详情、面板 overlay、生产构建和差异检查通过。既有 selection-actions 静态门仍要求第 306 / 307 前的 `setSelection({object})` 旧文本，而正式链已使用 `setSelection({object: nextObject})` 支持同点城市循环；该无关旧断言未在本项扩修。城市数据、编辑事务、API、存档、生成算法、`source/` 和用户 5410 均未改，未 stage、提交或推送。

## 2026-08-09：完成第 307 项——城镇移动固定屏幕热区

- 用户确认城镇移动热区必须保持固定的用户视角尺寸，不得随相机或画布缩放；远景密集城镇热区冲突时响应距鼠标最近者。现有实现把所有城镇固定为世界坐标圆形阈值，调用侧只给 `9px`，且城镇图标向锚点上方展开、名称 overlay 不参与命中。5410 当前视图中 28 个可见城镇名称中心距锚点 `13.76～17.47px`，全部超出旧热区。
- 正式 renderer 现以半径 `18 CSS px` 进行城镇屏幕空间拾取；世界索引仅召回候选，最终命中和排序使用投影后的 CSS 距离。热区冲突时选择最近城镇，距离在 `1e-6px` 内相等时按稳定 ID；同一落点只容忍既有轻量 cell 拾取的 `0.1 world` 释放量化误差，普通同 cell 或远景近邻不再被并入循环候选。移动模式继续优先拖动已选的重合对象。
- Node 专项覆盖 `0.5 / 1 / 4 / 10x` 的固定边界、密集最近、等距、同 cell 不同位置、精确 / 量化重合循环；城镇迁移与对象详情兼容门、生产构建和差异检查通过。隔离 Chrome 在 10k / 100k 上覆盖 `0.35 / 1 / 4 / 9x`：`17.5px` 全部命中、`18.5px` 全部不命中；两个锚点相距 `24px` 时选择距鼠标 `10px` 的城镇，同距选择较小 ID。
- 两档真实 `pointerdown` 均从图标锚点外侧 `17.5px` 启动移动，连续 `60` 帧 pointermove 后 Escape 取消；handler P95 / max 分别约 `0.1 / 0.1ms` 与 `0 / 0.1ms`，交互 `>=50ms` 长任务、active health、console、page 和 WebGL error 均为 `0`。用户现有 5410 标签页只做只读可见性检查，未刷新、生成、点击、切换模式或写地图；精确数值以隔离正式 renderer 门禁为准。

## 2026-08-09：完成第 304、305、306 项——河流实验室、河道控制与城镇自由迁移最终通过

- 第 304-F 将“夹具命中”与“候选接受”拆开，阶段失败不再静默回退；semantic checksum 排除运行遥测。304-G 同时约束共享水文关系与真实显示桥接距离，以局部 cell 尺度、河宽和父段长度形成容差；只有真实几何变化才生成 cubic Hermite / Bézier，过冲、自交、折返、水域、新非汇流交叉和保护河口漂移均有固定拒绝反例。10k / 50k / 100k 实验室和浏览器门通过，100k 同快照旧 baseline 仍为 BLOCK、新 candidate 为 accepted。该结论只批准实验候选，不代表已经改动正式河网生成器。
- 第 305 项移除 `RIVER_EDIT_WAYPOINT` 的旧 `session.stage(packCell)` 输入支路，只保留 `stageAction / working.controlPoints`；控制点以精确世界坐标和稳定 ID 工作，cell 只作派生兼容。共享向心 Catmull–Rom → 分段 cubic Bézier 及可视子段 provenance 已接入预览、正式 renderer、picking、长度、GeoJSON 和存档；点击新预览曲线能映射到正确原始河段。RAF 合并、pointerup 末点、双击删除、水域可见和异常释放 capture 均有故障注入与浏览器证据。
- 第 306 项把每帧完整路线预检拆为 O(1) 快速 inspector + 独立 WebGL 单实例 ghost；pointerup 只启动一次可取消、可让出的完整 preflight，提交复用同一不可变结果。新增同 cell 多值 city / burg 索引、稳定 singular 代表和 city→routes 邻接，覆盖加载、锁、查询、省会、picking、Grid 细分、软删和历史；完全重合对象可循环选择并再次拖动。路线、人口点、route mesh 与 selection draw 均改为局部或切片刷新，selected-only 不再误启全图动画。
- 城镇产品规则已落盘：人工移动除水域与结构损坏外不拒绝；允许同 cell 多城；跨省 / 跨国同步归属并重选原省 / 原国首府。目标已有首府时不篡位，缺首府时确定性接纳迁入对象；原国家无候选时允许 `capital=0` 但保持合法本国陆地 center，省份可 `burg=0` 且保持合法 center。港口失效清除，路线无法重寻时事务内删除并警告，同 cell 多市场保留各中心并以稳定代表维持旧单值索引、标记下游 stale。政治扩张、重生成锁、国家画笔与空国合并均已统一这些不变量。
- 独立终审连续两轮完整 10k + 100k：四档交互窗口 `>=50ms` 长任务均为 `0`；100k pointer handler P95 / max 为 `0.1 / 0.1ms`，ghost preview 为 `0.4 / 0.9ms`，异步 preflight `56.1ms`，commit `28.1ms`，迟到 health / console / page error 为 `0`。河流控制点 10k / 100k 的 60 / 120 次 move P95 / max 均约 `0.1 / 0.1ms`，交互长任务与四类应用错误为 `0`。
- 用户现有 `http://127.0.0.1:5410/?debug=1` 标签页没有刷新或生成新图。既有 100k 地图上把首都跨境拖入已有城镇所在 cell 后成功提交，再用撤销恢复原坐标；河流草稿完成新增、拖弯、在新预览曲线上继续新增和双击删除，最后未应用退出。验收后选中对象 / 编辑对象均为 none，WebGL error 为 `0`；健康列表与操作前逐条一致，只保留地图生成阶段原有事件，没有新增交互错误。
- `regress:river-network-lab`、`regress:river-network-lab-browser`、河流 cubic / 控制点 / waypoint / picking / GeoJSON、城镇迁移 / 浏览器、国家生命周期 / 拓扑、Grid / API / 浏览器存储 / 锁 / 路线兼容、生产构建、归档检查和 `git diff --check` 均通过。未修改 `source/`，未 stage、提交或推送。

## 2026-08-08：重新调查河流控制点、城镇拖动与河流实验室——登记第 304-F～G、305、306 项

- 用户在现有 `http://127.0.0.1:5410/?debug=1` 标签页复验推翻第 302、303 项交互验收。精确接管该标签页时，城镇移动失败后 `body` 仍为 `editing-locked`，全局工具栏被禁用，控制台已有长任务 / 帧间隙和三条读取 `undefined.length` 的未捕获异常；按 Escape 可解除锁，证明失败现场残留的工具锁可恢复，但尚无干净触发与完整 stack，不能把异常强归因某一行。本轮没有刷新、生成、提交移动或改变用户地图。
- 城镇静态链确认 pointerdown / 每个 pointermove 都同步执行完整 `inspectCityMove`，其中扫描全路线并对关联路线执行 A*，同时使 CityPanel 全列表指标 / 筛选 / 排序重算；pointerup、`isNoop`、`apply` 又重复同一预检，提交后宽泛刷新。一次性只读 100k Node 探索观察到 4 条关联陆路远目标单次预检约 `153.6ms`，即使无关联路线 execute / undo / redo 也约 `146 / 110 / 95ms`；该数字尚无固化脚本、seed、预热和样本口径，只用于热点定位，正式预算仍以新增 benchmark 与浏览器真实指针为准。
- 用户覆盖旧城镇规则：人工移动除水域和结构损坏外不受生成均衡限制，允许同 cell 多城；省会迁出后原省重选，城镇跨国后归目标国家，首都迁出后原国家重选。第 306 项登记 RAF 快速预览、可取消异步预检、immutable preflight、city / burg 多值索引、兼容代表、行政角色重算和异常 finally 清理；港口 / 市场 / 路线派生不得反向否决合法陆地移动，但迁入角色、无候选政治结构、同 cell 多市场和具体降级策略尚未获用户逐项确认。
- 5410 河流控制点现场出现可见旧候选菱形，但面板仍显示控制点 `0` 个；未点击应用并退出模式。静态链确认新 `stageAction / working.controlPoints` 与旧 `packCell / session.stage / legacy draft` 同时接收输入，自由位置和新预览河段会回落旧链，导致新增、拖动和双击删除状态分裂；预览画折线，正式 renderer 才另做 Chaikin。第 305 项登记唯一输入链、精确世界坐标、RAF 手势、稳定双击和向心参数化 Catmull–Rom 转分段三次 Bézier的共享几何。
- 第 304 项继续 BLOCK，但纠正原决策门证据：`pnpm run regress:river-network-lab` 的 `ok: true` 没有断言真实候选接受，公开 seed `304-river-network-lab-100000` 的汇流候选因 `16.568 > 12` 实际拒绝，后续水文阶段却回退原始河流并报告接受；正式摘要 checksum 混入 `timing / buildMs`，剔除遥测后三档 semantic digest 与 `networkChecksum` 稳定；原正式页长任务是候选未接入时的 baseline，不能归因候选。新增 304-F 精确验收语义和 304-G 纯 cubic 原语汇流候选 / 新浏览器决策门。
- 调查、根因、优化阶段、兼容与验收矩阵已写入 `docs/task-notes/task-304-306-river-city-reinvestigation.md`，并同步 `current-plan`、城镇规则、302 / 303 / 304 专题和接手说明。本轮没有修改产品代码、`source/`、地图、存档、schema 或 API，也没有提交 / 推送；工作树原有未提交差异保持原样。

## 2026-08-08：完成第304-E隔离浏览器决策门——结论 BLOCK

- 独立实验室 `http://127.0.0.1:5403/` 在隔离浏览器中完成真实页面验收：八类固定夹具全部命中，B/C/D 候选卡可见，页面明确显示只读边界；发现并修正候选拓扑序卡片的只读显示缺口，未引入正式应用依赖。
- 隔离正式页面 `http://127.0.0.1:5410/?debug=1` 的10k基线为 `1` canvas、`99` SVG、无 page / WebGL / console error；100k 隔离对照完成真实地图生成和截图，稳定后为 `1` canvas、`501` SVG，生成到稳定约 `15.3s`。
- 100k 生成后健康日志出现 `7` 个 `main-thread-long-task` 和 `2` 个 `render-frame-gap`，其中 `2` 个以 health error 捕获；固定 seed 的正式摘要 checksum 两次生成不稳定。用户原始 Chrome 标签页仍是精确 `http://127.0.0.1:5410/?debug=1`，未被接管、刷新或替换。
- 因为候选尚未在正式 generator 中证明改善截图类别，且性能 / 确定性硬门未通过，304-E 结论为 **BLOCK**：不把任何候选接入 `rivers.js`，不修改正式地图、存档、schema、API 或 `source/`。后续若要正式接入，必须由新的明确决策开封闭实施项。

## 2026-08-08：完成第304-D——流量、宽度与碎线视觉候选

- 实验室新增按父子拓扑序逆序累计输入流量的内存候选，并仅在接收河局部抬升 `discharge / flux / width`，不使用全局加粗掩盖断接；支流超过干流的固定夹具在候选后流量 / 宽度越级均归零。
- 碎线只输出可回滚的视觉策略：极短极细孤立线为 `hide-visual-only`，有合法汇流锚点的短支流为 `extend-to-confluence`，湖泊 / 入海 / 边界出口为 `preserve-protected-outlet`，其余保留。没有删除或覆盖任何河流对象。
- 固定夹具和10k / 50k / 100k seed 的水文指标重跑稳定，原始快照不变；本子任务仍未修改正式 `rivers.js`、地图、存档、历史、API、用户 Chrome 或 `source/`。下一封闭子任务为304-E，负责隔离 Chrome 真实视觉和正式接入决策门。

## 2026-08-08：完成第304-C——河流汇流锚点候选

- 实验室在 `src/algorithms.js` 增加父河折线最近点汇流锚点分析：合法父子关系只在固定 `12` 世界单位容差内生成候选末端，超出容差的支流直接拒绝，不用整体吸附掩盖断接。
- 湖泊、入海和边界出口被列为保护出口，不参与整体吸附；没有父子关系的交叉仍交给几何审计，未被误当作汇流。固定合法汇流、未贴合汇流、非汇流交叉和保护出口夹具，以及10k / 50k / 100k固定 seed 均通过，原始快照未改变。
- 304-C 仍只返回内存候选，未改正式 `river.points / cells`、地图、存档、历史、公开 API、用户 Chrome 或 `source/`；下一封闭子任务为304-D，只在实验室内比较流量 / 宽度单调性与碎线视觉策略。

## 2026-08-08：完成第304-B——河流父子图 DAG 候选

- 实验室新增 `src/algorithms.js`：按稳定 river id 建立父子图，以 visited 集合和确定性 Kahn 拓扑序检查父关系；缺失父级、自指和 A→B→A 关系环直接拒绝候选，不把删除关系伪装成修复。
- 固定夹具回归确认循环夹具被拒绝，合法汇流和湖泊进出水得到可解释拓扑序，非汇流交叉仍由几何审计单独报告；10k / 50k / 100k 固定 seed 的 visited、cycles、missing、self 指标重跑稳定。
- 304-B 候选只复制内存快照并展示拓扑证据，不改正式 `parent`、`cells`、`points`、地图、存档、历史、公开 API、用户 Chrome 或 `source/`；下一封闭子任务为304-C，只在实验室内处理汇流锚点和路径连续性。

## 2026-08-08：完成第304-A——河流网络实验室壳体与只读审计

- 新增独立 `prototype/river-network-lab/`，提供八类中文固定夹具、只读河网视图、结构化 issue 列表和夹具覆盖矩阵；页面不接管用户当前 `5410` 标签页，也没有地图、存档、历史或 API 写入口。
- `src/audit.js` 只在内存快照上检查短细碎片、重复 cell、父关系环、父河缺失、支流未贴合、支流流量 / 宽度越级、合法汇流、非汇流交叉，以及湖泊 / 边界出口观察项；`snapshotGeneratedMap` 只复制河流字段和摘要元数据。
- `regress:river-network-lab` 对八类固定夹具全部命中预期 issue ID，并在固定 seed 的 10k / 50k / 100k 上确认河网快照、诊断列表和 `networkChecksum` 重跑稳定，原始快照不变。正式地图摘要 checksum 两次生成出现变化，仅作为上下文记录，不把它误当作304-A的网络确定性证据，留给304-E统一核查。
- 本子任务没有修改正式 `rivers.js`、河网输出、地图数据、存档 schema、公开 API、用户 Chrome 或 `source/`；下一封闭子任务为304-B，只在实验室内比较流向 DAG、父子关系和循环候选。

## 2026-08-08：登记第304项——河流网络算法实验室与生成质量修复

- 根据当前地图截图和只读生成审计登记第 304 项。本轮观察到孤立细线、支流没有实际贴合干流、支流流量 / 宽度可能超过接收干流，以及疑似无效河流循环或非汇流交叉；合法河网分叉、湖泊溢流和河口相交不能被误判为错误。
- 现有 `rivers.js` 的流向阶段按高度处理并在 `flowDown` 中依据到达时的部分流量建立 `parent`，河流对象随后在 `defineRivers` 中先生成独立 meander 点和宽度，`normalizeRiverNetwork` 主要负责父子关系、连通性、出口和循环门禁；这三层之间没有统一的汇流锚点、累计流量回算和可见碎线策略。
- 固定种子只读矩阵提供了分层证据：30 个 10k / 50k 案例共 8087 条河流，探索性扫描发现 7752 条低于当前试验性细线阈值、147 条支流宽度高于父河、134 处原始折线相交；相交样本包含合法父子汇流，不能直接当作缺陷。该批样本未复现父关系循环或折线自交，因此截图右上角的循环仍是待分类问题，不能声称已经定位或修复。
- 实验室方案拆为 304-A～304-E：先建固定夹具和只读诊断，再分别比较流向 DAG / 循环、汇流几何、流量与宽度策略，最后用真实种子和隔离 Chrome 做正式接入决策。本轮不改正式河流生成算法、用户当前标签页、地图数据、存档、schema、API 或 `source/`。

## 2026-08-08：完成权威任务第303项——河流多控制点自由编辑

- 303-A～303-D 已完成：新增可选 `river.controlPoints` 稳定控制点集合，允许同一 pack cell 多点；集合 session 支持新增 / 移动 / 删除预览，renderer selection / picking 显示整条河与控制点，确认通过一个 `createEditRiverControlPointsCommand` 写入 `points / controlPoints / length`，撤销 / 重做和旧单点路径保持兼容。
- 303-E 真实隔离 Chrome 回归通过：10k / 100k 均完成新增、已有点拖动、双击删除、退出取消、重新进入提交、Ctrl+Z / Ctrl+Y；同一 pack cell 双点由数据 / 事务回归确认，真实 Chrome 选择不被面板遮挡的河段完成多控制点操作。旧 `1280 / 390 / 320px` 河流面板、selection / picking、完整导出、PNG / 高度图、API JSON / gzip 往返、旧数据兼容和生产构建通过。
- 发现并修复控制点模式的输入竞态：全局编辑锁曾拦截 pointerdown，渲染器又在 pointerup 先触发普通对象选中，城市 / overlay 命中会以 `target-switch` 取消河流模式。现明确允许河流控制点模式接收画布输入，并在该模式内抑制普通选中；新增 pointerup 抑制门避免单击新增与普通选中重复。
- 控制点交互期间 health error、application console error、page error 和 WebGL error 为 `0`；生成阶段长任务仍为已知基线（10k 约 `1.05s`，100k 约 `2.20s` / `4.55s`），不在本项扩大优化。`source/`、用户当前 Chrome 标签页和用户地图未改。

## 2026-08-08：完成第303项只读调查并冻结实施方案

- 只读复核确认当前 `river.points` 同时承担生成后的显示折线和视觉控制点写入目标，`inspectRiverVisualWaypoint` / `createAddRiverVisualWaypointCommand` 仍以单个 `packCell` 和单个 draft 为中心；同一个 cell 的第二个点会被坐标重复门禁拒绝，已有点没有拖动 / 双击删除路径。
- 隔离系统 Chrome 的现有河流回归在 `1280 / 390 / 320px` 通过，普通 application console、page、health 和 WebGL 错误均为 `0`；这证明旧单点契约稳定，但不把旧回归误记为多控制点能力。
- 冻结第 303 项为 303-A～303-E：可选 `river.controlPoints` 数据契约、预览交互、河流 / 控制点视觉与 picking、事务 / 导出 / 历史兼容、10k / 100k 隔离 Chrome 最终验收。明确保留旧 `points / cells / gridCells / parent / basin / source / mouth / flux / discharge`，不修改 `source/`，不重写河流生成算法，不接管用户当前 Chrome 标签页。

## 2026-08-08：完成第303-A子任务——河流控制点数据契约

- 新增可选 `river.controlPoints` 规范化层。每个控制点保存稳定 `id`、`x / y`、对应成品折线 `pointIndex`，并把 `packCell` 作为允许重复的派生定位引用；同一 cell 的多个坐标不再因 cell 唯一性互相拒绝。
- 地图存档归一化和河流对象 resolver 会校验并复制该可选集合；旧河流没有字段时继续按原 `river.points` 显示和导出，不静默把生成器 meander 点迁移为用户控制点。`source/`、水文 cells、河网关系和生成算法未改。
- `regress:river-control-points-data` 与既有单点河流回归通过，覆盖稳定 ID、同 cell 双点、索引重排和缺字段兼容。

## 2026-08-08：完成第303-B子任务——河流控制点预览交互

- session 扩展为基于当前工作副本的 `add / move / delete` 集合操作；每次操作只改变预览，确认前不写正式河流和历史。新增控制点仍允许共享 pack cell，已有点按稳定 ID 识别，河源 / 河口端点受保护。
- 新增独立河流控制点 pointer capture：点击非控制点新增预览，已有点拖动实时预览，双击已有点预览删除；无移动点击、pointercancel、Escape、换图和模式退出均不产生半条河流或额外历史。
- `createEditRiverControlPointsCommand` 已可把当前集合一次写入并完整撤销 / 重做；`regress:river-control-points`、既有单点交互和三档河流面板 Chrome 回归通过。正式集合节点 / 控制点 picking 留给 303-C。

## 2026-08-08：完成第303-C～D子任务——控制点视觉命中与单事务历史

- 编辑模式进入时保留整条河和当前 `controlPoints` 的基线预览；新增 / 移动 / 删除草稿绘制完整预览折线和节点。控制点节点复用选择层 depth 约束，不改变正式水文河道。
- renderer picking 增加只读 `riverControlPoint` 返回值，包含河流 ID、稳定控制点 ID、折线索引和可重复 pack cell；普通河流 selection 仍由 `river` 返回。
- `createEditRiverControlPointsCommand` 对当前 working state 单事务写入 `points / controlPoints / length`，完整保存控制点字段存在性并支持撤销 / 重做。focused mesh / picking、数据 / 历史、构建、selection 和既有三档河流面板 Chrome 回归通过；最终 10k / 100k 真实 pointer、导出和兼容门禁留在 303-E。

## 2026-08-08：完成权威任务第 302 项——城镇手动移位完整闭环

- 只读审计确认既有 `city:move`、`inspectCityMove`、`createMoveCityCommand` 和 `bindCityRelocationDrag` 已覆盖本项冻结的目标规则：普通城镇归属跟随，港城保留 / 改港 / 失效清除，首都不得跨国，省会不得跨省，市场中心保持镜像，陆路局部重寻，失效海路在预检中明示删除，写入失败整单快照恢复。既有事务没有新增必填 schema 字段，旧存档继续兼容。
- 第302核心回归的一个静态断言落后于统一画布工具管理器：Escape 已由活动模式通用取消，不再直接写出 `CITY_MOVE`。回归改为锁定通用 `activeModeId` 取消入口，并确认 `CITY_MOVE` 注册仍存在；没有改运行时 Escape 语义。
- 新增 `tools/webgl-generator-city-relocation-browser-regression.mjs` 及 `regress:city-relocation-browser`，用隔离系统 Chrome 对真实面板动作和地图指针完成 10k / 100k 的拖动预览、Escape 取消、重新进入、提交、selection / picking、Ctrl+Z / Ctrl+Y。10k 实际 `10004 / 5968`，港城 #20 预检显示港口失效和两条海路删除警告；100k 实际 `99846 / 63405`，城镇 #21 预览与提交通过。
- 取消阶段城市位置、grid / pack 占位、checksum、revision 和历史不变；确认各只增加一条历史，撤销 / 重做恢复准确。`regress:city-relocation`、隔离 Chrome 回归、API 往返、完整导出、PNG 选项和旧数据兼容回归通过；application console error、page error 和 WebGL error 为 `0`，`source/` 未改。
- 100k 隔离操作仍观测到约 `0.93s` 的 `input-delay`，生成 / 重载阶段长任务约 `2.45s`、`4.50s`。这是后续性能专题证据，不在第302越界优化；本项按事务和兼容性边界完成并归档。

## 2026-08-08：纠正第 301-E 高度面板高亮被视图同步覆盖

- 用户实测确认高度面板的“抬升 / 降低 / 平滑 / 整平 / 扰动 / 填充 / 线段”和“全部 / 仅陆地 / 仅水域”仍会出现选中高亮丢失。精确 `5410/?debug=1` 标签页的 CDP 证据是：选中按钮 `aria-pressed="true"`，但 `class` 为空、计算颜色仍为普通灰色；同时当前页面注入的 `.height-action-group button.active` CSS 规则存在，因此不是缺少 CSS 或禁用态颜色的问题。
- 根因是旧的全局 `setActiveModeButton()`、视图模式绑定、视图模式可用值扫描和偏好恢复都使用 `[data-mode]`，把高度动作 / 作用范围按钮与视图模式桥混在同一个集合里。视图模式同步到 `height` 后会清除高度按钮的 `active` class，却不会清除 Vue 的 `aria-pressed`，造成 DOM 语义与视觉状态分离。
- 修复方案是集中定义 `.ui-segmented-mode-bridge[data-mode]` 视图模式选择器，仅让视图模式绑定、选中同步、可用模式校验和偏好恢复使用该选择器；编辑锁仍保留全量 `[data-mode]`，不改变高度按钮在其它编辑模式下的禁用边界。高度按钮自身、地图数据、schema、存档、历史、公开 API 和 `source/` 不变。
- 高度专项浏览器回归新增真实 `layers.setViewMode("height")` 反例，并同时断言 `aria-pressed`、`.active`、计算颜色 `rgb(255, 224, 161)` 和唯一视图模式。隔离生产 Chrome 的实际 `10004 / 99846` cells 两档均通过，动作 / 范围在视图同步、停止再启用、面板重开和刷新链路保持；原有高度命令、光标、撤销 / 重做及 console、page、health、WebGL 门禁继续通过。
- 当前用户精确标签页只做 CDP 只读核对，URL 未变，未刷新、未导航、未点击或写入地图；热更新残留的运行时全局仍为空，因此不把该标签页宣称为完整交互验收。回滚只需撤销视图模式选择器收窄和新增断言，不涉及地图或存档回滚。

## 2026-08-08：完成第 301-E 补充验收——高度面板状态、选区高亮与画笔偏好

- 复核确认上一轮 E 的“保留运行时选区、清空 renderer 缓冲后重新进入模式”只覆盖合成状态，没有覆盖真实面板锁定选区和当前画笔作用范围；用户实测高亮仍丢失是有效反例。
- 进入 `height:brush` 时现在同时恢复 `heightEdit.terrainSelection` 的 cell / 羽化权重、主动调度 brush cursor；debug 动作与作用范围按钮补齐 `.active` / `aria-pressed`，并为普通分段按钮与 Element Plus 图标按钮补充“选中 + disabled”样式。
- 高度画笔大小新增全局用户偏好 `webgl-generator-height-editor-preferences-v1`，没有偏好时继续为 `28`，修改后跨面板关闭 / 重开、模式重入和页面刷新保持；不进入地图数据、schema、存档和历史。
- 隔离生产 Chrome 真实 10k / 100k 回归实际点击动作、作用范围和覆盖锁定，形成 `3327 / 39862` 个选区 cells；清空 renderer 缓冲后重入高度模式均恢复，高度动作 / 作用范围 `aria-pressed`、画笔 `42`、光标半径 `42` 和刷新恢复均通过。两档 checksum、撤销 / 重做、console、page、health、WebGL 错误门禁通过；`source/` 零改动。

## 2026-08-08：补充第 301-E 验收条件并登记第 302、303 项

- 用户补充发现：启用高度编辑后，高度面板中的多个当前选中按钮高亮会丢失；当前源码虽已有部分 `.active` / `aria-pressed`，因此后续必须先区分运行时状态丢失、DOM 语义丢失和“选中 + 禁用”视觉样式被普通 disabled 覆盖，不能只改一个颜色规则。
- 第 301-E 追加两条封闭验收：高度面板所有带当前状态的按钮在启用 / 退出 / 重开 / 刷新后保持选中语义，并为选中且禁用提供独立视觉；全局高度画笔大小首次无偏好仍为 `28`，修改后跨面板重开、模式重入和刷新记忆，且不写地图 schema、存档或历史。
- 登记第 302 项“城镇手动移位完整闭环”：以既有 `city:move`、城市迁移规则和拖动绑定为基础，冻结目标预览、行政 / 港口 / 路线引用同步、单事务和旧存档验收；登记阶段不实现代码。
- 登记第 303 项“河流多控制点自由编辑”：现有河流控制点命令、session 和 renderer 都以单个 `packCell` / 单个 preview 为中心，后续改为稳定控制点集合，允许同一 cell 多点，并冻结高亮、拖动、新增、双击删除、事务预览和兼容边界；登记阶段不实现代码。

## 2026-08-08：启动权威任务第 301 项——纠正高度笔刷停手卡顿调查

- 用户指出第 300 项报告的 `3～4ms` 与真实体感不符：连续涂抹停手后整画布仍会卡 `500～1000ms`。复核确认第 300 项 trace 在 pointerup 的末点补刷之后才启动，漏掉了完整停手链。
- 当前精确 5410 标签页已通过 Chrome 核对；真实连续长路径触发了新的 `input-delay`、`main-thread-long-task`、`render-frame-gap`，阻塞期间撤销按钮输入也曾等待超时，随后样本已撤销恢复。没有刷新、重生成或覆盖用户地图。
- 代码审计另发现高度空间索引和 Grid→Pack 映射均为首次使用时惰性构建，可能发生在第 300 项 trace 之外；当前 pointerup 仍在提交阶段执行一次最终 `renderer.draw()`。第 301 项先补完整生命周期 telemetry，再按证据决定是否收敛最终 draw 或预热索引，不在调查阶段扩大为全图派生重建。
- 隔离生产 100k 页进一步固定到真实岸线 cell 后，`292～314` 个受影响 cell 的 pointerup 实测 `6969～7178ms`；其中 `refreshHeightCells` 为 `6951.7～7161.8ms`，返回 `incremental=false, reason=shore-or-land-water-change`，并观察到 `6968～7177ms` 主线程长任务和同等帧间隙。提交使用 `HEIGHT_SURFACE_ONLY` 未延迟拓扑，`refreshHeightCells` 对任一 shore cell 直接进入 `rebuildCellVisualMesh → rebuildShoreVisualCache → refreshCellSurface`，这正是“停手后整画布卡住”的已证实根因；不是单独 cell 写入或 Map 查找。

## 2026-08-08：第 301 项实施同侧岸线局部刷新

- 收紧 `refreshHeightCells` 的完整拓扑重建条件：默认高度面板同水陆侧的岸线 cell 不再因为命中岸线集合而全图重建，只有实际 `storedSide !== currentSide` 才进入 `rebuildCellVisualMesh → rebuildShoreVisualCache`。
- 为岸线 surface correction / cover 建立四组 cell → GPU buffer span 索引，按正式 `buildShoreSurfaceDrawPacket` 命令顺序复用既有顶点布局；同侧变化只更新受影响颜色并执行局部 `bufferSubData`，没有改变 grid / pack 高度、schema、存档、API、历史或派生 stale 语义。
- 隔离生产 100k 真实岸线样本从旧版 pointerup 约 `7.5s` 降至约 `22ms`；正式 10k / 100k 浏览器回归的岸线样本均断言 `incremental=true`、停手小于 `500ms`，console、page、health、WebGL 错误为 `0`，撤销 / 重做通过。
- 故意跨海平面的反例仍固定为已知边界：`21` 个水陆侧变化 cell 的 pointerup 约 `7207ms`，其中 `rebuildCellVisualMesh` 约 `2325ms`、`rebuildShoreVisualCache` 约 `4377ms`；不在本轮用陈旧几何跳过该安全刷新，后续若实施需单独设计可观察、可取消且视觉正确的局部拓扑任务。

## 2026-08-08：第 301-C 完成跨水陆拓扑局部刷新

- 跨水陆变化改为按 changed cell 的邻接闭包局部重建 cell mesh；岸线先生成当前完整 source edge 分组，再按 source key 复用未变化 path，只对变化 path 建立局部拓扑快照。局部安全边界、保护对象过滤或重建数量超过上限时返回空结果，由 renderer 使用完整重建回退。
- surface span 长度稳定时只更新受影响的 GPU buffer 区间；拓扑使 span 长度变化时使用独立 surface patch buffer 覆盖受影响 cells，并保持 correction / cover 颜色更新，避免在 pointerup 重新上传全图基础 surface。完整 surface 刷新仍会清理 patch 并重建修正层，兼容边界保持明确。
- 隔离生产 Chrome 的 100k 跨水陆样本核心刷新约 `144ms`、pointerup 墙钟约 `192ms`；局部与完整重绘的 path、mesh、岸线边集合和 `readPixels` 对照全部一致，像素差为 `0`。正式 10k / 100k 专项回归、撤销 / 重做、console、page、health、WebGL 门禁通过。
- 本子任务不改变地图数据、schema、存档、公开 API、历史事务或 `source/`；第 301-D 继续处理首次空间索引 / Grid→Pack 映射构建，第 301-E 继续处理高度编辑作用范围高亮与拾取。

## 2026-08-08：登记第 301-E 项——高度编辑作用范围高亮与拾取

- 用户补充发现：开启高度编辑后，原有作用范围高亮会被取消，因而看不出当前高度编辑作用范围，范围内 cell 也无法按预期选中。
- 本轮只登记问题，不修改代码和用户当前地图。后续需检查高度编辑模式切换时 overlay / picking 的生命周期、显隐和优先级，保证当前 scope、半径、高亮和 cell 反馈保持一致。

## 2026-08-08：第 301-D 完成高度编辑首次索引预热

- 进入 `height:brush` 模式时同步预热高度空间索引和 Grid→Pack 映射，后续高度笔刷查询直接复用缓存；预热结果写入运行时 `heightEdit.lastCacheWarmup`，不进入地图存档。
- 100k 实际 `99846` cells 的预热总耗时约 `14.5ms`，其中空间索引 `9.5ms`、Grid→Pack 映射 `4.9ms`；10k 预热约 `5.1ms`。这笔一次性成本从 pointerup 移出，查询结果和高度命令语义不变。
- 真实 10k / 100k 浏览器专项回归、跨水陆局部拓扑、撤销 / 重做及 console、page、health、WebGL 错误门禁通过；随后由第 301-E 接续处理高度编辑作用范围高亮与拾取生命周期。

## 2026-08-08：第 301-E 完成高度编辑作用范围高亮与拾取恢复

- 高度编辑的权威作用范围仍由 `heightEdit.terrainSelection` 保存；进入 `height:brush` 时，在主题切换后将当前 cell ids 和羽化权重重新写入 renderer 的高度选区 GPU 缓冲，并补一次视觉绘制，修复“状态存在但高亮被清空”的生命周期断裂。
- 专项回归故意在真实模式入口前清空 renderer 选区缓冲、保留运行时选区，再通过实际 `canvasToolModes.enter("height:brush")` 验证恢复；10k / 100k 均恢复已有高亮 cell，并保持普通悬停、brush cursor、cell picking、选择事务和撤销 / 重做。
- 10k / 100k 笔刷、岸线、跨水陆局部拓扑与错误门禁通过；console、page、health、WebGL 错误均为 `0`。本子任务不改变地图数据、schema、存档、公开 API、历史事务或 `source/`，第 301 项进入统一验收。

# 2026-08-11：第 322 项阶段 0 盘点与阶段 A 止损

- 当前任务分支仍与 `main` 同基线，未提交树按阶段 A、B、C、D 分类记录在本地 `work/task322-stage0-checkpoint.md`；旧 10k 通过证据早于后续 renderer 改动，不能证明当前树。
- 100k 真实链已把剩余主线程成本归因为末端 overlay style / layout 与 `updateLabels`，Worker 单包、解码、GPU、commit 和 UI 均低于 50ms。未命中的 routes DOM 复用实验撤回，不在阶段 A 扩为 keyed DOM 事务。
- 阶段 A 启用最多两条、每条不超过 75ms 且必须完整归因的过渡预算；阶段 D 重新收紧标签 / DOM / picking，阶段 E 不继承该预算。后续四级流程只在阶段冻结 checkpoint 使用，不再逐夹具循环。

## 2026-08-11：第 322 项阶段 B2 完成整图生成与初始渲染 Worker 闭环

- 新增 `generation.compute`，在 Worker 内完成整图生成、结果编码与初始 render preparation；主线程复用既有 coordinator、prepared installer 和 map replace 事务，正式路径不再重复执行 legacy CPU renderer load，兼容降级路径保持。
- 100k 初次接入暴露的末端 GPU continuation 与完整 overlay 首次布局长任务，分别通过安装完成后的明确浏览器让步，以及每批 `32` 个节点的隐藏 wrapper 逐批展开收敛；最终 overlay 恢复原直接子节点结构，取消、迟到和回滚仍由同一 prepared transaction 持有。
- `webgl-generator-prepared-render-installer-regression` 与复合 Worker 专项通过，后者覆盖 generation Worker / fallback、取消、迟到和 `99846` grid cells 大图。生产构建通过；真实 Chrome 的 10k / 100k 分别为 `10004 / 99846` grid cells、prepared load `1 / 1`、legacy load `0 / 0`、输出包 `948 / 4435`、decode 最大 `4.6 / 33.7ms`、overlay reveal `102.8 / 239.8ms`，两档 LongTask、残留批次、非性能 health、普通应用 console、page、WebGL 与 Loading 错误全部为 `0`。
- 本阶段产品增量约 `434` 行，专项与薄浏览器工具约 `239` 行；浏览器工具为 `171` 行，未新增诊断大脚本。阶段内子智能体启动与等待均为 `0`。阶段 B 已闭合，第 322 项仍处于执行中，后续严格进入阶段 C，集成冻结前不重复启动独立复核角色。

## 2026-08-12：第 322 项阶段 C3a 完成经济正式入口 Worker 闭环

- 经济重算、市场归属 API、经济面板重算和市场归属预览应用统一进入 `economy.compute` 持久 map-mirror session；Worker 同一事务生成经济领域 patch 与 point / labels / picking prepared render，主线程只做绑定校验、领域 patch 提交、历史、GPU / DOM 与面板同步。经济撤销 / 重做也在 Worker 镜像应用目标历史 patch 并准备正式画面，不再走同步全量刷新。
- 首次 10k 浏览器门暴露正式入口在主线程前后各遍历整份 `pack / economy` 计算来源指纹，形成约 `274 / 304ms` LongTask；该重复指纹已移除，主线程沿用 map identity、revision、generation token、锁指纹和 session binding 时效门，权威来源指纹继续由 Worker 镜像生成。夹具自身约 `44ms` 的精确经济 digest 已与产品操作拆成独立 macrotask，没有放宽 `<50ms` 产品标准。
- `webgl-generator-economy-worker-task-regression` 覆盖 10k / 100k、Worker / fallback、锁、取消、故障以及镜像撤销 / 重做；市场归属、通用 Worker 协议、生产构建均通过。最终 10k Chrome 的经济重算、市场改派、撤销、重做共 `4` 次 Worker 运行与提交，后 `3` 次复用同一 session，历史和经济数据精确往返，最终 session idle；LongTask、非性能 health、普通应用 console、page、WebGL、Loading 与普通文案技术词均为 `0`。
- 本切片产品约 `+252 / -26`，测试约 `+198 / -2`，新浏览器夹具 `174` 行，委派与等待均为 `0`。C3a 已建立 checkpoint，第 322 项仍执行中；下一切片严格处理人口，路线 / 城市寻路、军事策略、P2 与最终集成门均不在本次扩写。

## 2026-08-12：第 322 项阶段 C3b 完成人口正式入口 Worker 闭环

- 人口增减、区域转移和人口面板同源入口统一进入 `population.compute` 持久 map-mirror session；Worker 构造人口分配 plan、来源指纹、领域 patch 与 surface / point / labels / picking prepared render，主线程只保留轻量请求、绑定、领域提交、历史和 UI / GPU / DOM 同步。人口撤销 / 重做也先在 Worker 镜像应用历史 patch并准备画面。
- 人口专项 Node 覆盖 10k / 100k、增减 / 转移、legacy 同源、容器 identity、镜像撤销 / 重做、锁、取消、故障、陈旧和非法写集；100k 固定图调整 `706` 个 pack cells、`30` 个城市、输出 `1496` 条 patch path，数据层耗时约 `603ms`。UI / API 动态门、Worker registry、生产构建和差异检查通过。
- 最终 10k Chrome 对国家 #1 / #2 完成人口增减、转移、双撤销和双重做共 `6` 次正式 Worker 运行 / 提交，后 `5` 次复用同一 session；人口载体、城市、行政汇总、经济需求与 stale 状态精确往返，正式 map / typed arrays / 城镇容器引用保持，最终 session idle。六个操作窗 LongTask、非性能 health、普通应用 console、page、WebGL、Loading 与普通文案技术词均为 `0`。
- 本切片产品约 `230` 行，测试约 `170` 行，专用浏览器夹具 `138` 行，委派与等待为 `0`。C3b 已建立 checkpoint，第 322 项仍执行中；下一切片严格进入路线 / 城市寻路，军事策略、P2 与最终集成门不在本次混写。

## 2026-08-13：完成第 322 项复杂计算 Worker 化

- A～D 已依次闭合十一类重生成、复合与整图生成、网格 / 社会 / 经济 / 人口 / 路线城市 / 军事核心入口、大型存档与动态显示准备；主线程保留绑定校验、锁、原子提交、历史、DOM 和 GPU 提交，Worker / fallback、取消、迟到、故障、撤销 / 重做及旧数据兼容保持同源。
- Stage E 的完整 10k 与 100k 正式链通过。100k hard-cell 冷启动原先会在主线程补算整图顶点，单格操作约 `206ms`；现按已提交 `surfaceVertices` 和 grid 拓扑建立轻量 cell float offset，从正式 base 精确复制目标几何并只重写颜色 / side alpha，首格为 `17.4ms`、第二格为 `9.2ms`。base `43,093,008B`、六段 GPU refs / bytes / checksum、patch range / GPU、历史和地图 identity 均通过。
- 100k routes 的末端样式 / 布局已完成多轮归因和 city 状态恢复、overlay DOM 复用等实质优化，独立目标复验曾连续两次 LongTask 为 `0`；按用户统一处置门，只对 reused 与取消后 fresh 两个窗口各登记最多一条 `≤80ms` 信号，其它 Stage E 操作仍为 LongTask `0`。最终完整 100k 本轮实际 `longTasks=[]`，非性能 health、console、page、WebGL 与 Loading 错误为 `0`。
- 旧 v1 数据、旧 v2 浏览器存档、`.webfmg` / JSON / gzip、浏览器保存与恢复、D1 真实 map-file Worker 四次运行、API action convergence 和普通用户文案专项通过；D1 浏览器门 `legacyLoads=0 / LongTask=0 / forbiddenMessages=0`。通用 UI 术语旧夹具仍引用仅开发模式可见的高度预览 banner，连续两次在产品断言前超时，已按规则冻结为独立夹具债，不计第 322 项 PASS 或 BLOCK，也未混入本项改动。
- 当前收尾 diff 为产品约 `+85 / -12`、测试约 `+59 / -25`，主线程之外仅使用一次集成复核和一次最终观察使；观察使只读核对冻结 diff、10k / 100k / 旧档 / D2 artifacts 与廉价专项后给出 `ACCEPT`。第 322 项达到最小验收，进入归档并按队列转向第 327 项。

## 2026-08-13：第 327 项上下文治理阶段 0～2

- 第 322 项完成提交已推送任务分支、快进合入并推送远端 `main`；第 327 项从该远端提交新建独立分支。唯一调查角色只读量化根说明、开发日志、文档路由和两个通用 Skill，未派生子智能体，主线程保持唯一写者。
- 改造前根 `AGENTS.md` 为 `59,493B`，runtime、renderer、tools 三条典型路径都继承同一超限说明；现根入口只保留长期约束、默认读取链、固定队列和极短状态，降为 `6,530B`，未新增嵌套 `AGENTS.md`。
- 原 `docs/development-log.md` 为 `2,770,910B`。可复现迁移按日期标题把 `1,017` 个正文段落归入 `10` 个日期片分卷，并保存原文件 SHA-256 与逐段摘要；轻量入口降为约 `1.5KB`，迁移检查为 `1017 / 1017`。
- 默认读取链固定为 `AGENTS.md → docs/README.md → docs/current-plan.md → 定向 rg / 行段读取`，新增精简 handoff 模板。`run-lean-staged-delivery` 与 `four-officials-flow` 主入口分别由 `6,609 / 7,728B` 降为 `4,056 / 4,260B`，低频限额和委派 brief 移入一层 reference；个人 Skill 不进入仓库 Git diff。
- 当前阶段产品代码、`source/`、Wiki、浏览器改动与浏览器运行均为 `0`。上下文审计、日志分卷检查和 Skill frontmatter / reference 契约已通过；下一步只做集成冻结、全新最小上下文 dry run、独立复核与最终验收。

## 2026-08-13：完成权威任务第 327 项——AI 上下文、文档与流程节流

- 根 `AGENTS.md` 从 `59,493B` 收敛到 `6,530B`，runtime、renderer、tools 三条典型路径累计说明均为 `6,530B`，满足根 `16KiB`、普通路径 `24KiB` 硬门；未机械新增嵌套说明。
- 原 `docs/development-log.md` 的 `1,017` 个日期段落按日期无损迁入 `10` 个分卷，逐段摘要和原文件摘要全部通过；轻量入口、两级索引、稳定相对链接和定向检索替代默认整卷读取。新增本项两段记录后，当前段落为 `1,019`，历史迁移基线仍为 `1,017 / 1,017`。
- 默认接手链固定为 `AGENTS.md → docs/README.md → docs/current-plan.md → 定向 rg / 行段读取`；阶段 handoff 只传目标、文件、约束、廉价门、阻断和 artifact 路径。个人 `run-lean-staged-delivery` 与 `four-officials-flow` Skill 主入口分别收敛到 `4,056 / 4,260B`，职责拆为内部阶段治理与任务级独立角色，个人 Skill 不进入仓库提交。
- 本项只使用一次独立调查、主线程唯一写者、一次独立集成复核和一次独立最终验收，两个只读角色均给出 `ACCEPT`，没有向内部阶段或夹具修正递归四级流程。上下文审计、开发日志审计、权威归档检查、两项工具语法、相对链接和 `git diff --check` 全部通过。
- `app/`、`source/`、`docs/wiki/` 的 tracked 与 untracked 改动均为 `0`；产品构建和浏览器运行按封闭范围未执行。第 327 项达到最小验收，归档后按固定顺序转向第 323 项。

## 2026-08-13：第 323 项统一地图规模阶段实现

- 新增唯一 `1～100000` 地图目标归一化模块，生成选项、生成面板、公开 API schema、网格细分预检和开发面板统一复用；有限越界值自动钳制，普通地图规模输入在输入、变更和失焦时立即回显正式值，不再只依赖 HTML `max`。
- 单格生成修复高度突出度对空邻接直接 `reduce` 的崩溃，固定种子已生成 `1 grid / 1 pack / 4 polygon vertices` 的有限地图。100k 细分上限返回明确 no-op，主线程只做一次预检和授权校验，不启动 Worker、不写历史或替换地图。
- 地图导入以 `grid.points.length` 为现代权威实际规模；超过 100k 的存档在替换前以结构化 `map-cell-limit-exceeded` 拒绝，旧 v1 缺少 points 时仅回退既有 cell 高度长度，绝不截断超限地图。Worker 错误协议补传 stage / suggestion，运行时保留 details，公开 API 不再把该预期拒绝降成普通失败。
- 阶段门已通过地图规模专项纯逻辑、旧 v1 / 100k 存档 Worker、通用 Worker 11 类协议、API 322 方法发现、10k→100k 网格细分和生产构建。专用浏览器门两次首败分别定位为夹具读取 `{options,map}` 路径错误与产品错误元数据丢失；已按精简规则停止循环并把最终工具压到 `161` 行，待集成复核后只运行一次冻结版本终验。
- 当前产品新增约 `180` 行，专用工具 `161` 行；主线程为唯一写者，调查角色已完成一次，阶段内新增委派与等待均为 `0`。未修改 `source/`、Wiki、用户地图或用户 Chrome。
- 冻结浏览器门首次正确归窗后发现 100k 细分入口 / Worker 返回 / 提交分别有 `349 / 241 / 134ms` 同步指纹扫描。产品改为按 `grid identity + map revision` 缓存来源指纹，并以 Worker 已生成的目标指纹预热新 revision；目标复验中细分和 cap no-op 的 LongTask 均降为 `0`，全部拓扑与迁移门保持通过。100k 整图生成仍观测到唯一 `61ms` LongTask，已按用户“调查一次、实质优化一次、复验后 ≤200ms 可登记”的统一规则显式登记放行；工具仍硬拒绝任何 `>200ms` 信号。

## 2026-08-13：完成权威任务第 323 项——地图规模统一限制为 1～100k

- 生成选项、生成面板、开发面板、公开 API / schema、URL / 预设恢复和 `grid.refine` 已统一使用 `1～100000` 整数规则；有限越界值自动钳制并回显，达到上限后的继续细分返回明确 no-op，且不启动 Worker、不写历史、不替换地图。
- 单格地图在真实浏览器中生成 `1 grid / 1 pack / 4 polygon vertices`，有限且 framebuffer 非清屏可见；PNG、保存、替换与恢复通过。超限生成请求正式使用 `100000`，10k→100k 细分和上限 no-op 保持拓扑、身份、历史与校验和契约。
- 现代存档以 `grid.points.length` 为实际规模权威；超过 100k 的导入在替换前以 `map-cell-limit-exceeded / map-import-validate` 拒绝并保留当前地图，旧 v1 缺少 points 时才兼容回退，未截断旧数据。Worker 错误的 stage / suggestion / details 保持到公开 API。
- 100k 细分的重复全图指纹扫描已收敛为按 grid identity + map revision 缓存，并由 Worker 结果预热提交目标指纹；目标复验中细分与 no-op LongTask 均为 `0`。此前唯一 `61ms` 整图生成信号按用户统一规则登记，最终独立终验本轮实际 `registeredLongTasks=[]`，任何 `>200ms` 仍为硬失败。
- 地图规模专项、网格细分、旧 v1 / 100k 存档、Worker 十一类协议、API `322 / 322` 发现、生产构建及冻结浏览器入口全部通过；application / page / 非性能 health / WebGL / Loading 错误为 `0`，`source/` 未修改。一次独立集成复核与一次最终观察使均给出 `ACCEPT`。第 323 项达到最小验收，归档后按固定顺序转向第 324 项。

## 2026-08-13：第 324 项阶段 A 冻结地图模板目录与来源

- 第 323 项提交 `71bece3` 已推送任务分支、快进合入并推送远端 `main`；第 324 项从该提交新建独立分支 `codex-task-324-map-templates`，当前固定顺序为 `324 → 326 → 328`。
- 阶段 A 只冻结 16 个模板的稳定 ID、中文名、顺序、范围、投影、推荐规模、关键锚点、历史年代和来源，不下载大资源、不接 UI / API、不生成或替换地图。中国六类指定区域、东亚四至、澳洲三部分以及 `117 / 1789` 历史年代均进入机器可读目录。
- 物理资源口径选择 Public Domain 的 Natural Earth `5.1.2` 与 GEBCO `2026`；罗马帝国采用 Public Domain 的 `RomanEmpire 117.svg`，神圣罗马帝国采用 CC0 的 `Map of Holy Roman Empire 1789.svg`。运行时必须离线，后续资源阶段需记录处理参数和 SHA-256。
- 本阶段主线程为唯一写者，委派与等待均为 `0`。首门只运行目录专项、语法和差异检查；通过后建立 checkpoint，再进入离线资源与确定性映射阶段。

## 2026-08-13：第 324 项阶段 B 完成离线资源与 Worker 映射

- 构建期从 GEBCO `2026` 的官方 OPeNDAP 稀疏请求生成 `720×360` Int16 全球高程，从 Natural Earth `5.1.2` 生成陆地、湖泊扣除和河流中心线掩膜；固定二进制为 `1,036,816B`，SHA-256 为 `891878e6f04ded05c19ea2547d4d23438e7484031f16b77429617d7c603bbfc3`，运行时按需加载并在使用前校验，不访问第三方网络。
- 16 个模板按区域等经纬、世界 Equal Earth 或南极极投影在现有生成 Worker 内映射。`1 / 10000 / 100000` 共 `48` 个目录规模组合具有稳定语义 checksum；常用规模按 manifest 保护关键岛屿 / 区域锚点，极低规模明确记录降级锚点。生成后的 grid 保存区域与 canonical 水文证据，气候纬度和经度范围使用模板边界。
- 公元 `117` 年罗马帝国与 `1789` 年神圣罗马帝国 SVG 已在构建期栅格化为带来源文件摘要、年代、许可、像素类别和 SHA-256 的离线政治掩膜；神圣罗马帝国资源同时保留源页 CC0 声明与其较早欧洲底图派生链提示。历史资源运行时同样按需加载并校验。
- 资源、映射、历史资源三项专项与一次中国模板真实 Worker 生成通过；后者生成 `999` 个 grid cells、模板经纬度同源且有 canonical 水文证据和河流。普通程序化高度模板、UI、API、旧图、`source/` 与浏览器均未改；主线程唯一写者，委派与等待为 `0`。

## 2026-08-13：第 324 项阶段 C 完成固定年代历史政治预设

- 历史政治资源只随对应历史模板进入生成 Worker。grid / pack 先使用正式物理底图与社会派生，再把来源掩膜映射为一次性的历史核心国 / 属邦领土，重新同步首都、国家、省份、城镇和统计；普通地理模板的政治资源、历史预设和政治 cell 均为 `0 / null`。
- 罗马帝国 10k 固定门生成 `10004` cells，公元 `117` 年核心疆域 `1035` 个 pack cells、属邦 `49` 个；神圣罗马帝国 10k 同为 `10004` cells，`1789` 年核心疆域 `1916` 个。两者政治资源 checksum、国家全名、首都、cell ownership、省份父国和城镇归属全部同源。
- 创建结果不持有模板运行时 binding；模板 ID / 版本、物理与政治资源 checksum、年代、请求 / 实际 cells 和人文预设摘要只进入 metadata。历史专项、普通地理反例、语法和差异检查通过；UI / API、存档 / 导出和浏览器仍未进入本阶段，主线程唯一写者，委派与等待为 `0`。

## 2026-08-13：第 324 项阶段 D 完成 UI、公开 API 与兼容链

- 生成 Tab 新增独立“地图模板”入口，不与程序化高度模板混用；公开 API 增加模板枚举、详情和确认式创建，基线更新为 `18` 个命名空间、`325` 个方法，发现与能力矩阵保持 `gap 0`。资源只在创建时按需加载并校验 manifest checksum，普通 UI 不出现 Worker、线程、消息包、buffer 或浏览器后端概念。
- 中国模板真实浏览器创建得到 `1014` 个实际 cells，renderer、metadata 和零历史同源；模板资源故障返回 `map-template-resource-load-failed`，当前 map / revision / history 精确不变。普通地图与模板地图双向导入保持 metadata 区分、renderer 当前绑定、WebGL 与 Loading 正常。
- 首次浏览器门因夹具对隐藏原生 select 使用 `selectOption` 等待超时；改为页面正式 change 事件后，第二次只剩工具把 `operation-stall` 性能 health 当功能错误。目标复验又发现主动中断 HRE 的 JSON / BIN 后 Chrome 必然打印两条 `net::ERR_FAILED`；最终夹具按 `requestfailed` 的资源名、错误码和数量一一核销，非注入 console 与非性能 health 仍为 `0`，本轮 LongTask 为 `0`。
- 目录、物理 / 历史资源、48 个 `1 / 10000 / 100000` 纯数据组合、历史 10k、运行时懒加载 / 存档、API / AI 文档 / 能力矩阵、生产构建与真实浏览器门均通过。阶段 A～D 已建立 checkpoint，下一步只进入阶段 E 的 16 项默认规模截图和冻结 10k / 100k 代表矩阵；`source/`、Wiki 和用户 Chrome 未改，主线程之外委派与等待为 `0`。

## 2026-08-13：第 324 项阶段 E1 完成 16 项默认规模真实验收

- 16 个模板均以各自目录默认 `10k` 规模从正式公开 API 创建，逐项通过 metadata、TypedArray、海陆、高程、水文、区域掩膜、保护锚点、历史政治、renderer、overlay、WebGL 与普通文案审计，并生成 `1280×820` 固定视口截图；共享物理资源只请求一组 JSON / BIN，两套历史政治资源只随对应模板按需请求。
- 首轮真实验收发现南极洲人口评分过低时没有 pack 首府，政治生成退回 grid 路径后与 pack 省份镜像不一致。产品现仅在“配置要求国家但普通人口候选无法生成一个首府”时使用合法陆地作 required 首府候选，仍保留陆地、占用和重复检查，普通城镇人口 / 文化门及既有 10k / 50k / 100k 省会矩阵不变。
- 历史模板目录年代原已进入网格映射与历史人文预设，但最终 `metadata.mapTemplate` 组装遗漏 `snapshotYear`；现补齐并由 Node 锁定目录、Worker 地图 metadata 与 human preset 同源。神圣罗马帝国 `1789` 定点浏览器复验通过。
- 最终完整门为 `16 / 16` 模板、`16 / 16` 截图、最大 LongTask `0`，application console、page、非性能 health、WebGL 和 Loading 错误均为 `0`；人工 contact sheet 抽检确认 16 个范围与投影视图可辨且无空白 / 清屏。产品改动 `2` 个文件，专项 / 浏览器工具改动 `3` 个文件，委派与等待均为 `0`。阶段 E1 建立 checkpoint，下一步只进入阶段 E2 的六项 `10k / 100k` 冻结矩阵。

## 2026-08-13：第 324 项阶段 E2 完成 10k / 100k 代表矩阵

- 世界、中国、东亚、澳洲组合、神圣罗马帝国和罗马帝国依次完成 `10k / 100k` 共 `12` 个独立页面案例；每例均锁模板来源、实际 cells、海陆、保护锚点、历史年代、renderer、城镇 picking、WebGL、Loading、普通文案与错误面。六个 100k 地图均完成 PNG 和全幅 GeoJSON，中国 100k 另完成完整地图导出后原样导入。
- 首轮矩阵在东亚 100k 捕获 `390ms` LongTask；窄诊断以同一案例复现 `392ms`，精确重合 `data.exportGEO` 的主线程构造与 `JSON.stringify` 区间，模板创建、PNG、renderer 与 Worker 安装均在该区间之外。产品复用既有 map-file Worker 新增 `export-geojson` 子操作，Worker 返回唯一 transferable 字节 buffer，主线程只负责 Blob / 下载与按需异步文本恢复；合法范围语义不变，非法 bbox 仍在 operation 前同步预检并返回结构化错误。
- map-file Worker/fallback GeoJSON 字节、FeatureCollection、feature 数量和 transfer list 同源；既有 GeoJSON 范围纯数据门、API action convergence 和构建通过。旧 `geojson-range-browser` 的四个正向文件实际有效，其最终失败仅因旧夹具把初始化 / 新图 `operation-stall` 性能事件当功能错误，未在本项扩修或计作产品阻断。
- 东亚 100k 目标复验与随后完整 `12 / 12` 矩阵均为 LongTask `0`；最终统计为 PNG `6`、GeoJSON `6`、完整导入往返 `1`，application console、page、非性能 health、WebGL 和 Loading 错误为 `0`。阶段 A～E2 冻结，下一步只做独立集成复核与最终验收。

## 2026-08-13：完成权威任务第 324 项——内置真实地理与历史场景地图模板系统

- 16 项地理 / 历史模板均完成默认 `10k` 正式 API 创建、结构审计和 `1280×820` 固定视口截图；共享物理资源与两套历史资源保持离线、按需、checksum 可审计，中国指定区域、东亚四至、澳洲三部分及 `117 / 1789` 年代均进入机器门和人工 contact sheet。
- 最终代表矩阵覆盖世界、中国、东亚、澳洲组合、神圣罗马帝国和罗马帝国各 `10k / 100k` 共 `12` 案例；六张 100k 地图完成 PNG 与全幅 GeoJSON，中国 100k 完成完整存档往返。南极洲受控首府兜底、历史年代 metadata、模板替换回滚、旧图兼容、picking、renderer、WebGL、Loading 和普通文案均通过。
- 东亚 100k 的 `390 / 392ms` 主线程 GeoJSON 构造与序列化已迁入既有 map-file Worker，返回唯一 transferable 字节 buffer；非法 bbox 在 runtime operation 前由权威范围归一化器同步拒绝。目标复验、完整矩阵和独立终验的 LongTask 均为 `0`。
- 独立集成复核在唯一 bbox 预检证据阻断闭合后给出 `ACCEPT`；独立观察使亲自运行最终 12 案例入口约 `248s`，结果为 `cases=12 / PNG=6 / GeoJSON=6 / roundtrip=1 / LongTask=0`，并复核 E1 的 `16 / 16` 截图。开发日志审计、范围专项、map-file Worker、模板运行时与差异检查通过，`source/` 零改动。第 324 项达到最小验收，归档后固定顺序转为 `326 → 328`。

## 2026-08-13：第 326 项阶段 A 完成地点解析与测量纯数据核心

- 第 324 项已归档、合入并推送远端 `main`；第 326 项从该提交新建独立分支 `codex-task-326-place-analysis`。一次独立只读调查给出 `ACCEPT`，冻结三类地点、精确引用、同名优先、行政代表点、单位换算、八方位与浏览器 / 无头共用边界；主线程保持唯一写者。
- 新增共享纯数据模块，名称按“国家 > 省份 > 城镇、同类 ID 升序”返回首选和完整候选；国家、省份与城镇六种正式锚点来源及非法、删除、缺失锚点错误均被固定夹具覆盖。精确 `{kind,id}` 与名称解析使用同一结果结构，不修改输入地图。
- 显示单位模块抽出正式距离换算供格式化与分析共同复用；距离回显世界值、换算值、单位、格式、精度、近似原因和 revision。方位按地图 Y 向下语义换算为北向 `0°`、顺时针角度；首轮专项发现 `22.5°` 浮点落边，产品在归一化角度处收口到 `1e-12`，八个半开边界与重合随后通过。
- `node --check`、`git diff --check` 与 `regress:place-analysis` 均通过，固定结果为四个跨类型 / 同类型候选、六种锚点、九个方位边界和正式英里中文格式。阶段 A 只改两个产品文件并新增一个专项工具；浏览器、构建、无头 API、schema、AI 文档、`source/` 与用户地图改动均为 `0`，下一步只进入阶段 B 接线。

## 2026-08-13：第 326 项阶段 B 接入浏览器 / 无头 API 与发现文档

- 浏览器与无头 `analysis` 同增 `resolvePlace / measureDistance / getDirection`，正式基线更新为 `18` 个命名空间、`328` 个方法、`320 / 7 / 1` 稳定等级。浏览器默认读取当前会话单位并可显式覆盖；无头依次使用显式单位、存档单位、默认单位，缺省来源进入近似原因。
- 浏览器可选屏幕距离使用当前 renderer 世界到屏幕转换，再按画布 backing store 比例返回 `canvas-device-pixel`；该诊断不写偏好或存档。无头对屏幕距离返回 `unsupported_coordinate_space`，正式世界距离、换算值与方位仍与浏览器共用纯数据核心。
- 三项方法已进入显式 schema、结构化业务错误、`info.describe`、AI 机器目录和区域分析两阶段调用手册；AI 目录同时修正整个 `analysis` 命名空间的只读分类。完整能力矩阵为 `1228` 行、`covered 1154 / excluded 74 / deferred 0 / gap 0`，无头目录为 `26` 项。
- 首个专项因 Node 直接导入浏览器 `console-api` 穿透 `.vue` 而停在工具边界；随后只把 Node 门收敛为共享核心、无头 API 与 schema，浏览器接线留给正式 Vite 入口。地点专项、无头、稳定契约、API / AI 发现、AI 文档、能力矩阵、编辑覆盖、动作收敛、差异检查和生产构建均通过；构建为 `1346` modules / `1.53s`。本阶段产品增量约 `265` 行、专项工具增量约 `66` 行，委派与等待为 `0`，`source/` 与用户地图未改。

## 2026-08-13：第 326 项阶段 C 完成真实浏览器验收与集成复核

- 新增 `214` 行专用浏览器工具，复用正式静态服务与 API-ready helper，只在隔离系统 Chrome 中执行一次固定 10k 新图、完整存档往返和正式 100k 细分；没有连接用户 Chrome，也没有把 100k 地图经 CDP 回传。
- 首轮 `38.3s` 即通过：10k、往返和 100k 三阶段均保持地图 / renderer 引用、checksum、revision、history、selection、layers、units、camera 零副作用；国家、省份正式锚点分别命中首都与唯一省会。三档 `0.75 / 1.5 / 3` 相机下正式世界距离不变，设备像素距离约 `1145 / 2291 / 4581` 递增；浏览器与无头的世界距离、单位换算和方位在 `1e-9` 内同源。
- 浏览器方法基线 `328`，三项 schema 均为 `mutates:none / requiresConfirm:false`；LongTask、application console、page error、非性能 health、WebGL 与 Loading 错误均为 `0`。构建产物和 `source/` 均未被夹具写入。
- 独立集成复核仅发现能力矩阵的名称启发式把 `analysis.measureDistance` 误标成持久写入。主线程精确特判该一个 action，并在矩阵专项锁定三项地点 API 各恰一行且 `mutates=none`；重新生成与审计通过，blocker-only 复查给出 `ACCEPT`。当前冻结树只待独立观察使最终验收。

## 2026-08-13：完成权威任务第 326 项——地点解析、距离与方位查询 API

- `analysis.resolvePlace / measureDistance / getDirection` 已在浏览器与无头环境复用同一纯数据核心。名称完整匹配、国家 `fullName`、国家 > 省份 > 城镇、同类 ID 升序、完整候选和精确 `{kind,id}` 均有固定反例；六类代表点、结构化错误、正式单位换算、近似原因、八方位半开边界和重合口径均已闭合。
- 浏览器可选屏幕距离明确为 `canvas-device-pixel`，三档相机只改变屏幕值，不改变世界距离或方位；无头拒绝屏幕坐标并依次使用显式、存档、默认单位。三项方法进入 `328 / 328` 发现、schema、AI 手册及 `1228` 行能力矩阵，均为只读、无确认、gap `0`。
- 独立观察使亲自运行最终真实入口约 `38s`：固定 10k、存档往返与正式 100k 细分通过，世界距离 `1147.1788085996009`、方位“西”、国家 / 省份锚点为首都 / 省会；三档屏幕距离约 `1145 / 2291 / 4581`，浏览器 / 无头误差 `<1e-9`。三个阶段地图、renderer、checksum、revision、history、selection、layers、units、camera 均零副作用，LongTask、Loading、WebGL、非性能 health、console 与 page error 为 `0`。
- 地点专项、无头、API 稳定契约、AI 发现 / 文档、能力矩阵、构建、开发日志和差异检查全部通过；一次调查、一次集成复核（含一个 blocker-only 闭合）和一次最终验收均按任务级预算完成，子智能体没有派生。`source/`、Wiki 和用户 Chrome / 地图未改，第 326 项达到最小验收并归档，固定队列只剩第 328 项。

## 2026-08-13：启动权威任务第 328 项——冻结大河政治软阻隔施工边界

- 第 326 项已以提交 `8e819cd` 快进合入并推送远端 `main`；第 328 项从该提交新建独立分支 `codex-task-328-river-political-boundaries`，当前权威队列只剩 `328`。
- 只读证据确认现有 pack 国家扩张只按目标 river cell 的 ID / flux 增加成本，整图分省不看河流，按国家局部重分省使用无权 BFS；国家 / 省份边界整理还会吸收毛刺。因此施工冻结为一个共享河障模型，并同时接入扩张、局部重分省与受控后处理，禁止各路径复制阈值。
- 内部阶段固定为 `A 纯核心与生成器 → B Worker / runtime / 回滚 → C 真实浏览器矩阵`。主线程是唯一写者，内部首败不重复调查、复核和观察使；任务完成前只在阶段 checkpoint 跑相称门，最终再做一次独立集成复核和一次观察使。
- 当前阶段产品改动 `0`、测试工具改动 `0`，只形成权威状态、专题设计和开发日志证据；浏览器、build、dist、用户地图、`source/` 与 Wiki 均未改。

## 2026-08-13：第 328 项阶段 A 完成河障核心与政治生成器接入

- 新增共享纯数据河障模型，从 canonical 河流的对数流量、宽度、Strahler 等级、连续长度与河段数计算稳定强度；道路、港口、行政中心与普通聚落只按不同系数降低过河成本，不把强河变成零成本硬通道。模型不按河流 ID 或显示像素判定，也不改写河网、道路或地图输入。
- 新图 grid / pack 政治生成、国家全图重生成和省份全图重生成已复用同一转移函数；扩张预算与河障竞争成本分离，避免强河导致大片未分配土地。国家 / 省份毛刺整理不跨强河吸收；无城镇的遗留连通块并入同国既有合法省份，不再为追求河界制造无中心“边地”碎片。
- 对称双中心夹具的强河合格段采用率为 `100%`，固定归属 checksum 为 `6811943`；同几何弱河 / 单侧中心不分裂，短支流、多汇流、道路、港口、锁定归属、无河与输入零改写均通过。政治 metadata 只保存候选 / 强弱数量和国家 / 省份采用率摘要，逐河明细留待阶段 B 的结构化操作结果，避免持久化重复大数组。
- `regress:river-political-barrier`、国家 / 省份锁、scoped regeneration、省名和国家拓扑门均通过，语法、package JSON 与差异检查通过。`regress:province-topology` 的城市 visual/group 撤销差异在远端 `main@8e819cd` 可独立同样复现，登记为既有基线红门且未在第 328 项扩修。阶段 A 产品 `2` 个文件、专项工具 `1` 个文件；浏览器、build、dist、用户地图、`source/` 与 Wiki 未改，下一步只进入阶段 B。

## 2026-08-13：第 328 项阶段 B 完成局部重分省、Worker 同源与结构化诊断

- 按国家局部重分省不再使用无权 BFS；只有明确 `regenerate` 计划才建立同一河障模型，并以确定性加权优先队列竞争领土。锁省 cell 和省会是不可覆盖锚点，强河仍为有限成本；手工国家合并 / 拆分继续沿用原拓扑语义，不被本项隐式重划。
- 国家 / 省份全图与单国重生成均返回逐河 `details.riverBoundaries`：包括模型 checksum、候选 / 强弱河、分量评分、沿河边界长度、同归属跨河、道路 / 港口 / 聚落走廊和采用 / 回退原因。政治 metadata 仅保留紧凑计数与采用率；生成日志记录强河比例和国家 / 省份采用率，普通状态文案不出现算法术语。
- 固定 10k / 50k / 100k 校准中，`0.65` 会令强河长期为零，已收敛到 `0.58`：代表样本分别约 `4 / 11 / 12` 条强河，仍只占少数。专项现直接调用正式局部分省函数，强河对称双中心采用率 `100%`、checksum `6811943`，弱河 / 单中心不分裂，锁定锚点优先。
- Worker 十一类与 fallback 同源门约 `47s` 通过，国家 / 省份锁、scoped regeneration、锁覆盖矩阵、国家拓扑、用户文案、语法与差异检查通过；生产构建为 `1347 modules / 1.64s`。阶段 B 产品 `5` 个文件、既有专项工具 `2` 个文件，新增测试行未超过产品增量；浏览器、用户地图、`source/` 与 Wiki 未改，下一步只进入阶段 C。

## 2026-08-13：第 328 项阶段 B 补齐行政连通性门

- 国家、省份与局部重分省的多源优先队列改为只在最小代价项正式出队时结算归属；后续候选不得覆写已经结算的 cell，避免旧候选后代保留成断裂飞地。国家 / 省份边界毛刺整理后另做同陆地 feature 的确定性连通修复，优先保留含中心的分量，并按河障成本、共享边数和稳定 ID 吸收到相邻合法行政区；锁定 cell 与锁定行政对象不被改写。
- 固定 `10k / 50k / 100k` 生成结果的国家断裂分量、省份断裂分量和应有省份但未分配的陆地 cell 均为 `0 / 0 / 0`；强河数仍为 `4 / 11 / 12`，国家采用率约 `0.163 / 0.116 / 0.095`，省份采用率约 `0.816 / 0.488 / 0.295`，没有以修连通性为由抹掉河界偏好。
- 河障专项新增真实局部分省归属连通断言；国家锁、省份锁、局部重生成、国家拓扑和 Worker 十一类协议门全部通过。当前增量只涉及 `politics.js`、`state-topology-commands.js` 和一个既有专项工具；浏览器、dist、用户地图、`source/` 与 Wiki 未改，下一步仍只进入阶段 C 真实入口。

## 2026-08-13：第 328 项阶段 C 完成真实浏览器阶梯

- 正式 `10k / 50k / 100k` 新图、国家重生成和省份重生成均通过；三档实际规模为 `10004 / 50142 / 99846`。国家、省份断裂分量、中心错位和应有省份但未分配的 cell 均为 `0`，河障模型 checksum 在同一地图的操作前后稳定。
- 浏览器门同时锁定河流对象 / GPU / picking 零改写、道路对象与 route GPU / picking 正式同源。10k 另覆盖省份重生成撤销 / 重做、单国局部重分省、accepted 后取消、after-render 故障回滚和小图 fallback；事务失败后的地图、历史、行政、河流、道路与 renderer 引用精确恢复。
- 10k 与 50k 全部操作 LongTask 为 `0`。100k 国家重生成先后观测 `59 / 64 / 65ms` 单条信号；一次窄诊断未复现，且 Worker 输入 / 输出单包最大 `1.5 / 4.3ms`、安装提交 `5.2ms`、UI `6.8ms`、overlay `18.3ms`，最长 `surface-ranges 50.2ms` 是八个主动让步分片的总跨度，并非单个同步任务。按用户统一规则只为 100k 国家重生成登记 `≤200ms`，省份重生成与其它入口仍硬要求 LongTask `0`。
- 最终 100k 目标门中登记信号为 `65ms`；国家 / 省份 river adoption rate 约 `0.046948 / 0.28169`，非性能 health、普通应用 console、page、WebGL 与 Loading 错误均为 `0`。河障、国家 / 省份锁、scoped regeneration、国家拓扑、Worker 十一类协议与生产构建通过；首页卷次由根版本 `0.2.0` 构建注入。当前实现冻结，只待一次独立集成复核和一次最终终验。

## 2026-08-13：完成权威任务第 328 项——大河参与国家与省份划界

- canonical 河流的流量、河宽、Strahler 等级、连续长度和河段数现共同形成稳定软阻隔；强河可优先成为国家或省份边界，道路、港口、中心和聚落提供受限走廊折减，小河不普遍制造碎片。新图、国家 / 省份全图重生成及单国局部重分省同源，锁和合法中心优先，旧图载入、普通渲染、手工边界与河网不被自动重划或反写。
- 顺序连通修复和 isolated neutral 归属纠偏消除了行政飞地；正式 10k / 50k / 100k 的 initial / states / provinces 及 10k undo / redo / scoped topology checksum 全部冻结。三档断裂、中心错位、未分配 cell 均为 `0`，河流 / 道路 / GPU / picking、取消、fallback、故障回滚与历史同源。
- 独立集成复核在夹具 `500` 行上限和 topology checksum 两个 blocker-only 门闭合后给出 `ACCEPT`。独立观察使亲跑河障、锁、局部、Worker、构建及三档 Chrome：10k / 50k 全部 LongTask `0`，100k states 唯一 `69ms` 属已调查并登记的 `≤200ms` 信号，其余 LongTask `0`；health、console、page、WebGL 与 Loading 错误均为 `0`。
- 新增 Git 交付规则：每次提交必须同步递增根语义化版本，普通改动升 patch，复杂功能评估 minor，不兼容契约评估 major；首页卷次继续由 package 版本构建注入。本次复杂功能由 `0.1.0` 升至 `0.2.0`。`source/`、Wiki、用户 Chrome 与用户地图未改，第 328 项达到最小验收并归档，当前权威队列为空。

## 2026-08-13：登记第 329 项——LongTask 登记例外复核与收敛

- 用户要求重新审查此前按统一规则登记放行的 LongTask，判断是否仍有优化价值并给出方案。只读审计确认八组历史窗口：C1 `159 / 112ms`、C2 首次文化 `56ms`、C3c `110 / 113 / 132 / 193ms`、D2 主题 `55～68ms` / 最大标签 `54～59ms`、100k routes `52 / 78ms`、第 323 项 100k 新图 `61ms`、第 328 项 100k 国家 `59～69ms`；另发现第 324 项虽然最终 LongTask 为 `0`，三个模板工具仍保留任意数量 `≤200ms` 的宽门。
- 当前裁定为：C3c 是唯一需要优先重新归因并实施优化的多条高时长产品债；C1 先用后续实现复验，复现才诊断；D2 和第 328 项只做目标复核，不在无热点证据下扩修；C2、100k routes、第 323 / 324 项以收紧机器门为主。第 329 项不得再使用跨多操作汇总或任意数量 `≤200ms` 的额度，保留例外必须精确到入口、数量、时长和来源。
- 阶段固定为 `A 回归额度 → B C3c 重点治理 → C C1 / D2 / routes / 323 / 328 条件复核`。每个浏览器入口最多一次最窄诊断和一次目标复验，夹具连续两次失败或同一 blocker 重现即停止。当前只登记权威计划与专题矩阵，产品、工具、build、浏览器、版本、用户地图、`source/` 与 Wiki 改动均为 `0`。

## 2026-08-13：登记第 330 项——优雅 Loading 与编辑面板文案治理

- 用户同时反馈部分 Loading 文案重新变得不优雅，且部分编辑面板说明过于生硬。初步静态遍历覆盖 `28` 个 Vue 面板、`30` 个 JS 适配文件，并检出至少 `56` 处显式 Loading / “正在……”状态；新增能力中已经重新出现“受控网格结构、气候下游、地图运行时、经济链、渲染准备、中心 pack cell、同事务、只读预检、目标 feature”等普通用户难以理解的内部表达。
- 第 330 项冻结为 `A 全量文案矩阵与业务词表 → B Loading 分层与防闪 → C 面板普通文案分批改写 → D 真实浏览器验收`。快速操作不显示提示，慢操作复用现有 token 化延迟反馈；普通界面描述用户目标、影响和下一步，调试模式、API details 与内部日志继续保留技术精度。气候面板现有“更新受气候影响的内容 / 查看更新范围”作为结果导向基准。

## 2026-08-13：第 329 项阶段 A～C——LongTask 额度收敛与 C3c 异步指纹

- 第 323、324、328、C1、C3c 与 100k routes 当前机器门已从跨入口或任意数量 `≤200ms` 收敛为严格零或精确到单入口的数量、来源和时长。C1 当前产品窗 LongTask 为零；100k routes 连续两次 reused 为零；D2 committed-display 与第 324 项主模板入口均为零。
- C3c 首次分窗锁定四条 `109 / 130 / 106 / 192ms`，共同从 Worker 返回后同步重算路线来源指纹开始。主线程改用同 checksum 的可让步异步指纹后，七个入口为零，只剩 `city-move 80ms`；最终登记为该入口最多一条 `self≤100ms`。第 323 项只保留 `generate-capped 1×self≤80ms`，第 328 项只保留 `fallback-provinces` 与 `100k states` 各 `1×self≤80ms`。
- 当前产品改动集中于 `route-path-worker-task.js / app.js`；Node 10k / 100k 同源、让步、陈旧拒绝、Worker / fallback、历史往返通过。浏览器证据保存在 `work/task329-*`；未修改地图数据、存档、公开 API、用户地图、Wiki 或 `source/`。
- 验收必须覆盖启动、生成 / 替换、重生成、导入导出、存档、云端、面板懒加载，以及全部 `28 / 28` 面板的空态、预览、危险动作和禁用原因；浏览器同时验证快速 / 慢速、成功 / 失败、取消、重叠、迟到完成和 chunk 故障恢复。当前只登记与形成初步清单，不修改产品、构建、浏览器、版本、用户地图、`source/` 或 Wiki；固定执行顺序更新为 `329 → 330`。

## 2026-08-13：完成权威任务第 329 项——LongTask 例外精确收敛

- C3c 的主线程同步路线来源指纹已改为同 checksum 的可让步异步校验；独立最终浏览器中八个入口仅城市移动出现 `89ms`，其余七项为零。C1、D2、100k routes 和第 324 项旧额度删除；第 323、328 项只保留四个精确到入口的单条额度，不再存在跨操作或任意数量 `≤200ms` 的假绿门。
- 独立集成复核发现并关闭最后一个 100k fresh routes `2×75ms` 隐藏额度，blocker-only 复查为 `ACCEPT`；独立观察使亲跑 Node、构建、C3c、第 323 与第 328 项真实浏览器门后同样给出 `ACCEPT`。数据、历史、回滚、拓扑、GPU / picking、Loading、health、console、page 与 WebGL 门通过，`source/`、Wiki 和用户地图未改。
- 第 329 项完成并移入归档；交付版本升至 `0.2.2`。第 330 项须待本分支合入并推送远端 `main` 后，从最新主线建立独立任务分支。

## 2026-08-13：第 330 项阶段 A～C——文案矩阵、Loading 防闪与普通面板改写

- 从远端 `main@a520d9f` 建立独立分支 `codex-task-330-loading-panel-copy`。机器可审计矩阵现精确覆盖 `28 / 28` 个 Vue 面板、`30 / 30` 个适配文件、`26` 个懒加载入口和六类 Loading 来源；普通文案与开发诊断分层，API、调试字段和内部日志不做伪业务化替换。
- 共享操作继续沿用既有 `420ms` token 延迟；懒面板新增 `180ms` 防闪，快速挂载不闪字，慢挂载统一为“正在打开……，请稍候片刻。”，chunk 获取、挂载失败、重试和刷新恢复链保持。启动、地图网格、气候影响、画面、市场与贸易、军事、文化 / 宗教、路线等高风险 Loading 已改为用户结果导向表达。
- 生物群系、城市、气候、文化、经济、湖泊、人口、国家 / 省份、宗教、河流、路线、地区等普通面板去除了已确认的 `cell / grid / pack / feature / 同事务 / 只读预检 / 经济链` 生硬表达；洋流筛选空态补齐“清空筛选”。调试专属底层字段继续可见。
- `audit:ui-copy` 输出 `28 / 30 / 26 / 6` 全覆盖；面板防闪与故障恢复、共享延迟反馈、十一类重生成用户文案和术语 / 状态专项均通过。当前产品和文案改动约 `150` 行，专项 / 审计约 `170` 行；委派与等待为 `0`，尚未运行构建或浏览器，下一阶段只做真实入口验收。

## 2026-08-13：第 330 项阶段 D——真实 Loading、分包恢复与全量面板验收

- 隔离 Chrome 已验证快速操作不闪、慢操作延迟显示、成功 / 失败 / 取消 / 重叠 / 迟到清理、320px Loading 无溢出，以及真实国家面板动态模块获取失败。浏览器会缓存失败的动态导入，产品因此不再提供无效的原地重试：普通组件故障仍可“重试加载”，分包 / 版本资源故障改为提示先保存地图后“刷新页面”，解除故障后刷新并重新打开面板通过。
- 普通模式 `28 / 28` 面板在 `1280×820 / 390×844` 两档全部打开；普通可见技术词、错误级 health、应用 console、page error、WebGL、Loading 残留和横向溢出为 `0`。面板工具修正了启动事务空闲等待、城镇正式主键 `id`，并在动作后等待 runtime operation 与操作 Loading 同时清空；路线完整阶段和成功事件不再错归到下一个面板，没有以固定 sleep 或旧字段制造假绿。
- 通用 profiler 继续默认把全部 health 事件作为严格性能门；第 330 项通过显式 `--error-health-only` 仅隔离文案 / 功能错误，原始告警仍写入报告。国家“取消新增”约 `477ms`、路线“编辑路线”约 `250ms` 作为范围外性能发现登记，未在本项扩修。生产构建、静态矩阵、专项 Node 和真实浏览器门已闭合，交付 patch 版本更新为 `0.2.3`，当前只待独立复核与最终验收。

## 2026-08-13：完成权威任务第 330 项——统一优雅 Loading 与编辑面板用户文案

- 六类 Loading 来源、`28 / 28` 个 Vue 面板、`30 / 30` 个适配文件和 `26` 个懒加载入口进入机器可审计矩阵；普通文案改为地图网格、画面、气候影响、市场与贸易、行政 / 文化 / 宗教分布等用户结果，调试、API details 和内部日志继续保留精确技术信息。
- 共享操作保持 `420ms` token 延迟，面板挂载使用 `180ms` 防闪。快速操作不闪，慢操作、成功、失败、取消、重叠和迟到清理通过；普通组件故障可原地重试，动态分包 / 版本资源故障诚实提示先保存地图后刷新，不再展示浏览器模块缓存下无效的重试动作。
- 独立复核关闭异步路线操作 health 错归阻断后给出 `ACCEPT`；独立观察使亲跑静态矩阵、五项专项、生产构建、真实分包故障和生产 dist 全量面板门，同样给出 `ACCEPT`。`1280×820 / 390×844` 两档均为 `28 / 28`、普通技术词 / 错误级 health / 应用 console / page / WebGL / Loading / 横向溢出 `0`，首页版本为 `0.2.3`。
- 国家“取消新增”约 `477ms`、路线“编辑路线”约 `250ms` 仅作为独立性能证据登记；通用 profiler 的严格默认未放宽。API、schema、存档、生成算法、地图数据、编辑事务、信息架构、视觉主题、`source/`、Wiki、用户 Chrome 与用户地图未改，第 330 项达到最小验收并归档，当前权威队列为空。

## 2026-08-14：第 332 项完成视图切换瓶颈调查与缓存复用

- 固定 100k 证据确认首次切换约 `14.6s`，其中完整地图镜像输入约 `8.4s`、首次渲染几何准备约 `5.7s`；同图后续颜色视图仍重复建立 cell visual、shore、state path 和 province path，导致每次约 `5.9～6.7s`。
- 持久显示 Worker session 现按 `mapIdentity + mapRevision` 复用四类只读几何缓存；revision 变化或领域写任务会清除，政治 mesh 仍按请求重建。100k 后续颜色视图降至约 `0.54～1.00s`，主题约 `1.4～1.5s`，标签约 `0.47～0.51s`；10k 后续颜色视图约 `0.10～0.18s`，当前浏览器窗 LongTask 为 `0`。
- 失败的后台预热方案已完整撤销，不把冷启动成本隐藏到新图完成之后。100k 主题 / 标签入口经一次分段调查后仅保留精确 `self≤130ms` 登记，其它视图入口和全部 10k 入口仍硬要求 LongTask `0`。
- 渲染准备专项、Worker 十一类 / session / cancel / deferred 回归、语法、差异检查和生产构建通过。当前产品 `3` 个文件、净增约 `36` 行；专项 `2` 个文件约 `23` 行，专用浏览器工具不足 `500` 行。用户 Chrome、用户地图、`source/` 与 Wiki 未改，冻结树待独立复核与最终两档验收。

## 2026-08-14：完成权威任务第 332 项——100k 视图切换缓存复用

- 独立集成复核关闭“上一动作 Worker 证据误归当前动作”和“缺证据动作先被过滤”的两个夹具假绿后给出 `ACCEPT`；13 个非 no-op 动作现必须逐项产生新的 operation ID / name 同源证据。
- 独立观察使亲跑渲染准备、Worker 十一类、生产构建及 10k / 100k Chrome。10k 后续切换为 `180.4～435.8ms` 且 LongTask `0`；100k 后续为 `513.3～1635.6ms`，仅主题入口登记 `64 / 52ms self`，标签和其它入口为 `0`。
- 两档 session / cache / 输入包、地图 / renderer / revision / checksum / history / camera / selection / highlight / picking、Loading、health、console、page 与 WebGL 门通过；首页为 `v0.2.9`，端口与隔离进程清理。第 332 项达到最小验收并归档，当前权威队列为空。

## 2026-08-14：登记第 333 项——共享 Worker 地图副本与紧凑二进制存档

- 用户反馈 Worker 化后多类画布操作显著变慢，并要求调查是否反复全量传输 100k 地图，同时把压缩后仍超过 `10MB` 的存档格式优化登记为权威任务。源码与既有真实 artifact 证明并非每次画布操作都传图：平移、缩放、悬停和选择留在页面，同一 task / binding 的热 session 约 `3` 个输入包；但单一 task-specific persistent session 会在跨领域任务时失效，普通 revision 又会同时失效计算与显示副本，因此 fresh / 跨 task / 撤销重做 / 主线程编辑后的下一操作仍反复发送约 `941～1053` 包并耗时约 `6～8s`。
- 固定 `99846` cells 存档实值约 `61.2MB` JSON、`14.2MB` gzip。只读结构审计把全部 TypedArray 替换为微型描述后仍有约 `56.2MB`，唯一底层 TypedArray buffer 约 `3.38MB`；主要体积来自 grid / pack 的邻接、顶点、多边形和坐标嵌套数组，以及 economy 对象表。因此第 333 项不采用“只换压缩算法 / 只改 TypedArray”假优化。
- 方案冻结为同一 canonical 字段注册表驱动两条能力：计算与显示 Worker 各自持有跨 task 的版本化 map replica，通过提交后 patch / inverse patch、ACK、checksum、有界 journal 和显式 resync 推进；`.webfmg v3` 以列式数值、CSR 邻接、字符串字典、对象表分区、section directory 和 checksum 保存同一字段。固定 canonical 100k 目标为 raw `≤16MiB`、gzip `≤8MiB`，旧 JSON / gzip / base64、IndexedDB、File / Blob 和云端存档继续兼容。
- 第 333 项独立分支为 `codex-task-333-worker-replica-binary-map`，阶段固定为 `A 字段注册表与证据 → B 版本化副本 / 增量日志 → C v3 二进制容器 → D 真实集成验收`。本轮只改权威计划、专题索引、专题方案、开发日志与接手说明；产品、测试工具、build、浏览器、用户地图、`source/`、Wiki 与根版本改动均为 `0`。

## 2026-08-14：第 333 项阶段 A 完成 canonical 字段注册表与双档审计

- 新增只读 canonical registry v1，覆盖 `24` 个顶层 section、`60` 个字段 / 形状描述；明确 `structured / dense / bitset / CSR / coordinate-pairs / fixed-tuples / string-dictionary / object-table` 编码候选，以及 `replace / ranges / sparse-values / table-rows` patch 边界。它尚未接入现有 Worker、保存或加载行为。
- 首轮 10k 门发现 `features.shore.coastline` 与 `grid.points` 等固定形状不能误归 CSR，同时纠正 `grid.cells.b / pack.cells.b` 为 bitset；一次窄结构诊断后目标复验通过。100k 首门再发现规模放大的 `features.shore.lakeShore`，补为同源固定 `2×2` shore segment 后唯一复验通过，没有继续逐字段追跑。
- 固定 100k 为 `99846` cells，当前 JSON / 去 TypedArray 结构 / 唯一 TypedArray buffer 约 `64.77 / 59.39 / 3.63MB`；`grid.cells.b` 默认值约 `98.78%`、`burg` 空值约 `98.62%`，多个政治 / 人口列默认值约 `59～67%`。大型 grid / pack 邻接、坐标与 shore segment 全部命中紧凑形状，JS holey array 为 `0`。
- 产品注册表 `151` 行、专项工具 `138` 行，符合测试不超过产品增量门。语法、package JSON、差异检查、10k / 100k `audit:canonical-map-fields` 均通过；未启动浏览器、build 或用户地图。阶段 A checkpoint 版本升至 `0.2.10`，下一步只进入阶段 B 版本化副本与增量日志。

## 2026-08-14：第 333 项阶段 B 完成跨任务长期副本与增量 ACK

- 计算 / 显示 Worker 的 retained map 已与 task 名称解耦；同一 binding 可跨重生成、存档导出等 handler 复用。协议新增相邻 revision patch 与 ACK，协调器串行发送连续 patch，Worker 在 idle 副本上原子应用，revision 间隙、错误 ACK、超时和故障均保守失效。
- 主线程 command 以登记 domain 或 Worker domain write set 生成 canonical replace / range patch。普通编辑、撤销 / 重做同步两个副本；Worker 事务的计算副本已经完成写入，只向显示副本发布同一写集，避免重复应用。未知写集和地图替换仍显式清理副本。
- journal / command patch 纯 Node 门和 Worker 十一类完整回归通过；跨 task 只创建一个 Worker，session id 保持且热输入显著少于 fresh，连续两条 patch 严格按 `revision 3→4→5` ACK。生产构建 `1350 modules / 1.65s` 通过。
- 阶段 B checkpoint 版本为 `0.2.11`。产品与测试均未修改 `source/`、Wiki、用户 Chrome 或用户地图；浏览器门留到阶段 C 容器接线冻结后统一执行。

## 2026-08-14：第 333 项阶段 C 完成 `.webfmg v3` 紧凑容器

- 新格式使用固定 magic、容器 / schema 版本、24 个 canonical section 目录、长度和逐分区 checksum；数值列、稀疏列、ragged CSR、对象表、字符串和 TypedArray 使用紧凑编码。Grid / Pack 的冗余 vertex-cell / vertex-neighbor 行由 cell polygon 重建，并以排列码和例外行精确保留原顺序。
- 默认文件、浏览器存储和压缩 API 已切换为 v3 gzip；plain JSON 与旧 gzip 仍可显式导出，既有纯 JSON、旧 gzip、gzip-base64 envelope、File / Blob、LocalStorage / IndexedDB 二进制记录继续读取。分区损坏在替换地图前按目录或 checksum 拒绝。
- 固定 `99846` cells 为 raw `13,197,127B`、gzip `7,410,044B`，低于 `16MiB / 8MiB` 硬门；100k encode 约 `1.90s`，完整 encode / gzip / decode / migrate / deep round-trip 约 `10.78s`，数据、拓扑顺序和 alias 精确同源。Worker I/O、旧格式与旧浏览器存档兼容门通过。
- 本阶段产品 `6` 个文件，新增容器 / codec 产品约 `700` 行；专项新增约 `90` 行并复用既有 Worker I/O 回归。一次把旧浏览器兼容脚本误判为纯 Node 门，隔离测试浏览器运行后已退出且通过，但不计作阶段 D 正式验收。checkpoint 版本为 `0.2.12`，用户 Chrome、用户地图、`source/` 与 Wiki 未改。

## 2026-08-14：第 333 项阶段 D 完成双档真实副本与 v3 存档验收

- 10k / 100k 首次存档输入约为 `200 / 952` 包；同一 Worker 地图副本上的浏览器保存和编辑后再次导出均为 `3` 包。城市改名、撤销、重做发布连续 `0 → 1 → 2 → 3` revision patch 并逐次 ACK，v3 导入后名称与重做结果一致；平移、缩放、悬停和选择窗口没有启动 Worker 地图任务。
- 浏览器默认保存现统一为 IndexedDB 直接二进制；10k 不再经过 base64 和 LocalStorage。100k v3 浏览器实测 raw `12,612,324B`、gzip `7,287,937B`，同源地图为 `99846` cells，低于 `16MiB / 8MiB` 门限。
- 100k 导入 commit 的单次 `67ms` LongTask 已完成两种窄优化对照。单根 staging 无改善；真正分批 DOM 虽把该段降到 `56ms`，却使 reveal 约翻倍至 `488ms` 并出现 `51 / 62ms` 新抖动，因此全部撤销，按用户既定的 `200ms` 内调查后登记规则只登记这一例外。测试没有提高全局阈值，其它保存 / 恢复 / 编解码与 10k 操作仍要求 LongTask 为 `0`。
- 最终阶段 D 产品增量只保留城市 canonical patch 域和默认直接二进制浏览器存储；失败的 overlay 优化没有留在源码。真实浏览器的非性能 health、应用 console / page error、WebGL error 和 Loading 残留均为 `0`，用户 Chrome、用户地图、`source/` 与 Wiki 未改。

## 2026-08-14：第 333 项独立复核关闭副本 checksum 假门

- 独立复核发现正式 command patch 的 `baseChecksum / targetChecksum` 恒为 `null`。现首次完整副本由主线程和 Worker 分别对 canonical 24 分区计算真实 checksum；每次 patch 以旧 checksum 和实际写集独立推进，缺失、漂移或错误 ACK 会销毁会话，下一次请求明确 `reused=false` 并完整重同步。
- checksum 分片计算后，浏览器首轮又暴露城市改名仍克隆八个整表并冻结约 `628ms`。正式对象重命名命令改为只发布目标 city / burg 行，100k 三次 patch 均为单行、revision/checksum 连续 ACK，编辑后存档仍为同 session / 三输入包。
- 精确行修复后 100k warm 导出在重复复验中出现一至两次 `55～57ms`，依用户既定规则精确登记为最多两次且单次 `≤70ms`；同一已调查 prepared-installer 路径在 v3 导入与浏览器恢复的 commit / reveal 窗各精确登记最多一次 `≤70ms`。10k 目标门通过，100k raw / gzip 约 `12.61MB / 7.29MB`，non-performance health、console、page、WebGL 与 Loading 错误为 `0`。

## 2026-08-14：完成权威任务第 333 项——共享 Worker 副本与 `.webfmg v3`

- 主线程与 Worker 独立 canonical checksum、错误 ACK 销毁副本并 fresh 重同步、跨 task 长期副本、精确 city / burg 两行 patch 及 v3 紧凑容器均通过专项。真实 10k / 100k 首次存档为 `200 / 952` 包，三次编辑后存档均为 `3` 包；100k raw / gzip 为 `12,612,332B / 7,287,947B`，旧 v1 / v2、JSON、gzip、gzip-base64 与浏览器存储兼容。
- 独立集成复核关闭 `city.id != burgId` 的真实阻断后给出 `ACCEPT`；最终观察使亲核全部 Node / static / build 与落盘浏览器证据，10k / 100k 三次 patch 均为 `writes=[2,2,2]`，revision / checksum 连续，错误面为 `0`，精确 LongTask 登记外无未归因信号。首页版本为 `0.3.0`，`source/`、Wiki、用户 Chrome 与用户地图未改；第 333 项达到最小验收并进入归档。

## 2026-08-14：登记第 334 项——100k Worker 端到端延迟与画布假死

- 用户确认第 333 项虽然消除了跨任务重复全图输入并缩小存档，但 100k 保存 / 读取、“显示海底”、颜色视图和图层切换仍明显过慢，Worker 运行期间画布也呈冻结观感；明确要求坚持 Worker 化，不开放冲突操作或并行任务积压。
- 当前源码初审表明，多类显示动作统一经过 `runDisplayMutation → applyRuntimeDisplayMutationViaWorker`，先 suspend renderer，再根据宽泛 effect 请求 `render.prepare` 并原子安装；这可能把局部呈现动作扩大为完整渲染事务。既有 100k 读取 artifact 则显示小输入之后仍有约 `4410` 输出包和数秒主线程 decode，保存 / 读取不能与显示事务混为同一瓶颈。
- 第 334 项固定为 `A 端到端证据 → B 显示 effect / renderer suspension → C 存档输出 / 读取 transport → D 双档真实验收`。冲突地图操作继续单 owner 拒绝或串行，Worker-only 计算期间最后已提交画面、RAF / Loading 与事件循环必须保持响应；不以主线程 fallback、自动后台预热、降低质量、删图层或放宽阈值冒充优化。
- 独立分支为 `codex-task-334-worker-end-to-end-latency`，登记版本为 `0.3.1`。本阶段只改权威计划、专题方案、索引、开发日志、接手说明与版本；产品、测试、build、浏览器、用户地图、`source/` 与 Wiki 改动为 `0`。

## 2026-08-14：第 334 项阶段 A 冻结端到端根因

- 100k 冷 archive 代理 wall `32.037s`，旧 telemetry 只覆盖输入 `6.483s`、Worker encode / gzip `1.988s` 和输出 `1.8ms`；剩余 `23.564s` 与源码中主线程发送前、Worker compute 计时前各一次完整 canonical checksum 精确吻合。暖浏览器保存 `1.897s`，其中 Worker v3 encode / gzip `1.875s`，storage 不是主瓶颈。
- 文件导入 / 浏览器恢复 wall `15.137 / 15.362s`：输入仅 `4` 包、约 `7ms`，Worker parse / migrate / 全层准备约 `6.6s`，通用 graph 输出均为 `4410` 包、约 `7.0～7.3s`，主线程 receive / decode `4.6～4.9s` 与其重叠，prepared install 约 `0.8s`。读取应先收敛紧凑 handoff，而非优化 IndexedDB 或再次压缩输入。
- 显示海底开 / 关 wall `669 / 904ms`，Worker compute 仅 `101 / 112ms`，却回传 `446` 包完整 surface；renderer suspend `649 / 887ms`，LongTask 为 `0`。因此“假死”主要来自主动停绘覆盖整个 Worker / output 窗，不是 Worker 在主线程同步计算。颜色视图同样回传完整 surface，point / line 类可见性也会无条件重建。
- 下一阶段只补可闭合的标量 telemetry，并复用既有 harness 做一次 100k 窄诊断；随后按 `显示 effect / 短 commit suspension → stream checksum → 读取 section handoff → 统一副本生命周期` 实施。阶段 A 产品 / 测试 / build / 浏览器增量为 `0`，唯一独立调查角色只读完成，委派角色切换为 `1` 次，等待约数分钟。

## 2026-08-14：第 334 项阶段 B0 冻结唯一 MapWorker 所有权

- 用户明确否决“多个长期完整地图镜像只优化传输”的局部终局。第 334 项现冻结为一个长期 `MapWorker` 持有 canonical map、revision、history、锁、派生缓存和存档状态；主线程只保留 UI / renderer / GPU、picking 紧凑索引与有界 DTO，其它辅助 Worker 不得持有完整地图或提交权。
- 新图与导入须在 owner 内直接生成 / 解析并 adoption；显示、保存、撤销 / 重做共用同一 owner。保存只回传最终 transferable bytes，读取只回传紧凑 section、渲染 buffers和小型投影；普通 `postMessage` 全图深克隆、计算 / 显示双副本、重复 graph encode / decode 和多副本 checksum 均不是合格方案。
- 内部阶段更新为 `B1 标量诊断 → B2 显示事务 → C1 session / checksum → C2 adoption / transport / 主线程投影 → D 双档终验`。B0 产品 / 测试 / build / 浏览器增量为 `0`，主线程唯一写者，委派与等待为 `0`；版本升至 `0.3.3`，下一步只补标量 telemetry 与复用既有 harness 的唯一 100k 窄诊断。

## 2026-08-14：第 334 项阶段 B1 闭合 100k 标量时间轴

- Worker 通用协议新增两侧 checksum、输入 / 输出 decode CPU、ACK wait / post；存档导入新增 parse / render prepare，显示事务新增 apply / Worker / install / commit / resume / suspend 标量。没有保留地图、packet、GPU、DOM 或 heap，也没有新建浏览器夹具。
- 唯一 100k 视图诊断在旧首次 `<30s` 门真实失败：冷首切 `39.853s`，其中主 checksum `10.615s`、输入 `8.997s / 1013包`、Worker checksum `13.199s`、compute `6.078s`；renderer suspend `39.849s`。暖省份为 `833ms`，海底开 / 关 `711 / 657ms`，后两者仍回传 `446` 包完整 surface，suspend `707 / 653ms`。
- 原始证据为 `work/task332-view-switch-100000/raw-result.json`。存档 Node、通用 Worker Node、语法、差异与 build 通过；B1 作为诊断阶段已闭合，但不宣称显示性能通过。版本升至 `0.3.4`，下一步只进入 B2 显示 effect / patch / 短提交窗。

## 2026-08-14：第 334 项阶段 B2 收窄显示挂起并启用 compact surface

- 显示动作改为先 capture 不可变 effect，Worker 准备和临时 GPU / DOM 安装期间继续呈现旧画面；仅 presentation、commit、session 和 resume 位于短 suspend 窗。失败、过期与 detached owner 仍走既有原子清理。
- 颜色视图只回传约 `1.60MB` cell colors；海底只回传 `55022` 个水域 cell、约 `0.88MB` colors，不再回传完整 surface base。主线程在旧画面可绘制时分片复制 CPU surface、构建独立 segmented GPU set，再原子 swap。
- 100k 暖省份约 `291ms`，海底开 / 关约 `444 / 420ms`，挂起约 `9 / 11ms`；B1 对应值为 `833ms`、`711 / 657ms` 与 `829 / 707 / 653ms`。surface output 从 `446` 包降到 `57` 包，海底和颜色入口 LongTask 为 `0`。
- 主题入口经一次 4MiB GPU upload 分片实质优化后仍有 `61 / 57 / 97 / 56ms` 已归因浏览器提交、DOM 和 draw 任务，按用户既定 `200ms` 内规则登记，不再循环微调。Node 专项、10k 真实入口、100k raw artifact、生产构建和错误面通过；版本升至 `0.3.5`，下一步只进入 C1 session / checksum。

## 2026-08-14：第 334 项阶段 C1 合并 Worker session 与 checksum 遍历

- 计算与显示 coordinator 已收敛为同一个长期 `MapWorker` session，兼容字段只指向同一对象；普通编辑 patch 去重为一次，Worker 已原地推进 revision 的事务不再重复 patch，换图和失败只销毁一次共享镜像。
- fresh 输入 checksum 随 graph packet encode / decode 增量生成和验证，主线程发送前与 Worker compute 前两次 canonical 深扫均删除；记录或 checksum 篡改会毒化 decoder，后续 patch 的实际写集复算、连续 ACK、错误销毁和 fresh 重同步保持。
- 100k cold 存档 `952` 包、输入约 `12.9s`，两侧独立 checksum 均为 `0`；warm 两次只发 `3` 包、约 `3～4ms`，同 session / `s1` 保持，改名 / 撤销 / 重做再连续推进到 `r1`。v3、IndexedDB、旧格式导入 / 恢复与错误面通过；已调查的导入 commit LongTask 本轮为 `71ms`，该精确类别登记上界从 `70ms` 收敛到 `80ms`。版本升至 `0.3.6`，下一步只进入 C2 adoption / 紧凑 handoff。

## 2026-08-14：第 334 项阶段 C2-A 生成与导入 adoption

- 新图生成和地图文件导入现直接在唯一 `MapWorker` 中建立 fresh adoption session；结果流 checksum 成为 owner 基线，装载成功后再以新地图 binding 提交。adoption 禁止主线程 fallback，失败或回滚不保留 pending owner。
- 100k 新图后的首次、暖保存和改名后导出均只发 `3` 个输入包；导入 / 浏览器恢复各为 `4` 个小输入包，恢复后首次导出继续复用 adopted owner。Node 协议、11 类 Worker、graph stream、生产构建和功能同源门通过。
- 读取性能尚未通过：导入 / 恢复仍回传 `4410` 包，主线程 decode CPU 约 `10.0 / 11.0s`，单包峰值约 `637 / 622ms`，操作窗最高 LongTask `641 / 626ms`。这些信号不登记放行，下一步 C2-B 只实施紧凑 section handoff 与主线程有界投影。版本升至 `0.3.7`。

## 2026-08-14：第 334 项阶段 C2-B 完成紧凑 handoff

- adopted canonical map 继续留驻唯一 `MapWorker`；正式生成 / 导入结果不再返回完整 `map` 或 `document` 对象图，而是返回 v3 canonical sections 形成的紧凑 handoff 与 prepared render。大 ArrayBuffer 改为 `256KiB` 有序 chunk 协议，section checksum、派生 topology 与主线程 materialize 均分片让步。
- 固定 `99846` cells 的导入 / 浏览器恢复各只输入 `4` 包、输出 `1472` 包；主线程 decode CPU 为 `2.965 / 2.916s`，单包峰值 `16.7 / 15.1ms`，关闭 C2-A 的约 `10～11s` decode 与 `600ms` 级大包阻塞。handoff 异步还原为 `1.967 / 1.599s`，owner、checksum、history、renderer、v3、IndexedDB 与恢复后复用保存保持同源。
- graph / v3 / Worker Node、语法、差异、生产构建与最终 100k 真实门通过，非性能 health、应用 console / page、WebGL、Loading 和普通文案禁词为 `0`。导入 / 恢复各有一条 `55ms` 已登记 commit / reveal 信号；本地兼容保存夹具的 `67 / 597ms` 未作为 handoff 通过，留待 C2-C / D。checkpoint 版本升至 `0.3.8`，下一步只移除主线程完整兼容地图并建立有界投影。

## 2026-08-14：第 334 项修复首次显示重复计算与控件反转

- 用户在 `0.3.8` 预览反馈切换视图、显示海底仍卡且快速切换会出现显示状态反转。生成阶段的 Worker 渲染缓存实际已经存在，但 adoption 提交后仍绑定临时 `generated:*` identity，首次视图因 cache miss 重新计算约 `5.74s`；现在同一 adopted 结果提交时只迁移 cache binding，不复制地图或降低渲染内容。
- UI 显示动作此前仅在 rejection 时从 renderer 回写控件。快速双击会让 busy 的第二次操作先写回旧状态，而第一次成功后不再同步，形成控件 / API 与 renderer 反转；现改为成功或失败都在 finally 收敛到最终 renderer，继续拒绝冲突操作且不建立队列。
- 固定 `99846` cells 的唯一生产浏览器复验中，首次国家视图由约 `6.13s` 降至 `290.4ms`，session / cache 真实复用且只输入 `3` 包；显示海底开 / 关为 `263.1 / 254.0ms`，快速双击后 renderer、复选框和 API 同源。上述窗口 LongTask、非性能 health、应用错误、Loading 残留与 WebGL error 均为 `0`。
- 预览 URL 匿名访问返回 Vercel 登录页，因此没有把受保护页面冒充线上验收。语法、差异、Worker 十一类、API action convergence、生产构建与唯一 100k 目标门通过；版本升至 `0.3.9`，下一步仍只进入 C2-C 主线程有界投影。

## 2026-08-14：第 334 项 C2-C 移除整容器拼接副本

- adoption handoff 的主线程 materialize 不再先分配并拼接一份完整连续 v3 容器；现直接从 chunks 读取 header / 目录，按 canonical section 校验、解码和恢复派生 topology，并在最后一个消费者结束后清空对应 chunk 引用。
- 成功、截断与 checksum 失败路径都释放全部 handoff 引用；旧同步 / 单 buffer 入口保持。10k v3 的 `10004` cells、24 sections、raw `2,496,662B` / gzip `1,172,097B` 往返、非整齐 chunk 边界、截断与故障清理通过，Worker 十一类与 session / cancel 专项通过。
- 本 checkpoint 只降低主线程读取 / 新图 adoption 的瞬时内存峰值，尚未移除最终完整兼容 map，不作为 C2-C 完成。产品 `2` 文件、既有 Node 工具 `1` 文件，新浏览器夹具、浏览器运行、委派与等待均为 `0`；版本升至 `0.3.10`，下一步继续迁移有界投影消费面。

## 2026-08-14：第 334 项 C2-C 惰性派生 pack 列

- adoption 主线程投影现识别 `pack.cells.temp / prec`：只有它们与 grid 原列及 pack→grid 映射逐项完全同源时，才释放重复数组并安装可枚举、可写的惰性属性；首次兼容读取会精确重建，差异旧图保持原值。
- 100k 的两份各约 `99846` 项派生数组因此不再默认长期驻留，字段、公开行为、v3 与旧同步入口不变。10k / 100k v3 往返、非同源反例和成功 / 故障 handoff 清理通过；100k raw / gzip 为 `13,197,139 / 7,410,037B`。
- 本 checkpoint 仍未移除主线程完整 map。产品 `2` 文件（含一个新模块）、既有 Node 工具 `1` 文件，浏览器、用户 5410、专用浏览器夹具、委派与等待均为 `0`；版本升至 `0.3.11`，下一步继续迁移 UI / 编辑 / renderer 有界投影。

## 2026-08-14：第 334 项修复视图高亮错位与 100k surface 重建

- 5410 现场确认视图 bridge / renderer 为国家时，组件可能仍保留失败请求留下的政体高亮；Element Plus 额外子节点也令四个视图快捷键 selector 整体错位。data-mode 现只显示父级已提交值，快捷键改用同类型子项定位，API schema 同步正式复数模式。
- 100k 颜色视图虽只从 Worker 接收约 `1.60MB` RGBA，主线程此前仍复制并重建约 `105MB` surface base。现单层 cell-colors 使用既有 segmented buffers 的短暂停绘原位事务，紧凑保存旧颜色，成功保留全部 base identity，失败恢复 CPU / GPU 字节且不覆写新 owner；其它安装路径不变。
- 10k 与 100k 专用真实入口通过；100k 国家 / 省份 / 生物群系 / 人口 / 高度为 `235.9 / 256.9 / 221.7 / 142.0 / 142.3ms` 且 LongTask 全 `0`。用户 5410 原图不刷新、不重新生成，政体 / 国家两次点击均让 visible Tab 与 hidden bridge 同源；debug 页同阶段重复进度改为 `80ms` 节流，现场 health 首阶段至 success 约 `245 / 213ms`。一次不成立的旧岸线 range 复用已在首败后撤销。版本升至 `0.3.12`，第 334 项仍进行中。

## 2026-08-14：第 334 项修复相机竞争导致的首次视图失效

- 5410 原图现场证明旧链会在 Worker 提交前先点亮目标 Tab / bridge，画布仍保持旧 renderer；等待窗内平移或缩放又会因 render context token 包含 camera / canvas 而把纯 surface 颜色结果判过期，叠加形成“第一次切换失效、之后显示反转”的观感。
- 控件现只在正式显示提交后从 renderer 收敛；失败仍恢复旧状态。纯 surface 准备不再受 camera / canvas 漂移影响，其它会依赖相机的标签、主题和路线准备仍保留完整过期校验。可见 segmented 只经一个 hidden bridge 进入运行时，不再形成双重意图。
- 10k 生产入口的国家切换为 `75.9ms`，Worker 输入 `3` 包、挂起 `11.5ms`、LongTask `0`；请求未提交时三套状态均为高度，真实 wheel 改变相机后最终严格收敛为国家 / states。health、console / page、WebGL 和 Loading 错误为 `0`。产品 `2` 文件、工具 `3` 文件、浏览器工具 `372` 行、委派等待 `0`；版本升至 `0.3.13`，第 334 项继续 C2-C。

## 2026-08-14：纠正第 334 项视图桥热更新漏绑

- 用户指出现场仍未修复后，重新接管原 5410 标签执行人口→国家→平移→缩放→政体→国家序列，并对比实际国家 / 政体画面。进一步静审发现 `panel.js` 仍只给启动时的隐藏桥按钮逐个绑定监听，且点击阶段提前激活；这条遗漏链会在 Vue 热更新换节点后直接失效，也保留未提交状态抢跑。
- 改为文档级事件委托并删除点击时提前激活，提交 / 回滚继续由运行时权威同步。专项新增委托、无逐节点监听和无提前 active 三项反例；版本升至 `0.3.14`。本轮不修改地图、Worker 协议、渲染算法或 `source/`。

## 2026-08-14：再次纠正第 334 项首次视图切换落后一帧

- 用户指出此前修复仍无效后，获准直接硬刷新其 5410 原标签页。真实画布证明首次点击后 Tab、bridge、renderer 与事务均已切换，但 GPU surface 仍显示旧视图；连续点击会让画面始终落后一次，平移 / 缩放后因此呈现“反转”。此前验收只看控件与标量状态，没有检查硬刷新后首次真实点击的 WebGL 画面，结论不成立。
- 根因是 deferred render request 忽略已捕获的 `snapshot.finalPresentation`，仍用旧 renderer 展示字段让 Worker 计算 compact colors。现请求在基础相机 / 选择上下文之上，以目标快照覆盖颜色、主题、视图选项、可见性、标签、单位、政治调试和洋流高亮；静态门阻止这些字段再次退回旧 renderer。
- 同一 5410 标签页已通过“硬刷新后的第一次国家→高度、再切国家、真实 wheel 缩放”实际画布复验，三次呈现均与高亮一致；临时日志已删除。版本升至 `0.3.15`，第 334 项继续 C2-C，不据此宣称整项完成。
- 修复后在用户原 `5410/?debug=1` 标签以真实鼠标完成国家、画布平移、缩放、政体、国家序列；Tab / 隐藏桥 / 政体图例和两类实际配色逐步同源。Chrome 原生滚轮注入命令的一次超时单列为工具问题，随后用画布正式 wheel 处理器完成相机变化；未刷新或重生成用户地图。
