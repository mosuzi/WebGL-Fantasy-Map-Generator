# 应用图标 A 与画布图标系统施工图

## 来源与目标

用户从多版应用图标中选择 A 方案，并要求进一步整理所有画布内图标：当前相似的城镇等级需要明显区分，资源点等其它语义图标也要有辨识度、彼此好区分且风格统一。第 265 项进一步明确大型城市必须直接采用 A 方案圆盘内的环城城池语言，其它城镇基础剪影与之统一；应用图标只保留补全后的中央圆形地图，外部方框和朱红方印均删除。本专题只服务权威任务第 263～265 项，不建立额外待办。

## 调查结论

### 应用图标链

正式链由用户原图母版 `app/webgl-generator/public/app-icon-master.jpg`、自包含母版的 `app-icon.svg`、六份派生 PNG、`index.html` 和 `site.webmanifest` 组成。生成脚本覆盖 `32 / 64 / 180 / 192 / 256 / 512px`，入口和文件名保持；静态回归冻结母版摘要并解码 SVG 内嵌图像逐字节比对。

A 方案以用户提供的 `1280×1280` JPEG 为唯一视觉真值：海岸比例、双峰、分叉河流、环城虚线路线、圆形城池、树林、旧纸纹理、墨绿边框与朱红方印均不得重画或简化。正式资源只允许等比缩放和格式封装；manifest 使用 `purpose: any`，避免系统 maskable 裁切完整方形构图。

第 265 项保留上述 JPEG 作为原始来源，不覆盖或重编码；用户明确撤销“外框与朱印必须保留”的旧构图要求。正式资源改从新增透明圆形派生母版生成：删除外部墨绿方框和朱红方印，补全圆盘外缘、纸面与附近地图内容。首轮机械拼接稿在右下出现色阶接缝并被独立审查阻断，因此最终资源采用以原图为唯一参考的完整圆盘高保真派生稿；原始 JPEG 继续冻结身份与构图来源，但不再宣称派生稿未遮挡区域与原图逐像素一致。

### 画布图标分母

正式重设计分母为 `80` 个稳定图形键：

- 城镇 `9 + 3`：`capital / provincial / port / city / town / village / hamlet / fort / camp` 九个基础剪影，加首都、省会、港口三个附加徽记。自动人口等级只有 `city / town / village / hamlet`。
- Marker `58`：自然 `4`、资源 `26`、水文 `1`、设施 `3`、商旅 `2`、危险 `7`、异象 `5`、文化 `8`、活动 `2`。
- 军事 `10`：`fleet-large / fleet-small / archers / archers-heavy / cavalry / cavalry-heavy / infantry / infantry-heavy / mountain / artillery`。

当前 `58` 个 Marker 只复用 `16` 个内部 symbol，且全部套同一 pin 外框；`life` 被 `13` 种类型共用，`danger` 被 `8` 种类型共用，是资源与地标难以区分的直接原因。军事十种图已有语义差异，但高细节 PNG 被压缩到 `20px` 观察尺寸，与城镇和 Marker 的扁平制图语言不一致。

战线箭头、洋流箭头和三类远景圆点只检查统一描边、红蓝 / 水系语义与 LOD 过渡。选择圆环、测量点、诊断 cell、编辑手柄、标签、HUD、图例、比例尺、边界与普通线层不进入图标重画。

## 视觉语法

### 城镇

- 村落：单体小屋，外框最窄。
- 村庄：两栋错落民居，横向展开。
- 城镇：民居围绕明确的中心建筑或街道，形成中等密度轮廓。
- 城市：城墙、城门与塔楼群形成连续天际线，不能只靠增加一栋房表达。
- 港镇、寨堡、营帐等手工剪影保留独立结构；文化风格只改变同等级建筑语言和配色。
- 首都、省会、港口继续使用可组合角部徽记，不参与人口等级与基础尺寸计算。

### Marker 与资源点

采用三层语言：底牌保持统一地图点位锚定，外框和类别色表达九个家族，内部轮廓表达具体类型。`58 / 58` 注册类型必须有显式图形键；同一类别内不能再靠通用叶子、警告三角、星星或镐头覆盖多个对象。受控 SVG 不包含文字、emoji、外部图片或任意 HTML。

### 军事

十个资产键和旧字符别名保持不变；在现有军团标签中统一观察尺寸、轮廓粗细、留白和明暗。步兵 / 弓兵 / 骑兵 / 器械 / 舰队 / 山地兵使用不同一级轮廓，重装与大小舰队使用稳定的二级标识，不改变军团生成和判定。

## 单一来源与旧数据

新增画布图标注册表并从中派生 renderer 和 Marker 管理面板选项。自动 Marker 以 `type` 解析新默认；旧图中 `manual !== true` 的老 symbol 只作历史字段，不阻止视觉升级。手工 Marker 保留旧 `16` 个 symbol 和安全 fallback，九个城市手工剪影、十个军事键及其旧字符别名继续可读。

本批不迁移或重写存档，不改变对象 ID、人口、角色、资源经济、生成概率、picking、碰撞、图层开关、公开 API 或 GeoJSON。DOM 与 PNG 继续消费同一 SVG / 图像来源，调整尺寸时同步碰撞盒和标签净空。

## 验收矩阵

- 静态分母：`9 + 3 + 58 + 10 = 80`，注册表、生成器、renderer 与面板双向差集为 `0`。
- 图形完整性：全部注册类型可渲染；资源点、同类别 Marker 和城市四级基础轮廓没有意外重复指纹；无文字、emoji、字体或外部网络资源。
- 兼容：旧自动 / 手工 Marker、城市手工视觉、军事旧别名、JSON、gzip 与浏览器缓存保持；地图 checksum、历史和对象语义不因纯渲染升级改变。
- 视觉图鉴：九个城镇剪影、三角色及组合、六种文化风格、58 个 Marker、十种军事图标、远景圆点、战线和洋流方向符号。
- 真实行为：阈值、选中、城市—资源重叠弱化、军事—城市标签避让、图层隐藏、picking、DOM / PNG 同源均保持。
- 浏览器：系统 Chrome 覆盖 `1440 / 390 / 320px` 和一张真实 `10k` 地图；application console、page、health 与 WebGL error 为 `0`。

## 排除

不修改 `source/`，不新增当前不存在的画布对象图标，不重做文字、标签、线层、面板信息架构或导出选项，不改地图数据和生成语义，不提交或推送。

## 第 265 项返工约束

- 应用图标原始 JPEG 继续冻结摘要；圆形派生母版另行冻结尺寸、格式与摘要，四角必须透明，圆盘外不得残留墨绿方框或朱印红块。
- 城市一级轮廓固定为椭圆环城墙、正面城门、中央尖顶高塔和墙内屋舍；其它八个基础键复用同一线宽、墙体、屋顶、轻俯视与留白语言，但保留单屋、村庄、城镇、都城、省会、港口、寨堡和营帐的语义差异。
- 优先保持既有 `34×26` 观察盒、锚点、碰撞盒与标签净空。人口阈值、P90、角色徽记、文化调色、手工视觉、旧图格式与地图数据不得随纯视觉返工改变。
- 验收除原有注册表、图鉴和真实地图矩阵外，新增圆形透明度、红色残留、右下补全接缝、城市城墙 / 城门 / 高塔 / 屋舍结构和 favicon 缓存后的实际资源字节检查。

## 第 276 项俯视返工约束

- 用户从三套概念稿中确认试用方案 C：保留暖墙体、深屋顶、厚墨线和轻微手绘不规则感，但所有基础剪影统一为严格俯视的聚落平面，不再使用正面门洞、立面窗、竖直塔楼或尖顶表达等级。
- 四个人口等级以围合程度和密度递进：村落为单屋与开放院篱，村庄为多屋与断续木栅，城镇为局部城墙和城门缺口，大城为完整墙带、多个城门缺口、墙内道路 / 院落与屋顶平面。首都、省会、港口、寨堡和营帐继续以宫城院落、官署院落、临水开口、棱堡和营栅保持一级轮廓差异。
- 概念稿中的树木、密集街巷和大量小屋不得机械照搬；正式图标优先保证 `34×26`、角色徽记叠加和真实地图缩放下的辨识度。人口阈值、角色组合、文化配色、碰撞盒、标签净空、存档兼容与 DOM / PNG 同源契约保持。

## 完成结果

- A 方案原始 JPEG 与用户附件均为 `103496 bytes`，SHA-256 同为 `50c20b1e8d908b0a45e91988e5421a0792d9f4570db7a504de91205851ab771d`，第 265 项未覆盖它。当前 SVG 改为内嵌 `1024×1024` RGBA 透明圆形派生母版，摘要为 `26a13c843177d3c094b8e3b7b4cc2f9b8364e20e0e07b2d4e14e99925e1ededb`；六档 PNG 由该母版确定性缩放生成，外部方框和朱印均为零残留。
- 画布注册表固定 `9 + 3 + 58 + 10 = 80` 个稳定键；Marker、四级自动城镇、军事结构指纹为 `58 / 4 / 10`，资源点 `26`，九类底牌均有不同一级轮廓。
- 首轮复核阻断的自动 Marker 面板旧 symbol、角色组合、六文化和军事正式样式覆盖均已限修，原审查者终验为 `ACCEPT`。
- 系统 Chrome 图鉴在 `1440 / 390 / 320px` 均为 `80` 个稳定卡加 `7` 个附加展示卡，无裁切或横向溢出。真实 `10004` grid cells 地图模型 / DOM 同为城镇 `839`、Marker `44`、军事 `114`；Marker 拾取、PNG 同源和 checksum / revision 不变通过，console、page、health 与 WebGL error 为 `0`。
- 第 265 项重画九个基础城镇剪影并保持原键、`34×26` 观察盒、角色组合与兼容链；九个几何指纹为 `9 / 9`，大型城市的双闭合环墙、正门、中央尖顶高塔和两组墙内屋舍均有直接 path 门禁。首轮独立审查阻断圆盘接缝、缓存回归和元数据伪指纹后，三项均已限修，原审查者与终轮观察使均为 `ACCEPT`。
- 生产预览会先预热 favicon 缓存再重载，SVG、32px、64px、Apple Touch Icon 与 manifest 的实际响应字节均匹配当前磁盘资源；实际三份 favicon 四角透明、朱印红色像素为 `0`，圆环连续性最大相邻色差为 `49.74`。生产构建、PNG crop、三档 Chrome 图鉴和真实 `10k` 地图再次通过，本批未提交或推送。

## 第 276 项完成结果

- 第 265 项的轻俯视建筑立面已由第 276 项覆盖为严格俯视聚落平面；九个基础键和 `34×26` 观察盒保持，统一登记 `c-top-down`。城镇与大城分别使用明显开放的半围城垣和完整四门城垣，首都 / 省会只通过内部院落分化，港口、寨堡与营帐保持临水开口、棱堡和营栅轮廓。
- 静态门禁直接拒绝正面门洞、立面窗、竖直塔楼、尖顶、帆面和直立旗帜等旧结构；大城固定四段墙体、四门缺口、道路骨架、六组屋顶平面和中央院落。九个基础几何指纹为 `9 / 9`，四个人口等级为 `4 / 4`。
- 系统 Chrome 图鉴覆盖 `1440 / 390 / 320px` 与四组角色组合；真实约 `10k` 地图固定 `839 / 839` 俯视城镇和 `430` 个角色徽记，大城定位、拾取、选中墙带、DOM / PNG、checksum / revision 与错误门禁通过。既有全图标浏览器回归的 Marker 定位等待超时在本项前已复现，本项由城镇定向浏览器链独立收口。

## 第 277 项方案 B 返工约束

- 用户实测方案 C 在真实地图上过密，改用方案 B 的稀疏符号语言。保留暖墙体、深屋顶、厚墨线和严格俯视，删除密集街巷、重复屋顶与多重院落；每类只保留一个一级轮廓和少量能解释语义的内部记号。
- 四个人口等级不得复用同一种圆形或方形底牌：村落使用开放折线院篱，村庄使用三角形聚落轮廓，城镇使用盾形城垣，大城才使用完整圆环城墙。首都、省会、港口、寨堡、营帐分别使用八角宫城、削角长院、朝水面开放的马蹄港湾、四角棱堡和断续营栅，九类一级轮廓的实际路径指纹必须全部唯一。
- SVG 继续使用 `34×26` 观察盒以保持注册表与导出兼容，画布 CSS 盒、碰撞盒和标签净空同步缩小。相机相关尺寸公式拆成“人口等级基准 × 共享相机倍率”；交互期只更新 overlay 上的共享 CSS 变量，让所有城镇在同一帧得到正确尺寸，禁止每次滚轮遍历数百个城镇节点。
- 验收必须分别量测缩放事件、首个 animation frame 和 viewport commit 后的城镇实际矩形；不得只凭 CSS 属性或最终截图宣称低延迟。人口、角色、文化、手工剪影、地图数据与导出语义保持。

## 第 277 项完成结果

- 九类基础 SVG 统一为 `b-distinct-top-down`，一级主体路径按开放院篱、三角村落、盾形城镇、圆环大城、八角宫城、削角长院、马蹄港湾、四角棱堡和断续椭圆营栅分化；路径指纹 `9 / 9`，人口四级 `4 / 4`。每枚基础图标只有 `4～5` 个可绘制元素，港口的水纹与朝水面开口在正式小尺寸仍能辨认。
- SVG 观察盒保持 `34×26`；正式 CSS 盒、碰撞盒和标签高度同步改为 `30×23`，人口等级基准为 `0.69 / 0.75 / 0.81 / 0.87`，共享相机倍率在 `1～1.22` 内缓增，缩放 `1` 时四级实际宽度为 `20.7 / 22.5 / 24.3 / 26.1px`。标签继续按同一公式保持既有 `2px` 净空。
- 交互预览通过 stage 上单个 `--city-icon-preview-scale` 组合根相机矩阵和目标相机倍率；`839` 个节点在滚轮期不逐个改写。生产 Chrome 相机倍率从 `1` 到 `1.24608` 时，大城宽度由 `26.09998px` 在首个 animation frame 到 `26.32483px`，commit 后为 `26.32477px`，误差 `0.00006px`；基线约 `6.9px` 的提交回弹消失。
- 生产 Chrome 覆盖 `839 / 839` 方案 B SVG、`430` 个角色徽记、真实城镇拾取、三档视口、viewport 道路 / 河流预览和六态 PNG crop；checksum / revision 不变，console、page、health 与 WebGL error 为 `0`。静态兼容、生产构建和差异检查通过，本项未暂存、提交或推送。

## 第 278 项极简方案与平移闪烁约束

- 用户确认采用极简方案 A：村落为小圆环、村镇为三角、城镇为菱形、大城为双环；港口改用方案 B 的断口马蹄，首都改用五角星。所有基础图形空心、无底牌、无内部建筑细节，每枚仅 `1～2` 个可绘制元素；省会、寨堡和营帐也必须按同一极简程度收敛。
- 角色主体已经表达首都 / 省会 / 港口时，过滤同名角色徽记；只为同一城镇的其它附加角色保留徽记。此调整只去掉重复表达，不修改 `roles`、手工视觉或存档数据。
- 真实 Chrome 已在修复前复现平移闪烁：一次水平平移使 `5` 个原可见城镇转为隐藏，commit 首帧中它们仍以 `opacity=1 / visibility=visible` 停在旧坐标，横向偏离当前投影约 `518.36～518.44px`。根因是 `updateCityIcons` 只给最终可见节点写位置，随后 CSS 在旧坐标执行 `130ms` 淡出。
- 修复只更新“最终可见或刚从可见转隐藏”的节点，持续隐藏节点不写位置；验收需用真实中键平移在 commit 首帧读取 DOM 坐标、世界投影、opacity 与 visibility，并在淡出结束后复查。

## 第 279 项 WebGL 实例图标与平移边缘约束

- 用户进一步确认正式实施 WebGL 实例化绘制、连续尺寸函数和平滑显隐；第 278 项尚未提交的 HTML/SVG 位置修补只作诊断证据，不作为最终架构。正式地图须移除城镇 DOM 节点，保留 SVG 注册表仅供图鉴和手工视觉选项预览。
- 独立城镇 program 以单批实例绘制九类解析式空心符号。世界中心直接乘当前相机，屏幕尺寸由共享连续函数求值；缩放帧只改变 uniform，阈值用解析 alpha 过渡。白色内线与深色硬外轮廓在同一距离场完成，不允许 CSS / canvas 模糊阴影。
- 现有拾取本来就从城市空间索引读取，不依赖 `pointer-events:none` 的 SVG；迁移必须继续维护 `cityIconItems[].visible`、标签净空和选择 fallback。PNG framebuffer 会自然包含图标，但必须为 `overlays.cityIcons=false` 提供只跳过导出帧批次的兼容路径，禁止二次合成 SVG。
- `1440×900 / DPR 1.5` 的只读复现表明：中键横移约 `518.4 CSS px` 时，canvas 为 `transform:none / filter:none` 并每帧按新相机重绘；主模糊来自七个新进入城市标签等到 `120ms` commit 才 `hidden → visible`，随后约 `20 / 50 / 100 / 140ms` 的 opacity 分别约为 `0.20 / 0.73 / 0.90 / 1`。标签必须在视口外预布局，进入边缘时已经是最终 alpha。
- 道路 / 河流次因是 screen-space mesh 只缓存旧视口外 `96 backing px`，在 DPR `1.5` 下约为 `64 CSS px`；同一次 `518px` 平移会留下约 `454px` 新边缘没有旧缓存细节，停手后才重建。新 overscan 必须按 canvas backing size 与 DPR 自适应并覆盖该拖动，同时保持旧 buffer camera 变换、异步取消、线宽和 LOD 契约。

## 第 279 项完成结果

- 正式地图不再创建城镇 HTML/SVG 节点。`city-icon-layer.js` 以一份实例 buffer 保存世界坐标、九类图形、人口等级、显隐插值、角色 bit 与选中态；真实约 `10k` 地图的 `839` 个城镇每帧只调用一次 `drawArraysInstanced`，相机缩放和平移只改 uniform，模型上传计数稳定为 `1`。
- 九类主体固定为小圆环、三角、菱形、双环、五角星、六边形、断口马蹄、方框和帐篷；自动首都 / 省会 / 港口直接使用对应主体，同城其它角色才以小型附加符号叠加。解析式距离场用 `fwidth` 抗锯齿，纯白内线外加深色硬描边，选中时内线改为金色；没有模糊阴影或国家底色调色。
- 相机尺寸为人口等级基准乘连续饱和函数，shader 的缩放阈值与碰撞目标分别用 `smoothstep` 和 `150ms` 插值。生产 Chrome 单次缩放在首个 animation frame 即改变尺寸，提交前后不发生阶梯回弹；图层关闭时实例绘制为 `0`，恢复后回到 `839`，选择与图层交互不重建模型 buffer。
- HTML 标签在最大 `720 CSS px` 的视口外预布局，进入边缘前已具有最终 alpha；overlay 实际矩阵同步使用设备像素对齐后的平移值。定向平移的 `17` 个预热标签中有 `10` 个进入首帧直接为约 `0.90` opacity，滤镜为 `none`。道路 / 河流 mesh 的交互 overscan 改为最大视口边的 `0.5`，限制在 `256～720 CSS px` 后按 DPR 换算；生产 Chrome 平移预览 `12 / 12` 帧均保留两层，平均 `1.02ms`、P95 `1.7ms`。
- PNG 直接读取同一 WebGL framebuffer；`cityIcons:false` 只在导出帧跳过城镇批次并立即恢复，开 / 关实际像素不同。注册表、城市规模、标签净空、选择、PNG、viewport、三档视口、约 `10k` 地图和生产构建通过，checksum / revision 与四类错误均保持，`source/` 未改。
