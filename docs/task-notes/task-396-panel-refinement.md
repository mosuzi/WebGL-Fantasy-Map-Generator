# 第 396 项：面板精简清单

本记录是实施边界与旧新对应；最终状态只见当前计划，浏览器检查在批末统一执行。

## 共用动作对应

| 原入口 | 保留入口 | 回调与范围 |
| --- | --- | --- |
| 多个导入图标 | 一个导入菜单，逐项选择原格式 | 原 import action key、文件 input、accept 与禁用条件不变 |
| 散排导入 / 导出 | 文件操作按钮组 | 原 export / import 回调不变 |
| 散排列表安全操作 | 列表操作按钮组 | 原 action key、中文可访问名称、选中反馈保留 |
| 仅图标的删除 / 重生成 | 图标与明确动作文字 | 原危险操作、确认与锁范围不变 |
| 对象详情“定位”文字 | 带中文提示的定位图标 | 原 onLocate 不变 |
| 高度调试区撤销 / 重做 | 现有标题栏历史按钮 | 两处均调用同一 onUndo / onRedo，删除内容区重复入口 |
| 城市属性后的固定更新提示 | 第 390 项关联更新说明 | 属性动作、人口控件保留；移除重复通用说明 |

## 全面板静态覆盖

表内“共用”包括浅色壳、字段、表格、状态、外置菜单 / 二级浮层、32px 命中区、6～8px 行内间距、12px 分组间距与可换行控件。既有专用业务动作均保留。

| 面板 | 动作与布局处理 |
| --- | --- |
| Height | 共用；标题栏历史收敛，保留笔刷、预检和重建区别 |
| Climate | 共用；依赖重建仍保留明确文字和作用域 |
| Biome | 共用；图标编辑 dock 与列表操作分组 |
| Feature | 共用；影响预检与正式应用分开 |
| Lake | 共用；局部修正点、确认与取消保留 |
| River | 共用；组件内错误 / 预览颜色同步浅色，控制点操作保留 |
| OceanCurrent | 共用；列表图标组与属性字段换行 |
| State | 共用；名称库批改、重生成、政体、省份后缀不混合 |
| Province | 共用；所属调整与疆域操作保持独立 |
| Government | 共用；预览、应用与批量范围保留 |
| City | 共用；六属性图标组保持，重复说明收敛 |
| Population | 共用；守恒转移和数值调整保持明确文字 |
| Culture | 共用；列表和对象操作分别分组 |
| Religion | 共用；列表和对象操作分别分组 |
| Route | 共用；道路列表动作聚合，改线点模式与清除区别保留 |
| Economy | 共用；商品、市场、交易操作分域，诊断说明完整换行 |
| Military | 共用；战斗、驻扎、事件删除保留语义与确认 |
| Diplomacy | 共用；关系选择保持直接提交规则和状态说明 |
| LabelNaming | 共用；独立标签 / 自动标签不同动作仍区分 |
| Marker | 共用；资源与普通标记操作不混用 |
| Zone | 共用；纹理预览原色保护，参数字段允许换行 |
| Notes | 共用；导入格式聚合，孤儿抢救和正文保持 |
| Namebase | 共用；多个导入格式聚合，用户库与内置库权限保持 |
| Emblem | 共用；纹章预览色彩保护，仅编辑壳改变 |
| Measurement | 共用；测量类型与结果操作保持 |
| Control | 共用；原顶层 / 二级标签规则保持，搜索与预设接入 |
| ObjectDetails | 共用；定位图标化，其它歧义动作保留文字 |
| CloudStorage | 仅共用壳、列表、菜单和确认弹层；不改上传逻辑 |

## 样式边界与验收

- `panel-theme.css` 是界面语义颜色及统一尺寸入口，独立于地图 theme；已将面板静态硬编码色值迁移为语义变量。迁移脚本排除地图覆盖层、图标、标签、图例、纹章和配色预览。
- 本项静态构建通过；桌面 / 690px / 200% 缩放、真实动作与弹层、旧图及 100k 检查尚待批末执行，不以此表冒充视觉已验收。
