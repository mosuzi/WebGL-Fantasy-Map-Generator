# 第 327 项精简四级流程 dry run

本记录验证四级职责只在整个权威编号执行一次，不向内部阶段、夹具或机械修正递归。

## 冻结任务包

| 字段 | 内容 |
| --- | --- |
| 权威目标 | 收敛 AI 上下文、文档路由、开发日志、阶段 handoff 与两个通用 Skill |
| 唯一写者 | 主线程 |
| 允许范围 | 根说明、`docs/` 路由 / 历史 / 专题、治理工具、`package.json`、个人 Skill |
| 禁止范围 | `app/` 产品、生成算法、存档、API、`source/`、Wiki、浏览器 |
| 首个廉价门 | `node --check tools/context-governance-audit.mjs` |
| 停止条件 | 历史段落缺失、相对链接失效、预算不达标、范围外 diff、重复阻断 |

## 角色记录

- 调查：独立只读角色，`fork_turns: "none"`，未派生子智能体；量化自动注入、日志日期结构、路由和 Skill 重复，交付阶段边界与不可做事项。
- 实现：主线程作为唯一写者；内部阶段没有重复启动调查、复核或观察使。
- 集成复核：独立只读角色已在冻结树上完成，结论为 `ACCEPT`；未编辑文件、未派生角色，也未扩展到产品或浏览器。
- 最终验收：另一个独立只读角色按全新接手链执行，结论为 `ACCEPT`；预算、日志迁移、Skill 引用、归档检查、范围隔离和定向历史检索均通过，未运行产品构建或浏览器。

## 当前证据

- 改造前 / 后预算：[`task-327-context-budget-before.md`](./task-327-context-budget-before.md)、[`task-327-context-budget-after.md`](./task-327-context-budget-after.md)。
- 开发日志迁移：`1,017 / 1,017` 个历史日期段落保留；新增本项记录后为 `1,018` 个段落。
- 当前廉价门：上下文审计、日志分卷审计、归档检查、入口相对链接和 `git diff --check` 已通过。
- 详细命令输出留在当前任务 artifact / 终端，不复制进本文。
