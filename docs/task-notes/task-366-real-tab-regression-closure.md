# 第 366 项：真实标签页三项回归闭环

## 任务契约

- 唯一存档为 `C:\Users\mosuzi\Downloads\krichars (3).webfmg`，SHA-256 `CF7402BC2BEA22AD1FCDE441444479F880DC0DB15D55520EF5A1A399D335DA61`。
- 唯一产品现场为用户原 Chrome 标签页；随机图、另开替代页、只读旧截图和未核对资源版本的旧页面均不得作为完成证据。
- 任务分支为 `codex/task-366-real-tab-regression-closure`；主线程唯一写者。

## 已冻结的重新调查结论

1. 用户原 `http://127.0.0.1:5413/?debug=1&loadTrace=1&healthClear=1` 标签页保留了旧页面内存，但 preview 已被新 build 替换。点击路线管理直接失败于缺失的旧散列模块 `RoutePanel-CwCX6TTB.js`，因此该页无法证明当前道路算法失败。
2. 同一标签页的“地图边缘渐隐”实际为开启；关闭后用户截图中的整图外框虚化立即消失。当前新 origin 默认关闭，但验收遗留偏好会按 origin 持久化。
3. 刷新到 `0.5.74`、重新导入指定存档后，地图身份为 `100000 grid / 43419 pack / 1251 cities / 442 routes / 7976 segments`，完整画面正确，加载到可交互为 `4354ms`。主要墙钟为并行解析 / 首屏准备 `3518.4ms`，安装 `512.2ms`。
4. 同页 UI 正式道路重算成功为 `442 -> 438 / 7976 -> 7821`；指定存档专项数据复核为西陆 `108` 条道路、`1440` 个道路 cell、触达 `203 / 350` 城市与 `6 / 6` 首都，陆路水域 cell `0`。全图可见西陆道路网络；验收后已撤销恢复 `442`。

## 实施边界

- 优化真实导入关键路径，不降低地图精度、不删首屏可见层、不延后已定义的可交互能力。
- 明确区分单元格 / 海岸 / 行政线的“平滑边界”和整张地图外框的柔化效果；控件必须可发现且说明二者无关。
- 验收运行期间不得 build 覆盖正在使用的 preview 资源；必须使用稳定开发服务器，或在重新验收前显式刷新并重新导入指定存档。
- 不再次改写已经通过当前完整页面和指定存档数据门的道路生成算法；若后续真实 UI 结果与冻结数据门冲突，再冻结并重新归因。

## 已完成修复

1. 海岸拓扑的最近线段查询改为数值分桶、代际访问标记和平方距离比较，移除每个查询的字符串 key、`Set` 和中间投影对象；指定存档单次 shore topology build 从约 `1.08～1.14s` 降至约 `0.74～0.82s`，拓扑摘要仍为 `79` arcs、`52` smoothed、`23` fallback、`5830 → 5665` points，保护对象仍为 `1251 / 442 / 1528`。
2. 首屏资源安装使用 `16ms / 1MiB` 分片；开发模式加载账本补齐 canonical main、主 / 次 Worker 的 parse / render 分段，便于确认剩余瓶颈而不再只看总墙钟。
3. `mapEdgeFade` 在 renderer、prepared display intent、运行时恢复和调试摘要中统一为“只有显式 `true` 才开启”；旧缺省值不再制造整图外框虚化。
4. 视图 UI 将原“平滑边界”明确命名为“疆界柔化（海岸/行政）”，并与“整图外框柔化”并列；说明文字明确前者处理海岸 / 行政边界，后者只处理整张地图四周渐隐。调试面板同步显示两个独立状态。

## 最终验收

1. 用户原 Chrome 标签页 `294257337` 刷新到 `v0.5.75` 正式构建后，五轮指定存档冷导入为 `3949 / 3802 / 3985 / 4068 / 3991ms`，中位 `3985ms`、最大 `4068ms`；相对同页修复前 `4354ms` 分别下降 `8.5% / 6.6%`。五轮均为 `100000 grid / 43419 pack / 1251 cities / 442 routes / 7976 segments`，完整彩色画面、标签和道路均存在。
2. 当前动态路线资源 `RoutePanel-DkL97aJR.js` 可正常打开；正式 UI“重算道路”明确完成 `442 → 438 / 7976 → 7821`。专项数据门为西陆 `108` 条道路、`1440` 个道路 cell、触达 `203 / 350` 城市和 `6 / 6` 首都、陆路水域 cell `0`；全图与西陆截图均能看到道路网。单历史提交后撤销恢复 `442 / 7976`，`redo=1`。
3. 指定存档初始为 `疆界柔化=开 / 整图外框柔化=关`。外框目标裁剪 `55600` 像素中开关改变 `7309` 像素（阈值 `8`），中心裁剪改变 `0`，关→开→关逐像素恢复；疆界裁剪 `612521` 像素中开关改变 `6559` 像素，开→关→开没有阈值以上差异，最大压缩误差 `7`。终态恢复疆界开、外框关。
4. 本地保存菜单经“保存到本地”显示“地图已保存到本地文件”。五轮导入窗口 health error / warn、产品 `>200ms` LongTask 与 WebGL error 均为 `0`；道路提交 / 撤销窗口健康事件、console error / warn 和 WebGL error 也均为 `0`。
5. `typecheck:core`、`regress:boundary-presentation`、`regress:render-preparation`、`regress:real-archive-route-coverage` 与 `1407 modules` production build 通过；完成版本为 `0.5.75`。

## 独立验收

- 加载验收 `ACCEPT`：独立 production 五轮墙钟 `4194 / 4198 / 4041 / 4042 / 4028ms`，中位 `4042ms`、最大 `4198ms`；五轮完整彩色海陆、道路、城市、标签和 `100000 / 43419 / 1251 / 442 / 7976` 身份均稳定，导入及至少 `8.2s` 观察窗产品 LongTask、console error / warn 与 WebGL error 为 `0`。
- 道路验收 `ACCEPT`：独立 UI 重算 `442 / 7976 → 438 / 7821`，西陆 `108 / 1440 / 203 of 350 / 6 of 6 / water 0` 与实际画面同时通过，撤销恢复 `442 / 7976`；目标操作窗健康、LongTask、console 和 WebGL 门为 `0`。
- 柔化首次验收因未取得可信首帧证据返回 `BLOCK`；blocker-only 复测在 filechooser 后、`setFiles` 前通过正式 UI 关闭面板，并拆分短调用取帧，最终 `ACCEPT`。无遮挡首个完整帧与 `≥1200ms` 稳定帧 SHA 均为 `a4501b4bd9da76b52a142bc077203bbff1311ce41992e073f53d60859155995e`，原始字节相等、解码像素差 `0`；疆界往返改变 `257692` 像素后恢复差 `0`，外框往返改变 `422905` 像素且差异距外缘最大 `86px`、中央区域 `0`，恢复差 `0`。
