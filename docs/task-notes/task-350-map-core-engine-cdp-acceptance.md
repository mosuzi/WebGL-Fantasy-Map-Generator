# 第 350 项：MapCoreEngine / TypeScript CDP 全量浏览器验收

> 2026-08-20 重新基线：用户已批准重新规划包括前五项在内的典型场景，并整体复查、修补已开发功能。新的唯一实施顺序见 [第 350 项地图核心引擎集成重新基线与整体修补方案](../plans/task-350-engine-integration-rebaseline.md)。本文件保留原始执行、失败与 artifact 证据，不再单独决定后续阶段顺序。

## 目标与边界

在 `codex/map-core-engine-architecture-plan` 的冻结提交上执行第 349 项已评估、未执行的浏览器方案，验证核心提交、Worker、投影、渲染、拾取、导出、旧档与故障恢复在真实 Chromium / CDP 中成立。任务只做验收和阻断当前验收的最窄修复；不合入 `main`，不扩大 MapCoreEngine / TypeScript 架构范围，不操作用户已有地图标签页。

详细断言继续以 [`task-349-browser-acceptance-plan.md`](./task-349-browser-acceptance-plan.md) 为准；本文件只冻结实际执行顺序，防止无证据地反复启动浏览器。

## 环境与证据

- 唯一写者：主线程；产品源码在首败前保持冻结。
- 隔离页面：由仓库既有 browser regression 启动固定端口 Chromium；综合终验使用单独固定 10k / 100k 页面，不接管用户标签页。
- 运行信息：记录 branch、commit、`package.json` version、Chromium / GPU、viewport、seed、cells、document / runtime identity、revision 与 build id。
- 详细日志、trace、截图和 JSON 放在 `Z:\tmp\codex\2026-08-20\task-350-map-core-cdp`；仓库只保留总结性文档，现有夹具明确写入 `docs/generated/` 的产物保持本地忽略。
- 浏览器入口必须真实启动 Chromium 或建立 CDP session；未执行的断言、启动失败、超时和环境失败不得记为通过。

## 分阶段执行

| 阶段 | 做什么 | 最小验收与停止条件 |
| --- | --- | --- |
| 350-0 环境冻结 | 静态核对 20 个既有入口及导入链；运行 `typecheck:core`、task-349 final-gate audit 和 production build；确认 CDP 可建立、WebGL2 可用 | commit / build / browser 信息写入 artifact；任一静态门失败即停 |
| 350-0a 冷启动 binding 修复 | 修复无既有地图时 generation / import adoption 请求无法建立合法 foundation binding；补正式入口静态回归并复验干净 10k Chromium | `regress:foundation-core-protocol` 与 `regress:map-transaction-browser` 通过；不放宽 binding validator，不改变已有地图任务语义 |
| 350-0b 政区双镜像恢复 | 历史 undo / redo 将 `politics` 与 `pack` 恢复为值相同但非同引用容器后，城镇 / 省份重生成仍必须显式同步两侧统计和 `state.provinces` | 非同引用 Node 正例与 `regress:settlements-zones-annotations-core-protocol` 通过；同一浏览器门限定复验失败即停 |
| 350-0c 资源点行政经济双镜像 | 资源点重算和经济重建在 `politics / pack` 已分离时，显式同步两侧 states / provinces，不让引用偶合掩盖写集缺口 | 分离引用 Node 正例与 `regress:features-networks-resources-core-protocol` 通过；独立评审 ACCEPT 后才进入浏览器重试 2/10 |
| 350-0c-a marker 直接事务 before-image | `350-0c` 评审发现通用 writer 也服务 add / move / delete / revert；补齐直接命令 politics 写集，并按受影响子根保存共享关系、撤销 / 重做与故障回滚 | Worker patch、直接 command、chunked success / failure 四条分离镜像 Node 路径通过；不得退回整图快照或靠重算模拟 before-image |
| 350-0d world-systems render binding | diplomacy / military 的 prepared render 使用正式三字段 legacy renderer identity，不得被 compute binding 的 token / lock / operation 字段误拒绝 | 带真实 preparedRender 的 diplomacy Node 正例、stale topology 与缺字段负例、world-systems 协议和 typecheck 通过；不跳过 schema / identity / revision 校验 |
| 350-0e 复合全图任务 picking 范围 | height-derived / climate-downstream / ocean-current-world / grid-topology 显式请求全图 render 时，必须声明全部五类 picking 对象族 | render-preparation 10k 与 foundation core Node 门覆盖四类复合任务，未知 kind 继续拒绝；独立评审接受后才进入浏览器重试 4/10 |
| 350-0f 副本 patch 失败队列收敛 | 首项 session patch 失败并销毁副本时，后续已排队 patch 不得继承旧 rejection 或泄漏未处理 Promise；下一 Worker run 可完整重同步 | worker-task Node 负例覆盖 bad checksum + 后续 resolved / rejected patch；页面不泄漏内部 checksum / revision 错误，评审接受后才进入重试 5/10 |
| 350-1a 目标门 LongTask 精确归因与收敛 | 重试 5/10 的功能断言已通过，但目标时间窗出现健康监控 `main-thread-long-task`；补齐精确时长 / 起点 / 操作归属并完成一次实质优化 | 目标时间窗 `>200ms` LongTask 为 0；不得把性能信号从 console 分类中移除或仅提高阈值，独立评审接受后继续 350-1 其余四门 |
| 350-1b history roundtrip 子阶段性能 | 分块 checksum 移除夹具同步阻塞后，重试 7/10 仍在多数事务的 after / undo / redo 区间出现三项 `312–417ms`，GEO 另有 `945ms`；细分 execute / fingerprint / undo / redo / baseline-undo 后修复真实产品阶段 | 同一 canonical map 相等断言保留；目标窗产品 `>200ms` LongTask 为 0，checksum Node 同尺寸最大同步 slice 保持 `<20ms`，独立评审接受后才启动下一目标复验 |
| 350-1c regeneration history 渲染分段 | 重试 9/10 已清除后台/API history 产品 LongTask，但打开国家面板后的 undo / redo / baseline-undo 仍为 `348 / 328 / 353ms`；聚焦 CDP 否定 StatePanel 投影假设并定位到同步 terrain / surface / lines 重复刷新 | terrain、line、point、object-index、surface/draw、labels、selection 分段让出主线程；历史与 UI 恢复语义不漂移，产品 `>200ms` LongTask 为 0，同一评审接受后才使用最后一次目标复验 10/10 |
| 350-1d map transaction 异步 history 夹具兼容 | 重试 10/10 在首个 features undo 因夹具同步解包 Promise 立即停止；产品 API 与事务尚未失败 | 六处 history undo / redo 均 await API receipt 后再 unwrap；Node / 静态检查与独立评审通过，不冒充浏览器门通过，额度已用尽如实登记 |
| 350-1e map transaction 最终 baseline 证明 | 追加复验 1/5 通过首次 undo/redo，但 UI 最终 baseline 只等待 click，未等待异步 settle 或复算 checksum；通用 API / climate 也只凭 receipt | UI 等 PanelManager pending settle 后复算 canonical checksum/history；通用 API 与 climate 最终 undo 后复算 checksum及引用，独立评审接受后才使用追加复验 2/5 |
| 350-1f 剩余 10k history Promise 夹具审计 | Worker regeneration 门首个 undo 同步读取 Promise receipt 而报 unknown；后续三门需一次核对避免重复空转 | Worker regeneration 两处 undo/redo await receipt；population/social/economy 的 runApi/spanAsync 明确 await task，Node/静态与独立评审通过后只复验首败门 |
| 350-1g zone 紧凑集合身份契约 | diplomacy 清理失效 Warzone 后会压紧数组但保留持久 id，旧 validator 错把 zone 当 id 槽数组而拒绝非零首 id | zone id 为唯一非负安全整数，可选 id/i 一致；删除首项后的紧凑非零 id 正例通过，重复/负数/冲突 id 负例拒绝，不放宽 cells、镜像、Warzone 与 metadata 门 |
| 350-1h states 中立省份的省会标记收敛 | 连续 features → states 后，城市可能从旧省份归入 `province = 0`，但 city / burg 残留 `provincial = true`，正式 pre-commit 拒绝孤立省会反向引用 | 只对非锁定城市在政治归属同步为中立省份时清除两侧 provincial；Node 固定 seed 覆盖真实 before-image，锁与有效省会不漂移，独立评审通过后才复验浏览器门 |
| 350-1i diplomacy 接纳 stale 军事派生 | 连续国家重生成后军事已明确标记 stale，新国家没有军团数组，外交只裁剪战争派生引用却被完整新鲜军事门拒绝 | stale 只允许外交跳过军团汇总新鲜度检查；国家军事字段、军事非战争根、汇总和 stale 状态必须与来源一致，campaign/front 仍严格校验，固定完整依赖链与三类越权负例通过后独立评审 |
| 350-1i-a diplomacy stale 形状局部恢复 | 350-1i 为保持缺失 stale 形状而修改通用 fresh helper，误使 routes 精确写集缺失必需的 `military.metadata.stale` | 完整恢复通用 helper；仅 diplomacy 在自身 fresh 调用前后保存/恢复 military stale 的 exists/value，routes 与 diplomacy 两组正式协议同时通过并独立评审 |
| 350-1j 普通 Worker 性能证据闭合 | 功能与 session 门已通过，但旧脚本只取 execute LongTask，未覆盖 undo / redo，且性能 health error 被过滤后没有独立 `>200ms` 硬门 | execute / undo / redo 分窗采集并逐窗断言产品 LongTask `<=200ms`；compact summary 与完整 JSON 固定保存到 Task 350 Z 盘 artifact，独立评审接受后只复验本入口 |
| 350-1k population picking 准备范围 | population Worker 显式请求 picking 层，但 target kind 未登记 component 范围，正式 render request 在 Worker 前拒绝 | 仅登记 population → cities；真实 task preparedRender 与单一来源范围门通过，不扩大到 markers / military / routes / rivers，独立评审后只复验 population |
| 350-1l population 来源指纹与整图 adoption 一致性 | 350-1k 后首个 adjustment 越过 render request，却发现 fresh adoption 的主线程 fingerprint 与 Worker 保留地图不一致 | generation / import 以同一 v3 bytes 建立 canonical projection，并在 prepared render / commit 前执行共享运行时规范化；保留值漂移 stale 硬拒绝，仅忽略值完全相同的镜像引用身份；Node 反例与独立评审后复验 |
| 350-1m population LongTask 归因与一次优化 | 350-1l 后完整功能链到达最终性能断言，但记录两次 `132ms / 130ms` LongTask；低于 `200ms` 硬线，旧夹具仍要求数量为 0 | 只做一次分窗窄诊断，区分产品 execute / refresh 与夹具 digest；完成一次实质优化，最终逐窗硬断言 `<=200ms` 并保存结构化 artifact，独立评审后只复验本入口 |
| 350-1n society picking 准备范围 | society 固定门在 Worker 前因 `culture-expansion` 未登记 picking 范围拒绝 | culture-expansion / religion-expansion 均只登记 cities；render-preparation 双映射与单个 10k 真实 task picking DTO 正例通过，未知 kind 仍拒绝，独立评审后只复验 society |
| 350-1o society 结构化验收证据 | 350-1n 后 society 产品主链已退出 `0`，但脚本只向终端打印超大 JSON，输出被截断且功能首败不会留下 Task 350 artifact | 保留全部原断言；页面返回 phase / active / completed / failure，顶层成功或失败都写 full / summary JSON，失败保持非零退出；独立评审接受后只复跑 society 一次形成可复核证据 |
| 350-1p economy 结构化验收证据预检 | economy 入口与 350-1o 修正前的 society 一样只在成功后打印超大 report，已知不能满足首败留证与截断友好复核 | 浏览器执行前复用 350-1o 的 page progress / failure、顶层 finally full+summary、既有断言标量摘要；独立评审接受后才首次运行 economy，避免先跑再为 artifact 重跑 |
| 350-1q economy picking 准备范围 | economy 首次固定门在 rebuild 的 Worker 前因 target kind 未登记 picking components 拒绝 | economy 只登记 cities；render-preparation 映射与 10k 真实 economy task picking DTO / binding 正例通过，未知 kind 仍拒绝；独立评审后只复验 economy |
| 350-1r economy pre-commit binding 参数 | 350-1q 后 rebuild 已越过 picking，却在主线程 assertOutput 报 `binding is not defined` | economy assertOutput 显式接收 executeWorkerMapMutation 已提供的 binding，再交统一 pre-commit validator；市场归属正式入口静态反例与三域协议通过，独立评审后只复验 economy |
| 350-1s economy pre-commit LongTask | 350-1r 后功能链完整通过，但 rebuild / assign 产品窗记录 `674ms / 189ms` LongTask，首项超过 200ms 硬线 | 只做一次窄归因，量化 Worker 输出 validator、commit/render install 与夹具 digest；完成一次实质优化，保留全部协议负例并将产品窗 `>200ms` 降为 0，独立评审后只复验 economy |
| 350-1 10k 核心事务 | 运行 map transaction、普通 Worker regeneration、population、society、economy 五个 10k 浏览器门 | 成功操作只产生一次 commit/history；拒绝完全回滚；错误、残留 Loading、pending session、WebGL error 均为 0 |
| 350-2a 锁定河流的 Feature 镜像边界 | 在 100k / session / direct-domain-locks 前，闭合 height-derived 的 features 前序步骤不得移除锁定河流 lake in/out 引用 | composite Worker 锁夹具与对应专项门通过；锁定对象 / 相关镜像不漂移，或在不安全拓扑下于写入前结构化拒绝 |
| 350-2b 100k / 锁浏览器证据预检 | session、grid topology、direct-domain locks、compound locks 入口沿用不同旧任务的终端输出与性能采集，无法统一证明功能首败留证和产品 `>200ms` 硬线 | 四入口在成功或失败时均向 Task 350 Z 盘写 full / compact summary，保留 progress / failure、功能标量、LongTask 明细与错误计数；独立评审接受后才运行昂贵门 |
| 350-2c generation session patch 后的 no-op 夹具契约 | 10k session 首败显示 `newMap` 已采用 generation session，锁命令经 map-replica patch 更新同一 session 后，两次 no-op 都合法复用；旧夹具仍要求首个 fresh | 显式等待 `mapReplicaPatchQueue`，证明 session id 不变、revision / lockFingerprint 已更新，再要求两次 no-op 均复用且 committed；Node / 静态门及独立评审接受后只复验 10k session 一次 |
| 350-2d 全锁 no-op renderer identity 归因（冻结） | 350-2c 后唯一目标复验越过 session 契约，推进到 renderer exact 失败；当前错误未区分 surface / line / point / route / river / picking / overlay / camera | 已把各子身份与 surface ref/bytes/valid 差异写入失败消息；按“一次诊断 + 一次复验”停止条件冻结，获得额外目标诊断授权后才运行一次聚焦门并决定产品修正或夹具 settle |
| 350-2e generation adoption 后的 session continuity 夹具契约 | `4 / 5` 已越过 no-op，首个 routes 合法复用 `newMap` 接纳的 generation session，但旧 invalidation/hard-cell/100k 仍假定换图后首操作 fresh | 证明 generation adopted id、undo/lock replica patch 的 id/revision/fingerprint 连续与换图后新 id；fault/cancel 后仍必须 fresh，独立评审接受后才使用 `5 / 5` |
| 350-2f refresh fault 的公开/内部错误双层契约 | `5 / 5` continuity 通过后，公开 API 返回 `operation_failed`，旧夹具误要求内部 refresh fault code | 公开 response 必须为 `operation_failed`，临时 wrapper 捕获的原始 cause 树必须含 `worker_regeneration_refresh_fault`；rivers/route/viewport 三处一致，wrapper 必恢复 |
| 350-2g refresh fault 的真实 action 捕获边界 | 第二批额度 `1 / 5` 实证公开码正确，但 `runtimeOperation.run` manager wrapper 未捕获 raw cause；console API 实际动态读取安装时传入的 runtime action | 临时包装 `runtimeActions.generate.regenerate`，捕获并重抛原始 rejection 后再由 apiCall 形成公开包络；共享 helper / pending viewport 均在 finally 恢复，产品文件 0 |
| 350-2h refresh fault raw tree 聚焦诊断 | 第二批额度 `2 / 5` 在 action wrapper 后仍缺内部码，继续猜测层级会空耗额度 | 记录 wrapper 精确调用次数，并将完整序列化 raw tree 纳入首败 artifact；只取得可判定证据，不改产品或放宽 public outer code |
| 350-2i 湖泊出口河的 water-tail 协议例外 | 第二批额度 `3 / 5` 的 raw tree 证明 refresh fault 尚未触发，正式 validator 先把合法 lake water→spill land 当作海洋回陆；首版原始高度/旧 closed 近似与 `Number()` 强转被评审阻断 | `buildRivers` 与 validator 共用同一 salt variation → alter → closed detection → depression resolution → climate/overflow evaluation 流水线；严格验证 lake i/id、outlet、spillCell 原始类型及完整 overflow expectation，海洋回陆、terminal lake 和伪造/强转值继续拒绝 |
| 350-2j refresh fault 公开 code 纠偏 | 第二批额度 `4 / 5` 越过湖泊协议后真正命中注入；具备 code/stage/suggestion 的 refresh fault 按正式 RuntimeOperation/API 契约保留精确 code，先前 `operation_failed` 实为前置 protocolError 的包络 | rivers、routes pending、pending viewport 的 public/raw 均含 `worker_regeneration_refresh_fault`；wrapper 次数、finally 恢复、回滚与性能门不变，静态门从正式 RuntimeOperation/API 源码锁定该契约 |
| 350-2k hard-cell 高度视图前置契约 | 第二批额度 `5 / 5` 越过前序故障后，政治视图被地图替换保留；旧 hard-cell 夹具仅关闭平滑 / 海洋着色，故正式 renderer 走全 surface refresh 而没有局部 patch range | 10k / 100k 均保存原 color mode，操作前经正式 API 固定 `height` 并逐次回读，成功与异常路径恢复；静态门锁定 renderer 前置条件及删除切换 / 恢复的反例，独立评审后等待新授权复验 |
| 350-2 100k 与 session | 运行 10k / 100k Worker session、grid topology、direct-domain locks、compound locks | binding / ACK / invalidate / owner 同源；100k 不丢 identity、history 或 render；产品 `>200ms` LongTask 为硬阻断 |
| 350-3 渲染、拾取、导出 | 运行 city picking、overlay pan、viewport line preview、heightmap export、PNG crop；直接 CDP 验证普通视图切换和 WebGL context loss / restore | presentation-only 不推进 map revision/history；恢复后 framebuffer、overlay、picking 同源，GL / page / console error 为 0 |
| 350-4 兼容、反馈与终验 | 运行 storage compatibility、storage fallback、save feedback、Loading single-source、delayed feedback；最后在冻结 commit 上串联固定 10k 与代表性 100k | v1 / 当前存档、保存 receipt、恢复与反馈通过；形成总 JSON、截图索引和最终 ACCEPT / BLOCK 结论 |

## 固定的 20 个自动化入口

1. `regress:map-transaction-browser`
2. `regress:worker-regeneration-browser`
3. `regress:population-worker-browser`
4. `regress:social-expansion-worker-browser`
5. `regress:economy-worker-browser`
6. `regress:worker-session-browser`
7. `regress:worker-session-100k-browser`
8. `regress:grid-topology-browser`
9. `regress:regeneration-lock-direct-domains-browser`
10. `regress:regeneration-lock-compound-browser`
11. `regress:city-picking-browser`
12. `regress:overlay-pan-stability-browser`
13. `regress:viewport-line-preview-browser`
14. `regress:heightmap-export-browser`
15. `regress:png-crop-browser`
16. `regress:browser-storage-compatibility`
17. `regress:browser-storage-fallback`
18. `regress:browser-save-feedback`
19. `regress:loading-single-source-browser`
20. `regress:delayed-operation-feedback-browser`

## 防空转规则

1. 每组只先跑一个最便宜 smoke；通过后才扩展该组。
2. 昂贵门首败即停，保存原始输出；用户已为当前目标门新增最多 `10` 次复验额度，每次必须先完成最窄诊断、Node 反例、专项门和独立评审，旧的失败轮次不计入该额度。
3. 同一 blocker 重现、没有新增归因 / 产品 / 测试证据、出现产品决策或 `>200ms` 未归因 LongTask 时冻结任务，不用重复全量运行消耗额度。
4. 若发现计划外但必须修复的问题，先把独立修复阶段插入本表并重排未完成阶段，再修改产品源码。
5. 每个阶段结束记录真实命令、耗时、关键标量、artifact 路径及 `ACCEPT / BLOCK`；没有新增产品或测试代码时明确记为 `0`。

## 执行记录

- `350-0` 静态入口 `20 / 20` 存在，`typecheck:core`、27 门防误触审计和 production build 通过；首次直接页面与干净 `regress:map-transaction-browser` 均在 `generate.newMap` 被 `foundation.binding` 拒绝，证明是产品冷启动阻断而非持久浏览器缓存。
- 已按规则插入 `350-0a`。根因是第 349 项把通用 regeneration binding 收紧为严格 foundation factory 后，初次 generation / import 在 `MapRevisionTracker.mapIdentity === null` 时没有使用其已存在的 `generated:* / imported:*` 临时 owner identity。
- 首次修正只给请求建立临时 identity，目标复验仍在 Worker 返回后的 current-binding 检查命中同一错误。二次只读定位确认协调器复核没有沿用候选 adoption identity；最窄补齐只允许冷启动 `generated:<正整数> / imported:<正整数>`，已有地图仍以 revision owner 为准。
- 第二次补齐后目标门第三次仍命中同一错误，证明错误还存在于 `loadMapIntoRuntime` 中 state map 已替换、revision owner 尚未 `replaceMap()` 的接纳中间窗口。按停止条件，`350-0a` 判定 `BLOCK`，后续 19 个浏览器门未执行；两次未通过的产品 / 测试改动已全部撤回，产品树恢复 `c358ae1` 冻结内容。
- 用户批准后，`0.5.58` 候选建立显式 `pendingMapAdoptionBindingOwner`，覆盖 `request -> worker-pending -> loading -> committing -> committed / invalidated`；generation / import 共享严格 binding factory，正式 commit 保持 operation / token 连续且 map / topology revision 从 `0` 开始，原 envelope / document validator 未放宽。新增 owner 专项、完整 foundation 协议、`typecheck:core` 与 `1393 modules` production build 通过。
- 第一轮干净 `regress:map-transaction-browser` 已越过冷启动和新地图 adoption，后续在 `generate.regenerate.cities` 被 `politics / pack state` 镜像门拒绝。按规则插入 `350-0b`；根因是 history restore 只保证双镜像值相同，`syncPoliticalSettlementStats` 却假定两侧共享引用。最窄修正改为分别更新两侧 states / provinces，并以非同引用夹具验证，城镇地区协议门通过。
- 限定的第二轮浏览器复验已越过 cities，再在 `generate.regenerate.provinces` 命中同类 state 镜像拒绝。精确根因是全图省份重生成会改写 `pack.states[*].provinces`，但 `regeneratePackProvincesWithinStates` 的 result 未携 states，`applyPoliticsRegenerationResult` 因而只重新绑定 provinces，恢复后分离的 `politics.states` 未接收新省份成员表。该门已连续两次失败，按硬止损冻结为 `350-0b BLOCK`，未运行第三次浏览器；其余 19 个浏览器入口仍未执行。
- 同一只读评审给出 `BLOCK / P0 0 / P1 3`：除省份 result 漏交 states 外，竞争 map-replace 在 `operation_busy` 的 rejected Promise `finally` 中会误清正在运行任务的 adoption owner；adopted Worker session / owner 已 commit 后，`preparedInstall.finalize()` 抛错仍会进入外层 rollback，使 Worker 新图与主线程旧图分叉。评审复跑 owner、城镇、society-politics、typecheck 与 diff-check 均通过，未运行浏览器、未修改文件。
- 最窄后续范围冻结为三项：省份 result 显式携带真实改写的 states 镜像；owner 只允许由成功开始且 operationId 相同的 map-replace 失效；adoption commit 后的旧资源释放改为 best-effort，不得反向破坏已提交地图。三项分别补非同引用、busy 竞争和 throwing-finalize Node 回归；不得删除 validator，也不得靠事后 JSON 复制掩盖写集。需要新的明确裁定后才能修改并仅复验一次目标浏览器门。
- 用户授权后上述三项已闭合；owner、城镇、society-politics、typecheck、diff-check 与 production build 通过，同一独立评审给出 `ACCEPT / P0 0 / P1 0`，确认 busy 请求不会误清当前 owner、finalize 失败不再反向回滚已提交地图、省份 states 双镜像在分离引用夹具成立。评审未运行浏览器、未修改文件。
- 用户随后为当前目标门给出最多 `10` 次复验额度。重试 `1 / 10` 已越过 adoption、cities、states、provinces，首次在 `generate.regenerate.markers` 被 `marker-state-mirror-invalid` 拒绝；精确根因是 `refreshMarkerResourceEconomy / buildEconomy` 只更新 `pack.states / pack.provinces`，而 markers 写集和 pre-commit validator 要求 `politics` 镜像同时提交。按规则插入 `350-0c`，不放宽 validator；分离引用 Node 正例和地理网络资源协议门已通过，待独立评审后再启动重试 `2 / 10`。
- `350-0c` 首轮评审给出 `BLOCK / P0 0 / P1 1`：同步位于通用 `writeMarkerCollection`，add / move / delete 也会写 politics，但 Manifest 未声明；旧快照只保存 markers / notes，撤销会靠重新构建经济而无法恢复原分离对象图和完整 JSON。按规则插入 `350-0c-a`：直接命令写集和 resource-economy derived writes 显式纳入 politics；快照只覆盖 markers 正式受影响的 markers / economy / politics states/provinces / pack 经济子根和 cell 数组，并在一次 structured clone 中保留共享关系，不复制整张地图。
- `350-0c-a` Node 证据现覆盖 Worker patch apply→undo→redo、直接 command apply→undo→redo、chunked 成功、chunked 注入故障回滚四条分离镜像路径；地理网络资源协议、markers core、零资源、锁保护、core Manifest / dependency、typecheck 和 diff-check 通过。一次初版整图 `assert.deepEqual` 夹具导致测试进程工作集异常膨胀，已终止该唯一进程并改为受影响域 SHA-256，不属于产品或浏览器失败；待 blocker-only 复审，浏览器重试仍为 `1 / 10`。
- 同一评审对 `350-0c-a` 给出 `ACCEPT / P0 0 / P1 0`：Manifest politics 写入、依赖方向、一次结构化快照、分离 / 共享关系恢复及四条事务夹具均闭合；独立复跑地理网络资源协议、core Manifest / dependency、typecheck、diff-check 通过，浏览器执行 `0`。现已按顺序启动目标门重试 `2 / 10`。
- 重试 `2 / 10` 已越过 markers，首次在 diplomacy 的 `preparedRender.binding` 被 world-systems validator 以“不完整”拒绝。对照正式 `render-preparation.js`、features 和 settlements validator，legacy renderer binding 固定只有 `mapIdentity / mapRevision / topologyRevision`；world-systems 独自复用完整 compute binding validator，错误要求 generation token、lock fingerprint 与 operation。按规则插入 `350-0d`：改为严格 render schema + 三字段非空 / number / 非负安全整数 / 同源校验，新增带真实 preparedRender 的 diplomacy 正例和 stale / missing / null / 数字字符串负例。首轮独立评审以 `P1` 拦住 `Number(...)` 强转接受 null / 字符串的缺口；修正后的 blocker-only 复审为 `ACCEPT / P0 0 / P1 0`，独立专项协议、typecheck、diff-check 通过且浏览器执行 `0`，现启动重试 `3 / 10`。
- 重试 `3 / 10` 已越过 diplomacy，首次在 `edit.height.rebuildBaseDerived` 构造 render request 时因 `height-derived` 没有 picking 准备范围被拒绝。按规则插入 `350-0e`：四类显式请求完整 `RENDER_PREPARATION_LAYERS` 的复合全图任务统一声明 `OBJECT_PICKING_COMPONENTS` 五类对象，不给未知 kind 静默 fallback；待 render-preparation / composite Node 门和独立评审后再启动重试 `4 / 10`。
- `350-0e` 的 render-preparation 10k 门已通过；追加的 composite 诊断先暴露旧夹具仍期望两字段 binding，已把 generation 与公共 fixture 对齐当前三字段 topology 契约。该门随后完整通过本阶段相关的 generation、height、climate、ocean Worker / fallback / patch / undo-redo，才在后置锁尾段发现 height features 会移除锁定河流 lake outlet 镜像。该 P1 属于既定 direct-domain-locks 验收面，不影响无锁 350-1，故重新排序为 350-1 后、350-2 前的 `350-2a`；不得以 composite 全绿冒充当前 picking 阶段证据。
- 同一独立评审对 `350-0e` 给出 `ACCEPT / P0 0 / P1 0`：五族范围与正式 `OBJECT_PICKING_COMPONENTS` 单一来源一致，四类任务均属全图 rebuild / replacement，完整结果会替换整个 picking index，未知 kind 仍硬拒绝；foundation core、typecheck、diff-check 独立通过且浏览器执行 `0`。锁 P1 的 350-2a 重排不阻断当前无锁门，现启动重试 `4 / 10`。
- 重试 `4 / 10` 已完成全部 API / UI 事务及撤销重做，但终验收到两次未处理的“地图副本 checksum 或 revision 不连续”。窄归因确认：首项 session patch 失败后会销毁副本，而 `pendingSessionPatch.then(...)` 令后续队列直接继承旧 rejection、从未消费各自的 patch Promise；两个后续 checksum/revision 检查拒绝因而成为 pageerror。插入 `350-0f`：排队时立即捕获 patch / binding outcome，串行前项失败后仍消费当前输入；会话已失效时合法后项收敛为 false，自身拒绝由当前调用返回，下一 run 忽略旧失败并完整重同步。
- `350-0f` 的 FakeWorker 负例确认 bad checksum 首项仍 protocol reject，排队合法后项在 session 已销毁时返回 false，排队自身拒绝仍返回同一 Error，后续 run 可完整重同步。完整 worker-task、typecheck、build、diff-check 通过；同一独立评审结论 `ACCEPT / P0 0 / P1 0`，确认没有吞掉当前错误或放宽连续性，浏览器执行 `0`。现启动重试 `5 / 10`。
- 重试 `5 / 10` 的功能面已通过：11 类重生成、3 类高度派生、GEO cells 导入、气候下游重建与国家面板 UI 事务均各形成恰好一条 history，撤销 / 重做恢复成立，`GL error / application console error / pageerror = 0 / 0 / 0`。但目标时间窗出现 `main-thread-long-task`；健康监控只在 `>=250ms` 时上报，已越过本任务 `>200ms` 硬线，因此该轮只记为功能 PASS、阶段仍 BLOCK。按防空转规则插入 `350-1a`，先让同一目标门输出 PerformanceObserver 的精确 `startTime / duration / operation` 并硬断言，再做一次窄归因与实质优化；剩余四个 350-1 浏览器入口尚未执行。
- 重试 `6 / 10` 已完成精确归因并按预期在硬断言失败：features / routes / cities / states / provinces / markers / diplomacy / religions / military / zones、两个高度分段与气候阶段各有一个 `1.06–1.221s` LongTask，`rebuildAllDerived` 为 `3.624s`，UI 三项为 `352–364ms`；health 记录的 `operation = null`。这些任务全部与夹具同步 `JSON.stringify(app.map)` 的全图 before / after / undo / redo 指纹窗口重叠，当前不能归为产品操作性能。
- `350-1a` 已把同步 stringify 指纹替换为既有 `computeCanonicalMapReplicaChecksum`：继续覆盖 canonical registry 的全部 canonical / persisted sections，以实际单调 map revision 作为 cache key，并以 `4ms` budget 主动让出主线程；测试静态服务器只读提供该模块及相对依赖，不进入产品 bundle。`regress:map-replica-command-patch`、脚本语法和 diff-check 通过；同一评审首轮只因三处权威文档少记重试 6 而给出 `BLOCK / P1 1`，代码未发现 P0 / P1。当前性能尚未宣称收敛，待文档 blocker-only 复审接受后才启动目标复验；额度已使用 `6 / 10`。
- 文档 blocker-only 复审已对 `350-1a` 给出 `ACCEPT / P0 0 / P1 0`，随后启动重试 `7 / 10`。该轮确认原同步 stringify 的 `1.06–3.624s` 阻塞已消失，但仍捕获 `49` 个候选 LongTask：多数主链在每个 roundtrip 各有三项 `312–417ms`，GEO 为 `945 / 390 / 500 / 399ms`，UI 为 `359 / 340 / 363ms`；rivers 没有 `>200ms`。初版 `1014 grid / 906 pack` Node checksum 对照低于公共入口实际 10k，不能作最终排除证据；补跑真实 `10004 / 5720` 后为 `240` 次 yield、最大同步 slice `4.9ms`、墙钟约 `1338.4ms`。
- 按规则插入 `350-1b` 并重排：目标门增加 execute / fingerprint / undo / redo / baseline-undo 子阶段标记，经同一评审 `ACCEPT / P0 0 / P1 0` 后执行重试 `8 / 10`。该轮把 `49` 项精确拆为 `45` 个测试专用 canonical fingerprint 和 `4` 个产品项；产品项为 markers undo `343ms` 与 UI undo / redo / baseline-undo `423 / 350 / 360ms`。测试 fingerprint 来自只在本夹具动态导入的 source 模块，仍保留完整 trace 与 canonical 相等断言，但不再计入产品 `>200ms` 硬门。
- 产品根因是 history `onMutation` 中 `captureCommandMapReplicaWrites` 对大领域写集同步执行 `structuredClone(writes)`，随后才排入副本 patch 队列。最窄修正新增基于既有 `cloneWorkerGraphByPackets` 的异步分包捕获：Node `10k` states 写集为 `38` 次 yield / `12` 包，跨路径共享引用保留、结果与活动地图脱离；捕获结束时严格复核 map 引用与 revision，期间若有新 mutation 则以 `map_replica_patch_capture_obsolete` 作废，绝不发送混合快照。无活动 session 时不再白做快照，空写集仍保守 invalidate。
- `regress:map-replica-command-patch`、完整 `regress:worker-task`、`typecheck:core`、`1393 modules` build、脚本语法和 diff-check 通过；同一独立评审对 350-1b 给出 `ACCEPT / P0 0 / P1 0`，未运行浏览器。
- 重试 `9 / 10` 再次通过全部功能断言，并把后台/API history 产品 LongTask 清零；测试专用 canonical fingerprint 仍以 `45` 项独立报告。剩余产品信号只在打开国家面板后的 undo / redo / baseline-undo，分别为 `348 / 328 / 353ms`，health 对应三项且 GL / 应用 console / page error 无新增错误。
- 350-1c 的首轮静态归因把差异面指向打开国家面板后的 Vue 投影，并曾提取纯 StatePanel projection；但专门增加的 UI-only CDP 仍复现 undo / redo / baseline-undo `424 / 354 / 359ms`，证明该假设不是产品根因。上述 projection 产品文件、专项脚本和 package 入口已完整回退，不保留无效优化；该诊断不计入完整目标门重试额度。
- 随后的 CDP CPU profile 精确显示 `editHistory` 与 `statePanel.update` 各不足 `1ms`，阻塞位于 `selectionStore.batch 359–439ms`，其中 `renderer.refreshTerrainCaches 281–337ms`，并存在 terrain 内 surface / lines 与 scheduler 后续步骤的重复刷新。最窄修正只对 regeneration history 使用异步 refresh scheduler：terrain 禁止内嵌 surface / line，随后按 line、point、object-index、surface/draw、labels、selection 分段让出；每段后复核活动 map / revision，header 在 Promise settle 前禁用 undo / redo。
- 首轮独立评审给出 `BLOCK / P0 0 / P1 2`：`assertCurrent` 未冻结 history mutation 后的 map revision，普通同步编辑可在 yield 期间推进同一 map；名称库与军事面板仍同步读取 regeneration history 返回的 Promise。最窄修正增加 committed identity/revision guard，并在 scheduler、panel、afterRefresh 和最终返回前逐段复核；名称库与军事消费者用统一 settlement 保留同步形状、异步 pending、成功和拒绝收尾。Node 负例确认 revision 在首个 yield 漂移后 terrain 与回调不再执行，Promise resolve / reject 均由消费者接管。
- 修正后最终产品树聚焦 CDP 通过，history delta 为 `1`，undo / redo / baseline-undo 均恢复，`GL / application console / page error = 0 / 0 / 0`，产品 `>200ms` LongTask 为 `0`，最大可见分段 `142ms`。map-replica 10k 为 `35 yields / 12 packets / max 7.024ms`，affected-summary 异步阶段 `24` 且 revision drift 在 terrain 前停止，panel-refresh、完整 worker-task、typecheck、`1394 modules` build 与 diff-check 通过；同一 blocker-only 评审为 `ACCEPT / P0 0 / P1 0`，现运行最后一次完整目标门 `10 / 10`。
- 重试 `10 / 10` 在 `history.undo.generate.regenerate.features` 立即停止。`api.history.undo()` 因 regeneration history 分段合法返回 Promise，夹具却先同步执行 `unwrap`，读取不到 receipt 的 `ok` 而报 `unknown`；本轮没有越过首个 features undo，没有形成产品事务、错误面或性能结论。插入的 `350-1d` 仅把 climate 与通用 roundtrip 的六处 history undo / redo 调整为 await receipt 后再解包，Node 语法、diff-check 与同一独立评审为 `ACCEPT / P0 0 / P1 0`，浏览器执行 `0`；目标门额度已用尽，不把夹具修正记作浏览器通过。
- 用户在原 `10 / 10` 用尽后追加授权 `5` 次完整目标门复验；继续沿用首败即停、最窄归因、Node 反例、同一独立评审后才启动下一次的规则，现运行追加额度 `1 / 5`。
- 追加复验 `1 / 5` 的 `regeneration 11 / height 3 / GEO 1 / climate 1 / UI 1` 单 history、首次 canonical undo / redo、climate `operation_busy`、GL / application console / page error 与产品 `>200ms` LongTask 门均通过；测试专用 fingerprint 超线 `16` 项、最大 `952ms`，产品最大可见分段 `170ms`。但独立评审 `BLOCK / P1 1`：UI 最终 baseline 只等待 click，未等待 PanelManager 异步 settle 或复算 fingerprint，通用 API / climate 最终 baseline 也仅凭 receipt。插入 `350-1e` 补齐最终 checksum/history/reference 证明，当前门仍未接受。
- `350-1e` 已补 UI final undo 的 PanelManager pending settle、canonical fingerprint 与 undo history 基线；通用 API 和 climate 也在最终 undo receipt 后复算 fingerprint 并核对 map/options 引用。Node 语法、diff-check 与同一独立评审结论 `ACCEPT / P0 0 / P1 0`，浏览器执行 `0`；现启动追加复验 `2 / 5`。
- 追加复验 `2 / 5` 完整通过：11 类 regeneration、3 类 height、GEO、climate、UI 均单 history，首次 canonical undo / redo 与最终 baseline checksum/reference 全部成立，UI 输出 `undoRestored / redoRestored / baselineRestored = true / true / true`；climate 并发仍为 `operation_busy`。`GL / application console / page error = 0 / 0 / 0`，产品 `>200ms` LongTask 为 `0`、最大可见分段 `157ms`；测试专用 fingerprint 超线 `16` 项、最大 `980ms`，继续独立报告。同一独立评审为 `ACCEPT / P0 0 / P1 0`，现进入 350-1 第二个浏览器入口。
- 普通 Worker regeneration 浏览器门在首个 undo/redo 路径因同步读取 Promise receipt 报 `undo失败：unknown`，首败即停。该脚本两处 history 消费需 await；只读核对 population 的 `runApi`、social / economy 的 `spanAsync` 已对 task 使用 await。插入 `350-1f` 一次闭合剩余四个入口的 Promise 夹具边界，产品 Worker 结果尚未判定失败。
- `350-1f` 已让 Worker regeneration 的 orphan-province undo 与通用 undo/redo await receipt；population `runApi`、social/economy `spanAsync` 已确认等待 task Promise。四脚本语法、diff-check 与同一独立评审为 `ACCEPT / P0 0 / P1 0`，浏览器执行 `0`；现仅复验首败门。
- 复验越过 Promise 后首次在 diplomacy pre-commit 被 `zone #0 身份槽无效` 拒绝。`reconcileWarDerivedData` 合法过滤失效 Warzone 并压紧数组，但地区 create/delete/lookup 以唯一持久 `zone.i ?? zone.id` 识别对象，允许数组首项 id 非零；旧 `validateZoneMirrors` 独自要求 `zone.i === index`。插入 `350-1g`，收敛为唯一非负安全 id 与可选 id/i 一致，保留 dense array、cells、镜像、Warzone reference/cell 与 metadata 严格门。首轮独立评审拦截先 `Number(...)` 让 `null / false / 空串 / 数字字符串` 绕过的 `P1`；最窄修正改为分别检查原始 `i/id` 的 number 类型、安全整数和非负，再检查一致与唯一，并新增 missing/null/boolean/empty-string/numeric-string 负例。两组协议门、`typecheck:core`、`git diff --check` 与 blocker-only 复核均通过，结论 `ACCEPT / P0 0 / P1 0`；浏览器执行 `0`，现只复验普通 Worker regeneration 首败门。
- 普通 Worker 门第一次复验仍返回旧 `zone #0 身份槽无效`，但源码新文案为 `zone row #0 身份无效`；确认入口固定服务 `dist/webgl-generator` 且 bundle 早于源码。完成 `1394 modules` build 后，新文案 bundle `1`、旧文案 `0`。同门目标复验越过 diplomacy，首次在连续链 states 命中 `首府标记缺少有效行政归属`，因此插入 `350-1h`。
- `350-1h` 聚焦 CDP 以 `10004 grid / 6305 pack / 853 cities` 基线运行 features，session 复用且 revision 单次推进；第一次 states 拒绝后 session 已失效，第二次从 canonical 新建 session 仍同样拒绝，排除旧副本污染。Node 搜索以 `states-chain-seed-5 / 2k` 稳定复现 `society-politics-capital-reference-invalid`，精确反例为 city `317` / burg `318` 在重建后 `province = 0` 但 `provincial = true`。最窄修正只在非锁定城市同步到中立省份时清除 city / burg 两侧标记；固定连续链验证同一 burg 被正确清零。society-politics 全套、settlements/zones、typecheck、浏览器脚本语法、diff-check 与同一独立评审通过，结论 `ACCEPT / P0 0 / P1 0`；修正后浏览器执行 `0`。诊断摘要位于 `Z:\tmp\codex\2026-08-20\task-350-map-core-cdp\350-1h-states-provincial-diagnostic.json`，现重建正式产物并复验普通 Worker 门。
- `350-1h` 后的正式 build 与普通 Worker 复验已越过 states / provinces / cities / routes / rivers / markers，首次在链式 diplomacy pre-commit 被 `military.state.2.regiments 必须是数组` 拒绝。固定 `worker-regeneration-browser-chain / 10k` Node 链证明国家重生成后 17 个新国家军团数组均缺失、军事仍正确标记 `stale=true`，旧摘要保留 `104` 个军团；外交命令按产品边界不重建军队，只裁剪 campaign / front / Warzone。因而插入 `350-1i`：仅在外交校验且 stale 为真时跳过军团集合与汇总的新鲜度检查，同时冻结来源国家的 alert / military / policy / diagnostics、军事非战争根、除 campaign/front 外的 metadata 与 stale 状态。首轮独立评审以 `P1` 拦截 Boolean 等价让 `false/undefined → 0/null/空串`、`true → 1/字符串` 漂移可通过；返工改为 stale 原值严格冻结、跳过授权只读取 source，并让 fresh helper 保留原本缺失的军事 stale 属性。完整链及 state / metadata / freshness 越权负例继续通过，另补 undefined→0、false→null、带有效旧军团的 true→1 反例；blocker-only 复审结论 `ACCEPT / P0 0 / P1 0`，浏览器执行 `0`，现重建并复验普通 Worker 门。
- 350-1i build 后的普通 Worker 门首败回退到 routes：`地理网络 Worker patch 的 military.metadata.stale 值或容器无效`。根因是保持 stale 缺失形状的改动落在通用 `markDerivedFresh`，让 routes 等精确写集也不再提交原本必需的 false。首败即停并插入 `350-1i-a`：通用 helper 已完整恢复原赋值，只在 Worker 与主线程 diplomacy 自身 fresh 调用前后保存/恢复 military stale 的 `exists/value`；features/networks/resources 与 economy/diplomacy/military 两组正式协议、两处语法、typecheck、diff-check 通过。同一独立评审确认 routes、missing/false/true、no-op、throw、镜像和 undo 边界均闭合，结论 `ACCEPT / P0 0 / P1 0`，浏览器执行 `0`；现再次 build / CDP。
- 普通 Worker regeneration 随后在同一正式 build 上返回 `ok=true`：独立 11 类与固定 10k 连续依赖链 11 类全部 accepted，持久 session 复用、输入包数、salt、单 history、Loading 两阶段、rivers 不改 routes / route salt、非性能 health、应用 console / page error 与 GL 均通过。但脚本在 undo / redo 前就读取并输出 LongTask，且明确过滤全部性能 health error，没有 `duration > 200ms` 断言；同一独立评审为 `BLOCK / P0 0 / P1 1`。动态插入 `350-1j`：execute / undo / redo 逐阶段 settle、采集、清空并硬断言 `<=200ms`，同时生成按 kind / phase 汇总的 compact JSON 和保留完整任务明细的 full JSON 到 `Z:\tmp\codex\2026-08-20\task-350-map-core-cdp`；评审门禁实现前不复跑浏览器。
- `350-1j` 首轮评审另以 `P1` 拦截“超线先抛错、artifact 后写入”的失败证据丢失。修正后 completed independent / chain 与 active kind / phase 持续累积，PerformanceObserver 明细在硬断言前落入 active progress；顶层 catch 记录 failure / overBudget，finally 无论成败都写 full / summary，再重新抛出原错误。blocker-only 复审为 `ACCEPT / P0 0 / P1 0`，语法、diff-check 通过且浏览器执行 `0`；现只复验普通 Worker 固定入口。
- `350-1j` 固定入口真实退出 `0`：独立 11 类与固定 10k 连续链 11 类全部 accepted，连续链 session id 一致、全部 input packets 为 `3`；独立 execute / undo / redo 与连续链 execute 全窗最大 LongTask `157ms`，`over200 = 0`。原单 history、salt、Loading、rivers 不改 routes / route salt、应用 console / page error、非性能 health 与 GL 断言继续通过。完整 artifact 为 `worker-regeneration-browser-full.json`（`116382 bytes`，SHA-256 `AB1E8674B914790121110E4EA30453FBCDFB327A6B4D5D4FABCC304C0EA84C6F`），摘要为 `worker-regeneration-browser-summary.json`（`8490 bytes`，SHA-256 `2D1680078B4D499C6A4EC93C288D071D2E1FDB820F2BD1285EA5C881F472FE09`），均位于既定 Z 盘目录；待同一独立评审核对本轮实证后转入 population。
- 同一独立评审重算 full / summary 并核对源码控制流：独立 11 项均含 execute / undo / redo，连续链 11 项只需 execute；最大 `157ms`、超线 `0`，连续链唯一 session 与 packets、accepted / committed / pending、旧功能 / history / error / GL 门均真实成立。结论 `ACCEPT / P0 0 / P1 0`，350-1j 完成，现进入 population Worker 固定入口。
- population Worker 固定入口首个 adjustment 立即返回 `operation_invalid_input / 没有 population 的 picking 准备范围`，首败即停，未形成 transfer、history 或性能结论；原始输出保存于 `population-worker-browser.log`。动态插入 `350-1k`：population patch 会更新 city 人口对象，而 picking bucket 持有 city 引用，故正式范围只登记 `population → cities`；保留调用方的 `surface / point / labels / picking` 层，不全量重建其它四类对象。render-preparation 范围断言与真实 population task 的 prepared picking / binding 正例作为最小 Node 门，独立评审接受后才 build / 复验。
- `350-1k` 实现后，render-preparation 固定断言 population 只返回 `cities`；真实 10k adjustment task 同时构造 prepared picking 并核对 components / 三字段 binding，既有 patch、撤销重做、锁 / 取消 / 故障 / stale 与 alias 断言保持通过；100k 为 `99846 grid / 57068 pack / 977ms`。两项 Node 门与 diff-check PASS，浏览器修正后执行 `0`，现待独立评审。
- 同一独立评审确认 cities-only 与真实人口写集一致，首次执行和 history 原样复用 renderLayers，通用 prepared installer 已覆盖 partial component 保留与 rollback；独立两项 Node 门及 diff-check PASS，结论 `ACCEPT / P0 0 / P1 0`。350-1k 完成，现 build 后只复验 population 固定入口。
- `1394 modules` 正式 build 后，population 复验已越过 350-1k，随后在首个 adjustment 被 `population-worker-source-stale` 拒绝，仍未形成 transfer / history / 性能结论；原始输出为 `population-worker-browser-after-350-1k.log`。动态插入 `350-1l`：结构化错误暂记录 expected / actual fingerprint 与 states / provinces / cultures / religions / markets 五组 alias，夹具同步输出 details；只做一次窄诊断以区分真实 canonical 值漂移与引用拓扑差异，source stale 硬门保持。
- 350-1l 唯一窄诊断返回 `expected 509ea587 / actual 4b5fba60`，alias profile 为 `states=false / provinces=true / cultures=true / religions=true / markets=true`。只读源码和纯 Node 复现进一步确认：generation Worker 原先保留 `createMapDocument` 原图，主线程 handoff 先经过 v3 canonical field 投影并丢弃 `provinces.timing / goods.biomeOutput / demandCoverage` 等非持久字段；随后 `loadMapIntoRuntime` 在 revision 0 又执行 social / suitability / label / war / river 规范化，其中 suitability 与 religion mirror 会改变 population 真读取值。因此引用无关指纹只能作为防御，不能替代 adoption 同步。
- 350-1l 修正冻结为两个整图 producer：generation 与 persistence-import 均只编码一次 v3 bytes，同时在 Worker 解码同一 canonical projection；Worker 与主线程在 prepared render 之前调用共享 `normalizeMapForRuntimeAdoption`，commit 时两侧因此拥有同一规范化地图。极简 v1 旧档缺少 `pack.cells.s / pop` 时由 suitability store 补齐对应长度的 typed arrays。人口指纹按每个可写根 / 真实读取依赖的值单独摘要，值一致的 detached mirror 可接受，任一 mirror 值漂移仍由正式 task 抛 `population-worker-source-stale`。
- 当前非浏览器门均通过：whole-map profile 同时断言 generation / import retained map 与 materialized main map 的全 canonical 值、population fingerprint、五组 alias profile 一致；map-file IO 覆盖 v1 极简档与 100k；population task 覆盖 10k 完整事务、100k `99846 / 57068 / 977.3ms`、detached-equal 正例和 drift 负例；另有 v3 container、map-load population preservation、suitability、war consistency、通用 Worker、`typecheck:core` 与 `git diff --check`。同一独立评审核对 bytes / chunks / metadata、prepared render 时点、normalizer 幂等、非 adoption import 与全部 drift 负例后给出 `ACCEPT / P0 0 / P1 0`；修正后 browser / CDP 执行 `0`，现只 build / 复验 population。
- `1395 modules` build 后 population 已完整执行 adjustment / transfer / 四次 undo-redo，最终只在旧 `LongTask === []` 断言首败；实际为 `132ms / 130ms` 两项，均未越过项目 `200ms` 硬线。动态插入 350-1m：先让 artifact 将 LongTask 对齐到已有 API / digest spans，只运行一次窄诊断；随后必须做一次实质优化并按每个窗口 `<=200ms` 硬断言，不能直接删除性能门。
- 350-1m 唯一窄诊断记录 `131ms / 128ms`，分别与 `api:adjustment / api:transfer` 从同一时刻开始；前后 JSON digest 只需 `5.4～6.6ms`，Worker `outputDecodeMaxMs <= 0.5ms`，因此根因为 `applyPopulationMutationViaWorker` 在首个 await 前同步遍历全部 source roots。实质优化新增同 FNV / 引用 / 排序语义的 async fingerprint，单片预算 `6ms` 并主动 yield；计算前后锁定 map identity、map revision 与 topology revision，期间发生变更即 `operation_obsolete`，Worker 侧同步 fingerprint 和 stale pre-apply 门不变。专项断言 async / sync 相等且真实 yield，10k / 100k、typecheck、语法、diff-check PASS。最终门按 spans 分类每项 LongTask，full / summary 固定写入 Z 盘，`duration > 200ms` 硬失败；修正后浏览器执行 `0`，现交独立评审。
- 350-1m 首轮独立评审为 `BLOCK / P0 0 / P1 1`：性能断言失败会在 assert 前写盘，但 `page.evaluate` 内 newMap / API / history / health 首败仍直接进入关闭流程，缺少 failure / active progress。最窄修正后，页面执行体自身捕获首败并返回当前 `phase / active / completed / trace / failure`；Node 层 catch 序列化原错误，finally 在 browser close 前无条件覆盖写入 full / summary，随后原样 rethrow 保持非零退出；写盘自身失败也由嵌套 finally 保证 browser/server 清理。语法和 diff-check PASS，blocker-only 复审 `ACCEPT / P0 0 / P1 0`，browser / CDP 仍为 `0`，现 build 后执行唯一目标复验。
- 350-1m 唯一目标复验真实退出 `0`：population adjustment / transfer / undo transfer / undo adjustment / redo adjustment / redo transfer 共 `6` 次 `population.compute`，均为 Worker map-mirror、同一 session 复用、无 full map payload、prepared `surface / point / labels / picking`、commit revision delta 均为 `1`；引用稳定、history delta `2` 与四个 digest 精确相等全部成立。最终 session idle，LongTask `0`，application console / page / health / GL error `0`，Loading false、普通 UI 禁词 `0`。`population-worker-browser-full.json` 为 `559785 bytes / SHA-256 0E0D6E033B3C26981BC1AFC4D2D1615C35295313AF43C1FAC4D9389DAA3F7010`；summary 为 `498 bytes / BCC9F253B3A9888D483A17BF8AC49F53799F5BDEA08B41BF28F392598E6CC1B5`。同一独立评审复算哈希、解析重放 6 个 task / packet / commit / digest / error / performance 断言后 `ACCEPT / P0 0 / P1 0`，population 完成，现进入 society。
- society 固定门在 culture 首次执行前以 `operation_invalid_input / 没有 culture-expansion 的 picking 准备范围` 首败，未形成 Worker / history / 性能结论。插入 350-1n：文化和宗教重新扩张都会同步 `settlements.cities[*].culture/religion`，object picking 的 city 引用需要按正式 prepared install 重绑，因此两个 target kind 都只登记 `cities`，不扩大到 markers / military / routes / rivers。render-preparation 两个映射断言、未知 kind 负例与单个 10k culture 真实 picking DTO / binding 正例通过；最初给文化/宗教各附全四层造成 Node 内存失败，收窄为单个 picking-only 后 parity 又把有无 prepared DTO 误作业务差异，显式排除已独立断言的 DTO 后最终专项通过。100k 原业务门为 `99846 / 62294 / 473.8ms`，typecheck、diff-check PASS；同一独立评审核对首次/history真实路径、city唯一性与测试收窄后 `ACCEPT / P0 0 / P1 0`，浏览器修正后执行 `0`，现 build 后只复验 society。
- `1395 modules` build 后 society 固定门首次完整退出 `0`：culture / religion、两次 undo、两次 redo 共 `6` 次 Worker / `6` 次 revision+1 commit，同一 session 最终 idle，精确历史恢复、引用稳定、Loading / application / page / health / GL error 与 LongTask 均为 `0`。但原入口只在成功后向终端打印完整 report，本轮输出被工具截断，且 evaluate 内功能首败不会生成 Task 350 artifact；这不推翻产品通过，却不满足独立复核证据要求。动态插入 350-1o，只加失败安全、紧凑且可持久化的验收输出；评审接受脚本前不重复浏览器。
- 350-1o 首轮独立评审 `BLOCK / P0 0 / P1 1`：失败落盘与原硬断言均闭合，但 compact summary 只含数量、session id、性能和错误，无法独立证明 changed、history roundtrip、引用稳定、session idle/reuse 与 commit delta/result。现把这些原断言的布尔、状态、计数和 packet / delta 摘要加入 compact 输出，full artifact 不扩大；语法与 diff-check 通过后只做 blocker-only 复审，浏览器仍不重复执行。
- 350-1o blocker-only 复审 `ACCEPT / P0 0 / P1 0`：compact summary 已覆盖 changed、historyDelta、四项 undo/redo/ref、session status/id/reuse、packet、commit delta/result 及错误/性能标量，并同时进入控制台与 summary artifact；旧硬断言无放宽，语法和 diff-check PASS，浏览器执行 `0`。现只运行一次 society 固定门生成正式 artifact。
- 350-1o society 正式复验退出 `0`：culture / religion 与四次 undo-redo 为 `6` 次 Worker / `6` 次 commit，historyDelta `2`，四项精确恢复 / 重做与 refs 全真；同一 session `map-mt1fab05-2`、后续 `5 / 5` reused，input packets `3 / 3 / 4 / 6 / 6 / 4`、commit delta 全 `1`、成功 commit `6 / 6`。产品窗 LongTask `0`，application/page/health/GL error `0`，Loading false；两项 operation-stall 均为清空目标 observer 前的 setup 信号。full / summary artifact 为 `607495 / 1506 bytes`，SHA-256 `25BB959B...EFCE1 / 68EBD009...5F63A`；现交同一独立评审复核实证后再进入 economy。
- 同一独立评审直接读取 artifact 并复算全部断言，350-1o 最终 `ACCEPT / P0 0 / P1 0`；确认两个 setup operation-stall 均早于首个目标 digest/API span，未被过滤成目标性能通过。society 完成。为避免 economy 重复同一“成功后才打印超大 JSON”的已知证据缺口，先动态插入 350-1p；只改 economy 验收脚本并经独立评审，产品文件新增 `0`，浏览器尚未启动。
- 350-1p 首轮独立评审 `BLOCK / P0 0 / P1 1`：失败安全与旧硬断言均成立，但 compact 尚缺逐 run 的 task / mode / map-mirror / payload-own-map / render-prepared 三层、精确 LongTask 数量与 forbidden 数量。现加入四项 runProfiles 及 longTaskCount / forbiddenCount，保留既有业务、history、session、packet、commit 和错误摘要；静态门后只做 blocker-only 复审，economy 浏览器执行仍为 `0`。
- 350-1p blocker-only 复审 `ACCEPT / P0 0 / P1 0`：runProfiles、longTaskCount、forbiddenCount 与既有功能、条件 history/commit delta、session/reuse、packet、错误摘要共同覆盖全部旧硬门；控制台与 summary 同源，语法、diff-check PASS，浏览器执行 `0`。现首次运行 economy 固定门。
- economy 首次固定门在 `api:rebuild` 的 Worker 前以 `operation_invalid_input / 没有 economy 的 picking 准备范围` 首败；350-1p 正确写出失败 full / summary `1850 / 724 bytes`，SHA-256 `F6FCAD0D...C78B8 / 5880E3E5...D30B`，页面 progress 为 operations / active api:rebuild。动态插入 350-1q：经济重建会同步 city 的 market / production / product / treasury，既有五类 object picking 中只有 cities 受影响，故只登记 `economy → cities`；补 render-preparation 与单个 10k 真实 task picking-only DTO / binding 正例，评审前不复跑浏览器。
- 350-1q 已实现 `economy → cities`；render-preparation 固定门、10k picking-only DTO/binding 与完整 economy task 通过，10k 为 `10004 / 6520 / 15276 deals`，100k 为 `99846 / 63463 / 28256 deals / 2634.5ms`，legacy parity、alias、object identity、undo/redo、locks/cancel/fault/stale 与 input buffer 均成立；typecheck、语法、diff-check PASS。现交同一独立评审，浏览器修正后执行 `0`。
- 350-1q 独立评审 `ACCEPT / P0 0 / P1 0`：economy 写集中五类 picking 来源仅 settlements.cities 受影响，首次与 history 均使用 targetKind economy 且复用三层 renderLayers；未知 kind 与其它映射未放宽。评审独立复跑四门，100k 为 `99846 / 63463 / 28256 / 2602.9ms`。现重建正式 dist 并只复验 economy。
- `1395 modules` build 后 economy 复验越过 picking，随即在 rebuild 主线程 pre-commit 首败 `operation_failed / binding is not defined`；失败 full / summary `4691 / 676 bytes`，SHA `3CB60007...7F663 / B47F063A...B684D`，active 仍为 api:rebuild。源码精确根因为 applyEconomyMutationViaWorker 的 assertOutput 解构缺 binding、下一行却传给正式 validator。动态插入 350-1r，只补该参数并在市场归属正式入口回归中断言回调签名；不删除 validator、不从闭包伪造 binding，评审前不复跑浏览器。
- 350-1r 初选的旧 market-assignment 聚合门在本修正断言前命中既有 UI 文案漂移：夹具期待“应用并重算”，产品现为“应用并更新”；三域协议与 typecheck 已通过。该失败与 binding 修正无关，未复跑、未顺手改 UI/旧夹具；将精确签名断言移入本阶段权威的 economy/diplomacy/military 协议门，记录旧聚合门漂移为非阻断残余，现只复验该协议、typecheck、语法和 diff-check。
- 首次签名反例又准确发现补丁落入相邻的 economy history 回调，而首次 mutation 回调仍缺 binding；未启动浏览器。现撤回 history 的无用参数，在 applyEconomyMutationViaWorker 精确上下文补 binding；三域协议（含新增签名反例）、typecheck、语法、diff-check PASS，现交独立评审。产品净改动仍为一个回调参数。
- 350-1r 独立评审 `ACCEPT / P0 0 / P1 0`：执行器确实传入真实 binding，仅首次 economy 回调接收并连同 sourceMap/output/policy 交正式 validator，history 无无用改动；限定函数切片的签名反例可拒绝补错相邻回调。validator 未改，旧 market-assignment 文案漂移未冒充通过。现重建正式 dist 并只复验 economy。
- 350-1r 后 economy 功能链完整到达最终性能断言：4 runs / commits、historyDelta 2、changed/undo/redo/map identity 与 session 均成立；唯一阻断为 rebuild / assign LongTask `674ms / 189ms`，前者超过 200ms。artifact `355973 / 3267 bytes`，SHA `D9F9EC7B...41B7E / 42C1E4E2...B66BE`；spans 将两项分别归到 api:rebuild / api:assign，digest 约 `47ms`。动态插入 350-1s，先给真实 10k economy 协议正例记录 operation 数与同步 validator 时长，再决定唯一实质优化；归因前不重跑浏览器。
- 350-1s 的 10k 正例确认 rebuild patch 为 `3241` 个 operation，旧 validator 同步耗时 `1158.2ms`。第一步把 operation ancestor overlap 与 writeSet 等价从二次扫描改为 prefix / Set，降到 `277.8ms`；第二步让 economy source projection 只读共享，applyOperations 对 patch 触达容器 copy-on-write，并移除 typed-array 逐字节重复 view 与对象 key 排序，独立两次为 `68.1 / 79.9ms`。validator 后 sourceMap 深等不变、既有 `44` 类协议拒绝全部成立，100k economy task 为 `99846 / 63463 / 28256 / 2655.2ms`；浏览器入口改为完整记录任意 LongTask、仅对 `>200ms` 硬失败，符合已冻结性能规则。修正后浏览器执行仍为 `0`，现交独立评审。
- 350-1s 独立评审 `ACCEPT / P0 0 / P1 0`。评审对 39 条 1～3 段路径的 `59319` 个有序序列穷举，新旧 overlap 判定完全一致；确认 COW 首触达复制 array / record / typed array、validator 只读 row.value，sameData / sameStringSet 语义未放宽。独立协议 `3241 ops / 81.1ms`、44 类拒绝，100k economy task `99846 / 63463 / 28256 / 2639.5ms`，typecheck、browser syntax 与 diff-check 全 PASS；browser `0`。现只重建并复验 economy。
- 350-1s 正式 build `1395 modules` 后 economy 固定 CDP 门退出 `0`：4 次 run / commit 均为 `economy.compute / worker / map-mirror / payloadOwnMap=false`，prepared 与 layers 都是 `point / labels / picking`；historyDelta `2`，changed / undoExact / redoExact / mapSame 为真，同 session `map-mt1gcqi0-2` 后续 `3/3` 复用，packet `3→436 / 3→9 / 3→6 / 3→6`，commit delta 全 `1`。仅记录 1 个 `128ms` LongTask，`overBudgetLongTasks=[]`；forbidden、application/page/health/GL error、Loading 残留全为 0。`economy-worker-browser-full.json` 为 `356088 bytes`、SHA-256 `3D0E03EC0D0F649BDA220438AC98613C04C5C903CFA76A134AA572B027093B35`；summary 为 `2535 bytes`、SHA-256 `0692A38ECB6434771B86633173E552FC1A4C316C9E2507E595800B7D8B27B607`，均位于既定 Z 盘目录。现交独立评审核对实证。
- 350-1s artifact 终核 `ACCEPT / P0 0 / P1 0`：同一独立评审重算大小与 SHA，确认 full 的单 session / runs / packets / commits / history / LongTask / error / Loading 实证与 summary 逐字段完全一致，未执行浏览器也未写文件。至此 350-1 五个 10k 核心事务入口完成，转入 350-2a。
- 350-2a 的旧 composite 首败已复现为 `regeneration_lock_conflict`：锁定河流 `#59` 的 `lakeEdges` 从 1 变 0，移除了湖 `#5` 的 outlet。修正不放宽锁断言，而是在 features topology 重建前由既有 river dependency closure 提取 lake guards，将关联湖泊与显式 Feature 锁按 ID 合并为 support snapshots；安全拓扑保留整套 pack/grid feature 身份、cells 与 in/out 镜像，不安全拓扑继续在 patch / commit 前拒绝。Composite 现报告 `40/40` 河流、`1/1` lake edge、rivers salt `0`；负例把关联湖泊 cells 改为陆地后得到 `regeneration_lock_conflict / feature / locked-feature-topology-changed`。同一 composite 的 100k 为 `99846 grid / 51950 pack / 57 patch operations / 8 checkpoints`；Feature 锁、50k/100k 河流锁、typecheck、语法和 diff-check 全 PASS，browser `0`。现交独立评审。
- 350-2b 实现统一 `task-350-browser-artifact.mjs`：artifact 固定含 schema/name/mode、started/finished、phase/active/completed、failure 与 full result，summary 单独保留每门关键标量；成功、功能失败和 teardown 失败都在 finally 持久化，再重抛原错误。Session 门 compact 递归列出 scenario、Worker run/session/packet 与每个 LongTask window；grid 门新增 refine/undo/redo 三窗；direct / compound 锁门只在各次 newMap settle 后观察产品操作，并补 final session idle、GL / Loading。四门保留任意 LongTask 明细，只对 `>200ms` 硬失败。helper 成功/失败落盘自测及四入口源码接线反例、五脚本 node check、diff-check 均 PASS；browser `0`，现交独立评审。
- 350-2b 首轮评审 `BLOCK / P0 0 / P1 3`：direct-domain 中 military ratios、market assignment、两次 economy rebuild 和冲突 assignment 均未 await Promise receipt；顶层 teardown 错误会覆盖原功能 failure，`Promise.race(close, delay)` 超时还会静默成功；grid passed 未要求 Loading false，compact 也不足以复核 fingerprint roundtrip、generation input、拓扑违规、afterMap / PNG / renderer。修正后五个调用全部 await；artifact 的首个 failure 不可覆盖，清理错误独立进入 `teardownFailures[]`，三类资源关闭超时统一产生 `browser_teardown_timeout`；grid 将 Loading 纳入硬门并把缺失标量写入 summary。helper 新增 dual-failure、teardown-only、never-close 三类反例，四入口源码反例、六脚本 node check 与 diff-check 全 PASS；browser `0`，等待 blocker-only 复审。
- 350-2b blocker-only 复审 `ACCEPT / P0 0 / P1 0`：同一独立评审逐项核对首轮三项 P1，并独立复跑 helper regression、六脚本 `node --check` 与 `git diff --check`，全部 PASS；确认四入口逐资源关闭、server callback error 拒绝、原错误重抛和 Grid 关键摘要真实。浏览器 / CDP 仍为 `0`；本预检阶段完成，转 350-2 正式 build 与五入口实跑。
- 正式 build `1395 modules` 后，350-2 的 10k session 首门按首败停止：两次全锁河流 no-op 都在已采用的 generation session 上返回 `reused=true / committed=true`，旧夹具却要求 `false → true`。唯一窄诊断确认 `sessionBefore` 为 `generation.compute / adopted / idle`，两次 id 都与其相同，teardown error `0`；这是 map-replica patch 允许锁命令原子更新常驻副本后的既定能力，不是 session stale。动态插入 350-2c：等待锁命令 patch 队列，硬证 session id 连续、mapRevision 对齐、lockFingerprint 变化，再要求两次 no-op 均复用；产品文件新增 `0`，独立评审前不做目标重试。
- 350-2c 独立评审 `ACCEPT / P0 0 / P1 0`：patch 源码对旧 checksum / revision 连续性做硬校验，失败即 invalidate；下一任务的 identity / revision / generation token / lock fingerprint 全匹配后才会 `reused=true`。评审据此确认新夹具没有接受 stale session，且 committed、salt/history、renderer、LongTask、Loading/GL/error 旧门均未放宽；artifact self-test、两脚本语法与 diff-check 独立 PASS，browser `0`。现只执行一次 10k session 目标复验。
- 350-2c 后唯一目标复验已越过 session patch / reuse，随后在 `rendererExact` 首败；原断言只报总布尔，尚不能区分 surface base、四类 buffer、picking、overlay 或 camera。此入口的一次窄诊断与一次目标复验已按规则用完，后四门继续冻结。动态登记 350-2d，当前仅把上述子身份和 surface ref/descriptor/bytes/valid 差异加入失败消息，Node 语法 / diff-check 后停止；需要额外一次聚焦浏览器诊断授权才能归因，不能凭静态猜测放宽 renderer 不变式。
- 用户随后为 350-2d 与修正后的目标复验新增最多 `5` 次浏览器额度；仍要求每轮首败即停、先完成最窄归因与独立评审。新增额度 `1 / 5` 只运行 `--no-op-diagnostic`，不执行后续 invalidation / fault 或其它 350-2 门。
- 新增额度 `1 / 5` 先暴露诊断夹具 TypeError：identity 中保存的是精简 surface before-image，却传入要求 aggregate / gpu 完整字段的 probe `exact()`；因此尚未获得产品漂移项。最窄修正改为直接比较 surface set / segments / alias / vertices、descriptor refs/ranges、CPU/GPU bytes/checksum/valid，并新增静态反例禁止不兼容 helper 调用；Node 门通过后仅以 `2 / 5` 重跑聚焦诊断。
- 新增额度 `2 / 5` 得到单一差异：所有 JS 身份与 CPU surface 精确不变，仅旧 probe 的 GPU aggregate bytes/checksum/综合 valid 为 false；该轮没有单列 geometry/color `isBuffer`，不能据此宣称 active buffer 已删除。下一次只在 afterMap、afterLockPatch、afterNoOps 三个时点记录 geometry/color liveness 与 context lost，区分真实删除与公式漂移；静态反例通过后使用 `3 / 5`，不运行完整 session 门。
- 新增额度 `3 / 5` 排除资源删除与 context loss：afterMap / afterLockPatch / afterNoOps 的 geometry、color 均 live。根因是旧 probe 将现有拆分 GPU 布局误按旧单 buffer / CPU 6-float bytes 校验。修正后分别读回 geometry + color，按正式 8MiB / 3-word / triangle-aligned 分段公式及 geometry/color/total bytes 验结构；完整 GPU before-image 与 after exact，CPU source 单独 exact，不再比较两个不同布局的 checksum。artifact 静态反例、脚本语法、prepared installer 9-case owner regression 与 diff-check PASS；产品改动 `0`，独立评审前不运行 `4 / 5`。
- 350-2d 首轮独立评审给出 `BLOCK / P0 0 / P1 1`：通用 capture/exact 已正确，但 hard-cell、两类 deferred replay、render recovery、100k upload 和 river invariant 仍留有跨布局 checksum 与 geometry-only 资源追踪。现统一改为 GPU aggregate 只与 GPU baseline 比 checksum，CPU/GPU 仅以 total byteLength 校验来源规模；temporary / active / baseline / upload binding 全部成对跟踪 geometry/color，静态自测同时禁止旧断言和单 alias 路径。helper regression、session/artifact 语法、prepared installer 9-case 与 diff-check PASS；browser 仍为 `3 / 5`，等待 blocker-only 复审。
- 首次 blocker-only 复审仍为 `BLOCK / P0 0 / P1 1`：late-context 的 CPU fingerprint 名为 `after.expected.surface`，绕过了按 `surfaceCpu` 变量名过滤的静态门。第六处直比已改为 total byteLength，静态自测改为拒绝所有 whole `.aggregate` 直接相等断言；GPU checksum 只允许在 GPU before-image `exact()` 内比较。browser 保持 `3 / 5`，再次 blocker-only 复审前不执行完整门。
- 第二次 blocker-only 复审 `ACCEPT / P0 0 / P1 0`：独立穷尽确认所有 `.aggregate` 硬断言仅取 byteLength，GPU checksum 只在 before-image `exact()` 比较；temporary/active/baseline 与 100k upload 的 geometry/color 双路径齐全，旧性能与错误门保持。独立 helper/语法/9-case/diff 门全 PASS，browser `0`；350-2d 接受，启动 `4 / 5` 完整 10k session。
- 新增额度 `4 / 5` 越过 350-2d，随后在 invalidation 首个 routes 的旧 `reused=false` 首败。`newMap` 已正式接纳 generation session，首操作 reuse 是预期；undo/lock 的 command patch 也应保持同 id 并推进 revision/fingerprint，换图应建立不同 adopted id 后再复用。插入 350-2e，迁移 10k invalidation、hard-cell、focused political 与 100k 首项；fresh-routes 诊断显式 invalidate 后才证明 fresh，fault/cancel 后 `reused=false` 原门保留。静态反例和独立评审前不使用最后 `5 / 5`。
- 350-2e 首轮评审 `BLOCK / P0 0 / P1 1`：原门对 checksum、adopted 与 binding 精确连续性不足。修正后统一 helper 逐阶段验证同 id/mapIdentity/generationToken、idle/adopted、revision 精确 `+1`、checksum changed/undo restored、lockFingerprint same/changed；replacement 必须新 id/new mapIdentity/revision 0/new checksum/fingerprint。artifact 自测固定 helper 与四阶段调用，blocker-only 接受前不使用 `5 / 5`。
- 首次 blocker-only 复审仍 `BLOCK / P0 0 / P1 1`：正式 replica checksum 是不可逆 lineage token；reused Worker commit 保持 token，undo/lock command patch 才生成新 token。现四次 reused operation 固定 checksum same，undo/lock patch 固定 changed，并移除“undo 恢复 generation checksum”；其余 revision/id/binding/fingerprint 硬门不变。静态自测同步，`5 / 5` 仍冻结。
- 第二次 blocker-only 复审 `ACCEPT / P0 0 / P1 0`：四次 reused operation checksum same、两次 command patch changed，七段 transition 的 exact revision/id/binding/fingerprint 与 replacement owner 门均成立；fault/cancel 后 fresh 未放宽。350-2e 接受，启动最后 `5 / 5` 完整 10k session。
- 新增额度 `5 / 5` 越过全部 continuity 后，在 one-shot rivers refresh fault 的公开错误码旧断言首败；实际 `operation_failed` 符合 RuntimeOperation 包络，内部 `worker_regeneration_refresh_fault` 只存在 cause。额度用尽。插入 350-2f：三类 fault caller 统一断言 public outer + raw cause，临时 run wrapper 在 finally 恢复；产品改动 `0`，无额外浏览器执行。
- 350-2f 首轮评审指出静态门可在删除 finally 后假绿；共享 helper 与 pending viewport 现分别锁住完整 try/finally/restore，并用两个删除-finally 负例证明不可绕过，blocker-only `ACCEPT / P0 0 / P1 0`。用户第二批再授权 `5` 次，`1 / 5` 完整 10k 复验越过 continuity 后确认公开 `operation_failed` 正确，但 raw cause 仍未被 manager wrapper 捕获。动态插入 350-2g：console API 每次通过安装时的 actions 对象动态读取 `runtimeActions.generate.regenerate`，故把临时 wrapper 移到该真实 action；wrapper 捕获 rejection 后重抛，外层 apiCall 保持公开包络，finally 恢复。产品文件 `0`，修正后 browser `0`，剩余 `4 / 5` 冻结至独立复审接受。
- 350-2g 在权威文档补齐后独立 `ACCEPT / P0 0 / P1 0`；第二批 `2 / 5` 仍在同一内部码断言首败，public `operation_failed` 正确。插入 350-2h：共享 action wrapper 与 pending viewport inline wrapper 都记录精确调用次数，raw error tree 直接写入 assertion / failure artifact；先用一次聚焦实证判断对象形状，再决定是否仍存在可观测内部 cause。产品文件 `0`，修正后 browser `0`，剩余 `3 / 5` 冻结至评审接受。
- 350-2h 首轮评审发现静态门未锁计数增量且三条 raw JSON 可跨函数借用；改成四函数独立 slice，并补删除两处 finally、两处增量和三条逐路径 JSON 的反例后 blocker-only `ACCEPT / P0 0 / P1 0`。第二批 `3 / 5` 取得 wrapperCalls=1 及完整 raw tree：`operation_failed / result-stream-complete` 的 cause 是 `river-water-tail-invalid`，river #291 在真实刷新故障之前已失败。Node 同 seed 重现同类 lake #7 / river #259：`2117 land → 2120 lake → 2121 spill land`，输出 lake 明确 natural-outlet。插入 350-2i；首版仅按原始 height/source closed 重算，被评审以正式 salt variation、closed 重判、depression resolution 与 climate evaluation 不同，以及 `Number()` / 自报 status 可绕过两项 P1 阻断。修正后生成器与 validator 共用精确流水线并按源 salt 复算完整 overflow expectation，lake 身份、outlet、spillCell 原始类型严格；开放湖、闭流可溢流、扰动改变最低岸格三正例及 terminal/伪造/强转负例齐全。地理协议、lake-overflow、river-network、锁定河流 50k/100k、worker-task、typecheck、build 与 diff-check PASS；Node validator 10k/100k `33.1/185.1ms`，独立 100k 冷/暖 `196.0/139.7–149.8ms`，未越硬线。产品/测试各 `2` 文件；blocker-only `ACCEPT / P0 0 / P1 0`、browser `0`，现使用第二批 `4 / 5` 完整 10k session。
- 第二批 `4 / 5` 越过 350-2i 后第一次到达真实 after-render fault，公开 code 为 `worker_regeneration_refresh_fault`。源码确认注入 Error 自带 code/stage/suggestion，RuntimeOperation 对这类结构化错误保持精确 code，console `apiCall` 同样保留 Error code；之前 `operation_failed` 是 lake protocolError 先失败后的包络，不能作为 fault 契约。插入 350-2j：三处 public code 改回精确注入码，raw tree、wrapperCalls=1、finally 恢复、回滚和 LongTask 门均保留；artifact self-test 增加正式源码证据及 public code 退回 operation_failed 的反例。Node/语法/diff-check PASS，产品 `0`、测试 `2` 文件；独立复审 `ACCEPT / P0 0 / P1 0`、browser `0`，现使用第二批最后 `5 / 5` 完整 10k session。
- 第二批最后 `5 / 5` 已通过此前 refresh fault、恢复 / 竞态、政治与 features / rivers，首败为 `hard-cell 第一格未生成 patch range`，artifact 位于 `Z:\tmp\codex\2026-08-20\task-350-map-core-cdp\worker-session-browser-full.json`，浏览器额度归零。正式 `refreshHeightCells` 明确以 `colorMode === "height"` 为局部 patch 前置；旧夹具没有从前序政治视图切回 height，地图替换按设计保留该显示偏好，因此本轮是夹具漂移。插入 350-2k：10k / 100k 统一保存、固定、逐操作核对并恢复 color mode；artifact regression 直接读取 renderer 源码，并证明删除 height 切换或原 mode 恢复会失败。产品 `0`、测试 `2` 文件；两脚本语法、artifact regression、diff-check 独立 PASS，评审 `ACCEPT / P0 0 / P1 0`、browser `0`。350-2k 已接受，没有新授权前不运行目标复验。

## 完成标准

- 20 / 20 固定浏览器入口在同一冻结产品提交上真实通过。
- 直接 CDP 终验覆盖固定 10k、代表性 100k、普通视图切换、context restore、旧档 / 保存恢复和错误面清零。
- 所有阻断完成一次窄归因；未执行项不以计划或旧结果代替。
- 浏览器结论、残余风险和 artifact 索引写入本文件；完成后第 350 项归档，但分支仍不得合入 `main`。
