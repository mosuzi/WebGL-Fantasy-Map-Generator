# 第 401 项：列表身份显示与选择交互精简

## 交付范围

用户要求普通模式列表隐藏 ID，重生成锁与定位固定右侧，去除多选框并改用 Ctrl+点击多选。主线程唯一写者，任务分支 `codex/task-401-player-list-identifiers`，版本 `0.5.119`，基线 `9f769be / 0.5.118`。已完成最终验收。

- 共用表格和排序栏按字段身份及 debug 元数据过滤，表头、表体、虚拟跨度和行缓存签名同步；普通模式不显示独立 ID 列。下拉选项、搜索和外交矩阵使用名称，debug 保留技术编号；内部身份、选择与导出字段保留，不清洗用户名称中的数字。
- 重生成锁移至定位左侧，两列固定右侧；无定位时锁贴右。删除列表多选框和原列表多选模式按钮，普通点击单选、Ctrl / Command 增减、Shift 连续选择，所有已选行高亮。键盘空格选择保持，锁 / 定位动作不触发行选择或双击编辑。
- 城市父级表格缓存补充锁定、批量选择和可锁对象依赖；锁命令的撤销 / 重做通过刷新调度发布锁快照，避免数据已写入而图标滞后。

## 验收结果

- 生产构建、版本领先、差异格式通过；既有列宽 / 表格几何 / 选择滚动 / 锁会话专项通过。正式 SFC 执行单选、Ctrl 增减、跨 240 行范围、行高亮、双击隔离及虚拟跨度断言通过。
- 模式恢复目标复验 5 类面板、桌面 / 窄屏 30 状态通过；锁修复目标复验 22 状态通过。
- `pnpm run check:ui-layout` 完整通过：28 个正式面板，桌面、690px、Chrome 实际 125% / 150% / 200% 共 5 布局，709 状态、24,200 次控件观察；排版问题 0、目标浏览器错误 0。本轮初始化健康错误也为 0。
- 24 张正式列表完成 debug 往返，其中 16 张实际恢复 ID 列，表头 / 表体 / 虚拟行一致，往返内容、选择、顺序与历史保持。
- 国家 / 城市 × 5 布局共 10 组真实 Ctrl 增减、锁定 / 解锁、撤销 / 重做及定位检查通过。横滚范围 184～200px，在开始 / 中间 / 末尾验证右侧固定列贴合且不重叠。
- 10 条既有不适用记录为高度面板与未连接云账号的列表选中；备注夹具只有一行，未冒充其真实多选验证，普通无锁列表选择逻辑由 SFC 专项覆盖。固定必需状态覆盖检查通过，未跳过正式面板。
- 人工复核窄屏和 200% 经济列表截图；构建来源与完整验收凭据绑定，提交继续由 pre-commit 核对。
- 验收产物根：`Z:/tmp/codex/2026-09-20/task-401-list-interaction/`。最终以 `accepted-final/report.json`、同目录截图及 `accepted-final.log` 为准；对应构建为 `lock-fix-build.log`。

## 首败与收敛记录

1. SFC 测试先后遇到编译器直接依赖路径和图标库 CJS 导入问题；最终使用 `vue/compiler-sfc` 与 `import.meta.resolve`，正式点击断言通过。
2. debug 开关打开开发面板时收起编辑面板；旧夹具用空 DOM id 恢复失败，改为注册的 `dataset.panelId` 后 30 状态通过。
3. 城市锁真实点击失败时，先修正虚拟行身份跟踪，仍失败。遵循连续失败规则暂停，用户再次授权修复后记录真实点击、锁命令返回和地图锁数据，证实 city #240 写入成功，图标仍旧。
4. 源码定位到城市父级 `v-memo` 遗漏锁依赖；补齐后通过锁定 / 解锁，另补上历史操作的状态发布，撤销 / 重做闭合。诊断注入代码已经移除，最终门不依赖调试钩子。

全部工作主线程完成，无委派；未扩大玩法或地图生成范围。旧暂停交接已由本记录取代，不再作为活动阻断。
