# 权威任务第 87 项来源与边界

本文记录用户于 2026-07-18 明确要求写入权威任务清单的悬停菜单层级问题。唯一执行状态、详细范围和最小验收仍以 `docs/current-plan.md` 为准；本文保存来源和排除边界，避免后续把单一层级缺陷扩大为全局浮层重构。

## 用户反馈

悬停菜单的层级低于国家和省份名称，菜单与政治标签重叠时会被标签遮挡。

## 计划内边界

- 调查悬停菜单、地图标签 overlay 和它们各自 stacking context 的真实结构。
- 只保证悬停信息卡（用户口语称悬停菜单）打开时位于国家、省份名称标签上方，并保持全部信息可见；该层是 `pointer-events: none` 的只读信息，不存在可点击动作。
- 保留国家、省份、城市和手工标签之间既有优先级、碰撞、点击、缩放和导出语义。
- 不顺带调整所有面板、对话框、下拉框、tooltip 或其它浮层的全局 z-index 体系。
- 不改变悬停信息卡的触发条件、定位算法、内容或关闭行为。

## 执行与验证

- 只采用“调查 → 开发 → 代码审查”并本地提交，不使用四级制度。
- 代码完成后只对第 87 项执行浏览器验证，第 64～86 项不得复测。
- 浏览器必须构造国家名、省份名分别与悬停信息卡重叠的场景，确认信息卡完整位于标签上方且不截获地图指针；同时检查缩放、平移和窄视口。
- 不影响最小验收的新发现只登记 `FOLLOWUPS.md`，不得在本项内顺手实施。

## 完成结果

悬停信息卡现以 `z-index: 5` 位于全部地图标签（最高 `4`）上方、地图工具栏（`6`）下方，并继续使用 `pointer-events: none`。专项回归、相关标签与选择回归、生产构建和差异检查通过；浏览器以真实国家 / 省份标签重叠、canvas 指针穿透、缩放、平移和窄视口验证通过，第 64～86 项没有复测。
