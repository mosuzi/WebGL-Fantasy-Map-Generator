# 第 384 项：城市人口编辑与即时统计方案

本文件是实施前建立的完整功能与验收方案；活动状态只看 [当前计划](../current-plan.md)。依赖第 383 项完成交付，不合并编号。

## 1. 现状

- CityPanel 已有隐藏在操作栏中的人口编辑器，但直接显示内部人口单位。显示函数以 `内部值 × 1000 × populationScale` 换算为“人”，当前输入容易被误解。
- `createSetCityPopulationCommand` 写 city / burg 并刷新城市大小，只重算 `politics.states.urban`，未覆盖省份、文化、宗教和非同一数组的 pack 镜像。
- 原统计逐城四舍五入到两位内部单位，细小人口变化可能被吞掉；撤销通过重新汇总，而非保存全部被写统计字段，旧数据不一定精确恢复。

## 2. 功能梳理

1. 选中城市时在详情附近直接展示人口输入和“应用人口”，输入单位明确为“人”，步长 `1`，支持 `0`，上限 `1,000,000,000` 人。保留操作栏入口作为同一编辑器的定位入口，避免两套状态。
2. 用当前显示倍率做双向换算；不改公开 `edit.cities.setPopulation` 的内部单位约定。保留足够内部精度，使倍率 `0.1 / 1 / 10` 及非整倍率下输入 / 显示往返不丢一个人。
3. 空白、NaN、Infinity、负数、超上限、删除城市、缺失合法 burg 拒绝，不生成历史。相同值 no-op，不把空输入静默当 `0`；应用有明确成功 / 失败反馈，忙碌时禁用，切换城市清理旧反馈与输入。
4. 应用后 city.population 与 burg.population 同步，立即重算所选城市所属国家、省份、文化、宗教的 `urban`，覆盖 politics / society 与 pack 的独立镜像数组；以当前有效城市为权威城镇载体，忽略删除对象。
5. 乡村人口 `rural`、grid / pack cell pop 不变，行政区总人口仍为 `rural + urban`，不改人口空间分布。刷新城市总人口、最大人口、大小 / 标签排序，以及已打开国家、省份、人口统计面板。中立 ID `0`、无省份城市不崩溃，不越界创造行政区。
6. 手工人口独立于潜在人口，不隐式设置锁或属性。第 383 项“按条件重算人口”共用同一统计路径；以后显式重算允许覆盖手工值，属性切换不会覆盖。
7. 人口变化不直接改经济 product / treasury、军事兵额或道路。界面说明这些值需相关重算，不承诺模拟自动发生。
8. 单一历史：保存实际写入字段的 before / after（含原缺字段），失败原子回滚，撤销 / 重做精确恢复人口、大小、受影响统计与镜像；不靠撤销时重新估算旧档。旧档正常保存 / 回读，公开 API 与 UI 一致。

## 3. 实施步骤

| 阶段 | 文件边界 | 验证 |
| --- | --- | --- |
| A 人口事务 | `runtime/city-edit-commands.js`；必要时独立人口统计 helper；沿用第 383 项局部标签刷新 | city / burg、四维所属统计、分离镜像、0 人、精度、单历史、no-op、回滚与旧字段精确撤销 |
| B UI 与换算 | `CityPanel.vue`、`city-panel.js`、`app.js`、`styles.css`、必要的共享人数转换 | 明确“人”、空值拒绝、倍率往返、忙碌和切换状态、成功失败反馈 |
| C 集成 | 目标专项与既有城市 / 人口检查，production 3k / 100k；一个独立验收智能体 | 输入后城市 / 省 / 国 / 人口面板即时正确，保存回读、撤销重做、窄布局、性能门 |

主线程唯一写者；不改变国家 / 省份边界、乡村人口、经济 / 军事自动刷新、整体人口调整算法和原 API 单位。分支从第 383 项合入后的最新 main 建立；每阶段有精简交接和冻结证据。

## 4. 逐项验收

- `384-F1` 按“人”输入，倍率换算正确，0 与上限可用，非法输入精确拒绝，不变值 no-op。
- `384-F2` city / burg 同步；国家 / 省份 / 文化 / 宗教的城市人口与总人口即时正确，独立镜像更新，乡村值不变。
- `384-F3` 未所属 / 中立 / 删除对象 / 旧档缺字段处理正确，无悬空统计写入。
- `384-F4` 原子失败、单历史、精确 undo / redo，不累积舍入误差；保存回读和 API 一致。
- `384-F5` 城市规模 / 自动图标和排序即时更新，手工视觉覆盖保持；设施、地形、路线、经济与军队不变。
- `384-F6` UI 同时检查城市、省份、国家和人口统计面板；倍率 `0.1 / 1 / 10`、键盘、切换城市、窄布局和忙碌状态。
- `384-F7` 小图后大图真实入口，目标 error 为 0，LongTask 遵守仓库规则，不用生成新图替代用户当前图操作。
- `384-F8` 一个只读智能体逐项对照此方案给出最终结论，接受后归档、递增版本、中文提交与推送主线。

## 5. 初始交接

已登记完整方案；产品 / 测试新增 `0`，未实施。下一步等待第 383 项完成，然后读取其最终接口与证据，从最新主线开始阶段 A。

## 6. 实施与集成冻结

- 第 383 项已以 `73de127 / 0.5.98` 推送主线，从该点创建 `codex/task-384-city-population-editing`。本项已准备版本 `0.5.99`。
- 人口事务独立为 `city-population-transaction.js`，九位内部小数、有效 city / burg 校验、四类所属 urban 及各独立镜像、单次求和、字段存在性与视觉 before / after。命令显式声明副本路径，保证后续重算消费更新后的文化 / 宗教统计。
- 详情直接显示“人”输入、应用与潜力重算；操作栏定位同一输入。提供验证、反馈和忙碌禁用，不再保留第二份编辑状态。接入运行时状态事件，因此同步交互清单排除说明；动作坞实际二级 58 / 直接 0 调整为 57 / 1，旧工具中 55 的计数同步校准。
- Node 专项 PASS：四倍率 × 四人数共 16 事务，含 0、1、上限、独立镜像、精确撤销重做、非法值、删除 / 缺失资料、中立无省份、故障回滚及保存回读。类型检查、inventory 和 production build PASS。
- 真实 3k / 100k UI 与一次独立方案对照验收进行中；完整产物 `Z:/tmp/codex/2026-09-16/task-384-city-population/`。
- 浏览器首败为夹具误读隐藏 DOM：主面板互斥，依次打开国家 / 省份 / 人口后再选城市会关闭前者。一次最窄校正为逐面板打开并在打开期间调用正式人口 API，随后唯一目标复验；3k 已通过，最大 `117ms`，错误为 `0`。未改变产品来迁就夹具。
- 本项工具约 200 行，超过产品约 150 行新增；例外仅用于四倍率、多所属镜像、旧字段快照及真实面板矩阵，不再扩展工具或重复大门。

## 7. 最终验收

- `browser-recheck.log`、`browser-recheck/report.json` 两档最终 PASS：3k / 100k 实际 `3015 / 99846` 格，LongTask `30 / 48` 条、最大 `117 / 171ms`；`>200ms`、console warning / error、health error、WebGL error 均为 `0`。四倍率 / 四面板即时同步、手动及条件重算、撤销重做、输入拒绝、忙碌、切换、键盘与 `574 / 574px` 窄布局通过。
- `node.log`、`static.log`、`build-final.log`、`dock-recheck.log`、`inventory.log`、`population-adjustment.log`、`panel-refresh.log` PASS；inventory 为 `109 / included 88 / excluded 21`，无未分类项。
- 既有 city-scale 的前置城市编辑与视觉 / 镜像检查通过；第 84 行固定 seed 必须四档齐全的假设在未改 `73de127` 同败，现为 `0 / 78 / 646 / 81`。同一 FOLLOWUPS 条目已更新当前证据，不在本项改生成器或旧夹具。
- 唯一只读智能体 `task384_plan_acceptance` 对照 F1～F8 全部 ACCEPT，并目视双规模窄布局截图。完成比例 100%，无委派等待，完成版本 `0.5.99`。
