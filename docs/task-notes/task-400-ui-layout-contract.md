# 第 400 项：界面排版约束和自动验收

## 冻结范围

主线程唯一写者；共用按钮和主题、调查命中的局部布局、浏览器检测器、构建指纹与提交门。保留文案、操作回调、地图渲染与数据。工具是本项主要交付，允许工具新增行数超过样式改动；没有委派。

## 阶段交接

- 调查完成：原检查只量溢出，普通中文字折行可以漏过。新增检测器已拒绝折行、裁切、重叠反例，多行例外仍检查裁切。
- 28 个正式面板和页签 / 选中 / 空筛选 / 搜索 / 对象详情完成集成调查，5 种布局共 610 个状态。36 条重复观察归为 2 处问题：文件名设置图标为 26px，经济交易筛选说明被满宽按钮挤压产生 11～13px 溢出。按证据修正到 32px 和内容宽度按钮 / 可换行说明。
- 首次调查脚本错误使用搜索按钮的精确无图标名称；源码诊断后改为稳定类，控制面板目标调查成功。没有把调查模式标成验收通过。
- 生成 / 经济 / 对象详情的桌面和窄屏目标复验 98 个状态、问题 0；补充备注、测量、全部公开对象类型和压力夹具后完成冻结终验。
- 检测器与提交门反例通过；产品规则采用默认单行和显式多行，生产构建、工作树、暂存树三处指纹闭合。
- checkpoint：任务分支未提交快照 `Z:/tmp/codex/2026-09-19/task-400-ui-layout/stage-checkpoint.diff`；调查和窄复验报告分别位于该目录的 `integrated-survey`、`target-recheck`。

## 最终验收

- `pnpm run build:app` 和 `pnpm run check:ui-layout` 通过，生产版本 `0.5.118`。
- 正式面板 28 个，桌面 / 690px / Chrome 实际 125% / 150% / 200% 共 5 种布局，每种 143 个状态，总计 715；控件观察 25,108 次，包含实际检查菜单项的 57 个状态。折行、裁切、重叠、尺寸及横溢问题均为 0，目标操作窗口浏览器错误 0。
- 10 条不适用记录为高度面板无列表选中流程，以及未连接云账号时无文件行，各布局如实记录，不冒充覆盖云端文件选择。忙碌 / 错误夹具仅证明排版规则，不替代异步业务测试。
- 页面初始化期间 1 条既有健康日志单独保存，不纳入目标操作窗口，也未写成“整页所有健康事件为零”。
- 人工查看桌面 / 窄屏交易筛选及对象详情截图。菜单项使用非 button 元素，最终检测器已补充其语义角色与反例，并要求打开菜单后实际检查到菜单项。
- 本地 `setup:ui-layout-hook` 已启用；缺失 / 过期 / 新增样式的隔离提交反例拒绝，最终真实暂存树凭据验证通过。源码压缩包无 Git 元数据仍能构建，但不签发 Git 提交凭据。
- 最终产物：`Z:/tmp/codex/2026-09-19/task-400-ui-layout/accepted/report.json`、同目录截图、`accepted.log` 和 `final-build.log`；旧 `final` 目录为菜单语义补强前的中间证据，以 `accepted` 为准。
- 产品改动为 5 个界面文件、新增 27 / 删除 8 行；新增主浏览器入口 209 行，未超过 500 行约束。其余为检测、构建和交付工具与文档；主线程完成，无委派等待。

本项达到验收边界后停止。长期命令与边界见 [界面排版契约](../architecture/ui-layout-contract.md)。
