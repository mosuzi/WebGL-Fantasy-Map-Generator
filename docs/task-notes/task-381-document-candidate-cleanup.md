# 第 381 项：失效候选与伪待办入口清理

## 冻结目标

现行文档只保留两个状态入口：活动任务看 [`../current-plan.md`](../current-plan.md#权威任务清单)，未获批准且仍有当前证据的阶段外事项看根目录 [`../../FOLLOWUPS.md`](../../FOLLOWUPS.md)。已经转正、完成、暂缓或只属于早期方案的内容只能作为历史证据，不得继续以“下一批候选”“当前进入”或旧分支禁令出现在现行索引中。

## 阶段矩阵

| 阶段 | 目标 | 最小验收 | 非目标 | 文件边界 | 廉价门 | 冻结门 |
|---|---|---|---|---|---|---|
| 1. 发现与判定 | 识别仍会被误读为当前待办的现行文档 | 每个命中归为现行规则、历史证据或失效候选源 | 不改归档语义、旧日志、计划备份和生成物 | 根入口、`docs/` 现行手写文档 | 定向关键词与链接检索 | 没有未分类的候选入口 |
| 2. 集中清理 | 校准索引、改名旧候选池、撤销伪待办语义 | 两个唯一入口明确；旧候选池只保留转正历史 | 不重新评估或实现远期功能 | `docs/README.md`、`docs/task-notes/`、相关审计、`FOLLOWUPS.md`、`AGENTS.md` | 旧文件名与已知陈旧句式差集为零 | 改动均能回指完成任务或本轮判定 |
| 3. 归档交付 | 同步权威计划、日志、归档、版本与索引 | 文档链接、归档检查、版本检查和差异检查通过 | 不运行产品浏览器或全量产品门禁 | 本项文档与 `package.json` | 相对链接检查 | `origin/main` 包含完成提交且任务分支删除 |

## 判定规则

- 任务归档、开发日志、计划备份、生成物中的旧“下一步”保持原文，只在追溯时读取。
- 已全部转为权威任务并完成的 `Q-*` 文件保留调查范围和编号映射，但改名为“转正历史”，不再称为下一批候选池。
- 未经本轮当前证据复核的远期设想从候选源降为历史对照记录；若未来重新发现真实缺口，必须以新证据登记到 `FOLLOWUPS.md`。
- 早期计划或里程碑若首屏已明确标为历史，不因正文保留当时用语而逐段改写；现行索引不得把其中的步骤当作当前状态。

## 首轮判定

- `docs/task-notes/README.md` 的第 349～351 项旧分支禁令、第 284 项“当前进入 284-E”均已被后续归档推翻，必须删除或改为完成历史。
- `Q-12～Q-33` 已分别转为第 88～93、101～107、201～203、211～216 项并完成；对应三份“下一批”文件只保留转正历史。
- `source-feature-review-history.md` 中完成项已有权威归档，剩余远期设想没有当前复核证据；本文件不再承担候选池职责。
- 早期 WebGL 验收计划与里程碑已经在首屏写明历史状态，可保留正文作为阶段证据。

## 完成结果

- 三份 `Q-12～Q-33` 旧候选池已改名并校准为转正历史，全部映射任务均明确为完成。
- 原版功能积压已改为功能对照复核历史，未复核的远期设想不再构成当前候选。
- `task-notes` 索引已撤销第 284、349～351 项旧状态，第 350 项计划撤销失效分支禁令；相关审计入口改为历史说明。
- 根目录 `FOLLOWUPS.md` 仍为唯一未批准事项入口且当前条目为 `0`；`docs/current-plan.md` 仍为唯一活动任务入口。
- 归档工具机械校准 2026-08-22～31 分卷的陈旧头部计数 `23 → 29`，并修正两条错误的 `./task-notes/` 相对路径，没有改变既有任务语义。
- 产品文件与工具文件改动均为 `0`；改动 Markdown 相对链接、归档一致性、版本和差异格式门禁通过，完成版本 `0.5.95`。
