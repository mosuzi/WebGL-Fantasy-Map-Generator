# 第 397 项：本批大文件边界复核

- `app.js` 保持地图、历史与生命周期的唯一所有者。本批新增保存状态、恢复点、城市引导、搜索和预设已分别在独立模块，应用文件只接线；继续机械拆分会转移闭包依赖，当前不做。
- `map-file-io.js` 的高分辨率临时绘制、显卡预检与恢复抽离到 `png-export-resolution.js`，纯尺寸规则在 `png-export-size.js`；裁剪 / 相机仍归原调用者。抽离层在绘制失败时自身恢复尺寸，调用者 finally 恢复相机和显示。
- 统一界面主题独立在 `panel-theme.css`，清除原大样式根节点被其覆盖的重复颜色变量，只有一个有效颜色入口。
- 曾评估进一步切分一万多行样式，但发现数十个既有验收直接读取原文件；为避免把本批条件性整理扩成全库测试读取迁移，暂保留原布局聚合文件。地图覆盖层与界面迁移边界已有脚本保护，不按行数机械拆分。
- 静态构建与差异检查通过；统一浏览器验收包含 PNG 成功 / 失败恢复和主题结果。未以抽离行数声称性能收益。
