# 权威任务第 82～86 项来源与边界

本文记录用户于 2026-07-18 明确批准进入权威任务清单的五项需求。唯一执行状态、详细范围和最小验收仍以 `docs/current-plan.md` 为准；本文只保存来源、编号映射和共同边界，避免后续接手时把口头反馈误拆、改序或扩展。

## 编号映射

| 编号 | 用户需求 | 计划内边界 |
|---|---|---|
| 82 | 未打开过的管理面板首次默认位于画布左侧 | 只改没有有效旧状态的首次自动落点；保留手动位置、旧状态、重开和窄视口契约 |
| 83 | 生成湖泊的名字大部分使用“湖” | 只改新生成湖泊类型词权重；不重写旧图、导入图或用户改名 |
| 84 | 画布及其标签等展示文本不可选中 | 只限制地图视口和 overlay；输入框、文本域与编辑器继续可选中 |
| 85 | 主题导入按钮文字与导出按钮不一致 | 只统一同一动作组文字和按钮盒模型；不改变文件导入 / 导出行为 |
| 86 | 水体与地貌统计详情区排版松散 | 只整理 `FeaturePanel` 详情字段布局；不改统计、筛选、编辑或其它面板数据 |

第 86 项的截图证据显示：桌面宽度下，身份字段之后的面积、岸线与水文字段被不均匀分配到多列，产生超过一个详情行的大块空白。验收应使用同类海洋 / 湖泊样本和窄面板对照，不能只靠静态 CSS 审查宣称完成。

## 共同执行方式

- 顺序固定为 `82 -> 83 -> 84 -> 85 -> 86`。
- 每项只采用“调查 → 开发 → 代码审查”并单独提交，不再使用四级制度。
- 五项代码全部完成后统一执行第 82～86 项浏览器验证；第 64～81 项已完成，不得复测。
- 浏览器统一验收通过后一次性推送本轮提交，中途不逐项推送。
- 不影响当前任务最小验收的新发现只写入 `FOLLOWUPS.md`，不得顺手实施或扩大完成标准。

## 完成结果

第 82～86 项已依次完成调查、开发、代码审查和单项本地提交，随后只统一验证本批五项，第 64～81 项没有复测。浏览器覆盖首次面板左侧落点与重开、湖泊列表后缀分布、地图文本全选 / 拖选边界、主题按钮计算样式与真实文件选择、水体详情桌面 / 窄面板布局；验证标签页、临时主题文件和烟测用户主题均已清理。唯一新发现是既有 Feature UI/API 回归仍硬编码旧 API 总数，已登记 `FOLLOWUPS.md`，没有在本批越界修改。
