# 第 386 项：回归失配校准

本项按用户简化三级要求处理：开发 → 静态验收 → 本地提交；实际功能回归统一在批末执行。

| 项目 | 归因与修改 | 尚待验证 |
| --- | --- | --- |
| 城市规模 | 六个阈值边界已有固定人口夹具；删除对单个 seed 必须生成四档的额外假设，保留全部城市 / burg / 自动剪影同公式断言与合法分组覆盖 | 运行专项剩余政治编辑与撤销断言 |
| 路线拾取 | 当前直接使用 canonical points 局部变量；源码循环文本已过时，改为 indexed / fallback 实际折线命中、拒绝端点弦线、路径变更后旧点失效 | 运行行为专项 |
| 经济双域锁 | createRebuildEconomyCommand 只在没有 pack.cells 时 no-op；经济重算还写城市产出、税收和摘要，锁市场 / 交易不能推导整个事务 no-op | 全锁原像、归属和完整经济撤销 / 重做专项 |
| 锁定首都路线 | 原断言混同干道直达与经支路接入；增加实际道路 / 支路图遍历及当前首都、锁身份分类；暂保留原断言，不凭静态推断删掉失败信号 | 批末实际输出分类；如证实为产品缺陷，按本项既有规则单独裁定窄修，不改生成锁语义 |

静态验收：四个修改脚本均通过 node --check，git diff --check 通过。未运行生成与浏览器，不声称四个专项已通过。产品改动 0，工具 4 文件；新增诊断和行为断言用于批末必要验收，属于本调查任务交付主体。

源码依据：runtime/economy-edit-commands.js 的经济命令；generator/economy.js 的城市生产、国家税收和政治经济更新；renderer/picking.js 的 routeSegments 与 pickRoute；生成关系约束文档不承诺每个锁定首都必须直达干道。
