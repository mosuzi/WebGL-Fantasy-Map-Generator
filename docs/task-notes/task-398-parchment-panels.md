# 第 398 项：编辑面板羊皮纸质感

用户反馈现有面板背景过白。本项将共用主题调整为暖米色纸底、墨色文字和棕色柔边，用轻微颗粒与静态明暗渐变表现纸张质感。输入和表格采用更浅的米色，选中、焦点继续使用清晰的青蓝色。

## 实现边界

- `app/webgl-generator/src/panel-theme.css` 统一面板、字段、表格和挂到页面外的弹层色调。
- `app/webgl-generator/src/assets/parchment-grain.svg` 为可平铺的静态纸纹；仅作为背景图片，不增加拦截输入的覆盖节点。
- 保持现有布局、编辑逻辑、地图颜色和导出行为；无动画、无额外主题开关。

## 验收结果

- `pnpm run build:app` 通过；只有既有的大包体积提示。
- 真实 Chrome 隔离页面，1440px / 690px 两种宽度下检查城市、高度、备注面板，共 6 组均显示暖纸底和纸纹，无横向溢出。
- 窄屏菜单与搜索弹层主题一致；搜索输入、结果及 Esc 关闭通过，搜索容器宽度与内容宽度均为 650px。
- 浏览器错误为 0；旧主题覆盖与新主题下正式地图 PNG 数据完全相同。
- 已人工查看城市面板和窄屏搜索截图，文字清楚、纸纹克制。此次只验证共用主题相关代表界面，不重复上一批的全部编辑业务验收。
- 第一次浏览器启动遇到预览服务未运行，未进入页面；启动服务后验收通过，没有产品首败或范围扩修。

本地产物：`Z:/tmp/codex/2026-09-19/task-398-parchment/`，含 `browser.json`、`build.log`、桌面及窄屏截图和临时验收脚本。交付版本 `0.5.116`，独立分支 `codex/task-398-parchment-panels`。
