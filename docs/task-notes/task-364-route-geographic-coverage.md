# 第 364 项：真实存档道路重生成的全陆地覆盖正确性

## 任务包

| 字段 | 内容 |
| --- | --- |
| 权威编号与目标 | 第 364 项；修复指定真实存档道路重生成后西陆整片无路 |
| 当前阶段 | `364-C` 正式 UI 联合终验 |
| 冻结点 | `codex/task-364-route-geographic-coverage`；从 `9fa4e2286c1e4af45743a97e777fc88a96089ddc` 派生，当前为未提交窄修 |
| 允许文件 | 道路生成消费边界、道路锁/覆盖专项回归、任务文档、版本与完成记录 |
| 禁止文件 | `source/`、城市/地形/视觉、其它重生成领域、全局锁闭包 |
| 必须保持 | 锁定城市和路线 before-image、共享边 owner、陆海可达、镜像、单历史提交、撤销/重做、失败回滚 |
| 最小验收 | 唯一存档两个正式 UI 入口重生成后，西陆及其它符合既有条件的陆地分区均有道路，目标首都全部接入且陆路不入海 |
| 首个廉价门 | `pnpm run regress:regeneration-lock-city-route` |
| 停止条件 | 真实存档身份变化、同一浏览器夹具连续两次失败、必须改变既有“合理覆盖”规则 |

## 364-A 调查结果

- 状态：`ACCEPT`。
- 指定存档身份：`krichars (3).webfmg`，`9,642,587 bytes`，SHA-256 `CF7402BC2BEA22AD1FCDE441444479F880DC0DB15D55520EF5A1A399D335DA61`。
- 指定存档西陆 `feature 3`：`9732` 个陆地 pack cell、`350` 座城市、`6` 座首都、`74` 座省会；`9052` 个可通行 cell 形成一个包含全部城市的连通分量。
- 首败：重生成前为 `114` 条陆路、`1439` 个道路格、触达 `205` 座城市；重生成后整片变为 `0 / 0 / 0`。候选预算保留全部 `6` 座西陆首都，Urquhart 候选边存在，排除候选/寻路断开。
- 根因：显式锁定城市 `8`（burg `3625`）的支撑闭包合法包含西陆 feature；`buildPackRoutes` 错误把支撑 feature 当成路线生成禁区，同时跳过 road / trail，导致整片大陆归零。锁闭包本身无错，道路生成不应把只读地理支撑解释成禁止新增路线。
- 产品改动：`0`。工具 / 文档改动：权威任务登记与本地只读诊断；正式测试尚未修改。
- artifact：`work/task-364-route-coverage-diagnose.mjs`，指定存档分区、候选、可达和锁闭包诊断。

## 364-B 阶段结果

- 状态：`ACCEPT`。
- 完成：道路生成继续保留锁路的边与 owner，但不再用 `lockedFeatures` 排除 road、trail 或 searoute 分组；全局锁闭包、城市和 feature 均未修改。
- 产品改动：`1` 文件，`+3 / -6`；工具改动：`1` 文件，`+38 / -1`。工具代码大于产品改动，是为了把“锁住多首都大陆的一个首都后，该大陆干道与全部目标首都仍受覆盖”固化为结果级回归。
- 已过门禁：锁城道路专项 PASS，确定性夹具 `feature 2 / 14` 座目标首都、`13` 条干道、未接入首都 `0`；锁路共享边 PASS；10k / 50k / 100k 路网连通 PASS；网络质量 PASS；海路同水体 PASS。
- 指定存档复验：西陆恢复 `110` 条陆路、`1448` 个道路格、触达 `209` 座城市，`6 / 6` 首都接入；东部主大陆 `192` 条陆路、`12 / 12` 首都接入；锁定首都原像不变，陆路进入水格 `0`。
- 首败或阻断：扩大到全领域 `regress:worker-task` 时，既有 `features` handler 出现与道路无关的补丁声明差异；按首败即停规则没有重复全跑，也不把该门声称为通过。本任务道路专项与指定存档门不受该首败影响。
- 下一步：`364-C`，production build 正式导入唯一存档，分别从中央入口和 RoutePanel 专用入口做可视化、分区覆盖、锁、历史与错误面联合验收。

## 364-C 阶段结果

- 状态：`ACCEPT`。
- production build 从正式导入控件两次加载唯一存档；页面显示 `seed stage-2-1`，初始路线 / 段数均为 `442 / 7976`。
- 中央入口结果为 `432 / 7615`，RoutePanel 明确显示路线重新生成完成；撤销 / 重做路线数为 `432 → 442 → 432`。
- 专用入口从新导入存档得到相同 `442 / 7976 → 432 / 7615`。再把路线 `#0` 锁定后重算为 `436 / 7822`，该路线的 `道路 / 庐陵 / 霜寒 / 61 / 1112.6 千米` before-image 与 `aria-pressed=true` 锁状态均不变。
- 两次无面板全图画面均直接看到西陆各国、城市之间的完整道路网；没有整片空白大陆。指定存档 Node 结果门同时证明西陆 `6 / 6` 目标首都接入、陆路水格 `0`。
- 浏览器终态：console error `0`、warn `0`、WebGL error `0`、可见 Loading `0`、`aria-busy=true` 为 `0`。路线 identity、拾取刷新与 render preparation 专项均 PASS。
- 完成比例：`100%`。产品改动 `1` 文件 `+3 / -6`；工具改动 `1` 文件 `+38 / -1`；其余为任务、专题、日志、版本与归档文档。委派等待 `0`。
