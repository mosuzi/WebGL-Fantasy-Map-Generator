# 2026-08-15 至 2026-08-21 权威任务归档

本卷保存 本时间片内完成的权威任务 的历史权威任务正文。当前可执行任务仍以 [当前开发计划](../current-plan.md) 为唯一权威来源。

- 归档条目数：4
- 排序方式：按权威任务编号升序
- 状态边界：只收录已完成、已移除或已由后续方案取代的条目；未完成与暂缓项不得进入归档。

## 任务条目

- **权威任务第 334 项：收敛 100k Worker 端到端延迟与画布假死。** `已完成；来源：用户要求坚持 Worker 化并解决大型地图保存、读取、显示、重生成与画布响应退化`
   - **目标与边界**：计算、显示、生成、导入、保存、撤销与重做共用一个长期 MapWorker canonical owner；冲突地图操作仍由单一 operation owner 拒绝，不开放任务积压，不恢复主线程重计算 fallback，不以删图层、降低标签 / picking / GPU 精度或放宽通用 LongTask 阈值换取提速。主线程保留现有 UI、编辑和 renderer 所需兼容 map 投影，辅助 Worker 不持有完整地图或提交权。
   - **显示与存档完成记录**：首次完整输入的 checksum 随 graph stream 增量生成和验证，暖操作复用同一 session；新图、导入和恢复在 Worker 内 adoption，读取使用 v3 canonical section 紧凑 handoff 与有界 chunk，保存由 owner 直接编码。100k 颜色视图使用约 `1.60MB` cell colors 原位事务，海底使用约 `0.88MB` 水域 patch；首次国家视图约 `290ms`，海底开 / 关约 `263 / 254ms`，控件、相机与实际画面在首次切换、连续切换及缩放后同源。
   - **可靠性与重生成完成记录**：省份 / 城镇在写入前执行省会镜像预检；已知不一致以 `regeneration_preflight_rejected` 安全拒绝，Worker session delta `0` 提交后继续 idle，不写 revision / history。省份只准备当前展示实际消费的 layer，全 cell color patch可原位提交；picking DTO 在 canonical 回绑循环内按预算分片，单 bucket `500000` 引用的同步切片 `<50ms`。固定 100k 省份由约 `9529ms` 降至 `2799ms`，城镇由约 `2558ms` 降至 `1197ms`，均复用 owner 且只输入 `3` 包。
   - **视觉与性能边界**：平滑边界对最终仍被安全三角化拒绝的异常 cell 使用 canonical 硬边 center fan 先绘兜底；正常 10k / 50k emergency 为 `0`，固定异常 cell 为 `1` 个兜底、`4` 个有限三角，六种隔离显示模式无缺面或 WebGL 错误。省份优化后仍有单条 `92ms` LongTask，已按用户既定“调查、实质优化一次、目标复验后，200ms 内可精确登记”的规则保留；通用阈值未提高。完整 shoreline 的既有 coverage / lineTriangleCount 历史阈值失败未伪称通过，也未归因于本修。
   - **验收记录**：preflight、render preparation、prepared installer、十一类 Worker、单 bucket 分片、生产构建与隔离浏览器拒绝 session 门通过；100k 两项 artifact 的 Loading、非性能 health、page 与 WebGL 错误为 `0`，console 仅保留性能 health。独立集成复核与最终观察使关闭各自唯一阻断后给出 `ACCEPT`。版本为 `0.3.16`，`source/`、Wiki 和用户地图未改。

- **权威任务第 335 项：GPU 常驻视图与零重编译切换。** `已完成；来源：用户要求根治 100k 视图切换端到端卡顿，不能再用 Worker 算法耗时冒充上屏性能`
  - **目标与边界**：坚持唯一长期 MapWorker canonical owner、单一地图写 operation owner、WebGL2 主画布和既有视觉 / picking / 回滚精度；普通颜色视图、主题、海底及不改变几何的图层切换不得再触发整图序列化、`regeneration.compute`、`render.prepare`、surface geometry 重建、全 overlay 替换或 picking 重绑。把当前烘焙为 `x/y/rgba` 的约 `105MB` 100k surface base 收敛为稳定几何、packed cell identity / side 与 GPU 常驻 cell attribute / palette；地图写入、撤销和重做只发布增量属性 / 拓扑失效。平滑边界保留独立几何语义，以边界 correction 或受控缓存切换，不能删图层、降精度、隐藏预热或放宽 LongTask 阈值。
  - **已知证据**：历史 100k fresh 路径总计约 `10.029s`，其中完整地图输入 `6.829s`、领域计算 `0.546s`、结果接收 `0.373s`、主线程渲染安装 `1.887s`；旧省份重生成 `9.529s` 中领域算法约 `1.346s`、Worker 渲染资料构建 `4.886s`、输出流 `1.073s`、主线程安装 `1.887s`。真正 WebGL draw 只约毫秒至几十毫秒，瓶颈是视图切换前的全图复制、CPU 渲染编译、解码、事务安装与 DOM 提交。第 334 项已让暖 100k 颜色视图约 `142～257ms`，但主题 / 平滑仍约 `0.59～1.16s`，cold / session 失效及旧开发服务混用没有被该热路径结果覆盖。
  - **阶段状态**：335-A～335-J 全部接受，版本依次为 `0.3.17～0.3.25 / 0.4.0`。B～C 建立稳定 packed surface 与 GPU cell attribute / palette；D～F 让普通 / 政治颜色、主题、海底和普通图层本地提交；G 收敛平滑 correction 与岸线缓存；H 稳定 point / overlay / city / picking identity；I 完成 latest-wins、build handshake 与一次性会话自愈。J 的 10k / 50k / 100k、旧档保存读取、故障与真实用户 5410 标签页统一验收通过。
  - **统一验收记录**：100k 主题 / 海底 / 标签 / 路线为 `105.5 / 16.1 / 17.3 / 15.6ms`，普通显示 LongTask 为 `0`；100k 保存压缩为 `7,288,011B`。真实 10k 强制 WebGL context 丢失 / 恢复后，高度 / 生物群系 / 国家逐像素一致，GPU program / buffer / texture 引用全部重建，LongTask、health、Loading 与 WebGL error 为 `0`。用户原 5410 标签页恢复到 `v0.4.0` 后，缩放前后多次切换控件与画面不再失效或反转。
  - **实施阶段**：335-A 冻结 10k / 50k / 100k 的 cold、warm、revision 变化、session 丢失和快速切换分段账本；335-B 把 surface base 改为位置 + packed cellId / side 的稳定几何；335-C 建立 GPU cell attribute / palette store 与增量更新；335-D 把高度 / 生物群系 / 人口等普通颜色模式改为 shader 状态切换；335-E 把国家 / 省份颜色与政治 topology cache 解耦；335-F 把主题、海底和普通图层移出 Worker 渲染准备；335-G 收敛平滑边界 correction / cache；335-H 保持 overlay / city / picking identity，只按真实语义变化增量更新；335-I 实现 UI latest-wins、正式提交后高亮、页面 / Worker build handshake 与一次性会话自愈；335-J 完成冷热、故障、旧数据、视觉、真实用户标签页和 100k 统一终验。
  - **最小验收**：固定 100k 普通颜色视图 cold 首次 `≤150ms`、warm `≤50ms`，Worker 地图输入、`render.prepare`、surface geometry 构建、overlay replace 与 picking rebuild 均为 `0`；主题 `≤150ms`、海底 `≤100ms`、普通图层 `≤50ms`；平滑边界首次 `≤300ms`、重复 `≤100ms`。全部入口单个主线程任务 `<50ms` 且 LongTask `0`，不继承既有 `≤200ms` 登记。首次点击、A→B→C 快速切换、切换中及切换后平移缩放、revision 前进、Worker 重启、撤销 / 重做、保存 / 读取和 WebGL context restore 后，控件、renderer、实际像素、标签、城市、路线 / 河流、选择 / 高亮、picking、PNG 必须同源。
  - **非目标与交付**：不修改 `source/`，不改变持久存档 schema，不开放冲突地图写任务积压，不恢复主线程地图重计算 fallback，不用后台隐藏预热换取表面成绩。每阶段只在当前任务分支建立一次本地 checkpoint 并同步递增 patch 版本；阶段 checkpoint 不推送、不合入 `main`。这是明显架构阶段升级，最终集成交付评估升至 `0.4.0`，全部阶段统一验收后才推送、合并和归档。详细设计与阶段冻结见 [`task-notes/task-335-gpu-resident-view-switch.md`](./task-notes/task-335-gpu-resident-view-switch.md)。

- **权威任务第 336 项：平滑边界跨视图陆海着色一致性。** `已完成；来源：用户报告多次切换视图后国家视图出现深绿色海洋 cell 与高原无人区异常色`
  - **根因与边界**：现场异常区域的 authoritative 数据仍是海洋 cell，政治 palette、无人区和 natural zone 均非根因。100k 细分网格含非凸 cell，旧 hard surface 以 seed center 对每条边扇分，部分中心不在安全多边形内，生成的三角越过真实 cell 边界；GPU 常驻视图切换复用这份 hard base 后显露为陆地色海洋三角。
  - **修复记录**：hard surface 复用正式安全 Earcut 边界三角化，只有安全三角化明确拒绝时才回退既有 center fan；可变三角数量通过正式 `surfaceCellRanges` 驱动 hard-cell 增量 patch，不恢复整图 Worker，不更改政治配色、平滑 correction、地图数据或持久格式。
  - **验收记录**：非凸外置中心 Node 反例、render preparation、生产构建与 100k 平滑专项通过；开 / 关 / 再开为 `18.2 / 18.6 / 16.7ms`，Worker、surface / line 重建和 LongTask 为 `0`。用户原 5410 标签页多轮高度、生物群系、省份、人口、国家往返后，海岸外越界色块消失，国家控件和画面同源，console / page error 为 `0`。版本为 `0.4.1`，`source/`、Wiki、存档 schema 和 Worker 协议未改。

- **权威任务第 337 项：精细共享顶点与 hard surface 一致性。** `已完成；来源：第 336 项发布后用户继续观察到旧色块与 cell 边框瞬态`
  - **根因与边界**：固定 100k 地图中，精细共享顶点解析会令 `60` 个 cell 边界自交；第 336 项安全 Earcut 拒绝后仍回退旧中心扇分，其中 `9` 个 cell 的 `12` 个三角越出自身边界。局部显示更新复用 hard surface 时便显露海岸深色色块及海岸 / 高山逐 cell 接缝；平滑 correction、政治 palette、地图数据与 HMR 生命周期不是根因。
  - **修复记录**：精细顶点提交前按全部关联 cell 检查自交，危险共享组整体恢复原始坐标，保证相邻 cell 使用同一边界；hard surface 仅接受安全精细边界或安全原始边界三角化，两者都不安全时禁止恢复越界中心扇分。Worker、地图 revision、history、overlay / picking、存档与生成数据不变。
  - **验收记录**：固定 `10k / 50k / 100k` resolved / stored 跳过均为 `0 / 0`，100k 的越界三角归零；surface Node 专项与生产构建通过。用户原 5410 标签页在平滑开启、约 `50 千米` 比例下连续切换高度 / 生物群系 / 省份 / 人口 / 国家，海岸与高地未再出现深色三角或逐 cell 接缝，最终恢复国家视图和适配视图。旧 shoreline pure 的陈旧源码字符串断言仍失败，未在本项扩修。版本为 `0.4.2`，`source/`、Wiki、schema、Worker 协议和生成算法未改。
