# 2026-09-15 至 2026-09-21 权威任务归档

本卷保存 本时间片内完成的权威任务 的历史权威任务正文。当前可执行任务仍以 [当前开发计划](../current-plan.md) 为唯一权威来源。

- 归档条目数：17
- 排序方式：按权威任务编号升序
- 状态边界：只收录已完成、已移除或已由后续方案取代的条目；未完成与暂缓项不得进入归档。

## 任务条目

- **权威任务第 382 项：城市管理选中城市属性图标快捷操作。** `已完成（2026-09-16）；六属性、兼容事务、局部刷新与性能门收口`

  - **用户需求**：城市选中详情旁增加首都、贸易中心、要塞、城墙、港口等 icon 按钮群；当前属性点亮，具备中文提示、可访问名称和键盘操作，随切换城市与撤销 / 重做同步。
  - **范围**：独立分支 `codex/task-382-city-attribute-shortcuts`，主线程唯一写者；城市面板、回调、局部样式与属性命令，复用编辑历史、首都事务和港口条件。不新增存档字段或重生成行为，不改 `source/`。性能窄修限于属性 effects、刷新调度与 renderer。
  - **语义**：六按钮为首都、贸易中心、要塞、城墙、港口、神庙。贸易中心使用既有 `plaza`；首都只设立 / 转移，取消需选择本国另一城市；港口不移动城市，使用有效水体 ID，有海路连接时提示先调整海路。city / burg 镜像、旧档单边港口和缺字段保持，单历史、no-op、异常回滚与保存回读闭合。
  - **最小验收**：类型 / 构建和目标静态检查、属性 / 既有城市编辑专项、真实浏览器逐属性操作与撤销 / 重做、首都迁移和港口地理拒绝、键盘、窄面板无溢出、目标错误为零；性能超过 `200ms` 为阻断，低于门槛的余留按一次调查 / 优化 / 复验记录收口。
  - **分步交付与首败**：首轮大图最大 `241ms`；去掉设施冗余标签刷新后仍 `210ms`，按仓库规则冻结。用户明确要求先提交推送后继续检查与尝试修复，因此先将 `9c62b25 / 0.5.96` 推送并快进主线，再继续同一编号。
  - **性能修复**：诊断确认首都 / 港口全覆盖层标签刷新 `143～189ms`；改为只重建城市与迁都所属国家标签，保留其余标签、资源和军事节点，城市排名、手工布局、图标轮廓与资源归属保持。诊断末尾 `301ms` 来自窄视口 resize，同步道路重建中巨大数组展开再转换造成重复复制；改为一次分配并按偏移写入三组顶点，保持 drawRanges。`301ms` 为带 profile 取证，未替代原 `210ms` 基线。
  - **正确性证据**：属性专项 `8` 组往返、缺字段、单边旧港口、故障回滚与保存回读通过；六组真实 DOM 投影等价覆盖迁都 / 回退 / 重做、港口开关、手工位置 / 优先级、隐藏、城市标签数量上限和跨海国家锚点。新旧道路 `1440 / 690px` 下 `157212 / 156672` 个浮点值、分组与统计逐值相同。类型、刷新路径、首都选项、对象详情、交互 inventory `108 / 88 / 20` 与 `1411 modules` build 通过。
  - **最终真实浏览器**：无 profile Chrome，3k 实际 `3015` 格，LongTask `1` 次、最大 `111ms`；100k 实际 `99846` 格，`15` 次、最大 `92ms`，相对 `210ms` 降低 `56.2%`；两档 `>200ms = 0`，应用 / health / WebGL 错误与 console warning 均为 `0`。六图标、单历史、撤销 / 重做、键盘、无效港口及 resize 均保留；`690px` 按钮组 `574 / 574px`，六按钮均 `32×32px`。
  - **独立验收**：初次复核修复单边港口遗漏，性能轮代码与投影复核 ACCEPT，最后按报告、日志和窄屏截图独立确认可归档。两条原有回归失配经未改 `63f176c` 基线复现，单列根 `FOLLOWUPS.md`，不扩修。
  - **交接与版本**：完成版本 `0.5.97`。精简证据见 [`任务记录`](../task-notes/task-382-city-attribute-shortcuts.md)；完整产物在 `Z:/tmp/codex/2026-09-16/task-382-city-shortcuts/`。

- **权威任务第 383 项：城市属性的后续生成作用。** `已完成（2026-09-16）；版本 0.5.98`

  - **需求与方案**：六属性仅在显式重算时影响人口潜力、经济、道路与军事；实施前冻结 [`完整方案`](../task-notes/task-383-city-generation-effects.md)，未改变全量城镇从空重生或锁优先级。
  - **实现**：共享属性系数，确定性潜在人口和显式重算；生产、交易、城市国库及国家税收消费经济系数，市场分配不覆盖贸易属性；有界主干枢纽候选；城墙 / 要塞提高陆军权重，保留国家兵额上限。
  - **验收**：专项、类型、军事锁、构建通过；正式 3k / 100k 的属性延迟作用、人口无复利、撤销重做及经济 / 道路 / 军事入口通过，最长任务 `96 / 155ms`，错误、health、WebGL 为 `0`，窄布局无溢出。经济与城市路线两个旧锁回归在未改基线同败，单列 FOLLOWUPS。
  - **独立验收**：一个智能体逐项对照 F1～F8；首轮发现国家税收漏消费设施增益，窄修城市交易单位后，城墙 / 神庙国家国库分别 `529.768 → 529.857`，重复重算逐字保持，最终 ACCEPT。
  - **交接**：产物 `Z:/tmp/codex/2026-09-16/task-383-city-development/`；下一编号按当前计划执行。

- **权威任务第 384 项：城市人口直接编辑与所属统计即时同步。** `已完成（2026-09-16）；版本 0.5.99`

  - **需求与方案**：先冻结 [`完整功能与实施方案`](../task-notes/task-384-city-population-editing.md)，从第 383 项完成提交 `73de127` 建立独立分支；实现后由一个智能体逐 F1～F8 验收。
  - **实现**：详情按整数“人”输入，支持 0～10 亿及显示倍率双向换算；校验、忙碌禁用、反馈和同值 no-op。城市 / burg、所属省份 / 国家 / 文化 / 宗教 urban 及独立镜像即时同步；乡村、道路、经济、军队不随之重算。
  - **事务**：九位内部精度，单次聚合，字段存在性及视觉 before / after 精确恢复；失败原子回滚、撤销重做、旧档保存回读和原 API 内部单位兼容。显式人口潜力重算使用同一统计路径，副本写入覆盖文化 / 宗教。
  - **验收**：16 组人数 / 倍率事务、独立镜像、非法和缺失对象、中立及旧字段、回滚、保存回读通过；类型、构建、人口调整、面板刷新、动作坞和 inventory 通过。真实 3k / 100k 四倍率与四面板、键盘、切换、忙碌、窄布局均通过，LongTask `30 / 48` 条、最大 `117 / 171ms`，错误、health、WebGL 均为 `0`。
  - **首败边界**：首轮浏览器误把互斥主面板的隐藏 DOM 当成打开面板；一次校正为逐面板打开及保持打开时正式 API 编辑，唯一目标复验通过。城市规模旧专项第 84 行四档分母假设在未改 `73de127` 基线同败，只更新 FOLLOWUPS 现状，不扩修。
  - **独立验收与交接**：`task384_plan_acceptance` 逐 F1～F8 ACCEPT；证据 `Z:/tmp/codex/2026-09-16/task-384-city-population/`。两项均完成，当前无活动任务。

- **权威任务第 385 项：所属城镇发展参与国力计算。** `已完成（2026-09-17）；版本 0.5.100`

  - **需求与方案**：先冻结 [`完整功能与实施方案`](../task-notes/task-385-city-national-power.md)，从 `9831659 / 0.5.99` 创建独立分支。所属城镇的实际数值参与正式国家国力与省份实力。
  - **实现**：基础城市数量加人口 / 产出 / 财富的递减有界项、六设施与已有道路 / 海路连接，按实际归属汇总；同类路线和城市身份去重。沿用 settlementPower / powerScore，保留其它评分项、显式重算时生效、旧数据、锁、镜像、历史和保存回读；两面板展示城镇贡献。
  - **验收**：专项、类型、正式经济计算、旧数据和最终构建通过。真实 3k / 100k 增加要塞并显式重算后国力 `60.81 → 60.96`、`113.09 → 113.13`，经济力保持；单历史、撤销 / 重做、重复重算与详情显示通过。
  - **性能闭环**：小图一次 `67ms` LongTask 经唯一诊断定位国家面板非虚拟表格无用尺寸读取；三行实质窄修及选中居中专项后唯一 3k 复验长任务 `0`，100k 原报告 `0`；两档目标 warning / error、health 与 WebGL 错误均为 `0`。
  - **独立验收与交接**：`task385_power_acceptance` 逐 F1～F6 最终 ACCEPT，无阻断。产物 `Z:/tmp/codex/2026-09-17/task-385-city-power/`，当前无活动任务。

- **权威任务第 386 项：四项已知回归失配归因与测试语义校准** `已完成（2026-09-19）；版本 0.5.115`

  - **来源与目标**：原 C-01；分别核对城市规模、路线样式、经济双域全锁、锁定首都道路四项既有首败，恢复规则与测试的一致解释。
  - **静态阶段记录（后续统一验收已闭合）**：四项实际专项均已通过；经济 7 组目标复验闭合，保留旧锁原像、显式归属编辑撤销及异常原子回滚。道路区分未锁首都连通与锁原像保持。见 [记录](../task-notes/task-386-regression-calibration.md) 和本批验收记录。
  - **范围**：核对固定输入、实际写集及网络连通；只修订证实过时的断言，保留有效行为反例。原始基线与日志位置已转入评估第 9 节。
  - **最小验收**：四项各有规则—行为—测试归因；城市阈值与生成分布分开验证；拾取用行为而非源码字符串验证；经济区分锁目标不变与全事务 no-op；道路区分干道直达、支路接入和不可达。未到达用例不计通过。
  - **非目标与停止点**：不改人口公式、规模阈值和锁后置合并；真实产品缺陷保留可复现证据并提出窄修范围，需改产品规则时冻结该子项，不通过改种子或删断言刷绿。
  - **首个廉价门**：复核四项现有脚本的对应最小行为；修订后只运行相关专项，再按改动面补必要检查。

  - **完成验收**：四项实际专项闭合；经济 7 组、稀疏编号、旧锁原像、归属编辑撤销与异常回滚通过。
  - **交付证据**：采用用户指定三级流程，逐项静态提交后统一浏览器验收；[批次记录](../task-notes/batch-386-397-acceptance.md) 保存具体产物、历史失败与目标复验。

- **权威任务第 387 项：当前存档格式及恢复说明校准** `已完成（2026-09-19）；版本 0.5.115`

  - **来源与目标**：原 C-02；让用户手册、当前 AI 资料与现行存档读写入口一致。
  - **范围**：区分默认 v3 二进制容器、逻辑 schema v2、可读旧格式、JSON 交换与浏览器恢复；明确完整文件不持久化本次撤销栈。历史验收保留历史语义。
  - **最小验收**：说明与编码常量、读写入口逐条对应；变更链接有效；既有 Wiki 审计通过。
  - **非目标**：仅文档，不改格式字节、扩展名、迁移和保存实现，不全库替换版本号或 gzip 字样。
  - **首个廉价门**：对照当前编码入口与版本常量；不依赖第 386 项全部回归通过。

  - **完成验收**：现行 v3 容器、schema v2 与旧档说明完成；Wiki 20 页 / 10 资源 / 18 命名空间审计通过。
  - **交付证据**：采用用户指定三级流程，逐项静态提交后统一浏览器验收；[批次记录](../task-notes/batch-386-397-acceptance.md) 保存具体产物、历史失败与目标复验。

- **权威任务第 388 项：准确的保存状态与离开提醒** `已完成（2026-09-19）；版本 0.5.115`

  - **来源与目标**：原 C-03；用户能知道当前内容是否保存、保存到哪里，以及离开是否有未保存内容。
  - **静态阶段记录（后续统一验收已闭合）**：独立保存内容位置与实际随档配置签名；下载、浏览器、云端成功回执及导入来源接线，撤销 / 重做、失败快照恢复和跨图迟到回执隔离；界面状态与离开提示已接入。语法、类型和 1415 模块构建通过，行为专项与浏览器待批末验收。
  - **范围**：建立基于实际持久化内容的保存基线，覆盖地图事实、随档样式与单位；统一 UI、API、快捷键。视口与面板状态等非持久内容不造成脏标记。
  - **保存语义**：浏览器事务成功、云端上传成功分别记录凭据；下载只能确认交给浏览器，不能声称已写入磁盘。PNG、GeoJSON 与仅返回内存数据不算完整地图保存。新图与导入来源分别标注。
  - **最小验收**：保存失败、保存中继续编辑、跨图迟到结果、撤销回到已保存内容、不同保存目的地、持久样式修改及离开提醒均正确；旧图兼容，真实浏览器入口和代表性 100k 性能通过。
  - **非目标与首门**：不做自动保存和云端冲突同步，不在每次操作同步序列化全图；先验保存内容身份、异步回执归属及撤销状态专项。

  - **完成验收**：保存位置、失败和迟到回执专项与浏览器 / 模拟云端入口通过；持久样式修改和离开提醒、撤销回已保存位置、内存导出边界正确。
  - **交付证据**：采用用户指定三级流程，逐项静态提交后统一浏览器验收；[批次记录](../task-notes/batch-386-397-acceptance.md) 保存具体产物、历史失败与目标复验。

- **权威任务第 389 项：独立自动恢复点与可选择恢复** `已完成（2026-09-19）；版本 0.5.115`

  - **来源与依赖**：原 C-04，依赖第 388 项；提供用户可开启的本地意外恢复能力。
  - **静态阶段记录（后续统一验收已闭合）**：独立恢复存储与显式选择 UI；默认关闭，30 秒检查内容位置，每份文档的独立写入副本最多 3 点、全局实际压缩负载 64 MiB 预算；新增写入与旧点淘汰同事务提交，失败回滚。跨地图导出不写入新图、恢复前复核身份；1418 模块构建与语法通过，额度 / 失败 / 多标签及真实恢复统一验收。
  - **范围**：恢复点独立于原手动存档槽；每个文档写入分支最多保留 3 个成功点，并设全局实测字节预算。先原子写入成功再清理旧点；同文档多标签或副本不得互相覆盖写入分支。
  - **最小验收**：开关、重开与用户选择恢复、额度不足、写入失败、损坏记录、多个文档 / 标签页、整图切换期间迟到结果均受控；失败保留原手动存档和可用恢复点；小图、100k、旧格式恢复与性能通过。
  - **非目标**：不默认覆盖当前地图、不同步云端、不保存历史闭包，不以恢复成功冒充手动保存成功。
  - **首个廉价门**：隔离存储的原子写入、保留策略及失败注入；冻结预算后再接入正式界面。

  - **完成验收**：两个同文档标签各保留三点；重开开关、所选恢复、配额失败和损坏保护、手动槽不覆盖通过；100k 旧 JSON 回读后的恢复点实际为 7,652,779 字节。
  - **交付证据**：采用用户指定三级流程，逐项静态提交后统一浏览器验收；[批次记录](../task-notes/batch-386-397-acceptance.md) 保存具体产物、历史失败与目标复验。

- **权威任务第 390 项：城市修改后的关联更新提示与引导** `已完成（2026-09-19）；版本 0.5.115`

  - **来源与目标**：原 C-05；解释六属性和直接人口修改已影响哪些事实、哪些派生结果需显式更新。
  - **静态阶段记录（后续统一验收已闭合）**：六属性 / 人口命令携带变化原因，独立只读提示服务随历史回退；城市面板展示影响域并打开既有领域入口，人口只定位既有控件。相关重算成功按领域清除，锁保护时保守保留，旧图未追踪不声称最新；语法与 1421 模块构建通过，专项及 UI 留待统一验收。
  - **范围**：追踪修改原因、受影响领域及既有操作入口；区分即时统计同步、人口潜力重算，以及从空重建道路 / 军事。沿用第 383～385 项已接受的显式重算规则。
  - **最小验收**：六属性、人口、撤销 / 重做、相关重算成功 / 失败、旧图缺少追踪信息均正确；只有真正更新的领域清除对应提示；旧图未知不标为已最新；真实 UI 引导不误触其它更新。
  - **非目标**：不自动覆盖手工人口，不自动连锁重生成，不增加一键全更新和生产 / 财政模拟。
  - **首个廉价门**：原因—领域映射、失效与清除规则；与第 388 项的未保存状态分开建模。

  - **完成验收**：六属性和人口真实入口、撤销重做、经济取消不清提示及成功只清经济、迁都新旧城市提示通过；旧图无追踪明确未知。
  - **交付证据**：采用用户指定三级流程，逐项静态提交后统一浏览器验收；[批次记录](../task-notes/batch-386-397-acceptance.md) 保存具体产物、历史失败与目标复验。

- **权威任务第 391 项：地图内容统一搜索、查看与定位** `已完成（2026-09-19）；版本 0.5.115`

  - **来源与目标**：原 C-06，按用户 2026-09-19 的“地图内容搜索”要求扩大范围；提供常驻可发现的搜索入口，统一查看和定位当前地图内容，不另建与原查找功能重复的入口。
  - **静态阶段记录（后续统一验收已闭合）**：统一入口、20 类对象及全部备注的分片文字索引、全量排序分页、只读详情及定位已接线；名称 / 全名 / 端点 / 正文、中文输入法和键盘支持，地图身份与修订隔离迟到结果。语法和构建静态检查通过，功能与大图浏览器留待批末验收。
  - **范围**：国家、省份、城市、文化、宗教、河流、湖泊、地貌、道路、洋流、地区 / 区域、市场、贸易、军团、外交关系、标记 / 资源点、标签、测量与备注；逐类对照现有对象类型和解析能力登记，不仅覆盖前三类。搜索名称 / 全名、标签文字、备注标题及正文、可见说明；无自有名称的对象使用已有端点或参与方显示名，不搜索内部运行字段。
  - **交互**：关键词加类型筛选，名称精确、前缀、包含依次优先于正文命中；结果显示类型、所属或端点及命中摘要，支持键盘选择和中文输入法。选中可查看，有合法位置再定位 / 高亮；孤儿备注等无位置结果可阅读并明确不可定位，不伪造坐标。搜索隐藏图层内容不永久改动用户图层偏好。
  - **最小验收**：每类有可复现命中与空结果；同名、有效 ID 0、稀疏 / 删除对象、独立及绑定备注、孤儿备注、长正文纯文本显示、IME、Esc、迟到结果和整图替换均正确；编辑 / 撤销 / 重做后索引更新。全局排序与分页一致，不能把现有每页 50 / 上限 200 当全图搜索上限；100k 下输入响应、真实窄界面和只读历史不变通过。
  - **非目标与首门**：不联网、不搜索其它文件 / 地图、不做 AI 问答、替换或批量编辑；复用已有对象查询和稳定引用，补齐文字索引而不每键复制全图或构造全量重几何。先验类型 / 字段覆盖、排序、身份及索引失效专项。详细设计见 [追加需求说明](../task-notes/map-search-and-panel-refinement-2026-09-19.md)。

  - **完成验收**：20 类搜索覆盖由大图 18 类非空与测量 / 备注补充合并完成；全量排序分页、ID 0、纯文本长备注、IME、Esc、隐藏层定位、删除撤销、整图替换失效通过。99,846 格 / 34,316 条操作窗口最大 LongTask 60ms，无 >200ms 和目标错误。
  - **交付证据**：采用用户指定三级流程，逐项静态提交后统一浏览器验收；[批次记录](../task-notes/batch-386-397-acceptance.md) 保存具体产物、历史失败与目标复验。

- **权威任务第 392 项：交易的现有路网可达性与路径诊断** `已完成（2026-09-19）；版本 0.5.115`

  - **来源与目标**：原 C-07；用户可以只读检查交易两端是否通过当前道路、支路及海路连接。
  - **范围**：按需从实际道路格和合法连接建立路径，正确映射市场中心；港口换乘校验地理关系，视觉交叉不当成连接。区分无路径、数据不足与损坏，不笼统判为不可达。
  - **最小验收**：连通 / 断路、陆海换乘、视觉交叉、重复共享边、旧档异常与路径长度通过；道路、港口、市场或整图变化使对应缓存失效；查询不改变内容、revision 或历史；100k 按需查询与真实 UI 通过。
  - **非目标**：不改交易、运费、税收或锁规则，不做全市场两两最短路，不将无路交易升级为全图硬错误。
  - **首个廉价门**：小型确定性网络的连通、换乘与长度验证。

  - **完成验收**：确定性陆海网络、共享边、损坏与长度专项通过；100k 跨端点查询经过 4 路、273.1 地图单位，历史不变、长任务 0。
  - **交付证据**：采用用户指定三级流程，逐项静态提交后统一浏览器验收；[批次记录](../task-notes/batch-386-397-acceptance.md) 保存具体产物、历史失败与目标复验。

- **权威任务第 393 项：孤儿备注重新绑定与正文抢救** `已完成（2026-09-19）；版本 0.5.115`

  - **来源与目标**：原 C-08；让失去对象的备注正文可编辑保存，并可显式重新绑定。
  - **范围**：首期目标限国家、省份、城市；以类型与稳定 ID 区分目标；目标已有备注时明确拒绝，原键删除和新键写入在同一事务完成。
  - **最小验收**：孤儿正文保存、同名目标、已有备注冲突、预检后目标删除 / 整图替换拒绝、精确撤销重做、旧备注导入和完整保存回读通过；无关备注不变。
  - **非目标**：不扩富文本、图片、AI 或人物图谱，不声称解决所有旧对象 ID 重用；可复用第 391 项选择能力，但不是其硬依赖。
  - **首个廉价门**：备注身份迁移、冲突与事务回滚专项。

  - **完成验收**：孤儿正文、绑定、冲突与失效目标、精确撤销重做、旧备注导入和完整存档回读通过。
  - **交付证据**：采用用户指定三级流程，逐项静态提交后统一浏览器验收；[批次记录](../task-notes/batch-386-397-acceptance.md) 保存具体产物、历史失败与目标复验。

- **权威任务第 394 项：PNG 导出预设与明确像素尺寸** `已完成（2026-09-19）；版本 0.5.115`

  - **来源与目标**：原 C-09；保存和复用 PNG 输出配置，明确输出像素尺寸。
  - **范围**：预设包含裁剪、标注类别、透明选项与目标尺寸；首期锁定宽高比。全幅 / 固定世界范围输出尺寸不随窗口改变，视口模式仍遵从当前可见范围；世界范围预设校验适用地图。
  - **最小验收**：预设保存重开、非法值、跨图范围、两种窗口宽度、实际 PNG 尺寸与标注、超限预拒绝及失败后相机 / 图层恢复通过；在目标分辨率绘制，不将低分辨率截图拉伸充作高质量输出；旧 1～4 倍 API 保持。
  - **非目标**：不含 PDF、地图册、多页排版、DPI 印刷承诺或超大分块拼图。
  - **首个廉价门**：配置归一、尺寸 / 预算与旧参数兼容；资源上限由现有 GPU / canvas 约束及实测冻结。

  - **完成验收**：两窗口实际 1200×800、旧倍率、跨图拒绝、六种裁剪标注、预设整页刷新及绘制失败恢复通过；地图 / 历史 / 视口 / 图层 / 主题保持。
  - **交付证据**：采用用户指定三级流程，逐项静态提交后统一浏览器验收；[批次记录](../task-notes/batch-386-397-acceptance.md) 保存具体产物、历史失败与目标复验。

- **权威任务第 395 项：移除和平条款的赔款支持** `已完成（2026-09-19）；版本 0.5.115`

  - **用户要求与现状**：用户明确“不设赔款逻辑，如果目前有，需要移除”。现有 API 接受结构化 `terms.reparations` 并写入外交纪事，标记为 `economicSettlement: record-only`；未实际划转财富。
  - **范围**：删除新增赔款的 schema、归一化与记录支持，清理相关配方示例、测试及现行能力矩阵表述；保留普通和平关系、自由文本备注、撤销 / 重做。移除专用于赔款的结算标记，不新建财政余额、支付或结算账本。
  - **输入与兼容**：新请求携带已删除赔款字段时明确拒绝，且拒绝前不修改地图或历史，不静默忽略后宣称成功。旧存档外交纪事中的历史文字原样保留并视为普通文本，不执行、不补扣、不清洗用户历史。
  - **最小验收**：UI / API 现行入口与发现文档不再提供结构化赔款能力；不支持输入原子拒绝；普通和平、文本备注、精确撤销重做与旧档保存回读通过；当前 schema、配方示例及语义矩阵一致。历史开发日志保留历史描述并明确已被本项取代的专题语义。
  - **首个廉价门**：和平输入、schema 与外交命令专项；再复验相关规划配方、矩阵及正式和平入口。不扩删其它既有军事或外交功能。

  - **完成验收**：新赔款输入原子拒绝，普通和平、文本与撤销重做通过；100k 旧 JSON 再保存回读保留外交历史原文。相关 schema 和 10 配方 / 43 步 / 86 引用同步；阶段外旧矩阵 / 殖民道路问题独立留在 FOLLOWUPS。
  - **交付证据**：采用用户指定三级流程，逐项静态提交后统一浏览器验收；[批次记录](../task-notes/batch-386-397-acceptance.md) 保存具体产物、历史失败与目标复验。

- **权威任务第 396 项：编辑面板操作精简、布局校准与明亮主题** `已完成（2026-09-19）；版本 0.5.115`

  - **用户要求**：逐项审查文字按钮图标化、同类操作按钮组、控件尺寸与横纵间距、冗余操作，以及编辑面板明亮主题；五方面共同验收，不以换背景色代替完成。
  - **静态阶段记录（后续统一验收已闭合）**：28 面板共用语义浅色主题、字段 / 弹层状态与尺寸间距；导入聚合、动作分组、危险文字保持、定位图标化和重复历史入口收敛。旧新动作对应见 [实施清单](../task-notes/task-396-panel-refinement.md)；静态构建通过，真实视觉与操作待批末验收。
  - **范围**：盘点全部正式编辑 / 管理面板、共用表格与字段、工具条、二级操作浮层及相关选择 / 确认弹层。控制面板和对象详情纳入相关入口与通用外观；云存储仅检查共享壳与弹层一致性。冻结清单、逐面板记录保留 / 合并 / 图标化 / 布局修正及理由。具体入口与阶段见 [追加需求说明](../task-notes/map-search-and-panel-refinement-2026-09-19.md)。
  - **动作精简**：定位、锁定、显隐等易识别动作优先图标化，具备中文提示、可访问名称和选中 / 禁用 / 忙碌反馈；有歧义或破坏性的动作保留必要文字及原确认。按同一对象与作用范围组织按钮组，互斥模式与独立动作区别呈现；重复入口可收敛，不同对象 / 不同写集不因名称相似而合并。保留每项能力的可达路径，不把全部高频操作藏入多层菜单。
  - **尺寸与间距**：统一控件高度、图标命中区、行内与组间间距；长中文、输入值、单位、错误、空态和忙碌文案均能容纳或完整查看。分组内紧凑、组间清楚，允许合理换行 / 滚动；不得靠压缩字号、裁剪控件、固定窄宽或撑出视口取得整齐。
  - **明亮主题**：默认浅色面板和字段、深色文字、明确边框 / 层次；同步表格、选中、悬停、焦点、禁用、警告和错误。统一主题变量并清理相关硬编码暗底，覆盖挂到面板外的菜单、下拉、颜色选择和对话框。界面明亮主题独立于地图配色、背景及导出，不修改地图视觉事实。
  - **最小验收**：逐面板动作映射无意外丢失，原回调、作用范围、锁、单历史、取消、失败及撤销重做保持；桌面、690px 窄视口及 200% 浏览器缩放下检查长中文和各状态，不遮挡关键动作，不意外横溢。真实浏览器验证所有面板外观与代表性编辑链、弹层主题、键盘焦点及地图手势隔离；小图 / 100k、交互审计和性能门通过，地图主题与导出不变。
  - **非目标与首门**：不换组件库、不重写编辑业务、不新增世界模拟、不做地图主题或暗亮双主题切换器。主线程唯一写者；先建立面板 / 动作清单，再以共用控件与城市 / 道路代表面板验证规范，随后分组推广及最终全清单验收；首个廉价门为动作、可访问名称与样式作用域静态核对。

  - **完成验收**：28 面板桌面 / 690px / Chrome 实际 200% 完成；字段横溢、锁表头及拖柄修正，六属性 / 人口 / 导入菜单 / 取消 / 键盘代表链通过；明亮主题独立于地图配色。
  - **交付证据**：采用用户指定三级流程，逐项静态提交后统一浏览器验收；[批次记录](../task-notes/batch-386-397-acceptance.md) 保存具体产物、历史失败与目标复验。

- **权威任务第 397 项：批末大文件拆分机会复核与必要优化** `已完成（2026-09-19）；版本 0.5.115`

  - **来源与范围**：用户要求最后检查大文件拆分是否仍有优化点。仅检查本批涉及的大文件与共用 UI / 服务边界；有明确独立职责、降低重复或缩小变更面收益时才拆分，不按行数机械切割。
  - **静态阶段记录（后续统一验收已闭合）**：抽离 PNG 临时绘制与恢复边界，清除重复主题颜色变量；进一步样式 / 应用切分的成本与不拆理由见 [复核记录](../task-notes/task-397-file-boundaries.md)。
  - **验收**：写出选定 / 不拆理由；实际抽离保持导出接口、调用顺序、状态所有权和副作用，静态检查后提交，并纳入统一功能 / 浏览器复验。无实质收益可以仅交付复核结论，不凭空制造重构。
  - **非目标**：不全库迁移、不更换框架、不改生成算法和数据格式，不吸收未批准候选。

  - **完成验收**：抽离 PNG 临时绘制、预算与恢复边界，清除重复主题颜色；应用生命周期和大样式不做无收益机械拆分。功能、失败恢复与统一浏览器结果保持。
  - **交付证据**：采用用户指定三级流程，逐项静态提交后统一浏览器验收；[批次记录](../task-notes/batch-386-397-acceptance.md) 保存具体产物、历史失败与目标复验。

- **权威任务第 398 项：编辑面板羊皮纸质感** `已完成（2026-09-19）；版本 0.5.116`

  - **用户要求**：编辑面板背景过白，改为羊皮纸质感。
  - **范围**：主线程唯一写者，共用主题与轻量 SVG 静态纹理；暖米色纸底、墨色文字、柔和边框，输入、表格、搜索和菜单共享色调，选中保持青蓝提示。无动画、无新增开关、不修改布局或编辑逻辑，不吸收 FOLLOWUPS。
  - **最小验收与结果**：构建通过；真实 Chrome 1440px / 690px 的城市、高度、备注面板共 6 组无横溢，菜单与搜索纸纹一致，搜索输入和 Esc 关闭通过；浏览器错误为 0。旧新界面主题下地图 PNG 数据完全相同。
  - **交付**：独立分支 `codex/task-398-parchment-panels`，版本提交后快进主线并推送；[验收记录](../task-notes/task-398-parchment-panels.md) 保存实际范围和证据。
