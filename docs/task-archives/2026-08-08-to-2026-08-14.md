# 2026-08-08 至 2026-08-14 权威任务归档

本卷保存 本时间片内完成的权威任务 的历史权威任务正文。当前可执行任务仍以 [当前开发计划](../current-plan.md) 为唯一权威来源。

- 归档条目数：31
- 排序方式：按权威任务编号升序
- 状态边界：只收录已完成、已移除或已由后续方案取代的条目；未完成与暂缓项不得进入归档。

## 任务条目

- **权威任务第 301 项：100k 高度笔刷停手后完整主线程阻塞与最终绘制修复。** `已完成；来源：用户直接反馈`
  - 范围：补齐从 pointerdown、连续 pointermove、待执行 RAF、pointerup 末点补刷、历史提交、最终视觉帧到浏览器长任务的完整证据，并完成岸线同侧局部刷新、跨水陆拓扑局部刷新、首次高度索引预热和高度编辑作用范围恢复。
  - 301-B：岸线同水陆侧变化按 cell → GPU buffer span 只刷新受影响颜色；只有水陆侧实际变化才进入拓扑路径。
  - 301-C：跨水陆变化按邻接闭包局部重建 cell mesh、当前 source edge 路径和 surface patch；安全边界不足时回退完整重建，不使用陈旧几何。
  - 301-D：进入 `height:brush` 时预热高度空间索引和 Grid → Pack 映射，避免首次笔刷把全图扫描伪装成停手卡顿。
  - 301-E：真实进入高度模式时恢复 `heightEdit.terrainSelection` 的 cell / 羽化权重到 renderer 选区缓冲并调度 brush cursor；动作 / 作用范围按钮补齐 `.active` / `aria-pressed`；普通分段按钮和 Element Plus 图标按钮补齐选中且禁用样式；全局画笔大小用 `webgl-generator-height-editor-preferences-v1` 记忆，首次无偏好仍为 `28`。
  - 最小验收：隔离生产 Chrome 10k / 100k 真实点击动作、作用范围和覆盖锁定，实际选区为 `3327 / 39862` cells；清空 renderer 缓冲后停止并重新启用高度编辑，两档恢复原高亮数量。画笔 `42` 在面板重开、模式重入和页面刷新后保持，光标半径为 `42`；真实高度笔刷、岸线、跨水陆局部拓扑、撤销 / 重做、checksum、console、page、health、WebGL 和 `source/` 门禁通过。
  - 兼容性与回滚：不改变地图数据、height / grid schema、存档、公开 API、历史事务或派生 stale 语义；偏好不进入地图 JSON、gzip 存档或历史。可单独回退 301-B / C / D / E 对应局部刷新、预热、视觉恢复和偏好代码。

- **权威任务第 302 项：城镇手动移位完整闭环。** `已完成；来源：用户要求继续第301项及之后任务`
  - 范围：复核并验收既有 `city:move`、目标预检、真实拖动预览、取消 / 提交、行政 / 港口 / 路线镜像同步和单事务回滚；不新增必填 schema 字段，不修改 `source/`。
  - 完成记录：`inspectCityMove` 覆盖普通城镇、港城、首都、省会、市场中心、陆地 / 占位 / 边界、陆路局部重寻和失效海路明示删除；`createMoveCityCommand` 使用有界快照和 `EditHistory` 完成原子写入、撤销 / 重做。统一 Escape 入口的静态回归契约已同步到当前活动模式管理器。
  - 验收记录：新增隔离系统 Chrome 回归，10k 实际 `10004 / 5968`，100k 实际 `99846 / 63405`；两档真实指针均完成面板进入、拖动预览、Escape 取消、重新进入、提交、selection / picking、Ctrl+Z / Ctrl+Y。取消不改变位置、占位、checksum、revision 和历史，确认只增加一条历史；10k 港城预览明确显示港口失效及两条海路删除警告。核心城市事务、API JSON / gzip 往返、完整地图导出、PNG 选项、旧数据兼容、生产构建与 `git diff --check` 通过；普通 application console / page error 和 WebGL error 为 `0`，`source/` 未改。
  - 性能观察：100k 隔离操作保留约 `0.93s` 输入延迟证据，生成 / 重载阶段约 `2.45s`、`4.50s` 长任务；记录为后续性能专题，不在本项扩大改动。

- **权威任务第 303 项：河流多控制点自由编辑。** `已完成；来源：用户要求继续第301项及之后任务`
  - 范围：在旧单控制点河流编辑基础上增加可选 `river.controlPoints` 集合；支持同一 pack cell 多点、河流非控制点新增、已有点拖动、双击删除、完整河流 / 控制点高亮与稳定 picking；所有操作先预览，确认一次写入并支持撤销 / 重做；不改生成算法和 `source/`。
  - 完成记录：303-A 建立 `id / x / y / pointIndex / packCell / flux / schemaVersion` 规范化层，旧河流缺字段保持兼容；303-B 接入集合 session、pointer capture 和双击删除；303-C 扩展 selection mesh、控制点 picking 和预览折线；303-D 以 `createEditRiverControlPointsCommand` 把 `points / controlPoints / length` 作为单事务写入；303-E 修复控制点模式被普通 pointerup 选中链取消的问题，模式允许画布输入并抑制普通对象选中。
  - 验收记录：`regress:river-control-points-data` 与 `regress:river-control-points` 通过，同一 pack cell 双点身份独立、分别移动 / 删除、hydrology 保持、确认一条历史；隔离系统 Chrome 10k / 100k 真实指针均完成新增、移动、双击删除、退出取消、重新进入提交、Ctrl+Z / Ctrl+Y，控制点交互期间 health error、application console error、page error 和 WebGL error 为 `0`。旧单点河流 `1280 / 390 / 320px` 面板回归、selection / picking、完整导出套件、PNG / 高度图、API JSON / gzip 往返、旧数据兼容和生产构建通过。
  - 健康与兼容边界：生成阶段仍记录已知主线程长任务，10k 约 `1.05s`，100k 约 `2.20s` 与 `4.55s`；交互阶段无健康错误，该生成证据保留给后续性能专题，不在本项扩展优化。`river.cells / gridCells / parent / basin / source / mouth / flux / discharge`、旧存档和旧导出语义保持；新增控制点字段可选，不改变必填 schema，用户当前 Chrome 标签页未接管或刷新。

- **权威任务第 304 项：河流网络算法实验室与生成质量修复。** `已完成；来源：用户要求重新调查并修正验收`
  - 范围：在独立 `prototype/river-network-lab/` 中校准夹具覆盖、候选接受与真实 A/B 证据；修正汇流关系的局部容差、曲线桥接和安全拒绝门。实验室只读正式快照，不把候选直接写入正式生成器、地图、存档或用户标签页。
  - 完成记录：304-F 将夹具命中与候选验收分离，阶段失败不再静默回退，semantic checksum 排除遥测并保留河网 checksum；304-G 以共享水文 cell 作为水文凭据、显示折线最近投影作为几何端点，同时约束真实显示桥接距离与水文距离。只有真实几何变化才生成 cubic Hermite / Bézier，单关系失败保持局部 rejected / partial；过冲、自交、折返、水域、新非汇流交叉和保护河口漂移均有固定拒绝反例。
  - 验收记录：`regress:river-network-lab`、`regress:river-network-lab-browser`、生产构建和差异检查通过。10k / 50k / 100k 三阶段候选均接受；100k 同快照 A/B 保留旧算法 baseline `blocked`，新候选为 `accepted`，真实显示距离 `16.56837` 未超过局部容差 `17.02766`，曲率 `1.037495`。390px 页面无横向溢出，console / page / health error 为 `0`；实验室使用 SVG，GPU upload / WebGL 明确记为 N/A。结论仅批准实验室候选与证据门，不等于已接入正式 `rivers.js`。

- **权威任务第 305 项：河流自由控制点与共享三次曲线整改。** `已完成；来源：用户反馈第 303 项仍不可用`
  - 范围：删除旧 `packCell / session.stage` 画布输入支路，统一为 `stageAction / working.controlPoints`；允许精确世界坐标、同 cell 多点、水域点、拖动和双击删除；预览、正式渲染、picking、长度、GeoJSON 与存档共用三次曲线采样。
  - 完成记录：新增向心参数化 Catmull–Rom 转分段 cubic Bézier 的无状态几何与可视子段 provenance；点击新预览曲线能映射回正确原始河段。pointermove 由 RAF 合并，pointerup 使用最终坐标并取消待执行帧；新增、拖动、双击、取消、异常和 dispose 均可靠释放 capture 并恢复 working。控制点和曲线进入水陆都可见的 ordinary 层，无 `controlPoints` 的旧河流继续保持原 Chaikin、旧 gzip 和导出语义。
  - 验收记录：cubic、控制点数据 / 交互、picking、GeoJSON、旧新 gzip、生产构建和差异检查通过。隔离 Chrome 10k / 100k 的 60 / 120 次真实 move P95 与最大值均约 `0.1ms`，交互长任务为 `0`；同 cell 双点可独立拖动 / 删除，水域点可见、可再次命中并双击删除，preview 与 commit 几何摘要一致，交互 health / console / page error 为 `0`。用户现有 5410 的既有 100k 地图上又完成新增、拖弯、在新预览曲线上继续新增、双击删除和不应用退出，草稿未写入地图，WebGL error 为 `0`，健康列表未增加。

- **权威任务第 306 项：城镇自由迁移、行政重算与交互性能整改。** `已完成；来源：用户反馈城镇拖动卡死并确认新规则`
  - 范围：人工移动只以水域为产品硬拒绝，结构损坏另行拒绝；允许同 cell 多城；跨省 / 跨国后更新归属，并为原省 / 原国家重选省会 / 首都；任何指针、预检、路线或 renderer 异常都必须释放交互状态。
  - 完成记录：城镇拖动改为 RAF 合帧的 O(1) 快速预览、独立 WebGL 单实例 ghost 与 DOM 回退，pointerup 仅执行一次可取消异步完整预检并复用 immutable preflight。新增 city / burg 多值 cell 索引、稳定 singular 代表和 city→routes 邻接，覆盖加载、锁、查询、省会、picking、Grid 细分、软删与历史；完全重合城镇按稳定候选循环且均可再次拖动。路线重寻失败按事务删除并警告，港口失效清除，市场冲突保留各中心并用稳定代表 / stale 降级。迁入目标已有首府时不篡位，目标缺首府时确定性晋升；原国家无候选城镇时允许 `capital=0` 但保留合法本国陆地 center，省份可 `burg=0` 且保留合法 center，政治扩张、重生成锁、国家画笔和空国合并均已统一该不变量。
  - 验收记录：城镇迁移、国家生命周期 / 拓扑、锁、cell 查询、路线、Grid 细分、旧数据 / 浏览器存储兼容、生产构建和差异检查通过。独立终审连续两轮覆盖完整 10k + 100k：四档交互窗口 `>=50ms` 长任务均为 `0`；100k pointer handler P95 / max 为 `0.1 / 0.1ms`，ghost preview 为 `0.4 / 0.9ms`，异步 preflight `56.1ms`，commit `28.1ms`，迟到 health / console / page error 为 `0`。用户现有 5410 上把首都跨境拖入已有城镇所在 cell 后成功提交，随后撤销恢复原坐标；页面全程响应，编辑态清空，WebGL error 为 `0`，原有健康事件逐条不变。

- **权威任务第 307 项：城镇移动固定屏幕热区与密集候选最近响应。** `已完成；来源：用户反馈城镇移动热区难以点中并冻结缩放 / 冲突规则`
  - 范围：只调整正式 WebGL 城镇 picking 和移动 pointerdown 的命中语义；热区必须保持固定用户视角尺寸，密集冲突选择距鼠标最近城镇。不改变城镇图标、地图数据、生成均衡、行政迁移事务、公开 API、存档或 `source/`。
  - 完成记录：城镇热区固定为半径 `18 CSS px`；世界空间索引只负责保守召回，renderer 对每个候选投影后以 CSS 像素计算最终距离。不同位置候选按距离、稳定 ID 排序；普通同 cell 不再触发循环。同一落点只容忍轻量 cell 拾取既有的 `0.1 world` 释放量化误差，继续支持完全重合对象循环和移动模式优先已选对象。
  - 验收记录：Node 专项覆盖 `0.5 / 1 / 4 / 10x`、密集最近、等距、同 cell、精确 / 量化重合；城镇迁移、对象详情、生产构建与差异检查通过。隔离 Chrome 10k / 100k 在 `0.35 / 1 / 4 / 9x` 下均为 `17.5px` 命中、`18.5px` 不命中；24px 间距双城选择离鼠标最近者，同距按较小 ID。两档从 `17.5px` 边缘真实 pointerdown，连续 60 帧移动后取消，handler P95 / max 不超过 `0.1 / 0.1ms`，交互长任务、active health、console、page 与 WebGL error 均为 `0`。用户现有 5410 只读检查且保持原页，不把该可见性检查冒充数值门禁。

- **权威任务第 308 项：城市对象详情打开城市管理与两行动作布局。** `已完成；来源：用户要求城市详情增加管理入口并避免按钮拥挤`
  - 范围：只修改对象详情组件、包装层、运行时面板接线、城市专属动作布局与直接回归；复用既有城市管理和当前 selection。不改城市数据、编辑事务、公开 API、存档、生成算法、其它对象详情、用户 5410 或 `source/`。
  - 完成记录：城市对象详情新增“打开城市管理”，与“定位 / 名称库改名 / 编辑名称”组成两列两行；新入口仅在城市对象出现。点击后走既有选中感知打开链，把当前城市同步为城市管理选中项；非城市对象维持原按钮和自适应布局。
  - 验收记录：`regress:object-details-edit`、`regress:object-details-city-browser`、`regress:panel-overlay-policy`、生产构建与差异检查通过。隔离 Chrome `1280 / 390 / 320px` 均为两行各两个按钮，动作区宽 `294 / 294 / 278px`，横向溢出和文字裁切为 `0`；点击后城市管理保持原城市选中，非城市反例、health / console / page / WebGL error 均通过。既有 selection-actions 仍含第 306 / 307 前的文本断言，作为无关旧门禁单列且未扩修。

- **权威任务第 309 项：细分 100k 城镇拖动完整现场修复。** `已完成；来源：用户在原 5410 细分 100k 地图上再次确认无法拖动`
  - 范围：修复细分网格的城镇目标拾取、固定屏幕起拖反馈、单 DOM ghost 和城市管理局部详情刷新；保留第 306 / 307 项已冻结的人工迁移、行政重算、同 cell 多城和固定热区规则。不改公开 API、存档格式、生成算法、河流或 `source/`。
  - 完成记录：根因是正式 `pickGridCell` 仍按规则行列推导 ID，无法识别 10k→100k 细分追加的子 cell；此前直接生成 100k 的隔离用例不具备该结构。拾取器现从附近母 cell 展开 `grid.refinement.children`，以子多边形包含和有界最近生成点回退确定 cell；城镇水陆预检使用正式画面的 grid 高度。移动模式显示固定 `36×36 CSS px` 起拖环，热区半径保持 `18 CSS px`，密集冲突取屏幕最近城镇；ghost 不再创建第二 WebGL context，面板只局部刷新所选城市详情。
  - 验收记录：用户原 5410 页面与原细分 100k 地图通过真实 Windows 鼠标把城市 `#0` 从 `grid 3521 / pack 1660` 移到 `grid 41503 / pack 19870`，城市管理即时同步且页面可交互；随后通过同一应用历史撤销恢复原 grid / pack 和选择。细分回归验证代表子 cell 及全部迁移城市可拾取；城镇迁移、固定热区、面板刷新、对象详情、编辑执行、生产构建和差异检查通过。隔离 100k handler P95 / max 为 `0 / 0.1ms`，preview 为 `0.3 / 0.7ms`，交互长任务与交互期 console / health / page error 为 `0`；生成期既有长任务单独保留，不冒充交互结果。

- **权威任务第 310 项：城镇连续移动与显式退出。** `已完成；来源：用户要求一次提交后继续移动同一城镇`
  - 范围：只调整 `city:move` 成功提交后的交互复位、非当前城镇 pointerdown 的退出语义、城市面板提示和直接 Node / 浏览器回归。复用第 306～309 项迁移事务、固定热区、细分拾取、单 DOM ghost 与面板局部刷新；不改行政规则、API、存档、其它编辑模式、生成算法、河流或 `source/`。
  - 完成记录：成功提交后清理本次 preview / pending，但保留 `city:move` 与原 cityId，并把固定 `36×36 CSS px` 起拖环迁到新位置；同一城镇可直接继续拖动，每次提交独立写入历史。非当前城镇点击会退出模式并继续普通选择链；活动按钮明确显示“退出移动城市”。提交失败仍恢复起拖环供重试，手动退出与 Escape 清理保持。
  - 验收记录：Node 专项覆盖连续两次提交、两次 pointer capture 释放和 pointer miss 退出。隔离 Chrome 10k / 100k 均完成第一次提交后保持 active、同一城镇第二次提交、两条历史、面板与起拖环更新、手动退出、点击别处退出及撤销 / 重做；100k handler P95 / max `0 / 0.1ms`、preview `0.3 / 0.5ms`、preflight `45.9ms`、commit `16.1ms`，交互长任务和交互期错误为 `0`。固定热区、对象详情、编辑执行、面板刷新、Grid 细分、生产构建与差异检查通过；生成期既有长任务单列，用户 5410 未写入。

- **权威任务第 311 项：河流控制点拖动后持久预览与应用状态。** `已完成；来源：用户反馈拖动后变化曲线消失且应用按钮误禁用`
  - 范围：只修 `river-waypoint-session` 对 working / begin baseline 的累计 changed 判定，并补控制点 Node、交互和浏览器回归；持久预览复用既有共享三次曲线与控制点层。不改控制点几何、河网生成、正式河流、API、存档、其它面板或 `source/`。
  - 完成记录：pointermove 已经更新 working 后，pointerup 常再次 stage 相同末点，单次 inspection 合理返回 `changed=false`；旧 session 错把该动作局部值当成整个草稿 dirty，导致 RiverPanel 禁用 Apply、selection layer 停画变化曲线。现由 session 以完整 working 相对 begin baseline 重算累计 changed；拖动结束仍持续显示变化河道并允许直接应用，只有净变化回到 baseline 才恢复未改状态。
  - 验收记录：Node 精确覆盖最后 RAF 与 pointerup 同坐标、动作局部 no-op 但累计 dirty 保留，以及变化曲线 mesh 多于仅手柄。隔离 Chrome 10k / 100k 的 60 / 120 次真实 pointermove 后，draft / renderer preview 均保持 changed，Apply 立即启用并直接提交，预览 / 正式曲线摘要一致；两档交互长任务、application console、page 与 interaction health error 为 `0`。控制点、waypoint、生产构建和差异检查通过；100k 生成阶段既有长任务单列，用户 5410 与 `source/` 未改。独立河流实验室已按用户要求在 Chrome 的 `http://127.0.0.1:5403/` 新标签页打开。

- **权威任务第 312 项：河流实验室未接入支流可修复正例。** `已完成；来源：用户现场确认主夹具 candidate 仍为 rejected`
  - 范围：只修“支流未接入干流”固定主夹具与直接 Node / 浏览器门禁，使其代表旧固定阈值之外、局部安全容差之内的可修复正例；保留独立 500+ 超长桥拒绝反例。不放宽候选算法阈值，不改正式生成器、正式河流、API、存档、用户 5410 或 `source/`。
  - 完成记录：原主夹具 gap `52/12` 本身就是不可安全修复的长桥，浏览器回归还固化 confluence / hydrology rejected，导致“证据通过”只表示诊断命中。主夹具现为 gap `16`、局部容差 `20.595445`，仍精确命中 `tributary-unattached`，但生成曲率 `1.787951` 的三次汇流段并通过过冲、自交、折返、水域与新非汇流交叉门，C / D 均 accepted。`shared-cell-display-gap` 继续拒绝 500+ 显示长桥。
  - 验收记录：`regress:river-network-lab` 与 `regress:river-network-lab-browser` 通过；页面主夹具 candidate 几何实际变化、拒绝连接段为 `0`，10k / 50k / 100k 固定快照继续全 accepted，100k baseline 保持 blocked。用户当前 5403 页刷新后现场为 `evidenceOk：true / candidateDecision：accepted`，关系 `#2 → #1` 显示 `gap 16/20.595445`；正式生成器和 5410 地图未改。

- **权威任务第 313 项：河流实验室候选接入正式河网生成器并分批发布。** `已完成；来源：用户批准正式应用并要求分批提交、统一推送`
  - 范围：把实验室 DAG、汇流曲线、水文单调和安全门抽为正式共享模块，在 canonical 河网归一化后接入新图生成；只合并安全显示几何及对应流量 / 宽度派生。不得改 `cells / gridCells / parent / basin / source / mouth`、锁定河流、旧图加载语义、持久化 schema 或 `source/`。
  - 完成记录：正式 `generator/river-network-candidate.js` 成为唯一算法实现，实验室改为薄转发。正式 `buildRivers` 在 `normalizeRiverNetwork` 后执行候选并写入结构化 metadata；通过关系应用 sampled cubic Hermite / Bézier，拒绝关系和冻结对象保持原样。固定 100k 的已知断接关系实际贴合，三条曲线与 77 条水文派生应用后仍无拓扑、流量或宽度越级。
  - 验收记录：10k / 50k / 100k 正式 Node、正式 10k / 100k Chrome、河网 / 锁 / 存档 / GeoJSON / PNG / picking / 确定性、实验室 Node / Chrome、生产构建和差异检查通过；500+ 长桥与六类安全反例继续拒绝，旧存档不自动升级。正式 Chrome 的 application console、page、active health 与 WebGL error 为 `0`，生成长任务单列。发布前城镇严格门连续两轮完整 10k + 100k 交互长任务为 `0`。改动按正式河网、河流控制、城镇迁移、共享集成和文档边界分批中文提交，最后统一推送 `main`。

- **权威任务第 314 项：正式河网残留支流断接修复。** `已完成；来源：用户提供正式地图仍有细支流停在干流旁的截图`
  - 范围：只修正式新图和显式河流重生成中已经声明 parent 的汇流显示几何；不凭视觉距离为独立河流补造 parent，不修改 canonical `cells / gridCells / parent / basin / source / mouth`、冻结河流、旧图加载或 `source/`。
  - 完成记录：共享终端水文 cell 现在可使用既有 `24 world` 最大上限作为强恢复证据，但仍同时检查真实显示距离与全部曲线安全门；任何非零短 gap 都生成同源 cubic Hermite / Bézier 并精确落到父河。主曲线若触发水域门，只尝试确定性局部弯曲候选，仍须逐项通过过冲、自交、折返、水域、新非汇流交叉与保护河口门；500+ 长桥和无共享水文证据关系继续拒绝。
  - 验收记录：固定真实 residual 种子中 `50k #279 → #283` 和另一独立关系由 partial 修为 accepted；正式 Chrome 的 10k / 50k / 100k 分别检查全部 `26 / 70 / 156` 条父子汇流，最大缝隙为 `0 / 1.14e-13 / 1.14e-13`，50k 冻结关系精确为 `0`。PNG、Feature GeoJSON、picking、WebGL、控制点、锁定河流、旧存档、API、实验室 Node / Chrome、生产构建与差异检查通过；500+ 长桥和六类安全反例保持拒绝。

- **权威任务第 315 项：延迟全局操作提示。** `已完成；来源：用户要求等待一段时间仍未完成的操作显示文雅 loading`
  - 范围：为正式共享 runtime operation 增加统一延迟反馈；快速操作不闪烁，生成原有阶段提示保持独立，完成、错误、取消、重叠与迟到回调可靠清理。不改事务、历史、API 数据结果或 `source/`；同步长任务继续作为性能问题治理，不用 loading 掩盖卡顿。
  - 完成记录：新增 token 化 delayed-operation-feedback 协调器和独立 HUD 气泡。操作超过 `420ms` 才真正从 hidden 状态显示；默认文案为“山河正徐徐推演，请稍候片刻。”，已有“正在……”阶段文案自动补为文雅等待句。共享 operation 的阶段更新、取消、失败和成功统一驱动提示；generation loading 与操作提示可同时存在且不互相覆盖。
  - 验收记录：Node 覆盖快速、慢速、阶段更新、重叠和销毁；隔离 Chrome 中 `0.8ms` 快速操作动画启动次数为 `0`，`680ms` 异步操作在阈值后可见，取消 / 故障注入 / 共存均清理。`320px` 无横向溢出，active health、application console、page 与 WebGL error 为 `0`；operation、画布文字注册表、旧存档 / API、生产构建和差异检查通过。

- **权威任务第 316 项：独立实验室文档、README 与 AI 路由。** `已完成；来源：用户要求给当前各个实验室建立文档，包括 README 中的引用与 AI 文档，不需要 Wiki`
  - **范围**：为 `prototype/` 当前四个独立页面建立统一的长期说明和逐页 README 入口；同步根 README 中英文版、`docs/README.md`、AI 接手入口、部署说明及归档索引。说明覆盖用途、正式应用关系、入口、启动 / 回归命令、数据与写入边界、维护规则和 AI 选择路径。
  - **完成记录**：新增 `docs/architecture/laboratory-prototypes.md` 与 `docs/ai/laboratory-prototypes.md`，固定 `webgl-cells`、`boundary-topology-lab`、`loading-scroll-showcase`、`river-network-lab` 四项分母。补齐 WebGL cells 历史原型 README，并让其余三份实验室 README 回链总览与 AI 边界；根 README 中英文版、docs 索引、AI 接手入口和 Vercel 说明均已路由到统一说明。拓扑文档明确三个纯函数已被正式岸线链受控复用，河流文档明确页面只读、正式候选实现的唯一来源及 `formalGeneratorWrite: false` 的含义。
  - **验收记录**：11 份受影响 Markdown 的相对链接逐条解析通过；`regress:loading-scroll-showcase`、`regress:boundary-topology-lab`、`regress:river-network-lab`、`regress:deployment` 与归档检查通过，部署回归确认四个静态路径均进入当前产物。`audit:ai-docs` 仍报告既有 `docs/generated/ai` 的 `manifest.json / api-catalog.json / domain-capability-map.json / api-catalog.md` 与源码不同步；本项未生成无关机器目录，保留为仓库现有独立状态。未修改 `docs/wiki/`、产品代码、构建 / 路由配置、存档、API、用户 Chrome、`source/`，未暂存、提交或推送。

- **权威任务第 317 项：正式实验室路径与简介导引。** `已完成；来源：用户要求实验室正式环境路径写入文档，并在控制面板简介增加中文实验室下拉入口`
  - **范围**：为 `https://fmg.mosuzi.top/prototype/web-cells/` 建立 WebGL cells 实验室的兼容入口并保留 `/prototype/webgl-cells/`；控制面板“简介”聚合为一个“实验室”下拉，列出四项中文名称并在新标签页打开正式 URL；同步实验室总览、逐页 README、部署与 AI 文档。
  - **完成记录**：Vercel 精确改写在通用 prototype 与 SPA fallback 前将 `web-cells/` 页面和其相对 `src/`、`data/` 资源透明指向既有 `webgl-cells` 静态目录。简介页新增 WebGL 单元格、共享边界拓扑、画卷加载页概念和河流网络算法四项入口，均使用 `noopener,noreferrer` 新标签页；旧静态入口保持。部署文档与每个实验室 README 已固定 `fmg.mosuzi.top/prototype/` 下的正式路径。
  - **验收记录**：构建、部署回归、静态 alias 页面 / 相对模块 / 固定样本实测、交互面审计、Markdown 链接和差异检查通过。生产 Chrome 覆盖 `1280 / 390 / 320px`，菜单无横向溢出、四项中文名称完整显示、当前地图页不跳转、console / page / health / WebGL error 为 `0`。线上四页标题分别为 `FMG WebGL Cells Prototype`、`共享边界拓扑实验室`、`古代画卷加载页概念稿`、`河流网络算法实验室`；`web-cells/src/main.js` 与 `data/sample-map.json` 返回 `200`，样本为 `99846` cells。`audit:ai-docs` 仍仅报告既有四份机器目录陈旧，未扩大范围重生成；全量 UI 系统审计在无关的大型面板矩阵超时且已回收临时服务。未改 Wiki、`source/`、地图、存档、API 或生成流程。

- **权威任务第 318 项：边界与河流实验室自包含静态部署。** `已完成；来源：用户确认正式应用不受影响后，要求修复两项线上实验室无用例`
  - **范围**：把边界与河流实验室的依赖图构建为各自的自包含静态产物，禁止生产页面请求未部署的正式源码模块；补齐部署回归、真实静态浏览器回归和实验室说明，不改正式应用运行时、地图、存档、公开 API、生成流程或实验室算法。
  - **完成记录**：线上无用例的根因是实验室模块请求被 SPA fallback 以 HTML 返回，触发模块 MIME 错误并在初始化前中止。`deploy-prototypes` 现为两页执行独立 Vite 子构建，输出各自的 `assets/`；边界页保留既有只读几何 vendor。新增静态浏览器门禁，实验室面向读者的说明统一收敛为一项在线预览链接，不再展开部署路由或历史入口。
  - **验收记录**：本地和生产浏览器门禁均确认边界页固定夹具 / 候选 / 矩阵为 `20 / 7 / 20`、河网页为 `8 / 3 / 8`；两页 API 初始化，console / page error 与正式源码模块请求均为 `0`。生产构建、部署回归、控制面板实验室导引（`1280 / 390 / 320px`）、拓扑与河网专项、归档检查及差异检查通过；由本次启动的临时服务均已关闭。未改 `app/`、`source/`、Wiki、地图、存档、API 或生成流程。既有 `regress:shoreline` 阈值漂移与 AI 机器目录四项陈旧状态保持单列，未在本项扩修。

- **权威任务第 319 项：重生成城镇或河流后的海路局部化。** `已完成；来源：用户报告正式环境现有地图的跨海蜘蛛网路线`
  - **范围**：修正海路 Urquhart 候选边的距离取舍，保留相近港口的连线而非最远连线；为纯候选边和“城镇 / 河流重生成会重建路线”补充确定性回归。
  - **完成记录**：根因是水域候选边在保留 `65%` 时按距离倒序排列，实际保留了最远的边；散布在同一大洋的港口于是被跨世界连接，渲染器只是如实绘制。现按距离正序保留近边，陆路候选逻辑未改变。固定纯候选集稳定选中 `1～13` 号最近边；6k 固定地图依次执行初始生成、重生成城镇、重生成河流后仍保留海路，最长端点跨度分别为 `119.480 / 93.150 / 178.839` world。
  - **验收记录**：隔离 Chrome 通过正式 API 依次执行新图、城镇重生成和河流重生成，海路最长端点跨度占地图对角线 `6.56% / 9.62% / 6.21%`，均低于 `15%` 的跨世界门禁；route mesh、console、page 和 WebGL error 均为 `0`。锁定城镇 / 路线的 Node 与生产构建静态 Chrome 回归、10k / 50k / 100k 陆路连通、生产构建、归档检查与差异检查通过。未修改 `source/`、用户当前线上地图、存档格式、API、河流 / 城镇 / 政治数据或手工锁定路线；既有地图只会在用户后续明确执行“重算道路”或同类重生成时使用修复后的海路生成器。

- **权威任务第 320 项：浏览器地图保存反馈收敛。** `已完成；来源：用户要求取消控制面板内的保存提示，仅保留下方 toast，并延长失败提示`
  - **范围**：仅调整“保存到浏览器”入口的进行中、成功和失败反馈；成功只显示页面下方 toast，失败 toast 显示原因并延长可见时间；清理该入口遗留的旧成功状态。
  - **完成记录**：浏览器保存不再向 `file-operation-status` 写入“正在保存”、IndexedDB / LocalStorage 降级体积摘要或失败信息。若面板仍残留旧版本的浏览器保存摘要，下一次保存会精确清除该旧文本；其它导入、导出和错误诊断文本不受影响。成功继续显示下方“保存成功”；失败改为下方红色“保存到浏览器失败：具体原因”，持续 `6s`。
  - **验收记录**：隔离 Chrome 经真实“保存”下拉执行成功保存，控制面板为空、下方 success toast 正常显示；注入浏览器存储故障后，面板仍为空、下方 error toast 含原因，`5.5s` 时仍显示、约 `6s` 后关闭，console / page error 为 `0`。`regress:api-data-compatibility`、海路 Node / Chrome 回归和生产构建通过；归档与差异检查通过。未改变 IndexedDB / LocalStorage 降级、保存数据、压缩、存档格式、自动恢复、云端保存、地图内容、API 或其它操作的面板提示。既有全局交互流程审计仍由 HF-03 的 ControlPanel `detail: {target}` 证据与快捷键模式 `30 / 29` 基线漂移阻断，本项未扩修。

- **权威任务第 321 项：重生成网络质量与单一 Loading 纠偏。** `已完成；来源：用户复查正式环境后指出道路蜘蛛网、重生成无 Loading、存档加载双 Loading，以及河流断连 / 过连 / 打结 / 离散细线仍存在`
   - **321-A 固定失败基线**：以用户截图为视觉反例，并冻结 `10k / 50k / 100k` 固定 seed 与城镇、河流、道路重生成 salt；记录路线总量、逐段邻接、陆海合法性、实际路径 / 弦长比、港口度数，以及河流父链、共享汇流 cell、显示汇流距离、自交、非父子交叉和视觉碎片。
   - **321-B 道路质量**：第 319 项的海路近邻排序继续保留，但不得再以端点跨度代替完整路径验收。海路候选必须形成有界稀疏网络，逐段沿 pack 邻接行进，只通过声明水体、港口端点或可航河道；陆路不得进入水域。城镇、河流和道路三条显式重生成链必须同时通过。
   - **321-C Loading 单一来源**：重生成从同步 operation 改为可先完成一次浏览器绘制的异步入口，计算开始前显示唯一 Loading，成功、失败、取消和锁冲突均可靠清理。新图、完整存档导入、浏览器恢复、云端导入和高度图替换只由 generation Loading 展示阶段；operation 继续持有 busy、回滚和健康记录，但不得再叠出第二个气泡。
   - **321-D 河网显示质量**：声明父子河流只允许在 canonical 共享汇流 cell 对应的父河局部段上建立唯一显示锚点，禁止投影到整条父河的任意近点。基础路径和补接段都必须检查自交、回头和非父子交叉；无父河、无受保护出口且低于确定性长度 / 宽度门的孤立碎片只在显示层隐藏，不改 canonical `cells / gridCells / parent / basin / source / mouth`。
   - **最小验收**：固定三档及多 salt 中，路线相邻 / 陆海错误为 `0`，海路数量与港口度数有界；声明汇流显示距离、错误汇流、自交、非父子新增交叉和可见孤立细碎片均为 `0`。真实浏览器操作确认重生成期间 Loading 已绘制、地图替换 Loading 同时可见数不超过 `1`，完成 / 失败后清理；存档、锁、历史、拾取、PNG / GeoJSON、生产构建与错误面通过。
   - **边界**：不修改 `source/`，不自动重写用户既有存档，不把第 322 项 Worker 化混入本项；旧图只在用户显式重生成对应派生数据时采用新算法。
   - **完成记录**：道路与海路改为按陆地 / 水体分组的确定性 Kruskal 稀疏骨架并只保留少量局部冗余，首都、省会及可路由行政组具有必要覆盖；路线合并拒绝重复 cell 闭环。河网只按 canonical 共享汇流 cell 的父河局部进度窗建立锚点，以交点位置和双河进度一对一识别新增交叉，并单独拦截同河非相邻回折、跨河顶点接触与共线重叠；同父 sibling 的亚像素双尾巴收束为单一 junction，改形点按原路径进度保留 flux。冻结对象按实际几何记为 partial；孤立细碎片策略只为本次非冻结候选固化到显示 metadata，旧图、手工河和锁定河默认保留。重生成改为先绘制 generation Loading 再计算，完整地图替换由统一 owner 从读取 / 解析覆盖到成功或回滚，普通 operation 气泡不再叠加；空存档、取消、锁冲突、任务占用和失败均统一清理。
   - **验收记录**：路线 `10k / 50k / 100k` 的初始、城镇、河流和道路四阶段全部通过，逐段邻接、陆海、重复 cell、镜像和行政覆盖错误为 `0`，最大端点度数为 `3`，最大实际路径 / 弦长比为 `5.645`；10k 的 trail / searoute 四阶段最大值由失败基线 `564 / 136` 收敛到 `206 / 43`。河流三档与 `3 seed × 2 salt` 的断接、错锚、自交、回头、新增交叉、可见碎片和逐点 flux 异常均为 `0`，浏览器最大汇流 gap 为 `5.684e-14`，977 河高密候选 apply 为 `156.587ms`。Loading 成功、失败、取消、锁冲突、文件读取、回滚、空存档和已显示 operation 的并发反例峰值均为 `1` 且结束归零；路线 / 河流锁、连通、存档、拾取、PNG / GeoJSON、生产构建、application / page / active health / WebGL error 通过。整图生成和正式装载仍有已登记第 322 项的 `main-thread-long-task / operation-stall`，本项没有伪称已完成 Worker 化。未修改用户现有地图或 `source/`，未提交或推送。

- **权威任务第 322 项：复杂计算 Worker 化。** `已完成；来源：用户要求梳理复杂计算清单并逐步迁出主线程，避免阻塞交互`
   - **P0 重生成事务**：`generate.regenerate` 实际公开的地理要素、路线、河流、城镇、国家、省份、标记、外交、宗教、军事与地区十一类重算，以及这些领域连带的人口 / 经济下游；高度基础 / 下游 / 全量派生重建、气候下游重算和洋流世界重算。现有整图生成 Worker 还需继续拆出结果复制、正式地图装载和派生渲染准备，避免“Worker 已算完但主线程安装结果”仍形成长任务。
   - **P1 核心算法**：网格细分与 Delaunay / Voronoi、pack 重建、Feature / 岸线拓扑、河流洼地消解与流量累计、路线寻路、文化 / 宗教 / 国家 / 省份扩张、人口重布、城镇选址、经济交易、军事和地区生成。
   - **P2 大数据与 CPU 渲染准备**：大型存档 JSON 解析、迁移、校验、序列化与压缩；surface / shore / political / route / river mesh、标签布局和 picking index 的 CPU 构建。WebGL buffer 上传与最终 draw 保留主线程。
   - **架构边界**：主线程只负责确认、map revision / generation token 校验、锁保护、事务提交、历史、DOM / 面板和 GPU 提交。Worker 输入必须是可复制或明确 transfer 的不可变快照，不得转移仍由正式地图持有的 typed arrays；旧浏览器 / Worker 初始化失败需安全降级。
   - **用户无感知与中文文案门**：Worker 化属于内部实现，普通用户不得在全局 Loading、控制面板、toast、对话框、状态栏、进度、取消、降级或错误提示中看到 `Worker / worker`、线程、任务会话、消息包、结构化克隆、buffer、`LocalStorage / sessionStorage / IndexedDB`、Blob、缓存后端等技术实现或浏览器存储概念。`routes / rivers` 等内部 kind 必须按语义显示为“路线 / 河流”等自然中文；所有第 322 项触达的普通用户界面文案一律优先使用纯中文，只有确实没有合适中文译法的专名才可保留外文。技术标识只允许出现在开发模式、内部日志、测试产物或公开 API 的结构化诊断字段，不得泄漏到普通控制面板和 Loading；安全降级应描述为“正在改用兼容方式继续处理”等用户可理解的结果，不解释其底层机制。
   - **最小验收**：同 seed 与同 salt 的 Worker / 主线程降级结果语义一致；取消、换图、迟到结果、故障注入和锁冲突均不产生半写入；100k 代表流程的交互窗口主线程长任务归零或达到分阶段预算，Loading 阶段、进度、回滚、旧存档与 API 兼容通过。真实浏览器覆盖成功、长任务、取消、兼容降级、故障与回滚路径，对全局 Loading、控制面板及关联提示做可见文本审计：普通用户界面不得出现上述技术词、内部英文 kind 或可自然翻译却未翻译的英文文案。
   - **精简执行门（2026-08-10）**：本项不再以一个持续移动的共享树同时承载全部 P0～P2。恢复施工前先做阶段 0 只读盘点，把当前未提交树按“已独立验收 / 数据层已冻结 / 正式入口未接 / 动态门未过 / 非本项改动”分类，并记录可恢复 checkpoint；盘点期间不补代码、不跑全量 CDP。
   - **阶段 A——十一类重生成闭环（已接受，2026-08-11）**：只收敛通用 Worker 协议、持久镜像、十一类正式入口、领域补丁、prepared renderer 原子提交与故障恢复。静态 / Node、单次 100k fresh routes 代表门和冻结后的唯一一次 10k focused 已依次通过并由独立给事中、观察使接受，形成内部 checkpoint；其它 P0 / P1 / P2 handler 即使数据层已存在，也不在本阶段接入。10k focused 的连续全锁河流 no-op 旧首败最终定位为夹具 CPU surface 指纹产生 `120ms` LongTask 后未 drain observer，正式两次 no-op 窗口为 LongTask `0`，CPU / GPU / renderer exact 门未放宽。
   - **阶段 A 性能预算（2026-08-11）**：单次 `100k 新图 → 河流 → accepted 后取消 → fresh 路线` 代表门中，Worker 单包发送、主线程单包解码、单次同步 GPU 调用、原子 render commit 与 UI refresh 均须 `< 50ms`；允许至多 `2` 条已完整归因的末端 LongTask，且每条 `≤ 75ms`，仅可来自 overlay 首次 style / layout 或 `resume / draw / updateLabels`。任何未归因 LongTask、第三条 LongTask、超过预算、原子性 / 取消 / 回滚 / 错误面失败仍为 BLOCK。该预算只用于阶段 A checkpoint；阶段 D 必须重新收紧标签 / DOM / picking，阶段 E 不得自动沿用。
   - **阶段 B——P0 复合与整图链**：接入高度基础 / 下游 / 全量、气候下游、洋流世界，以及现有整图生成的结果复制、正式装载和初始渲染准备；复用阶段 A 的唯一事务状态机，不复制第二套提交协议。
   - **阶段 B1——P0 复合派生闭环（已接受，2026-08-11）**：高度基础 / 下游 / 全量、气候下游和洋流世界重算已统一接入阶段 A 的 Worker map-mirror 会话、prepared renderer、单历史事务与失败恢复；同 seed 的 Worker / fallback、锁、取消、迟到、故障、undo / redo 由专项 Node 门覆盖。真实浏览器中十一类地图事务门和独立洋流世界门通过，后者锁定正式 session、输入 / 输出分片、十一阶段、稳定 map 引用、单历史、undo / redo、Loading、WebGL 与普通用户技术词审计。阶段 B 尚余整图生成结果复制、正式装载和初始渲染准备，B1 checkpoint 不代表阶段 B 或第 322 项完成。
   - **阶段 B2——整图生成与初始渲染闭环（已接受，2026-08-11）**：整图生成已接入独立 `generation.compute`，Worker 直接返回完整地图与同一事务的 prepared render DTO，主线程只做绑定校验、原子 renderer 安装、正式地图替换、面板同步和 GPU 提交；旧浏览器 / Worker 失败仍走原主线程兼容路径。正式装载不再重复构建 CPU surface / shore / political / route / river / point / label / picking，100k surface base 继续按既有分段 GPU 所有权提交。大量 overlay 节点先进入隐藏的 32 节点批次，随后逐批恢复原有直接子节点结构并让浏览器提交布局；安装完成到地图接管之间另有明确让步，取消、迟到和 rollback 仍由 prepared installer 事务统一处理。专项 Node 覆盖 Worker / fallback 同源、取消 / 迟到、100k 与 installer 嵌套所有权；生产构建和真实 10k / 100k Chrome 门分别得到 `10004 / 99846` grid cells、prepared load `1`、legacy load `0`、输入包 `3 / 3`、输出包 `948 / 4435`、decode 最大 `4.6 / 33.7ms`、LongTask `0 / 0`，残留 overlay 批次、非性能 health、普通应用 console、page、WebGL 与 Loading 错误均为 `0`。阶段 B 已闭合，但该 checkpoint 不代表第 322 项完成；下一步进入阶段 C。
   - **阶段 C——P1 核心入口**：按网格拓扑、社会扩张、经济 / 人口、路线 / 城市寻路、军事策略等可独立验收的垂直切片逐一接入；每个切片由主线程各自冻结、自检、checkpoint，不等待整批完成才发现正式入口问题，也不重复启动独立复核角色。
   - **阶段 C1——网格拓扑（已接受，2026-08-12）**：`grid.applyWrite / grid.refine` 已改为复用阶段 A～B 的唯一 Worker map-mirror、prepared renderer、原子地图替换、单历史与失败恢复链；Worker 同一事务准备完整 renderer DTO，主线程不再另建整图 snapshot transaction 或调用 legacy 全量 renderer load。正式 binding 同时进入普通请求、持久 session 和 fallback，输出在提交前校验 map identity、revision、grid / lock fingerprint，提交后保持正式 map identity并支持撤销恢复。重复的 `Maximum call stack size exceeded` 已由 100k 邻接表安全归约闭合；cell visual 大循环每 `32` 项或末项进入异步 checkpoint 后，render install prepare 从 `37.8s` 降至约 `2.9s`，操作从 `51.3s` 降至约 `16.7s`。操作窗与 setup 已严格分离，原先的 `59ms` 属于预检 / 基线采集；`legacyLoadUnchanged` 也改为在 forward refine 后、undo / redo 前取样，现真实为 `true`。最新正式 Chrome 的 Worker accepted / committed、输入 / 输出 `212 / 3954` 包、100k 网格、全部 `13` 个 prepared layer、单历史、撤销 / 重做、PNG、GL 与非性能错误面均通过。用户明确批准把该阶段操作窗的 `159 / 112ms` 两条 LongTask 登记为阶段性例外；C1 自动门固定为最多 `2` 条且单条不超过 `160ms`，不得向其它入口或更多 LongTask 扩张。该例外不代表阻断已消除，阶段 D 的 CPU renderer / 标签 / picking 治理和阶段 E 的最终性能终验必须重新收紧，不自动继承。此前无收益的解码索引 / overlay 试修已完整撤回。当前阶段累计产品 `4` 文件 `+109 / -54`、工具 `3` 文件 `+103 / -4`，专用浏览器夹具 `209` 行，委派等待为 `0`；C1 建立 checkpoint 后进入 C2。
   - **阶段 C2——社会扩张与历史恢复（已接受，2026-08-12）**：文化 / 宗教正式扩张以及对应撤销 / 重做已统一复用同一 `social-expansion.compute` Worker map-mirror session、prepared renderer、绑定校验、原子提交与失败恢复链。历史命令保存可复制的前后 patch，撤销 / 重做先在 Worker 镜像应用目标 patch 并准备正式 renderer DTO，主线程只移动历史栈、提交 GPU / DOM 与恢复界面；其它领域历史继续沿用既有同步路径。专项 Node、UI / API 契约、通用 Worker 协议和生产构建通过；真实 Chrome 中文化 / 宗教的应用、撤销、重做共 `6` 次运行与 `6` 次提交均复用同一 session，数据、对象引用、历史栈和最终 idle 状态同源，原同步历史刷新约 `251～329ms` 的主线程阻塞已消除。用户明确批准把最终复验首次文化操作入口唯一一条 `56ms` LongTask 登记为 C2 阶段性例外并放行；该例外仅允许首次 fresh Worker 启动窗口最多 `1` 条且不超过 `60ms`，不适用于撤销 / 重做、后续复用操作、其它 C 切片、阶段 D 或阶段 E 终验。C2 累计产品 `7` 文件 `+244 / -22`、既有工具 `2` 文件 `+33 / -7`、专用浏览器夹具 `72` 行，委派等待为 `0`；第 322 项已接受 checkpoint 为 A、B1、B2、C1、C2，即 `5 / 6`，下一步进入阶段 C 的后续核心切片，不继续追查本条已登记信号。
   - **阶段 C3a——经济重算、市场归属与历史恢复（已接受，2026-08-12）**：`edit.economy.rebuild / assignCells`、经济面板重算入口和市场归属预览应用已接入 `economy.compute` 持久 map-mirror session、prepared point / labels / picking、领域 patch、原子提交与失败恢复；经济命令的撤销 / 重做也先在同一 Worker 镜像应用目标 patch并准备画面，主线程只移动历史栈和提交正式状态。主线程不再遍历整份 `pack / economy` 计算来源指纹，时效由既有 map identity、revision、generation token、锁指纹和 session binding 保证，权威经济指纹只在 Worker 镜像内生成。专项 Node、通用 Worker、API 异步契约、生产构建均通过；真实 10k Chrome 完成经济重算、单 cell 市场改派、撤销、重做共 `4` 次运行 / 提交，后 `3` 次复用同一 session，数据精确往返、最终 session idle，操作窗 LongTask、非性能 health、普通应用 console、page、WebGL、Loading 和普通文案技术词均为 `0`，本切片不登记性能例外。C3a 产品约 `3` 文件 `+252 / -26`，测试约 `3` 文件 `+198 / -2`，专用浏览器夹具 `174` 行，委派等待为 `0`；阶段 C 仍未闭合，下一切片只处理人口，不混入路线 / 城市寻路或军事策略。
   - **阶段 C3b——人口调整、转移与历史恢复（已接受，2026-08-12）**：`edit.population.applyAdjustment / transfer` 和人口面板同源入口已接入 `population.compute` 持久 map-mirror session、prepared surface / point / labels / picking、领域 patch、原子提交与失败恢复；人口命令撤销 / 重做先在 Worker 镜像应用目标历史 patch并准备画面，主线程只移动历史栈和提交正式状态。人口分配 plan 与来源指纹只在 Worker 镜像构造，主线程不再同步遍历人口载体；非 Worker legacy 直调继续兼容，历史测试注入点与 grid 分支统一。专项 Node 覆盖 10k / 100k、调整 / 转移、镜像历史、锁、取消、故障、陈旧和写集，100k 数据层约 `603ms`；UI / API、registry、生产构建通过。真实 10k Chrome 完成人口调整、人口转移、双撤销、双重做共 `6` 次运行 / 提交，后 `5` 次复用同一 session，数据与容器引用精确往返、最终 session idle，LongTask、非性能 health、普通应用 console、page、WebGL、Loading 和普通文案技术词均为 `0`，不登记性能例外。C3b 产品约 `3` 文件 `+230` 行，测试约 `3` 文件 `+170` 行，专用浏览器夹具 `138` 行，委派等待为 `0`；阶段 C 下一切片处理路线 / 城市寻路，不混入军事策略。
   - **阶段 C3c——路线 / 城市寻路与历史恢复（已接受，2026-08-13）**：路线创建 / 改线预检与提交、城市迁移关联路线重寻，以及两类命令的撤销 / 重做已接入同一 `route-path.compute` 持久 map-mirror session、prepared route / point / labels / picking、绑定校验、原子提交与失败恢复。正式面板与公开 API 的预检改为异步 latest-wins；主线程不再执行完整寻路，历史恢复先在 Worker 镜像应用冻结 patch 并准备画面，再只移动正式历史栈。专项 Node 覆盖 10k / 100k、create / edit / relocation / city-relocation、plan-only、取消、故障、陈旧、fallback、写集和镜像撤销 / 重做；UI / API、registry、生产构建通过。真实 10k Chrome 完成路线预检 / 更新 / 撤销 / 重做与城市预检 / 移动 / 撤销 / 重做共 `8` 次运行 / 提交，后 `7` 次复用同一 session，主 map、路线、城市和 burg 权威集合身份稳定，四次历史往返精确。测试重指纹与产品窗口初次混采得到 `11` 条 `52～194ms` 信号；隔离重指纹并完成一次夹具优化后仍有 `4` 条 `110～193ms`，用户按统一处置门登记 C3c 最多 `4` 条且单条不超过 `200ms` 后放行，不继续重复诊断，且不得扩张到军事、阶段 D 或 E。C3c 产品 `6` 文件约 `+720 / -121`，测试 `3` 文件约 `+377 / -5`，专用浏览器夹具 `206` 行，委派等待为 `0`；阶段 C 下一切片只处理军事策略。
   - **阶段 C3d——军事策略与历史恢复（已接受，2026-08-13）**：会按保锁语义重建整套军事部署的 `edit.military.setRatios` 及军事面板同源入口已接入既有 `military-policy.compute` 持久 map-mirror session、prepared point / line / labels / picking、领域 patch、绑定校验、原子提交与失败恢复；对应撤销 / 重做先在 Worker 镜像应用历史 patch 并准备画面，主线程只移动历史栈和提交正式状态。军团状态、驻地、基地与战报等局部命令保持原同步路径，不扩大本切片。专项 Node 覆盖 10k / 100k、锁定军团、正式 / fallback 同源、17 条写集、取消、迟到、故障、陈旧、prepared 四层和 Worker 历史往返；100k 数据层约 `1.42s`。真实 10k Chrome 完成应用、撤销、重做共 `3` 次运行 / 提交，后 `2` 次复用同一 session，国家 / 军事镜像别名、状态对象身份和前后值精确，LongTask、非性能 health、普通应用 console、page、WebGL、Loading 与普通文案技术词为 `0`。C3d 产品 `3` 文件 `+208 / -33`，测试 `3` 文件 `+158 / -5`，专用浏览器夹具 `113` 行，委派等待为 `0`；阶段 C 已闭合，下一步进入阶段 D，不再扩收局部军事命令。
   - **阶段 D——P2 大数据与 CPU 渲染**：接入大型存档读写 / 恢复、整图初始 renderer prepare、动态 mesh / 标签 / picking 会话；File / Blob / 大字符串兼容、二进制存档降级与 GPU 主线程边界分别验收。
     - **D1——大型存档读写与恢复（已接受，2026-08-13）**：大型存档导出、浏览器保存、完整导入与浏览器恢复已接入 `map-file-io` Worker，导入结果同时携带完整 prepared renderer；`.webfmg`、JSON、gzip-base64、File / Blob、LocalStorage / IndexedDB 和旧迁移保持兼容。初次目标门的导入 / 恢复各有 `729 / 684ms` LongTask，最终定位为 prepared install 前对即将替换的旧图强制应用导入主题，导致同步重建 surface / line / labels 和 draw；renderer 新增只接收 prepared presentation 的轻入口后，导入主题与单位不再刷新旧图，新图仍由 Worker prepared 结果原子安装。最终 10k Chrome 四条正式入口均 Worker accepted，prepared load `2`、legacy load `0`、旧图完整主题 / 单位刷新 `0`、LongTask `0`，地图、历史、存储、Loading、health、console、page、WebGL 与普通用户技术词门通过。阶段 D 尚余 D2 动态 mesh / 标签 / picking 盘点与必要接线。
     - **D2——动态 mesh / 标签 / picking（已接受，2026-08-13）**：主题、视图模式、图层与图层组可见性、平滑边界、海洋高度和最大城市标签数等普通显示入口已接入独立 `render.prepare` map-mirror session；首次传递地图，后续复用只发送 `3` 个输入包，主线程只应用展示标量、提交 prepared renderer、恢复控件和完成 GPU / DOM 原子安装。静态盘点确认普通显示入口不触发 picking 重建；数据事务的 picking 继续由既有 prepared 路径负责。100k 目标复验中同步 `refreshCellSurface / refreshLineLayers / refreshLabels` 调用均为 `0`，视图、点图层和平滑边界 LongTask 为 `0`；主题由 `905ms` 降为 `55 / 60 / 68ms`，最大标签由 `255ms` 降为 `54 / 59ms`，均已完成一次归因与一次实质性 Worker 化。按统一阶段门登记 D2 仅允许主题最多 `3` 条且单条不超过 `75ms`、最大标签最多 `2` 条且单条不超过 `60ms` 的末端样式 / 布局或 draw 信号；该额度不适用于其它显示入口，也不得带入阶段 E。非性能 health、WebGL、Loading 错误为 `0`，兼容降级只使用普通中文并在失败后恢复 renderer 权威控件值。阶段 D 已闭合，下一步只进入阶段 E 集成终验。
   - **阶段 E——集成终验与归档**：仅在 A～D checkpoint 均被接受后运行完整 10k / 100k、Worker / fallback、旧档、取消 / 迟到 / 故障、真实 UI / API、视觉、health / console / WebGL 和性能终验，再更新完成记录与归档。100k routes 末端样式 / 布局此前已完成多轮归因及 city 状态恢复、overlay DOM 复用等实质优化，独立复验曾连续两次 LongTask 为 `0`，本轮正式终验仍偶发 `52 / 78ms` 信号；按用户统一处置门，仅 `100k routes` 的 reused 与取消后 fresh 两个窗口各登记最多 `1` 条且单条不超过 `80ms`，其它阶段 E 操作继续要求 LongTask `0`。任何内部 checkpoint 都不得把本项标为完成。
   - **阶段 E 完成记录（2026-08-13）**：最终 10k 与 100k 正式链均通过，覆盖十一类重生成、持久镜像复用、prepared 安装、显示 replay、取消、迟到、故障、回滚、hard-cell patch、旧档、存档读写、真实 API 和普通用户文案。100k hard-cell 首次单格编辑从约 `206ms` 收敛到 `17.4ms`，第二格为 `9.2ms`；正式 base 的 `43,093,008B` CPU / 六段 GPU identity 与 checksum 不变，patch 颜色、side alpha、range 与 GPU 字节同源。100k routes 仅保留上述精确登记预算，最终一次完整门实际 LongTask 为 `0`；最终 health、console、page、WebGL 与 Loading 错误为 `0`。独立给事中与观察使均 `ACCEPT`，第 322 项完成并转入归档。
   - **协作与证据（2026-08-11 收紧）**：阶段 A 已完成本编号所需的独立调查证据和阶段性复核；阶段 B～D 默认由主线程作为唯一写者直接完成，不再逐阶段重复中书舍人、尚书郎、给事中、观察使接力。只有遇到会改变冻结架构的重大决策才可使用尚未消耗的任务级调查额度；A～D 集成冻结后只做一次独立给事中复核，阶段 E 只做一次独立观察使终验。夹具、正则、字段名、等待时序和首败归因均由主线程处理。详细日志、trace、截图和大矩阵只存本地产物，主线程只保留结论、关键数值、首败、artifact 路径和下一阶段输入；新增发现若不阻断当前阶段最小验收，只登记，不扩项。

- **权威任务第 323 项：地图规模统一限制为 1～100k。** `已完成；来源：用户要求地图规模最大支持 100k，越界数字自动降级且不得小于 1`
   - **统一口径**：建立唯一的地图规模归一化规则，所有用于创建或扩大地图的目标值均为整数 `1～100000`；有限数值大于 `100000` 时自动使用 `100000`，小于 `1` 时自动使用 `1`，两个边界值保持不变。非数值输入继续走明确的默认值或输入错误，不得静默变成意外规模。
   - **覆盖入口**：生成 Tab、整图生成选项、公开 API / schema / 配方、URL 或预设恢复，以及 `grid.refine` 的 `targetCells` 必须复用同一规则；当前 UI 的最小值 `1000` 和网格细分的最大值 `200000` 一并收敛。归一化后的值必须回显到控件、地图 options / metadata、预检结果和操作日志，禁止界面值、API 值与实际计算值分叉。
   - **一格与细分边界**：`cellsTarget=1` 必须能够完成生成并至少绘制一个有限、可见的地图单元；依赖多样本的人文或网络阶段在样本不足时应确定性降级为空结果或最小合法结果，不得崩溃。细分仍只允许增长，但请求超过 `100000` 时先按 `100000` 执行；地图已经达到上限时返回明确 no-op，不再分配超过上限的网格。
   - **存档安全**：不允许通过静默删除 cells 把既有超限存档伪装成 100k；实际网格超过 `100000` 的导入应在替换当前地图前明确拒绝并保留原图，`≤100000` 的旧存档保持兼容。不得修改 `source/`。
   - **最小验收**：固定覆盖 `-1 / 0 / 1 / 99999 / 100000 / 100001 / 200000`，确认 UI、生成器、API、schema 与细分预检归一化一致；1-cell 地图完成真实浏览器生成、稳定显示、PNG、保存与恢复；100k 生成 / 细分继续通过，超限存档拒绝与回滚无数据丢失，application / page / active health / WebGL error 为 `0`。

- **权威任务第 324 项：内置真实地理与历史场景地图模板系统。** `已完成；来源：用户要求内置常见世界地图场景，并按用户选择的 cell 数一键创建`
   - **产品入口与概念边界**：生成 Tab 新增“地图模板”选择与一键创建入口，先选模板、再选第 323 项统一约束下的目标 `cellsTarget`。新系统是带真实地理范围、版本和映射规则的场景模板，不得与现有“大陆 / 群岛 / 地中海”等程序化高度模板混为一谈；公开 API 提供等价的模板枚举、详情和创建能力，AI 能发现每个模板的范围、版本、推荐规模及历史年代。
   - **首批固定模板**：
      1. 世界地图。
      2. 中国地图：完整包括藏南、阿克赛钦、台湾、南海诸岛和钓鱼岛，不得因画布裁剪、投影、岛屿面积阈值、几何简化或 cell 重采样丢失。
      3. 欧洲地图。
      4. 北美地图。
      5. 南美地图。
      6. 非洲地图。
      7. 亚洲地图。
      8. 东亚地图：西边界从七河平原所在经度开始，东边界到北海道所在经度结束，北边界从贝加尔湖所在纬度开始，南边界到越南河内所在纬度结束。
      9. 澳洲及巴布亚新几内亚岛、新西兰岛地图，三部分都必须完整进入画布。
      10. 南极洲地图。
      11. 中原地图：北到内蒙古中部，南到浙江与福建交界，西到兰州，东到东海。
      12. 日本本州岛地图。
      13. 朝鲜半岛地图。
      14. 中东地图。
      15. 神圣罗马帝国地图。
      16. 地中海地图：以复刻罗马帝国场景为主要目标。
   - **模板契约与来源**：每个内置模板使用稳定 `id / version` 和可审计 manifest，至少记录中文名、类别、地理范围、投影 / 裁剪规则、推荐 cell 数、canonical 海陆 / 高程 / 水文 / 区域掩膜、必须保留的岛屿与锚点、数据来源、再发布许可、内容摘要和历史快照年代。所有正式资源随应用版本化并按需加载，不依赖创建时访问第三方网络。神圣罗马帝国与罗马帝国场景必须先固定单一明确年代和史料口径，并内置对应年代的政治 / 人文场景预设，不得把不同年代边界拼成无日期模板；普通地理模板默认只复刻地理底图，人文世界继续由生成器派生。
   - **模板到 cell 的确定性映射**：模板 canonical 几何投影到按目标 `cellsTarget` 建立的新网格，派生海陆、高度、Feature、湖泊、河流、区域掩膜及后续人文数据；不得把模板原始 cell 数直接复制给所有规模。相同模板版本、cell 数、seed 和 salt 必须得到相同语义 checksum。映射需保持海岸与湖岸拓扑、陆岛归属、河口和跨日界线连续性，并用受保护锚点避免关键小岛在常用规模下消失；极低 cell 数允许明确标注的示意化降级，但创建不得崩溃或伪称完整精度。
   - **普通地图与兼容性**：模板创建完成后得到可编辑、可重生成、可保存、可导出和可撤销替换的普通地图；metadata 只保留模板 `id / version / source checksum / requestedCells / actualCells` 等来源证据，不建立会覆盖用户后续编辑的持续绑定。旧存档和未选择地图模板的生成链保持原行为，既有 `heightmapTemplate` 字段继续兼容；不得修改 `source/`。
   - **最小验收**：16 个模板在 UI 与 API 中顺序、名称和 manifest 一致，并各自完成默认规模的真实生成、结构审计和固定视口截图；纯数据门禁对全部模板覆盖 `1 / 10000 / 100000` 目标值及第 323 项上下限钳制，浏览器再覆盖世界、中国、东亚、澳洲组合、神圣罗马帝国和罗马帝国场景的 10k / 100k。中国五类指定区域、东亚四边界、澳洲三部分和两类历史年代均有机器断言与人工截图证据；存档往返、PNG / GeoJSON、picking、回滚、旧图兼容、延迟资源加载及 application / page / active health / WebGL error 通过。

- **权威任务第 325 项：河流重算与道路解耦、细分旧存档道路身份修复。** `已完成；来源：用户要求河流重算不得直接重算道路，并要求在当前 5410 存档实测修复道路蜘蛛网`
   - **完成记录**：河流成功、全锁、取消、失败回滚及撤销 / 重做均只更新河流域；道路三镜像、全部 route / packRoute 对象、道路 salt、锁仓与条目、route mesh / GPU buffer、道路 picking bucket / segment identity 保持。旧档 city / burg / grid / pack 身份改为先完整校验、后原子应用；非法槽位、孤立 burg、冲突端口、TypedArray 容量和端点四身份均在写入前门禁，split 路线不再借用非首尾 cell 的旧 burg。
   - **港口兼容记录**：加载旧图只诊断并标道路待派生，不静默搬城、清港或改坐标 / port；显式细分和道路重算才在同一 refinement mother、同陆地 feature / 国家 / 省份及互为主导 overlap 的同水体映射内修复普通港口。正式 haven、riverEdges、冻结温度与水路寻路共用同源函数；锁城、锁路端点和锁海路写前拒绝不兼容变更，普通无候选港才双边清除。city / burg 单侧 port 镜像缺失可在当前正式港位原地补齐并同步派生，同 cell 多港按“锁定合法港优先、再合法港、再可同步港、最后稳定 ID”保留唯一 owner；不扩张 provenance 或 schema。
   - **精确验收**：匿名旧档、保存往返、fresh `10k / 50k / 100k`、真实 `10k → 100k`、锁、全锁 no-op、取消、冲突 / 渲染后失败回滚和成功后的 A/B/A/B 撤销重做通过。冻结真实 `100000` cell 存档经正式文件导入后为 `607` 条道路、`1531` 条河流；河流重算为 `1528` 条时道路仍为 `607`，道路同步 / 异步 mesh 与全量对象索引刷新均为 `0`，只发生 `1` 次河流局部 picking 刷新。随后显式道路重算得到 `288` 条路线（道路 `16`、小径 `227`、海路 `45`），`188` 个港口全部通过正式 placement，正式可达水体 `1`、缺失海路 `0`；逐段邻接、陆海、正式水路、重复 cell、三镜像、端点身份、display teleport、港口身份、重复港口 cell、picking、行政覆盖、稀疏度与路径比例错误全部为 `0`，真实 route picking 命中。最大显示段 `71.491 world`、最大路径弦长比 `5.723`、最大端点度数 `4`；非性能 health error、console / page error 与 WebGL error 均为 `0`，第 322 项已登记的长任务 / operation stall 仅作性能基线。独立系统 Chrome 使用外部冻结 SHA-256 校验输入，三阶段截图存于临时目录，未连接用户 Chrome、未写回输入，也未提交私人存档指纹或样本；观察使另外在用户原 `5410` 同一标签页经页面批准执行正式 `rivers → routes`，路线面板实显 `288 / 45`，实际选择、高亮和定位的道路沿陆地连续，最终全图无蜘蛛网，且未覆盖既有浏览器恢复点。

- **权威任务第 326 项：地点解析、距离与方位查询 API。** `已完成；来源：用户要求通过地名或 ID 查询两地距离与方位，并为 AI 提供先查 ID、再精确测量的稳定流程`
   - **公开方法与共用实现**：在现有只读 `analysis` 命名空间新增 `resolvePlace(reference, options?)`、`measureDistance(from, to, options?)` 和 `getDirection(from, to, options?)`，浏览器 `window.webglGeneratorApi` 与无头 `createHeadlessMapApi` 复用同一纯数据解析、锚点和测量模块。三者均不得改变地图、selection、相机、图层、历史、checksum 或显示偏好，并须进入 `info.describe`、schema、能力矩阵和 AI 文档。
   - **名称与 ID 解析**：首期地点固定为国家 `state`、省份 `province`、城镇 `city`。名称去除首尾空白后按完整名称匹配，国家同时接受当前 `name / fullName`，不默认做模糊猜测；同名跨类型按“国家 > 省份 > 城镇”选择，同一类型有多个同名对象时按稳定升序 ID 选择第一个。返回值必须同时给出选中对象的 `kind / id / name`、全部同名候选、候选数和实际采用的优先规则，不能因自动首选而隐藏歧义。精确 ID 输入复用项目统一对象引用 `{kind, id}`，以避免三类对象各自编号空间重叠；找不到、已删除、类型非法或锚点无效时返回结构化错误，不回退到相近名称或其它对象。
   - **AI 精确调用流程**：允许距离与方位方法直接接收名称引用或精确 ID 引用；但 AI 需要可复现的精确结果时，必须先用 `resolvePlace` 按名称读取候选并取得选中对象的 `{kind, id}`，再把两端的精确引用传给 `measureDistance` 或 `getDirection`。文档示例需展示同名跨类型、同类型重名和两阶段调用，不得只给依赖数组偶然顺序的裸名称示例。
   - **地点锚点契约**：城镇使用当前正式 `x / y`；国家优先使用有效首都坐标，否则使用国家中心 cell；省份优先使用唯一有效省会坐标，否则使用 `province.pole`，再回退省份中心 cell。结果必须回显每端的世界坐标、`anchorSource` 和地图 revision，使行政区域的“距离”明确是代表点直线距离而非边界最短距离、道路里程或航路长度。
   - **距离口径**：正式距离始终从地图世界坐标以数据计算，并复用当前单位 / 比例尺换算，至少返回有限的 `distanceWorld`、换算值、单位、格式化文本以及 `precision / approximate / reason`；浏览器与无头环境对同一地图、同一精确 ID 和同一单位配置必须在文档化浮点容差内一致。无头结果允许因旧存档坐标精度、行政代表点回退或缺少浏览器会话单位偏好产生已声明误差；缺少单位配置时仍须精确返回世界距离，并对默认换算明确标记近似，不得改用估算 cell 数或名称相似度。浏览器可选返回当前相机下的 `screenDistancePx` 作为界面诊断；该值随缩放、DPR 和视口改变，不能作为正式地理距离或写入存档。
   - **方位口径**：`getDirection` 复用 `measureDistance` 的同一解析、锚点与距离结果，并返回 `dx / dy`、以地图正北为 `0°` 且顺时针递增的 `bearingDegrees`、八方位中文结果（北、东北、东、东南、南、西南、西、西北）和完整距离摘要。地图世界坐标的 Y 轴向下时必须显式换算为“Y 减小即北”；两端重合或低于统一 epsilon 时返回“重合”，不得伪造任一方位。
   - **最小验收**：固定夹具覆盖国家 / 省份 / 城镇三类同名优先、两个省份或城镇同名首选稳定、候选清单、精确 ID 与名称结果一致、非法与删除对象拒绝、国家 / 省份锚点逐级回退、八方位边界和重合。10k / 100k、细分图及保存往返中，浏览器与无头正式距离和方位在容差内一致；浏览器切换至少三档缩放时 `screenDistancePx` 可变化而正式距离不变。API 发现、schema、AI 两阶段示例、单位换算、无副作用、生产构建及 application / page / active health / WebGL error 通过；不得修改 `source/`。

- **权威任务第 327 项：AI 上下文、项目文档与四级流程节流。** `已完成；来源：用户要求把项目结构、文档体系、通用 Skill 和尚未完成的 token 节省方案统一收敛，避免后续任务重复消耗额度`
   - **封闭范围与顺序**：本项只治理 AI / Codex 的项目上下文、文档检索、阶段交接、个人通用 Skill 与四级流程，不修改地图产品行为、生成算法、存档格式、公开 API、`source/` 或 Wiki。第 322 项已完整验收、归档并合入远端 `main`；当前第 327 项分支从该最新 `main` 建立。后续顺序固定为 `327 → 323 → 324 → 326 → 328`，不得因第 323 项编号较小而跳过本项，也不得跨编号复用施工分支。
   - **自动注入与项目结构**：先只读量化根目录及典型子目录任务实际会自动注入的 `AGENTS.md` 层级、字符 / token 估算和重复规则，再把根 `AGENTS.md` 收敛为永久约束、权威入口、当前执行顺序和极短状态。历年完成正文迁入既有归档或按日期索引；仅在确有局部规则的目录设置短小嵌套 `AGENTS.md`。机器门禁固定根说明不超过 `16 KiB`，任一普通工作路径累计项目说明不超过 `24 KiB`，且不得靠删除仍具约束力的规则达标。
   - **文档分层与检索**：保持 `docs/current-plan.md` 只含活动任务与最小验收，`docs/README.md` 只做权威路由；把持续膨胀的开发历史按日期分卷并建立稳定索引，保留全部可追溯内容和既有链接。`docs/generated/`、任务归档、旧计划备份、trace、截图和大矩阵明确为按需材料，默认读取链固定为 `AGENTS.md → docs/README.md → docs/current-plan.md → 定向 rg / 行段读取`，禁止把完整文档语料、完整开发日志或完整归档作为普通任务前置上下文。
   - **阶段交接与本地产物**：提供统一的精简任务包和阶段 handoff 模板，只传目标、当前阶段、所属文件、不可破坏约束、首个廉价门禁、结论 / 阻断及 artifact 路径；完整日志、diff、trace、截图和矩阵留在本地产物。长任务在 checkpoint 后从精简 handoff 恢复，不依赖重放整个历史会话；新权威项默认新会话，除非短会话继续执行明显更省。
   - **通用 Skill 与四级流程**：复核并补完 `$run-lean-staged-delivery` 与 `$four-officials-flow`。Skill 主入口只保留通用决策、停止条件和按需 reference 路由；普通复杂任务默认由主线程直接执行，完整四级也只在权威编号级安排一次调查、主线程实现、一次集成复核和一次最终验收，不得向内部阶段递归。委派默认不继承完整历史、顺序接力、冻结后复核、只返 blocker、复用原角色；昂贵门禁首败即停并回到最窄复现，不得以省 token 为由削弱旧档、回滚、真实浏览器、视觉或最终独立验收。
   - **可复现审计与兼容**：新增项目内上下文预算审计，检查说明文件体积 / 重复、权威路由、活动任务与归档隔离、Skill 入口与 reference 边界、阶段模板及执行顺序；审计只读取项目文档和 Skill 元数据，不上传私人会话、账号额度、存档指纹或完整历史日志。文档迁移前后，任务编号、归档索引、相对链接和中文文档规则必须保持，现有 `archive --check` 与文档检查继续通过。
   - **最小验收**：保存改造前后同一根任务和至少两个典型子目录任务的上下文预算报告，根自动注入体积达到上述硬门且较基线显著下降；从全新会话仅按权威读取链能够准确找到 `322 → 327 → 323 → 324 → 326 → 328`、第 327 项完整边界和一个历史完成项，而不加载完整开发日志 / 归档。以一个只读调查和一个小型实现做四级流程 dry run，确认最小角色选择、无完整历史继承、单写者、冻结复核、首败停止、artifact 交接与最终独立验收均有机器或可核证记录；审计、归档、链接、Skill 语法、`git diff --check` 通过，`source/`、Wiki 和产品代码为零改动。

- **权威任务第 328 项：大河参与国家与省份划界。** `已完成；来源：用户要求省份划界或重新生成省份时适度考虑大河走势，并允许大河成为省界乃至国界`
   - **适用入口与非目标**：作用于新图政治生成以及正式 `generate.regenerate` 的国家与省份重生成链，重点覆盖省份自动划界和重生成省份；不得在载入旧图、普通渲染或用户手工编辑边界时擅自重划行政区，也不得修改河道、流域、河流控制点或 `source/`。旧存档保持原边界，只有用户明确执行对应生成操作后才使用新规则。
   - **大河权威判定**：从当前 canonical 河网的流量、河宽、河流等级、连续长度和有效河段建立稳定、可审计的“大河阻隔分数”，不得按河流 ID、显示像素宽度或偶然数组顺序判断。连续主河段的权重高于短支流和季节性小河；小河可以提供轻微地形信息，但不得普遍把行政区域切成碎片。
   - **软边界语义**：大河是政治扩张和行政分配的软阻隔，不是不可跨越的硬墙。跨越成本随河流强度提高，并可由首都 / 省会可达性、既有聚落、港口、道路和合法渡河走廊适度降低；算法仍须综合人口、文化、地形、距离和区域完整性。强河两岸都具备可持续行政中心和连通腹地时，可以形成不同省份，条件充分时也可以形成不同国家；不得为追求沿河边界而制造无中心飞地、单 cell 长条、断裂行政区或不可达领土。
   - **锁、确定性与兼容**：冻结国家 / 省份、锁定中心、受保护对象和用户已确认边界优先于河流偏好；同 seed、salt、地图 revision 与锁状态必须得到同一结果。没有大河、只有弱河或合法行政结构无法沿河形成时，确定性回退到既有综合划界语义，不得拒绝整次生成。Worker 与兼容降级、撤销 / 重做、取消、迟到结果和失败回滚均须保持原子，普通用户不新增必须理解的技术开关。
   - **结构化诊断**：生成结果记录大河候选、阻隔分数、沿河边界长度、跨河行政邻接、因中心 / 道路 / 港口而允许的跨河走廊以及国家 / 省份层级的实际采用原因；诊断只进入开发模式、内部日志或结构化 API 结果，不向普通地图标签泄漏算法术语。河流只作为输入证据，不得因行政结果反写河流属性。
   - **最小验收**：纯数据夹具至少覆盖强连续大河、同几何弱河、短支流、多条汇流、合法渡河走廊、单侧无可用中心、锁定行政区和无河地图。对称且两岸均有合法中心的强河夹具中，至少 `70%` 的合格主河段成为国家或省份边界，并能形成两侧独立行政区；匹配的弱河夹具不得仅因河流而强制分国或产生碎片。固定 10k / 50k / 100k 新图及国家 / 省份重生成锁定边界河段、跨河走廊、行政连通、中心归属、未分配 cell、飞地 / 长条退化和确定性 checksum；锁定对象零改写。真实浏览器覆盖新图、省份重生成、国家重生成、撤销 / 重做、取消、Worker 兼容降级与故障回滚，确认河流 / 道路 / picking / GPU 同源，application / page / active health / WebGL error 为 `0`。
   - **完成记录**：新增共享 canonical 河障模型，以流量、河宽、Strahler 等级、连续长度和河段数形成可审计软成本；道路、港口、行政中心和聚落只提供有下限的折减。新图、国家 / 省份全图重生成及单国局部重分省复用同一模型，锁定对象优先；顺序连通修复避免飞地，孤立聚落不再跨 neutral 环瞬移认领，旧图载入、普通渲染、手工边界和河网数据均不被隐式改写。
   - **验收记录**：强河对称双中心采用率 `100%`，弱河、短支流、汇流、道路 / 港口走廊、单侧中心、锁与无河反例通过。正式 `10k / 50k / 100k` 新图与国家 / 省份重生成逐阶段精确锁定 topology checksum，断裂、中心错位和未分配 cell 均为 `0`；10k 另覆盖撤销 / 重做、局部分省、fallback、accepted 后取消和 after-render 故障回滚。河流 / 道路 / GPU / picking 同源，非性能 health、console、page、WebGL 与 Loading 错误为 `0`。100k 国家重生成的偶发 `59～69ms` 单条信号经窄诊断未发现同步热点，只对此入口登记 `≤200ms`，最终观察使本轮为 `69ms`，其余操作 LongTask `0`。独立集成复核和最终观察使均给出 `ACCEPT`；首页版本随复杂功能提交升至 `0.2.0`。

- **权威任务第 329 项：收敛已登记 LongTask 性能债与过宽回归额度。** `已完成；来源：用户要求重新审查此前登记放行的 LongTask，判断是否仍需优化并给出方案`
   - **范围**：复核第 322 项 C1、C2、C3c、D2、阶段 E routes，以及第 323、324、328 项现存 LongTask 登记；已消失的历史信号恢复严格零门，仍保留的例外精确限定入口、数量、时长与来源。只对 C3c 的当前可复现热点实施一次诊断、一次实质优化和一次目标复验，不改生成结果、地图数据、存档、公开 API、用户地图、Wiki 或 `source/`。
   - **完成记录**：C3c 八个入口首次分窗锁定路线预检 `109ms`、路线修改 `130ms`、城市预检 `106ms`、城市移动 `192ms`，共同归因于 Worker 返回后主线程同步重算路线来源指纹。正式校验改为同 checksum、可让步的异步指纹后，路线预检 / 修改 / 撤销 / 重做和城市预检 / 撤销 / 重做均恢复 LongTask `0`；城市移动最终仅保留最多 `1` 条 `self≤100ms`。C1、D2、100k routes 和第 324 项当前入口均为零，旧宽额度已删除；第 323 项只保留 `generate-capped 1×self≤80ms`，第 328 项只保留 `fallback-provinces` 与 `100k states` 各 `1×self≤80ms`。
   - **验收记录**：Node 的 10k / 100k 指纹同源、实际让步、陈旧 / binding 拒绝、Worker / fallback 与历史往返通过；生产构建通过。独立观察使亲跑 C3c、第 323 项和第 328 项真实 Chrome：城市移动为 `89ms`，100k 截断新图为 `52ms`，小图省份 fallback 为 `74ms`，100k 国家重生成为 `65ms`，均在精确单入口额度内，其余对应入口 LongTask 为 `0`；数据、历史、回滚、拓扑、GPU / picking、Loading、health、console、page 与 WebGL 门均通过。独立集成复核关闭最后一个隐藏的 100k fresh routes `2×75ms` 宽门后给出 `ACCEPT`，最终观察使同样给出 `ACCEPT`。

- **权威任务第 330 项：统一优雅 Loading 与编辑面板用户文案。** `已完成；来源：用户发现部分加载文案重新变得生硬，并要求遍历编辑面板文字描述后系统优化`
   - **目标**：系统盘点启动页、地图生成 / 替换、共享 runtime operation、十一类重生成、导入导出与存档、云端操作、面板懒加载和面板内异步状态；同时遍历正式 `28` 个 Vue 面板及对应适配层的普通用户文案。等待反馈要清楚、克制、自然，编辑说明要从用户任务和结果出发，不直接暴露内部实现。
   - **阶段 A——清单与词表**：建立可审计文案矩阵，逐条记录位置、用户情境、当前文字、问题类型、建议文字和普通 / 调试可见性。普通界面禁止无解释地出现 `cell / grid / pack / feature / Worker / buffer / revision / checksum / 镜像 / 补丁 / 派生 / 运行时 / 同事务` 等内部词；开发模式、结构化 API 诊断和内部日志可保留技术原文。沿用既有“资源点 / 通用标记、地貌单元、测量 / 测量对象”等权威业务词表。
   - **阶段 B——Loading 分层治理**：快速操作不得闪烁；共享操作继续由 token 化延迟反馈处理取消、失败、重叠和迟到完成。只有用户能理解且确实不同的阶段才更新文案，生成规模、消息包、Worker、渲染安装和缓存后端不得进入普通提示。面板首次加载也应避免短暂闪字，超过小延迟后才显示“正在打开……，请稍候片刻”一类稳定提示；加载失败仍保留可操作恢复态。
   - **阶段 C——面板文案分批改写**：优先处理普通模式中已经确认的 `目标 cell / pack cells / grid cells / feature # / 中心 pack cell / 同事务联动 / 只读预检 / 重算经济链` 等生硬表达；统一按钮为“动作 + 对象 / 结果”，预览说明明确尚未写入，危险动作说明影响和撤销能力，空态给出下一步。调试专属统计、ID 和底层字段不伪装成普通业务词，也不强行删除。
   - **最小验收**：静态矩阵覆盖全部 Loading 来源、`28 / 28` Vue 面板及对应加载适配层；共享文案专项拒绝普通界面技术词回流。隔离系统 Chrome 验证快速 / 慢速、成功 / 失败、取消、重叠、迟到完成和面板 chunk 故障；普通模式依次打开全部面板，检查文案、空态、预览、危险动作和禁用原因，并在 `1280px` 与窄视口抽检无裁切 / 溢出。真实浏览器中 Loading、普通文案、console、page、非性能 health、WebGL 错误为 `0`，业务行为、历史和数据保持不变。
   - **非目标**：不修改 API / schema / 存档格式、生成算法、地图数据、编辑事务、面板信息架构或视觉主题；不把调试信息全部隐藏，不改 `source/`、Wiki 或用户现有地图。优先复用现有文案模块、懒加载 harness 和 UI 审计工具，不新建超过 `500` 行的浏览器夹具。详细盘点见 [`task-notes/task-330-loading-and-panel-copy-polish.md`](./task-notes/task-330-loading-and-panel-copy-polish.md)。

- **权威任务第 331 项：收敛 100k 重生成与存档耗时，并让 Loading 与真实阶段同源。** `已完成；来源：用户要求分析并优化 100k 重生成、浏览器保存耗时及普通 Loading 文案`
   - **完成记录**：普通 Loading 改为与真实工作同源的“收拢全图资料、压制存档体积、整理存档内容、妥存至浏览器”及十一类自然中文重生成阶段，内部输入完成不再提前宣告保存成功。live map 只做一次权威 JSON 序列化，gzip 直接消费同一文本；大于 `4 MiB` 的 gzip 以版本化 `Uint8Array` 直接写入 IndexedDB，不再 base64、信封 stringify 或先试 LocalStorage，旧 plain / gzip-base64、LocalStorage / IndexedDB、`.webfmg`、JSON 与旧 schema 继续读取。
   - **性能记录**：存档导出复用 `regeneration.compute` 持久 map-mirror session，固定 `99846` cells 首次保存约 `10.54s / 953` 输入包，随后两次为 `3.61 / 3.67s` 且各 `3` 包；相比优化前 `16.3～18.8s` 明显下降。对象 picking 按十一类领域真实写集局部重绑，未变化对象族保持正式引用，缺基线或 generation 自动走完整准备；100k routes render install 为约 `1.53s`、picking rebind `1.28s`，产品 LongTask 为 `0`，point / labels / overlay / GPU 语义未删减。
   - **验收记录**：专项 Node 覆盖单次序列化、压缩同源、二进制存储、map-mirror delta `0`、陈旧 / binding 拒绝、五类 picking 组件、空 bucket、统计 / 顺序 / 引用与精确 rollback；通用十一类 Worker/session/packet/ack/cancel、prepared transaction 与生产构建通过。独立观察使亲跑 10k routes、100k 三次保存 / 恢复和旧 v2 gzip-base64，单历史、undo / redo、checksum / seed / cells、Loading、health、console、page、WebGL 与残留清理通过；首页版本 `0.2.7`。独立集成复核和最终观察使均 `ACCEPT`，`source/`、Wiki 和用户地图未改。

- **权威任务第 332 项：收敛 100k 地图视图切换卡死。** `已完成；来源：用户补充报告大型地图切换视图也会卡死`
   - **完成记录**：持久 `render.prepare` / render-only Worker session 现按 `mapIdentity + mapRevision` 复用 cell visual、shore、state path 和 province path 四类只读几何缓存；revision 变化、领域写任务、换图或 session 失效会清除，政治 mesh 仍按当前主题 / debug 请求重建。失败的后台预热已撤销，不把冷启动成本隐藏到新图完成之后。视图切换继续使用独立 map-mirror、no-fallback、delta `0` 与 prepared renderer 原子事务，图层、政治 mesh、标签、hover 和 picking 未删减。
   - **性能记录**：独立观察使固定 `10004 / 99846` cells 运行 `height → states → provinces → biomes → population → height`、主题与关键视图选项。10k 首次 states 为 `2947.2ms / 210` 输入包，后续同 session 每项 `3` 包、`180.4～435.8ms`，LongTask `0`；100k 首次 states 为 `15076.3ms / 1013` 包，后续每项 `3` 包、`513.3～1635.6ms`。100k 仅主题切换出现 `64 / 52ms` 两条 `self`，符合经一次调查和一次缓存实质优化后登记的单入口 `≤130ms` 边界；标签与其它视图入口为 `0`。
   - **验收记录**：13 个真实动作各有独立 operation ID 与精确入口名，session / cache 命中及最终 idle 同源；两档 map / renderer identity、revision、checksum、history、camera、selection、highlight、picking 均不变，Loading、错误级 health、application console、page 和 WebGL 错误为 `0`。渲染准备专项、Worker 十一类 / session / cancel / deferred renderer、生产构建、独立集成复核和最终观察使均 `ACCEPT`；首页版本 `0.2.9`，`source/`、Wiki、用户 Chrome 与用户地图未改。
