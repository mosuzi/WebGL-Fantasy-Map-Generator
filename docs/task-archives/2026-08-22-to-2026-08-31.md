# 2026-08-22 至 2026-08-31 权威任务归档

本卷保存 本时间片内完成的权威任务 的历史权威任务正文。当前可执行任务仍以 [当前开发计划](../current-plan.md) 为唯一权威来源。

- 归档条目数：15
- 排序方式：按权威任务编号升序
- 状态边界：只收录已完成、已移除或已由后续方案取代的条目；未完成与暂缓项不得进入归档。

## 任务条目

- **权威任务第 350 项：MapCoreEngine / TypeScript 集成重新基线与 CDP 全量验收。** `已完成；R6b 20/20、R7 5/5 与任务级终验通过`
  - **目标与边界**：保留第 349 项渐进式 MapCoreEngine / TypeScript 架构，不推倒重写；以 16 项共同不变量和 17 类典型场景重新审计既有实现，先整体修补、再冻结夹具、最后按固定顺序执行真实浏览器验收。主线程始终是唯一写者，产品 blocker 与夹具 blocker 分阶段处理；`source/`、用户地图与 `main` 未改，任务分支不得自行合入 `main`。
  - **R0～R1 契约基线**：重新冻结 `350-R0 → R1 → R2a → R2b → R3a → R3b → R4a → R4b → R5a → R5b → R6a → R6b → R7` 唯一顺序；机器目录覆盖 `16 invariants / 17 scenarios / 20 fixed entries / 4 profile owners`，逐项 owner stage、依赖和浏览器前置均有删除或漂移反例。
  - **R2 核心集成**：MapCoreEngine、DomainModuleManifest、全图 / 分域 Worker、持久 session、CommitEnvelope、map adoption binding 与渐进 TypeScript 契约完成统一；source binding 与完整 render resource binding 分离，缺字段、旧 revision、伪造 preparation / generation、越界 patch 和非声明写集均在提交前拒绝。
  - **R3 事务与所有权**：唯一 map / revision / history owner、latest-issued preparation、full / in-place surface commit、迟到 rollback、换图 rollback、context staged restore 与 async refresh 原子交接闭合；surface、六族 render cache、picking、label、overlay 的 owner / wrapper / physical reference 在任何写入前严格 preflight，失败保持 before-image。
  - **R4 渲染与上下文**：正式绘制资源 owner、partial/full topology、surface-only retained commit、viewport/presentation 与 WebGL context-loss 恢复完成统一；实验性 `debug.simulateContextLoss` 只用于验收，成功固定同图、source/topology 不变、render generation `+1`、唯一 draw、全部 owner 同源且资源回到 ready。
  - **R5 持久化与反馈**：旧档迁移、binary browser storage、gzip/fallback、地图命名、导入/导出、故障回滚、Loading 单一来源、延迟操作反馈与普通 UI 文案边界闭合；普通界面不泄漏 Worker、线程、buffer、结构化克隆或存储后端等内部术语。
  - **R6a 夹具冻结**：20 个固定入口映射到 19 个唯一浏览器夹具；failure-safe full/summary artifact、teardown 首败分栏、200ms LongTask 原始数据与 setup/product 分窗均被冻结。最终静态契约为 R4b `29` mutations、artifact `222 hard assertions / 381 mutations / 5 partitions`，fixture freeze `19` 项。
  - **R6b 浏览器全量**：固定入口 `20 / 20` 最终全部 PASS，覆盖 10k 核心事务、10k/100k Worker session、10k→100k topology、直接/复合锁、城市拾取、overlay/viewport、heightmap/PNG export、v2 存档、binary fallback、保存反馈、故障回滚与延迟反馈。期间按首败规则分别修复真实 owner/binding、military-policy 写集、全锁快照、heightmap Worker 编码、rollback prepared restore 和同步刷新路径；纯夹具问题均单列修正，没有通过放宽产品断言或 200ms 阈值换取通过。
  - **R7 冻结树终验**：同一最终树依次通过固定 10k transaction、代表性 100k session、v2 旧档、稳定故障恢复和 S-17 context restore `5 / 5`。10k 隔离复验的 zones undo/redo/baseline 最长为 `162 / 149 / 168ms`；100k session 最大 LongTask `70ms`；旧档与故障恢复目标 LongTask `0`；S-17 为 `10004` cells、sameMapRef、generation `+1`、draw delta `1`、owners/state/pick exact，LongTask/application/page/health/GL/loading 全 `0`。
  - **最终证据**：最终聚合再次通过 `16 / 17 / 20`、19 fixtures、typecheck 与 `1401 modules` production build；逐文件读取 R6b `20 / 20` 与 R7 `5 / 5` 共 `25` 组正式 artifact，full/summary 全存在且所有 summary `ok=true`。完整阶段、blocker、数值和 artifact 路由见 [第 350 项集成重新基线交接](../task-notes/task-350-engine-integration-rebaseline-handoffs.md) 与 [验收目录](../../tools/task-350-acceptance-contract-catalog.mjs)。
  - **独立验收与交付边界**：最终任务级复核先因 AGENTS 状态滞后给出 `BLOCK / P0 0 / P1 1 / P2 0`，同步默认接手入口后 blocker-only 复审为 `ACCEPT / P0 0 / P1 0 / P2 0 / browser 0`，明确允许归档。完成版本为 `0.5.58`；分支 `codex/map-core-engine-architecture-plan` 以 `origin/main` / 本地 `main` 为祖先且 HEAD 未合 main，混合工作树未暂存、提交、推送或合并。

- **权威任务第 351 项：修复 100k 视图切换失败并完成全视图 GPU 快切与后台预热** `已完成；本地 10k / 100k 与用户精确预览标签页终验通过`

  - **来源与现场**：用户在精确 Chrome 标签页 `https://preview-fmg.mosuzi.top/` 的 100k 当前地图实测：高度 / 生物群系 / 人口 / 国家 / 省份五类视图首次切换仍出现约 `2～3s` Loading，其余七类视图不能正式切换。只读现场确认最终仍回滚到 `height`，health 两次记录 `operation-failed：surface 颜色补丁与正式资源 owner 不属于同一受控 revision`，并存在 `2099.9 / 2264.7 / 2488.5ms` frame gap。
  - **最终目标**：修复 deferred display 把原位 `cell-colors` patch 错发为 replacement generation 的产品错误；十二类颜色视图全部通过 GPU 常驻 attribute / palette 快切；地图装载或正式数据提交后，以最新 `mapIdentity + sourceRevision + topologyRevision + renderGeneration` 为键在独立、可丢弃的 ComputeWorker 中低优先级预热仍需几何计算的共享缓存。新 revision 到来时清空未开始队列、协作取消或拒绝正在运行的旧结果，只为最新版本重算，不占用或终止长期 MapWorker。
  - **硬不变量**：不得放宽 surface owner、binding、checksum、revision、topology 或 WebGL context 校验；不得让 Worker 直接接管正式 WebGL context；后台预热不得阻塞 map-ready、显示普通 Loading、重建 picking / overlay 或静默安装陈旧结果；用户前台显示、编辑、撤销 / 重做、生成、保存和导入优先于后台任务。
  - **351-0——产品阻断窄修**：在决定 `replaceResources` 和发放 render binding 之前确定 deferred `surfacePatchScope`；原位颜色补丁保持同一 render generation，完整 surface replacement 才推进 generation。补真实 `display → cell-colors → prepared installer` 正例及 identity / revision / topology / generation 负例。
  - **351-1——十二类 GPU 常驻快切**：保留现有高度 / 生物群系 / 人口 / 国家 / 省份路径；温度 / 降水读取 numeric texture，文化 / 宗教读取既有 identity + palette，区域使用整数 identity + palette，政体使用 state→government palette，外交使用当前 subject 对应的 state relation palette。普通颜色切换必须为 Worker input `0`、`render.prepare=0`、surface geometry rebuild `0`、picking rebuild `0`。
  - **351-2——最新 revision 后台预热**：只预热岸线 correction、政治路径等仍需 CPU 几何的共享资源，不生成十二套完整 surface。队列单并发、前台抢占、尾随防抖；queued 旧项立即丢弃，running 旧项在安全 checkpoint 协作取消，无法即时中断时只允许算完后拒绝安装，不得终止并丢失长期 MapWorker 副本。
  - **351-3——分级终验**：静态与专项 Node 通过后，依次运行小数据真实入口、固定 100k、快速 A→B→C、数据 revision 前进、撤销 / 重做、换图、Worker 重启与 context restore。最终必须在用户精确预览标签页对应正式构建上确认十二类逐项成功、控件 / renderer / 像素 / picking 同源、Loading / application / page / health error / WebGL error 为 `0`。
  - **当前证据**：专项 scheduler 证明 running 旧 revision 被 abort、旧结果安装数 `0`、仅最新 revision 接纳，queued 前台抢占不启动；固定 `10004 / 99846` cells 的十二类 cold 分别为 `13.0～26.2ms / 15.5～29.1ms`，warm 为 `12.7～16.3ms / 15.4～19.2ms`。每项 Worker input/output、surface/line/picking/labels rebuild、LongTask、application/page/health/WebGL error 与 Loading 残留均为 `0`。用户精确 Chrome 标签页 `294256474` 刷新后恢复原地图，十二类视图逐项成功，人口与高度画面可见，最终回到 `height / startup ready / Loading 0`；以目标复验开始时间为界，高度→温度→高度新增 error / warn 为 `0`。
  - **性能门**：固定 100k 十二类普通颜色视图 cold `≤150ms`、warm `≤50ms`；单个主线程任务 `<50ms`、LongTask `0`。若现有硬件真实 draw 下限证明 cold `150ms` 不可达，首败即停并提交分段证据，不得先放宽阈值。后台预热不计入 map-ready，前台请求须在一个协作 checkpoint 内取得优先权。
  - **非目标**：不修改地图生成算法、存档 schema、地图 canonical 数据、对象数量、视觉配色语义、`source/`、Wiki 或用户当前地图；不以删除平滑边界、图层、标签、picking、回滚或严格 owner 门换取性能。
  - **阶段与证据**：详细冻结矩阵、根因、文件边界、门禁与 checkpoint 记录见 [第 351 项全视图快切交接](../task-notes/task-351-view-switch-repair.md)。主线程为唯一写者，不启用四级流程和子智能体。

- **权威任务第 353 项：正式保存性能与路线锁定优先重生成。** `已完成；来源：用户反馈正式保存仍慢，且路线重生成错误地以“锁定内容冲突”整批拒绝`
  - **目标与锁定语义**：以用户提供的 `krichars (3).webfmg` 为只读代表存档，复现正式“重新生成路线”的实际拒绝并追到具体锁对象、依赖和校验阶段。锁定对象优先级高于主动路线重生成：锁定路线及其必要端点、路径和派生镜像必须作为不可覆盖约束预先种入，新路线只使用剩余空间；旧派生关系失效时在事务内修复，不得用笼统 `regeneration_lock_conflict` 要求用户解除合法锁。多个锁之间需要竞争同一资源时采用稳定、可解释且不丢锁对象的保留策略；只有输入本身已损坏到无法表达锁对象时才允许安全拒绝，并必须返回精确对象、字段和原因。
  - **保存性能目标**：测量正式压缩保存从地图归一化、Worker 输入、v3 编码、gzip、结果回传到下载就绪的分段墙钟，消除已证重复序列化、复制或无效重算。优化后同一构建、浏览器和存档的正式保存端到端中位数必须明确改善，已证主瓶颈至少下降 `20%`，不得用降低压缩级别、跳过校验、丢字段或改成非正式入口换取成绩。
  - **最小验收**：用户存档的路线重生成在存在合法锁定路线 / 锁定城镇及旧派生关系时成功提交，锁定对象核心身份、端点和路径保持，未锁路线从空结果重建；重复边、交叉依赖和失效港口 / 城镇镜像均有确定性处理，失败与撤销保持事务原子。保存前后内存地图、checksum、对象数量、TypedArray、稠密身份槽和历史不变；新压缩文件经正式 UI 回读恢复同一地图和柔化样式，旧 JSON / gzip / v3 兼容回归通过。专项 Node、正式构建和真实浏览器路线重生成 / 保存性能各跑一次代表性终验，新增 LongTask 不得超过 `200ms`。
  - **重构分支集成边界**：只移植第 353 项产品、专项与文档变更，不带入第 352 项或旧架构方案提交；保留 MapCoreEngine 的 owner、binding、revision、后台预热和显示快切约束。集成版本为 `0.5.62`，目标分支只推送自身，不合入 `main`。

- **权威任务第 354 项：全部重新生成入口落实锁定最高优先级。** `已完成；来源：用户要求道路之外的一切重新生成都必须强制执行，不能被合法锁定内容阻塞`
  - **统一契约**：十一类正式重生成和高度、气候下游、洋流世界、海底等复合重建统一把可读取的显式锁及其必要支撑闭包作为不可覆盖约束。锁定国家可带入省份、城镇、外交与军事；锁定 Feature 可带入相关城镇、路线和标记；经济端点、首都 / 省会、Warzone 外交对、河流关联 Feature 等依赖均在生成前闭包。目标领域全部锁定时返回成功 no-op；只有对象缺失、越界、镜像损坏或结构本身不可解析时才精确拒绝。
  - **生成与提交修复**：移除国家重生成对合法锁定外交的主动冲突；军事跳过锁定国家；政治扩张、城镇落位、省会重选和 Feature 拓扑在剩余空间生成并保留受保护所有者。Worker 与主线程复合入口在生成前捕获完整约束、补丁提交后恢复显式社会对象快照，再以稳定键断言锁定内容未变；补齐复合入口 picking 写集和洋流故障注入，使正常提交、no-op、故障回滚与撤销 / 重做共用同一安全边界。
  - **矩阵与浏览器验收**：入口盘点覆盖 `14` 个面板、`15` 类锁、`22` 个重生成入口，差异为 `0`。十一类正式入口 × 十五类锁共 `165` 组：`164` 组成功执行并保持锁快照，唯一全锁标记场景成功 no-op，失败 `0`。原修复分支真实浏览器完成国家 / 外交 / 军事直接入口、高度派生、气候下游、洋流世界、海底复合重建、全锁 no-op 与 `after:rivers` 故障回滚；应用错误、页面错误、WebGL 错误与 `regeneration_lock_conflict` 均为 `0`。详细证据见 [`task-notes/task-354-regeneration-lock-priority.md`](../task-notes/task-354-regeneration-lock-priority.md)。
  - **重构分支集成边界**：只移植第 354 项产品、专项和浏览器夹具，不带入第 352 项或旧架构方案提交；保留 MapCoreEngine 的 owner、binding、revision、提交前校验、后台预热和显示快切约束。集成版本为 `0.5.63`，目标分支只推送自身，不合入 `main`。

- **权威任务第 355 项：调试模式显示重新生成错误码。** `已完成；普通模式文案边界与真实浏览器失败态通过`
  - **目标与边界**：控制面板重新生成失败时，普通模式继续只显示既有友好文案；显式 `debug=1` 或已启用开发模式时，在同一失败反馈中显示公开错误码。通用 `operation_failed` 包装会把原错误的结构化 `code` 保留到 `details.internalCode`，因此调试模式还能显示被包装前的内部码；地图数据、存档 schema、生成算法、渲染资源和公开 API 方法集合均未改变。
  - **文案与运行时契约**：调试失败格式固定为 `重设失败（错误码：<公开码>；内部码：<内部码链>）：<底层消息>`；没有内部码时只显示公开码。内部码去重且不重复公开码。普通模式仍只按公开码映射用户文案，专项反例确认 `operation_failed`、`render-install-shape` 等标识不会泄漏。
  - **门禁与真实浏览器**：`regress:regeneration-user-copy`、`regress:api-operation`、UI 文案审计与 `1403 modules` 生产构建通过。真实本地页面对“重新生成城镇”制造并发冲突：调试模式可见 `重设失败（错误码：operation_busy）：当前正在执行 generate.regenerate，不能同时启动 generate.regenerate`；普通模式同一失败仅显示“当前还有操作正在进行，请稍后再试。”且错误码泄漏为 `0`。两边先发操作随后正常完成，新增应用错误均为 `0`。
  - **交付边界**：任务分支为 `codex/task-355-regeneration-debug-error-code`，完成版本为 `0.5.64`；本地 checkpoint 合入 `main` 后再从最新主线建立下一任务分支，远端推送留待集成完成后统一执行。

- **权威任务第 356 项：修复地图加载迟缓、首帧边界虚化与道路重生成失败。** `已完成；代表性旧存档与真实浏览器终验通过`
  - **目标与边界**：在代表性 `100k grid / 43,419 pack` 旧存档上分别取得加载和道路重生成的正式首败；合法锁定路线、城镇与必要端点必须保留，失败与撤销保持 before-image；加载消除已证重复传输和首帧错误柔化，不修改 `source/`、存档 schema、公开 API 方法集合或视觉主题。
  - **道路修复**：调试模式确认首败为 `operation_failed / settlement-politics-mirror-invalid`。路线 Worker 在锁快照冻结后，以当前 pack 政治数据收敛旧 `politics / pack` 镜像，再执行原有 owner、revision、锁 envelope 和提交前校验；正式重生成 `442 → 83`，锁定路线 `#0` 的类型、端点、`65` 段路径和 `1,388.1 km` 长度保持，单历史提交撤销恢复 `442 / 7,976`。
  - **加载与首帧修复**：生成加载使用 `runtime-compact` cell visual 传输，省略可从已提交 mesh 确定性恢复的逐 cell 边界点、edge curve、shore edge 和稠密 fallback；边界自交检查增加 AABB 早拒绝。三次导入墙钟 `16,026.5 / 15,878 / 15,975ms`，中位数由 `20,300ms` 降至 `15,975ms`（`-21.3%`），Worker 传输下降 `30.2%`、解包下降 `69.48%`；首个正式帧与 `1,200ms` 后稳定帧 PNG 逐字节一致。
  - **最终验收**：路线身份、路线锁、城镇路线锁、网络协议、渲染准备、prepared installer、调试文案、API operation、`typecheck:core`、`git diff --check` 与 `1403 modules` 构建通过。正式导入目标窗口新增产品 `>200ms` LongTask、页面错误和 WebGL error 均为 `0`；岸线全量浏览器门暴露既有 10k smooth 覆盖阈值首败，按连续夹具失败规则冻结，未在本项放宽算法或阈值。完成版本为 `0.5.65`。

- **权威任务第 357 项：统一全部重生成入口的重算、锁定加入与单次合并契约** `已完成；22 个入口与真实浏览器终验通过`

  - **来源**：用户要求把道路之外的全部重新生成逻辑按同一语义调查并修复：先按各领域既有限制重算全部未锁元素，再加入已经锁定的元素且锁定元素不参与限制验证计算，最后合并且不重复执行生成限制；调整完成后需自行启动本地开发服务器，在真实浏览器完成验收并提交推送。
  - **统一契约**：事务内先把旧镜像、旧数组、旧计数器和必要派生身份收敛到只属于本次事务的 canonical working copy；随后捕获显式锁完整快照与最小结构闭包。锁对象只校验稳定 ID、边界、唯一身份和必要引用可读性，不验证地理落点、中心重叠、数量、评分、平衡或新生成器偏好；它们只向生成器提供占位、边 / 端点保留和固定 owner。未锁对象按现有领域限制从空重算；合并时锁快照绝对优先、生成 ID 避开保留空间、镜像一次重建。最终只执行一次领域结构 / 引用 / 镜像 / 锁快照一致性校验，既有 MapCoreEngine owner、binding、revision、写集、历史、取消、过期和回滚安全门不放宽。
  - **调查基线**：覆盖 `14` 个面板、`15` 类锁和 `22` 个入口：正式 `generate.regenerate` 十一类、复合高度 / 气候 / 世界洋流 / 海底六类，以及洋流、文化、宗教、市场归属和经济链五类直接入口。代表性 `100k grid / 43,419 pack` 旧存档上，正式入口 `5 / 11` 通过、复合入口 `1 / 6` 通过、直接入口 `3 / 5` 通过；已证 Feature / 标记镜像、政治镜像、旧省份闭包、缺首都锚点、外交 salt、军事旧 shape、标记锁合并改写、社会中心水域 / 重叠重复限制，以及经济旧数组回滚二次异常。
  - **阶段顺序**：`357-A 旧数据 canonical working copy 与原子回滚 → 357-B 十一类正式重生成统一锁 / 生成 / 合并 → 357-C 六类复合与五类直接入口闭合 → 357-D 22 入口旧存档、撤销与本地真实浏览器终验`。每阶段冻结唯一写者、精确文件边界和一个廉价门；夹具连续两次失败或同一阻断再次出现时按项目规则冻结。
  - **最小验收**：入口盘点差异 `0`；干净图 `11 × 15 = 165` 锁矩阵保持全通过；代表性旧存档的全部 `22` 个入口分别成功执行或在目标领域全部锁定时成功 no-op，锁定 before-image 逐项不变，未锁对象按原限制重建，最终镜像共享 / 等价，失败注入和撤销 / 重做恢复完整 before-image。经济旧普通数组可成功改派与重算，失败回滚不遮蔽原错误。真实本地页面逐项触发正式入口，调试错误、应用 / 页面 / WebGL error 为 `0`，目标窗口不得新增 `>200ms` 产品 LongTask。
  - **非目标**：不修改 `source/`、对象数量算法、地理评分权重、美术主题、公开 API 方法集合或存档 schema；不删除 owner、revision、binding、结构安全、提交前锁快照、故障回滚或撤销门。
  - **实施边界**：任务分支 `codex/task-357-unified-regeneration-contract`，主线程唯一写者；代表存档只读，临时报告放入 `Z:\tmp\codex\2026-08-26\task-357-unified-regeneration-contract`。阶段冻结、首败与验收矩阵见 [`task-notes/task-357-unified-regeneration-contract.md`](./task-notes/task-357-unified-regeneration-contract.md)。
  - **完成记录**：新增事务内 canonical working copy，统一旧政治、标记、社会、地区和经济镜像，补齐旧国家省份 / 军事 shape、外交 salt、失效市场中心与普通数组回滚；锁定 Feature、河流、国家 / 省份、文化 / 宗教、外交、军事、地区、市场 / 交易与洋流只做结构可读校验，不再参加水陆、中心、扩张、驻地、关系对称、市场连通、数量或平衡等生成限制。代表性 `100k grid / 43,419 pack` 存档正式十一类 `11 / 11`、复合六类 `6 / 6`、直接五类 `5 / 5` 通过，同类锁补测 `22 / 22`；干净图锁矩阵 `165` 组为 `164` 执行、`1` no-op、失败 `0`。复合 Worker、typecheck、`1404 modules` 生产构建与差异门通过。真实本地调试页面路线 `296 → 300`，目标操作窗口 health、console error / warn 与 WebGL error 均为 `0`；首个正式可见帧和 `1,200ms` 后稳定帧 PNG 同为 `94,950 bytes`、SHA-256 `d0c3221d5e2b822916a461bba81c9c5c0cf09897157f1bfe9a8ca0d6cbcfcf05`。开发模式失败反馈专项确认公开码与内部码链保留，普通模式错误码泄漏为 `0`。完成版本为 `0.5.66`。

- **权威任务第 358 项：真实存档加载、道路重生成与首帧边界失效复盘修复。** `已完成；唯一使用用户指定真实存档完成本地正式浏览器终验`
  - **来源与验收边界**：第 357 项最终浏览器误用随机 10k 地图，不能证明用户指定存档的问题已解决。第 358 项将 `C:\Users\mosuzi\Downloads\krichars (3).webfmg` 冻结为唯一正式验收输入；文件 `9,642,587 bytes`、SHA-256 `CF7402BC2BEA22AD1FCDE441444479F880DC0DB15D55520EF5A1A399D335DA61`，回读身份为 `100000 grid / 43419 pack / 1251 cities / 442 routes / 7976 segments`。随机图和小夹具只用于廉价专项，不再作为完成证据。
  - **加载修复**：导入 Worker 不再把已解析正式文档重复 encode→decode，只编码一次 adoption handoff 并直接投影 parsed document。首次渲染交接改为 boundary-only cell visual、当前岸线 resident 顶点；完整编辑期 cell mesh 和另一岸线模式仅在实际需要 surface 重建或用户切换时惰性建立。真实存档传输 `123,573,771 → 71,422,775 bytes`（`-42.2%`），本地正式页面导入 `21,757 → 16,805ms`（`-22.76%`），目标窗口无新增 `>200ms` 产品 LongTask，WebGL error `0`。
  - **重生成竞态与错误码**：runtime operation busy 状态同步到控制面板，中央十一类和道路、河流、国家、外交、军事、地区、资源点、洋流等专用重生成入口统一拒绝在加载事务内并发启动，并保留结构化 `operation_busy` 供调试模式显示。加载 `1,527ms` 时路线按钮 disabled；加载完成后路线 `442 → 273`、`3,039ms`，撤销恢复 `442 / 7976`，历史 `undo 0 / redo 1`，WebGL error `0`。第 357 项的 `11 / 11 + 6 / 6 + 5 / 5` 与干净锁矩阵 `164 executed / 1 no-op / 0 failure` 仍作为全部入口生成语义门。
  - **首次边界修复**：原 renderer 无条件绘制 edge fade，导致用户无法把“边缘渐隐”和“平滑边界”分开控制。现新增独立持久化开关并把首次默认设为关闭，切换只刷新线视图。全新 origin 与真实存档均为平滑开启、边缘渐隐关闭；首个正式帧和 `1,200ms` 后稳定帧均 `74,054 bytes` 且逐字节相同。右缘关闭 / 开启 / 恢复为 `2,468 / 2,635 / 2,468 bytes`，恢复与初始关闭画面逐字节相同。
  - **最终门禁与交付**：map file Worker、render preparation、prepared installer、重生成调试文案专项，`typecheck:core`、`1404 modules` production build 与 `git diff --check` 通过。地图 schema、公开 API 方法集合、生成数量算法、视觉主题和 `source/` 未改；完成版本为 `0.5.67`，详细证据见 [`task-notes/task-358-real-archive-regression.md`](../task-notes/task-358-real-archive-regression.md)。

- **权威任务第 359 项：真实存档加载关键路径重构。** `已完成；绝对时限、身份、交互与后台稳定门通过`
  - **输入与根因**：唯一使用 `C:\Users\mosuzi\Downloads\krichars (3).webfmg`（`9,642,587 bytes`，SHA-256 `CF7402BC2BEA22AD1FCDE441444479F880DC0DB15D55520EF5A1A399D335DA61`）。第 358 项仍有 Worker 完整 handoff 再编码、主线程再解码、单 Worker 串行准备、picking 对象化回绑和首屏空面板刷新。
  - **实现**：主线程 canonical 解析与双 ComputeWorker 渲染准备并行，正式 v3 不再重编码 adoption handoff；旧格式兼容路径保留。首屏 surface 使用三字 geometry 与 GPU 属性纹理，picking 从已提交地图直接建立；后台预热绑定 map / revision 并可由下一次地图替换取消。地图接纳不再被重复通用 operation-stall 计量，LongTask 和 load-stage 阈值均未放宽；紧凑 surface 重复导入的失效 color attribute pointer 已修复。
  - **真实浏览器终验**：`v0.5.68` production preview 预热一次后五次独立冷导入墙钟 `4324 / 4274 / 4725 / 4970 / 5150ms`，中位 `4725ms`、最大 `5150ms`。五轮均为 `100000 / 43419 / 1251 / 442 / 7976`，导入窗口产品 `>200ms LongTask`、health error、console error / warn、WebGL error 均为 `0`；拾取城市、路线面板和完整存档导出成功。无遮挡 canvas 区域在 map-ready 与 `5s` 后像素哈希均为 `cada9b22`，连续导入取消前一延迟预热且没有晚到提交。
  - **门禁与交付**：typecheck、map-file Worker、whole-map profile protocol、render preparation、prepared installer、surface buffer、API operation 专项和 `1404 modules` production build 通过。详细证据见 [`task-notes/task-359-real-archive-load-critical-path.md`](../task-notes/task-359-real-archive-load-critical-path.md)；分支 `codex/task-359-real-archive-load-critical-path` 只推送自身，不合入 `main`。

- **权威任务第 360 项：道路专用重生成入口事务闭环。** `已完成；真实专用入口、锁、撤销与错误码通过`
  - **真实首败**：指定存档的道路核心能在约 `2.4s` 完成 `442 / 7976 → 273 / 3645`；专用 RoutePanel 却丢弃 Promise，操作期间按钮始终 enabled 且没有 pending / result。故障属于 UI 事务，不需要扩大修改路线生成器。
  - **实现**：RoutePanel 以本地 request identity 同步进入 pending，并订阅共享 runtime operation；按钮在本地或全局 busy 时禁用，成功显示前后路线 / 段数与撤销提示，失败复用统一普通 / 调试文案。runtime 原始结果与公共 API 响应归一为同一结构化语义，快速双击只启动一个 command。
  - **指定存档终验**：`v0.5.69` 专用和中央入口在无锁原档上均得到扰动 `#2`、`442 / 7976 → 273 / 3645` 和一条 history。正式 UI 锁定路线 `#0` 与端点城市 `#7 / #18` 后，专用入口得到 `83 / 1230`；路线及两城市 before-image 不变，history 只 `+1`，撤销恢复 `442 / 7976`，重做恢复 `83 / 1230`，console error / warn、health error、WebGL error 为 `0`。
  - **故障与门禁**：`operation_failed ← worker_regeneration_refresh_fault` 普通模式只显示友好失败，调试模式显示公开码和内部码。route edit / pending、user copy、route + city lock、typecheck、`1405 modules` build 与差异门通过。详细证据见 [`task-notes/task-360-route-regeneration-transaction.md`](../task-notes/task-360-route-regeneration-transaction.md)；分支 `codex/task-360-route-regeneration-transaction` 只推送自身，不合入 `main`。

- **权威任务第 361 项：首次边界不可逆第三态消除。** `已完成；真实存档两种初始状态、往返像素与显示指纹通过`
  - **归因与实现**：第 359 项并行加载改造已顺带让当前首个正式帧可往返，但 primary shore 与 secondary surface 仍缺少显示意图契约。现为 prepared bundle 固定携带 map / revision / topology / generation、color mode、海底显示、平滑开关、edge fade、主题和 shore surface key 指纹；并行合并与首次安装均在提交前校验，缺失、篡改或错配 fail closed。首屏 surface key 与可见状态只取自已验证指纹，开发摘要显示 `smooth / canonical` 或 `hard / canonical`。
  - **指定存档视觉门**：平滑初始 `F0 == F1200 == 两次关→开`，固定裁剪 SHA-256 均为 `e3c301367d5055cae49a273fa3d357fae0c1d037e858d25ef8eec37f19a5e19c`；硬边界初始 `F0 == F1200 == 开→关`，均为 `da59408a75759f4e4a5ffb06ce0c3a1982c31191a3ad60259c1aba4e560d9753`。两种状态互相对应且 edge fade 始终关闭，不存在第三组像素或资源指纹。
  - **性能与门禁**：唯一真实存档五轮冷导入为 `4733 / 5112 / 5063 / 4963 / 5447ms`，中位 `5063ms`、最大 `5447ms`；每轮身份均为 `100000 / 43419 / 1251 / 442 / 7976`、canonical true、WebGL error `0`，目标窗口无新增 console error / warn 或产品 LongTask。boundary presentation、render preparation、prepared installer、GPU display mutation、map-file Worker、typecheck、`1406 modules` build 与差异门通过；既有 shoreline 数量阈值首败未在本项放宽。详细证据见 [`task-notes/task-361-initial-boundary-third-state.md`](../task-notes/task-361-initial-boundary-third-state.md)；分支 `codex/task-361-initial-boundary-third-state` 只推送自身，不合入 `main`。

- **权威任务第 362 项：闭合第 359～361 项独立验收阻断并复测** `已完成；原三名验收者第三轮全部 ACCEPT`

  - **状态**：已完成；加载、道路、边界三份独立终验全部 `ACCEPT`。
  - **唯一输入**：`C:\Users\mosuzi\Downloads\krichars (3).webfmg`，SHA-256 `CF7402BC2BEA22AD1FCDE441444479F880DC0DB15D55520EF5A1A399D335DA61`；不得以随机图或旧报告替代。
  - **封闭范围**：
  1. 定位并消除真实存档 `map-ready` 后后台期出现的产品 `>200ms LongTask`，让开发态直接暴露当前 map identity / revision 与按 `200ms` 收集的 LongTask 证据；不得把计时窗口裁短来规避阻断。
  2. 道路复测不再强制不可用的应用内浏览器后端；验收者使用当前可用且受 Browser 技能支持的浏览器，从正式 `控制面板 → 简介 → 导入` 入口完成两个重生成入口、锁、撤销 / 重做与错误面验证。单纯浏览器工具问题不得触发产品改写；真实道路历史 LongTask 与共享边 owner 校验缺口必须按 `200ms` 门和锁 before-image 契约闭合。
  3. 消除平滑边界开关的 `>200ms` 产品 LongTask；首次 smooth / hard、两次 off / on 往返必须在固定 viewport / device scale 的同一截图坐标系闭合，不能用 canonical 摘要代替像素证据。
  - **最小验收**：原三名独立验收者分别在全新浏览器状态重新采证并明确返回 `ACCEPT`；任一 `BLOCK` 即停止合入。加载保持五次冷导入中位 `≤6.0s`、最大 `≤7.5s`、导入及 ready 后观察窗产品 `>200ms LongTask = 0`；道路两个正式入口与锁 / undo / redo / ordinary-debug 错误面实际通过；边界仅有 smooth / hard 两态且目标开关窗 `>200ms LongTask = 0`。
  - **非目标**：不放宽 health / shoreline / 像素阈值，不把浏览器后端缺失归为产品成功，不顺手重构其它显示或生成流程，不提前合并第 359～361 项。
  - **详细记录**：[`task-notes/task-362-independent-acceptance-blockers.md`](./task-notes/task-362-independent-acceptance-blockers.md)。

- **权威任务第 363 项：真实存档加载结果正确性与长寿命页面资源一致性** `已完成；唯一真实存档五轮联合门与完整画面通过`

  - **唯一输入**：`C:\Users\mosuzi\Downloads\krichars (3).webfmg`，SHA-256 `CF7402BC2BEA22AD1FCDE441444479F880DC0DB15D55520EF5A1A399D335DA61`；不得用随机地图、随机存档或只读对象摘要替代。
  - **用户反例**：当前 `http://127.0.0.1:5410/` 长寿命 production preview 页面完成导入后，cell surface 主体呈近黑/缺失，只剩路线、河流、标签与四周渐隐；导入成功提示和对象数量正确不能视为加载成功。
  - **既有正确基线**：同一存档在第 361 项第三轮验收的 `F0-full.png` 中完整显示蓝色水域与高度色阶陆地。第 362 项把加载性能、道路与边界分开验收，未把完整画面正确性纳入加载门，原 `ACCEPT` 对本项无效。
  - **363-A 归因**：黑图首败位于 compact GPU surface：基础 grid geometry 后仍追加没有 cell identity 的 legacy political band，导致 cell ranges 无法覆盖全部顶点，紧凑传输把基础顶点编码成无效 identity，shader 退回黑色。加载慢则由三路 v3 解析、compact surface 额外对象化，以及岸线保护关系对同一线段重复求交 / 求距共同构成；“边缘虚化”实际是独立的地图边缘渐隐，但旧切换路径仍错误重建整套线与 surface。
  - **363-B 修复**：GPU resident surface 不再混入 legacy political band；紧凑 surface 直接写入三字 position + cell identity，并对 100,000 个连续 cell ranges、末尾覆盖和无效 identity fail closed。surface correction 直接从 map 的共享边流式写入，不再创建 100k boundary cell 图；v3 整数解包对 `≤46 bits` 使用与编码端同源的安全 Number 位包路径。岸线保护扫描复用已知相交结果，对象一旦确认 near 后只继续计 crossings，不再重复求距；地图边缘渐隐几何常驻，开关只改变 draw range。
  - **363-C 终验**：最终 `0.5.72` production build 使用唯一存档从正式 `控制面板 → 简介 → 导入` 连续五轮，应用 trace 为 `5947 / 5433 / 5496 / 5340 / 5197ms`，中位 `5433ms`、最大 `5947ms`。五轮均为 `imported:n / r0`、`100000 grid cells`、`smooth / canonical`、Loading `0`、WebGL error `0`，健康窗口无 LongTask / operation / input / frame 事件。最终完整画面恢复蓝色水域、政治着色陆地、路线、河流与标签；目标地图区域 black / near-black ratio 均为 `0`、mean RGB `162.45`、saturated ratio `0.9537`。
  - **完整画面硬门**：固定 viewport、DPR、相机、主题、视图、平滑边界与“地图边缘渐隐”状态；canvas 必须存在非空且与正确基线一致的 water/land surface，主体亮度与颜色分布、边缘渐隐覆盖率、surface/line/overlay 资源摘要均在门内。只比较 smooth/hard 内部相等、截图文件大小、对象数量或肉眼局部均不算通过。
  - **边缘控制与专项门**：UI 位于 `控制面板 → 视图 → 地图边缘渐隐`。真实存档开→关改变 `11.1817%` 目标区域像素，关→开逐像素恢复（差异 `0`），切换仅 `draw 0.7ms`，identity / canonical / WebGL / health 不变。map-file Worker、v3 container、render preparation、prepared installer、GPU display mutation、boundary presentation、typecheck、`1406 modules` production build 与差异检查通过；既有 shoreline triangle 数量阈值在未修改基线 `8b29e02` 同样失败，未在本项放宽或归为完成证据。
  - **停止条件**：找不到正确基线、用户页面与存档身份不一致、同一夹具连续两次失败、或修复需要改变地图视觉产品定义时，冻结并请用户裁定；完成前不得归档、合入或再次宣称三项问题已全部解决。

- **权威任务第 364 项：真实存档道路重生成的全陆地覆盖正确性。** `已完成（2026-08-27）；禁止以命令完成、路线总数或索引一致代替地理结果验收`

  - **来源与唯一输入**：用户在 `krichars (3).webfmg`（`9,642,587 bytes`，SHA-256 `CF7402BC2BEA22AD1FCDE441444479F880DC0DB15D55520EF5A1A399D335DA61`）正式点击“重新生成道路”后，发现西陆整片有城市陆地没有道路。随机地图和小夹具只能作为廉价门，不能作为完成证据。
  - **最终目标**：道路重生成必须在遵守既有路线类型、海陆可达、政治/行政优先级与锁定最高优先级契约的同时，对每个含两个及以上可连通目标城市的陆地连通分量建立合理道路网络；不得因全图候选预算、排序截断、跨大陆不可达或锁合并而让整个有城市大陆被静默跳过。
  - **364-A 调查冻结**：同一次真实导入固定重生成前后地图 identity、陆地连通分量、各分量城市数、道路数、被道路触达城市数和无路城市簇，并保存西陆实际画面。首败必须定位到生成候选、寻路、预算/截断、锁合并或显示安装中的具体一层；在归因前不改生成器。
  - **364-B 最小修复**：只修改造成整片陆地漏算的最窄生成/合并边界，新增确定性的分量级覆盖回归；保持合法锁定路线及端点 before-image、共享边 owner、路线镜像、单历史提交和失败回滚，不顺手调整道路视觉、城市生成、地形或其它重生成领域。
  - **364-C 联合终验**：最终 production build 从正式 UI 导入唯一存档，分别执行中央入口与 RoutePanel 专用入口。每次必须同时检查：所有具备生成条件的陆地分量均有道路、分量内目标城市覆盖/连通达到冻结阈值、西陆画面实际出现道路、无跨海伪道路、路线结构与 picking 索引一致、WebGL error `0`、Loading `0`；再验证锁定路线、撤销、重做及普通/调试错误面。只看到路线数量变化或按钮成功提示一律不得通过。
  - **阶段边界**：唯一写者为主线程，任务分支 `codex/task-364-route-geographic-coverage`。首个廉价门为指定存档只读分量/路线覆盖统计；专项门只跑路线生成、锁与历史相关回归；真实浏览器首败后只允许一次最窄诊断和一次目标复验。同一夹具连续两次失败、存档身份不符、或必须重新定义“合理覆盖”产品规则时冻结请用户裁定。
  - **完成证据**：指定存档的显式锁定城市 `8` 经优先级闭包带入西陆 feature `3`，道路生成器曾错误把只读支撑 feature 当成 road / trail 禁区，导致西陆 `114 → 0` 条陆路。窄修后真实 Node 入口为西陆 `110` 条陆路、`1448` 个道路格、触达 `209` 座城市、`6 / 6` 首都接入，锁定首都原像不变且陆路水格 `0`。production UI 中央和专用入口均为 `442 / 7976 → 432 / 7615`，撤销 / 重做为 `432 → 442 → 432`；再锁定道路 `#0` 后重算为 `436 / 7822`，该路线表格 before-image 与锁状态均不变。全图实际画面确认西陆恢复道路网，console error / warn、WebGL error、Loading 与 aria busy 均为 `0`；版本 `0.5.73`。

- **权威任务第 365 项：生成与重生成的数据关系、容错和最小硬门重构。** `已完成（2026-08-28）；指定存档 22 / 22 入口逐项通过`

  - **产品契约**：长期关系文档固定“生成必需输入 / 可选偏好 / 可重建派生 / 可丢弃陈旧引用 / 锁定后置合并 / 物理结构硬门”六类关系。主动重生成先丢弃目标域旧错误，显式锁只捕获本体且在生成阶段不可见，新结果按空白地图逻辑生成后再按稳定 ID 原样覆盖；跨域镜像、首都 / 省会、父级、统计和支撑闭包不再被提升为全图硬门。
  - **底层与普通生成**：正常锁、内部损坏锁的 `12 × 15` 交叉矩阵分别为 `180 / 180`，旧目标损坏容错 `12 / 12`，no-op 与失败均为 `0`。10k / 50k / 100k 普通生成保持全部主要域非空；100k 为 `1207` 城镇、`505` 路线、`743` 河流、`43` 市场、`22328` 交易、`12` 洋流。
  - **唯一存档结果门**：`krichars (3).webfmg` 的 SHA-256 为 `CF7402BC2BEA22AD1FCDE441444479F880DC0DB15D55520EF5A1A399D335DA61`，正式回读 `100000 grid / 43419 pack`、锁定 city `#8`。底层十二类 `12 / 12`；道路 `442 → 438`，西陆 `108` 条陆路、`1440` 道路格、触达 `203 / 350` 城市，`6 / 6` 首都接入，陆路水格 `0`。
  - **production UI 终验**：正式十一类、复合六类、直接五类共 `22 / 22`，每项重新导入原档并检查实际内容、基础物理门、锁 before-image、单历史、撤销 / 重做指纹、Loading、health、console / page 与 WebGL。最终全部 `ok=true`、错误面为 `0`；人工查看道路与最终截图未见黑图、边界第三态或大陆漏路。相关协议、路线质量、经济 Worker、typecheck 与 `1407 modules` build 通过；完成版本 `0.5.74`。
