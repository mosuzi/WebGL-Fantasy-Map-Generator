# 2026-08-01 至 2026-08-07 权威任务归档

本卷保存 第 229～299 项中的已完成或已取代条目 的历史权威任务正文。当前可执行任务仍以 [当前开发计划](../current-plan.md) 为唯一权威来源。

- 归档条目数：71
- 排序方式：按权威任务编号升序
- 状态边界：只收录已完成、已移除或已由后续方案取代的条目；未完成与暂缓项不得进入归档。
- 特别说明：首轮归档时第 284、298 项仍为暂缓任务，未进入本卷；其后若完成，必须按实际完成日期进入新时间卷。

## 任务条目

- **权威任务第 229 项：AI 文档去存档上下文化。** `已完成；来源：用户直接纠正`
  - 范围：移除以晴布赫斯、南北平原和两大陆高山区为固定上下文的三份 AI playbook 及 Wiki 专属教程入口；把区域定义、对照组、保持约束、预检、执行后复验等可复用方法合并到通用区域分析与安全修改文档，机器目录不得再路由到私人地图案例。
  - 最小验收：`docs/ai`、机器目录、Wiki 首页 / 侧栏 / 覆盖矩阵和同步门禁不再把三个私人存档问题作为产品入口；通用文档仍能指导任意地图完成气候与承载、区域发展差距和地形梯度分析；AI 文档与 Wiki 门禁通过。私人存档、cell id、对象 id、修改前后数值和截图均不进入仓库。
  - 排除：不删除第 224～225 项历史验收记录，不公开或复制用户存档，不把本次地图实操结果写入开发文档。
  - 完成记录：删除三份存档专属 AI playbook 与 Wiki 教程，区域定义、对照组、因果诊断、约束和复验方法并入通用区域分析与安全修改文档；AI 机器目录和 Wiki 入口不再路由私人案例，私有案例目录由根级 `.gitignore` 整体排除。

- **权威任务第 230 项：开发模式面板宽屏可读性。** `已完成；来源：用户直接提出`
  - 范围：只调整开发模式面板的桌面宽度、窄屏收缩、旧窄宽度状态兼容和长日志呈现；不得改变调试开关、AI 桥授权、地图数据或其它业务面板语义。
  - 最小验收：桌面视口下开发面板默认提供足够宽的运行时与 AI 调试空间，生成日志、加载追踪和健康事件不再被任意碎断；既有窄宽度记录不会把大屏面板永久锁窄；`480 / 390 / 320px` 与 150% 缩放下仍位于安全区、正文可滚动且 document 横向溢出为 `0`。专项、生产构建和系统 Chrome 验收通过，application console、page、health 与 WebGL error 为 `0`。
  - 完成记录：开发面板默认宽度提升到 `760px`，增加响应式 `680px` 下限和 `1100px` 上限；旧 `360px` 持久化样本在桌面恢复为 `680px`，窄屏自动收缩。生成日志改为逐条单行的等宽局部滚动区，加载追踪与健康事件不再任意换行。系统 Chrome 构建产物在 `1440 / 480 / 390 / 320px` 四档全部位于安全区、正文可滚、document 横向溢出为 `0`，AI 调试入口可达，application console、page、活动 application health 与 WebGL error 均为 `0`。

- **权威任务第 231 项：三个私有地图案例的 AI 桥接修复。** `已完成；来源：用户直接批准`
  - 范围：通过第 227 项当前标签页受控桥读取当前地图，先保存完整私有基线，再依次完成用户指定的地形梯度、区域气候与人口、对照区域人口与国力三个案例；每步的存档、指标、截图和复验记录只写入 `private-cases/`。
  - 最小验收：由独立调查智能体先冻结每个案例的量化指标、不变量和回退条件；主线程按“只读基线 → inspection → 显式写入 → verify → 阶段快照”执行；三个案例均满足 API 指标与视觉证据，地图身份、非目标疆域和指定地理关系保持，失败步骤可回退；最终由另一个全新智能体独立复验全部通过后方可提交。
  - 排除：不把真实存档、对象 id、cell id、修改前后私有数值或截图提交仓库；不为通过验收绕过现有领域事务、桥接授权或历史机制。
  - 执行记录：真实 Chrome 首次视觉配对时，令牌 `password` 输入会触发密码管理扩展提示层并在连接按钮执行前夺走页面控制；当前把令牌入口改为 `text + -webkit-text-security: disc`，同时声明常见密码管理器忽略标记，保持视觉掩码、只读默认和显式连接语义不变。该窄修复须先通过开发面板专项与真实 Chrome 配对，再继续冻结私有基线。
  - 执行记录：只读基线进一步暴露桥接把 `climate.get / history.get` 等 `mutates:none` 方法误判为写操作。当前改为以公开方法级副作用元数据判定：所有 `mutates:none` 只读放行，`data` 仅放行 `download-or-export-result`，导入与所有真实写入继续要求页面写授权、revision 和 requestId；桥接专项新增气候读取、压缩导出与导入阻断反例。
  - 执行记录：刷新自动恢复还会在浏览器存档完成 `loadMapIntoRuntime` 前看到 `ready=true / mapIdentity=null`，旧桥用 seed 与生成时间拼成临时 documentId，正式 `MapRevisionTracker.replaceMap()` 后全部请求被安全拒绝。桥现在必须等公开 `mapIdentity` 非空才注册，不再把临时 fallback 当文档身份；专项以“临时 ready 无身份 → 正式身份”反例冻结刷新重连。
  - 执行记录：压缩全图导出与导入分别超过桥原共用的 `1 MiB` JSON 限制。当前保持注册、轮询与控制请求上限为 `1 MiB`，把 AI 命令入口 `/v1/request` 设为 `8 MiB`、已鉴权页面结果回传 `/v1/result` 设为 `128 MiB`，均为有界且受既有鉴权、页面写授权、revision 与 requestId 约束；专项分别用大命令和大结果冻结边界。
  - 执行记录：实操发现气候重算会重新定义适居度人口，且存档载入时适居度归一化也会覆盖合法的人工人口调整。当前气候写接口支持显式 `preservePopulation`；`generate.regenerate` 仅允许 `features / routes / rivers` 三类地理派生重算使用同一保护，避免在国家、城市、省份等人口业务重算中制造聚合不一致；加载归一化改为只刷新适居度与人口镜像，不再覆盖存档人口。专项和完整 gzip 导入后立即导出复验均证明人口、行政聚合和地图身份稳定。
  - 完成记录：主线程按私有验收口径完成三个案例的只读基线、inspection、显式写入、阶段存档、指标复算、同相机全图 / 局部视觉证据与最终 gzip 回读。首轮独立复验发现派生重建顺序、控制区选择、路线保护和截图遮挡四类问题，已改为“地形 → 地理派生 → 气候 → 人口”的固定顺序，并以路线锁保护关键对象、以未受地形影响的控制区验证气候漂移、关闭开发面板重拍证据；第二轮独立复验最终 `ACCEPT`。真实名称、对象编号、坐标、数值、存档、报告和截图仍只保存在 gitignore 排除目录。
  - 完成记录：存档回读另暴露旧图可选路线镜像字段与零号未归属人口桶的兼容边界。路线锁只以正式路线对象、pack 镜像的稳定 `i / points` 和 cell 链路判定，不因加载补齐可选字段误报冲突；人口聚合在重复加载前统一清零包括索引 `0` 的所有有效桶，避免未归属人口累加。专项、API / inventory、生产构建、开发面板 Chrome 回归和 `git diff --check` 均通过。

- **权威任务第 232 项：私有案例特征抽象与 AI 行动手册。** `已完成；来源：用户直接批准`
  - 范围：在第 231 项通过后，把三个实操过程抽象成不依赖真实存档、专名与坐标的通用问题特征、数据获取步骤、因果判断、保持约束、动作选择和复验模板，写入 AI 手册与机器目录。
  - 最小验收：公开手册能指导默认 AI 针对任意地图完成地形梯度自然化、地形雨影下的区域承载改善、跨障碍同政权区域发展差距分析；示例只使用合成或匿名化特征，不包含可还原当前私有地图的数据；AI 文档同步、审计、Wiki 审计和相关能力矩阵门禁通过。
  - 完成记录：新增匿名化复杂区域干预行动手册，以“证据快照 → 因果诊断 → inspection → 显式事务 → 分层复验 → 存档回读”的统一闭环描述地形梯度自然化、雨影下承载改善和跨障碍同政权发展平衡；明确固定执行顺序、人口保护、路线锁、对照区、回退点和视觉证据要求，不包含真实名称、对象 / cell id、坐标、私有数值、存档或截图。
  - 完成记录：机器领域目录新增 `3` 个 `problemPatterns`，逐项绑定手册锚点、读取 / 预检 / 执行方法和保持不变量，并在同步时校验所引用浏览器 API 方法真实存在；AI 手册、三份 Wiki 领域页和数据模型 / 运行时 / 安全边界文档完成同步。AI 文档、Wiki、完整能力矩阵和复合语义审计均通过，公开 API 总数与分类分母保持不变。

- **权威任务第 233 项：重写人类友好的中英文 README。** `已完成；来源：用户直接要求`
  - 范围：重写根目录中文版 README，以可读叙述介绍项目定位、从自然世界到人文世界的使用旅程、生成后的观察编辑与保存导出，并用简明准确的语言让用户知道页面 API、离线只读分析、受控无头写入和当前标签页 AI 受控桥；新增具有相同事实、层次与气质的自然英文版。必要细节只链接仓库内权威 Wiki 源稿、AI 手册、权威任务清单和开发日志。
  - 最小验收：正文没有实现细节或功能点堆砌，自然、人文、编辑、保存与导出体验得到叙述性覆盖；API 系统说明其适用场景和安全边界；本地运行要求、命令、线上入口、项目状态与许可证真实；所有链接有效，中英文事实和边界一致；AI 文档、Wiki 与差异门禁通过，并由独立智能体审查通过。
  - 排除：不修改功能代码、`source/`、Wiki 或 AI 手册正文，不补远端 GitHub Wiki，不新增或引用产品截图，不扩写逐方法 API 手册，不从本项创造后续待办。
  - 撰写记录：中文版按“世界生成 → 生成后的观察与编辑 → 保存导出 → API 与自动化 → 快速开始与权威文档”组织可读叙述，不再以领域名词堆叠能力；新增英文版按相同事实与信息层级自然重写，并在中文专题链接上明确标注语言。两版均不固化易漂移的 API 分母，只说明页面 API、无头只读、受控无头写入和当前标签页 AI 桥的适用场景与安全边界。
  - 自检记录：两版相对链接逐项解析均存在，语言切换互链正确；Node.js、pnpm、`dev / build` 命令和默认地址已与当前配置核对。该轮线上入口与原项目致谢链接均返回 HTTP `200`，页面标题符合当时名称契约；`audit:wiki`、`audit:ai-docs` 与 `git diff --check` 通过。中文名称后由第 260 项更新。
  - 完成记录：独立审查首轮发现两版把远端 GitHub Wiki 写成“尚未启用”，与仓库已启用 Wiki 元数据、但受当前私有仓库套餐限制而无法实际发布的状态混淆，因此结论为 `BLOCK`。两版已限修改为“受当前私有仓库套餐限制，远端 GitHub Wiki 尚未发布”，英文同步表达同一事实；复验确认措辞、事实、中英文一致性、链接和差异范围均正确，终轮结论为 `ACCEPT`。

- **权威任务第 234 项：清理中英文 README 的翻译腔与模板化表达。** `已完成；来源：用户直接反馈`
  - 范围：以项目作者口吻通读并优化根目录中英文 README，重点清理宣传腔、对称排比、空泛抒情、翻译式第二人称和模板化转折；中文版自然、克制、具体，英文版只按结构与事实需要同步，不追求逐句对齐。
  - 最小验收：项目定位、在线体验、自然与人文世界、生成后编辑、保存导出、旧图兼容边界、API 与 AI 自动化、快速开始、文档导航、项目状态、致谢和许可证等既有事实均保留；所有链接、命令和安全边界准确；不引入实现细节或功能点堆砌；两版相对链接、Wiki 审计、AI 文档审计和差异检查通过。
  - 排除：不修改功能代码、`source/`、Wiki 或 AI 手册正文，不新增截图，不扩写 API 方法清单，不从本项创造后续任务。
  - 完成记录：中文版删去“你可以在这里得到一张地图”“生成之后，地图才真正开始”“想做什么，就从这里继续”等翻译式第二人称和拔高标题，改为“从地貌到人文”“查看、编辑和保存”“文档导览”等直接标题；正文用地图生成、查看、修改、保存和导出的具体动作串联信息，保留少量世界构建画面感，不再依赖整齐排比、宣传式转折或空泛判断。
  - 一致性记录：英文版保留原有事实和章节顺序，只同步必要的标题、导航和对应段落，并清理 `not merely`、`only the beginning` 等相同倾向的表达；两版继续准确说明旧图不保证无损迁移、无头写入默认保留原文件、当前标签页桥须显式授权且不执行任意脚本或绕过编辑规则。
  - 验证记录：两版全部相对文件链接逐项解析均存在，页内锚点与文档锚点对应现有标题；`pnpm run audit:wiki`、`pnpm run audit:ai-docs` 和 `git diff --check` 通过。本项只修改文档，不需要构建或浏览器验证；完成后恢复为当前没有活动权威任务。

- **权威任务第 235 项：明确 README 的全 AI 实现声明。** `已完成；来源：用户直接纠正`
  - 范围：修正中英文 README 对项目实现方式的描述，明确当前应用代码 100% 由 Codex + GPT 实现，文档同样由 Codex + GPT 编写。未来继续保持这一开发方式是内部约束，不在 README 中强调。
  - 最小验收：中英文事实一致，不再使用“编写过程中使用了”这类弱化参与程度的措辞；作者归属、模型版本范围、当前全 AI 实现和文档来源完整准确。`100%` 已经说明代码参与方式，README 不重复添加同义解释，也不加入未来开发承诺。
  - 排除：不修改功能代码、生成方式、依赖、许可证或其它项目介绍，不从本项扩展新的实现或流程要求。
  - 完成记录：中文版改为“当前应用的代码 100% 由 Codex + GPT 实现”，并明确文档来源；英文版同步表达相同事实。用户随后明确不在 README 中限定 GPT 版本，且 `100%` 已足以表达代码参与方式，未来约束也无需强调；两版均已删除模型版本、重复解释和未来承诺。Wiki、AI 文档和差异检查通过，本项为纯文案修正，不运行构建或浏览器验证。

- **权威任务第 236 项：整理图层页与复合地区 / 标记入口。** `已完成；来源：用户直接要求`
  - 范围：按语义分组图层页；地区和标记各收敛为一个首层复合入口，并正确显示全开、全关和部分开启。
  - 最小验收：全部能力仍可达；五个地区键和两个标记键的 API / 偏好 / renderer 状态不合并；复合点击不改变地图数据、历史或 checksum。
  - 排除：不合并底层图层字段，不改变存档格式、渲染顺序或公开 API。
  - 完成记录：图层页改为五个语义分组，“地区内容与名称”统一承载五个地区键，“资源与标记”统一承载两个标记键；复合按钮支持全开、全关和部分开启，底层状态、偏好、渲染和 API 均保持独立。

- **权威任务第 237 项：统一网格单元默认关闭状态。** `已完成；来源：用户直接要求`
  - 范围：无显式偏好时，控制面板与 renderer 均显示 `gridCells=false`；用户已保存偏好继续优先。
  - 最小验收：首次按钮、API 和 draw call 均为关闭；首次点击立即开启，刷新保持；定位诊断仍可主动打开。
  - 排除：不改变其它图层默认值，不清理用户本地偏好。
  - 完成记录：`gridCells` 显式声明默认关闭，Vue 控制面板和运行时在没有偏好时统一读取该默认值；既有用户偏好继续优先，首次点击仍能正常开启。

- **权威任务第 238 项：自动无人区使用地理语义名称。** `已完成；来源：用户直接要求`
  - 范围：按邻近城镇、省份、文化和必要时的国家语义生成稳定名称，序号只作同名消歧。
  - 最小验收：默认种子不再出现“荒野 4”式内部编号名；同地理输入稳定，id 变化不影响；手工名、锁定、分裂 / 合并继承和旧图加载保持。
  - 排除：不在旧图加载时静默重命名，不改变无人区识别和连通分块规则。
  - 完成记录：自动无人区依次参考邻近城镇、省份、文化和明确占优的国家语义，缺少邻接语义时按方位与地貌命名；序号只用于同名消歧，手工名称、稳定继承和旧图加载不变。

- **权威任务第 239 项：隐藏纹章统计入口。** `已完成；来源：用户直接要求`
  - 范围：移除管理页可见入口和空的实验分组，保留内部面板及全部纹章数据兼容链。
  - 最小验收：DOM 无入口；国家 / 城市纹章字段、对象查询、JSON 往返、旧图和公开 API 分母不变。
  - 排除：不删除纹章功能代码、数据、生成或存档字段。
  - 完成记录：管理页不再显示纹章入口和空实验分组；纹章面板模块、地图数据、对象查询、存档兼容和公开 API 保留。

- **权威任务第 240 项：收紧共享下拉框箭头右间距。** `已完成；来源：用户直接要求`
  - 范围：只调整共享 `UiSelectField` 的箭头容器和必要文本留白。
  - 最小验收：箭头靠近右边框且不压住文本；disabled / focus / open 正常；1440、390、320px 无横向溢出。
  - 排除：不顺带改写原生 select 或少数直接 ElSelect。
  - 完成记录：共享下拉框箭头右侧实测间距收紧到约 `8.8px`；`430px` 以下标签与控件改为上下排列，系统浏览器在 `390 / 320px` 均无横向溢出。

- **权威任务第 241 项：新增显式配置的 Dropbox / Google Drive 云存储。** `已完成；来源：用户直接要求`
  - 范围：以统一 provider registry 提供连接、断开、新建、列出、明确目标覆盖和载入；复用完整地图 gzip 与既有导入安全链；提交统一空值环境变量示例和自部署说明。
  - 最小验收：Dropbox PKCE / App Folder 与 Google GIS / `drive.file` 的 fixture 和 mock 浏览器闭环通过；令牌不持久化、不泄露；未配置 provider 稳定禁用；保存不改变 checksum、历史和相机，载入 / 覆盖具有明确确认；旧图导入链、生产构建与差异检查通过。
  - 排除：不复用原作 app key，不接收 client secret，不做自动同步、远端删除、分享链接、冲突合并、任意文件浏览或 headless 云写入。没有项目方 OAuth 配置时不宣称真实账号端到端已联调。
  - 完成记录：保存菜单新增懒加载云端面板，统一 provider registry 已覆盖 Dropbox PKCE / App Folder 与 Google GIS / `drive.file` 的连接、断开、新建、列出、明确目标覆盖和载入；公开 client identifier 由 `VITE_FMG_*` 环境变量配置，第 241 项完成时令牌只保存在内存，后由第 262 项调整为当前标签页 `sessionStorage` 中的短期会话。fixture、mock transport、未配置状态、旧浏览器存档、生产构建与独立复核通过；因没有项目方 OAuth client 与测试账号，真实云账号端到端仍明确标记为未联调。

- **权威任务第 242 项：修复地区管理跨构建首次加载失败。** `已完成；来源：用户截图反馈`
  - 范围：让 `ZonePanel` 组件随入口构建加载、面板保持延迟挂载；共享懒加载器识别动态模块获取失败，提供版本更新说明、保存提醒、重试与显式刷新操作，并保留原始开发日志。
  - 最小验收：完整当前构建正常打开地区管理；旧哈希 chunk 失效时出现可操作恢复态且不自动刷新；普通组件异常不被误判，懒加载状态只暴露有界错误类型与消息。
  - 排除：不自动刷新、保存或覆盖地图，不关闭其它面板懒加载，不修改地区业务。
  - 完成记录：`ZonePanel` 由应用入口静态注入、继续延迟挂载，生产构建不再生成独立地区 chunk。共享懒加载器已区分模块获取与组件异常，提供保存提醒、重试和显式刷新；首轮复核发现 mount 失败保留半初始化实例，限修后真实覆盖二次 `createApp + mount` 成功且自动刷新次数为 `0`。

- **权威任务第 243 项：修复云端存储跨构建首次加载与组件注册。** `已完成；来源：用户截图反馈`
  - 范围：让 `CloudStoragePanel` 组件随入口构建加载、面板保持延迟挂载；显式导入模板使用的 `UiButton`，复用第 242 项共享恢复态。
  - 最小验收：完整当前构建正常显示云端未配置态和真实按钮组件；入口不再首次打开才请求独立云端 chunk；云专项、禁用态和点击接线通过。
  - 排除：不修改 OAuth、环境变量、令牌持久化、远端文件语义或真实账号联调边界。
  - 完成记录：`CloudStoragePanel` 同样由入口静态注入、继续延迟挂载，生产构建无独立云端 chunk；模板显式导入 `UiButton`。Chrome 显示 Dropbox / Google Drive 为真实 `BUTTON.el-button`，未配置态完整且无动态模块 404 / TypeError。

- **权威任务第 244 项：把地区接入用户可见的重新生成。** `已完成；来源：用户直接要求`
  - 范围：控制面板统一目录和地区管理面板新增“重新生成地区”，复用既有 `generate.regenerate("zones", {confirm:true})`、地图快照事务、锁定保护和 `buildZones`；允许空地区集合从零生成。
  - 最小验收：UI / API 同路径，锁定对象不变，salt、地区统计、对象索引与面板刷新正确；全锁不推进，失败和撤销 / 重做完整恢复，旧结构 / 空集合可生成，公开 API 分母不变。
  - 排除：不新增并行地区算法，不改生成概率、存档格式、旧图加载或手工 / 自然 / 锁定地区的既有保留规则。
  - 完成记录：控制面板和地区管理面板均接入同一 `zones` 重生成事务，空集合和旧 `pack.zones` 可生成、全锁保持 no-op。Chrome 实测地区从 `2` 重生成到 `12`（扰动 `#4`），撤销恢复 `2` 行，重做恢复 `12` 行；自动无人区使用“翠源荒原”“云信外野”等语义名称。

- **权威任务第 245 项：地区名称接入地图语义标签样式。** `已完成；来源：用户直接反馈`
  - 范围：标签类型从五类兼容扩为六类，新增 `zone / 地区名称`；默认与主题显式继承手工标签语义，用户覆盖独立；删除 `.zone-label` 对 resolved 颜色和字号的硬覆盖。
  - 最小验收：地区字体、字号、颜色、描边、透明度可独立编辑、撤销、重做和重置；五个旧类型隔离，字号进入碰撞盒，地区显隐、选择、定位和改名不变；旧 v1 样式存储无需升级。
  - 排除：不改变地区生成、图层和布局算法，不增加公开 API 方法。

- **权威任务第 246 项：建立画布文字分层 registry 与机器分母。** `已完成；来源：用户要求整理样式覆盖`
  - 范围：登记六类地图语义标签、军事 / 图例 / 比例尺三类成品注记、HUD / 诊断文字，以及河流 / 道路 / marker 等 `not-rendered` 对象；逐项声明样式来源、持久化、导出和用户可编辑状态，并与 renderer / CSS / PNG 双向核对。
  - 最小验收：六类语义标签、三类注记和全部排除项零差集；不存在未归属画布文字，也不把 UI / HUD 冒充地图标签覆盖。
  - 排除：不为注记层新增持久化 schema，不新增河流 / 道路沿线文字，不把 marker 文字化，不开放 HUD 字体设置。

- **权威任务第 247 项：统一验收地图文字样式覆盖。** `已完成；来源：用户直接要求`
  - 范围：统一验证六类语义标签的实时 / PNG 一致、旧图和浏览器缓存、现有标签 API、主题继承、重置、撤销 / 重做、地区操作与窄屏；复核成品注记和排除项没有误入。
  - 最小验收：公开 API 保持 `316 / 316`；普通 JSON、gzip、旧图和浏览器恢复通过，纯样式修改不改 checksum / 地理；Chrome 完成地区明显样式、刷新、PNG、显隐 / 定位 / 改名和 `390 / 320px`，无新增 console、page、WebGL error。
  - 排除：不重写标签布局，不新增 PNG overlay key，不扩展为完整符号化编辑器。

- **权威任务第 248 项：恢复地区管理批量选择按钮间距。** `已完成；来源：用户直接反馈`
  - 范围：为共享重生成锁定批量操作区补足稳定的纵向布局和按钮间距，优先复用现有面板留白尺度；地区管理的“列表多选 / 在地图多选 / 锁定选中 / 解锁选中 / 清空选择”不得再无间隙相连。
  - 最小验收：默认面板宽度和 `390 / 320px` 下相邻按钮间距可量测且不小于 `8px`；按钮宽度、禁用态、批量选择和地图多选行为保持；其它复用该组件的管理面板不溢出。
  - 排除：不重做批量锁定交互，不改变对象表、选择语义或重生成锁仓。
  - 完成记录：共享批量操作区改为 `8px` 网格间距，五个操作继续保持全宽纵向布局；系统 Chrome 在 `1440 / 390 / 320px` 的四处相邻间距均为 `8px`，document 横向溢出为 `0`。

- **权威任务第 249 项：统一地区重生成后的数据、列表与画布名称。** `已完成；来源：用户直接反馈`
  - 范围：地区重生成必须同时刷新地区覆盖与地区标签；自动编号旧名（兼容“荒野4 / 荒野 4 / 荒原4”等空格差异）在显式重生成时改用地理语义名称，旧图中无法证明为自动编号的名称继续保留；地区列表把名称置于编号之前，并把内部 ID 明示为“编号”。
  - 最小验收：同一次重生成后列表与画布使用同一组名称，不再残留旧“荒野 4”；列表首列为可读名称，编号仍可查看和排序；撤销 / 重做同时恢复数据、覆盖与标签；锁定、手工名和旧图兼容不变。
  - 排除：不在普通旧图加载时静默改名，不改变无人区识别、连通分块或事件地区生成规则，不隐藏对象编号。
  - 完成记录：地区重生成刷新集合补齐 `line-layers / labels`；兼容识别“荒野4 / 荒野 4 / 荒原4”等旧自动编号名，无 `nameMode` 的其它旧名称按手工结果保护。地区表格改为“名称 → 编号”，真实 `5411` 中列表与画布从“荒野 4”同步更新为“梁门外野”，撤销 / 重做一致。

- **权威任务第 250 项：恢复地区图层的分级控制。** `已完成；来源：用户直接反馈`
  - 范围：保留“全部地区”总开关，同时恢复“事件地区 / 自然地区 / 自动无人区 / 地区名称”四个独立开关；只调整控制面板的信息架构，继续复用既有五个底层状态键。
  - 最小验收：五个入口的状态、点击和部分关闭语义清楚；四个子开关可独立控制相应覆盖或名称，总开关可整体隐藏 / 恢复；偏好、renderer、API、地图数据、历史和 checksum 不变。
  - 排除：不合并或迁移底层字段，不改变图层默认值、渲染顺序、存档格式或资源 / 标记复合入口。
  - 完成记录：地区分组现有“全部地区 / 事件地区 / 自然地区 / 自动无人区 / 地区名称”五个入口，各自直接对应既有状态键；总开关与四个子开关均可独立关闭、恢复，底层字段和资源 / 标记复合入口不变。

- **权威任务第 251 项：统一复核并在 `5411` 验收地区修正。** `已完成；来源：用户要求逐项解决`
  - 范围：先复核第 248～250 项差异和旧数据边界，再在当前 `5411` 系统 Chrome 标签页验证按钮留白、重生成后的列表 / 画布名称、撤销 / 重做、五级地区图层开关和窄屏布局。
  - 最小验收：聚焦回归、受影响信息架构 / 地区 / 响应式专项、生产构建和差异检查通过；Chrome 中无 application console / page / WebGL error，验收后 `5411` 服务继续保持可访问。
  - 排除：不处理本批之外的地区生成质量、其它对象图层分组或性能候选，不提交或推送。
  - 完成记录：地区重生成、无人区、锁定 UI、图层信息架构、标签布局、响应式与复杂工作区回归、生产构建、差异检查全部通过。新增系统 Chrome 专项直接连接 `5411`，覆盖 `1440 / 390 / 320px`、名称同步、撤销 / 重做与五级图层；application console / page / WebGL error 为 `0`。重生成、撤销和重做记录 `6` 条既有 `input-handler-stall` 性能遥测，已与功能错误分离，不在本批扩展性能治理。

- **权威任务第 252 项：统一云端存储面板的深色主题样式。** `已完成；来源：用户截图反馈`
  - 范围：移除云端存储说明卡片、配置提示、连接信息、远端文件项和底部边界说明中残留的浅色纸张硬编码配色，统一使用现有深色面板的背景、边框、正文、弱化文字、链接、代码标签和状态色；不改变云存储业务与 OAuth 边界。
  - 最小验收：未配置与已配置两类页面均不再出现土黄色卡片；标题、正文、环境变量代码、链接、文件选中态、成功 / 错误状态保持清晰可读；云存储专项、生产构建与差异检查通过，并在当前 `5411` 系统 Chrome 页面确认默认及窄视口无新增溢出、console、page 或 WebGL error。
  - 排除：不改 Dropbox / Google Drive provider、配置键、授权流程、token 生命周期、上传下载或真实账号联调边界；不扩展其它面板的主题整改，不提交或推送。
  - 完成记录：说明、配置、连接与边界卡片统一为 `#10171b` 背景和 `#33444f` 边框，弱化正文、代码标签、链接、文件项及成功 / 错误状态全部复用现有暗色主题变量。新增系统 Chrome 专项在 `1440 / 390 / 320px` 验证三类卡片颜色、面板安全区和横向溢出均通过；当前 `5411` 用户标签页实测一致，console、page、health 与 WebGL error 为 `0`，仅保留既有性能遥测。本项未改云存储业务，未提交或推送。

- **权威任务第 253 项：把“全部地区”改为真实的地区图层批量开关。** `已完成；来源：用户直接反馈`
  - 范围：不再把 `zones` 总门控伪装成一层独立图层；“全部地区”改为组合控制，统一覆盖 `zones / zoneEvents / zoneNatural / zoneWilderness / zoneLabels`，保留事件地区、自然地区、自动无人区和地区名称四个细分开关及全部底层状态键。
  - 最小验收：任一细分开关关闭时“全部地区”为半选，点击后五个状态全部开启；五个状态全开时点击“全部地区”会全部关闭，再次点击全部恢复；单独控制四个子层仍有效，偏好、renderer、API、存档和地图 checksum 不变。信息架构、地区浏览器专项、生产构建与差异检查通过，当前 `5411` 标签页无新增 console、page、health 或 WebGL error。
  - 排除：不删除 `zones` 底层兼容键，不合并四个子状态，不改变地区渲染、生成、名称或样式语义，不提交或推送。
  - 完成记录：“全部地区”现以 `data-layer-group` 组合 `zones / zoneEvents / zoneNatural / zoneWilderness / zoneLabels`，不再显示为独立 `zones` 图层。系统 Chrome 地区专项在 `1440 / 390 / 320px` 通过半选→全开→全关→全开闭环，四个子层仍可单独控制，document 横向溢出与 application console / page / health / WebGL error 均为 `0`。同一 Chrome 新开临时 `5411` 页面复验原状态“仅自动无人区关闭”为半选，一键全开 / 全关后完整恢复原状态；原用户标签页因 HMR 替换按钮后保留旧事件绑定，为保护未保存地图未强制刷新，用户保存后刷新即可取得新行为。

- **权威任务第 254 项：在悬停信息面板集中展示全部快捷键。** `已完成；来源：用户直接要求`
  - 范围：在悬停信息面板右上角增加信息圆按钮，鼠标悬停或键盘聚焦时显示快捷键总表；全局快捷键必须从现有 `KEYBOARD_SHORTCUTS` 权威注册表派生，并同时纳入删除测量点、确认 HEX 颜色两项场景快捷键，避免维护第二份易失真的硬编码清单。
  - 最小验收：Windows 与 macOS 修饰键文案由现有格式化逻辑生成；总表覆盖 `22` 项全局动作、`23` 组全局按键及 `2` 项场景动作，按用途分组且长列表可滚动。信息圆按钮具备可读的无障碍名称，悬停与键盘聚焦均可稳定查看；地图悬停内容仍会随命中对象更新，弹层不越出默认及 `390 / 320px` 视口，不改变快捷键触发、地图数据、历史、相机或存档。
  - 验证：补快捷键分母与派生关系门禁、悬停面板结构和浏览器专项；通过既有快捷键、悬停信息、画布文字、响应式、生产构建与差异检查，并在当前 `5411` 系统 Chrome 页面确认真实悬停、焦点、滚动、命中更新以及 console、page、health、WebGL error。
  - 排除：不新增、删除或重绑定快捷键，不改现有按键冲突规则，不把快捷键说明做成可编辑设置，不修改 `source/`，不提交或推送。
  - 完成记录：悬停卡右上角新增圆形 `i` 按钮，弹层从现有注册表派生 `22` 项全局动作、`23` 组全局按键，并纳入删除测量点和确认 HEX 颜色两项场景动作，共 `24` 条说明、`5` 个用途分组。首轮独立复核因弹层 `8px` 命中断层和高度画笔 `pointerleave` 清空 pick 判定为 `BLOCK`；限修为按钮与弹层轻微重叠，并在进入帮助区时保留高度 pick，三轮复核最终 `ACCEPT`。冻结生产构建 Chrome 专项与 `5411` 独立终验覆盖普通 / 高度画笔悬停、弹层滚动、键盘焦点、pick 更新节点恒等、`1440 / 390 / 320px` 边界和画布透传；application console、page、health error 与 WebGL error 均为 `0`，仅记录 `2` 条既有主线程耗时 warning。本项未改变快捷键执行、地图数据、历史或存档，未提交或推送。

- **权威任务第 255 项：合入并验收共享拓扑边界平滑。** `已完成；来源：用户直接要求`
  - 来源冻结：目标为 `origin/codex-smooth-boundaries` 的 `a536d51053cdfd8f14459571a3743ba98424f46a`；它与当前 `main` 的共同祖先为 `e4c5fe54961d27777edf1dbd91aff27e4ea63326`，双方独有提交为 `37 / 9`，必须使用常规三方 merge，不以 cherry-pick 改写专项历史。
  - 范围：合入正式海岸 / 湖岸共享拓扑快照、有效三角面积 Visvalingam、有限 Chaikin、端点与最大位移约束、fill / stroke 同源修正、局部安全回退，以及对应拓扑实验室、压力夹具和 shoreline 门禁；只改变 renderer 视觉几何。
  - 兼容边界：保留当前主线第 245～254 项状态、最新 API 分母和现有未提交工作；canonical `grid / pack / feature`、地图 checksum、存档、API、编辑事务与 picking 不变。国家 / 省份边界继续使用既有硬边，不纳入平滑；关闭平滑必须完整恢复共享 Voronoi 硬岸线。
  - 合入策略：只临时保护与目标分支重叠的 `AGENTS.md`、`placeholder-renderer.js`、`docs/current-plan.md`、`docs/development-log.md`、`package.json` 本地差异；合并提交不得夹带第 236～254 项其它未提交修改。历史文档冲突以当前主线为准并由本项记录取代旧活动状态，renderer 与依赖逐块语义合并。
  - 最小验收：拓扑实验室、shoreline `--pure / --pure-formal`、当前 Cells / API 接线、生产构建和差异检查通过；系统 Chrome 覆盖高度 / 国家 / 省份视图下的平滑与硬边六态，海岸、湖岸、孤岛、三角岛、狭窄水道和湖洞无错侧色带、长针或断口，城镇、道路、河流及河口保护关系不漂移，地图数据与 checksum 不变，console、page、health error 与 WebGL error 为 `0`。
  - 排除：不扩展国界 / 省界平滑，不修改生成、编辑、存档或 API 语义，不顺带处理其它视觉或性能候选；本项不推送。
  - 完成记录：创建双父 merge 提交 `a5832dcb90134a5308a355119cfb9c6561b864b2`，父提交严格为原主线 `e393599` 与目标 `a536d51`；相对第一父只合入目标专项的 `25` 个有效文件，旧活动任务和旧 API 分母未回灌。路径限定 stash `471b616f` 已成功 apply 且继续保留为恢复点，第 236～254 项其它未提交工作未进入合并提交。
  - 验收记录：拓扑实验室 `20` 个夹具、推荐算法 `14 / 14`，10k / 50k pure / formal 回放的错侧像素、最长针、冲突覆盖、重复覆盖和 seam 均为 `0`；API `316 / 316`、Cells、生产构建与差异检查通过。冻结构建和 `5411` 当前源码均完成高度 / 国家 / 省份 × 平滑 / 硬边六态，平滑模式为 `shore-topology-snapshot + butt-join-political`，硬边为 `hard-cell-shore + butt-join-political`；六态 checksum、历史、选择与 picking 不变，WebGL、health、application console 和 page error 均为 `0`。六张截图中孤岛、狭水道、湖洞无错侧色带、长针或断口，国家 / 省份边界保持硬边。首次 `5411` 因旧 Vite 进程未重扫新增依赖而 `BLOCK`，重启为 PID `33912` 并完成依赖优化后二次验收 `ACCEPT`；本项未推送。

- **权威任务第 256 项：修正中英文 README 的项目名称。** `已完成；来源：用户直接纠正`
  - 范围：中文版项目名统一为当前的“WebGL 幻想地图生成器”，英文版使用自然对应的“WebGL Fantasy Map Generator”，删除“FMG WebGL 地图生成器 / FMG WebGL Map Generator”中由 FMG 与“地图生成器 / Map Generator”造成的语义重复；中文用词后由第 260 项更新。
  - 最小验收：两版标题和开篇自称一致、自然，不再把 `FMG` 当作项目名称前缀；对原 Fantasy Map Generator 的参考与致谢保持准确，语言切换、在线入口和文档链接不变。
  - 排除：不修改应用页面标题、包名、域名、代码命名空间、许可证或原项目名称，不借更名扩写其它 README 内容。
  - 完成记录：中文标题现为“WebGL 幻想地图生成器”，英文标题为“WebGL Fantasy Map Generator”，两版开篇同步去除 `FMG WebGL` 自称；全文仅在指向原作及致谢时保留 Fantasy Map Generator 名称。

- **权威任务第 257 项：设计并接入项目应用图标。** `已完成；来源：用户直接要求`
  - 范围：为“WebGL 幻想地图生成器”设计原创、无文字、适合小尺寸识别的应用图标；视觉语言与现有墨绿色界面、旧纸画卷和朱印保持协调。提交 SVG 主稿及浏览器 / 安装入口需要的派生资源，接入 favicon、manifest、Apple Touch Icon 和主题色。
  - 最小验收：标签页不再使用空 `data:,` 图标；SVG 在 `16 / 32 / 64px` 下主体轮廓清楚，常用位图尺寸与 manifest 声明一致；正式构建包含全部资源，直接访问无 404；桌面和窄视口不因元数据接入产生布局、console、page 或 WebGL 错误。
  - 排除：不复制或改绘原 Fantasy Map Generator 图标，不加入依赖字体或可读文字，不借本项重做加载页、应用标题、README 排版、主题系统或其它品牌资产。
  - 完成记录：原创图标以墨绿色圆角底框承载米色地图圆盘、墨绿色地貌与浅青水系，并以右下朱红方印形成小尺寸第二识别点；SVG 不含可见文字、字体、渐变或原作资产。提交 `app-icon.svg` 主稿和 `32 / 180 / 192 / 512px` PNG，正式入口已接入双 favicon、Apple Touch Icon、Web App Manifest 和 `#18231f` 主题色。
  - 验收记录：静态专项核对 SVG 结构、四份 PNG 尺寸、manifest 三项声明和入口元数据；生产构建通过且全部资源进入 `dist`。系统 Chrome 分别访问 `5411` 当前源码与 `5412` 生产构建，四个入口资源均返回 `200` 与正确内容类型，`1440 / 390 / 320px` 横向溢出为 `0`，canvas 正常、错误界面隐藏，console / page error 为 `0`。`16 / 32 / 64 / 128px` 预览经人工检查仍能辨认浅色地图圆盘与朱红角标；当前没有活动权威任务。

- **权威任务第 258 项：补齐应用图标尺寸并建立 Cloud Provider Config。** `已完成；来源：用户直接要求`
  - 范围：从第 257 项既有 SVG 主稿补齐 `64 / 256px` PNG，并同步 favicon、manifest、生成脚本与专项回归；新增独立 Cloud Provider Config，供正式应用在启动前读取 Dropbox App key、Dropbox Redirect URI 与 Google Drive Client ID。官方部署无覆盖变量时自动使用用户明确提供的公开配置，私有部署可通过同一组构建环境变量或直接替换构建产物中的配置文件启用自己的云存储，无需修改应用源码。
  - 配置契约：统一 JSON 环境变量 `FMG_CLOUD_PROVIDER_CONFIG` 优先，也接受 `FMG_DROPBOX_APP_KEY / FMG_DROPBOX_REDIRECT_URI / FMG_GOOGLE_CLIENT_ID`；现有 `VITE_FMG_*` 仅作兼容回退。运行时配置优先于旧构建变量，配置只允许公开 OAuth client identifier，不得包含 client secret、refresh token 或用户 access token。
  - 最小验收：六份 PNG 与声明尺寸一致，正式入口和 manifest 包含新增尺寸且构建产物直接访问无 404；空配置仍明确禁用 provider，运行时文件、统一 JSON、逐项部署变量和旧 `VITE_FMG_*` 的优先级有自动化覆盖；生产构建会生成规范配置文件，系统 Chrome 验证空配置与注入配置两态，云存储面板不泄露真实值，console、page 与 WebGL error 为 `0`。
  - 排除：除用户明确要求写入的三个浏览器公开 identifier 外，不提交 OAuth secret、refresh token 或用户 access token；不修改 Dropbox / Google Drive 的授权范围、上传下载、令牌持久化或 provider 业务语义，不借本项接入其它云服务、部署平台或重做图标视觉。
  - 实现记录：从既有 SVG 主稿生成 `64 / 256px` PNG，入口新增 64px favicon，manifest 新增 256px 安装图标。应用启动前同步装载可独立替换的 `cloud-provider-config.js`；Vite 从仓库根环境读取统一 JSON、新逐项变量及旧 `VITE_FMG_*`，构建后只输出三个允许字段。Vercel 对配置文件使用 `no-store`，私有部署可设置环境变量或直接替换构建产物配置文件，不需修改源码或重新打包应用。
  - 安全记录：provider state 继续只暴露字段 presence；构建规范化与运行时均拒绝把 `GOCSPX-` Google client secret 当作 Client ID，敏感字段名及值不会进入公开产物或错误输出。`.env.example` 继续保持空模板，正式配置只包含浏览器本就可见的三项公开 identifier。
  - 验收记录：六份 PNG、manifest 四项声明、配置四级来源、fixture / mock 云协议、生产构建与差异检查通过。无环境覆盖时最终产物与官方配置一致；统一 JSON 与逐项变量可以覆盖，显式统一空配置可以禁用 provider。系统 Chrome 在 `5411 / 5412` 验证官方配置已加载且正式回调受同源保护，空配置为 `providers-disabled`，私人 fixture 覆盖后两 provider 可连接；`http://localhost:5410` 使用本地回调后两 provider 均进入可连接状态。配置值不在面板回显，console、page、health 与 WebGL error 为 `0`。独立审查先后阻断构建层 secret 前缀漏检和本地分流负例缺口，修复后复审为 `ACCEPT`。
  - 部署边界：代码与构建产物已具备官方配置，但本项未推送或触发线上部署，也未启动真实 OAuth 或访问用户云盘；线上账号联调必须在发布后另行验收，不能由本地 fixture 结果替代。

- **权威任务第 259 项：修正 Dropbox 独立 OAuth 回调与授权窗口收口。** `已完成；来源：用户真实联调反馈`
  - 范围：把官方与 `localhost:5410` 的 Dropbox Redirect URI 统一改为 `/oauth/dropbox/callback`；新增不启动地图应用的同源轻量回调页，只把授权码、state 或错误送回发起授权的原窗口并自动关闭；由原窗口完成 PKCE state 校验、授权码换令牌和连接状态更新。保留既有整页回调处理作为旧配置兼容路径。
  - 最小验收：开发服务、生产构建与 Vercel 精确回调路径均直接返回轻量页面；授权小窗口不会挂载地图 canvas 或把 token / verifier 写入 URL、DOM、日志与跨窗口消息；同源、provider 与 state 校验失败时拒绝连接，正确回调后由原窗口完成 token 交换并关闭小窗口；既有 Dropbox fixture、云存储回归、生产构建及真实系统 Chrome 回调专项通过。
  - 排除：Google Drive 已由用户确认恢复正常，本项不改 Google Client ID、授权范围、GIS 流程或 Drive 文件行为；不访问真实 Dropbox 用户文件、不改 App Console 配置、不持久化 access token，不提交或推送。
  - 实现记录：官方与 `localhost:5410` 配置分别使用 `https://fmg.mosuzi.top/oauth/dropbox/callback` 和 `http://localhost:5410/oauth/dropbox/callback`。新增无外部资源的独立回调页，开发 / preview 中间件与 Vercel 精确 rewrite 均先于 SPA fallback；新旧 popup 回调只向 opener 发送 `type / provider / code / state / error`，令牌交换统一回到 opener。握手增加同源、popup source、state、十分钟期限和 in-flight 一次性门禁，弹窗被拦截时保留地图页并给出明确错误。
  - 验收记录：语法检查、云存储与 Cloud Provider Config 专项、生产构建和差异检查通过。`localhost:5410` 开发服务与 `127.0.0.1:5414` 生产预览的 Chrome 专项均确认回调页不挂载 canvas、不加载主应用资源、消息字段受限且 popup 自动关闭；旧 popup token 请求为 `0`，过期 / 过期错误 token 请求为 `0`，并发重复只产生 `1` 次交换且首条成功。首轮安全复核对旧 token 消息、期限绕过和并发竞争给出 `BLOCK`，限修后二次复核与独立终验均为 `ACCEPT`；`source/` 零改动，未启动真实 OAuth、访问云盘、提交或推送。

- **权威任务第 260 项：将中文项目名称统一为“WebGL 幻想地图生成器”。** `已完成；来源：用户直接要求`
  - 范围：把 README、正式应用标题、加载画卷、Web App Manifest、图标无障碍标题、加载概念稿及对应机器门禁中的中文项目品牌统一为“WebGL 幻想地图生成器”或适配界面长度的“幻想地图生成器”；英文正式名称继续使用“WebGL Fantasy Map Generator”。
  - 最小验收：代码、入口元数据、当前文档与品牌回归中不再使用旧中文项目名；页面标题、加载标题、manifest 与 README 一致，正式构建通过。普通语义中的“架空历史创作”等措辞保持不变。
  - 排除：不改英文项目名、包名、域名、代码命名空间、原 Fantasy Map Generator 名称，不把所有“架空”字样做机械替换，不提交或推送。
  - 完成记录：根 README、正式页面标题、加载画卷、Web App Manifest、SVG 图标标题、加载概念稿、部署说明和品牌门禁已统一使用“WebGL 幻想地图生成器 / 幻想地图生成器”；manifest 短名为“幻想地图”。英文 README 标题保持“WebGL Fantasy Map Generator”，“架空历史创作”等普通类型描述保留。
  - 验收记录：旧中文品牌词在 `source/`、构建产物和依赖目录之外搜索结果为 `0`；加载画卷、全局外壳、应用图标专项与生产构建通过。系统 Chrome `http://127.0.0.1:5410/` 确认页面标题、加载标题、manifest 名称和短名正确，五项图标 / manifest 资源均为 `200`，`1440 / 390 / 320px` 无横向溢出、错误界面、console 或 page error；`source/` 零改动，未提交或推送。

- **权威任务第 261 项：将 Google Drive 新存档默认保存到可配置文件夹。** `已完成；来源：用户直接要求`
  - 范围：Cloud Provider Config 新增 `providers.googleDrive.folderPath`，默认 `/webFMG`，同时提供统一 JSON、新逐项环境变量和旧 `VITE_FMG_*` 兼容入口。Google Drive provider 按路径逐级查找或创建应用可访问的文件夹，并把最终文件夹 ID 写入新建地图的 `parents`。
  - 兼容契约：`/` 显式表示 My Drive 根目录；支持规范化的多级绝对路径。列表同时读取配置目录与根目录中由本应用创建的旧地图，避免升级后旧存档消失；覆盖旧根目录文件时保持原位置，不做静默迁移。文件夹只在首次新建存档时按需创建，单纯连接或刷新不产生空目录。
  - 最小验收：默认 `/webFMG`、自定义多级路径、显式根目录、目录查询 / 创建 / 复用、上传 `parents`、旧根目录列表兼容、配置优先级和敏感值边界均有 fixture 回归；既有 Google 新建 / 覆盖 / 下载、Dropbox、生产构建与云存储浏览器回归通过。
  - 排除：不扩大 `drive.file` scope，不读取任意用户文件夹，不移动或删除旧根目录存档，不接入 Google Picker、共享云端硬盘或后台迁移，不启动真实 OAuth、提交或推送。
  - 实现记录：构建配置、公开运行时配置和旧 `VITE_FMG_GOOGLE_FOLDER_PATH` 回退统一规范化目录路径；Google provider 在新建前逐级查找目录，缺失时使用 folder MIME type 与父级 `parents` 按需创建，上传 metadata 明确写入最终目录 ID。连接与列表只查询目录，不创建空目录；已有同名应用目录优先复用。
  - 兼容与验收记录：配置目录存在时同时列出该目录和根目录中带应用标记的地图；旧根目录目标继续按原 ID 覆盖，不迁移。Cloud Provider Config、云存储 mock、目录延迟创建 / 多级创建 / 复用 / 根目录 / 旧文件兼容、生产构建和 `5411` 系统 Chrome 云存储两项回归均通过，桌面及 `390 / 320px` 无溢出，console、page、health 与 WebGL error 为 `0`；`source/` 零改动，未启动真实 OAuth、提交或推送。

- **权威任务第 262 项：刷新后自动恢复当前标签页的云存储连接。** `已完成；来源：用户直接要求`
  - 范围：Dropbox 与 Google Drive 成功连接后，默认把短期 access token、到期时间和 provider / 当前 origin / 配置 / scope 指纹保存到当前标签页的 `sessionStorage`；页面刷新或应用 registry 重建时，只恢复仍未过期且指纹完全一致的连接。Google Identity Services 后续取令牌不再强制 `prompt: "consent"`，复用既有授权记录并继续要求用户手势启动令牌流程。
  - 清理契约：令牌过期、云 API 返回 `401`、配置或 origin 变化、记录损坏、用户主动断开时同步清除内存与 `sessionStorage`；registry / 面板普通销毁只释放内存与计时器，不撤销或删除仍有效的标签页会话。主动断开继续尝试远端 revoke。
  - 安全边界：不增加用户开关，不保存 refresh token，不写入 LocalStorage、IndexedDB、地图、日志、错误详情或公开 API；不改变 Dropbox `online` + PKCE、App Folder、Google `drive.file`、远端文件范围和上传下载语义。关闭标签页、令牌到期或授权被服务端撤销后仍需重新连接。
  - 最小验收：两 provider 的成功写入、刷新恢复、到期 / 损坏 / 配置变化拒绝、401 清理、主动断开清理与 revoke、registry dispose 保留有效会话均有回归；Google 默认请求不含强制 consent。云存储 UI、部署说明和专题安全边界同步；Dropbox 回调、Cloud Provider Config、云存储协议、旧浏览器存储、生产构建与差异检查通过。系统 Chrome 使用 mock transport 验证同一标签页刷新前后两 provider 均保持连接，断开后刷新不恢复，console、page、health 与 WebGL error 为 `0`。
  - 排除：不做跨标签页共享、关闭浏览器后的长期登录、后端 OAuth / BFF、后台同步、自动保存、远端删除、冲突合并或真实云盘写入；不提交或推送。
  - 实现记录：共享会话管理器按 provider 生成版本化 key 与 provider / origin / 配置 / scope 指纹，只保存短期 access token 和绝对到期时间；初始化恢复、主动断开、服务端 `401`、记录损坏、指纹变化和到期计时器共用同一清理出口。registry 普通 `dispose` 只销毁内存令牌和计时器，保留当前标签页会话；鉴权请求携带发起时的 token + 连接代次快照，延迟 `401` 只有仍属于当前连接时才允许清理。Google 默认调用 `requestAccessToken()`，不再传强制 consent。
  - 复核记录：首轮独立复核以 `BLOCK` 指出 Google 旧连接的异步目录解析可能在断开或切换账号后污染新连接缓存。限修为令牌会话增加单调连接代次，并要求目录解析结果同时匹配请求对象、token 与代次后才能写缓存，二轮复核为 `ACCEPT`。最终全差异审计又以 `BLOCK` 复现两 provider 的旧请求延迟 `401` 会误清新连接；限修后 Dropbox / Google 各自的同 provider 断开重连、`dispose` 后重建四类反例均通过，原审计者复验为 `ACCEPT`。
  - 验收记录：云存储协议、Cloud Provider Config、旧浏览器存储、会话竞态反例、Dropbox 独立回调页与生产构建均通过。系统 Chrome 访问 `http://127.0.0.1:5411`，确认两 provider 刷新后保持连接、分别主动断开后刷新不恢复；云存储面板在桌面、`390px`、`320px` 下 document / content overflow 均为 `0`，console、page、health 与 WebGL error 为 `0`。既有 PID `8856` 服务未重启或停止；未启动真实 OAuth、访问真实云盘、提交或推送，`source/` 零改动。

- **权威任务第 263 项：把用户选定的 A 方案接入正式应用图标。** `已完成；来源：用户直接选择并提供原图纠正`
  - 范围：以用户选定的 A 方案为准，把现有抽象地图圆盘重绘为“海岸地形 + 山脉 + 河流 + 路线 + 城池 + 朱红方印”的微缩地图；继续使用原创无文字 SVG 主稿、现有品牌色、墨绿圆角底框与朱印识别点，并从同一主稿确定性生成 `32 / 64 / 180 / 192 / 256 / 512px` 六份 PNG。
  - 原图优先契约：用户已提供选中 A 方案的 `1280×1280` 原图作为唯一视觉真值。正式主稿与六档资源必须保留其海岸比例、双峰、分叉河流、环城虚线路线、圆形城池、树林分布、旧纸手绘质感、配色、边框与朱印构图，只允许确定性等比缩放、格式封装和为避免系统蒙版裁切而调整 manifest purpose；不得再按“方向参考”简化或重画。
  - 最小验收：仓库保留原图主稿及可核对摘要，SVG / PNG 直接从同一原图派生且视觉内容一致；正式入口、favicon、Apple Touch Icon、manifest 尺寸和主题色正确。人工对照原图检查 `16 / 32 / 64 / 128 / 256 / 512px`，生产构建与系统 Chrome 资源回归通过，三档视口无布局、console、page 或 WebGL 错误。
  - 排除：不改页面标题、加载画卷、README、主题系统、云存储配置或其它品牌资产；不复制原 Fantasy Map Generator 图标，不把预览生成图直接当作正式位图主稿，不提交或推送。
  - 返工与完成记录：首轮实现误把 A 方案当作方向性参考，改成简化矢量海岸、山峰与城池；用户以原图对照明确判定视觉不符，该轮结果已撤回。最终把用户附件逐字节保存为 `1280×1280` JPEG 母版，SHA-256 固定为 `50c20b1e8d908b0a45e91988e5421a0792d9f4570db7a504de91205851ab771d`；SVG 只自包含一份母版且回归会解码逐字节比对，六份 PNG 均由母版直接等比缩放。Manifest 的 PNG purpose 改为 `any`，避免系统蒙版裁切原构图。原图 / 五档预览人工对照一致，静态专项、等价本地 Vite 生产构建与系统 Chrome 资源 / `1440 / 390 / 320px` 回归通过，console、page 与 WebGL error 为 `0`。

- **权威任务第 264 项：统一并强化全部画布语义图标。** `已完成；来源：用户直接要求`
  - 封闭分母：城镇 `9` 个基础剪影与 `3` 个角色徽记、Marker / 资源点 `58` 个注册类型、军事 `10` 个稳定资产键，共 `80` 个稳定图形键。战线箭头、洋流箭头与远景圆点只纳入描边、颜色和缩放过渡审计；选择圆环、测量点、诊断 cell、编辑手柄、标签、HUD、图例、比例尺、地区纹理、边界和普通线层不属于语义图标，不新增当前并不存在的文化 / 宗教 / 河流 / 道路中心点图标。
  - 视觉契约：四个人口等级必须靠外轮廓一眼区分，村落为单屋、村庄为错落民居、城镇包含明确中心建筑、城市使用城墙或塔楼群；九个旧手工剪影值继续可读，首都、省会、港口仍是可组合附加徽记，不能覆盖人口规模。Marker 使用统一制图底牌、类别外框与色彩、逐类型内部轮廓三层语言，`58 / 58` 注册类型必须有明确图形键，尤其 `26` 类资源点不得继续共用少量叶子、镐头或水滴。军事保留十个键与旧字符别名，在现有标签内统一观察尺寸、描边、明暗与留白。
  - 单一来源与兼容契约：新增画布图标注册表，供生成器、renderer、Marker 管理面板、旧数据解析和回归共同派生。自动 Marker 按 `type` 采用新默认图标，不被旧存档自动写入的旧 `visual.symbol` 锁死；`manual === true` 的旧 `16` 个 symbol 与未知值安全回退继续保留。保留 `marker.icon` emoji 元数据、对象 ID、城市人口阈值与角色、资源生成和经济、军事判定、公开 API、GeoJSON、存档、picking、图层开关、碰撞优先级、标签净空、PNG 导出和主题语义。
  - 最小验收：机器门禁精确冻结 `9 + 3 + 58 + 10 = 80`，注册类型、UI 选项与 renderer 双向差集为 `0`，注册类型没有通用 fallback 或意外重复指纹；完整图鉴覆盖九个城镇剪影、角色组合、六种文化风格、58 个 Marker 与十种军事图标，并检查远景、阈值、选中、重叠弱化、碰撞、picking、DOM / PNG 同源。旧自动 / 手工 Marker、城市手工视觉、军事旧别名、JSON / gzip / 浏览器缓存通过；复跑城市、军事、PNG、选择标记、画布文字和生产构建门禁，系统 Chrome 覆盖 `1440 / 390 / 320px` 与真实 `10k` 地图，console、page、health 与 WebGL error 为 `0`。
  - 排除：不改地图数据、生成概率、对象身份、人口或经济数值，不借本项重做文字、标签、线层、图层入口、编辑 API、管理面板信息架构或导出选项，不修改 `source/`，不提交或推送。
  - 实现记录：新增唯一画布图标注册表，集中保存九个城镇基础剪影、三个角色徽记、九类底牌、`58` 个逐类型 Marker 内部轮廓、十个军事 SVG 与旧 `16` 个手工 symbol。四级自动城镇改为单屋、双屋、中心建筑群、城墙塔楼四种一级轮廓；首都、省会、港口继续组合为角部徽记。自动 Marker 按 `type` 解析实际图标而不受旧自动 symbol 锁死，手工 Marker、扩展视觉字段、九个城市旧剪影和军事十五个旧字符别名保持。Renderer、Marker 管理面板和回归共用注册表；面板对自动 `type:*` 只读展示具体类型，选择旧手工 symbol 后才允许写入，写面另有 guard，存档不新增 `type:*`。军事旧 PNG 不再进入正式 bundle，标签改为同源扁平 SVG。
  - 复核记录：首轮独立审查以 `BLOCK` 指出 Marker 面板仍显示旧自动 symbol，图鉴也遗漏三角色组合与六种文化风格；同时建议军事图鉴改用正式 DOM / CSS。限修后自动茶山显示“自动：茶山 / 资源”，图鉴保持 `80` 个稳定键并增加角色组合一张、六文化六张共 `7` 个附加展示卡，军事十键使用正式标签结构和实际 `19～20px` 观察尺寸。原审查者复验确认 `type:*` 无写回路径、旧手工兼容和正式 CSS 均成立，结论为 `ACCEPT`。两条旧静态断言只同步现行 `.zone-label` 与 PNG 生产契约常量，没有修改生产行为掩盖问题。
  - 验收记录：静态门禁固定城镇 `9 + 3`、Marker `58`（含资源点 `26`）、军事 `10`，Marker / 四级城镇 / 军事结构指纹分别为 `58 / 4 / 10`，生成器与注册表双向差集为 `0`。城市规模、标签净空、Marker 面板、军事标签布局 / 国家色 / 战线、选择标记、PNG 选项、画布文字、地图迁移、应用图标、生产构建和差异检查通过；生产构建转换 `1295` 个模块，generation worker 未打入图标注册表或旧军事 PNG。系统 Chrome 图鉴在 `1440 / 390 / 320px` 均为稳定卡 `80`、附加卡 `7`、裁切与横向溢出 `0`。真实 `10004 / 5968` grid / pack 地图中，模型与 DOM 均为城镇 `839`、Marker `44`、军事 `114`，真实 Marker 定位与拾取命中，`1440×900` PNG 与 DOM 画面同源，交互和导出前后 checksum / revision 不变；console、page、health 与 WebGL error 为 `0`。既有 `military-regeneration` 仍停在本批开始前的旧战报归档源码断言，与本批图标文件无数据流或差异交集，不转为新任务。`source/` 零改动，未提交或推送；当前没有活动权威任务。

- **权威任务第 265 项：以 A 方案统一城镇剪影，并将应用图标收敛为完整圆形地图。** `已完成；来源：用户直接纠正`
  - 应用图标范围：保留 `app-icon-master.jpg` 及既有摘要作为 A 方案原始视觉来源，新增透明圆形高保真派生母版；彻底移除外部墨绿方框和朱红方印，只保留并补全中央圆形地图。曾尝试把原图未遮挡区与局部补绘机械拼接，但因右下色阶接缝被独立审查阻断；最终完整圆盘统一派生，不再声称未遮挡像素与 JPEG 逐字节相同。`app-icon.svg` 与 `32 / 64 / 180 / 192 / 256 / 512px` 六档 PNG 从圆形母版确定性生成，入口文件名和 manifest `purpose: any` 保持。
  - 城镇图标范围：重画现有九个基础剪影而不改变键名。`city` 明确采用 A 方案圆盘内“完整椭圆环城墙、正面城门、中央尖顶高塔、墙内屋舍”的一级轮廓；`hamlet / village / town` 依次使用单屋、错落屋舍、中心塔楼与局部围墙，`capital / provincial / port / fort / camp` 保留自身语义并统一厚墨线、暖墙体、深屋顶、轻俯视和留白。三项角色徽记继续独立组合，不参与人口等级、基础尺寸或阈值计算。
  - 兼容与行为边界：保留四级人口阈值、P90、手工 `visual.silhouette`、文化调色、旧 JSON / gzip / 浏览器缓存、对象 ID、picking、碰撞、标签净空、DOM / PNG 同源、地图 checksum、revision 与历史；不迁移或重写旧图。Marker、资源点、军事、生成算法、数据 schema、API、面板信息架构、`source/`、提交和推送均排除。
  - 最小验收：原始 JPEG 摘要继续通过，圆形母版四角透明、无外框和朱印红色残留，右下补全区域没有断边或明显接缝；SVG 只内嵌圆形母版，六档 PNG 均由它生成。城镇分母保持 `9 + 3`，四个自动等级几何指纹为 `4 / 4`，`city` 结构门禁包含闭合城墙、正门、中央高塔和墙内建筑；九个剪影在同一图鉴中风格统一且四级无需颜色或文字即可区分。应用图标、图标注册表、城市规模、标签净空、旧图迁移、选择与 PNG 专项、生产构建、差异检查通过；系统 Chrome 覆盖 `1440 / 390 / 320px` 图鉴和真实 `10k` 地图，检查角色叠加、碰撞、picking、DOM / PNG 同源、资源实际字节与 favicon 缓存，console、page、health 与 WebGL error 为 `0`。
  - 实现记录：原始 `1280×1280 / 103496 bytes` JPEG 摘要继续为 `50c20b1e8d908b0a45e91988e5421a0792d9f4570db7a504de91205851ab771d`；新增 `1024×1024` RGBA 圆形母版，摘要为 `26a13c843177d3c094b8e3b7b4cc2f9b8364e20e0e07b2d4e14e99925e1ededb`。SVG 只封装该母版，六档 PNG 重新生成；入口和 manifest 全部增加 `?v=265`。九个基础城镇继续使用原键与 `34×26` 观察盒，结构指纹为 `9 / 9`，四个自动等级为 `4 / 4`；`city` 的内外环墙、正门、中央高塔与两组墙内屋舍均由实际 path 几何门禁约束，指纹会剥离全部 `data-*`、ARIA 与其它非几何属性。
  - 复核记录：首轮独立审查以 `BLOCK` 指出右下补绘接缝、favicon 回归绕过缓存、城镇唯一性可被元数据伪造。限修后弃用拼接稿、改为完整无缝圆盘；浏览器先预热实际链接缓存再重新导航，并逐项核对 SVG、32px、64px、Apple Touch Icon 与 manifest 的响应摘要；城镇指纹和城市结构改为直接几何断言。原审查者复验与独立观察使终验均为 `ACCEPT`。
  - 验收记录：应用图标静态专项、注册表、城市规模、标签净空、Marker 面板、选择标记、PNG 选项、旧图迁移、生产 PNG crop、生产构建与 `git diff --check` 通过；生产构建转换 `1295` 个模块。生产预览的五个实际图标入口均为 `200` 且命中当前磁盘摘要，SVG / 32px / 64px 四角透明、中心不透明、朱印红色像素为 `0`，圆环连续性最大相邻色差为 `49.74`。系统 Chrome 图鉴在 `1440 / 390 / 320px` 均为稳定卡 `80`、附加卡 `7`、裁切与横向溢出 `0`；真实 `10004 / 5968` grid / pack 地图中模型与 DOM 均为城镇 `839`、Marker `44`、军事 `114`，picking 命中，`1440×900` PNG 与 DOM 同源，checksum / revision 不变。console、page、health 与 WebGL error 为 `0`；`source/` 零改动，未提交或推送，当前没有活动权威任务。
  - 专题施工图：`docs/task-notes/app-and-canvas-icon-system.md`。

- **权威任务第 266 项：优化默认地图展示，并提供零操作 README 截图生成链。** `已完成；来源：用户直接要求`
  - 范围：集中 fresh session 的默认图层可见性，保留地貌、河流、道路、城市、国家名称、人口、比例尺、海岸 / 湖岸与国界等核心信息，把洋流、省份边界 / 名称、Marker / 资源点、军事 / 战线、地区、测量、地图徽记和网格等专题或截图噪声默认关闭；既有本地用户偏好继续覆盖默认值。收敛默认城市标签上限并改善国家、省份、首都和城市的默认字号、字重、间距、颜色与透明度，同时补齐本机实际可用的 Noto 简体中文字体名；旧图及用户显式样式不被重写。
  - README 展示：新增一条无需人工操作的确定性生成命令，自行启动临时静态服务和隔离的系统 Chrome，通过 CDP 驱动固定 `1440×960`、`10k`、`continents`、`12` 国、`30%` 省份、种子 `mountains-and-seas` 的地图；显式应用不含人口、资源和管理叠层的展示预设，分别从正式 PNG 合成链输出 relief 与 atlas 两张长期入库图片，并在中英文 README 接入。
  - 最小验收：fresh session 的默认图层、字体栈、默认标签参数和城市标签上限有静态 / 浏览器门禁；单命令能在没有既有浏览器会话、用户输入或网络依赖的情况下确定性重建两张 `1440×960` PNG，输出前后 checksum / revision 不变，工具可自动清理服务、Chrome 与临时 profile；README 引用存在；生产构建、标签专项、PNG 专项、系统 Chrome 视觉检查和 `git diff --check` 通过。
  - 排除：不修改地图生成算法、数据 schema、存档迁移、人口语义、标签编辑 API、用户已有图层 / 标签偏好、其它 README 文案结构或 `source/`；截图工具不得连接、刷新或关闭用户现有 Chrome / CDP 会话，不引入在线字体或其它网络依赖。
  - 执行边界：全局默认仍保留没有首层恢复开关的 `population`，全局与 README 展示的城市标签上限均固定为 `128`；README 展示预设另行关闭人口、资源等附加信息。实施期间只允许施工、专项回归与隔离 CDP 调试，独立审查和观察使终验完成后才标记完成。
  - 实现记录：新增单一 `display-defaults.js` 作为 renderer、runtime、Vue 面板与回归的 fresh 默认来源；显式旧 `localStorage` 偏好继续优先，岸线联动与 `tradeFlows` 不持久化语义保持。标签 registry 与 relief / atlas 主题同步收敛国家、省份、首都和城市层级，字体栈补入 `Noto Sans SC / Noto Serif SC`，地图级显式覆盖仍按原优先级生效。截图工具自行启动动态端口静态服务、Playwright 系统 Chrome 隔离 profile 与专属 CDP session，调用正式 `data.exportPNG` 输出 `showcase-relief-overview.png / showcase-atlas-overview.png`，不使用页面截图。
  - 复核记录：首轮独立代码复核和最终观察使均为 `ACCEPT`。系统 Chrome 验证 fresh 默认、`128` 上限、旧偏好覆盖与刷新持久化、空地图级标签覆盖、导出前后 checksum / revision 不变；正常和故障注入路径 profile 均为 `0 → 0`，存活 Chrome 中任务 profile 参数与旧 `9333` 监听均为 `0`，没有终止用户 Chrome 会话。
  - 验收记录：两张成品均为 `1440×960`，连续 clean run 摘要稳定；relief SHA-256 为 `72b860d37150b55dcea059c5f7c2002c8defcc9a680d2e8428e6544fcd503dd0`，atlas 为 `7c7998983fad64533e4f6229575ef9581e70db4a3b3068156a44d9935497d42c`。标签样式、主题、布局、政治碰撞、城市净空、地图迁移、API 兼容、控制信息架构、PNG 选项、默认标签与 PNG crop 浏览器专项、生产构建、README 静态门禁和 `git diff --check` 通过；console、page、health 与 WebGL error 为 `0`，`source/` 零改动。本机全局 pnpm 11 离线切换项目固定 pnpm 10 时受限，最终以 package script 完全相同的本地 Vite + Node 本体完成构建和出图验收；本项未提交或推送，当前没有活动权威任务。

- **权威任务第 267 项：区分 README 自然地貌与国家政治视角。** `已完成；来源：用户直接要求`
  - 范围：沿用第 266 项同一固定地图与两张 `1440×960` 文件；relief 场景固定 `height` 视图，关闭国家 / 城市等全部标签、城市图标、道路和政治边界，只保留地貌、岸线、河流与比例尺；atlas 场景固定 `states` 视图，保留国家着色、国界、国家名称、首都 / 城市、河流、道路、岸线与比例尺，继续关闭省份、人口、Marker / 资源、军事、地区和测量。
  - README：中英文表头、alt 和说明分别改为“自然地貌 / 国家视角”，不再把 relief 描述成国家与城市概览，也不再把浅色图仅描述为同一世界的主题换肤。
  - 最小验收：截图工具逐场景应用独立 `viewMode + layers`，relief 的国家 / 省份 / 城市可见标签均为 `0` 且政治边界关闭；atlas 为 `states` 视图，国家标签与城市标签均大于 `0`、省份标签为 `0`、国界开启。两张图继续走正式 PNG 导出，尺寸、checksum / revision 不变、连续运行确定性、错误面和清理门禁不退化；系统 Chrome 与人工视觉检查确认两张图职责清楚，`source/` 零改动。
  - 排除：不修改产品 fresh 默认、标签 registry、主题配色、地图生成算法、schema、用户偏好、输出尺寸 / 种子或其它 README 结构；本项不提交或推送。
  - 实现记录：截图工具新增 `relief / states` 两个完整场景契约，每个场景独立声明 theme、viewMode、完整图层矩阵和 PNG overlay；导出前依次调用正式 `layers.setTheme / setViewMode / setVisible / fitView`。旧共享 profile 被移除，动态图层稳定门禁只等待当前场景实际可见的路线 / 贸易流 / 河流，避免隐藏道路后 `routesDirty` 合理保留却导致无效超时。中英文 README 表头与 alt 已改为“自然地貌 / 国家视角”。
  - 验收记录：生产构建与隔离系统 Chrome 通过；relief 为 `height / 246090 bytes / 7b96922b8fc7a2dc0d2cf7e8cf75719dd5efe9cc76a70aeb7c634becd0405257`，可见国家 / 省份 / 城市标签 `0 / 0 / 0`；atlas 为 `states / 285591 bytes / b19943c52fa5df608ddcea8df936d70e4f04b177c50f4c59ecf8614af4468967`，可见标签 `12 / 0 / 10`。连续两次 clean run 摘要一致，人工视觉确认自然地貌无政治噪声、国家图政治着色与国界清楚；故障注入按预期失败且 profile 为 `0`，console、page、health 与 WebGL error 为 `0`。README 静态门禁、语法、`git diff --check` 与 `source/` 零改动通过；未提交或推送，当前没有活动权威任务。

- **权威任务第 268 项：地图名称、共享存档模板与 `.webfmg` 扩展名。** `已完成；来源：用户直接要求`
  - 范围：生成 Tab 新增地图名称；名称输入右侧的无边框设置图标按需展开共享存档格式、token 帮助和实时预览。模板支持 `{name}`、`{date}`、`{time}`、`{seed}`、`{checksum}`、`{ext}`，默认 `{name}-{date}-{time}.{ext}`，本地保存、高级压缩导出和云端新建共用同一持久化偏好；云端覆盖仍按原 id / path / revision 和原名执行。
  - 格式与兼容：新建原生存档采用 `.webfmg`，内部继续为 `application/gzip` 的 gzip JSON，不升级 schema；旧 `.webgl-map.json.gz / .json.gz / .gz / .json` 在导入、云端列表、gzip-base64 与两套无头 CLI 中继续兼容。旧图缺少名称时按 seed 回填，名称不进入 checksum，浏览器固定单槽存档 key 不变。
  - 验收记录：首轮独立复核阻断的换图后云端预览陈旧问题已在统一加载链修复并复验 `ACCEPT`。命名、云端、迁移、无头读写、API 往返和生产构建通过；系统 Chrome 实际捕获本地保存与高级压缩导出的同名 `.webfmg` 并 gunzip 核对，刷新持久化、默认收起、图标展开、无边框及 `1440 / 390 / 320px` 无溢出通过。2026-08-04 CDP 复验测得按钮盒与输入框中心一致、齿轮 SVG 原向上偏 `1.5px`，专属内部位移修正后两轮均为 `0px`；console、page、health 与 WebGL error 为 `0`。`source/` 零改动，未访问真实云盘。
  - 专题施工图：`docs/task-notes/map-save-naming-and-extension.md`。

- **权威任务第 269 项：调查正式画布性能并形成优化建议。** `已完成；来源：用户直接要求`
  - 范围：先写调查方案并交独立智能体审阅；审阅通过后，基于当前生产构建和系统 Chrome 调查初次生成 / 装载、连续缩放 / 平移、idle commit、颜色专题 / 图层 / 适配 / 定位 / selection / hover 状态切换，以及完整图层、消融矩阵、测量重场景和选中态重场景。必须分离 generation、renderer `loadMap()`、WebGL draw / mesh / buffer、DOM / SVG overlay、浏览器 style / layout / long task。
  - 允许的诊断改动：只在现有入口不足时补充可复现的 profile 脚本、Performance API 标记或 renderer 只读 stats；不得改变渲染输出、交互、地图数据、schema、存档、API 或默认图层。原始报告进入 `docs/generated/reports/`，长期结论进入 `docs/performance/`。
  - 最小验收：独立方案审阅为 `ACCEPT` 后才开始采样；10k / 50k / 100k 生产基线、100k 图层消融、测量 / 选中重场景和状态切换均有可复现证据或明确失败记录；实际报告逐项列出已证实瓶颈、已排除猜测、剩余盲区、指标与代码路径，并给出 `P0 / P1 / P2` 建议、预期收益、风险与长期门禁。同一审查智能体终审证据和建议一致，文档索引、权威计划、开发日志与差异检查同步。
  - 排除：本项不直接实施优化，不修改 `source/`，不优化生成算法、编辑事务、导入导出、云存储、管理面板或业务 API，不降低画质或改变 picking / 碰撞 / 选择 / 导出表现，不提交或推送。
  - 专题调查方案：`docs/task-notes/canvas-performance-investigation-plan.md`。
  - 实施记录：在 renderer 增加只读递增性能事件、父子计时边界与有界历史，在 E2E / overlay profile 中补独立进程预热、idle commit、图层消融、heavy fixture、恢复和错误门禁，并新增 startup / transitions / trace 状态脚本。生产构建后用系统 Chrome 完成冷启动 `3` 轮、E2E `9` 轮、最终 overlay `17` 轮、状态动作 `3×18` 和一份 `fit-to-view` trace；32 份非 trace 正式 JSON 全部通过实际启用的完整性 / 恢复 / 错误门禁，真实 console / page、WebGL 和 health error 为 `0`。
  - 调查结论：100k 完整图层连续 zoom / pan frame p95 三轮中位为 `176.6 / 305.8ms`，draw CPU p95 均仅 `0.2ms`，overlay p95 为 `161.6 / 122.7ms`，标签占 overlay 平均约 `93% / 94%`。同轮 no-DOM 总控把 frame p95 降到 `6.0 / 41.2ms`，隔离复测方向一致；该结果只作为可见 overlay 工作短路的诊断上界，不承诺产品优化收益。100k 装载另由 visual cell mesh + shore cache paired 中位 `9.66s` 主导；color mode、fit、labels 组合和 locate 也确认全量刷新或重复事务。
  - 审查与验收记录：调查方案先经两轮 `BLOCK` 修正冷 / 暖态、顺序、事件去重、GPU 证据边界与真实 `states` mode，第三轮 `ACCEPT` 后才采样；诊断和 harness 的证据契约也在审查通过后重采受影响矩阵。最终报告首轮终审以 `BLOCK` 要求补原始值、波动、嵌套边界、no-DOM 上界和长期门禁，二轮只剩阈值观察分类名错误；修正为 `36` 条 main-thread-long-task、`193` 条未强制阈值观察、render-frame-gap `0` 后，同一审查智能体最终 `ACCEPT`。生产构建、四个 `node --check` 与 `git diff --check` 通过；未实施优化、未修改 `source/`、未提交或推送。
  - 实际调查报告：`docs/performance/canvas-performance-investigation-report.md`。

- **权威任务第 270 项：收敛连续视口与确定性重复事务。** `已完成；来源：用户批准第 269 项方案后直接要求实施`
  - 范围：为 wheel / pan 建立每 rAF 至多一次的 viewport preview；交互中以 overlay 根节点相机变换保持标签、图标和测量视觉连续，commit 后才完整重排；`onViewChange` 分离 preview / commit；组合图层使用单一批事务；locate selection 去重并按闪烁相位更新 geometry。
  - 最小验收：固定输入的最终 camera 不变，交互期不完整重排 overlay 且 DOM 层不消失，commit 后变换清零；labels / zones 组合开关各最多一次 draw / overlay / runtime 同步；locate 保持约 `2.6s` 产品时长且 selection rebuild 数量受相位上限约束；picking、selection、测量和 PNG 结果不退化。
  - 排除：不改地图数据、标签预算、碰撞规则、默认图层、业务 API、生成算法或 `source/`。
  - 完成记录：wheel / pan 现按 rAF 合并 preview，每个输入仍立即推进 camera、版本号与 idle commit 取消；交互期 WebGL 使用最新 camera，DOM / SVG overlay 与 measurement overlay 使用已提交 camera 到当前 camera 的根变换连续跟随，commit 后清除临时变换并只完整重排一次。100k 三轮均为 zoom `18 preview / 18 draw / 0 overlay`、pan `47 / 47 / 0`，相同输入同步触发 `12` 次 wheel 时严格合并为 `1 preview / 1 draw / 0 overlay`。labels、markers/resources、zones 五成员组合均收敛为至多 `1` 次 refresh / draw / overlay；locate 保留约 `2.6s` 闪烁，最终样本为 `16 draw / 15 selection mesh / 1 overlay`。

- **权威任务第 271 项：优化装载、颜色切换与低频大事务。** `已完成；来源：第 269 项 P1 建议`
  - 范围：同次岸线构建复用相同安全探针；视觉 cell 的严格凸边界使用等价线性验证，非凸和失败边界保留完整安全回退；颜色专题复用现有 cell geometry / TypedArray，只更新颜色与确实随专题变化的岸线修正层；route / river 保持 idle 异步与版本取消。
  - 最小验收：三档 cell mesh / 岸线拓扑统计、顶点数、像素与对象保护不变；100k `cell-visual-mesh + shore-cache`、`height ↔ states` 相对合并后基线下降并报告实际值；fit、路线、河流、取消、旧图与导出回归通过。
  - 排除：不改海岸算法参数、保护门禁、线宽、picking、地图生成或数据格式，不把高风险 worker / shader extrusion 混入本批。
  - 完成记录：严格凸的已清理 cell 边界在保持相同 earcut 结果后改用线性等价验证，非凸、退化和失败路径保留原完整自交 / 覆盖 / 硬边界回退；岸线同次构建缓存相同 side-safety 探针；颜色专题复用现有 visual-cell geometry 与 TypedArray，仅重写颜色 / side 和专题相关海岸修正层。100k 三轮中 `cell-visual-mesh + shore-cache` 中位由 `9657.9ms` 降为 `7069.4ms`，下降 `26.8%`；`loadMap` 中位由 `12085.7ms` 降为 `8656.2ms`，下降 `28.4%`。100k 状态门禁中 `states / height` 分别为 `139.2 / 138.0ms`，均记录 `geometryReused=true`。

- **权威任务第 272 项：建立画布性能回归门禁并完成真实浏览器验收。** `已完成；来源：第 269 项 P2 建议`
  - 范围：在现有 profile 上增加 preview / overlay transform / 批图层 / locate 事件预算；生产构建后串行执行专项回归，并用隔离系统 Chrome 覆盖 10k / 50k / 100k 完整图层和 100k 状态动作，形成优化前后报告。
  - 最小验收：事件预算、状态恢复、checksum / revision、camera / layers / selection、console / page / WebGL / health error 全部通过；绝对毫秒按独立进程报告中位数与范围，不以单轮波动掩盖回归；`git diff --check` 与 `source/` 零改动通过，最后中文提交并推送当前分支。
  - 排除：不迁移标签渲染架构，不把 no-DOM 消融当产品目标，不声称没有 GPU timestamp 的硬件耗时。
  - 完成记录：overlay profile 新增同步输入 coalescing、preview / draw / overlay 数量、DOM 与 measurement transform 连续性门禁；状态 profile 覆盖 surface geometry 复用、labels / markers / zones 批事务和 locate 相位预算。系统 Chrome 串行完成 10k / 50k / 100k、100k measurement-heavy、100k 状态动作及三轮 100k E2E；生产构建、视口线层纯算法 / 浏览器、标签布局、selection、PNG、测量导入、海岸纯算法 / 浏览器回归均通过，checksum / revision、camera / layers / selection 恢复、console / page / WebGL 错误门禁通过。可见浏览器人工检查确认缩放中标签、图标与覆盖层不断层，commit 和适配视图正常。10k 单轮旧绝对 idle-frame `80ms` 观察线仍记录 zoom / pan `141.1 / 112.0ms`，属于报告保留的长任务波动，不影响事件预算结论，也未被伪装为硬件 SLA。实际优化报告见 `docs/performance/canvas-performance-optimization-report.md`。

- **权威任务第 273 项：合入画布性能优化并重新评估实际收益。** `已完成；来源：用户直接要求`
  - 范围：把 `codex-canvas-performance-optimization` 以双父提交合入本地 `main`，保留主线第 268 项后续光学居中修正与性能专题两边历史；在合并后的生产构建上重新运行 10k / 50k / 100k 完整图层、100k 三轮 overlay 与 E2E、100k 状态动作、measurement-heavy、关键渲染回归和可见浏览器检查。
  - 最小验收：合并提交的两父、工作树、`source/` 边界与冲突解决均可审计；结构事件预算、状态恢复、checksum / revision、console / page / WebGL 错误继续通过；100k 装载、交互 frame / overlay、idle commit 与颜色 / 组合图层 / locate 指标按合入后独立样本重新给出中位数和范围，并与第 272 项结果及优化前基线对照。
  - 排除：本项只复评，不继续实现新的性能优化，不迁移标签架构，不调整绝对阈值掩盖机器长任务，不修改地图生成、数据、schema、业务 API、默认图层或 `source/`；是否推送 `main` 由用户另行授权。
  - 完成记录：本地 `main` 以双父提交 `cd660ad` 合入性能分支，唯一文档冲突保留双方追加记录，性能代码相对专题提交不变且 `source/` 零改动。合入后 100k 三轮 zoom / pan 交互 overlay 继续为 `0 / 0`，frame p95 中位为 `82.5 / 141.2ms`；100k `loadMap`、visual cell mesh、shore cache 和 paired 中位为 `7982.5 / 2479.9 / 3988.1 / 6404.5ms`，paired 相对优化前仍下降 `33.7%`。状态动作、measurement-heavy、关键渲染 / 海岸回归、生产构建和可见浏览器通过；pan idle 中位 `596.6ms` 的尾延迟波动如实保留。详细复评见 `docs/performance/canvas-performance-optimization-report.md`；本项没有继续写性能实现，也未推送 `main`。

- **权威任务第 274 项：扩写中文 Wiki 功能手册并建立可复现截图链。** `已完成；来源：用户直接反馈`
  - 范围：以当前正式 UI、运行时和 API 契约为事实来源，重组并扩写 `docs/wiki/` 的中文人类手册。内容覆盖控制面板七个页签、五组二十四个普通用户管理入口、对象详情、云端存储、开发模式、地图专题与图层、自然与人文领域、编辑安全、存档导出、自动化、数据分析、兼容和排障；每个领域说明入口、用途、关键操作、结果与副作用、历史 / 锁定、安全边界及相关导出或 API 路由。
  - 截图与发布链：建立固定 seed、固定 `1440×960 @ DPR 1`、清空浏览器存储的隔离系统 Chrome 截图脚本，使用正式 API 构造地图与稳定 aria / DOM 入口，生成至少十个真实界面场景。长期图片随 Wiki 源稿入库，每张都有描述性 alt 与正文图注；同步审计校验清单、引用、文件、PNG 尺寸和双向差集，发布脚本递归复制 Markdown 与图片资产。
  - 最小验收：首页与侧栏能按用户任务导航；现有简页完成事实校准并按领域补齐必要页面，公开功能分母都有明确归属；全部用户操作型页面至少有一张相关截图或明确复用的界面总览，纯数据 / API 页面不为凑数强制配图。两次 clean capture 的文件摘要一致，人工确认图片无裁切、遮挡、令牌、私有地图或临时状态；`audit:wiki`、`audit:ai-docs`、生产构建、截图浏览器错误门禁、`git diff --check` 与 `source/` 零改动通过。
  - 排除：不修改地图生成、渲染、交互、schema、存档、API、默认偏好或 `source/`；不使用真实私有案例、云盘令牌或用户浏览器会话；不把远端 Wiki 套餐阻塞伪装成已解决，不公开仓库、不发布远端、不提交或推送。
  - 专题施工图：`docs/task-notes/wiki-functional-manual-and-screenshot-plan.md`。
  - 完成记录：中文 Wiki 已由 12 个简页、193 行、0 张图片扩展为 20 个页面、1544 行、14 处图片引用和 10 张长期 PNG；首页、侧栏、总览与能力矩阵按用户任务路由五个核心领域页。递归审计从当前源码核对七个页签、十二种专题、五组二十二个图层开关、五组二十四个管理入口、二十九个注册面板及 24 / 24 领域标题归属，并要求 Markdown alt / 图注、截图清单与磁盘资产三方一致。隔离系统 Chrome 两次 clean capture 的逐图 SHA-256 和总摘要完全一致，总摘要为 `d449abc847dca78b0fd619c9eb41fdf9f15ad1e09c6365b3ea72195a7dbd2771`；application console、page、active health 与 WebGL error 均为 `0`，固定地图生成的一次 `main-thread-long-task` 作为独立性能观察保留。Wiki / AI 文档 / 信息架构门禁、脚本语法、生产构建、差异检查与专属 profile 清理通过，两轮独立复核均为 `ACCEPT`；产品逻辑、`source/`、远端 Wiki、Git 提交与推送均未触碰。

- **权威任务第 275 项：修复远端 GitHub Wiki 图片裂图并补足页面级验收。** `已完成；来源：用户直接反馈`
  - 范围：保留主仓库 `docs/wiki/` 的相对图片引用，使仓库内源稿继续可直接预览；发布到 `.wiki.git` 时只改写 Markdown 图片目标为公开、稳定的 Wiki raw 资源地址。审计新增发布态转换检查，避免再次只验证磁盘资产或人为构造的 raw URL。
  - 最小验收：重新发布后，远端所有 `14` 处 `<img src>` 均来自预期 raw Wiki 资源地址并返回 `200 image/png`；十张唯一图片全部覆盖，首页和至少一个复用图片的正文页实际渲染正常；远端 Markdown 除图片目标转换外与源稿语义一致。`audit:wiki`、`audit:ai-docs`、脚本语法、`git diff --check` 和 `source/` 零改动通过。
  - 排除：不修改图片像素、截图清单、产品功能、地图数据、schema、API、默认偏好或 `source/`；不把主仓库源稿改为依赖远端 Wiki 才能显示；不扩展独立文档站。
  - 完成记录：同步脚本在发布副本中把相对图片目标转换为 `https://raw.githubusercontent.com/wiki/mosuzi/WebGL-Fantasy-Map-Generator/assets/...`，源稿保持不变；静态审计同时拒绝未转换的发布态图片。Wiki 提交 `f16e2ce25db62469bc11473b9cdecf2e80b8124e` 改写 `12` 个页面的 `14` 处引用。重新克隆确认 `31` 个远端文件齐全，Markdown 除图片目标转换外与源稿语义一致；公开读取全部 `12` 个含图页面，实际 HTML 中 `14` 处 `<img src>` 覆盖 `10` 张唯一 PNG，全部返回 `200 image/png`，错误的相对图片路由为 `0`。真实 Chrome 另确认首页图片 `complete=true`、天然尺寸 `1440×960`、实际渲染 `896×597`，视觉上完整显示。

- **权威任务第 276 项：将九类城镇图标改为方案 C 的俯视聚落平面。** `已完成；来源：用户确认概念方案`
  - 范围：保持九个基础键与 `34×26` 观察盒，把当前正面门洞、立面窗、竖直塔楼和尖顶改为严格俯视的围墙、城门缺口、屋顶平面、道路或院落。采用方案 C 的轻微不规则手绘轮廓，但按实际小尺寸删减细节；`hamlet / village / town / city` 通过院篱、断续木栅、局部城墙、完整城垣与聚落密度表达等级，`capital / provincial / port / fort / camp` 分别保留宫城、官署院落、临水开口、棱堡和营栅一级语义。
  - 最小验收：九个基础剪影继续唯一，四个人口等级无需文字或颜色即可区分；全部 SVG 登记俯视语言，`city` 至少包含闭合墙带、两个以上城门缺口、墙内道路 / 院落和多组屋顶平面，不再依赖正面拱门、立面窗或竖直尖塔。注册表、城市规模、标签净空、选择与 PNG 专项、生产构建和差异检查通过；系统 Chrome 图鉴覆盖 `1440 / 390 / 320px`，真实约 `10k` 地图检查可读性、角色徽记、picking、DOM / PNG 同源以及 console、page、health、WebGL error。
  - 兼容与排除：保留人口阈值、P90、角色徽记、文化调色、手工 `visual.silhouette`、碰撞盒、标签净空、picking、存档、schema、API、地图 checksum / revision / history 与 PNG 合成链；不修改 Marker、资源点、军事、生成算法、应用图标或 `source/`，不提交或推送。
  - 完成记录：九个基础 SVG 统一登记 `c-top-down`，村落 / 村庄使用开放院篱与断续木栅，城镇使用底部大开口的半围城垣，大城使用四段完整围合、四门缺口、十字道路、六组屋顶平面与中央院落；首都、省会、港口、寨堡、营帐分别收敛为宫城院落、官署院落、临水开口、方形棱堡与圆形营栅。正面门洞、立面窗、竖直塔楼和尖顶结构门禁为 `0`，墙带与木栅补入自身选中高亮。
  - 验收记录：注册表、城市规模、标签净空、选择、PNG、旧图迁移、生产构建与差异检查通过；九个基础指纹 `9 / 9`、四级人口指纹 `4 / 4`。系统 Chrome 图鉴在 `1440 / 390 / 320px` 均为基础 `9`、角色组合 `4`、裁切与横向溢出 `0`；真实 `10004` grid cells 地图的 `839 / 839` 个城镇均使用俯视 SVG，角色徽记 `430`，真实大城定位与拾取、`1440×900` PNG、选中墙带金色描边通过，checksum / revision 不变，console、page、health 与 WebGL error 为 `0`。既有完整图鉴回归仍在与本项无关的 Marker 定位等待处超时，该现象在本项改图前已同位置复现；本项用覆盖全部城镇验收口径的定向系统 Chrome 链收口。`source/` 零改动，未暂存、提交或推送；当前没有活动权威任务。
  - 专题施工图：`docs/task-notes/app-and-canvas-icon-system.md`。

- **权威任务第 277 项：改用方案 B 的轻量异形城镇图标并消除缩放滞后。** `已完成；来源：用户实测反馈`
  - 范围：以方案 B 的稀疏俯视符号覆盖第 276 项方案 C；九个稳定键和 `34×26` SVG 观察盒保留，但画布实际占用尺寸与碰撞 / 标签净空同步缩小。`hamlet / village / town / city` 必须使用不同的一级外轮廓，不能只靠内部细节、颜色或文字区分；`capital / provincial / port / fort / camp` 继续以不同异形轮廓表达角色，其中港口保留朝水面开放的马蹄形一级轮廓。单图形严格限制可绘制元素数量，避免回到密集聚落平面。
  - 缩放响应：城镇位置继续跟随 overlay 的即时相机矩阵；城镇自身屏幕尺寸使用一个共享、可组合的相机倍率因子，在缩放事件同一轮更新，不等待 `120ms` viewport commit，也不逐城镇遍历更新 DOM。交互预览、提交后和 PNG 合成必须收敛到同一尺寸公式，不能出现明显回弹。
  - 最小验收：九个一级轮廓路径指纹 `9 / 9`、四个人口等级 `4 / 4`，各图标可绘制元素不超过冻结上限；实际画布盒较第 276 项缩小且标签继续保持既有 `2px` 固定净空。真实 Chrome 对连续滚轮缩放记录相机、overlay 与城镇图标首帧：位置变换和共享尺寸因子在一个 animation frame 内生效，提交前后屏幕尺寸误差不超过 `0.5px`，无 `120ms` 后跳变。注册表、城市规模、标签净空、选择、PNG、viewport 预览、生产构建和三档图鉴通过，真实约 `10k` 地图确认可读性、picking、DOM / PNG 同源及四类错误为 `0`。
  - 兼容与排除：保留人口阈值、P90、角色徽记、文化调色、手工 `visual.silhouette`、图层、存档、schema、API、checksum / revision / history；不修改 Marker、资源点、军事、生成算法、应用图标或 `source/`，不提交或推送。
  - 完成记录：九个 SVG 统一登记 `b-distinct-top-down`，人口四级分别使用开放院篱、三角村落、盾形城镇和圆环大城；首都、省会、港口、寨堡、营帐分别使用八角宫城、削角长院、朝水面开放的马蹄港湾、四角棱堡和断续椭圆营栅。一级路径指纹 `9 / 9`，每枚只有 `4～5` 个可绘制元素；SVG 观察盒保持 `34×26`，画布、碰撞和标签几何同步缩至 `30×23`，缩放 `1` 时四级实际宽度约为 `20.7 / 22.5 / 24.3 / 26.1px`，既有 `2px` 标签净空保持。
  - 缩放验收：尺寸公式改为“人口等级基准 × 共享相机倍率”。滚轮交互仍立即移动整个 overlay，但用单个 `--city-icon-preview-scale` 抵消根矩阵的过度放大并直接得到当前相机目标尺寸，不遍历 `839` 个节点。生产 Chrome 单次相机倍率 `1.24608` 时，首个 animation frame 的大城宽度为 `26.32483px`，commit 后为 `26.32477px`，差 `0.00006px`；原基线从 `29.92px` 暂时跳到 `37.28px`、再回到 `30.38px` 的约 `6.9px` 回弹已消失。捕获事件到共享变量可读为 `15.2ms`，首个 animation frame 为 `25.2ms`。
  - 验收记录：注册表、城市规模、标签净空、选择、PNG 选项、viewport 预览、旧图迁移、生产构建、生产 PNG crop 与 `git diff --check` 通过。生产 Chrome 真实 `10004` grid cells 地图为 `839 / 839` 方案 B 城镇和 `430` 个角色徽记；拾取、checksum / revision 不变、`1440 / 390 / 320px` 无横向溢出，console、page、health 与 WebGL error 为 `0`。`source/` 零改动，未暂存、提交或推送；当前没有活动权威任务。
  - 专题施工图：`docs/task-notes/app-and-canvas-icon-system.md`。

- **权威任务第 278 项：采用极简方案 A 混合图形并修复平移旧坐标闪烁。** `已由第 279 项取代；来源：用户确认概念方案与实测反馈`
  - 图形范围：人口四级固定为空心小圆环、空心三角、空心菱形、空心双环；首都使用空心五角星，港口使用方案 B 的断口马蹄。省会、寨堡与营帐同步收敛为单线六边形、方框和帐篷记号，九类基础 SVG 每枚只允许 `1～2` 个可绘制元素，不使用填充、屋顶、道路、院落、阴影或纹理。保留 `34×26` 观察盒、`30×23` 画布盒和第 277 项即时缩放公式。
  - 角色组合：当 `capital / provincial / port` 已由基础主体图形编码时，不再重复叠加同名圆形角色徽记；一个城镇同时承担其它角色时仍显示额外徽记，角色数据与组合能力不得丢失。
  - 平移修复：viewport commit 对原先可见、当前因离屏或碰撞而消失的城镇，必须先把节点更新到当前相机下的真实坐标，再触发 `130ms` 淡出；不得在移除 overlay 根矩阵后让节点回到旧坐标淡出。保持隐藏对象不逐节点更新，避免扩大提交成本。
  - 最小验收：九类实际路径 / 椭圆指纹 `9 / 9`、人口四级 `4 / 4`，每枚 `1～2` 个元素且填充为 `none`；真实 Chrome 图鉴与约 `10k` 地图检查极简密度、首都五角星、港口断口马蹄及复合角色。真实中键平移必须产生至少一个从可见转隐藏的城镇，首个 commit 帧中其 DOM 坐标与当前世界投影误差不超过 `0.5px`，旧坐标残留为 `0`，淡出结束后不可见；缩放首帧 / commit 尺寸误差继续不超过 `0.5px`。注册表、标签净空、选择、PNG、viewport、生产构建、`1440 / 390 / 320px` 与四类错误门禁通过。
  - 兼容与排除：不修改人口、阈值、P90、角色数据、文化数据、手工 `visual.silhouette`、存档、schema、API、checksum / revision / history、Marker、军事、应用图标、生成算法或 `source/`；不暂存、提交或推送。
  - 专题施工图：`docs/task-notes/app-and-canvas-icon-system.md`。

- **权威任务第 279 项：把极简城镇图标迁入 WebGL 并优化平移边缘清晰度。** `已完成；来源：用户确认实施与实测反馈`
  - WebGL 图标：正式地图不再创建 `.city-map-icon` HTML/SVG 节点；九类极简图形以独立 WebGL2 program、VAO、实例 buffer 和单次 `drawArraysInstanced` 绘制，中心坐标直接使用当前相机矩阵。SVG 注册表只保留图鉴和手工选项预览，不再参与正式地图渲染。主图形固定为小圆环、三角、菱形、双环、五角星、六边形、方案 B 断口马蹄、方框和帐篷，复合角色用同批次的小型附加符号表达。
  - 连续响应：人口等级基准与相机倍率使用一份 CPU / GPU 同源的连续尺寸函数；缩放每帧只更新 uniform，不按城镇逐节点更新或等待 `120ms` viewport commit。显隐阈值在 shader 中用 `smoothstep` 过渡，碰撞目标变化也必须平滑插值；城镇实例保留视口外预热区，平移进入画面时不得重新从透明态开始。
  - 清晰制图：图标使用解析式距离场或等价的确定性矢量线条，`fwidth` 抗锯齿；默认是无模糊阴影的深色硬外轮廓加纯白内线，选中态把内线改为金色。文化调色、手工视觉字段和存档继续兼容，但国家底色不得再改变主体白线的可辨识度。
  - 平移边缘：HTML 标签按大于常见单次拖动距离的视口外 overscan 预布局，进入视口时已经清晰可见，不再在 commit 后执行 `140～240ms` 从透明到实色的边缘淡入。道路 / 河流屏幕 mesh 的旧 `96 backing px` 固定边缘缓存改为随 canvas 与 DPR 计算的交互 overscan，真实 `1440×900 / DPR 1.5` 下需覆盖约 `518 CSS px` 的测试拖动；不修改线宽、LOD、路线 / 河流数据或拾取。
  - PNG 与兼容：`PNG overlays.cityIcons` 独立开关继续有效；开启时图标来自 WebGL framebuffer，关闭时只对导出帧跳过城镇批次并立即恢复屏幕。保持人口阈值、P90、角色数据、手工 `visual.silhouette`、标签净空、选择 fallback、picking、图层、schema、API、checksum / revision / history、Marker、军事和 `source/`。
  - 最小验收：真实约 `10k` 地图的全部城镇由一个实例化 draw call 绘制，正式 overlay 中 `.city-map-icon` 数量为 `0`；连续滚轮逐帧尺寸单调、首帧生效且提交前后误差不超过 `0.5px`，阈值两侧 alpha 连续。真实中键平移约 `0.72` 相机偏移，城镇中心始终与当前投影误差不超过 `0.5px`，新进入标签在首个可见帧即为最终不透明度，道路 / 河流在 commit 前后无边缘补绘跳变。验证九类图形、角色组合、选中、拾取、标签净空、图层开关、PNG 城镇开 / 关、性能统计、生产构建、`1440 / 390 / 320px` 和四类错误；完成后按用户要求精准提交并推送当前 `main`。
  - 完成记录：正式地图的 `839` 个城镇进入独立 WebGL2 实例层，九类解析式空心图形由单次 `drawArraysInstanced` 绘制；相机帧只更新 uniform，缩放和图层 / 选择交互中的模型上传始终为 `1`，overlay 内 `.city-map-icon` 为 `0`。主体使用纯白内线、深色硬外轮廓和 `fwidth` 抗锯齿，不再使用国家色、文化墙色或阴影；SVG 只保留为图鉴 / 手工选项预览。
  - 交互记录：真实 Chrome 相机倍率从 `1` 到 `1.24608` 时，城镇连续尺寸函数在第一个 animation frame 生效，实例 draw call 同帧增加而模型 / 状态 buffer 不重传；选中态、城镇图层关 / 开、城市空间拾取和 checksum / revision 不变通过。中键平移 `316.8 CSS px` 的连续 `8` 帧均绘制完整 `839` 个实例，模型上传不变；HTML overlay 的实际矩阵平移量逐帧对齐设备像素，`17` 个视口外预热标签中 `10` 个进入首帧即为约 `0.90` opacity 且 `filter:none`，不再从透明态淡入。
  - 边缘与导出记录：道路 / 河流 overscan 改为 `0.5 ×` 最大视口边、最少 `256 CSS px`、最多 `720 CSS px` 并按 DPR 转为 backing 像素；生产 Chrome 平移预览 `12 / 12` 帧保留道路和河流，draw 平均 `1.02ms`、P95 `1.7ms`。PNG `cityIcons` 开 / 关产生不同 framebuffer 像素，关闭导出帧后立即恢复屏幕；真实 `10004` grid cells 地图、三档视口、PNG crop、生产构建及 console / page / health / WebGL 四类错误门禁通过，`source/` 零改动。
  - 专题施工图：`docs/task-notes/app-and-canvas-icon-system.md`。

- **权威任务第 280 项：收紧城镇图标视觉尺度并消除平移标签闪烁。** `已完成；来源：用户实测反馈`
  - 城镇尺度：继续使用第 279 项的 WebGL 单批实例层与连续尺寸函数，但收紧基准尺寸及相机增长上限；在常用缩放档位中，城镇主体的实际可见宽高必须小于对应名称的实测文字盒，首都、港口等角色图形也不得因外轮廓重新压过名称。九类形状、人口等级差异、角色语义、硬描边、选择、拾取和 PNG 同源保持。
  - 平移稳定：国家、省份名称纳入有界的视口外预布局，交互期间继续只变换共享 overlay 根节点，停手提交不得重新从透明态出现、闪一下再稳定，也不得在提交前后跳到不同候选位置。同步审计城市、手工、地区、Marker、资源点、军事与测量覆盖层，只有可复现同类问题才在本项限修。
  - 性能平衡：不恢复逐帧扫描全部 DOM overlay；政治标签只在初始化、图层 / 数据变化和 viewport commit 时重新布局，平移预览帧保持 WebGL 相机 uniform 与单个 overlay transform。视口外缓冲必须有明确上限，不让离屏节点扩大到无界绘制；保留路线 / 河流异步取消、overscan、LOD、线宽和游戏数据语义。
  - 最小验收：系统 Chrome / CDP 在真实约 `10k` 地图和至少三个常用相机尺度逐项测量可见城镇主体与对应名称，图标不大于名称；连续滚轮仍首帧响应、提交前后尺寸差不超过 `0.5px`，实例模型上传保持不变。真实中键往返平移逐帧记录国家、省份、城市、手工与地区标签的坐标、opacity、visibility 和候选索引，新移入政治标签首个可见帧即为最终透明度且提交前后无位置跳变；Marker、资源点、军事和测量层不出现旧坐标闪烁。记录 frame / overlay / commit 耗时、长任务与 DOM 写入，生产构建、专项回归、三档视口、PNG、拾取、checksum / revision / history 及 console、page、health、WebGL error 通过。
  - 兼容与排除：不修改人口、阈值、P90、角色数据、标签文字 / 样式配置、地图生成、存档、schema、API、游戏规则、路线 / 河流数据、Marker / 军事视觉、应用图标或 `source/`；本项不暂存、提交或推送。
  - 完成记录：城镇 WebGL 基准盒由 `30×23` 收紧为 `24×18 CSS px`，并把每座城镇名称的实际文字宽度换算为实例级最大尺寸因子；shader 在共享连续相机倍率与该上限之间取小值，仍为单批实例、逐帧只改 uniform。系统 Chrome 在 `1 / 1.5 / 2 / 2.5 / 3 / 4×` 下逐个隔离 GPU 实例并从固定 `24 CSS px` 窗口读取 framebuffer，图标 / 名称最大宽度比分别为 `0.661 / 0.874 / 0.905 / 0.905 / 0.905 / 0.935`，模型上传始终为 `1`。
  - 平移与性能记录：国家标签按最大 `720 CSS px`、省份按最大 `384 CSS px` 有界预热；只有纯平移提交保留既有政治标签候选，缩放、图层和数据变化仍可重新避让。城市、手工、地区标签沿用共享预热，Marker、资源点和军事补入有界预热；离场节点按当前投影更新，军事节点保留上一帧避让偏移。测量 SVG 世界层随预览矩阵移动，HUD 读数保持屏幕固定。`1440×900 / DPR 1.5` 中键平移 `280 CSS px` 的 `8` 个预览帧命中 `14` 个政治标签移入和城镇 / 资源 / Marker / 军事离场，候选、坐标与首帧 opacity 均稳定；两次最终专项的预览 P95 为 `3～5.7ms`、overlay commit 为 `19～22.5ms`。完整 profiler 的平移 overlay / commit 由修复前约 `51 / 180.9ms` 收敛为 `35.2 / 124.8ms`，没有恢复逐帧 DOM 重排。
  - 验收记录：城镇实例契约、尺寸、标签净空、政治标签碰撞、viewport 预览、既有缩放 / 平移、PNG crop、新增 framebuffer 比例和 overlay 平移稳定性专项、生产构建与 `git diff --check` 通过；缩放专项首次只被一次孤立的 `main-thread-long-task` 健康事件拦截，隔离复跑无功能或错误事件。完整 profiler 仍报告第 269 项已有的同帧输入合并 / draw 计数口径差异，未把它误记为本项回归。独立复核为 `ACCEPT`，`source/` 零改动；未暂存、提交或推送，当前没有活动权威任务。
  - 专题施工图：`docs/task-notes/app-and-canvas-icon-system.md`。

- **权威任务第 281 项：城镇图标再减半并去除阴影感。** `已完成；来源：用户实测反馈`
  - 范围：在第 280 项未提交的 WebGL 实例层上继续把城镇图标线性尺寸缩小约一半，名称宽度上限同步减半，避免高倍缩放重新长回上一版大小。删除厚重深色外圈造成的阴影感，改用无偏移、无模糊的对称细描边；九类主体、角色组合、选中态、连续缩放、平滑显隐和单批实例保持。
  - 最小验收：至少三个常用缩放档位从真实 framebuffer 量测图标主体，宽度约为第 280 项对应档位的一半且继续小于名称；实际 CSS / shader 不含阴影、滤镜或偏移，深色外沿与白色线芯均明显收细。使用同一张真实地图、同一观察中心输出多档缩放截图供用户评审；生产构建、城镇实例、缩放响应、PNG 与四类错误门禁通过。
  - 排除：不修改政治标签、Marker、资源点、军事、测量、道路 / 河流、地图数据、人口、角色、存档、schema、API、游戏规则、应用图标或 `source/`；视觉评审完成前不暂存、提交或推送，之后只按用户另行授权发布。
  - 完成记录：基准盒最终由第 280 项的 `24×18` 收紧为 `10.5×8 CSS px`，名称驱动的轮廓上限同步由 `9～20px` 收紧为 `4～9px`；深色外沿与白色线芯改为无偏移的约 `1.8 / 0.9px` 对称细描边，源码继续拒绝 shadow、filter 和偏移投影。系统 Chrome 在 `1 / 1.5 / 2 / 2.5 / 3 / 4×` 逐实例读取 framebuffer，最大图标 / 名称宽度比为 `0.331 / 0.422 / 0.452 / 0.452 / 0.452 / 0.452`，实际可见宽度为 `8.67～10px`，模型上传保持 `1`。
  - 验收记录：同一真实地图和缩放中心分别输出高度背景及国家背景的适配 / 中景 / 近景截图；用户查看国家视图中景与近景后要求提交推送，视为当前视觉确认。城镇实例、标签净空、实际 framebuffer 比例、缩放首帧响应、生产构建和差异检查通过；截图只保存在任务临时目录，不进入长期资源。`source/` 零改动，提交前当前没有活动权威任务。
  - 专题施工图：`docs/task-notes/app-and-canvas-icon-system.md`。

- **权威任务第 282 项：消除缩放与平移中的文字错位闪烁。** `已完成；来源：用户实测反馈`
  - 交互生命周期：平移从导航指针按下持续到松开或取消，期间只做 WebGL 预览与共享 overlay 变换，不得被固定静默定时器提前提交；松手后只提交一次。滚轮按连续事件序列收束，保留版本取消，旧提交不得覆盖新相机。
  - 文字缩放：HTML 标签锚点继续随地图矩阵移动，但文字、Marker、军事等固定屏幕尺寸内容不得随 overlay 根矩阵同比放大。使用共享预览比例与反向缩放，不在预览帧逐项扫描、碰撞或改写全部 DOM；测量 SVG 世界几何继续随地图缩放，HUD 读数保持屏幕固定。
  - 提交稳定：预览矩阵移除与新相机坐标写入必须在同一视觉提交中收敛；平移和缩放序列内保留仍有效的政治标签候选，缩放级别新启用的城市标签不得以 `viewport-entering` 满透明度整屏瞬现。标签密度、碰撞和显隐只在交互结束后重算一次，并对缩放阈值使用有界滞回或等价连续过渡。
  - 最小验收：真实约 `10k` 地图中，慢速中键拖动每步间隔超过旧 `120ms` 时，松手前 viewport commit 为 `0`、松手后为 `1`；连续缩放预览中文字包围盒宽高相对正式字号误差不超过 `1px`，预览到提交的既有标签锚点误差不超过 `1 CSS px`。同一手势内既有政治标签候选不跳变，单标签不反复显隐；预览 P95 不超过 `8ms`、overlay commit 不超过 `35ms`，生产构建及 console、page、health、WebGL error 通过。
  - 兼容与排除：不把中文文字迁入 WebGL，不修改标签文字、字体与用户样式、地图数据、生成、存档、schema、API、游戏规则、城镇实例、路线 / 河流数据或 `source/`；本项不暂存、提交或推送。
  - 完成记录：中键 / 右键平移改为显式指针生命周期，按住期间不再安装静默提交计时器，松手后以一次短延迟提交收束；滚轮继续使用版本取消并延长为连续事件防抖。HTML overlay 保留共享根矩阵，标签内容以共享反比例抵消字号缩放，政治标签保存既有候选、弯曲字形和屏幕偏移快照；国家名透明度预览与提交共用同一连续函数，缩放新进入标签改为淡入。手工 / 地区标签的命中、选中底板和 PNG 文字读取同步迁移到内容节点，Marker 与军事固定尺寸内容使用同一反比例机制。
  - 验收记录：系统 Chrome 在 `1440×900 / DPR 1.5`、初始 `1.5×` 缩放上连续滚轮至 `2.424×`，保留 `56` 个既有标签并核对 `44 / 80` 个 Marker / 军事固定尺寸内容；最大字号包围盒误差 `0.000244px`、预览到提交锚点误差 `0.379px`、政治候选切换 `0`。中键五步慢速平移每步间隔 `170ms`，按住期间提交 `0`、松手后提交 `1`，最大锚点误差 `0.000031px`；预览 P95 `2.9ms`、overlay 更新 `22.5ms`，console、page、health 与 WebGL error 为 `0`。标签样式、政治碰撞、城市净空、PNG crop、生产构建、语法与差异检查通过；手工标签真实拖动 / 取消已越过旧直接操控专项，完整旧专项随后停在与本项无差异交集的城市表格列宽把手命中问题，不扩展修复。`source/` 零改动，未暂存、提交或推送；当前没有活动权威任务。
  - 专题施工图：`docs/task-notes/canvas-performance-optimization-plan.md`。

- **权威任务第 283 项：收细道路线宽并恢复地图视觉层级。** `已完成；来源：用户实测反馈`
  - 调查结论：默认主题道路世界宽度为 `primary / secondary / minor = 1.8 / 1.15 / 0.7`，主路是国界世界宽度 `0.36` 的 `5` 倍，且默认不透明度 `0.88` 明显高于国界 `0.34`。系统浏览器在同一国家视图量得 `500 / 200 / 100 千米` 三档主路约为 `1.60 / 3.07 / 5.10 CSS px`，中近景路网压过城镇名称、河流与政治边界。
  - 范围：仅把三档道路世界宽度收为 `0.75 / 0.48 / 0.28`，保留道路颜色、透明度、连续世界宽度投影、亚像素 alpha、路线数据与路径几何。选中路线固定 `2.4 CSS px` halo、`7px` 拾取容差、异步取消、交互预览和 PNG 共路径均保持。
  - 最小验收：同一国家视图 `500 / 200 / 100 千米` 三档主路目标约为 `0.67 / 1.28 / 2.12 CSS px`，次路约为 `0.43 / 0.82 / 1.36px`，支路约为 `0.25 / 0.48 / 0.79px`；三级单调层级、亚像素连续透明度、DPR 语义、选中高亮与拾取保持。道路宽度、选择高亮、PNG 与视口动态线层专项、生产构建、真实系统 Chrome、四类错误与差异检查通过。
  - 排除：不修改道路生成、路线类型 / 等级、地图数据、存档、schema、API、主题颜色、河流、政治边界、城镇、标签、游戏规则或 `source/`；验收后按用户授权提交并推送当前 `main`。
  - 完成记录：三级道路世界宽度已收为 `0.75 / 0.48 / 0.28`。系统 Chrome 在同一国家视图把比例尺分别校准为 `500 千米 / 168px`、`200 千米 / 129px`、`100 千米 / 108px`，换算主路约为 `0.67 / 1.28 / 2.14 CSS px`、次路约为 `0.43 / 0.82 / 1.37px`、支路约为 `0.25 / 0.48 / 0.80px`；远景道路保持轻量可辨，中近景不再压过国家名称、城镇、河流与政治边界。道路宽度、选择高亮、PNG 选项、视口动态线层、生产构建、语法、干净 Chrome 与差异检查通过，干净页 console、页面、health、WebGL error 和横向溢出均为 `0`；完整路线编辑专项仍停在与本项无差异交集的既有撤销夹具字段不一致，不扩展修复。颜色、透明度、连续投影、选中 halo、拾取、路线数据、地图 schema 与 `source/` 均未改；当前没有活动权威任务。

- **权威任务第 284 项：100k 地图存档、编辑与渲染性能专题。** `已完成；调查、方案、284-A～284-E与统一验收均完成，待按日期归档`
  - 专题文档：[`docs/task-notes/task-284-100k-performance-investigation-and-plan.md`](./task-notes/task-284-100k-performance-investigation-and-plan.md)。本次用户授权把原文字交互范围扩展为 284-A 存档编码 / 浏览器存储、284-B 高度编辑输入 / 脏区 / 刷新、284-C renderer / overlay / picking、284-D 导出 / 撤销重做 / 面板刷新、284-E 统一 CDP 基线与发布门禁；严格逐项实施。
  - 现状证据：`99846 grid / 54634 pack / continents / full layers / 1280×820` 下，当前版本四个独立系统 Chrome 进程的缩放 / 平移 frame P95 中位为约 `97.1 / 76.5ms`，直接父提交三轮中位约为 `41.3 / 53.1ms`。交互期完整 overlay 仍为 `0`，最终 commit、dirty 清理、checksum / revision / camera / layers 恢复和错误面均正常；当前不是逐帧 DOM 重排回归，而是约 `2521` 个标签、Marker 与军事内容节点参与反向缩放及统一 `will-change: transform` 后的样式 / 合成压力。
  - 已知独立问题：`12` 个同帧输入稳定产生 `3 draw / 1 viewport preview`，在第 282 项直接父提交同样复现，不归因于文字修复；平移 `draw` 相对 `viewport preview` 多 `0～4` 次，两版本范围重叠但当前偏高，需在正式实现前用事件序号和 Chrome trace 继续拆分。
  - 预定范围：保留第 282 项无错位、固定屏幕字号、政治候选稳定和手势结束单次提交语义；优先限制反向缩放与 `will-change` 到实际可见 / 预热节点，修复同步输入 rAF 合并，避免恢复逐帧扫描全部 DOM或把中文文字迁入 WebGL。
  - 最小验收：固定同一 100k 地图和完整图层，至少五个平衡交错的独立系统 Chrome 进程；交互期完整 overlay 为 `0`，同步输入每 rAF 至多一次 preview / draw，平移 draw 不超过有效 preview，缩放 / 平移 frame P95 相对本项当前基线显著下降。10k 文字稳定性、100k 最终 commit、PNG、标签命中与四类错误通过，地图数据、schema、存档、API、生成和 `source/` 不变。
  - 调查结果：精确用户页已取到保存 quota 错误、100k 高度 brush 延迟 / 触碰 cell / 长任务和初始 100k renderer / overlay 证据；10k / 100k 隔离 profile 复现加载、长任务和同帧多 draw。调查中精确页曾短暂显示 10k、checksum `e63d4fa5`、undo / redo 为 `0 / 0`；用户随后恢复原 100k，当前只读核对为 checksum `28eede3c`、生成输入 `100000`、WebGL error `0`。284-B 的隔离交互回归已将100k连续长拖 pointer / 反馈 P95 收敛至 `38.3 / 27.8ms`，交互 health 长任务为 `0`；284-C 双规模 viewport preview / overlay / picking 回归已通过；284-D 已将压缩结果 base64 改为原生 Blob/FileReader 优先，100k base64 路径耗时约 `5.24s`，显式回收后堆增量约 `60KB`，存档与导出兼容回归通过。
  - 实施门禁：用户已确认恢复原 100k 页面并主动保存；仍不在用户标签页点击保存、恢复、重做、细分、生成或导入。284-E 继续只用隔离上下文统一验证，用户页只做最终只读核对。
  - 子任务最小验收：284-A 旧 / 新存档往返与 quota 降级；284-B 高度 pointer-to-feedback、dirty / derived、取消 / undo / redo；284-C 10k / 100k viewport / overlay / picking / 标签与 PNG；284-D 导出 / 历史 / 面板峰值与输出一致；284-E 五组交错系统 Chrome、错误面、checksum / schema / save 兼容与真实视觉。每项只改授权边界内文件，失败可回滚到该子任务旧路径。

- **权威任务第 285 项：收束同一连续陆地上的孤立道路碎片。** `已完成；来源：用户要求列入并尝试修复，使用 CDP 验证后截图确认`
  - 问题证据：当前 pack 路线先为首都生成国家主路，再按省份独立生成支路；共享 `connections` 只提供复用成本，没有最终连通性收口。因此同一连续陆地上会保留大量仅含 `1～2` 条边的省内 `trail` 分量，并非单纯由岛屿、海峡或渲染断裂造成。
  - 实施范围：只在新生成 / 显式路线重生成阶段，对同一 land feature 内包含城镇、但未接入该 feature 主道路分量的陆路碎片补成本最低的陆地连接；不得跨海、穿越不可通行地形、改写锁定路线或修改既有地图加载结果。连接仍使用现有 A* 成本、路线 metadata、ID 分配、pack 镜像与 cell 链接。
  - 最小验收：固定 `continents` 种子的 10k / 50k / 100k 生成中，同一 land feature 的陆路碎片显著收敛；没有条件接入的独立岛屿 / 无主干陆地允许保留。锁路、路线编辑、地图迁移和生产构建通过，地图 schema、API、道路视觉宽度、城市 / 河流 / 政区生成与 `source/` 不变。
  - 浏览器验收：使用真实 Chrome CDP 在国家视图检查至少中景、近景两档，确认补路实际进入 WebGL 画布、没有跨海直线、道路层无 console / page / health / WebGL error；截图只保存在 `Z:\tmp\codex\2026-08-05\road-connectivity-285`，待用户确认，不进入长期资源。
  - 完成记录：陆路按实际 land feature 分组，省内至少已有两座城镇、确实会形成道路的组才允许接入当前陆地道路网，避免给单城镇组凭空增路；连接继续复用现有 A* 成本和已占用边。固定 10k / 50k / 100k `continents` 样本分别得到 `507 / 718 / 1021` 条陆路、`1628 / 4451 / 8171` 条边，所有有路 land feature 的最大分量数均为 `1`，过水陆路与跨 feature 陆路均为 `0`。锁路生成专项、锁城 / 路线专项、语法、生产构建和差异检查通过；完整路线编辑专项仍停在第 283 项已经记录的既有撤销镜像字段差异，本项没有扩大修复。系统 Chrome CDP 在 `1280×720` 国家视图完成中景与近景缩放检查，页面 error 与可见错误面均为 `0`；启动生成阶段记录 `3` 个既有 long-task 警告，地图随后正常进入 `map-ready`。用户确认截图并授权提交推送，第 285 项正式结项；地图 schema、API、道路视觉宽度、城市 / 河流 / 政区生成和 `source/` 均未改，当前没有活动权威任务。

- **权威任务第 286 项：修复对象详情无效编辑并接通道路 / 河流路径调整。** `已完成；来源：GitHub issue #4，用户要求使用新分支解决并通过 PR 合入`
  - 问题证据：对象详情把除贸易流外几乎所有对象统一显示为“可编辑”，但运行时多数只写入 `editingObject` 和交互锁，没有打开领域面板或进入实际画布工具。道路因此锁在对象详情且没有路线草稿，河流虽允许河流面板却没有从详情打开；区域等无正式编辑器对象也会出现纯状态按钮。关闭详情又会清理编辑态，形成 issue 描述的“进入编辑但无法操作”。
  - 实施范围：建立对象详情显式动作能力表；只有名称内联编辑或已有正式领域面板 / 画布工具的对象才显示编辑入口，按钮文案区分“编辑名称”和“打开某领域编辑”，不再用通用 `editingObject` 冒充功能。道路直接进入既有路线编辑草稿并可在地图选择改线点；河流直接进入河流面板，并新增只修改成品河道折线、不改变水文 cell / 父河 / 流域 / 流量的可撤销视觉控制点。其它对象路由到真实领域面板，未实现对象隐藏编辑按钮；定位和名称库按钮也必须由显式能力门禁覆盖。
  - 安全边界：河流视觉控制点只改变 `river.points` 与显示长度，水文拓扑继续以 `river.cells` 为权威，不允许把河道 cell 任意拖过分水岭；地图 schema 继续兼容既有 `points`，不新增静默迁移。路线继续复用 `inspectRouteEdit / createEditRouteCommand`；所有实际写入经过编辑历史，关闭 / Escape / 换图必须结束一次性工具。不得修改生成算法、道路连通规则、河网生成、API 稳定面、旧图加载或 `source/`。
  - 最小验收：对象详情支持的每种按钮都有可观察结果，不支持的对象不显示无效入口；直接点击道路后可一键打开路线草稿、在地图选改线点并应用 / 撤销，直接点击河流后可一键打开河流面板、添加河道视觉控制点并撤销。对象详情、选择路由、交互锁、路线编辑、河流编辑、一次性画布模式、地图往返和生产构建通过；真实 Chrome / CDP 覆盖道路与河流各一条完整操作链，console / page / health / WebGL error 为 `0`。完成后提交中文 commit，推送独立 issue 分支，创建 PR 关联并关闭 issue #4，检查通过后合入 `main`。
  - 完成记录：对象详情改用覆盖全部 `20` 类对象的显式动作策略；道路详情直接打开既有路线改线草稿，河流详情直接打开河流面板，并提供一次性“在地图添加河道控制点”工具。河流命令只修改 `river.points` 和显示长度，水文 cells、父河、流域、flux / discharge 保持不变且支持撤销 / 重做；区域与贸易流不再显示无写入能力的编辑按钮。实施中同时修复路线历史镜像遗漏 `from / to / fromProvincial / toProvincial / administrativePriority` 导致撤销不精确的既有问题。
  - 验收记录：对象详情专项覆盖全部对象种类、河流控制点和能力路由；路线编辑、选择动作 / 面板策略、30 种画布模式、14 条核心流 / 130 个动作、30 种直接操控模式 / 90 行审计、地图迁移、API 往返和生产构建全部通过。真实 Chrome 验证道路详情可直接打开完整路线表单，改线预览、应用和撤销成立；河流面板为真实河流添加控制点后折线由 `6` 段增为 `7` 段，撤销恢复为 `6` 段。应用 console / page / health / WebGL error 为 `0`；只保留地图加载时既有 main-thread long-task 警告。地图 schema、API、生成算法、旧图加载和 `source/` 均未改。

- **权威任务第 287 项：把重生成锁定共享选择动作收束为紧凑图标栏。** `已完成；来源：GitHub issue #6，用户要求独立分支调查、修正、审查、验证并通过 PR 合入`
  - 问题与范围：共享 `UiRegenerationLockActions` 当前把列表多选、地图多选、锁定、解锁和清空全部渲染为纵向文字按钮，在多个管理面板中占用过多正文高度。本项只调整共享动作栏的按钮表达与响应式布局，保留“已选 N 项”和全部五个独立动作，不合并按钮、不移除功能。
  - 交互与无障碍：五个动作改为互不混淆的紧凑图标按钮；列表 / 地图模式继续提供随状态变化的 `title`、`aria-label` 与 `aria-pressed`，锁定、解锁、清空在空选择时继续禁用。自动化定位优先依赖稳定 `aria-label`，不再读取按钮可见文字。
  - 最小验收：共享动作栏默认保持一行，窄面板可安全换行；动作间距不小于 `8px`，图标在桌面与窄视口中均可辨识且不裁切。相关 Vue 模板静态契约、重生成锁定 UI 专项、生产构建和真实 Chrome 覆盖桌面 / 窄视口、五项动作、动态模式文案、禁用态及四类错误；不修改选择 / 锁定语义、业务运行时、地图数据、schema、API 或 `source/`。
  - 完成记录：五个共享动作收为 `32×32px` 图标按钮，桌面、`390px` 与 `320px` 均保持同一行和 `8px` 间距；列表 / 地图模式使用动态可访问名称与按下态，锁定、解锁、清空继续保持空选择禁用。审查首轮阻断的垃圾桶歧义已改为非破坏性的圆形关闭图标，复核为 `ACCEPT`。
  - 验收记录：重生成锁定 UI 专项、两份相关脚本语法、生产构建与 `git diff --check` 通过。生产构建浏览器确认 `21` 行国家列表、五按钮 `32px`、工具栏高度 `32px`、`390 / 320px` 横向溢出均为 `0`；列表多选后计数 `0 -> 1`，三项写入按钮启用，锁定 / 解锁 / 清空闭环恢复为 `0` 且禁用态正确，控制台、health 与可见错误面为 `0`。运行时会话、地图数据、schema、API 与 `source/` 未改。

- **权威任务第 288 项：让海路使用独立白色线条。** `已完成；来源：GitHub issue #7，用户要求独立分支调查、修正、审查、验证并通过 PR 合入`
  - 问题与范围：正式 renderer 当前只按 `route.level` 从主题读取三级道路颜色，因而 `route.type === "searoute"` 的海路继续使用次级道路的棕色，海面上难以与陆路区分。海路 canonical 路径会保留港口所在的陆地边界 cell，不能把整条路线粗暴染白；本项仅在正式渲染期把未选海路按最终视觉海陆面分色，水面片段为明确白色，陆地 / 港口片段沿用对应 level 的主题色。道路与小径继续使用现有主题色，选中态继续整条金色，三级世界宽度仍只按 `level` 决定。
  - 实现边界：复用 surface 已写入的同源像素 depth 遮罩；陆地、水面与路线深度分别为 `-0.5 / +0.5 / 0`，未选海路的棕色 / 白色同几何 batch 分别以 `GREATER / LESS` 通过最终视觉陆地 / 水面。“水面”沿用统一 surface side 语义，包含海洋与湖泊，不另造 ocean-only mask。平滑开启时自然跟随 shore surface correction / cover，关闭时自然跟随硬 cell surface；不做 CPU 岸线求交，也不裁切或写回 canonical points / cells。
  - 最小验收：focused regression 必须执行正式样式、mesh batch 与 draw helper，覆盖跨岸线和两端港口夹具，断言硬边 / 平滑两态的 depth 交点随最终 surface 移动、海白陆棕、选中全金、道路 / 小径颜色不变、三级 `worldWidth` 和输入 points / cells 不变；静态锁定 surface 先于路线、`GREATER / LESS`、depth 状态恢复及 picking 继续读取 canonical points。路线样式专项、线宽投影、海岸回归、生产构建、真实 Chrome route #530、语法和 `git diff --check` 通过。
  - 排除：不修改路线生成、路线 points / cells、海岸平滑算法、主题用户配置结构、地图 schema、API、存档 / 迁移、线宽、透明度、层级、拾取或 `source/`。完成后提交中文 commit，推送独立 issue 分支，创建 PR 关联并关闭 issue #7，检查通过后合入 `main`。
  - 完成记录：首轮按 `searoute` 整条染白被真实 route #530 反例与独立审查阻断；限修后未选海路以同位置、同透明度的棕色 / 白色双 batch 复用最终 surface depth，视觉陆地 / 港口段保持原等级主题色，视觉水面段为白色。严格 `GREATER / LESS` 遮罩自动跟随硬边和平滑 correction / cover；遮罩 batch 后无条件恢复 GL depth 状态，普通道路、小径与选中整条金色海路最后绘制。第二轮独立复审为 `ACCEPT`。
  - 验收记录：路线样式、线宽投影、viewport line preview、选择高亮、视觉主题、海岸回归、相关语法、生产构建与 `git diff --check` 通过。生产 Chrome 的 `10k` 固定图包含 `672` 条路线、`148` 条海路；route #530 定位后选中全线金色，取消后视觉水面海路为白色且陆地道路保留棕色，平滑边界开 / 关均无错位。`390 / 320px` document 横向溢出为 `0`；实际 1x / 2x PNG 分别导出 `1600×900 / 3200×1800`，浏览器错误日志与可见错误面为 `0`。路线数据、拾取、schema、API、存档和 `source/` 未改；完成后当前没有活动权威任务，第 284 项继续暂缓。

- **权威任务第 289 项：重整河道视觉控制点为先预览后确认。** `已完成；来源：GitHub issue #9，用户要求独立分支调查、修正、审查、验证并通过 PR 合入`
  - 问题与范围：当前“在地图添加河道控制点”进入一次性画布模式后，单击任意 pack cell 会直接把该点插入最近河段并写入历史；面板既不显示候选点与原河道的关系，也没有明确确认步骤，远离河道的点击还可能形成难以预期的剧烈折弯。动作使用的 `⌁` 图标同样不能明确表达“路径节点”。本项只重整既有河流视觉折线编辑交互，不扩展为水文拓扑编辑器。
  - 交互语义：选择河流后进入候选点拾取；单击有效候选点只生成地图预览和面板草稿，不修改 `river.points`、显示长度或历史。面板必须明确展示插入河段、pack cell、候选距离和新长度，并提供“应用控制点 / 重新选择 / 取消”；只有应用才写一条可撤销历史。取消、Escape、面板关闭、目标河流切换 / 删除、换图、无效候选和过远候选都不得写数据或历史，预览必须同步清除。
  - 视觉与安全边界：地图预览使用独立选择层绘制暖金 / 橙色候选折线和醒目候选点，不改主 river buffer，也不进入 PNG。候选点必须受与地图尺度相称的最大偏移约束，避免静默剧烈折弯；河流 `cells`、父河、流域、flux / discharge 继续保持权威且不变。原暧昧图标替换为明确的节点连线路径图标，保留可访问名称。
  - 最小验收：focused regression 覆盖 inspect、草稿、应用、取消、重新选择、重复 / 无效 / 过远候选、单条历史、撤销 / 重做、水文与 canonical cells 不变、预览清理和图标 / 文案；直接操控审计、相关脚本语法、生产构建和 `git diff --check` 通过。真实 Chrome 覆盖完整“拾取 → 预览 → 确认 → 撤销”及取消反例、桌面与窄视口和四类错误。完成后使用中文 commit 推送独立 issue 分支，创建 PR 关联并关闭 issue #9；合入后删除本地与远端修复分支。
  - 完成记录：控制点模式现在以正式 session 保存候选草稿；单击只做预检并在选择层绘制橙色候选折线与菱形节点，后续单击替换或清空候选，只有面板“应用控制点”才执行一条可撤销命令。草稿绑定地图身份和河流 `points / length` 指纹，应用前同步重新预检；候选距离以 pack 点云尺度为主并受局部河段约束，异常 metadata 不能放宽或收紧门禁。PNG API 在异步导出期间按 revision 临时抑制未保存预览，期间取消、新候选或异常不会复活旧预览；普通 selection / highlights、河流水文 cells、父河、流域、流量、schema、API 方法契约、存档和 `source/` 未改。
  - 审查与验收：首轮独立审查以 PNG 预览泄漏、无效点击残留旧草稿、异常 metadata 绕过距离线、Apply 基线竞态和生命周期测试不足五项 `BLOCK`；限修后同一审查员复验为 `ACCEPT`。focused、PNG options、对象详情、30 个画布模式、90 项直接操控审计、7 个相关语法、1303 modules 生产构建和 `git diff --check` 通过。生产浏览器的河流 #67 完成 `16 → 17` 河段、撤销 `17 → 16`、重做 `16 → 17`，最终再次撤销恢复；重复 / 过远候选、Escape、面板取消和关闭均清理草稿而不改河流。橙色预览、状态说明与 `●+` 图标清楚可见；`390 / 320px` 文档横向溢出均为 `0`，三按钮完整单行且可滚动到达。干净生产页 console / page 可见错误为 `0`、加载状态为 `ready`、可见 alert 为 `0`；首个重负载验收页仅记录一次既有 `main-thread-long-task` 性能事件，不归因于本项。

- **权威任务第 290 项：按最终视觉岸线裁切河口并阻止控制点进入水域。** `已完成；来源：用户实测反馈与调查确认`
  - 问题证据：生成器虽然把河口终点裁到原始陆水 cell 的共享边，并让最后一段跳过 meander，但平滑后的最终视觉岸线允许在保护门限内偏离该共享边；正式河流屏幕 mesh 的末端方帽还会沿切线继续伸出半个线宽。地表已经把最终视觉陆地 / 水面写入 depth，河流绘制却在关闭 depth test 后整批覆盖，因而平滑开 / 关时都不能保证河道像素止于可见岸线。现有折点 inspector 只检查最近河段距离，不拒绝水域 cell，也不比较新折线是否额外穿过开放水体。
  - 实施范围：正式河流及其选中高亮、有效折点预览复用地表同源 depth，仅保留最终视觉陆地侧像素；不得用 CPU 重新求岸线交点或写回 `river.points / cells`。折点候选必须位于陆地，并以原插入河段为基线拒绝新增的穿水路径；合法河口末段原有的边界采样不得被误判。所有 depth 状态必须在绘制后恢复，PNG 与实时画布继续同源。
  - 最小验收：focused regression 直接执行正式河流 draw helper、选择层分段和折点 inspector，覆盖硬边 / 平滑两态的河口像素裁切、普通 / 选中 / 有效预览、depth 状态恢复、水域候选和穿水候选拒绝、陆地候选应用与撤销，以及 canonical points / cells / 水文属性不变。河流折点、选择高亮、海岸、PNG、生产构建、相关语法和真实 Chrome 通过；console / page / health / WebGL error 为 `0`。
  - 排除：不修改河网生成、河口 canonical 坐标、岸线平滑算法、河流宽度 / 颜色 / 层级、地图 schema、API、存档 / 迁移、拾取、游戏规则或 `source/`。完成后只显式暂存本项文件并中文提交，不推送。
  - 完成记录：普通河流、河流 selection / highlight 和有效折点预览现统一在 surface 完成最终硬边或平滑陆水 depth 后，以 `GREATER` 只绘制视觉陆地侧像素；非河流 selection 仍走普通批次，helper 在有 / 无 draw range 时都恢复 `DEPTH_TEST` 关闭、`depthMask(false)` 和 `LESS`。河流 canonical `points / cells`、水文父河 / 流域 / 流量及地图 checksum 均不因渲染裁切改变。折点 inspector 先拒绝水域 cell，再比较原河段与候选两段采样经过的水域 cell 集合；新增任何水域 cell 即拒绝，只在同一原有河口水域内允许一个采样间距级的暴露差。
  - 审查与验收：独立审查首轮用“总穿水长度相近但改穿另一水域 cell”的精确反例判定 `BLOCK`；改为水域 cell 集合门禁并同时固定合法河口反例后复验 `ACCEPT`，独立观察使终验也为 `ACCEPT`。河口 focused、河道折点、选择高亮、路线样式、viewport line preview、PNG options、`10k / 50k` 正式海岸 pure 回归、系统 Chrome viewport line preview、1303 modules 生产构建和差异检查全部通过；硬边 / 平滑夹具的可见像素分别为 `2 / 3`，水文与 canonical 数据不变。真实 `10k` 地图定位入海主河 #14，普通河流与红色选择态均止于最终白色岸线，应用 console error 为 `0`。

- **权威任务第 291 项：重做河道折点模式说明并让错误在河流面板就地可见。** `已完成；来源：用户实测反馈与调查确认`
  - 问题证据：桌面实测中河流面板覆盖了左下画布工具 HUD，详细拒绝原因又只写入控制面板状态区；河流面板自身会把无效候选重置成泛化提示。折点草稿卡位于可滚正文底部，进入模式时常只有顶部少量内容可见，操作按钮在首屏外；同时存在两个同名入口，整条橙色预览覆盖既有河流后也难以判断究竟改变了哪一小段。
  - 实施范围：保留唯一一个“调整河道折线”入口，进入后让步骤、当前候选和三项操作在河流面板内优先可见；面板自主管理空闲、有效和拒绝状态，具体失败原因以醒目、可访问的就地反馈显示，不再依赖被遮挡的全局 HUD 或控制面板。预览只强调原插入河段、候选两段和候选节点；无效候选显示红色节点 / 引线并保留原因。继续沿用第 289 项先预览后确认和第 290 项水域安全门禁。
  - 最小验收：focused UI/runtime regression 覆盖单一入口、步骤文案、有效 / 过远 / 水域 / 穿水反馈、重新选择 / 取消 / Escape / 面板关闭、无历史写入与应用单历史；地图预览能区分旧段、候选两段、有效节点和拒绝节点。真实 Chrome 在桌面、`390px`、`320px` 进入模式后无需打开控制面板即可看见当前状态与完整操作，面板 / document 无横向溢出，四类错误为 `0`；生产构建和相关专项通过。
  - 排除：不扩展为拖拽 / 删除 / 批量折点编辑器，不修改水文拓扑、河流数据格式、公开 API、存档、PNG 内容、其它对象面板、全局 HUD 布局或 `source/`。完成后只显式暂存本项文件并中文提交，不推送。
  - 完成记录：河流面板现在只保留一个“调整河道折线”入口，使用“折”字图标；进入模式后，置顶工作卡始终显示“选位置 → 核对旧 / 新段 → 应用”三步、当前状态和“应用折点 / 重新选择 / 退出模式”三项操作。空闲、有效与拒绝状态由面板自主管理；拒绝原因为红色 `role=alert / aria-live=assertive` 文本，显式错误优先于任何残留草稿并禁用应用。有效预览只画被替换原段的浅色虚线、两段橙色新线和菱形节点；拒绝预览走非陆地遮罩 batch，在水面也显示红色节点及到最近河段的虚线引导。session 仍只保存可应用草稿，拒绝诊断不会进入历史或 PNG。
  - 审查与验收：独立审查首轮以应用失败错误优先级、浏览器错误链不足和 health 清理三项 `BLOCK`；限修为失败先清 session、组件 error 优先、真实水域点击与冲突反例，并在 API ready 后清理基线 health 后复验 `ACCEPT`，独立观察使终验也为 `ACCEPT`。river waypoint、river mouth、object details、30 个 canvas tools、90 项直接操控审计、selection highlight、PNG options、1303 modules 生产构建和差异检查通过。系统 Chrome 在 `1280 / 390 / 320px` 的非零面板滚动位置确认工作卡完整可见，三按钮宽度分别为 `468 / 322 / 252px`，document 横向溢出均为 `0`；真实水域点击得到 `draft=null / previewCode=waypoint-water / Apply disabled`，真实有效候选使陆地遮罩顶点从 `258` 增至 `288` 且不写历史，三档 active health、console、page error 均为 `0`，WebGL error 为 `0`。人工浏览器另确认有效局部预览、真实错误卡和“应用 → 撤销”闭环。

- **权威任务第 292 项：小幅放大 WebGL 城镇图标并收紧解析式抗锯齿。** `已完成；来源：用户实测反馈与调查确认`
  - 问题证据：正式城镇已经由 WebGL2 实例层的解析式几何绘制，不使用纹理贴图或正式 HTML/SVG 节点；当前 `10.5×8 CSS px` 基准盒在常用缩放中实际主体约 `8.67～10px`，约 `0.9px` 白色线芯与更细的深色外沿和 `fwidth` 过渡带处于相近量级，DPR `1～1.5` 下容易显得偏小、发虚。问题不在纹理过滤，换贴图不会改善。
  - 实施范围：保持现有九类解析式图形、单批实例和纯白线芯 / 深色对称外沿，把基准盒小幅提高约 `15%～20%`，同步校准名称宽度上限、碰撞 / 标签净空和描边阈值；抗锯齿继续使用导数，但收紧过渡带并设置 DPR 感知下限，避免硬锯齿或重新出现厚重阴影感。不得引入纹理 atlas、canvas 贴图或正式 SVG 节点。
  - 最小验收：真实 framebuffer 在 DPR `1 / 1.25 / 1.5 / 2` 和至少三个常用缩放档位量测九类主体、白芯、深色外沿和过渡像素；图标较第 281 项基线小幅增大、仍小于对应名称，轮廓连续且没有阴影 / 滤镜 / 偏移。角色组合、选中、单次实例 draw、无相机重传、picking、标签净空 / 碰撞、PNG 城镇开关、生产构建、三档视口和四类错误通过。
  - 排除：不修改九类几何语义、人口阈值、P90、角色 / 文化 / 手工视觉、标签文字 / 样式、地图数据、schema、API、存档、生成、Marker、军事、应用图标或 `source/`。完成后只显式暂存本项文件并中文提交，不推送。
  - 完成记录：正式 WebGL2 城镇图标基准盒由 `10.5×8` 调整为 `12.5×9.5 CSS px`，宽高约增加 `19%`；九类几何、人口四级、三类角色、颜色和连续缩放函数保持不变。名称驱动轮廓上限同步调整为约名称半宽并限定在 `4.8～10.5px`，碰撞框和标签净空继续消费共享基准盒；真实短名称样本的图标 / 名称最大宽度比为 `0.48245`。外轮廓固定为共享 `2px` 契约，fragment shader 复用现有 DPR uniform，把抗锯齿改为 `max(fwidth(distancePx) * 0.5, 0.22 / DPR)`，仍使用解析式距离场与单批实例，不存在纹理 atlas、canvas 贴图或正式 SVG / DOM 城镇图标。
  - 审查与验收：新增系统 Chrome framebuffer 专项，在浏览器 DPR 与 WebGL backing DPR `1 / 1.25 / 1.5 / 2` 下量测九类图形和 `1.5× / 2.5× / 4×` 三档缩放；每个样本均有深色核心、白色核心和抗锯齿过渡，最大过渡占比分别为 `0.778 / 0.664 / 0.496 / 0.467`，角色组合和金色选中态通过且不改变宽高。正式 `10k` 地图为 `839 / 839` 个 WebGL 实例、城镇 DOM 节点 `0`；单批 draw、缩放帧零模型重传、picking、标签比例 / 净空、平移预热、PNG 城镇开关、`1440 / 390 / 320px`、1303 modules 构建与差异检查通过，checksum / revision 不变，application console、page、active health 与 WebGL error 均为 `0`。独立审查和独立观察使均为 `ACCEPT`；正式主画布既有 `1.5` 性能上限不变，DPR `2` 专项只在测试期间临时调整同一真实 framebuffer backing，结束后恢复。

- **权威任务第 293 项：收紧河道折线工作卡操作按钮。** `已完成；来源：用户实测反馈`
  - 问题证据：工作卡三个 `UiButton` 没有局部尺寸规则，继承共享动作按钮与 Element Plus 默认规格；第 291 项三档浏览器实测中，每个按钮分别占满 `468 / 322 / 252px` 内容宽度，在窄面板还会换成多排，视觉体量明显大于状态说明。
  - 实施范围：只在河流折点工作卡内把“应用折点 / 重新选择 / 退出模式”收为紧凑单排文本按钮，使用局部 deep 样式覆盖宽度、高度、内边距、字号和间距；保留主次色、禁用态、焦点态、键盘操作与窄屏安全换行。不得修改共享 `UiButton`。
  - 最小验收：系统 Chrome `1280 / 390 / 320px` 中三个按钮高度为 `28px`、文字完整且优先单排，单个按钮不再占满工作卡；空闲、有效、拒绝与应用失败态保持，主按钮禁用规则、三项动作、键盘焦点和面板 sticky 行为不变，document 无横向溢出，console / page / active health / WebGL error 为 `0`。focused、生产构建和差异检查通过。
  - 排除：不修改按钮文案、河流预览、候选门禁、session、历史、河流数据、共享组件、其它面板、schema、API、存档、PNG 或 `source/`。完成后只显式暂存本项文件并中文提交，不推送。
  - 完成记录：工作卡动作区使用 scoped deep 局部覆盖，将三个按钮收为内容宽度、`28px` 高、`12px` 字号、`0 10px` 内边距和 `6px` 间距；父级继续允许安全换行，但 `1280 / 390 / 320px` 实测均为单排，按钮尺寸稳定为 `68 / 70 / 70 × 28px`，不再各占整行。共享 `UiButton`、全局按钮颜色 / 焦点 / 禁用样式和河流交互均未改。
  - 审查与验收：系统 Chrome 三档覆盖空闲、有效、真实水域拒绝和应用失败态；主按钮启停与主次色正确，实际 `Tab` 聚焦保留 `2px solid` focus-visible，sticky、重新选择和退出动作通过。河流点数、checksum、revision、undo 均保持基线，document 横向溢出与 application console、page、active health、WebGL error 均为 `0`；river waypoint browser / runtime、object details、1303 modules 构建和差异检查通过，独立审查与独立观察使均为 `ACCEPT`。

- **权威任务第 294 项：拉开 WebGL 城镇人口四级的连续尺寸层级。** `已完成；来源：用户实测反馈`
  - 问题证据：当前 `hamlet / village / town / city = 0.69 / 0.75 / 0.81 / 0.87`，相邻仅相差约 `7%～9%`；现有算法又先乘 tier、再以同一个最终 factor 封顶，使常见名称宽度下的村庄、城镇、城市在中高缩放趋于同尺寸。shader 只能消费被截平的 factor，无法恢复等级差。
  - 实施范围：只拉开四级 tier 比例，并把名称封顶改为先限制共享相机增长、再乘 tier，使封顶前后始终保留人口等级比例；名称上限、`12.5×9.5` 基准盒、`2px` 描边和第 292 项 DPR 抗锯齿保持不变。碰撞框、标签净空和实例数据继续消费同一最终 factor。
  - 最小验收：数学回归覆盖相机 `1 / 1.5 / 2 / 2.5 / 3 / 4 / 12`，四级尺寸严格递增，相邻倍率至少约 `15%`、城市 / 村落至少约 `1.55`，名称封顶前后都不归一；真实 framebuffer 以固定几何验证四级 CSS bbox 单调并保持清晰，真实 `10k` 地图的四级分母与可见样本可核对。名称比例继续满足 P95 `≤0.5`、最大 `≤0.52`，标签净空、碰撞、角色、选中、picking、PNG 城镇开关、单批 draw、相机零模型重传、DPR 四档、三视口、生产构建和四类错误通过。
  - 排除：不修改人口阈值、P90、九类几何语义、角色 / 文化 / 手工视觉、颜色、基准盒、抗锯齿、生成、地图数据、schema、API、存档、Marker、军事、应用图标或 `source/`。完成后只显式暂存本项文件并中文提交，不推送。
  - 完成记录：人口四级尺寸系数由 `0.69 / 0.75 / 0.81 / 0.87` 拉开为 `0.62 / 0.76 / 0.92 / 1.10`；CPU 与 vertex shader 统一改为先用最大等级反推共享相机尺寸上限、再乘当前等级，名称封顶后四级仍保持约 `1.226 / 1.211 / 1.196` 的相邻倍率，城市 / 村落约为 `1.774`。名称上限、`12.5×9.5 CSS px` 基准盒、`2px` 描边、DPR 抗锯齿、九类解析式几何、角色和实例结构均未改；碰撞框与标签净空继续消费同一最终 factor。
  - 审查与验收：数学门禁覆盖相机 `1 / 1.5 / 2 / 2.5 / 3 / 4 / 12` 的自然态与 `cap=0.9` 封顶态；固定种子真实 `10k` 图生成 `828` 个城镇，四级分母为 `4 / 333 / 408 / 83`，city / burg 差异为 `0`。独立审查首轮因 framebuffer 未触发封顶分支、默认图弱分母断言而 `BLOCK`；限修后以同一 town 几何、`cameraScale=12 / cap=0.9` 在 DPR `1 / 1.25 / 1.5 / 2` 下得到严格递增的四级 bbox 面积，复核和最终观察使均为 `ACCEPT`。真实 `5410` 名称比例最大 `0.48245`，`839` 个实例保持单批与模型上传 `1`；缩放首帧、平移八帧、拾取、标签净空、PNG 城镇开关、`1440 / 390 / 320px`、1303 modules 构建和差异检查通过，checksum / revision 不变，application console、page、active health 与 WebGL error 均为 `0`。地图数据、人口阈值、schema、API、存档、生成和 `source/` 未改。

- **权威任务第 295 项：整体放大 WebGL 城镇四级图标。** `已完成；来源：用户实测反馈`
  - 问题证据：第 294 项的四级差异正确，但名称封顶改为先限制共享相机增长后，封顶态 `hamlet / village / town / city` 只能占城市上限的约 `0.564 / 0.691 / 0.836 / 1`。真实 `10k` 图中村庄在 `2.5× / 4×` 相对第 292 项旧尺寸缩小约 `28% / 31%`，城镇高倍约缩小 `16%`，所以用户观察到缩放后整体越来越小。
  - 实施范围：保留四级严格递增与第 294 项“先封顶、再乘等级”的数学顺序，适度抬高四级系数和名称轮廓上下限，使适配视图与中高缩放的四级实际 CSS bbox 都比第 294 项基线增大；碰撞框、标签净空、marker 避让和拾取继续消费同一最终 factor。允许按真实 framebuffer 结果校准名称比例门槛，但不得用取消名称限幅或统一尺寸冒充放大。
  - 最小验收：数学门禁覆盖自然态与名称封顶态四级严格递增，相邻倍率不低于约 `15%`、城市 / 村落不低于约 `1.55`；真实 `10k` 固定样本在相机 `1 / 1.5 / 2.5 / 4` 的各级可见 CSS bbox 均较第 294 项基线增大且不随放大回落。DPR `1 / 1.25 / 1.5 / 2` framebuffer 保持深色外沿、白色内线和抗锯齿，名称比例、标签净空 / 碰撞、角色、选中、picking、PNG 城镇开关、单批 draw、相机零模型重传、三档视口、生产构建和四类错误通过。
  - 排除：不修改人口阈值、P90、四级顺序、九类几何语义、角色 / 文化 / 手工视觉、颜色、基准盒、描边、抗锯齿、生成、地图数据、schema、API、存档、Marker、军事、应用图标或 `source/`。完成后只显式暂存本项文件并中文提交，不推送。
  - 完成记录：四级尺寸由 `0.62 / 0.76 / 0.92 / 1.10` 整体抬高为 `0.72 / 0.86 / 1.02 / 1.20`；名称轮廓范围由 `4.8～10.5px` 放宽为 `5.4～12.1px`，名称宽度系数由 `0.5` 调整为 `0.575`。CPU 与 vertex shader 继续先限制共享相机增长再乘等级，shader 最大等级除数同步为 `1.20`；基准盒、描边、抗锯齿、实例结构和四级顺序未改。全数学样本相对第 294 项同名封顶基线最小增长 `1.091×`，代表性正常名称在 `1.5×～4×` 封顶后四级约增长 `20.6%～28.4%`。
  - 审查与验收：独立审查首轮因缺少真实 `10k` 四级新旧 framebuffer 基线对照而 `BLOCK`；限修后正式 API 固定生成 `city-scale-217 / 10k`，DPR `2` 选取四级各一真实对象并保留实际位置、几何、角色和名称 cap，在相机 `1 / 1.5 / 2.5 / 4` 隔离绘制当前版与严格等价的第 294 项模拟。`16 / 16` 组当前 bbox 面积全部增大，最小面积倍率 `1.0909`、最少增加 `10` 个 backing pixels；DPR `1 / 1.25 / 1.5 / 2` 均保留深色外沿、白色内线与抗锯齿，复核与最终观察使均为 `ACCEPT`。真实 `5410` 名称比例最大 `0.54276`，`839` 个实例保持单批、模型上传 `1`；缩放首帧、平移八帧、标签净空、拾取、选择、PNG 城镇开关和 `1440 / 390 / 320px` 通过，checksum / revision 不变，生产构建及差异检查通过，application console、page、active health 与 WebGL error 均为 `0`。人口阈值、地图数据、runtime、schema、API、存档、生成和 `source/` 未改。

- **权威任务第 296 项：补齐云端存档导入的可发现性与真实字节闭环。** `已完成；来源：用户实测反馈`
  - 问题证据：Dropbox 与 Google Drive provider 已有列表和 `downloadFile`，云端面板也有底部“载入所选地图”，但唯一公开入口藏在“保存 → 云端存储…”且面板先强调新建 / 覆盖，普通用户合理地会判断为只有保存。更严重的是两个 provider 下载后只返回无文件名 Blob；本地解析器只按文件名或 gzip MIME 判断压缩，Dropbox 常见的 `application/octet-stream` `.webfmg` 会被误当普通 JSON，现有回归只核对下载字节数和源码按钮存在，没有覆盖下载、解压、迁移、替换地图的真实闭环。
  - 实施范围：在简介页保留原本地 `#import-map-file` 的 id、accept、change 与键盘链，新增并列的显式“从云端导入…”入口；入口模式经 `ui/panel.js` 和 runtime 传入云端面板，每次打开规范为 `save / import`。面板明确分成“保存到云端”和“从云端导入”两区，导入按钮紧邻列表并视觉优先。controller 在下载后按远端文件名把 Blob 包装为具名 File，压缩扩展强制 `application/gzip`、JSON 使用 `application/json`，同一 File 同时作为 payload 与 `sourceFile` 交给唯一 `data.importMap` 解析、迁移、诊断与回滚链；provider 保持纯 transport。
  - 安全语义：实际导入只确认一次，文案明确会替换当前地图、清空撤销 / 重做且未保存修改不会合并；取消确认不得发送下载请求。busy 时禁止 provider / 文件切换与重复操作，下载完成后、导入前必须复核 operation epoch，陈旧 provider 请求不得替换当前地图。失败先显示包含文件名的 `role=alert`，当前地图与完整可见状态保持；成功保留云端连接和列表选择，不覆盖或删除远端文件。
  - 最小验收：mock transport 在真实 Chrome 覆盖 Dropbox `application/octet-stream` `.webfmg`、Google 普通 JSON 与旧 `.gz`；成功导入替换 identity / checksum / options 并清空历史，取消为零下载。坏 JSON、损坏 gzip、未来版本和 renderer 注入失败均不得改变 map identity / checksum / revision、options、undo / redo、selection / editing、renderer 当前 map、主题与单位；若现有 map-replace 回滚遗漏，只允许补共享快照 / 恢复。桌面跑完整功能矩阵，`1440 / 390 / 320px` 验入口、双区、按钮顺序和零溢出；本地导入仍可达，生产构建、云存储 / 会话 / 地图迁移相关门禁和最终四类错误通过。
  - 排除：不新增公开 API，不修改 provider OAuth 权限、目录、列表 / 覆盖协议、refresh token、跨标签页会话、存档 schema 或 parser 主体；不做 dirty 检测、自动保存、后台同步、冲突合并、删除、分享、通用网盘浏览、真实账号联调或 `source/` 修改。
  - 完成记录：简介页保留原本地导入 input，并新增“从云端导入…”入口；云端面板按保存 / 导入双区重排。下载 Blob 在 controller 边界按远端名称、压缩扩展和修改时间包装为同一个具名 `File`，继续进入唯一 `data.importMap` 的解析、迁移、确认、诊断和事务链。共享地图替换快照补齐 pending generation、map revision、单位和 canvas tool mode；早期解析失败不会重复进入活动工具，renderer 及 `replaceMap()` 后晚期失败均能恢复旧地图、renderer、revision、历史、选择 / 编辑、主题、单位和工具上下文。
  - 审查与验收：独立方案审查首轮 `BLOCK`、收紧后 `PLAN ACCEPT`；独立代码审查两轮分别阻断 revision 晚期回滚、canvas mode 重入、错误数组清空和三档入口测量缺口，限修后为 `CODE REVIEW ACCEPT`。系统 Chrome mock 覆盖 Dropbox `application/octet-stream` `.webfmg`、Google JSON、Dropbox / Google 旧 `.gz`、取消零下载、busy 互斥、坏 JSON、损坏 gzip、未来版本、renderer 早期失败与 revision 晚期失败；`1440 / 390 / 320px` 的入口、双区、激活态和零溢出通过，非预期 console、page、health 与 WebGL error 为 `0`。生产构建、云存储静态门禁、地图迁移、显示单位和 API 兼容门禁通过；provider、OAuth、目录、parser、schema、公开 API、地图数据和 `source/` 未改，真实账号联调仍不在本项范围。

- **权威任务第 297 项：为首都增加独立角色尺寸并拉开与普通大城镇的视觉差。** `已完成；来源：用户实测反馈`
  - 问题证据：当前人口四级统一使用 `0.72 / 0.86 / 1.02 / 1.20`，首都只把自动剪影换成五角星，没有任何角色尺寸倍率；五角星轮廓 extent 为 `0.84`，还略大于普通大城双环的 `0.80`，在同一名称宽度上限下会更早封顶。固定 `city-scale-217 / 10k` 实测有 `825` 个城镇和 `20` 个首都，首都人口级别为 `village / town / city = 1 / 9 / 10`；代表性 city 级首都在相机 `1 / 1.5 / 2.5 / 4` 的 factor 为 `1.086 / 1.210 / 1.210 / 1.210`，从 `1.5×` 起即被 cap 锁死，因而继续缩放也没有形成比普通大城更明确的体量差。
  - 实施范围：保留人口四级和九类解析式几何，为 `capital` 角色增加固定 `R = 1.15` 的独立线性倍率。现有基线公式为 `B = min(camera(scale), M / 1.20) × T`，其中 `M` 是原始 `cityIconMaxSizeFactor`、`T` 是人口 tier；首都最终公式只能是 `F = B × R`，`M` 不得再乘 `R`，禁止自然态漏乘或封顶态得到 `R²`。GPU 不增加实例属性，而是在 pack 时只依据原始 `roles` 把 `T × R` 写入现有 effective tier scale；不得只读会消除同形角色的 additional role bits，也不得用五角星 shape 冒充首都角色。CPU helper 使用同一 roles-derived `R`，碰撞框、标签净空和视口预热消费同一个最终 factor。自动五角星和保留手工剪影的首都都按角色放大，普通大城、省会、港口以及无首都角色但手工选择五角星的对象保持原尺寸；不得新增纹理、DOM / SVG 城镇节点或相机帧实例上传。
  - 最小验收：数学门禁覆盖四个人口 tier、相机 `1 / 1.5 / 2.5 / 4` 和自然 / 有限 cap，所有非首都对象（普通、省会、港口、无首都角色的手工五角星）factor 与第 296 项基线严格相等；所有首都线性 factor 比均约为 `1.15`，并单列“手工非五角星剪影 + capital role”样本，证明只乘一次。普通对象 collision box / label anchor 必须零差；首都 CPU box 与 GPU factor 一致，scale `12` 的稳定 cap 净空只增加 `1.15` 图标高度带来的差值，`CITY_LABEL_ICON_GAP` 保持常量，不能 `R²` 或让所有名称一起远离。真实 `10k` 固定图冻结全部首都与普通 city 对照，在相机 `1 / 1.5 / 2.5 / 4`、DPR `2` 隔离绘制当前版与第 296 项基线，首都每组线性尺寸比接近 `1.15`、bbox 面积均增大且普通大城像素零差；三档视口人工确认首都与大城层级清楚。四级分母、角色组合、手工视觉、拾取、选择、PNG 城镇开关、单批 draw、相机零模型重传、DPR 清晰度、生产构建和四类错误通过，checksum / revision 不变。
  - 排除：不修改人口阈值、P90、四级系数、九类几何、颜色、线宽、抗锯齿、首都 / 省会数据、生成、地图数据、runtime 规则、schema、API、存档 / 迁移、Marker、军事、应用图标或 `source/`；本项未授权提交或推送。
  - 后续微调：用户反馈首都仍需更醒目，本地实现将角色倍率从 `1.15×` 提高为 `1.25×`；普通对象、人口四级、GPU 实例结构、名称 cap 公式和其它角色保持不变，仅同步固定值回归与本记录。
  - 完成记录：新增 `CITY_ICON_CAPITAL_ROLE_SCALE = 1.15`，CPU 最终 factor 按原始 `roles` 在既有人口 / cap 结果后只乘一次；GPU pack 把 `T × R` 写入既有 `a_tierScale`，`M`、shader、11-float / 44-byte 实例结构和单批绘制均未改。碰撞框、视口预热和 scale `12` 标签净空继续通过同一个角色感知 `cityIconScale`；动态 roles 始终从 `baseTierScale` 重算，添加、重复添加、移除或只改 roleBits 都不会累乘或重建模型。普通、省会、港口、无首都角色的手工五角星完全不变；带首都角色的其它手工剪影同样放大。
  - 审查与验收：方案审查首轮因 `R²` 风险和手工正反例不足 `BLOCK`，明确 `F = min(camera, M / 1.20) × T × R` 且 `M` 不变后 `PLAN ACCEPT`。代码审查首轮又阻断普通大城 current / baseline 自比较；限修为保持同 roles / shape / cap、直接覆写第 296 项 packed tierScale 的独立 GPU 基线，并补动态角色状态链后 `CODE REVIEW ACCEPT`。固定 `city-scale-217 / 10k`、DPR `2` 的 `20` 个首都 × 四档相机共 `80` 组 factor 均为 `1.15`，bbox 面积最少增加 `17`；`73` 个非首都大城 × 四档共 `292` 组像素 hash / 宽高零差，覆盖普通、省会和港口。DPR `1 / 1.25 / 1.5 / 2` 清晰度、名称比例最大 `0.543`、标签净空、`839` 实例单批 / modelUploads `1`、平移、拾取、选择、PNG 城镇开关和 `1440 / 390 / 320px` 通过；checksum / revision 不变，console、page、health 与 WebGL error 为 `0`。人口四级、几何、地图数据、schema、API、存档和 `source/` 未改，本项未提交或推送。

- **权威任务第 298 项：建立现有地图网格拓扑细分与内部结构受控 API。** `已完成；来源：用户直接要求；2026-08-07 获授权并完成`
  - **目标**：在不调用整图重生成的前提下，让当前已有地图能够从约 `10k` Grid Cells 细分到约 `100k`，尽量保持原有海陆边界、地形走向、高度场连续性、邻接连通关系及河流 / 城市等对象的空间语义；以该能力作为“现有 10k 地图拓扑保持式扩展到 100k”的基准用例。
  - **范围**：新增受控的内部网格 API，覆盖 `grid.points`、`grid.cells.i / c / v / p / h`（简称 `c/v/h` 等拓扑与基础高度字段）的只读快照、结构摘要、版本 / 引用绑定、写入预检、事务式写入、撤销 / 重做和失败回滚；允许受控构造母 cell → 子 cell 映射，并在细分后重建 pack、feature、气候、河流、路线、城市拾取等派生结构。读写必须通过 schema、范围、长度、索引、邻接对称性、边界闭合和地图版本校验，不得暴露可绕过领域规则的裸引用。
  - **拓扑细分要求**：细分算法必须以当前地图的既有 `points / c / v / h` 和海陆边界为输入；新 cell 只能在母 cell 及其原邻接闭包内建立关系；高度采用母 cell / 邻接约束插值并保留海平面与边界符号；输出需保留母子映射，使国家、城市、河流、道路及选择 / 拾取能够迁移或明确拒绝迁移。现有 `generate.newMap({cellsTarget: ...})` 不得伪装为本任务实现。
  - **最小验收**：固定旧地图夹具由约 `10k` 扩展到约 `100k`；海陆连通分量、海岸 / 湖岸边界、主要地形高度分位数、山脊 / 河网骨架和母 cell 邻接连通性满足冻结阈值；`c / v / h` 的读写 schema、版本绑定、非法拓扑拒绝、事务回滚、撤销 / 重做、旧存档兼容和 API 能力发现全部通过；真实 5410 Chrome 页面能够在用户现有地图上执行一次受控细分并恢复稳定渲染。性能、内存和大地图派生重建需单列门禁。
  - **排除**：不开放裸内部对象、不新增远程写入、不改变现有 `generate.newMap` 语义；不在没有独立拓扑夹具、算法审查和回滚证明前直接改写用户当前地图。
  - **实施顺序**：结构契约与施工门禁已冻结在 [专题说明](./task-notes/grid-topology-refinement-and-controlled-api.md)；随后实现细分事务与派生重建，最后完成旧数据 / 5410 浏览器验收和性能门禁。
  - **完成结果**：受控 `grid` API 已提供摘要、快照、通用同规模写入预检 / 提交和拓扑细分预检 / 提交六个方法；固定夹具由 `10004` 细分为 `100000` cells，旧点与旧高度逐项保持，海陆连通分量分别保持 `6 / 4`，非法跨母域邻接、陈旧绑定与非对称邻接均拒绝。国家、城市、Marker、行政中心、河流、路线与地区引用迁移通过，河流 / 路线连续 cell 均保持邻接；事务回滚、撤销 / 重做、旧存档与细分存档往返通过。
  - **浏览器与资源门禁**：用户现有 `5410` 页面完成 `10004 → 100000` 受控细分并稳定渲染，撤销回到 `10004`、重做回到 `100000`；独立生产 Chrome 回归耗时约 `7.3s`，显式回收后保留 JS 堆约 `696 MiB`，`1440×960` PNG、WebGL2、application console、page、active health 与 WebGL error 均通过。能力矩阵同步为 `1213 / 1139 / 74 / 0`，公开 API 为 `18` 个命名空间、`322` 个方法；`source/` 未修改。

- **权威任务第 299 项：按时间分卷归档已完成的权威任务。** `已完成；来源：用户直接要求`
  - **目标**：把已完成、已移除或已被后续方案取代的权威任务从当前执行清单迁入独立归档，使接手智能体默认只读取未完成任务，同时保留按时间和编号查找历史任务的稳定入口。
  - **范围**：建立 `docs/task-archives/` 索引和按时间切分的首批归档卷；当前计划只保留未完成 / 暂缓任务、最新执行门禁、API 基线和归档规则；同步 `docs/README.md`、`docs/development-log.md` 与 `AGENTS.md`。归档条目保留原任务标题、状态、范围、验收与完成记录，不把全部历史塞入单一大文件。
  - **持续归档规则**：后续任务完成后按完成日期写入对应周卷；跨月必须新建分卷，单卷过大时按半月进一步拆分；索引同时维护日期范围、任务编号范围和例外状态，当前计划不得重新积累已完成任务正文。
  - **最小验收**：当前标准任务条目与归档条目编号无重复、无丢失；第 284、298 项继续留在当前计划且暂缓语义不变；第 278 项保留“由第 279 项取代”的历史状态；第 53 项移除状态和第 45～52、54～297 项完成门禁可追溯；归档链接、中文文档、`source/` 零改动与 `git diff --check` 通过。
  - **排除**：不修改产品代码、任务完成结论、API / schema / 地图数据，不启动第 284 或第 298 项，也不提交或推送。
  - **完成记录**：建立 `docs/task-archives/` 索引和四个首批时间卷，迁移前 `235` 个标准条目与迁移后当前计划 / 归档逐项正文比对为缺失 `0`、改写 `0`、重复 `0`，唯一新增编号为本项第 299 项。第 299 项归档后共有 `234` 个标准完成 / 取代条目进入时间卷，当前计划只保留暂缓的第 284、298 项；归档脚本支持后续按完成日期写入不跨月时间片，并拒绝重复编号或未完成任务进入归档。`docs/README.md`、`AGENTS.md` 与开发日志已同步，`source/` 零改动。

- **权威任务第 300 项：100k 高度笔刷提交耗时分段与增量优化。** `已完成；来源：用户实测反馈与直接授权`
  - **问题证据**：用户反馈高度笔刷拖动结束仍有卡顿。代码审计显示预览阶段只修改受影响的 grid / pack cell，pointerup 还会执行一次提交刷新、最终绘制、高度统计、缩略图与面板收尾；普通陆地样本没有自动执行高度派生全链。
  - **实施范围**：新增内部高度提交 trace，分段记录变更收集、命令构造、历史执行、局部 `refreshHeightCells`、最终 draw、runtime / pick 面板、高度统计、预览和 DOM 收尾；pointerup 最后一批预览刷新不立即 draw，提交完成后统一执行一次最终 draw。隔离生产 Chrome 对照覆盖实际 `10004`（10k）与 `99846`（100k）网格。
  - **最小验收**：普通 100k 连续长拖提交 trace 约 `10.2～11.0ms`，其中最终 draw `3.9～4.7ms`、高度预览 `2.4～2.8ms`、runtime 面板 `1.5～1.8ms`；`refreshHeightCells` 约 `0.7～0.9ms` 且 `incremental=true`。普通样本 surface / shore 拓扑重建为 `0`，每次提交 draw 次数为 `2`；撤销 / 重做、地图 checksum、schema、存档、console、page、health 和 WebGL 通过。
  - **性能边界**：独立高度派生横幅回归另记录首次加载 10k 地图约页面时间 `5.3s` 的一条 `main-thread-long-task`，回归脚本已将其与应用错误分列并保留遥测。普通样本未证明岸线 / mesh 拓扑分支为热点，因此不实施未证实的局部拓扑重构；该分支仍保留原安全路径。
  - **排除与兼容**：不修改地图数据、grid / pack 字段、schema、存档、公开 API、历史事务、派生 stale 规则、生成算法或 `source/`；不接管、刷新或写入用户当前 `http://127.0.0.1:5410/?debug=1` 标签页。
  - **验收记录**：高度画笔、节流、派生链、派生待更新横幅、隔离 10k / 100k 性能浏览器回归、生产构建、Node 语法检查与 `git diff --check` 通过。性能回归中 `10k / 100k` 的 surface 刷新均为 `0`、health long task 为 `0`、WebGL / console / page error 为 `0`；第 300 项已完成，当前没有活动权威任务。
