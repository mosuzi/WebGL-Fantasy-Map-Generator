# 浮动面板架构约束

> 状态校准（2026-07-16）：本文前半保留早期迁移约束；固定侧栏 fallback、最小对象详情和第一版河流面板等步骤已经被正式 Vue 浮动面板体系取代。文末“当前状态”已更新为现行边界，旧迁移顺序不代表待办。

本文档记录正式应用 UI 面板的长期方向和已经落地的架构边界。

## 目标

后续所有配置、查看和编辑类面板都应作为普通 HTML/DOM 浮动面板覆盖在地图工作区上方，而不是用 canvas 绘制。适用范围包括：

- 生成配置面板。
- 高度编辑面板。
- 河流编辑面板。
- 城市、道路和路线编辑面板。
- 国家、省份、文化、宗教编辑面板。
- 标签、纹章和对象详情面板。
- 调试、统计和图层控制面板。

## 基本原则

- 面板不占用地图主画布的绘制职责，canvas 只负责地图和 WebGL 图层。
- 面板使用 HTML 表单、按钮、列表、tabs、slider、checkbox、select 等普通控件。
- 面板应可拖动，并保留未来折叠、最小化、关闭、停靠和恢复位置的扩展空间。
- 面板层级应由统一的 panel manager 管理，避免每个功能各自维护 z-index。
- 面板状态应能按 workspace/session 保存：位置、尺寸、折叠状态、当前 tab 和打开/关闭状态。
- 面板不应阻塞地图默认操作；只有鼠标事件发生在面板内部时才由面板消费，地图区域仍负责 pan、zoom、hover picking 和 object selection。

## 与地图交互的边界

- WebGL renderer 不直接创建业务编辑面板。
- renderer 可以暴露选中对象、hover 对象、camera、图层状态和编辑工具状态。
- 面板通过应用 runtime 或 store 修改生成 options、图层开关、编辑工具参数和选中对象。
- picking、selection 和高亮是地图交互层职责；详情面板只读取对象摘要并提供领域面板入口，不自行计算命中。
- 对象详情面板应跟随 selection 状态打开或刷新，但不应该成为 selection 的唯一来源。

## 推荐模块划分

当前已开始在 `app/webgl-generator/src/ui/` 下增加面板模块，后续继续按以下方向扩展：

- `panel-manager.js`：管理面板注册、打开/关闭、激活层级、拖动、尺寸和持久化；当前已覆盖注册、打开/关闭、拖动、层级，以及位置和宽度的 `localStorage` 持久化。
- `floating-panel.js`：通用面板壳，包含标题栏、拖动句柄、关闭/折叠按钮和内容插槽；当前先由 `panel-manager.js` 内联创建面板壳，后续复杂化时再拆出。
- `panels/generation-panel.js`：生成配置。
- `panels/terrain-panel.js`：高度和地形编辑。
- `panels/river-panel.js`：河流查看、统计、定位和编辑。
- `panels/settlement-panel.js`：城市、道路和路线编辑。
- `panels/object-details-panel.js`：选中对象详情。
- `panels/layers-panel.js`：图层和专题面控制。

当前固定侧栏可以先保留；迁移时应先抽出 panel manager，再逐步把现有控制区搬进浮动面板。

## 河流面板约束

demo 中的河流统计和河流编辑为了快速验证交互暂时混在左侧编辑器区域；正式应用不得沿用这种结构。正式版已新增第一版独立浮动 `river-panel`，并通过 panel manager 注册、打开、关闭、置顶、拖动和持久化位置。

- 河流统计、全量列表、筛选、长度/流量排序、快速定位和选中河流属于 `panels/river-panel.js`。
- 河道拖点、宽度/流量参数、河口/源头编辑和撤销入口也属于河流浮动面板的编辑 tab 或子区域。
- 河流面板不得混入对象详情面板、图层控制面板、生成配置面板或固定侧栏。
- 对象详情面板可以只显示选中河流摘要，并提供“打开河流面板”入口；不应承载完整河流列表和统计。
- 地图上的红色闪烁高亮、selection 和 picking 仍归地图交互层/runtime 管理，河流面板只读取 selection 并发出定位或编辑命令。
- 河流面板的打开状态、位置、宽度、高度、当前 tab、筛选词和排序字段应纳入 workspace/session 持久化边界。
- 当前第一版只持久化位置和宽度；筛选词、排序字段、打开状态和高度持久化留到后续面板状态扩展。

## 第一阶段迁移建议

1. 保留现有侧栏作为 fallback。
2. 已新增 panel manager 和一个最小浮动“对象详情”面板。
3. 对象详情面板显示当前 selection 摘要，并提供定位、进入编辑状态和打开领域面板入口。
4. 已补最小持久化边界：保存面板位置和宽度，不保存打开状态。
5. 再迁移图层控制和生成配置。
6. 最后进入高度、河流、城市等编辑工具面板。

## 验收标准

- 面板可拖动，拖动不触发地图 pan。
- 面板内部滚动、点击、输入不触发地图 selection。
- 面板关闭/打开后不会丢失当前地图状态。
- selection 变化时，对象详情面板能刷新为城市、路线、国家、河流等不同对象摘要。
- 没有 canvas 绘制的 UI 控件；canvas 只绘制地图。

## 当前状态

截至 2026-07-16：

- 固定侧栏已经退出主交互，生成、视图、图层、单位、管理和高级入口由控制面板承载；主要领域使用 Vue SFC 浮动面板。
- `PanelManager` 统一管理打开 / 关闭、层级、拖动、尺寸、持久化、主面板槽位、碰撞整理、缩放安全区和性能诊断。
- selection、editingObject、定位、高亮和对象详情已由 runtime / store 统一；方案 B 只更新已经打开的领域面板，关闭时落对象详情。
- `UiObjectTable`、动作栏、详情网格、历史入口、筛选排序、虚拟滚动、列宽和批量选择已成为共享组件能力。
- 国家、省份、城市、文化、宗教、路线、河流、湖泊、地区、标记、备注、测量、经济、外交和静态军事等面板均已接入正式体系。
- 纹章主入口已降级且短期不深化；动态军事、复杂贸易动画和其它远期系统不得从本文自动入队。
