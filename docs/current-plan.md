# 当前开发计划

本文件是唯一权威任务清单，只保留未完成、进行中或暂缓的任务。已完成任务按时间分卷移入 [权威任务归档](./task-archives/README.md)，不得从 README、开发日志、专题文档或归档中的“下一步”自行恢复为当前任务。

## 当前状态

> **执行门禁（2026-08-08）**：权威任务第 301、302、303 项已完成并按完成日期归档；第 304 项“河流网络算法实验室与生成质量修复”已完成 A～E 决策门，但 304-E 结论为 BLOCK，候选暂不接入正式生成器，等待明确的产品 / 性能决策。其余既有完成状态见归档索引；不得从历史专题、回归报告或归档中的“下一步”自行恢复任务。

当前 API 基线为：`window.webglGeneratorApi` 覆盖 `18` 个命名空间、`322` 个公开方法和 `179` 个编辑方法，稳定等级为 `314 / 7 / 1`；`322 / 322` 方法可通过 `info.describe` 发现，新增 `grid` 六个受控结构摘要、快照、预检与事务方法；`planner.listRecipes / getRecipe` 只读公开 `10` 个配方和 `43` 个顶层步骤，`objects` 覆盖 `20` 类对象，`cells` 已提供八个读取、定位、扫描与动作预检方法。完整能力矩阵为 `1213` 行、`covered 1139 / excluded 74 / deferred 0 / gap 0`；复合语义矩阵为 `80` 个动作、`70` 个完整事务与 `10` 个玩法配方。

## 权威任务清单

### 第304项：河流网络算法实验室与生成质量修复

- **状态**：304-A～D 已完成，304-E 隔离浏览器决策门已完成但结论为 **BLOCK**；候选继续留在独立实验室，不批准接入 `rivers.js` 或正式河网输出，等待明确的产品 / 性能决策。用户当前 Chrome 标签页和地图保持不动。
- **目标**：定位并修复离散细线、支流未接入干流、支流宽度 / 流量超过接收干流、无效循环和非汇流交叉；保留合法河网分叉、湖泊进出水和河口边界语义。
- **封闭子任务**：304-A 实验室壳体与固定夹具 / 只读审计；304-B 流向 DAG、父子关系与循环候选算法；304-C 汇流锚点与路径连续性候选算法；304-D 流量 / 宽度单调性与碎线视觉策略；304-E 固定种子、真实浏览器视觉对照和正式接入决策门。
- **304-A 最小验收**：固定夹具能分别复现孤立碎线、未接入汇流、支流越级、无效循环、合法汇流、非汇流交叉、湖泊溢流和边界河口；10k / 50k / 100k 固定种子输出确定性诊断，给出河流 ID、cell / 坐标、指标和证据等级；实验室不写入地图、存档、历史或 API，正式应用构建与 `source/` 零改动。
- **304-A 结果**：`prototype/river-network-lab/` 与 `tools/river-network-lab-regression.mjs` 已完成；8 类固定夹具全部命中预期 issue ID，10k / 50k / 100k 的河网快照、诊断列表和 `networkChecksum` 重跑稳定，原始快照不变。正式地图摘要 checksum 在两次生成中仅作上下文记录且出现变化，不作为本子任务的确定性门禁，留给 304-E 继续核查；本阶段没有正式生成器、地图、存档、历史、API、用户 Chrome 或 `source/` 写入。
- **304-B 结果**：实验室新增确定性父子图分析和 DAG 候选；缺失父级、自指和关系环会拒绝候选，合法汇流可得到稳定拓扑序，非汇流交叉不会被误判为父关系错误。固定夹具与 10k / 50k / 100k 重跑的 visited、cycles、missing、self 指标稳定；候选只复制内存快照，不重写 `points`、`cells` 或正式河网。
- **304-C 结果**：实验室新增汇流锚点分析和候选；合法父子末端在固定容差内才允许复制并贴到父河最近折线点，超容差的未贴合支流拒绝候选；无父关系的非汇流交叉继续由几何审计标识，湖泊 / 入海 / 边界出口不参与整体吸附。固定夹具与三档 seed 的关系数、未贴合数、保护出口数和最大距离重跑稳定，原始快照不变。
- **304-D 结果**：实验室新增流量 / 宽度单调性候选和碎线只读策略；按拓扑序逆序累计输入流量并局部抬升接收河，不做全局加粗；碎片分类为只隐藏视觉、延长到汇流、保护出口保留或普通保留。支流越级夹具的候选越级数归零，孤立细线只返回视觉策略，河口保护夹具不被降级；三档 seed 指标稳定且没有快照写入。
- **304-E 结果：BLOCK**：隔离浏览器实验室页面显示 `8/8` 夹具命中，B/C/D 候选卡和只读边界正常；隔离正式页面 10k 基线为 `1` canvas、`99` SVG、无 page / WebGL / console error，100k 对照为 `1` canvas、`501` SVG，生成到稳定约 `15.3s`。100k 后捕获 `7` 个 `main-thread-long-task`、`2` 个 `render-frame-gap`，其中 `2` 个被记录为 health error；同时固定 seed 的正式摘要 checksum 两次生成不稳定。故没有足够证据批准正式接入，候选只保留实验室，未修改正式 generator、地图、存档、schema、API、用户标签页或 `source/`。
- **兼容边界**：在正式接入决策前保持 `river.cells`、`points`、`parent`、`basin`、`source`、`mouth`、`flux`、`discharge` 等既有字段和旧存档语义；不把实验室候选结果直接覆盖正式生成结果。
- **明确排除**：不通过删除所有交叉来伪造河网、不把所有环都判成错误、不用单纯加粗掩盖断线、不在用户当前地图上重新生成或刷新验证。
- **专题方案**：详见 [`task-304-river-network-lab.md`](./task-notes/task-304-river-network-lab.md)。

## 执行与归档规则

- 已批准的编号任务视为封闭范围；达到最小验收后立即转向下一项，不扩展完成标准。
- 新功能先登记权威任务，再实施并同步开发日志与接手说明。
- 任务完成后按完成日期移入 docs/task-archives/ 对应时间卷，当前文件不保留完成正文。
- 归档默认按每月四个时间片切分，跨月必须新建文件；单卷过大时可继续细分。
- 历史检索、分卷索引和检查命令见 [权威任务归档索引](./task-archives/README.md)。
