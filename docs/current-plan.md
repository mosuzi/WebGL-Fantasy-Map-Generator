# 当前开发计划

本文件是唯一权威任务清单，只保留未完成、进行中或暂缓的任务。已完成任务按时间分卷移入 [权威任务归档](./task-archives/README.md)，不得从 README、开发日志、专题文档或归档中的“下一步”自行恢复为当前任务。

## 当前状态

> **执行门禁（2026-08-29）**：当前未归档任务为第 371 项。当前固定顺序为 `371`。第 53 项已移除，第 278 项已由第 279 项取代，其余既有完成状态见归档索引。

当前 API 基线为：`window.webglGeneratorApi` 覆盖 `18` 个命名空间、`329` 个公开方法和 `179` 个编辑方法，稳定等级为 `320 / 8 / 1`；`329 / 329` 方法可通过 `info.describe` 发现，`analysis` 保留地点解析、距离和方位三项只读入口，`debug` 新增本任务专用的实验性 context-loss 验收桥接，并保留地图模板三项、`grid` 六个受控结构方法、`planner` `10` 个配方、`objects` `20` 类对象及 `cells` 八个读取 / 预检方法。完整能力矩阵为 `1229` 行、`covered 1155 / excluded 74 / deferred 0 / gap 0`；复合语义矩阵保持 `80` 个动作、`70` 个完整事务与 `10` 个玩法配方。

## 权威任务清单

### 第 371 项：回溯并实验验证国界颜色柔化

- **目标**：先在隔离的 `1af5381 / v0.5.4` 需求前基线中复现用户曾看到的存档载入国界颜色过渡，确认其实际渲染路径；再只在 `prototype/boundary-topology-lab/` 中建立拓扑合法的同一行政边界夹具，对比第 370 项九轨羽化、需求前颜色带与至少一种真正连续的朦胧候选。用户确认实验结果之前不得改动正式 `app/webgl-generator/` 渲染实现。
- **阶段**：A. 历史提交与资源回溯；B. 实验室确定性对照、参数控制、行政夹具联动与尖角 / 突变诊断；C. 专项回归和真实浏览器截图。正式产品集成不属于本项当前批准范围。
- **最小验收**：历史效果对应到明确提交和代码路径；固定夹具同时包含锐角、凹角和唯一三方交汇，且国界不得在节点外相交、区域不得自交；国界朦胧与拓扑回归使用互斥页签，国界页的左侧选择必须同时驱动四种候选且切换后保持状态；旧实验室推荐管线回归保持通过；浏览器产出肉眼可区分的全景与近景截图。
- **保护与停止条件**：`app/webgl-generator/**`、地图数据、存档、主题和现有用户偏好均只读；不得把实验室结论冒充正式应用验收。若旧效果不可复现、候选仍有可见尖角 / 裂缝，或需要选择视觉方向，立即冻结并由用户裁定，不进入正式实现。
- **分支与写者**：分支 `codex/task-371-state-border-blend-lab`；主线程为唯一写者，只允许修改权威文档、`prototype/boundary-topology-lab/` 与其专项回归。使用 `run-lean-staged-delivery`，不启用子智能体。



## 执行与归档规则

- 已批准的编号任务视为封闭范围；达到最小验收后立即转向下一项，不扩展完成标准。
- 新功能先登记权威任务，再实施并同步开发日志与接手说明。
- 任务完成后按完成日期移入 docs/task-archives/ 对应时间卷，当前文件不保留完成正文。
- 归档默认按每月四个时间片切分，跨月必须新建文件；单卷过大时可继续细分。
- 历史检索、分卷索引和检查命令见 [权威任务归档索引](./task-archives/README.md)。
