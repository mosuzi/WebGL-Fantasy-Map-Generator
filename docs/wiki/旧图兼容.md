# 旧图兼容

完整地图加载通过版本化迁移与兼容归一化。兼容范围包括浏览器缓存、JSON、gzip / base64、既有地图和旧导出；typed array 会恢复为原类型。

迁移的原则是补结构和默认值，不静默改变地图事实。旧地区类型、外交、河网、湖泊、标签、视觉、锁定等按各自规则归一化；歧义关系明确标为待补充，不从名称强猜。

涉及人口、城市视觉、省会、疆域、地形和派生系统的重评必须显式预览 / 确认，并能够撤销或输出新文件。无头读取只在内存迁移，不覆盖旧文件；建议先保留原文件副本，再用新版导出形成新的 v2 文件。

